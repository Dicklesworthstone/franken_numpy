[toolchain]
# Pinned to match .github/workflows/ci.yml env.RUST_TOOLCHAIN.
# Bump in both files together when updating the workspace nightly.
channel = "nightly-2026-02-20"
components = ["rustfmt", "clippy"]
