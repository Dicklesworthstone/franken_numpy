# Hypothesis Ledger (INTERPRET phase)

Run `2026-06-01_perf_baseline`. Three orthogonal angles
(baseline timing · stage isolation · source read) must agree before a verdict.

| # | Hypothesis | Verdict | Evidence |
|---|-----------|---------|----------|
| H1 | Per-element float-error checking (`note_binary_float_errors`, default errstate = warn) blocks vectorization of the add loop | **REJECTS** | `levelsplit_errstate.txt`: default-warn vs all-ignore ratio = 0.97–1.02× (no difference). Both ~14.5 ms. |
| H2 | The broadcast path misses its fast lane and falls to a per-element index loop | **REJECTS** | `tail_contiguous_broadcast_span([1024],[1024,1024])` → `Some(1024)` and lhs==out, so the tail-contiguous fast path *is* taken; broadcast (14.29 ms) ≈ equal-shape (14.51 ms). The slowness is not broadcast-specific. |
| H3 | The f64 add loop itself is the cost (poor codegen / no SIMD) | **REJECTS** | `raw_handwritten_add_1m` = 0.526 ms; an identical hand loop is 27× faster than `add()`. The loop is not the bottleneck. |
| H4 | Output allocation/zeroing of the 8 MB result dominates | **REJECTS** | `alloc_zero_8mb` = 0.092 ms — negligible. |
| H5 | **Result-array construction dominates**: `from_storage_with_dtype` routes F64 storage through `cast_to(F64)` → `to_f64_vec()`, and `to_f64_vec` rebuilds the `Vec<f64>` via per-element `get_f64(i)` (enum match + bounds + `Result`), ~19 ns/elem | **SUPPORTS** | `construct_from_vec_1m` = 18.90 ms in isolation; `add` ≈ 14.5 ms ≈ construction; `reduce_sum` = 0.70 ms because its output is 1024 elems not 1M. Source: `fnp-dtype/src/lib.rs:1204` (`to_f64_vec`), `:974` (`get_f64`), `fnp-ufunc/src/lib.rs:4764` (`from_storage_with_dtype`, F64 has no move-fast-path). |

## Scaling law

The cost is a **per-element slope (~14–19 ns/elem)**, not a fixed intercept: `add`
on 1M elems ≈ 27 × the 0.5 ms arithmetic, and `reduce_sum` (same 1M *inputs*, 1024
*outputs*) is 21× cheaper — cost tracks output element count through the per-element
`get_f64` accessor. So it scales linearly with output size and dominates at the 1M
fixture; at tiny sizes the dispatch fixed cost would matter more (not measured this pass).

## Hypothesized fix (for the optimizer agents — do NOT apply here)

In `UFuncArray::from_storage_with_dtype` (`fnp-ufunc/src/lib.rs:4764`), add a fast path
for storage whose dtype already equals the target (at least F64): **move the inner
`Vec` straight into `Self::new` / `from_storage`**, skipping `cast_to` + `to_f64_vec`.
Equivalently/additionally, specialize `ArrayStorage::to_f64_vec` (`fnp-dtype:1204`) to
return the inner `Vec<f64>` directly for the `F64` variant (`match self { F64(v) =>
v.clone(), .. }`) instead of the per-element `get_f64` loop. Expected: elementwise
F64 ops drop from ~14.5 ms toward the ~0.5–1 ms memory-bound floor (order-of-magnitude).
Guard with the existing differential/conformance suites + ufunc invariants; one lever,
re-baseline against this artifact.

## Filed beads (perf-labeled)

- **franken_numpy-71n7p** (P1) — H5 root cause: `to_f64_vec`/`from_storage_with_dtype` F64 per-element construction. THE unblock for the optimizer umbrella bead `franken_numpy-perf-current-baseline-hotspot-fbv3z`.
- **franken_numpy-evqs4** (P2) — `matmul` naive 256³ triple loop (2.76 ms); cache-blocking / B-transpose.
- **franken_numpy-g9jvo** (P2) — `fft` Cooley–Tukey 65 536 (2.51 ms); twiddle reuse / iterative in-place.

`sort_quicksort` (4.67 ms) left unfiled — algorithmically expected O(n log n); revisit only after #1.

## Code-first batch attempt ledger

| Date | Bead | Lever | Status | Benchmark guard | Conformance guard | Do not retry unless |
|------|------|-------|--------|-----------------|-------------------|---------------------|
| 2026-06-18 | franken_numpy-ixs5y | Reuse per-worker LU/permutation scratch in `batch_det`/`batch_slogdet` for small stacked matrices (`n < 16`) and write scalar outputs directly instead of allocating a fresh LU bundle and pair vector per lane. | **PENDING BATCH TEST** - compile-only batch by instruction; no performance claim yet. | New Criterion group `batch_det_slogdet/{det,slogdet}` at `8192x4x4` and `2048x8x8`, plus the existing Python-vs-NumPy `fnp_slogdet_f64_batch8192_4x4` gate. | New bitwise guard compares the scratch path against scalar `det_nxn`/`slogdet_nxn` lane references, including singular, NaN, Inf, and negative-zero lanes. | Same-worker Criterion and Python-vs-NumPy evidence show a real win with unchanged guards. Reject and record as negative evidence if the new rows regress or the nonfinite/singular guard fails. |
| 2026-06-18 | franken_numpy-ixs5y | Parallelize flat F64 `boolean_set` masked scalar assignment for large no-sidecar arrays, leaving integer sidecar updates on the serial path. | **PENDING BATCH TEST** - compile-only batch by instruction; no performance claim yet. | New Criterion row `boolean_set_f64_masked` at 100k and 1M elements with a realistic sparse boolean mask and signed-zero scalar fill. | New bitwise SHA-256 guard compares the fast path against a serial reference over NaN, Inf, signed-zero, true-mask, and false-mask lanes. | Same-worker Criterion evidence beats the serial scalar masked fill and the golden guard stays unchanged. Reject and record as negative evidence if the new row regresses or the bitwise guard fails. |
| 2026-06-18 | franken_numpy-ixs5y | Parallelize large PCG64/PCG64DXSM `Generator::random_f32` fills by jump-ahead chunking over the exact NumPy low-then-high u32 half-word stream; fall back when an existing high-half buffer is live. | **PENDING BATCH TEST** - compile-only batch by instruction; no performance claim yet. | Extended Criterion `generator_random/{f32,f64}` through 100000 elements so `f32` crosses the PCG parallel threshold; compare against the original/NumPy random float32 workload in the next batch. | New bitwise guard compares even, odd, and prebuffered large fills against one-at-a-time scalar `random_f32(1)` draws and checks subsequent `next_f32` state. | Same-worker Criterion and Python-vs-NumPy evidence show `random(dtype=float32)` wins with unchanged stream guards. Reject if odd-length half-word buffering diverges, scalar-prebuffer fallback diverges, or the new f32 row regresses. |
| 2026-06-18 | franken_numpy-ixs5y.244 | Parallelize large flat F64 `extract` masked gather with cache-local chunks that preserve chunk-local and global order; sidecars, non-F64 arrays, and small arrays stay serial. | **PENDING BATCH TEST** - compile-only batch by instruction; no performance claim yet. | New Criterion row `extract_f64_masked` at 100k and 1M elements with a realistic sparse boolean mask. | New bitwise SHA-256 guard compares the fast path against a serial reference over NaN truth, signed-zero false masks, infinities, NaN payload lanes, and the all-false empty result. | Same-worker Criterion evidence beats the serial source-index gather with unchanged golden guard. Reject and record as negative evidence if the new row regresses or the bitwise guard fails. |
| 2026-06-18 | franken_numpy-ixs5y | Parallelize PCG64/PCG64DXSM `Generator::standard_exponential_inv` by jump-ahead chunking the fixed one-f64-per-output stream and fusing `-ln1p(-u)` into each cache-hot chunk. | **PENDING BATCH TEST** - compile-only batch by instruction; no performance claim yet. | Extended Criterion `generator_exponential/standard_inv` through 100000 elements so the inverse-CDF workload crosses the PCG parallel threshold; compare against legacy NumPy `standard_exponential(method="inv")` in the next batch. | New bitwise guard compares two large back-to-back parallel fills against one-at-a-time scalar `standard_exponential_inv(1)` draws for PCG64 and PCG64DXSM, then checks subsequent `next_f64` state. | Same-worker Criterion and Python-vs-NumPy evidence show inverse-CDF standard exponential wins with unchanged stream guards. Reject if the post-fill f64 state diverges, the transform row regresses, or NumPy method="inv" conformance exposes stream drift. |
| 2026-06-18 | franken_numpy-ixs5y | Parallelize positive-scale PCG64/PCG64DXSM `Generator::logistic` by jump-ahead chunking the fixed one-f64-per-output stream and fusing the logit transform into each cache-hot chunk; preserve zero-scale as a no-draw path. | **PENDING BATCH TEST** - compile-only batch by instruction; no performance claim yet. | New Criterion `generator_logistic/loc_scale` through 100000 elements so the realistic inverse-CDF logistic workload crosses the PCG parallel threshold; compare against legacy NumPy `Generator.logistic` in the next batch. | New bitwise guard compares two large back-to-back parallel fills against one-at-a-time scalar `logistic(..., size=1)` draws for PCG64 and PCG64DXSM, checks subsequent `next_f64` state, and verifies zero-scale preserves buffered `next_f32` state. | Same-worker Criterion and Python-vs-NumPy evidence show `Generator.logistic` wins with unchanged stream guards. Reject if the post-fill state diverges, zero-scale consumes entropy, or the new logistic row regresses. |
| 2026-06-18 | franken_numpy-ixs5y | Parallelize PCG64/PCG64DXSM `Generator::triangular` by jump-ahead chunking the fixed one-f64-per-output stream and fusing the branchy inverse-CDF transform into each cache-hot chunk; leave rejection-loop distributions such as `gumbel` and `laplace` serial. | **PENDING BATCH TEST** - compile-only batch by instruction; no performance claim yet. | New Criterion `generator_triangular/skewed` through 100000 elements so the realistic skewed triangular workload crosses the PCG parallel threshold; compare against legacy NumPy `Generator.triangular` in the next batch. | New bitwise guard compares two large back-to-back parallel fills against one-at-a-time scalar `triangular(..., size=1)` draws for PCG64 and PCG64DXSM, checks subsequent `next_f64` state, and verifies zero-size calls preserve buffered `next_f32` state. | Same-worker Criterion and Python-vs-NumPy evidence show `Generator.triangular` wins with unchanged stream guards. Reject if post-fill state diverges, zero-size consumes entropy, the branch transform row regresses, or NumPy triangular conformance exposes stream drift. |
| 2026-06-18 | franken_numpy-ixs5y | Parallelize PCG64/PCG64DXSM `Generator::vonmises` near-uniform branch (`kappa < 1e-8`) by jump-ahead chunking its fixed one-f64-per-output angle transform; leave Best-Fisher, large-kappa normal, and rejection-loop branches serial. | **PENDING BATCH TEST** - compile-only batch by instruction; no performance claim yet. | New Criterion `generator_vonmises/near_uniform` through 100000 elements so the uniform-angle workload crosses the PCG parallel threshold; compare against legacy NumPy `Generator.vonmises(..., kappa=0.0)` in the next batch. | New bitwise guard compares two large back-to-back parallel fills against one-at-a-time scalar `vonmises(..., kappa=0.0, size=1)` draws for PCG64 and PCG64DXSM, checks subsequent `next_f64` state, verifies prebuffered `next_f32` discard semantics, and verifies zero-size calls preserve buffered `next_f32` state. | Same-worker Criterion and Python-vs-NumPy evidence show near-uniform `Generator.vonmises` wins with unchanged stream guards. Reject if post-fill state diverges, prebuffered halfword semantics change, zero-size consumes entropy, or the new vonmises row regresses. |
| 2026-06-18 | franken_numpy-ixs5y.245 | Parallelize large flat F64 `flatnonzero` index selection with cache-local chunk filters that preserve ascending C-order indices; sidecars, non-F64 arrays, and small arrays stay serial. | **PENDING BATCH TEST** - compile-only batch by instruction; no performance claim yet. | New Criterion row `flatnonzero_f64_sparse` at 100k and 1M elements with sparse truthy lanes, NaN truth, signed-zero false lanes, and infinities. | New bitwise SHA-256 guard compares the parallel output to a serial flat-index reference over 3,070 indices in a threshold-crossing 16,721-element fixture and verifies empty all-false output. | Same-worker Criterion evidence beats the serial sidecar-building path with unchanged golden guard. Reject and record as negative evidence if the new row regresses or the SHA-256 guard fails. |
| 2026-06-18 | franken_numpy-ixs5y.246 | Parallelize large flat F64 `count_nonzero(axis=None)` by summing cache-local chunk counts over the same `v != 0.0` predicate; sidecars, axis reductions, and small arrays stay serial. Final code raises the activation threshold to `1 << 19` and keeps 4096-element chunks. | **MEASURED KEEP 2026-06-19** - original `1 << 14` threshold rejected for 100k; final narrowed threshold kept. | Criterion row `count_nonzero_flat_f64_sparse` at 100k and 1M elements plus local NumPy timing on the same host. Original threshold was 3.56x slower than NumPy at 100k but 4.17x faster at 1M. Final code was 4.69x faster than NumPy at 100k and 3.48x faster at 1M. | SHA-256 guard passed after updating the threshold-crossing golden digest; `cargo check -p fnp-ufunc` and `cargo clippy -p fnp-ufunc --all-targets -- -D warnings` passed. | Do not restore the `1 << 14` activation threshold unless same-host evidence shows 100k is faster than NumPy and remains inside the prior FNP CI. Reopen only if final 1M same-host median regresses to NumPy median or if the golden guard changes again without an intentional threshold fixture update. |
| 2026-06-18 | franken_numpy-ixs5y.247 | Parallelize large F64 `where_nonzero(condition)` coordinate materialization with cache-local per-dimension chunk gathers and an ordered merge; sidecars, non-F64 arrays, small arrays, and 0d error handling stay on the existing path. | **PENDING BATCH TEST** - compile-only batch by instruction; no performance claim yet. | New Criterion row `where_nonzero_f64_2d_sparse` at 512x512 and 1024x1024 with sparse truthy lanes, NaN truth, signed-zero false lanes, and infinities. | New SHA-256 guard compares the parallel coordinate arrays to a serial row/column reference over 2,889 nonzero coordinates in a threshold-crossing 129x131 fixture and verifies all-false signed-zero output. | Same-worker Criterion evidence beats the serial coordinate push loop with unchanged golden guard. Reject and record as negative evidence if the new row regresses, C-order coordinate ordering changes, or the SHA-256 guard fails. |
| 2026-06-18 | franken_numpy-ixs5y.248 | Parallelize large F64 `argwhere()` interleaved coordinate materialization with cache-local chunk gathers that directly emit `[N, ndim]` rows; sidecars, non-F64 arrays, and small arrays stay serial. | **MEASURED KEEP 2026-06-19** - same-host head-to-head vs NumPy. | Criterion row `argwhere_f64_2d_sparse` at 512x512 and 1024x1024 plus local NumPy timing on the same host. Final code was 3.04x faster than NumPy at 512x512 and 4.79x faster at 1024x1024. | SHA-256 guard passed; `cargo check -p fnp-ufunc` and `cargo clippy -p fnp-ufunc --all-targets -- -D warnings` passed. | Reopen only if a same-host rerun shows NumPy minimum below the FNP Criterion upper CI bound or if the interleaved C-order coordinate golden guard changes. Do not retry this as a standalone patch solely for lower NumPy-CV reruns. |
| 2026-06-18 | franken_numpy-ixs5y.249 | Parallelize large flat F64 `compress(condition, axis=None)` by directly filtering bool-mask/value chunks and preserving ordered flattening; sidecars, non-F64 arrays, short arrays, and axis-specific paths stay serial. | **MEASURED REJECTED 2026-06-19** - production fast path reverted. | Criterion row `compress_f64_bool_flat_sparse` at 100k and 1M elements plus local NumPy timing on the same host. Candidate was 7.15x slower than NumPy at 100k and 2.05x slower at 1M; it also regressed the local Criterion history by +339.69% and +66.84%. | The candidate bitwise guard passed after fixing a `NaN`-unsafe assertion; post-revert `cargo test -p fnp-ufunc compress -- --nocapture` passed 7 tests. | Do not retry this per-chunk `Vec<Vec<f64>>` parallel gather shape as a standalone patch. Retry only if a new design avoids per-chunk selected-vector allocation and beats NumPy on the same-host 100k and 1M rows with CV below 10%. |

Avoided negative-evidence families for this batch: broad SVD row/panel/finalization retreads, packed-GEMM tile retunes, and batch-solve blocking experiments. Prior runs already routed those deeper. The putmask candidate was also not retried because `franken_numpy-ixs5y.243` already landed upstream with the same family of F64 masked flat-cycle fill. Pre-edit inspection also rejected `Generator::standard_cauchy` and `Generator::power` as PCG jump-ahead targets because both currently consume ziggurat normal/exponential paths with variable draw counts, so they cannot preserve scalar stream state with fixed-offset chunking.
