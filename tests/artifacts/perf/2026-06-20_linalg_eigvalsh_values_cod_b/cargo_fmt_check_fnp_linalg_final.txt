Diff in /data/projects/.scratch/franken_numpy-cod-b-bold-verify-20260620-2024/crates/fnp-linalg/benches/batch_eigh.rs:26:
         let base = b * ms;
         for i in 0..n {
             for j in i..n {
-                s ^= s << 13; s ^= s >> 7; s ^= s << 17;
+                s ^= s << 13;
+                s ^= s >> 7;
+                s ^= s << 17;
                 let v = (s >> 11) as f64 / (1u64 << 53) as f64 * 2.0 - 1.0;
                 data[base + i * n + j] = v;
                 data[base + j * n + i] = v;
Diff in /data/projects/.scratch/franken_numpy-cod-b-bold-verify-20260620-2024/crates/fnp-linalg/benches/batch_matrix_rank.rs:17:
     let mut s = 0x2545u64;
     (0..batch * ms)
         .map(|_| {
-            s ^= s << 13; s ^= s >> 7; s ^= s << 17;
+            s ^= s << 13;
+            s ^= s >> 7;
+            s ^= s << 17;
             (s >> 11) as f64 / (1u64 << 53) as f64 * 2.0 - 1.0
         })
         .collect()
Diff in /data/projects/.scratch/franken_numpy-cod-b-bold-verify-20260620-2024/crates/fnp-linalg/benches/batch_solve.rs:18:
 fn make(batch: usize, n: usize) -> (Vec<f64>, Vec<f64>) {
     let ms = n * n;
     let mut s = 0x2545u64;
-    let mut rnd = || { s ^= s << 13; s ^= s >> 7; s ^= s << 17; (s >> 11) as f64 / (1u64 << 53) as f64 * 2.0 - 1.0 };
+    let mut rnd = || {
+        s ^= s << 13;
+        s ^= s >> 7;
+        s ^= s << 17;
+        (s >> 11) as f64 / (1u64 << 53) as f64 * 2.0 - 1.0
+    };
     let mut a = vec![0.0f64; batch * ms];
     // diagonally dominant -> well-conditioned, non-singular
     for k in 0..batch {
Diff in /data/projects/.scratch/franken_numpy-cod-b-bold-verify-20260620-2024/crates/fnp-linalg/benches/batch_solve.rs:25:
         for i in 0..n {
-            for j in 0..n { a[k * ms + i * n + j] = rnd(); }
+            for j in 0..n {
+                a[k * ms + i * n + j] = rnd();
+            }
             a[k * ms + i * n + i] += n as f64 * 2.0;
         }
     }
Diff in /data/projects/.scratch/franken_numpy-cod-b-bold-verify-20260620-2024/crates/fnp-linalg/benches/batch_solve.rs:42:
             bb.iter(|| black_box(old_batch(black_box(&a), black_box(&b), batch, n)))
         });
         g.bench_with_input(BenchmarkId::new("parallel", batch), &batch, |bb, _| {
-            bb.iter(|| black_box(batch_solve(black_box(&a), &a_shape, black_box(&b), &b_shape, true).unwrap()))
+            bb.iter(|| {
+                black_box(
+                    batch_solve(black_box(&a), &a_shape, black_box(&b), &b_shape, true).unwrap(),
+                )
+            })
         });
         g.finish();
     }
Diff in /data/projects/.scratch/franken_numpy-cod-b-bold-verify-20260620-2024/crates/fnp-linalg/benches/criterion_linalg.rs:16:
 use criterion::{BenchmarkId, Criterion, criterion_group, criterion_main};
 use fnp_linalg::{
     batch_cholesky, batch_det, batch_eigvalsh, batch_inv, batch_matrix_norm, batch_slogdet,
-    batch_trace, cholesky_nxn, complex_matmul, cond_nxn, det_nxn, eigvalsh_nxn, inv_nxn,
-    kron_nxn, matrix_norm_frobenius, matrix_norm_nxn, matrix_power_nxn, multi_dot, qr_nxn,
+    batch_trace, cholesky_nxn, complex_matmul, cond_nxn, det_nxn, eigvalsh_nxn, inv_nxn, kron_nxn,
+    matrix_norm_frobenius, matrix_norm_nxn, matrix_power_nxn, multi_dot, qr_nxn,
     sbr_stage1_dense_to_band_lower_nxn, solve_nxn, svd_mxn_full, svd_nxn,
 };
 use std::hint::black_box;
Diff in /data/projects/.scratch/franken_numpy-cod-b-bold-verify-20260620-2024/crates/fnp-linalg/benches/criterion_linalg.rs:526:
     for (batch, n) in [(8192usize, 4usize), (2048, 8)] {
         let (data, shape) = generate_batch_invertible(batch, n);
         let id = format!("{batch}x{n}x{n}");
-        group.bench_with_input(BenchmarkId::new("det", id.clone()), &shape, |bench, shape| {
-            bench.iter(|| {
-                let result = batch_det(black_box(&data), black_box(shape));
-                black_box(result)
-            });
-        });
+        group.bench_with_input(
+            BenchmarkId::new("det", id.clone()),
+            &shape,
+            |bench, shape| {
+                bench.iter(|| {
+                    let result = batch_det(black_box(&data), black_box(shape));
+                    black_box(result)
+                });
+            },
+        );
         group.bench_with_input(BenchmarkId::new("slogdet", id), &shape, |bench, shape| {
             bench.iter(|| {
                 let result = batch_slogdet(black_box(&data), black_box(shape));
Diff in /data/projects/.scratch/franken_numpy-cod-b-bold-verify-20260620-2024/crates/fnp-linalg/benches/criterion_linalg.rs:555:
         let id = format!("{batch}x{m}x{n}");
         group.bench_with_input(BenchmarkId::new("shape", id), &shape, |bench, shape| {
             bench.iter(|| {
-                let result = batch_matrix_norm(black_box(&data), black_box(shape), black_box("fro"));
+                let result =
+                    batch_matrix_norm(black_box(&data), black_box(shape), black_box("fro"));
                 black_box(result)
             });
         });
Diff in /data/projects/.scratch/franken_numpy-cod-b-bold-verify-20260620-2024/crates/fnp-linalg/benches/kron.rs:26:
 fn bench_kron(c: &mut Criterion) {
     // (m, n, p, q) — large outputs where parallelism pays off.
     let cases: &[(usize, usize, usize, usize)] = &[
-        (64, 64, 64, 64),  // 16.7M output
+        (64, 64, 64, 64), // 16.7M output
         (256, 256, 16, 16),
         (16, 16, 256, 256),
     ];
Diff in /data/projects/.scratch/franken_numpy-cod-b-bold-verify-20260620-2024/crates/fnp-linalg/examples/perf_clu.rs:1:
 //! Times complex_solve_nxn (interleaved complex LU). Serial vs parallel via
 //! RAYON_NUM_THREADS=1 vs default.
-use std::time::Instant;
 use fnp_linalg::complex_det_nxn;
+use std::time::Instant;
 fn build(n: usize, seed: u64) -> Vec<f64> {
     let mut s = seed | 1;
-    let mut v: Vec<f64> = (0..2 * n * n).map(|_| {
-        s = s.wrapping_mul(6364136223846793005).wrapping_add(1442695040888963407);
-        ((s >> 11) as f64 / (1u64 << 53) as f64) * 2.0 - 1.0
-    }).collect();
-    for i in 0..n { v[2 * (i * n + i)] += 2.0 * n as f64; }
+    let mut v: Vec<f64> = (0..2 * n * n)
+        .map(|_| {
+            s = s
+                .wrapping_mul(6364136223846793005)
+                .wrapping_add(1442695040888963407);
+            ((s >> 11) as f64 / (1u64 << 53) as f64) * 2.0 - 1.0
+        })
+        .collect();
+    for i in 0..n {
+        v[2 * (i * n + i)] += 2.0 * n as f64;
+    }
     v
 }
-fn median(mut xs: Vec<f64>) -> f64 { xs.sort_by(|a,b| a.partial_cmp(b).unwrap()); xs[xs.len()/2] }
+fn median(mut xs: Vec<f64>) -> f64 {
+    xs.sort_by(|a, b| a.partial_cmp(b).unwrap());
+    xs[xs.len() / 2]
+}
 fn main() {
     println!("threads={}", rayon::current_num_threads());
     for &n in &[256usize, 512, 1024] {
Diff in /data/projects/.scratch/franken_numpy-cod-b-bold-verify-20260620-2024/crates/fnp-linalg/examples/perf_clu.rs:18:
         let a = build(n, 0x1234);
-
+
         let it = if n <= 512 { 7 } else { 4 };
-        let mut ts = Vec::new(); let mut cs = 0u64;
+        let mut ts = Vec::new();
+        let mut cs = 0u64;
         for _ in 0..it {
             let t = Instant::now();
             let (dr, di) = complex_det_nxn(&a, n).unwrap();
Diff in /data/projects/.scratch/franken_numpy-cod-b-bold-verify-20260620-2024/crates/fnp-linalg/examples/perf_eig.rs:1:
+use fnp_linalg::{eig_nxn, eigh_nxn};
 use std::time::Instant;
-use fnp_linalg::{eigh_nxn, eig_nxn};
 fn sym(n: usize, seed: u64) -> Vec<f64> {
     let mut s = seed | 1;
-    let b: Vec<f64> = (0..n*n).map(|_| { s = s.wrapping_mul(6364136223846793005).wrapping_add(1442695040888963407); ((s>>11) as f64/(1u64<<53) as f64)*2.0-1.0 }).collect();
-    let mut a = vec![0.0; n*n];
-    for i in 0..n { for j in 0..n { let mut t=0.0; for k in 0..n { t+=b[i*n+k]*b[j*n+k]; } a[i*n+j]=t; } }
+    let b: Vec<f64> = (0..n * n)
+        .map(|_| {
+            s = s
+                .wrapping_mul(6364136223846793005)
+                .wrapping_add(1442695040888963407);
+            ((s >> 11) as f64 / (1u64 << 53) as f64) * 2.0 - 1.0
+        })
+        .collect();
+    let mut a = vec![0.0; n * n];
+    for i in 0..n {
+        for j in 0..n {
+            let mut t = 0.0;
+            for k in 0..n {
+                t += b[i * n + k] * b[j * n + k];
+            }
+            a[i * n + j] = t;
+        }
+    }
     a
 }
-fn gen_mat(n: usize, seed: u64) -> Vec<f64> { let mut s=seed|1; (0..n*n).map(|_| { s=s.wrapping_mul(6364136223846793005).wrapping_add(1442695040888963407); ((s>>11) as f64/(1u64<<53) as f64)*2.0-1.0 }).collect() }
+fn gen_mat(n: usize, seed: u64) -> Vec<f64> {
+    let mut s = seed | 1;
+    (0..n * n)
+        .map(|_| {
+            s = s
+                .wrapping_mul(6364136223846793005)
+                .wrapping_add(1442695040888963407);
+            ((s >> 11) as f64 / (1u64 << 53) as f64) * 2.0 - 1.0
+        })
+        .collect()
+}
 fn main() {
     for &n in &[512usize, 1024] {
         let a = sym(n, 0x1234);
Diff in /data/projects/.scratch/franken_numpy-cod-b-bold-verify-20260620-2024/crates/fnp-linalg/examples/perf_eig.rs:14:
-        let t = Instant::now(); let _ = eigh_nxn(&a, n).unwrap(); println!("eigh(vectors) n={n} {:.1} ms", t.elapsed().as_secs_f64()*1e3);
+        let t = Instant::now();
+        let _ = eigh_nxn(&a, n).unwrap();
+        println!(
+            "eigh(vectors) n={n} {:.1} ms",
+            t.elapsed().as_secs_f64() * 1e3
+        );
         let g = gen_mat(n, 0x55);
-        let t = Instant::now(); let _ = eig_nxn(&g, n); println!("eig(nonsym) n={n} {:.1} ms", t.elapsed().as_secs_f64()*1e3);
+        let t = Instant::now();
+        let _ = eig_nxn(&g, n);
+        println!(
+            "eig(nonsym) n={n} {:.1} ms",
+            t.elapsed().as_secs_f64() * 1e3
+        );
     }
 }

Diff in /data/projects/.scratch/franken_numpy-cod-b-bold-verify-20260620-2024/crates/fnp-linalg/examples/perf_eigvalsh.rs:1:
-use std::time::Instant;
 use fnp_linalg::eigvalsh_nxn;
+use std::time::Instant;
 fn sym(n: usize, seed: u64) -> Vec<f64> {
     let mut s = seed | 1;
-    let b: Vec<f64> = (0..n*n).map(|_| { s = s.wrapping_mul(6364136223846793005).wrapping_add(1442695040888963407); ((s>>11) as f64/(1u64<<53) as f64)*2.0-1.0 }).collect();
-    let mut a = vec![0.0; n*n];
-    for i in 0..n { for j in 0..n { let mut t=0.0; for k in 0..n { t+=b[i*n+k]*b[j*n+k]; } a[i*n+j]=t; } }
+    let b: Vec<f64> = (0..n * n)
+        .map(|_| {
+            s = s
+                .wrapping_mul(6364136223846793005)
+                .wrapping_add(1442695040888963407);
+            ((s >> 11) as f64 / (1u64 << 53) as f64) * 2.0 - 1.0
+        })
+        .collect();
+    let mut a = vec![0.0; n * n];
+    for i in 0..n {
+        for j in 0..n {
+            let mut t = 0.0;
+            for k in 0..n {
+                t += b[i * n + k] * b[j * n + k];
+            }
+            a[i * n + j] = t;
+        }
+    }
     a
 }
 fn main() {
Diff in /data/projects/.scratch/franken_numpy-cod-b-bold-verify-20260620-2024/crates/fnp-linalg/examples/perf_eigvalsh.rs:12:
         let a = sym(n, 0x1234);
         let t = Instant::now();
         let _ev = eigvalsh_nxn(&a, n).unwrap();
-        println!("eigvalsh n={n} {:.1} ms", t.elapsed().as_secs_f64()*1e3);
+        println!("eigvalsh n={n} {:.1} ms", t.elapsed().as_secs_f64() * 1e3);
     }
 }

Diff in /data/projects/.scratch/franken_numpy-cod-b-bold-verify-20260620-2024/crates/fnp-linalg/examples/perf_misc.rs:1:
-use std::time::Instant;
 use fnp_linalg::matrix_power_nxn;
-fn gm(n: usize, seed: u64) -> Vec<f64> { let mut s=seed|1; (0..n*n).map(|_| { s=s.wrapping_mul(6364136223846793005).wrapping_add(1442695040888963407); ((s>>11) as f64/(1u64<<53) as f64)*0.1 }).collect() }
-fn med(mut xs: Vec<f64>) -> f64 { xs.sort_by(|a,b| a.partial_cmp(b).unwrap()); xs[xs.len()/2] }
+use std::time::Instant;
+fn gm(n: usize, seed: u64) -> Vec<f64> {
+    let mut s = seed | 1;
+    (0..n * n)
+        .map(|_| {
+            s = s
+                .wrapping_mul(6364136223846793005)
+                .wrapping_add(1442695040888963407);
+            ((s >> 11) as f64 / (1u64 << 53) as f64) * 0.1
+        })
+        .collect()
+}
+fn med(mut xs: Vec<f64>) -> f64 {
+    xs.sort_by(|a, b| a.partial_cmp(b).unwrap());
+    xs[xs.len() / 2]
+}
 fn main() {
     for &n in &[512usize] {
-        let mut a = gm(n, 1); for i in 0..n { a[i*n+i] += 1.0; }
+        let mut a = gm(n, 1);
+        for i in 0..n {
+            a[i * n + i] += 1.0;
+        }
         // warmup
         let _ = matrix_power_nxn(&a, n, 2);
         for &p in &[2i64, 5, 16] {
Diff in /data/projects/.scratch/franken_numpy-cod-b-bold-verify-20260620-2024/crates/fnp-linalg/examples/perf_misc.rs:11:
             let mut ts = Vec::new();
-            for _ in 0..4 { let t = Instant::now(); let _ = matrix_power_nxn(&a, n, p); ts.push(t.elapsed().as_secs_f64()*1e3); }
+            for _ in 0..4 {
+                let t = Instant::now();
+                let _ = matrix_power_nxn(&a, n, p);
+                ts.push(t.elapsed().as_secs_f64() * 1e3);
+            }
             println!("matrix_power^{p} n={n} {:.1} ms (warmed median)", med(ts));
         }
     }
Diff in /data/projects/.scratch/franken_numpy-cod-b-bold-verify-20260620-2024/crates/fnp-linalg/examples/perf_qr.rs:35:
             let t = Instant::now();
             let (q, r) = qr_nxn(&a, n).unwrap();
             times.push(t.elapsed().as_secs_f64() * 1e3);
-            checksum = q.iter().chain(r.iter()).fold(0xcbf29ce484222325u64, |h, x| {
-                x.to_bits()
-                    .to_le_bytes()
-                    .iter()
-                    .fold(h, |h, &b| (h ^ b as u64).wrapping_mul(0x100000001b3))
-            });
+            checksum = q
+                .iter()
+                .chain(r.iter())
+                .fold(0xcbf29ce484222325u64, |h, x| {
+                    x.to_bits()
+                        .to_le_bytes()
+                        .iter()
+                        .fold(h, |h, &b| (h ^ b as u64).wrapping_mul(0x100000001b3))
+                });
         }
         let ms = median(times);
         println!("n={n:5} {ms:9.3} ms  checksum=0x{checksum:016x}");
Diff in /data/projects/.scratch/franken_numpy-cod-b-bold-verify-20260620-2024/crates/fnp-linalg/examples/perf_svd.rs:1:
-use std::time::Instant;
 use fnp_linalg::{svd_mxn, svd_mxn_full};
-fn gen_m(n: usize, seed: u64) -> Vec<f64> { let mut s=seed|1; (0..n*n).map(|_| { s=s.wrapping_mul(6364136223846793005).wrapping_add(1442695040888963407); ((s>>11) as f64/(1u64<<53) as f64)*2.0-1.0 }).collect() }
+use std::time::Instant;
+fn gen_m(n: usize, seed: u64) -> Vec<f64> {
+    let mut s = seed | 1;
+    (0..n * n)
+        .map(|_| {
+            s = s
+                .wrapping_mul(6364136223846793005)
+                .wrapping_add(1442695040888963407);
+            ((s >> 11) as f64 / (1u64 << 53) as f64) * 2.0 - 1.0
+        })
+        .collect()
+}
 fn main() {
     for &n in &[256usize, 512] {
         let a = gen_m(n, 0x1234);
Diff in /data/projects/.scratch/franken_numpy-cod-b-bold-verify-20260620-2024/crates/fnp-linalg/examples/perf_svd.rs:7:
-        let t = Instant::now(); let _ = svd_mxn(&a, n, n).unwrap(); println!("svd_values n={n} {:.1} ms", t.elapsed().as_secs_f64()*1e3);
-        let t = Instant::now(); let _ = svd_mxn_full(&a, n, n).unwrap(); println!("svd_FULL   n={n} {:.1} ms", t.elapsed().as_secs_f64()*1e3);
+        let t = Instant::now();
+        let _ = svd_mxn(&a, n, n).unwrap();
+        println!("svd_values n={n} {:.1} ms", t.elapsed().as_secs_f64() * 1e3);
+        let t = Instant::now();
+        let _ = svd_mxn_full(&a, n, n).unwrap();
+        println!("svd_FULL   n={n} {:.1} ms", t.elapsed().as_secs_f64() * 1e3);
     }
 }

Diff in /data/projects/.scratch/franken_numpy-cod-b-bold-verify-20260620-2024/crates/fnp-linalg/src/lib.rs:2525:

         let slot = self.len;
         self.taus[slot] = tau;
-        for (row, &value) in w_house
-            .iter()
-            .enumerate()
-            .take(n)
-            .skip(reflector_col + 1)
-        {
+        for (row, &value) in w_house.iter().enumerate().take(n).skip(reflector_col + 1) {
             self.v[(row - self.start) * SVD_RIGHT_VT_PANEL_NB + slot] = value;
         }
         self.len += 1;
Diff in /data/projects/.scratch/franken_numpy-cod-b-bold-verify-20260620-2024/crates/fnp-linalg/src/lib.rs:2966:

 #[inline]
 fn sort_singular_values_descending_in_place(values: &mut [f64]) {
-    values.sort_by(|a, b| {
-        b.partial_cmp(a)
-            .unwrap_or(std::cmp::Ordering::Equal)
-    });
+    values.sort_by(|a, b| b.partial_cmp(a).unwrap_or(std::cmp::Ordering::Equal));
 }

 /// Core QR-phase SVD implementation for the bidiagonalized matrix.
Diff in /data/projects/.scratch/franken_numpy-cod-b-bold-verify-20260620-2024/crates/fnp-linalg/src/lib.rs:3979:
             // scan rows once while preserving the per-column addition order.
             Ok(matrix_norm_column_sum(a, m, n, false))
         }
-        "-1" => {
-            Ok(matrix_norm_column_sum(a, m, n, true))
-        }
-        "inf" => {
-            Ok(matrix_norm_row_sum(a, n, false))
-        }
-        "-inf" => {
-            Ok(matrix_norm_row_sum(a, n, true))
-        }
+        "-1" => Ok(matrix_norm_column_sum(a, m, n, true)),
+        "inf" => Ok(matrix_norm_row_sum(a, n, false)),
+        "-inf" => Ok(matrix_norm_row_sum(a, n, true)),
         "2" => {
             // Spectral norm = largest singular value (works on MxN)
             if let Some(value) = matrix_norm_spectral_precheck(a)? {
Diff in /data/projects/.scratch/franken_numpy-cod-b-bold-verify-20260620-2024/crates/fnp-linalg/src/lib.rs:4169:
 // a large matrix also stay serial.
 const TRIDIAG_MATVEC_PAR_MIN: usize = 1024;

-fn tridiag_symmetric_matvec_serial(
-    work: &[f64],
-    n: usize,
-    start: usize,
-    v: &[f64],
-    u: &mut [f64],
-) {
+fn tridiag_symmetric_matvec_serial(work: &[f64], n: usize, start: usize, v: &[f64], u: &mut [f64]) {
     debug_assert_eq!(work.len(), n * n);
     debug_assert_eq!(v.len(), n);
     debug_assert_eq!(u.len(), n);
Diff in /data/projects/.scratch/franken_numpy-cod-b-bold-verify-20260620-2024/crates/fnp-linalg/src/lib.rs:4244:
 // trailing-matrix memory traffic of the unblocked per-column left+right sweep
 // (which is DRAM-bound). Same reflectors as the unblocked path → tolerance
 // equivalent (reassociated updates; never bit-exact). Returns (d, e).
-fn tridiag_reduce_blocked(a: &[f64], n: usize, accumulate_q: bool) -> (Vec<f64>, Vec<f64>, Vec<f64>) {
+fn tridiag_reduce_blocked(
+    a: &[f64],
+    n: usize,
+    accumulate_q: bool,
+) -> (Vec<f64>, Vec<f64>, Vec<f64>) {
     let mut work = a.to_vec();
     let mut d = vec![0.0f64; n];
     let mut e = vec![0.0f64; n - 1];
Diff in /data/projects/.scratch/franken_numpy-cod-b-bold-verify-20260620-2024/crates/fnp-linalg/src/lib.rs:4664:
             for row in t..h {
                 let vi = vv[row * nb + t];
                 let src = row * nb + t;
-                for (dj, &cell) in dpanel[..width].iter_mut().zip(panel[src..src + width].iter()) {
+                for (dj, &cell) in dpanel[..width]
+                    .iter_mut()
+                    .zip(panel[src..src + width].iter())
+                {
                     *dj += vi * cell;
                 }
             }
Diff in /data/projects/.scratch/franken_numpy-cod-b-bold-verify-20260620-2024/crates/fnp-linalg/src/lib.rs:4674:
             for row in t..h {
                 let vi = vv[row * nb + t];
                 let dst = row * nb + t;
-                for (cell, &dj) in panel[dst..dst + width].iter_mut().zip(dpanel[..width].iter()) {
+                for (cell, &dj) in panel[dst..dst + width]
+                    .iter_mut()
+                    .zip(dpanel[..width].iter())
+                {
                     *cell -= dj * vi;
                 }
             }
Diff in /data/projects/.scratch/franken_numpy-cod-b-bold-verify-20260620-2024/crates/fnp-linalg/src/lib.rs:5369:
             // Exceptional shift to break a rare cycle: complex shifts of magnitude
             // ~the local subdiagonal scale.
             let sa = h[m * n + (m - 1)].abs()
-                + if m >= lo + 2 { h[(m - 1) * n + (m - 2)].abs() } else { 0.0 };
+                + if m >= lo + 2 {
+                    h[(m - 1) * n + (m - 2)].abs()
+                } else {
+                    0.0
+                };
             (1.5 * sa, sa * sa)
         } else {
             let tr = h[(p - 2) * n + (p - 2)] + h[(p - 1) * n + (p - 1)];
Diff in /data/projects/.scratch/franken_numpy-cod-b-bold-verify-20260620-2024/crates/fnp-linalg/src/lib.rs:5385:
         let h01 = h[lo * n + (lo + 1)];
         let mut x = h00 * h00 + h01 * h10 - s * h00 + t;
         let mut y = h10 * (h00 + h11 - s);
-        let mut zz = if lo + 2 <= m { h10 * h[(lo + 2) * n + (lo + 1)] } else { 0.0 };
+        let mut zz = if lo + 2 <= m {
+            h10 * h[(lo + 2) * n + (lo + 1)]
+        } else {
+            0.0
+        };

         let mut k = lo;
         while k <= p - 2 {
Diff in /data/projects/.scratch/franken_numpy-cod-b-bold-verify-20260620-2024/crates/fnp-linalg/src/lib.rs:6037:
     true
 }

-fn symmetric_cond_from_eigvalsh(
-    a: &[f64],
-    n: usize,
-    reciprocal: bool,
-) -> Result<f64, LinAlgError> {
+fn symmetric_cond_from_eigvalsh(a: &[f64], n: usize, reciprocal: bool) -> Result<f64, LinAlgError> {
     let eigvals = eigvalsh_nxn(a, n)?;
     let mut sigma_min = f64::INFINITY;
     let mut sigma_max = 0.0f64;
Diff in /data/projects/.scratch/franken_numpy-cod-b-bold-verify-20260620-2024/crates/fnp-linalg/src/lib.rs:8514:
     }
     let resolved_rcond = resolve_pinv_tolerance_aliases(rcond, rtol)?;
     let lanes = batch_map_lanes(batch, mat_size, |b| {
-        pinv_mxn(&data[b * mat_size..(b + 1) * mat_size], m, n, resolved_rcond)
+        pinv_mxn(
+            &data[b * mat_size..(b + 1) * mat_size],
+            m,
+            n,
+            resolved_rcond,
+        )
     })?;
     let mut result = Vec::with_capacity(batch * n * m);
     for lane in &lanes {
Diff in /data/projects/.scratch/franken_numpy-cod-b-bold-verify-20260620-2024/crates/fnp-linalg/src/lib.rs:8565:
         if batch_should_parallelize(batch, mat_size) {
             use std::sync::Mutex;
             let first_err: Mutex<Option<(usize, LinAlgError)>> = Mutex::new(None);
-            result.par_chunks_mut(mat_size).enumerate().for_each(|(idx, out_chunk)| {
-                let a_sub = &data[idx * mat_size..(idx + 1) * mat_size];
-                if let Err(e) = cholesky_nxn_into_out(a_sub, n, out_chunk) {
-                    let mut slot = first_err.lock().unwrap();
-                    let replace = match slot.as_ref() {
-                        None => true,
-                        Some((i, _)) => idx < *i,
-                    };
-                    if replace {
-                        *slot = Some((idx, e));
+            result
+                .par_chunks_mut(mat_size)
+                .enumerate()
+                .for_each(|(idx, out_chunk)| {
+                    let a_sub = &data[idx * mat_size..(idx + 1) * mat_size];
+                    if let Err(e) = cholesky_nxn_into_out(a_sub, n, out_chunk) {
+                        let mut slot = first_err.lock().unwrap();
+                        let replace = match slot.as_ref() {
+                            None => true,
+                            Some((i, _)) => idx < *i,
+                        };
+                        if replace {
+                            *slot = Some((idx, e));
+                        }
                     }
-                }
-            });
+                });
             if let Some((_, e)) = first_err.into_inner().unwrap() {
                 return Err(e);
             }
Diff in /data/projects/.scratch/franken_numpy-cod-b-bold-verify-20260620-2024/crates/fnp-linalg/src/lib.rs:9411:
         }
         assert_eq!(parallel.len(), serial.len());
         for (i, (p, s)) in parallel.iter().zip(&serial).enumerate() {
-            assert_eq!(p.to_bits(), s.to_bits(), "lane-flattened index {i} diverged");
+            assert_eq!(
+                p.to_bits(),
+                s.to_bits(),
+                "lane-flattened index {i} diverged"
+            );
         }
     }

Diff in /data/projects/.scratch/franken_numpy-cod-b-bold-verify-20260620-2024/crates/fnp-linalg/src/lib.rs:9847:
             super::packed_gemm_sub_assign_strided(&a, &b, m, k, n, row_stride, &mut fused)
         });
         assert_eq!(fused.len(), materialized.len());
-        for (idx, (fused_value, materialized_value)) in
-            fused.iter().zip(&materialized).enumerate()
+        for (idx, (fused_value, materialized_value)) in fused.iter().zip(&materialized).enumerate()
         {
             assert_eq!(
                 fused_value.to_bits(),
Diff in /data/projects/.scratch/franken_numpy-cod-b-bold-verify-20260620-2024/crates/fnp-linalg/src/lib.rs:11308:
                 .map(|i| {
                     let cell = i % ms;
                     let (r, c) = (cell / n, cell % n);
-                    if r == c { n as f64 + 2.0 } else { ((i % 11) as f64 - 5.0) * 0.1 }
+                    if r == c {
+                        n as f64 + 2.0
+                    } else {
+                        ((i % 11) as f64 - 5.0) * 0.1
+                    }
                 })
                 .collect();
             let b: Vec<f64> = (0..batch * n).map(|i| (i % 17) as f64 - 8.0).collect();
Diff in /data/projects/.scratch/franken_numpy-cod-b-bold-verify-20260620-2024/crates/fnp-linalg/src/lib.rs:11319:
             let old = || -> usize {
                 let lanes: Vec<Vec<f64>> = (0..batch)
                     .into_par_iter()
-                    .map(|i| super::solve_nxn(&a[i * ms..(i + 1) * ms], &b[i * n..(i + 1) * n], n).unwrap())
+                    .map(|i| {
+                        super::solve_nxn(&a[i * ms..(i + 1) * ms], &b[i * n..(i + 1) * n], n)
+                            .unwrap()
+                    })
                     .collect();
                 let mut out = Vec::with_capacity(batch * n);
                 for x in &lanes {
Diff in /data/projects/.scratch/franken_numpy-cod-b-bold-verify-20260620-2024/crates/fnp-linalg/src/lib.rs:11329:
             };
             // NEW: batch_solve scratch fast path (reused lu/perm per thread).
             let new = || -> usize {
-                super::batch_solve(&a, &a_shape, &b, &b_shape, true).unwrap().len()
+                super::batch_solve(&a, &a_shape, &b, &b_shape, true)
+                    .unwrap()
+                    .len()
             };
             let _ = old();
             let _ = new();
Diff in /data/projects/.scratch/franken_numpy-cod-b-bold-verify-20260620-2024/crates/fnp-linalg/src/lib.rs:11375:
             // Independent per-lane reference via the unchanged solve_nxn.
             let mut reference = Vec::with_capacity(batch * n);
             for lane in 0..batch {
-                let x = super::solve_nxn(&a[lane * ms..(lane + 1) * ms], &b[lane * n..(lane + 1) * n], n)
-                    .expect("solve_nxn");
+                let x = super::solve_nxn(
+                    &a[lane * ms..(lane + 1) * ms],
+                    &b[lane * n..(lane + 1) * n],
+                    n,
+                )
+                .expect("solve_nxn");
                 reference.extend_from_slice(&x);
             }
             assert_eq!(got.len(), reference.len());
Diff in /data/projects/.scratch/franken_numpy-cod-b-bold-verify-20260620-2024/crates/fnp-linalg/src/lib.rs:11383:
             for (i, (g, r)) in got.iter().zip(&reference).enumerate() {
-                assert_eq!(g.to_bits(), r.to_bits(), "n={n} lane-flat index {i} diverged");
+                assert_eq!(
+                    g.to_bits(),
+                    r.to_bits(),
+                    "n={n} lane-flat index {i} diverged"
+                );
             }
         }
     }
Diff in /data/projects/.scratch/franken_numpy-cod-b-bold-verify-20260620-2024/crates/fnp-linalg/src/lib.rs:11441:
                 .map(|i| {
                     let cell = i % ms;
                     let (r, c) = (cell / n, cell % n);
-                    if r == c { n as f64 + 2.0 } else { ((i % 11) as f64 - 5.0) * 0.1 }
+                    if r == c {
+                        n as f64 + 2.0
+                    } else {
+                        ((i % 11) as f64 - 5.0) * 0.1
+                    }
                 })
                 .collect();
             let b: Vec<f64> = (0..batch * rw).map(|i| (i % 19) as f64 - 9.0).collect();
Diff in /data/projects/.scratch/franken_numpy-cod-b-bold-verify-20260620-2024/crates/fnp-linalg/src/lib.rs:11451:
                 let lanes: Vec<Vec<f64>> = (0..batch)
                     .into_par_iter()
                     .map(|i| {
-                        super::solve_nxn_multi(&a[i * ms..(i + 1) * ms], &b[i * rw..(i + 1) * rw], n, m)
-                            .unwrap()
+                        super::solve_nxn_multi(
+                            &a[i * ms..(i + 1) * ms],
+                            &b[i * rw..(i + 1) * rw],
+                            n,
+                            m,
+                        )
+                        .unwrap()
                     })
                     .collect();
                 let mut out = Vec::with_capacity(batch * rw);
Diff in /data/projects/.scratch/franken_numpy-cod-b-bold-verify-20260620-2024/crates/fnp-linalg/src/lib.rs:11462:
                 out.len()
             };
             let new = || -> usize {
-                super::batch_solve(&a, &a_shape, &b, &b_shape, false).unwrap().len()
+                super::batch_solve(&a, &a_shape, &b, &b_shape, false)
+                    .unwrap()
+                    .len()
             };
             let _ = old();
             let _ = new();
Diff in /data/projects/.scratch/franken_numpy-cod-b-bold-verify-20260620-2024/crates/fnp-linalg/src/lib.rs:11584:
             let got = super::batch_cholesky(&a, &shape).expect("batch_cholesky");
             let mut reference = Vec::with_capacity(batch * ms);
             for lane in 0..batch {
-                let l = super::cholesky_nxn(&a[lane * ms..(lane + 1) * ms], n).expect("cholesky_nxn");
+                let l =
+                    super::cholesky_nxn(&a[lane * ms..(lane + 1) * ms], n).expect("cholesky_nxn");
                 reference.extend_from_slice(&l);
             }
             assert_eq!(got.len(), reference.len());
Diff in /data/projects/.scratch/franken_numpy-cod-b-bold-verify-20260620-2024/crates/fnp-linalg/src/lib.rs:11654:
                 .map(|i| {
                     let cell = i % ms;
                     let (r, c) = (cell / n, cell % n);
-                    if r == c { n as f64 + 2.0 } else { ((i % 11) as f64 - 5.0) * 0.1 }
+                    if r == c {
+                        n as f64 + 2.0
+                    } else {
+                        ((i % 11) as f64 - 5.0) * 0.1
+                    }
                 })
                 .collect();
             let shape = [batch, n, n];
Diff in /data/projects/.scratch/franken_numpy-cod-b-bold-verify-20260620-2024/crates/fnp-linalg/src/lib.rs:11785:
         let mut a = vec![0.0f64; n * n];
         let mut state = 0x00c0_ffee_1234_5678_u64;
         for v in a.iter_mut() {
-            state = state.wrapping_mul(6364136223846793005).wrapping_add(1442695040888963407);
+            state = state
+                .wrapping_mul(6364136223846793005)
+                .wrapping_add(1442695040888963407);
             *v = ((state >> 11) as f64 / (1u64 << 53) as f64) - 0.5;
         }
         for i in 0..n {
Diff in /data/projects/.scratch/franken_numpy-cod-b-bold-verify-20260620-2024/crates/fnp-linalg/src/lib.rs:13451:
                 d[hi] - e[hi - 1].abs()
             } else {
                 let sign = if delta >= 0.0 { 1.0 } else { -1.0 };
-                d[hi] - e[hi - 1] * e[hi - 1] / (delta + sign * (delta * delta + e[hi - 1] * e[hi - 1]).sqrt())
+                d[hi]
+                    - e[hi - 1] * e[hi - 1]
+                        / (delta + sign * (delta * delta + e[hi - 1] * e[hi - 1]).sqrt())
             };
             let mut x = d[lo] - shift;
             let mut z = e[lo];
Diff in /data/projects/.scratch/franken_numpy-cod-b-bold-verify-20260620-2024/crates/fnp-linalg/src/lib.rs:13516:
                 assert!(maxq < 1e-12, "Q mismatch {maxq:e}");
             }
             let (o, nn) = (med(to), med(tn));
-            println!("n={n:5} stride-n={o:9.2}ms transposed={nn:9.2}ms speedup={:.2}x", o / nn);
+            println!(
+                "n={n:5} stride-n={o:9.2}ms transposed={nn:9.2}ms speedup={:.2}x",
+                o / nn
+            );
         }
     }

Diff in /data/projects/.scratch/franken_numpy-cod-b-bold-verify-20260620-2024/crates/fnp-linalg/src/lib.rs:13617:
                 let sd = disc.sqrt();
                 let l1 = (trace + sd) / 2.0;
                 let l2 = (trace - sd) / 2.0;
-                if (l1 - a22).abs() < (l2 - a22).abs() { l1 } else { l2 }
+                if (l1 - a22).abs() < (l2 - a22).abs() {
+                    l1
+                } else {
+                    l2
+                }
             } else if iter % 10 == 0 {
                 a22 + h[(p - 1) * n + (p - 2)].abs()
             } else {
Diff in /data/projects/.scratch/franken_numpy-cod-b-bold-verify-20260620-2024/crates/fnp-linalg/src/lib.rs:13630:
                 let ff = h[k * n + k];
                 let gg = h[(k + 1) * n + k];
                 let r = ff.hypot(gg);
-                let (c, s) = if r > 0.0 { (ff / r, gg / r) } else { (1.0, 0.0) };
+                let (c, s) = if r > 0.0 {
+                    (ff / r, gg / r)
+                } else {
+                    (1.0, 0.0)
+                };
                 cos_vals[k] = c;
                 sin_vals[k] = s;
                 for j in k..n {
Diff in /data/projects/.scratch/franken_numpy-cod-b-bold-verify-20260620-2024/crates/fnp-linalg/src/lib.rs:13663:
             let mut s = seed | 1;
             (0..n * n)
                 .map(|_| {
-                    s = s.wrapping_mul(6364136223846793005).wrapping_add(1442695040888963407);
+                    s = s
+                        .wrapping_mul(6364136223846793005)
+                        .wrapping_add(1442695040888963407);
                     ((s >> 11) as f64 / (1u64 << 53) as f64) * 2.0 - 1.0
                 })
                 .collect()
Diff in /data/projects/.scratch/franken_numpy-cod-b-bold-verify-20260620-2024/crates/fnp-linalg/src/lib.rs:13686:
             let mut ed = super::extract_schur_eigenvalues(&hd, n);
             es.sort_by(|a, b| a.partial_cmp(b).unwrap());
             ed.sort_by(|a, b| a.partial_cmp(b).unwrap());
-            let maxd = es.iter().zip(&ed).map(|(a, b)| (a - b).abs()).fold(0.0f64, f64::max);
+            let maxd = es
+                .iter()
+                .zip(&ed)
+                .map(|(a, b)| (a - b).abs())
+                .fold(0.0f64, f64::max);
             assert!(maxd < 1e-6, "eigenvalue mismatch {maxd:e} (n={n})");
             for _ in 0..it {
                 let mut h1 = h0.clone();
Diff in /data/projects/.scratch/franken_numpy-cod-b-bold-verify-20260620-2024/crates/fnp-linalg/src/lib.rs:13698:
                 super::hessenberg_qr_iter(&mut h2, None, n);
                 tn.push(t.elapsed().as_secs_f64() * 1e3);
             }
-            println!("n={n:5} single-shift={:9.2}ms double-shift={:9.2}ms speedup={:.2}x", med(to.clone()), med(tn.clone()), med(to) / med(tn));
+            println!(
+                "n={n:5} single-shift={:9.2}ms double-shift={:9.2}ms speedup={:.2}x",
+                med(to.clone()),
+                med(tn.clone()),
+                med(to) / med(tn)
+            );
         }
     }

Diff in /data/projects/.scratch/franken_numpy-cod-b-bold-verify-20260620-2024/crates/fnp-linalg/src/lib.rs:13777:
             let mut s = seed | 1;
             (0..n * n)
                 .map(|_| {
-                    s = s.wrapping_mul(6364136223846793005).wrapping_add(1442695040888963407);
+                    s = s
+                        .wrapping_mul(6364136223846793005)
+                        .wrapping_add(1442695040888963407);
                     ((s >> 11) as f64 / (1u64 << 53) as f64) * 2.0 - 1.0
                 })
                 .collect()
Diff in /data/projects/.scratch/franken_numpy-cod-b-bold-verify-20260620-2024/crates/fnp-linalg/src/lib.rs:13787:
             // bit-exactness of the new contiguous left apply vs the stride-n ref.
             let hs = hessenberg_reduce_stridn_ref(&a, n);
             let (hn, _q) = super::hessenberg_reduce(&a, n);
-            let maxd = hs.iter().zip(&hn).map(|(a, b)| (a - b).abs()).fold(0.0f64, f64::max);
+            let maxd = hs
+                .iter()
+                .zip(&hn)
+                .map(|(a, b)| (a - b).abs())
+                .fold(0.0f64, f64::max);
             assert!(maxd == 0.0, "Hessenberg not bit-exact: {maxd:e} (n={n})");
             let med = |mut xs: Vec<f64>| {
                 xs.sort_by(|a, b| a.partial_cmp(b).unwrap());
Diff in /data/projects/.scratch/franken_numpy-cod-b-bold-verify-20260620-2024/crates/fnp-linalg/src/lib.rs:13805:
                 std::hint::black_box(&r);
                 tn.push(t.elapsed().as_secs_f64() * 1e3);
             }
-            println!("n={n:5} stride-n={:9.2}ms contiguous={:9.2}ms speedup={:.2}x", med(to.clone()), med(tn.clone()), med(to) / med(tn));
+            println!(
+                "n={n:5} stride-n={:9.2}ms contiguous={:9.2}ms speedup={:.2}x",
+                med(to.clone()),
+                med(tn.clone()),
+                med(to) / med(tn)
+            );
         }
     }

Diff in /data/projects/.scratch/franken_numpy-cod-b-bold-verify-20260620-2024/crates/fnp-linalg/src/lib.rs:13816:
         let threads = rayon::current_num_threads();
         if m >= 128 && k >= 128 && n >= 128 && threads >= 2 {
             let band_rows = m.div_ceil(threads * 4).max(1);
-            c.par_chunks_mut(band_rows * n).enumerate().for_each(|(bi, c_band)| {
-                let row_start = bi * band_rows;
-                let rows = c_band.len() / n;
-                let a_band = &a[row_start * k..row_start * k + rows * k];
-                super::packed_gemm_serial(a_band, b, rows, k, n, c_band);
-            });
+            c.par_chunks_mut(band_rows * n)
+                .enumerate()
+                .for_each(|(bi, c_band)| {
+                    let row_start = bi * band_rows;
+                    let rows = c_band.len() / n;
+                    let a_band = &a[row_start * k..row_start * k + rows * k];
+                    super::packed_gemm_serial(a_band, b, rows, k, n, c_band);
+                });
         } else {
             super::packed_gemm_serial(a, b, m, k, n, &mut c);
         }
Diff in /data/projects/.scratch/franken_numpy-cod-b-bold-verify-20260620-2024/crates/fnp-linalg/src/lib.rs:13836:
             let mut s = seed | 1;
             (0..rows * cols)
                 .map(|_| {
-                    s = s.wrapping_mul(6364136223846793005).wrapping_add(1442695040888963407);
+                    s = s
+                        .wrapping_mul(6364136223846793005)
+                        .wrapping_add(1442695040888963407);
                     ((s >> 11) as f64 / (1u64 << 53) as f64) * 2.0 - 1.0
                 })
                 .collect()
Diff in /data/projects/.scratch/franken_numpy-cod-b-bold-verify-20260620-2024/crates/fnp-linalg/src/lib.rs:13847:
             // bit-exactness of aligned vs misaligned (same serial kernel).
             let ca = super::packed_gemm(&a, &b, n, n, n);
             let cm = packed_gemm_misaligned(&a, &b, n, n, n);
-            let maxd = ca.iter().zip(&cm).map(|(x, y)| (x - y).abs()).fold(0.0f64, f64::max);
-            assert!(maxd == 0.0, "GEMM not bit-exact across band split: {maxd:e}");
+            let maxd = ca
+                .iter()
+                .zip(&cm)
+                .map(|(x, y)| (x - y).abs())
+                .fold(0.0f64, f64::max);
+            assert!(
+                maxd == 0.0,
+                "GEMM not bit-exact across band split: {maxd:e}"
+            );
             let med = |mut xs: Vec<f64>| {
                 xs.sort_by(|a, b| a.partial_cmp(b).unwrap());
                 xs[xs.len() / 2]
Diff in /data/projects/.scratch/franken_numpy-cod-b-bold-verify-20260620-2024/crates/fnp-linalg/src/lib.rs:13865:
                 std::hint::black_box(&r);
                 tn.push(t.elapsed().as_secs_f64() * 1e3);
             }
-            println!("n={n:5} misaligned={:9.2}ms aligned={:9.2}ms speedup={:.2}x", med(to.clone()), med(tn.clone()), med(to) / med(tn));
+            println!(
+                "n={n:5} misaligned={:9.2}ms aligned={:9.2}ms speedup={:.2}x",
+                med(to.clone()),
+                med(tn.clone()),
+                med(to) / med(tn)
+            );
         }
     }

Diff in /data/projects/.scratch/franken_numpy-cod-b-bold-verify-20260620-2024/crates/fnp-linalg/src/lib.rs:13884:
                 .then(x.1.partial_cmp(&y.1).unwrap())
         });
         // Expected sorted by (re, im): (0,-1), (0,1), (2,0).
-        assert!((pairs[0].0 - 0.0).abs() < 1e-9 && (pairs[0].1 + 1.0).abs() < 1e-9, "{pairs:?}");
-        assert!((pairs[1].0 - 0.0).abs() < 1e-9 && (pairs[1].1 - 1.0).abs() < 1e-9, "{pairs:?}");
-        assert!((pairs[2].0 - 2.0).abs() < 1e-9 && (pairs[2].1 - 0.0).abs() < 1e-9, "{pairs:?}");
+        assert!(
+            (pairs[0].0 - 0.0).abs() < 1e-9 && (pairs[0].1 + 1.0).abs() < 1e-9,
+            "{pairs:?}"
+        );
+        assert!(
+            (pairs[1].0 - 0.0).abs() < 1e-9 && (pairs[1].1 - 1.0).abs() < 1e-9,
+            "{pairs:?}"
+        );
+        assert!(
+            (pairs[2].0 - 2.0).abs() < 1e-9 && (pairs[2].1 - 0.0).abs() < 1e-9,
+            "{pairs:?}"
+        );
     }

     // Inline unblocked two-sided tridiagonalization WITH Q accumulation, for the
Diff in /data/projects/.scratch/franken_numpy-cod-b-bold-verify-20260620-2024/crates/fnp-linalg/src/lib.rs:13911:
             if cn < f64::EPSILON * work[j * n + j].abs().max(1.0) {
                 continue;
             }
-            let sign = if work[(j + 1) * n + j] >= 0.0 { 1.0 } else { -1.0 };
+            let sign = if work[(j + 1) * n + j] >= 0.0 {
+                1.0
+            } else {
+                -1.0
+            };
             for vi in &mut v[..=j] {
                 *vi = 0.0;
             }
Diff in /data/projects/.scratch/franken_numpy-cod-b-bold-verify-20260620-2024/crates/fnp-linalg/src/lib.rs:13992:
                 std::hint::black_box(&r2);
                 tn.push(t.elapsed().as_secs_f64() * 1e3);
             }
-            println!("n={n:5} unblocked-Q={:9.2}ms blocked-Q={:9.2}ms speedup={:.2}x", med(to.clone()), med(tn.clone()), med(to) / med(tn));
+            println!(
+                "n={n:5} unblocked-Q={:9.2}ms blocked-Q={:9.2}ms speedup={:.2}x",
+                med(to.clone()),
+                med(tn.clone()),
+                med(to) / med(tn)
+            );
         }
     }

Diff in /data/projects/.scratch/franken_numpy-cod-b-bold-verify-20260620-2024/crates/fnp-linalg/src/lib.rs:14027:
                         r += q[i * n + k] * tqt[k * n + j];
                         o += q[k * n + i] * q[k * n + j];
                     }
-                    max_recon = max_recon.max((r - a[i * n + j]).abs() / (1.0 + a[i * n + j].abs()));
+                    max_recon =
+                        max_recon.max((r - a[i * n + j]).abs() / (1.0 + a[i * n + j].abs()));
                     let t = if i == j { 1.0 } else { 0.0 };
                     max_orth = max_orth.max((o - t).abs());
                 }
Diff in /data/projects/.scratch/franken_numpy-cod-b-bold-verify-20260620-2024/crates/fnp-linalg/src/lib.rs:14224:
             }

             let mut triangular = work;
-            super::sbr_apply_symmetric_rank2k_update(
-                &mut triangular,
-                n,
-                active,
-                h,
-                nb,
-                &vv,
-                &ww,
-            );
+            super::sbr_apply_symmetric_rank2k_update(&mut triangular, n, active, h, nb, &vv, &ww);
             for row in 0..h {
                 for col in 0..h {
                     let idx = (active + row) * n + active + col;
Diff in /data/projects/.scratch/franken_numpy-cod-b-bold-verify-20260620-2024/crates/fnp-linalg/src/lib.rs:14344:
         assert_eq!(q.len(), n * n);
         let unpacked = super::unpack_lower_band(&lower_band, n, bandwidth);
         for (idx, (&dense, &packed)) in dense_band.iter().zip(&unpacked).enumerate() {
-            assert_eq!(dense.to_bits(), packed.to_bits(), "compact band drift at flat index {idx}");
+            assert_eq!(
+                dense.to_bits(),
+                packed.to_bits(),
+                "compact band drift at flat index {idx}"
+            );
         }

         let mut max_outside = 0.0f64;
Diff in /data/projects/.scratch/franken_numpy-cod-b-bold-verify-20260620-2024/crates/fnp-linalg/src/lib.rs:14392:
         let reduced = eigvalsh_nxn(&dense_band, n).expect("band eigvalsh");
         for (idx, (&lhs, &rhs)) in original.iter().zip(&reduced).enumerate() {
             let err = (lhs - rhs).abs() / (1.0 + lhs.abs().max(rhs.abs()));
-            assert!(err < 2e-8, "eigval {idx} drifted: {lhs} vs {rhs}, rel {err:e}");
+            assert!(
+                err < 2e-8,
+                "eigval {idx} drifted: {lhs} vs {rhs}, rel {err:e}"
+            );
         }
     }

Diff in /data/projects/.scratch/franken_numpy-cod-b-bold-verify-20260620-2024/crates/fnp-linalg/src/lib.rs:14481:
             }
             let (u, b) = (med(tu2), med(tb));
             let _ = med(tu);
-            println!("n={n:5} unblocked={u:9.2}ms blocked={b:9.2}ms speedup={:.2}x", u / b);
+            println!(
+                "n={n:5} unblocked={u:9.2}ms blocked={b:9.2}ms speedup={:.2}x",
+                u / b
+            );
         }
     }

Diff in /data/projects/.scratch/franken_numpy-cod-b-bold-verify-20260620-2024/crates/fnp-linalg/src/lib.rs:14503:
             if cn < f64::EPSILON * work[j * n + j].abs().max(1.0) {
                 continue;
             }
-            let sign = if work[(j + 1) * n + j] >= 0.0 { 1.0 } else { -1.0 };
+            let sign = if work[(j + 1) * n + j] >= 0.0 {
+                1.0
+            } else {
+                -1.0
+            };
             for vi in &mut v[..=j] {
                 *vi = 0.0;
             }
Diff in /data/projects/.scratch/franken_numpy-cod-b-bold-verify-20260620-2024/crates/fnp-linalg/src/lib.rs:14571:
                     for k in 0..n {
                         s += qb[i * n + k] * rb[k * n + j];
                     }
-                    max_recon = max_recon.max((s - a[i * n + j]).abs() / (1.0 + a[i * n + j].abs()));
+                    max_recon =
+                        max_recon.max((s - a[i * n + j]).abs() / (1.0 + a[i * n + j].abs()));
                     // Q^T·Q
                     let mut o = 0.0;
                     for k in 0..n {
Diff in /data/projects/.scratch/franken_numpy-cod-b-bold-verify-20260620-2024/crates/fnp-linalg/src/lib.rs:14579:
                     }
                     let target = if i == j { 1.0 } else { 0.0 };
                     max_orth = max_orth.max((o - target).abs());
-                    max_q = max_q.max((qb[i * n + j] - qu[i * n + j]).abs() / (1.0 + qu[i * n + j].abs()));
-                    max_r = max_r.max((rb[i * n + j] - ru[i * n + j]).abs() / (1.0 + ru[i * n + j].abs()));
+                    max_q = max_q
+                        .max((qb[i * n + j] - qu[i * n + j]).abs() / (1.0 + qu[i * n + j].abs()));
+                    max_r = max_r
+                        .max((rb[i * n + j] - ru[i * n + j]).abs() / (1.0 + ru[i * n + j].abs()));
                 }
             }
             assert!(max_recon < 1e-9, "Q·R=A err {max_recon:e} (n={n})");
Diff in /data/projects/.scratch/franken_numpy-cod-b-bold-verify-20260620-2024/crates/fnp-linalg/src/lib.rs:14606:
             let (q, r) = pool.install(|| super::qr_nxn(&a, n).expect("qr"));
             let (qref, rref) = qr_unblocked_ref(&a, n);
             for (p, s) in q.iter().zip(&qref) {
-                assert_eq!(p.to_bits(), s.to_bits(), "Q drifted from serial ref (n={n})");
+                assert_eq!(
+                    p.to_bits(),
+                    s.to_bits(),
+                    "Q drifted from serial ref (n={n})"
+                );
             }
             for (p, s) in r.iter().zip(&rref) {
-                assert_eq!(p.to_bits(), s.to_bits(), "R drifted from serial ref (n={n})");
+                assert_eq!(
+                    p.to_bits(),
+                    s.to_bits(),
+                    "R drifted from serial ref (n={n})"
+                );
             }
         }

Diff in /data/projects/.scratch/franken_numpy-cod-b-bold-verify-20260620-2024/crates/fnp-linalg/src/lib.rs:14737:
                 tb.push(t.elapsed().as_secs_f64() * 1e3);
             }
             let (u, b) = (med(tu), med(tb));
-            println!("n={n:5} unblocked={u:9.2}ms blocked={b:9.2}ms speedup={:.2}x", u / b);
+            println!(
+                "n={n:5} unblocked={u:9.2}ms blocked={b:9.2}ms speedup={:.2}x",
+                u / b
+            );
         }
     }

Diff in /data/projects/.scratch/franken_numpy-cod-b-bold-verify-20260620-2024/crates/fnp-linalg/src/lib.rs:14811:
             for idx in 0..n * m {
                 max_diff = max_diff.max((xb[idx] - xu[idx]).abs() / (1.0 + xu[idx].abs()));
             }
-            assert!(max_diff < 1e-9, "blocked vs unblocked TRSM err {max_diff:e} (n={n})");
+            assert!(
+                max_diff < 1e-9,
+                "blocked vs unblocked TRSM err {max_diff:e} (n={n})"
+            );
             // A·X must reconstruct B (identity).
             let mut max_recon = 0.0f64;
             for i in 0..n {
Diff in /data/projects/.scratch/franken_numpy-cod-b-bold-verify-20260620-2024/crates/fnp-linalg/src/lib.rs:14824:
                     max_recon = max_recon.max((s - target).abs());
                 }
             }
-            assert!(max_recon < 1e-7, "A·X=I reconstruction err {max_recon:e} (n={n})");
+            assert!(
+                max_recon < 1e-7,
+                "A·X=I reconstruction err {max_recon:e} (n={n})"
+            );
         }
     }

Diff in /data/projects/.scratch/franken_numpy-cod-b-bold-verify-20260620-2024/crates/fnp-linalg/src/lib.rs:14976:
                 tb.push(t.elapsed().as_secs_f64() * 1e3);
             }
             let (u, b) = (med(tu), med(tb));
-            println!("n={n:5} unblocked={u:9.2}ms blocked={b:9.2}ms speedup={:.2}x", u / b);
+            println!(
+                "n={n:5} unblocked={u:9.2}ms blocked={b:9.2}ms speedup={:.2}x",
+                u / b
+            );
         }
     }

Diff in /data/projects/.scratch/franken_numpy-cod-b-bold-verify-20260620-2024/crates/fnp-linalg/src/lib.rs:15002:
                     for k in 0..=j {
                         s += lb[i * n + k] * lb[j * n + k];
                     }
-                    max_recon = max_recon.max((s - a[i * n + j]).abs() / (1.0 + a[i * n + j].abs()));
-                    max_diff =
-                        max_diff.max((lb[i * n + j] - lr[i * n + j]).abs() / (1.0 + lr[i * n + j].abs()));
+                    max_recon =
+                        max_recon.max((s - a[i * n + j]).abs() / (1.0 + a[i * n + j].abs()));
+                    max_diff = max_diff
+                        .max((lb[i * n + j] - lr[i * n + j]).abs() / (1.0 + lr[i * n + j].abs()));
                 }
             }
-            assert!(max_recon < 1e-9, "blocked L·L^T=A err {max_recon:e} (n={n})");
-            assert!(max_diff < 1e-9, "blocked vs unblocked err {max_diff:e} (n={n})");
+            assert!(
+                max_recon < 1e-9,
+                "blocked L·L^T=A err {max_recon:e} (n={n})"
+            );
+            assert!(
+                max_diff < 1e-9,
+                "blocked vs unblocked err {max_diff:e} (n={n})"
+            );
         }
     }

Diff in /data/projects/.scratch/franken_numpy-cod-b-bold-verify-20260620-2024/crates/fnp-linalg/src/lib.rs:15026:
                 for k in 0..=j {
                     s += l[i * n + k] * l[j * n + k];
                 }
-                max_recon =
-                    max_recon.max((s - a[i * n + j]).abs() / (1.0 + a[i * n + j].abs()));
+                max_recon = max_recon.max((s - a[i * n + j]).abs() / (1.0 + a[i * n + j].abs()));
             }
         }
         assert!(
Diff in /data/projects/.scratch/franken_numpy-cod-b-bold-verify-20260620-2024/crates/fnp-linalg/src/lib.rs:15066:
                 for k in 0..=j {
                     s += l[i * n + k] * l[j * n + k];
                 }
-                max_recon =
-                    max_recon.max((s - a[i * n + j]).abs() / (1.0 + a[i * n + j].abs()));
+                max_recon = max_recon.max((s - a[i * n + j]).abs() / (1.0 + a[i * n + j].abs()));
             }
         }
         assert!(
Diff in /data/projects/.scratch/franken_numpy-cod-b-bold-verify-20260620-2024/crates/fnp-linalg/src/lib.rs:15116:
                 tb.push(t.elapsed().as_secs_f64() * 1e3);
             }
             let (u, b) = (med(tu), med(tb));
-            println!("n={n:5} unblocked={u:9.2}ms blocked={b:9.2}ms speedup={:.2}x", u / b);
+            println!(
+                "n={n:5} unblocked={u:9.2}ms blocked={b:9.2}ms speedup={:.2}x",
+                u / b
+            );
         }
     }

Diff in /data/projects/.scratch/franken_numpy-cod-b-bold-verify-20260620-2024/crates/fnp-linalg/src/lib.rs:15181:
             let thr = (n as f64) * f64::EPSILON * max_abs;
             let (lu, perm, _sign) = super::lu_decompose_blocked(&a, n, thr).expect("blocked lu");
             let (_lu_ref, perm_ref) = lu_unblocked_ref(&a, n);
-            assert_eq!(perm, perm_ref, "pivot sequence must match unblocked (n={n})");
+            assert_eq!(
+                perm, perm_ref,
+                "pivot sequence must match unblocked (n={n})"
+            );
             let mut max_err = 0.0f64;
             for i in 0..n {
                 for j in 0..n {
Diff in /data/projects/.scratch/franken_numpy-cod-b-bold-verify-20260620-2024/crates/fnp-linalg/src/lib.rs:15201:
                     max_err = max_err.max((s - pa).abs() / (1.0 + pa.abs()));
                 }
             }
-            assert!(max_err < 1e-9, "blocked P·A=L·U reconstruction err {max_err:e} (n={n})");
+            assert!(
+                max_err < 1e-9,
+                "blocked P·A=L·U reconstruction err {max_err:e} (n={n})"
+            );
         }
     }

Diff in /data/projects/.scratch/franken_numpy-cod-b-bold-verify-20260620-2024/crates/fnp-linalg/src/lib.rs:15283:
                 tb.push(t.elapsed().as_secs_f64() * 1e3);
             }
             let (u, b) = (med(tu), med(tb));
-            println!("n={n:5} unblocked={u:9.2}ms blocked={b:9.2}ms speedup={:.2}x", u / b);
+            println!(
+                "n={n:5} unblocked={u:9.2}ms blocked={b:9.2}ms speedup={:.2}x",
+                u / b
+            );
         }
     }

Diff in /data/projects/.scratch/franken_numpy-cod-b-bold-verify-20260620-2024/crates/fnp-linalg/src/lib.rs:15549:
     #[test]
     fn cond_p_spectral_symmetric_matches_svd_reference() {
         let a = [
-            5.0, 0.25, -0.5, 0.75, 0.25, 4.0, 0.125, -0.25, -0.5, 0.125, 3.0, 0.5,
-            0.75, -0.25, 0.5, 2.0,
+            5.0, 0.25, -0.5, 0.75, 0.25, 4.0, 0.125, -0.25, -0.5, 0.125, 3.0, 0.5, 0.75, -0.25,
+            0.5, 2.0,
         ];
         let fast = cond_nxn(&a, 4).unwrap();
         let sigmas = svd_nxn(&a, 4).unwrap();
Diff in /data/projects/.scratch/franken_numpy-cod-b-bold-verify-20260620-2024/crates/fnp-linalg/src/lib.rs:18234:
             let mat_size = n * n;
             (0..batch)
                 .map(|b| {
-                    trace_nxn(&data[b * mat_size..(b + 1) * mat_size], n)
-                        .expect("reference trace")
+                    trace_nxn(&data[b * mat_size..(b + 1) * mat_size], n).expect("reference trace")
                 })
                 .collect()
         }
