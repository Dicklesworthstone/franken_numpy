Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:219:
             });
         });
         group.bench_function("numpy_datetime_ns_to_us_16m", |b| {
-            b.iter(|| black_box(dt.call_method1("astype", ("datetime64[us]",)).expect("np astype")));
+            b.iter(|| {
+                black_box(
+                    dt.call_method1("astype", ("datetime64[us]",))
+                        .expect("np astype"),
+                )
+            });
         });
     });

Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:440:

         // 3-D middle axis (axis=1): block-parallel non-last path.
         let input3d = numpy
-            .call_method1("linspace", (-1.0_f64, 1.0_f64, 256_usize * 256_usize * 64_usize))
+            .call_method1(
+                "linspace",
+                (-1.0_f64, 1.0_f64, 256_usize * 256_usize * 64_usize),
+            )
             .expect("4M f64 3d source")
             .call_method1("reshape", ((256_usize, 256_usize, 64_usize),))
             .expect("256x256x64 reshape");
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:554:

         // 3-D axis=0: outer==1, inner=128*256=32768 — the fixed single-group path.
         let input3d = numpy
-            .call_method1("linspace", (-1.0_f64, 1.0_f64, 256_usize * 128_usize * 256_usize))
+            .call_method1(
+                "linspace",
+                (-1.0_f64, 1.0_f64, 256_usize * 128_usize * 256_usize),
+            )
             .expect("8M f64 3d source")
             .call_method1("reshape", ((256_usize, 128_usize, 256_usize),))
             .expect("256x128x256 reshape");
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:1569:
             .call_method1("maketrans", ("abcdXYZ9", "ABCDxyz0"))
             .expect("maketrans");
         let fnp_tr = fnp_char.getattr("translate").expect("fnp char.translate");
-        let numpy_tr = numpy_char.getattr("translate").expect("numpy char.translate");
+        let numpy_tr = numpy_char
+            .getattr("translate")
+            .expect("numpy char.translate");
         group.bench_function("fnp_char_translate_u20_ascii_1m", |bench| {
             bench.iter(|| black_box(fnp_tr.call1((&input, &tbl)).expect("fnp translate")));
         });
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:1594:
         let kw3 = PyDict::new(py);
         kw3.set_item("dtype", "<U20").expect("dtype kw3");
         let input_ws = numpy
-            .call_method("full", ((1_000_000_usize,), "   azByCxD0123   "), Some(&kw3))
+            .call_method(
+                "full",
+                ((1_000_000_usize,), "   azByCxD0123   "),
+                Some(&kw3),
+            )
             .expect("1M U20 ws-padded input");
         let fnp_strip = fnp_char.getattr("strip").expect("fnp char.strip");
         let numpy_strip = numpy_char.getattr("strip").expect("numpy char.strip");
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:1849:
             });
             group.bench_function(format!("numpy_histogram_{label}"), |bench| {
                 bench.iter(|| {
-                    black_box(numpy_hist.call((x,), Some(&big_kwargs)).expect("numpy hist big"))
+                    black_box(
+                        numpy_hist
+                            .call((x,), Some(&big_kwargs))
+                            .expect("numpy hist big"),
+                    )
                 });
             });
         }
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:2479:
             bch.iter(|| black_box(numpy_argsort.call1((&permf32,)).expect("numpy argsort f32")));
         });
         // datetime64 flat argsort on DISTINCT ticks (int64-backed; numpy non-simd introsort)
-        let permdt = perm.call_method1("astype", ("datetime64[s]",)).expect("perm datetime64");
+        let permdt = perm
+            .call_method1("astype", ("datetime64[s]",))
+            .expect("perm datetime64");
         group.bench_function("fnp_argsort_datetime64_16m", |bch| {
             bch.iter(|| black_box(fnp_argsort.call1((&permdt,)).expect("fnp argsort dt64")));
         });
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:2497:
             .call_method1("standard_normal", (16_000_000_usize,))
             .expect("c imag");
         let permc = perm
-            .call_method1("__add__", (cim.call_method1("__mul__", (pyo3::types::PyComplex::from_doubles(py, 0.0, 1.0),)).expect("1j*im"),))
+            .call_method1(
+                "__add__",
+                (cim.call_method1(
+                    "__mul__",
+                    (pyo3::types::PyComplex::from_doubles(py, 0.0, 1.0),),
+                )
+                .expect("1j*im"),),
+            )
             .expect("re+1j*im")
             .call_method1("astype", ("complex128",))
             .expect("perm c128");
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:2586:
             bch.iter(|| black_box(fnp_argsort.call1((&la_f32,)).expect("fnp argsort la f32")));
         });
         group.bench_function("numpy_argsort_f32_lastaxis_16Mx", |bch| {
-            bch.iter(|| black_box(numpy_argsort.call1((&la_f32,)).expect("numpy argsort la f32")));
+            bch.iter(|| {
+                black_box(
+                    numpy_argsort
+                        .call1((&la_f32,))
+                        .expect("numpy argsort la f32"),
+                )
+            });
         });
         let a0_f32 = a0.call_method1("astype", ("float32",)).expect("a0 f32");
         group.bench_function("fnp_argsort_f32_axis0_16Mx", |bch| {
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:2631:
         let la_c = la
             .call_method1(
                 "__add__",
-                (rng
-                    .call_method1("standard_normal", (la.getattr("shape").expect("la shape"),))
-                    .expect("la imag")
-                    .call_method1("__mul__", (&onej,))
-                    .expect("1j*la_im"),),
+                (
+                    rng.call_method1("standard_normal", (la.getattr("shape").expect("la shape"),))
+                        .expect("la imag")
+                        .call_method1("__mul__", (&onej,))
+                        .expect("1j*la_im"),
+                ),
             )
             .expect("la re+im")
             .call_method1("astype", ("complex128",))
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:2644:
             bch.iter(|| black_box(fnp_argsort.call1((&la_c,)).expect("fnp argsort la c128")));
         });
         group.bench_function("numpy_argsort_c128_lastaxis_16Mx", |bch| {
-            bch.iter(|| black_box(numpy_argsort.call1((&la_c,)).expect("numpy argsort la c128")));
+            bch.iter(|| {
+                black_box(
+                    numpy_argsort
+                        .call1((&la_c,))
+                        .expect("numpy argsort la c128"),
+                )
+            });
         });
         let a0_c = a0
             .call_method1(
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:2651:
                 "__add__",
-                (rng
-                    .call_method1("standard_normal", (a0.getattr("shape").expect("a0 shape"),))
-                    .expect("a0 imag")
-                    .call_method1("__mul__", (&onej,))
-                    .expect("1j*a0_im"),),
+                (
+                    rng.call_method1("standard_normal", (a0.getattr("shape").expect("a0 shape"),))
+                        .expect("a0 imag")
+                        .call_method1("__mul__", (&onej,))
+                        .expect("1j*a0_im"),
+                ),
             )
             .expect("a0 re+im")
             .call_method1("astype", ("complex128",))
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:2660:
             .expect("a0 c128");
         group.bench_function("fnp_argsort_c128_axis0_16Mx", |bch| {
             bch.iter(|| {
-                black_box(fnp_argsort.call((&a0_c,), Some(&axis0_kwargs)).expect("fnp argsort a0 c128"))
+                black_box(
+                    fnp_argsort
+                        .call((&a0_c,), Some(&axis0_kwargs))
+                        .expect("fnp argsort a0 c128"),
+                )
             });
         });
         group.bench_function("numpy_argsort_c128_axis0_16Mx", |bch| {
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:2667:
             bch.iter(|| {
-                black_box(numpy_argsort.call((&a0_c,), Some(&axis0_kwargs)).expect("numpy argsort a0 c128"))
+                black_box(
+                    numpy_argsort
+                        .call((&a0_c,), Some(&axis0_kwargs))
+                        .expect("numpy argsort a0 c128"),
+                )
             });
         });
         let am_c = am
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:2672:
             .call_method1(
                 "__add__",
-                (rng
-                    .call_method1("standard_normal", (am.getattr("shape").expect("am shape"),))
-                    .expect("am imag")
-                    .call_method1("__mul__", (&onej,))
-                    .expect("1j*am_im"),),
+                (
+                    rng.call_method1("standard_normal", (am.getattr("shape").expect("am shape"),))
+                        .expect("am imag")
+                        .call_method1("__mul__", (&onej,))
+                        .expect("1j*am_im"),
+                ),
             )
             .expect("am re+im")
             .call_method1("astype", ("complex128",))
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:2682:
             .expect("am c128");
         group.bench_function("fnp_argsort_c128_midaxis_16Mx", |bch| {
             bch.iter(|| {
-                black_box(fnp_argsort.call((&am_c,), Some(&axis1_kwargs)).expect("fnp argsort am c128"))
+                black_box(
+                    fnp_argsort
+                        .call((&am_c,), Some(&axis1_kwargs))
+                        .expect("fnp argsort am c128"),
+                )
             });
         });
         group.bench_function("numpy_argsort_c128_midaxis_16Mx", |bch| {
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:2689:
             bch.iter(|| {
-                black_box(numpy_argsort.call((&am_c,), Some(&axis1_kwargs)).expect("numpy argsort am c128"))
+                black_box(
+                    numpy_argsort
+                        .call((&am_c,), Some(&axis1_kwargs))
+                        .expect("numpy argsort am c128"),
+                )
             });
         });
         // COMPLEX128 VALUE sort (np.sort): flat (permc, 16M distinct-real) + last-axis (la_c, distinct-per-lane)
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:2706:
         // COMPLEX128 VALUE sort AXIS0 + MIDAXIS: reuse a0_c (distinct-per-column) + am_c (distinct-per-lane)
         group.bench_function("fnp_sort_c128_axis0_16Mx", |bch| {
             bch.iter(|| {
-                black_box(fnp_sort.call((&a0_c,), Some(&axis0_kwargs)).expect("fnp sort a0 c128"))
+                black_box(
+                    fnp_sort
+                        .call((&a0_c,), Some(&axis0_kwargs))
+                        .expect("fnp sort a0 c128"),
+                )
             });
         });
         group.bench_function("numpy_sort_c128_axis0_16Mx", |bch| {
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:2713:
             bch.iter(|| {
-                black_box(numpy_sort.call((&a0_c,), Some(&axis0_kwargs)).expect("numpy sort a0 c128"))
+                black_box(
+                    numpy_sort
+                        .call((&a0_c,), Some(&axis0_kwargs))
+                        .expect("numpy sort a0 c128"),
+                )
             });
         });
         group.bench_function("fnp_sort_c128_midaxis_16Mx", |bch| {
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:2718:
             bch.iter(|| {
-                black_box(fnp_sort.call((&am_c,), Some(&axis1_kwargs)).expect("fnp sort am c128"))
+                black_box(
+                    fnp_sort
+                        .call((&am_c,), Some(&axis1_kwargs))
+                        .expect("fnp sort am c128"),
+                )
             });
         });
         group.bench_function("numpy_sort_c128_midaxis_16Mx", |bch| {
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:2723:
             bch.iter(|| {
-                black_box(numpy_sort.call((&am_c,), Some(&axis1_kwargs)).expect("numpy sort am c128"))
+                black_box(
+                    numpy_sort
+                        .call((&am_c,), Some(&axis1_kwargs))
+                        .expect("numpy sort am c128"),
+                )
             });
         });
         // COMPLEX64 VALUE sort (np.sort): permc/la_c cast to complex64 (distinct-real -> tie-free)
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:2728:
-        let permc64 = permc.call_method1("astype", ("complex64",)).expect("permc64");
+        let permc64 = permc
+            .call_method1("astype", ("complex64",))
+            .expect("permc64");
         group.bench_function("fnp_sort_c64_16m", |bch| {
             bch.iter(|| black_box(fnp_sort.call1((&permc64,)).expect("fnp sort c64")));
         });
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:2750:
             bch.iter(|| black_box(fnp_argsort.call1((&la_c64,)).expect("fnp argsort la c64")));
         });
         group.bench_function("numpy_argsort_c64_lastaxis_16Mx", |bch| {
-            bch.iter(|| black_box(numpy_argsort.call1((&la_c64,)).expect("numpy argsort la c64")));
+            bch.iter(|| {
+                black_box(
+                    numpy_argsort
+                        .call1((&la_c64,))
+                        .expect("numpy argsort la c64"),
+                )
+            });
         });
         // COMPLEX64 argsort AXIS0 + MIDAXIS: reuse a0_c/am_c (distinct-real) cast to complex64
         let a0_c64 = a0_c.call_method1("astype", ("complex64",)).expect("a0_c64");
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:2757:
         group.bench_function("fnp_argsort_c64_axis0_16Mx", |bch| {
             bch.iter(|| {
-                black_box(fnp_argsort.call((&a0_c64,), Some(&axis0_kwargs)).expect("fnp argsort a0 c64"))
+                black_box(
+                    fnp_argsort
+                        .call((&a0_c64,), Some(&axis0_kwargs))
+                        .expect("fnp argsort a0 c64"),
+                )
             });
         });
         group.bench_function("numpy_argsort_c64_axis0_16Mx", |bch| {
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:2763:
             bch.iter(|| {
-                black_box(numpy_argsort.call((&a0_c64,), Some(&axis0_kwargs)).expect("numpy argsort a0 c64"))
+                black_box(
+                    numpy_argsort
+                        .call((&a0_c64,), Some(&axis0_kwargs))
+                        .expect("numpy argsort a0 c64"),
+                )
             });
         });
         let am_c64 = am_c.call_method1("astype", ("complex64",)).expect("am_c64");
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:2768:
         group.bench_function("fnp_argsort_c64_midaxis_16Mx", |bch| {
             bch.iter(|| {
-                black_box(fnp_argsort.call((&am_c64,), Some(&axis1_kwargs)).expect("fnp argsort am c64"))
+                black_box(
+                    fnp_argsort
+                        .call((&am_c64,), Some(&axis1_kwargs))
+                        .expect("fnp argsort am c64"),
+                )
             });
         });
         group.bench_function("numpy_argsort_c64_midaxis_16Mx", |bch| {
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:2774:
             bch.iter(|| {
-                black_box(numpy_argsort.call((&am_c64,), Some(&axis1_kwargs)).expect("numpy argsort am c64"))
+                black_box(
+                    numpy_argsort
+                        .call((&am_c64,), Some(&axis1_kwargs))
+                        .expect("numpy argsort am c64"),
+                )
             });
         });
         // COMPLEX64 VALUE sort AXIS0 + MIDAXIS: reuse a0_c64 (distinct-per-column) + am_c64 (distinct-per-lane)
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:2779:
         group.bench_function("fnp_sort_c64_axis0_16Mx", |bch| {
             bch.iter(|| {
-                black_box(fnp_sort.call((&a0_c64,), Some(&axis0_kwargs)).expect("fnp sort a0 c64"))
+                black_box(
+                    fnp_sort
+                        .call((&a0_c64,), Some(&axis0_kwargs))
+                        .expect("fnp sort a0 c64"),
+                )
             });
         });
         group.bench_function("numpy_sort_c64_axis0_16Mx", |bch| {
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:2785:
             bch.iter(|| {
-                black_box(numpy_sort.call((&a0_c64,), Some(&axis0_kwargs)).expect("numpy sort a0 c64"))
+                black_box(
+                    numpy_sort
+                        .call((&a0_c64,), Some(&axis0_kwargs))
+                        .expect("numpy sort a0 c64"),
+                )
             });
         });
         group.bench_function("fnp_sort_c64_midaxis_16Mx", |bch| {
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:2790:
             bch.iter(|| {
-                black_box(fnp_sort.call((&am_c64,), Some(&axis1_kwargs)).expect("fnp sort am c64"))
+                black_box(
+                    fnp_sort
+                        .call((&am_c64,), Some(&axis1_kwargs))
+                        .expect("fnp sort am c64"),
+                )
             });
         });
         group.bench_function("numpy_sort_c64_midaxis_16Mx", |bch| {
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:2795:
             bch.iter(|| {
-                black_box(numpy_sort.call((&am_c64,), Some(&axis1_kwargs)).expect("numpy sort am c64"))
+                black_box(
+                    numpy_sort
+                        .call((&am_c64,), Some(&axis1_kwargs))
+                        .expect("numpy sort am c64"),
+                )
             });
         });
         // datetime64 last-axis argsort: la (16384x1024 distinct-per-lane int64) cast to datetime64[s]
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:2800:
-        let la_dt = la.call_method1("astype", ("datetime64[s]",)).expect("la dt64");
+        let la_dt = la
+            .call_method1("astype", ("datetime64[s]",))
+            .expect("la dt64");
         group.bench_function("fnp_argsort_datetime64_lastaxis_16Mx", |bch| {
             bch.iter(|| black_box(fnp_argsort.call1((&la_dt,)).expect("fnp argsort la dt64")));
         });
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:2804:
         group.bench_function("numpy_argsort_datetime64_lastaxis_16Mx", |bch| {
-            bch.iter(|| black_box(numpy_argsort.call1((&la_dt,)).expect("numpy argsort la dt64")));
+            bch.iter(|| {
+                black_box(
+                    numpy_argsort
+                        .call1((&la_dt,))
+                        .expect("numpy argsort la dt64"),
+                )
+            });
         });
         // datetime64 last-axis VALUE sort (np.sort) on the same 16384x1024 distinct-per-lane dt64.
         group.bench_function("fnp_sort_datetime64_lastaxis_16Mx", |bch| {
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:2823:
             .call_method1("astype", ("int64",))
             .expect("m3 int64");
         group.bench_function("fnp_sort_int64_midaxis_16Mx", |bch| {
-            bch.iter(|| black_box(fnp_sort.call((&m3,), Some(&m3_kwargs)).expect("fnp sort m3")));
+            bch.iter(|| {
+                black_box(
+                    fnp_sort
+                        .call((&m3,), Some(&m3_kwargs))
+                        .expect("fnp sort m3"),
+                )
+            });
         });
         group.bench_function("numpy_sort_int64_midaxis_16Mx", |bch| {
-            bch.iter(|| black_box(numpy_sort.call((&m3,), Some(&m3_kwargs)).expect("numpy sort m3")));
+            bch.iter(|| {
+                black_box(
+                    numpy_sort
+                        .call((&m3,), Some(&m3_kwargs))
+                        .expect("numpy sort m3"),
+                )
+            });
         });
-        let m3_dt = m3.call_method1("astype", ("datetime64[s]",)).expect("m3 dt64");
+        let m3_dt = m3
+            .call_method1("astype", ("datetime64[s]",))
+            .expect("m3 dt64");
         group.bench_function("fnp_sort_datetime64_midaxis_16Mx", |bch| {
-            bch.iter(|| black_box(fnp_sort.call((&m3_dt,), Some(&m3_kwargs)).expect("fnp sort m3 dt")));
+            bch.iter(|| {
+                black_box(
+                    fnp_sort
+                        .call((&m3_dt,), Some(&m3_kwargs))
+                        .expect("fnp sort m3 dt"),
+                )
+            });
         });
         group.bench_function("numpy_sort_datetime64_midaxis_16Mx", |bch| {
-            bch.iter(|| black_box(numpy_sort.call((&m3_dt,), Some(&m3_kwargs)).expect("numpy sort m3 dt")));
+            bch.iter(|| {
+                black_box(
+                    numpy_sort
+                        .call((&m3_dt,), Some(&m3_kwargs))
+                        .expect("numpy sort m3 dt"),
+                )
+            });
         });
     });

Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:3125:
             let fnp_kwargs = PyDict::new(py);
             fnp_kwargs.set_item("axis", 1_i64).expect("fnp axis kwarg");
             let numpy_kwargs = PyDict::new(py);
-            numpy_kwargs.set_item("axis", 1_i64).expect("numpy axis kwarg");
+            numpy_kwargs
+                .set_item("axis", 1_i64)
+                .expect("numpy axis kwarg");

             group.bench_function(format!("fnp_var_f64_axis1_{label}"), |bench| {
                 bench.iter(|| {
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:3205:
             .call_method1("astype", (&f32_dtype,))
             .expect("astype f32");

-        for (label, input, axis) in [("mid_256x128x256", &mid, 1_i64), ("axis0_4000x2000", &ax0, 0_i64)] {
+        for (label, input, axis) in [
+            ("mid_256x128x256", &mid, 1_i64),
+            ("axis0_4000x2000", &ax0, 0_i64),
+        ] {
             let fkw = PyDict::new(py);
             fkw.set_item("axis", axis).expect("axis");
             let nkw = PyDict::new(py);
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:3214:
                 bench.iter(|| black_box(fnp_var.call((input,), Some(&fkw)).expect("fnp var f32")));
             });
             group.bench_function(format!("numpy_var_f32_{label}"), |bench| {
-                bench.iter(|| black_box(numpy_var.call((input,), Some(&nkw)).expect("numpy var f32")));
+                bench.iter(|| {
+                    black_box(numpy_var.call((input,), Some(&nkw)).expect("numpy var f32"))
+                });
             });
             group.bench_function(format!("fnp_std_f32_{label}"), |bench| {
                 bench.iter(|| black_box(fnp_std.call((input,), Some(&fkw)).expect("fnp std f32")));
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:3221:
             });
             group.bench_function(format!("numpy_std_f32_{label}"), |bench| {
-                bench.iter(|| black_box(numpy_std.call((input,), Some(&nkw)).expect("numpy std f32")));
+                bench.iter(|| {
+                    black_box(numpy_std.call((input,), Some(&nkw)).expect("numpy std f32"))
+                });
             });
         }
     });
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:3335:
             b.iter(|| black_box(fnp_nansum.call((&a64,), Some(&kw)).expect("fnp nansum f64")));
         });
         group.bench_function("numpy_nansum_f64_mid", |b| {
-            b.iter(|| black_box(numpy_nansum.call((&a64,), Some(&kw2)).expect("np nansum f64")));
+            b.iter(|| {
+                black_box(
+                    numpy_nansum
+                        .call((&a64,), Some(&kw2))
+                        .expect("np nansum f64"),
+                )
+            });
         });
         // f64 nansum LAST axis: per-lane pairwise (now bit-exact, was sequential) + parallel.
         let lax = PyDict::new(py);
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:3342:
         lax.set_item("axis", 2_i64).unwrap();
         let lax2 = lax.clone();
         group.bench_function("fnp_nansum_f64_last", |b| {
-            b.iter(|| black_box(fnp_nansum.call((&a64,), Some(&lax)).expect("fnp nansum f64 last")));
+            b.iter(|| {
+                black_box(
+                    fnp_nansum
+                        .call((&a64,), Some(&lax))
+                        .expect("fnp nansum f64 last"),
+                )
+            });
         });
         group.bench_function("numpy_nansum_f64_last", |b| {
-            b.iter(|| black_box(numpy_nansum.call((&a64,), Some(&lax2)).expect("np nansum f64 last")));
+            b.iter(|| {
+                black_box(
+                    numpy_nansum
+                        .call((&a64,), Some(&lax2))
+                        .expect("np nansum f64 last"),
+                )
+            });
         });
     });

Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:3383:
             let idx = numpy
                 .call_method1("arange", (0_i64, total, 10_i64))
                 .expect("nan stride");
-            arr.call_method1("__setitem__", (idx, &nan)).expect("inject NaN");
-            arr.call_method1("reshape", (PyTuple::new(py, dims.iter().copied()).unwrap(),))
-                .expect("reshape")
+            arr.call_method1("__setitem__", (idx, &nan))
+                .expect("inject NaN");
+            arr.call_method1(
+                "reshape",
+                (PyTuple::new(py, dims.iter().copied()).unwrap(),),
+            )
+            .expect("reshape")
         };
         let mid = build(&[256, 128, 256], 256 * 128 * 256);
         let ax0 = build(&[4000, 2000], 4000 * 2000);
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:3392:

-        for (label, input, axis) in [("mid_256x128x256", &mid, 1_i64), ("axis0_4000x2000", &ax0, 0_i64)] {
+        for (label, input, axis) in [
+            ("mid_256x128x256", &mid, 1_i64),
+            ("axis0_4000x2000", &ax0, 0_i64),
+        ] {
             let fkw = PyDict::new(py);
             fkw.set_item("axis", axis).expect("axis");
             let nkw = PyDict::new(py);
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:3397:
             nkw.set_item("axis", axis).expect("axis");
             group.bench_function(format!("fnp_nanvar_f32_{label}"), |b| {
-                b.iter(|| black_box(fnp_nanvar.call((input,), Some(&fkw)).expect("fnp nanvar f32")));
+                b.iter(|| {
+                    black_box(
+                        fnp_nanvar
+                            .call((input,), Some(&fkw))
+                            .expect("fnp nanvar f32"),
+                    )
+                });
             });
             group.bench_function(format!("numpy_nanvar_f32_{label}"), |b| {
-                b.iter(|| black_box(numpy_nanvar.call((input,), Some(&nkw)).expect("numpy nanvar f32")));
+                b.iter(|| {
+                    black_box(
+                        numpy_nanvar
+                            .call((input,), Some(&nkw))
+                            .expect("numpy nanvar f32"),
+                    )
+                });
             });
             group.bench_function(format!("fnp_nanstd_f32_{label}"), |b| {
-                b.iter(|| black_box(fnp_nanstd.call((input,), Some(&fkw)).expect("fnp nanstd f32")));
+                b.iter(|| {
+                    black_box(
+                        fnp_nanstd
+                            .call((input,), Some(&fkw))
+                            .expect("fnp nanstd f32"),
+                    )
+                });
             });
             group.bench_function(format!("numpy_nanstd_f32_{label}"), |b| {
-                b.iter(|| black_box(numpy_nanstd.call((input,), Some(&nkw)).expect("numpy nanstd f32")));
+                b.iter(|| {
+                    black_box(
+                        numpy_nanstd
+                            .call((input,), Some(&nkw))
+                            .expect("numpy nanstd f32"),
+                    )
+                });
             });
             group.bench_function(format!("fnp_nanmean_f32_{label}"), |b| {
-                b.iter(|| black_box(fnp_nanmean.call((input,), Some(&fkw)).expect("fnp nanmean f32")));
+                b.iter(|| {
+                    black_box(
+                        fnp_nanmean
+                            .call((input,), Some(&fkw))
+                            .expect("fnp nanmean f32"),
+                    )
+                });
             });
             group.bench_function(format!("numpy_nanmean_f32_{label}"), |b| {
-                b.iter(|| black_box(numpy_nanmean.call((input,), Some(&nkw)).expect("numpy nanmean f32")));
+                b.iter(|| {
+                    black_box(
+                        numpy_nanmean
+                            .call((input,), Some(&nkw))
+                            .expect("numpy nanmean f32"),
+                    )
+                });
             });
         }
     });
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:3454:
             let idx = numpy
                 .call_method1("arange", (0_i64, total, 10_i64))
                 .expect("nan stride");
-            arr.call_method1("__setitem__", (idx, &nan)).expect("inject NaN");
-            arr.call_method1("reshape", (PyTuple::new(py, dims.iter().copied()).unwrap(),))
-                .expect("reshape")
+            arr.call_method1("__setitem__", (idx, &nan))
+                .expect("inject NaN");
+            arr.call_method1(
+                "reshape",
+                (PyTuple::new(py, dims.iter().copied()).unwrap(),),
+            )
+            .expect("reshape")
         };
         let last2d = build(&[1000, 2048], 1000 * 2048);
         let trail3d = build(&[512, 64, 64], 512 * 64 * 64);
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:3475:
             let fkw = kw;
             let nkw = kw;
             group.bench_function(format!("fnp_nanvar_f32_{label}"), |b| {
-                b.iter(|| black_box(fnp_nanvar.call((input,), Some(&fkw)).expect("fnp nanvar f32")));
+                b.iter(|| {
+                    black_box(
+                        fnp_nanvar
+                            .call((input,), Some(&fkw))
+                            .expect("fnp nanvar f32"),
+                    )
+                });
             });
             group.bench_function(format!("numpy_nanvar_f32_{label}"), |b| {
-                b.iter(|| black_box(numpy_nanvar.call((input,), Some(&nkw)).expect("numpy nanvar f32")));
+                b.iter(|| {
+                    black_box(
+                        numpy_nanvar
+                            .call((input,), Some(&nkw))
+                            .expect("numpy nanvar f32"),
+                    )
+                });
             });
             group.bench_function(format!("fnp_nanstd_f32_{label}"), |b| {
-                b.iter(|| black_box(fnp_nanstd.call((input,), Some(&fkw)).expect("fnp nanstd f32")));
+                b.iter(|| {
+                    black_box(
+                        fnp_nanstd
+                            .call((input,), Some(&fkw))
+                            .expect("fnp nanstd f32"),
+                    )
+                });
             });
             group.bench_function(format!("numpy_nanstd_f32_{label}"), |b| {
-                b.iter(|| black_box(numpy_nanstd.call((input,), Some(&nkw)).expect("numpy nanstd f32")));
+                b.iter(|| {
+                    black_box(
+                        numpy_nanstd
+                            .call((input,), Some(&nkw))
+                            .expect("numpy nanstd f32"),
+                    )
+                });
             });
             group.bench_function(format!("fnp_nanmean_f32_{label}"), |b| {
-                b.iter(|| black_box(fnp_nanmean.call((input,), Some(&fkw)).expect("fnp nanmean f32")));
+                b.iter(|| {
+                    black_box(
+                        fnp_nanmean
+                            .call((input,), Some(&fkw))
+                            .expect("fnp nanmean f32"),
+                    )
+                });
             });
             group.bench_function(format!("numpy_nanmean_f32_{label}"), |b| {
-                b.iter(|| black_box(numpy_nanmean.call((input,), Some(&nkw)).expect("numpy nanmean f32")));
+                b.iter(|| {
+                    black_box(
+                        numpy_nanmean
+                            .call((input,), Some(&nkw))
+                            .expect("numpy nanmean f32"),
+                    )
+                });
             });
         }
     });
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:3532:
                 .expect("nanvar midaxis f64 input")
                 .call_method1("reshape", ((d0, d1, d2),))
                 .expect("nanvar midaxis 3-D shape");
-            let flat = input.call_method1("reshape", ((size,),)).expect("flat view");
+            let flat = input
+                .call_method1("reshape", ((size,),))
+                .expect("flat view");
             let idx = numpy
                 .call_method1("arange", (0_i64, size, 10_i64))
                 .expect("nan index stride");
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:3543:
             let fnp_kwargs = PyDict::new(py);
             fnp_kwargs.set_item("axis", 1_i64).expect("fnp axis kwarg");
             let numpy_kwargs = PyDict::new(py);
-            numpy_kwargs.set_item("axis", 1_i64).expect("numpy axis kwarg");
+            numpy_kwargs
+                .set_item("axis", 1_i64)
+                .expect("numpy axis kwarg");

             group.bench_function(format!("fnp_nanvar_f64_axis1_{label}"), |bench| {
                 bench.iter(|| {
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:3636:
             let fnp_kwargs = PyDict::new(py);
             fnp_kwargs.set_item("axis", 0_i64).expect("fnp axis kwarg");
             let numpy_kwargs = PyDict::new(py);
-            numpy_kwargs.set_item("axis", 0_i64).expect("numpy axis kwarg");
+            numpy_kwargs
+                .set_item("axis", 0_i64)
+                .expect("numpy axis kwarg");

             group.bench_function(format!("fnp_var_f64_axis0_{label}"), |bench| {
                 bench.iter(|| {
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:3793:
         let fnp_prod = module.getattr("prod").expect("fnp_python.prod");
         let numpy_prod = numpy.getattr("prod").expect("numpy.prod");

-        for (label, rows, cols) in [("8192x1024", 8192_i64, 1024_i64), ("65536x256", 65536_i64, 256_i64)]
-        {
+        for (label, rows, cols) in [
+            ("8192x1024", 8192_i64, 1024_i64),
+            ("65536x256", 65536_i64, 256_i64),
+        ] {
             let size = rows * cols;
             // values near 1.0 so the product stays finite across the axis.
             let input = numpy
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:3805:
             let fnp_kwargs = PyDict::new(py);
             fnp_kwargs.set_item("axis", -1_i64).expect("fnp axis kwarg");
             let numpy_kwargs = PyDict::new(py);
-            numpy_kwargs.set_item("axis", -1_i64).expect("numpy axis kwarg");
+            numpy_kwargs
+                .set_item("axis", -1_i64)
+                .expect("numpy axis kwarg");

             group.bench_function(format!("fnp_prod_f64_axis_last_{label}"), |bench| {
                 bench.iter(|| {
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:3844:
         let fnp_cumsum = module.getattr("cumsum").expect("fnp_python.cumsum");
         let numpy_cumsum = numpy.getattr("cumsum").expect("numpy.cumsum");

-        for (label, rows, cols) in [("8192x1024", 8192_i64, 1024_i64), ("65536x256", 65536_i64, 256_i64)]
-        {
+        for (label, rows, cols) in [
+            ("8192x1024", 8192_i64, 1024_i64),
+            ("65536x256", 65536_i64, 256_i64),
+        ] {
             let size = rows * cols;
             let input = numpy
                 .call_method1("linspace", (-1.0_f64, 1.0_f64, size))
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:3855:
             let fnp_kwargs = PyDict::new(py);
             fnp_kwargs.set_item("axis", -1_i64).expect("fnp axis kwarg");
             let numpy_kwargs = PyDict::new(py);
-            numpy_kwargs.set_item("axis", -1_i64).expect("numpy axis kwarg");
+            numpy_kwargs
+                .set_item("axis", -1_i64)
+                .expect("numpy axis kwarg");

             group.bench_function(format!("fnp_cumsum_f64_axis_last_{label}"), |bench| {
                 bench.iter(|| {
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:3914:
             .call1((0.0_f64, 1.0_f64))
             .expect("1j");
         let base = cosv
-            .call_method1("__add__", (sinv.call_method1("__mul__", (&j,)).expect("i*sin"),))
+            .call_method1(
+                "__add__",
+                (sinv.call_method1("__mul__", (&j,)).expect("i*sin"),),
+            )
             .expect("complex base")
             .call_method1("reshape", ((rows, cols),))
             .expect("complex 2-D shape");
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:3921:

         for (label, cname) in [("complex128", "complex128"), ("complex64", "complex64")] {
-            let input = base.call_method1("astype", (cname,)).expect("astype complex");
+            let input = base
+                .call_method1("astype", (cname,))
+                .expect("astype complex");
             let fnp_kwargs = PyDict::new(py);
             fnp_kwargs.set_item("axis", -1_i64).expect("fnp axis kwarg");
             let numpy_kwargs = PyDict::new(py);
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:3927:
-            numpy_kwargs.set_item("axis", -1_i64).expect("numpy axis kwarg");
+            numpy_kwargs
+                .set_item("axis", -1_i64)
+                .expect("numpy axis kwarg");

             group.bench_function(format!("fnp_cumprod_{label}_axis_last_16M"), |bench| {
                 bench.iter(|| {
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:3980:
             .call1((0.0_f64, 1.0_f64))
             .expect("1j");
         let base = cosv
-            .call_method1("__add__", (sinv.call_method1("__mul__", (&j,)).expect("i*sin"),))
+            .call_method1(
+                "__add__",
+                (sinv.call_method1("__mul__", (&j,)).expect("i*sin"),),
+            )
             .expect("complex base")
             .call_method1("reshape", ((rows, cols),))
             .expect("complex 2-D shape");
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:3987:

         for (label, cname) in [("complex128", "complex128"), ("complex64", "complex64")] {
-            let input = base.call_method1("astype", (cname,)).expect("astype complex");
+            let input = base
+                .call_method1("astype", (cname,))
+                .expect("astype complex");
             let fnp_kwargs = PyDict::new(py);
             fnp_kwargs.set_item("axis", -1_i64).expect("fnp axis kwarg");
             let numpy_kwargs = PyDict::new(py);
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:3993:
-            numpy_kwargs.set_item("axis", -1_i64).expect("numpy axis kwarg");
+            numpy_kwargs
+                .set_item("axis", -1_i64)
+                .expect("numpy axis kwarg");

             group.bench_function(format!("fnp_nancumprod_{label}_axis_last_16M"), |bench| {
                 bench.iter(|| {
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:4049:
             .call1((0.0_f64, 1.0_f64))
             .expect("1j");
         let base = cosv
-            .call_method1("__add__", (sinv.call_method1("__mul__", (&j,)).expect("i*sin"),))
+            .call_method1(
+                "__add__",
+                (sinv.call_method1("__mul__", (&j,)).expect("i*sin"),),
+            )
             .expect("complex base")
             .call_method1("reshape", ((n, n, n),))
             .expect("complex 3-D shape");
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:4056:

         for (label, cname) in [("complex128", "complex128"), ("complex64", "complex64")] {
-            let input = base.call_method1("astype", (cname,)).expect("astype complex");
+            let input = base
+                .call_method1("astype", (cname,))
+                .expect("astype complex");
             let fnp_kwargs = PyDict::new(py);
             fnp_kwargs.set_item("axis", 1_i64).expect("fnp axis kwarg");
             let numpy_kwargs = PyDict::new(py);
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:4062:
-            numpy_kwargs.set_item("axis", 1_i64).expect("numpy axis kwarg");
+            numpy_kwargs
+                .set_item("axis", 1_i64)
+                .expect("numpy axis kwarg");

             group.bench_function(format!("fnp_cumprod_{label}_axis_mid_16M"), |bench| {
                 bench.iter(|| {
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:4132:
             let cosv = numpy.call_method1("cos", (&theta,)).expect("cos");
             let sinv = numpy.call_method1("sin", (&theta,)).expect("sin");
             let base = cosv
-                .call_method1("__add__", (sinv.call_method1("__mul__", (&j,)).expect("i*sin"),))
+                .call_method1(
+                    "__add__",
+                    (sinv.call_method1("__mul__", (&j,)).expect("i*sin"),),
+                )
                 .expect("complex base")
                 .call_method1("reshape", ((n, n),))
                 .expect("complex 2-D shape");
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:4139:
             for (dlabel, cname) in [("complex128", "complex128"), ("complex64", "complex64")] {
-                let input = base.call_method1("astype", (cname,)).expect("astype complex");
+                let input = base
+                    .call_method1("astype", (cname,))
+                    .expect("astype complex");
                 let fnp_kwargs = PyDict::new(py);
                 fnp_kwargs.set_item("axis", 0_i64).expect("fnp axis kwarg");
                 let numpy_kwargs = PyDict::new(py);
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:4144:
-                numpy_kwargs.set_item("axis", 0_i64).expect("numpy axis kwarg");
+                numpy_kwargs
+                    .set_item("axis", 0_i64)
+                    .expect("numpy axis kwarg");

                 group.bench_function(format!("fnp_cumprod_{dlabel}_axis0_{label}"), |bench| {
                     bench.iter(|| {
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:4232:
         let fnp_max = module.getattr("maximum").expect("fnp maximum");
         let numpy_max = numpy.getattr("maximum").expect("numpy maximum");
         group.bench_function("fnp_maximum_accumulate_f64_8m", |b| {
-            b.iter(|| black_box(fnp_max.call_method1("accumulate", (&x,)).expect("fnp max.accum")));
+            b.iter(|| {
+                black_box(
+                    fnp_max
+                        .call_method1("accumulate", (&x,))
+                        .expect("fnp max.accum"),
+                )
+            });
         });
         group.bench_function("numpy_maximum_accumulate_f64_8m", |b| {
             b.iter(|| {
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:4253:
             .call_method1("__mod__", (1_000_003_i64,))
             .expect("xi");
         group.bench_function("fnp_maximum_accumulate_f32_8m", |b| {
-            b.iter(|| black_box(fnp_max.call_method1("accumulate", (&x32,)).expect("fnp max.accum f32")));
+            b.iter(|| {
+                black_box(
+                    fnp_max
+                        .call_method1("accumulate", (&x32,))
+                        .expect("fnp max.accum f32"),
+                )
+            });
         });
         group.bench_function("numpy_maximum_accumulate_f32_8m", |b| {
-            b.iter(|| black_box(numpy_max.call_method1("accumulate", (&x32,)).expect("np max.accum f32")));
+            b.iter(|| {
+                black_box(
+                    numpy_max
+                        .call_method1("accumulate", (&x32,))
+                        .expect("np max.accum f32"),
+                )
+            });
         });
         group.bench_function("fnp_maximum_accumulate_i64_8m", |b| {
-            b.iter(|| black_box(fnp_max.call_method1("accumulate", (&xi,)).expect("fnp max.accum i64")));
+            b.iter(|| {
+                black_box(
+                    fnp_max
+                        .call_method1("accumulate", (&xi,))
+                        .expect("fnp max.accum i64"),
+                )
+            });
         });
         group.bench_function("numpy_maximum_accumulate_i64_8m", |b| {
-            b.iter(|| black_box(numpy_max.call_method1("accumulate", (&xi,)).expect("np max.accum i64")));
+            b.iter(|| {
+                black_box(
+                    numpy_max
+                        .call_method1("accumulate", (&xi,))
+                        .expect("np max.accum i64"),
+                )
+            });
         });

         // add.accumulate(int) routes to the parallel cumsum path (== np.cumsum); a win
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:4273:
             .call_method1("arange", (8_000_000_i64,))
             .expect("8M i64 arange");
         group.bench_function("fnp_add_accumulate_i64_8m", |b| {
-            b.iter(|| black_box(fnp_add.call_method1("accumulate", (&xa,)).expect("fnp add.accum i64")));
+            b.iter(|| {
+                black_box(
+                    fnp_add
+                        .call_method1("accumulate", (&xa,))
+                        .expect("fnp add.accum i64"),
+                )
+            });
         });
         group.bench_function("numpy_add_accumulate_i64_8m", |b| {
-            b.iter(|| black_box(numpy_add.call_method1("accumulate", (&xa,)).expect("np add.accum i64")));
+            b.iter(|| {
+                black_box(
+                    numpy_add
+                        .call_method1("accumulate", (&xa,))
+                        .expect("np add.accum i64"),
+                )
+            });
         });

         // bitwise_or.accumulate(int) native two-pass prefix vs numpy serial.
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:4283:
         let fnp_or = module.getattr("bitwise_or").expect("fnp bitwise_or");
         let numpy_or = numpy.getattr("bitwise_or").expect("numpy bitwise_or");
         group.bench_function("fnp_bitwise_or_accumulate_i64_8m", |b| {
-            b.iter(|| black_box(fnp_or.call_method1("accumulate", (&xi,)).expect("fnp or.accum i64")));
+            b.iter(|| {
+                black_box(
+                    fnp_or
+                        .call_method1("accumulate", (&xi,))
+                        .expect("fnp or.accum i64"),
+                )
+            });
         });
         group.bench_function("numpy_bitwise_or_accumulate_i64_8m", |b| {
-            b.iter(|| black_box(numpy_or.call_method1("accumulate", (&xi,)).expect("np or.accum i64")));
+            b.iter(|| {
+                black_box(
+                    numpy_or
+                        .call_method1("accumulate", (&xi,))
+                        .expect("np or.accum i64"),
+                )
+            });
         });

         // logical_and/or/xor.accumulate(bool): numpy runs a serial dependency-chain scan
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:4302:
         let fnp_land = module.getattr("logical_and").expect("fnp logical_and");
         let numpy_land = numpy.getattr("logical_and").expect("numpy logical_and");
         group.bench_function("fnp_logical_and_accumulate_bool_8m", |b| {
-            b.iter(|| black_box(fnp_land.call_method1("accumulate", (&xb,)).expect("fnp land.accum bool")));
+            b.iter(|| {
+                black_box(
+                    fnp_land
+                        .call_method1("accumulate", (&xb,))
+                        .expect("fnp land.accum bool"),
+                )
+            });
         });
         group.bench_function("numpy_logical_and_accumulate_bool_8m", |b| {
-            b.iter(|| black_box(numpy_land.call_method1("accumulate", (&xb,)).expect("np land.accum bool")));
+            b.iter(|| {
+                black_box(
+                    numpy_land
+                        .call_method1("accumulate", (&xb,))
+                        .expect("np land.accum bool"),
+                )
+            });
         });
         let fnp_lor = module.getattr("logical_or").expect("fnp logical_or");
         let numpy_lor = numpy.getattr("logical_or").expect("numpy logical_or");
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:4312:
         group.bench_function("fnp_logical_or_accumulate_bool_8m", |b| {
-            b.iter(|| black_box(fnp_lor.call_method1("accumulate", (&xb,)).expect("fnp lor.accum bool")));
+            b.iter(|| {
+                black_box(
+                    fnp_lor
+                        .call_method1("accumulate", (&xb,))
+                        .expect("fnp lor.accum bool"),
+                )
+            });
         });
         group.bench_function("numpy_logical_or_accumulate_bool_8m", |b| {
-            b.iter(|| black_box(numpy_lor.call_method1("accumulate", (&xb,)).expect("np lor.accum bool")));
+            b.iter(|| {
+                black_box(
+                    numpy_lor
+                        .call_method1("accumulate", (&xb,))
+                        .expect("np lor.accum bool"),
+                )
+            });
         });
         let fnp_lxor = module.getattr("logical_xor").expect("fnp logical_xor");
         let numpy_lxor = numpy.getattr("logical_xor").expect("numpy logical_xor");
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:4320:
         group.bench_function("fnp_logical_xor_accumulate_bool_8m", |b| {
-            b.iter(|| black_box(fnp_lxor.call_method1("accumulate", (&xb,)).expect("fnp lxor.accum bool")));
+            b.iter(|| {
+                black_box(
+                    fnp_lxor
+                        .call_method1("accumulate", (&xb,))
+                        .expect("fnp lxor.accum bool"),
+                )
+            });
         });
         group.bench_function("numpy_logical_xor_accumulate_bool_8m", |b| {
-            b.iter(|| black_box(numpy_lxor.call_method1("accumulate", (&xb,)).expect("np lxor.accum bool")));
+            b.iter(|| {
+                black_box(
+                    numpy_lxor
+                        .call_method1("accumulate", (&xb,))
+                        .expect("np lxor.accum bool"),
+                )
+            });
         });
     });

Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:4362:
             let fnp_kwargs = PyDict::new(py);
             fnp_kwargs.set_item("axis", 1_i64).expect("fnp axis kwarg");
             let numpy_kwargs = PyDict::new(py);
-            numpy_kwargs.set_item("axis", 1_i64).expect("numpy axis kwarg");
+            numpy_kwargs
+                .set_item("axis", 1_i64)
+                .expect("numpy axis kwarg");

             group.bench_function(format!("fnp_cumsum_f64_axis1_{label}"), |bench| {
                 bench.iter(|| {
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:4449:
             });
             group.bench_function(format!("numpy_cumsum_i64_{label}"), |bench| {
                 bench.iter(|| {
-                    let r = numpy_cumsum.call((arr,), Some(&nk)).expect("numpy int cumsum");
+                    let r = numpy_cumsum
+                        .call((arr,), Some(&nk))
+                        .expect("numpy int cumsum");
                     black_box(r);
                 });
             });
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:4474:
         let fnp_vander = module.getattr("vander").expect("fnp_python.vander");
         let numpy_vander = numpy.getattr("vander").expect("numpy.vander");

-        for (label, n, cols) in [("200k_x8", 200_000_i64, 8_i64), ("500k_x12", 500_000_i64, 12_i64)]
-        {
+        for (label, n, cols) in [
+            ("200k_x8", 200_000_i64, 8_i64),
+            ("500k_x12", 500_000_i64, 12_i64),
+        ] {
             let x = numpy
                 .call_method1("linspace", (-1.5_f64, 1.5_f64, n))
                 .expect("vander x input");
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:4521:
         let fnp_polyval = module.getattr("polyval").expect("fnp_python.polyval");
         let numpy_polyval = numpy.getattr("polyval").expect("numpy.polyval");

-        for (label, n, deg) in [("1M_deg5", 1_000_000_i64, 5_i64), ("4M_deg8", 4_000_000_i64, 8_i64)]
-        {
+        for (label, n, deg) in [
+            ("1M_deg5", 1_000_000_i64, 5_i64),
+            ("4M_deg8", 4_000_000_i64, 8_i64),
+        ] {
             let x = numpy
                 .call_method1("linspace", (-3.0_f64, 3.0_f64, n))
                 .expect("polyval x input");
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:4532:

             group.bench_function(format!("fnp_polyval_f64_{label}"), |bench| {
                 bench.iter(|| {
-                    let result = fnp_polyval
-                        .call1((&p, &x))
-                        .expect("fnp polyval call");
+                    let result = fnp_polyval.call1((&p, &x)).expect("fnp polyval call");
                     black_box(result);
                 });
             });
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:4541:
             group.bench_function(format!("numpy_polyval_f64_{label}"), |bench| {
                 bench.iter(|| {
-                    let result = numpy_polyval
-                        .call1((&p, &x))
-                        .expect("numpy polyval call");
+                    let result = numpy_polyval.call1((&p, &x)).expect("numpy polyval call");
                     black_box(result);
                 });
             });
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:4776:
             let fnp_kwargs = PyDict::new(py);
             fnp_kwargs.set_item("axis", 0_i64).expect("fnp axis kwarg");
             let numpy_kwargs = PyDict::new(py);
-            numpy_kwargs.set_item("axis", 0_i64).expect("numpy axis kwarg");
+            numpy_kwargs
+                .set_item("axis", 0_i64)
+                .expect("numpy axis kwarg");

             group.bench_function(format!("fnp_gradient_f64_axis0_{label}"), |bench| {
                 bench.iter(|| {
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:4803:
             });
             group.bench_function(format!("numpy_gradient_f64_full_{label}"), |bench| {
                 bench.iter(|| {
-                    let result = numpy_grad.call1((&input,)).expect("numpy gradient full call");
+                    let result = numpy_grad
+                        .call1((&input,))
+                        .expect("numpy gradient full call");
                     black_box(result);
                 });
             });
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:4869:
             });

             let fnp_l1_kwargs = PyDict::new(py);
-            fnp_l1_kwargs.set_item("ord", 1_i64).expect("fnp l1 ord kwarg");
             fnp_l1_kwargs
+                .set_item("ord", 1_i64)
+                .expect("fnp l1 ord kwarg");
+            fnp_l1_kwargs
                 .set_item("axis", -1_i64)
                 .expect("fnp l1 axis kwarg");
             let numpy_l1_kwargs = PyDict::new(py);
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:4901:

             let inf = f64::INFINITY;
             let fnp_inf_kwargs = PyDict::new(py);
-            fnp_inf_kwargs.set_item("ord", inf).expect("fnp inf ord kwarg");
             fnp_inf_kwargs
+                .set_item("ord", inf)
+                .expect("fnp inf ord kwarg");
+            fnp_inf_kwargs
                 .set_item("axis", -1_i64)
                 .expect("fnp inf axis kwarg");
             let numpy_inf_kwargs = PyDict::new(py);
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:4970:
         .expect("norm f32 setup");
         let a = ns.get_item("a").expect("a");
         let inf = ns.get_item("inf").expect("inf");
-        let fnp_norm = module.getattr("linalg").unwrap().getattr("norm").expect("fnp norm");
-        let numpy_norm = numpy.getattr("linalg").unwrap().getattr("norm").expect("np norm");
+        let fnp_norm = module
+            .getattr("linalg")
+            .unwrap()
+            .getattr("norm")
+            .expect("fnp norm");
+        let numpy_norm = numpy
+            .getattr("linalg")
+            .unwrap()
+            .getattr("norm")
+            .expect("np norm");
         for (label, ordv) in [("maxabs", inf.clone())] {
             let kw = PyDict::new(py);
             kw.set_item("ord", &ordv).unwrap();
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:5030:
                 fnp_kwargs.set_item("ord", ord_val).expect("fnp ord kwarg");
                 fnp_kwargs.set_item("axis", axis).expect("fnp axis kwarg");
                 let numpy_kwargs = PyDict::new(py);
-                numpy_kwargs.set_item("ord", ord_val).expect("numpy ord kwarg");
-                numpy_kwargs.set_item("axis", axis).expect("numpy axis kwarg");
+                numpy_kwargs
+                    .set_item("ord", ord_val)
+                    .expect("numpy ord kwarg");
+                numpy_kwargs
+                    .set_item("axis", axis)
+                    .expect("numpy axis kwarg");

                 group.bench_function(format!("fnp_norm_{ord_label}_{label}"), |bench| {
                     bench.iter(|| {
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:5401:
         kw.set_item("axis", 1_i64).unwrap();
         let kw2 = kw.clone();
         group.bench_function("fnp_compress_2d_axis1", |b| {
-            b.iter(|| black_box(fnp_compress.call((&cond, &x), Some(&kw)).expect("fnp compress")));
+            b.iter(|| {
+                black_box(
+                    fnp_compress
+                        .call((&cond, &x), Some(&kw))
+                        .expect("fnp compress"),
+                )
+            });
         });
         group.bench_function("numpy_compress_2d_axis1", |b| {
-            b.iter(|| black_box(numpy_compress.call((&cond, &x), Some(&kw2)).expect("np compress")));
+            b.iter(|| {
+                black_box(
+                    numpy_compress
+                        .call((&cond, &x), Some(&kw2))
+                        .expect("np compress"),
+                )
+            });
         });
     });

Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:5513:
         let shifts = (3_i64, 5_i64);
         let axes = (0_i64, 1_i64);
         group.bench_function("fnp_roll_2d_multi_int64", |b| {
-            b.iter(|| {
-                black_box(
-                    fnp_roll
-                        .call1((&x, shifts, axes))
-                        .expect("fnp roll"),
-                )
-            });
+            b.iter(|| black_box(fnp_roll.call1((&x, shifts, axes)).expect("fnp roll")));
         });
         group.bench_function("numpy_roll_2d_multi_int64", |b| {
-            b.iter(|| {
-                black_box(
-                    numpy_roll
-                        .call1((&x, shifts, axes))
-                        .expect("np roll"),
-                )
-            });
+            b.iter(|| black_box(numpy_roll.call1((&x, shifts, axes)).expect("np roll")));
         });
     });

Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:6403:
         .expect("int matpow setup");
         let imp = imp_ns.get_item("imp").expect("imp");
         group.bench_function("fnp_matrix_power_i64_256_n5", |bench| {
-            bench.iter(|| black_box(fnp_matrix_power.call1((&imp, 5_i64)).expect("fnp int matpow")));
+            bench.iter(|| {
+                black_box(
+                    fnp_matrix_power
+                        .call1((&imp, 5_i64))
+                        .expect("fnp int matpow"),
+                )
+            });
         });
         group.bench_function("numpy_matrix_power_i64_256_n5", |bench| {
-            bench.iter(|| black_box(numpy_matrix_power.call1((&imp, 5_i64)).expect("np int matpow")));
+            bench.iter(|| {
+                black_box(
+                    numpy_matrix_power
+                        .call1((&imp, 5_i64))
+                        .expect("np int matpow"),
+                )
+            });
         });

         for (label, input) in [
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:6460:
             .expect("8M base")
             .call_method1("__sub__", (4_000_000_i64,))
             .expect("centered base");
-        let f32_in = base.call_method1("astype", ("float32",)).expect("f32 input");
+        let f32_in = base
+            .call_method1("astype", ("float32",))
+            .expect("f32 input");
         let i64_in = base.call_method1("astype", ("int64",)).expect("i64 input");
         let i32_in = base.call_method1("astype", ("int32",)).expect("i32 input");
         let u64_in = base.call_method1("astype", ("uint64",)).expect("u64 input");
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:6521:
         let fnp_clip = module.getattr("clip").expect("fnp clip");
         let numpy_clip = numpy.getattr("clip").expect("numpy clip");
         group.bench_function("fnp_clip_f64_8m", |b| {
-            b.iter(|| black_box(fnp_clip.call1((&input, -1000.0_f64, 1000.0_f64)).expect("fnp clip")));
+            b.iter(|| {
+                black_box(
+                    fnp_clip
+                        .call1((&input, -1000.0_f64, 1000.0_f64))
+                        .expect("fnp clip"),
+                )
+            });
         });
         group.bench_function("numpy_clip_f64_8m", |b| {
-            b.iter(|| black_box(numpy_clip.call1((&input, -1000.0_f64, 1000.0_f64)).expect("numpy clip")));
+            b.iter(|| {
+                black_box(
+                    numpy_clip
+                        .call1((&input, -1000.0_f64, 1000.0_f64))
+                        .expect("numpy clip"),
+                )
+            });
         });

         let input_f32 = input
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:6605:
             b.iter(|| black_box(fnp_conv.call1((&a, &v, "full")).expect("fnp int convolve")));
         });
         group.bench_function("numpy_convolve_i64_200k_256", |b| {
-            b.iter(|| black_box(numpy_conv.call1((&a, &v, "full")).expect("numpy int convolve")));
+            b.iter(|| {
+                black_box(
+                    numpy_conv
+                        .call1((&a, &v, "full"))
+                        .expect("numpy int convolve"),
+                )
+            });
         });
     });

Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:6649:
             b.iter(|| black_box(fnp_conv.call1((&a, &v, "same")).expect("fnp convolve")));
         });
         group.bench_function("numpy_convolve_f64_1m_256_same", |b| {
-            b.iter(|| {
-                black_box(
-                    numpy_conv
-                        .call1((&a, &v, "same"))
-                        .expect("numpy convolve"),
-                )
-            });
+            b.iter(|| black_box(numpy_conv.call1((&a, &v, "same")).expect("numpy convolve")));
         });
         group.bench_function("fnp_correlate_f64_1m_256_valid", |b| {
             b.iter(|| black_box(fnp_corr.call1((&a, &v, "valid")).expect("fnp correlate")));
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:6754:
         let ia32 = ia.call_method1("astype", ("int32",)).expect("ia32");
         let ib32 = ib.call_method1("astype", ("int32",)).expect("ib32");
         group.bench_function("fnp_where_i32_8m", |bn| {
-            bn.iter(|| black_box(fnp_where.call1((&imask, &ia32, &ib32)).expect("fnp where i32")));
+            bn.iter(|| {
+                black_box(
+                    fnp_where
+                        .call1((&imask, &ia32, &ib32))
+                        .expect("fnp where i32"),
+                )
+            });
         });
         group.bench_function("numpy_where_i32_8m", |bn| {
             bn.iter(|| {
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:6805:
             .call_method1("view", ("complex128",))
             .expect("c128 view");
         group.bench_function("fnp_around_c128_4m", |b| {
-            b.iter(|| black_box(fnp_around.call1((&input_c, 3_i64)).expect("fnp around c128")));
+            b.iter(|| {
+                black_box(
+                    fnp_around
+                        .call1((&input_c, 3_i64))
+                        .expect("fnp around c128"),
+                )
+            });
         });
         group.bench_function("numpy_around_c128_4m", |b| {
-            b.iter(|| black_box(numpy_around.call1((&input_c, 3_i64)).expect("numpy around c128")));
+            b.iter(|| {
+                black_box(
+                    numpy_around
+                        .call1((&input_c, 3_i64))
+                        .expect("numpy around c128"),
+                )
+            });
         });

         // f32 sibling — compute-heavy (round-ties-even + mul/div) so wins at 4-byte.
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:6815:
-        let input32 = input.call_method1("astype", ("float32",)).expect("f32 input");
+        let input32 = input
+            .call_method1("astype", ("float32",))
+            .expect("f32 input");
         group.bench_function("fnp_around_f32_8m", |b| {
             b.iter(|| black_box(fnp_around.call1((&input32, 3_i64)).expect("fnp around f32")));
         });
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:6819:
         group.bench_function("numpy_around_f32_8m", |b| {
-            b.iter(|| black_box(numpy_around.call1((&input32, 3_i64)).expect("numpy around f32")));
+            b.iter(|| {
+                black_box(
+                    numpy_around
+                        .call1((&input32, 3_i64))
+                        .expect("numpy around f32"),
+                )
+            });
         });
     });

Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:7052:
         // width and asymmetric tuple width; panics on any mismatch.
         for (arr, label) in [(&x, "f64"), (&xi, "i32")] {
             let scalar = (
-                fnp_pad.call1((arr, 4000_i64, "edge")).expect("fnp pad edge scalar"),
-                numpy_pad.call1((arr, 4000_i64, "edge")).expect("numpy pad edge scalar"),
+                fnp_pad
+                    .call1((arr, 4000_i64, "edge"))
+                    .expect("fnp pad edge scalar"),
+                numpy_pad
+                    .call1((arr, 4000_i64, "edge"))
+                    .expect("numpy pad edge scalar"),
             );
             let tuple = (
-                fnp_pad.call1((arr, (3_i64, 7_i64), "edge")).expect("fnp pad edge tuple"),
-                numpy_pad.call1((arr, (3_i64, 7_i64), "edge")).expect("numpy pad edge tuple"),
+                fnp_pad
+                    .call1((arr, (3_i64, 7_i64), "edge"))
+                    .expect("fnp pad edge tuple"),
+                numpy_pad
+                    .call1((arr, (3_i64, 7_i64), "edge"))
+                    .expect("numpy pad edge tuple"),
             );
             for (f, n) in [scalar, tuple] {
                 let eq: bool = np_array_equal
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:7069:
             }
         }
         group.bench_function("fnp_pad_edge_f64_8m", |b| {
-            b.iter(|| black_box(fnp_pad.call1((&x, 4000_i64, "edge")).expect("fnp pad edge f64")));
+            b.iter(|| {
+                black_box(
+                    fnp_pad
+                        .call1((&x, 4000_i64, "edge"))
+                        .expect("fnp pad edge f64"),
+                )
+            });
         });
         group.bench_function("numpy_pad_edge_f64_8m", |b| {
-            b.iter(|| black_box(numpy_pad.call1((&x, 4000_i64, "edge")).expect("numpy pad edge f64")));
+            b.iter(|| {
+                black_box(
+                    numpy_pad
+                        .call1((&x, 4000_i64, "edge"))
+                        .expect("numpy pad edge f64"),
+                )
+            });
         });
         group.bench_function("fnp_pad_edge_i32_8m", |b| {
-            b.iter(|| black_box(fnp_pad.call1((&xi, 4000_i64, "edge")).expect("fnp pad edge i32")));
+            b.iter(|| {
+                black_box(
+                    fnp_pad
+                        .call1((&xi, 4000_i64, "edge"))
+                        .expect("fnp pad edge i32"),
+                )
+            });
         });
         group.bench_function("numpy_pad_edge_i32_8m", |b| {
-            b.iter(|| black_box(numpy_pad.call1((&xi, 4000_i64, "edge")).expect("numpy pad edge i32")));
+            b.iter(|| {
+                black_box(
+                    numpy_pad
+                        .call1((&xi, 4000_i64, "edge"))
+                        .expect("numpy pad edge i32"),
+                )
+            });
         });
     });
     group.finish();
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:7122:
         // width and asymmetric tuple width; panics on any mismatch.
         for (arr, label) in [(&x, "f64"), (&xi, "i32")] {
             let scalar = (
-                fnp_pad.call1((arr, 4000_i64, "wrap")).expect("fnp pad wrap scalar"),
-                numpy_pad.call1((arr, 4000_i64, "wrap")).expect("numpy pad wrap scalar"),
+                fnp_pad
+                    .call1((arr, 4000_i64, "wrap"))
+                    .expect("fnp pad wrap scalar"),
+                numpy_pad
+                    .call1((arr, 4000_i64, "wrap"))
+                    .expect("numpy pad wrap scalar"),
             );
             let tuple = (
-                fnp_pad.call1((arr, (3_i64, 7_i64), "wrap")).expect("fnp pad wrap tuple"),
-                numpy_pad.call1((arr, (3_i64, 7_i64), "wrap")).expect("numpy pad wrap tuple"),
+                fnp_pad
+                    .call1((arr, (3_i64, 7_i64), "wrap"))
+                    .expect("fnp pad wrap tuple"),
+                numpy_pad
+                    .call1((arr, (3_i64, 7_i64), "wrap"))
+                    .expect("numpy pad wrap tuple"),
             );
             for (f, n) in [scalar, tuple] {
                 let eq: bool = np_array_equal
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:7152:
             .expect("bool");
         assert!(eq, "pad wrap multi-tile correctness mismatch");
         group.bench_function("fnp_pad_wrap_f64_8m", |b| {
-            b.iter(|| black_box(fnp_pad.call1((&x, 4000_i64, "wrap")).expect("fnp pad wrap f64")));
+            b.iter(|| {
+                black_box(
+                    fnp_pad
+                        .call1((&x, 4000_i64, "wrap"))
+                        .expect("fnp pad wrap f64"),
+                )
+            });
         });
         group.bench_function("numpy_pad_wrap_f64_8m", |b| {
-            b.iter(|| black_box(numpy_pad.call1((&x, 4000_i64, "wrap")).expect("numpy pad wrap f64")));
+            b.iter(|| {
+                black_box(
+                    numpy_pad
+                        .call1((&x, 4000_i64, "wrap"))
+                        .expect("numpy pad wrap f64"),
+                )
+            });
         });
         group.bench_function("fnp_pad_wrap_i32_8m", |b| {
-            b.iter(|| black_box(fnp_pad.call1((&xi, 4000_i64, "wrap")).expect("fnp pad wrap i32")));
+            b.iter(|| {
+                black_box(
+                    fnp_pad
+                        .call1((&xi, 4000_i64, "wrap"))
+                        .expect("fnp pad wrap i32"),
+                )
+            });
         });
         group.bench_function("numpy_pad_wrap_i32_8m", |b| {
-            b.iter(|| black_box(numpy_pad.call1((&xi, 4000_i64, "wrap")).expect("numpy pad wrap i32")));
+            b.iter(|| {
+                black_box(
+                    numpy_pad
+                        .call1((&xi, 4000_i64, "wrap"))
+                        .expect("numpy pad wrap i32"),
+                )
+            });
         });
         group.bench_function("fnp_pad_wrap_i32_multitile_8m", |b| {
             b.iter(|| {
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:7222:
             for (arr, label) in [(&x, "f64"), (&xi, "i32")] {
                 let scalar = (
                     fnp_pad.call1((arr, 4000_i64, md)).expect("fnp pad scalar"),
-                    numpy_pad.call1((arr, 4000_i64, md)).expect("numpy pad scalar"),
+                    numpy_pad
+                        .call1((arr, 4000_i64, md))
+                        .expect("numpy pad scalar"),
                 );
                 let tuple = (
-                    fnp_pad.call1((arr, (3_i64, 7_i64), md)).expect("fnp pad tuple"),
-                    numpy_pad.call1((arr, (3_i64, 7_i64), md)).expect("numpy pad tuple"),
+                    fnp_pad
+                        .call1((arr, (3_i64, 7_i64), md))
+                        .expect("fnp pad tuple"),
+                    numpy_pad
+                        .call1((arr, (3_i64, 7_i64), md))
+                        .expect("numpy pad tuple"),
                 );
                 for (f, n) in [scalar, tuple] {
                     let eq: bool = np_array_equal
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:7239:
             }
         }
         group.bench_function("fnp_pad_reflect_f64_8m", |b| {
-            b.iter(|| black_box(fnp_pad.call1((&x, 4000_i64, "reflect")).expect("fnp reflect f64")));
+            b.iter(|| {
+                black_box(
+                    fnp_pad
+                        .call1((&x, 4000_i64, "reflect"))
+                        .expect("fnp reflect f64"),
+                )
+            });
         });
         group.bench_function("numpy_pad_reflect_f64_8m", |b| {
-            b.iter(|| black_box(numpy_pad.call1((&x, 4000_i64, "reflect")).expect("numpy reflect f64")));
+            b.iter(|| {
+                black_box(
+                    numpy_pad
+                        .call1((&x, 4000_i64, "reflect"))
+                        .expect("numpy reflect f64"),
+                )
+            });
         });
         group.bench_function("fnp_pad_symmetric_i32_8m", |b| {
-            b.iter(|| black_box(fnp_pad.call1((&xi, 4000_i64, "symmetric")).expect("fnp symmetric i32")));
+            b.iter(|| {
+                black_box(
+                    fnp_pad
+                        .call1((&xi, 4000_i64, "symmetric"))
+                        .expect("fnp symmetric i32"),
+                )
+            });
         });
         group.bench_function("numpy_pad_symmetric_i32_8m", |b| {
-            b.iter(|| black_box(numpy_pad.call1((&xi, 4000_i64, "symmetric")).expect("numpy symmetric i32")));
+            b.iter(|| {
+                black_box(
+                    numpy_pad
+                        .call1((&xi, 4000_i64, "symmetric"))
+                        .expect("numpy symmetric i32"),
+                )
+            });
         });
     });
     group.finish();
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:7404:
         kw.set_item("return_inverse", true).expect("rinv");
         kw.set_item("return_counts", true).expect("rc");
         {
-            let got = fnp_unique.call((&a,), Some(&kw)).expect("fnp unique full S8");
-            let exp = numpy_unique.call((&a,), Some(&kw)).expect("numpy unique full S8");
+            let got = fnp_unique
+                .call((&a,), Some(&kw))
+                .expect("fnp unique full S8");
+            let exp = numpy_unique
+                .call((&a,), Some(&kw))
+                .expect("numpy unique full S8");
             let np_array_equal = numpy.getattr("array_equal").expect("np.array_equal");
             for t in 0..4usize {
                 let g = got.get_item(t).expect("got item");
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:7415:
                     .expect("array_equal")
                     .extract()
                     .expect("bool");
-                assert!(eq, "string unique return_* correctness mismatch at tuple index {t}");
+                assert!(
+                    eq,
+                    "string unique return_* correctness mismatch at tuple index {t}"
+                );
             }
         }
         group.bench_function("fnp_unique_S8_full_2m", |b| {
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:7422:
-            b.iter(|| black_box(fnp_unique.call((&a,), Some(&kw)).expect("fnp unique full S8")));
+            b.iter(|| {
+                black_box(
+                    fnp_unique
+                        .call((&a,), Some(&kw))
+                        .expect("fnp unique full S8"),
+                )
+            });
         });
         group.bench_function("numpy_unique_S8_full_2m", |b| {
-            b.iter(|| black_box(numpy_unique.call((&a,), Some(&kw)).expect("numpy unique full S8")));
+            b.iter(|| {
+                black_box(
+                    numpy_unique
+                        .call((&a,), Some(&kw))
+                        .expect("numpy unique full S8"),
+                )
+            });
         });
     });
     group.finish();
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:7464:
         for side in ["left", "right"] {
             let kw = PyDict::new(py);
             kw.set_item("side", side).unwrap();
-            let f = fnp_ss.call((&h8, &q8), Some(&kw)).expect("fnp searchsorted");
-            let n = numpy_ss.call((&h8, &q8), Some(&kw)).expect("numpy searchsorted");
+            let f = fnp_ss
+                .call((&h8, &q8), Some(&kw))
+                .expect("fnp searchsorted");
+            let n = numpy_ss
+                .call((&h8, &q8), Some(&kw))
+                .expect("numpy searchsorted");
             let eq: bool = np_array_equal
                 .call1((&f, &n))
                 .expect("array_equal")
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:7621:
         let np_array_equal = numpy.getattr("array_equal").expect("np.array_equal");
         // Correctness gate for both ops.
         for op in ["intersect1d", "setdiff1d"] {
-            let f = module.getattr(op).unwrap().call1((&a, &b)).expect("fnp setop");
-            let n = numpy.getattr(op).unwrap().call1((&a, &b)).expect("numpy setop");
+            let f = module
+                .getattr(op)
+                .unwrap()
+                .call1((&a, &b))
+                .expect("fnp setop");
+            let n = numpy
+                .getattr(op)
+                .unwrap()
+                .call1((&a, &b))
+                .expect("numpy setop");
             let eq: bool = np_array_equal
                 .call1((&f, &n))
                 .expect("array_equal")
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:7737:
             assert!(eq, "bytes {op} correctness mismatch");
         }
         {
-            let f = module.getattr("union1d").unwrap().call1((&a, &b)).expect("fnp union1d");
-            let n = numpy.getattr("union1d").unwrap().call1((&a, &b)).expect("numpy union1d");
+            let f = module
+                .getattr("union1d")
+                .unwrap()
+                .call1((&a, &b))
+                .expect("fnp union1d");
+            let n = numpy
+                .getattr("union1d")
+                .unwrap()
+                .call1((&a, &b))
+                .expect("numpy union1d");
             let eq: bool = np_array_equal.call1((&f, &n)).unwrap().extract().unwrap();
             assert!(eq, "bytes union1d correctness mismatch");
         }
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:7748:
         let numpy_uniq = numpy.getattr("unique").unwrap();
         let fnp_uni = module.getattr("union1d").unwrap();
         let numpy_uni = numpy.getattr("union1d").unwrap();
-        group.bench_function("fnp_sort_S8_2m", |bn| bn.iter(|| black_box(fnp_sort.call1((&a,)).unwrap())));
-        group.bench_function("numpy_sort_S8_2m", |bn| bn.iter(|| black_box(numpy_sort.call1((&a,)).unwrap())));
-        group.bench_function("fnp_unique_S8_2m", |bn| bn.iter(|| black_box(fnp_uniq.call1((&a,)).unwrap())));
-        group.bench_function("numpy_unique_S8_2m", |bn| bn.iter(|| black_box(numpy_uniq.call1((&a,)).unwrap())));
-        group.bench_function("fnp_union1d_S8_2m", |bn| bn.iter(|| black_box(fnp_uni.call1((&a, &b)).unwrap())));
-        group.bench_function("numpy_union1d_S8_2m", |bn| bn.iter(|| black_box(numpy_uni.call1((&a, &b)).unwrap())));
+        group.bench_function("fnp_sort_S8_2m", |bn| {
+            bn.iter(|| black_box(fnp_sort.call1((&a,)).unwrap()))
+        });
+        group.bench_function("numpy_sort_S8_2m", |bn| {
+            bn.iter(|| black_box(numpy_sort.call1((&a,)).unwrap()))
+        });
+        group.bench_function("fnp_unique_S8_2m", |bn| {
+            bn.iter(|| black_box(fnp_uniq.call1((&a,)).unwrap()))
+        });
+        group.bench_function("numpy_unique_S8_2m", |bn| {
+            bn.iter(|| black_box(numpy_uniq.call1((&a,)).unwrap()))
+        });
+        group.bench_function("fnp_union1d_S8_2m", |bn| {
+            bn.iter(|| black_box(fnp_uni.call1((&a, &b)).unwrap()))
+        });
+        group.bench_function("numpy_union1d_S8_2m", |bn| {
+            bn.iter(|| black_box(numpy_uni.call1((&a, &b)).unwrap()))
+        });
     });
     group.finish();
 }
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:7783:
 trand = rng.integers(97, 123, (100_000, 8), dtype=np.uint8).view('S8').reshape(-1)\n\
 test = np.concatenate([a[:100_000], trand])\n";
         let ns = PyDict::new(py);
-        py.run(std::ffi::CString::new(setup).unwrap().as_c_str(), Some(&ns), Some(&ns)).expect("setup");
+        py.run(
+            std::ffi::CString::new(setup).unwrap().as_c_str(),
+            Some(&ns),
+            Some(&ns),
+        )
+        .expect("setup");
         let a = ns.get_item("a").unwrap();
         let h = ns.get_item("h").unwrap();
         let q = ns.get_item("q").unwrap();
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:7791:
         let test = ns.get_item("test").unwrap();
         let eqf = numpy.getattr("array_equal").unwrap();
         // Correctness for all 5 ops.
-        let ss_f = module.getattr("searchsorted").unwrap().call1((&h, &q)).unwrap();
-        let ss_n = numpy.getattr("searchsorted").unwrap().call1((&h, &q)).unwrap();
-        assert!(eqf.call1((&ss_f, &ss_n)).unwrap().extract::<bool>().unwrap(), "S searchsorted mismatch");
+        let ss_f = module
+            .getattr("searchsorted")
+            .unwrap()
+            .call1((&h, &q))
+            .unwrap();
+        let ss_n = numpy
+            .getattr("searchsorted")
+            .unwrap()
+            .call1((&h, &q))
+            .unwrap();
+        assert!(
+            eqf.call1((&ss_f, &ss_n))
+                .unwrap()
+                .extract::<bool>()
+                .unwrap(),
+            "S searchsorted mismatch"
+        );
         let is_f = module.getattr("isin").unwrap().call1((&a, &test)).unwrap();
         let is_n = numpy.getattr("isin").unwrap().call1((&a, &test)).unwrap();
-        assert!(eqf.call1((&is_f, &is_n)).unwrap().extract::<bool>().unwrap(), "S isin mismatch");
+        assert!(
+            eqf.call1((&is_f, &is_n))
+                .unwrap()
+                .extract::<bool>()
+                .unwrap(),
+            "S isin mismatch"
+        );
         for op in ["intersect1d", "setdiff1d", "setxor1d"] {
             let f = module.getattr(op).unwrap().call1((&a, &b)).unwrap();
             let n = numpy.getattr(op).unwrap().call1((&a, &b)).unwrap();
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:7803:
-            assert!(eqf.call1((&f, &n)).unwrap().extract::<bool>().unwrap(), "S {op} mismatch");
+            assert!(
+                eqf.call1((&f, &n)).unwrap().extract::<bool>().unwrap(),
+                "S {op} mismatch"
+            );
         }
         let ss_ff = module.getattr("searchsorted").unwrap();
         let ss_nn = numpy.getattr("searchsorted").unwrap();
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:7808:
         let is_nn = numpy.getattr("isin").unwrap();
         let xr_ff = module.getattr("setxor1d").unwrap();
         let xr_nn = numpy.getattr("setxor1d").unwrap();
-        group.bench_function("fnp_searchsorted_S8_2m", |bn| bn.iter(|| black_box(ss_ff.call1((&h, &q)).unwrap())));
-        group.bench_function("numpy_searchsorted_S8_2m", |bn| bn.iter(|| black_box(ss_nn.call1((&h, &q)).unwrap())));
-        group.bench_function("fnp_isin_S8_2m", |bn| bn.iter(|| black_box(is_ff.call1((&a, &test)).unwrap())));
-        group.bench_function("numpy_isin_S8_2m", |bn| bn.iter(|| black_box(is_nn.call1((&a, &test)).unwrap())));
-        group.bench_function("fnp_setxor1d_S8_2m", |bn| bn.iter(|| black_box(xr_ff.call1((&a, &b)).unwrap())));
-        group.bench_function("numpy_setxor1d_S8_2m", |bn| bn.iter(|| black_box(xr_nn.call1((&a, &b)).unwrap())));
+        group.bench_function("fnp_searchsorted_S8_2m", |bn| {
+            bn.iter(|| black_box(ss_ff.call1((&h, &q)).unwrap()))
+        });
+        group.bench_function("numpy_searchsorted_S8_2m", |bn| {
+            bn.iter(|| black_box(ss_nn.call1((&h, &q)).unwrap()))
+        });
+        group.bench_function("fnp_isin_S8_2m", |bn| {
+            bn.iter(|| black_box(is_ff.call1((&a, &test)).unwrap()))
+        });
+        group.bench_function("numpy_isin_S8_2m", |bn| {
+            bn.iter(|| black_box(is_nn.call1((&a, &test)).unwrap()))
+        });
+        group.bench_function("fnp_setxor1d_S8_2m", |bn| {
+            bn.iter(|| black_box(xr_ff.call1((&a, &b)).unwrap()))
+        });
+        group.bench_function("numpy_setxor1d_S8_2m", |bn| {
+            bn.iter(|| black_box(xr_nn.call1((&a, &b)).unwrap()))
+        });
     });
     group.finish();
 }
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:7839:
 im = rng.integers(0, 1000, 2_000_000).astype(np.float64)\n\
 c = re + 1j * im\n";
         let ns = PyDict::new(py);
-        py.run(std::ffi::CString::new(setup).unwrap().as_c_str(), Some(&ns), Some(&ns)).expect("setup");
+        py.run(
+            std::ffi::CString::new(setup).unwrap().as_c_str(),
+            Some(&ns),
+            Some(&ns),
+        )
+        .expect("setup");
         let cc = ns.get_item("c").expect("c");
         let fnp_u = module.getattr("unique").expect("fnp unique");
         let numpy_u = numpy.getattr("unique").expect("numpy unique");
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:7846:
         let eqf = numpy.getattr("array_equal").expect("np.array_equal");
         let f = fnp_u.call1((&cc,)).expect("fnp unique");
         let n = numpy_u.call1((&cc,)).expect("numpy unique");
-        assert!(eqf.call1((&f, &n)).unwrap().extract::<bool>().unwrap(), "complex unique mismatch");
-        group.bench_function("fnp_unique_c128_2m", |bn| bn.iter(|| black_box(fnp_u.call1((&cc,)).unwrap())));
-        group.bench_function("numpy_unique_c128_2m", |bn| bn.iter(|| black_box(numpy_u.call1((&cc,)).unwrap())));
+        assert!(
+            eqf.call1((&f, &n)).unwrap().extract::<bool>().unwrap(),
+            "complex unique mismatch"
+        );
+        group.bench_function("fnp_unique_c128_2m", |bn| {
+            bn.iter(|| black_box(fnp_u.call1((&cc,)).unwrap()))
+        });
+        group.bench_function("numpy_unique_c128_2m", |bn| {
+            bn.iter(|| black_box(numpy_u.call1((&cc,)).unwrap()))
+        });
     });
     group.finish();
 }
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:7873:
 im = rng.integers(0, 1000, 2_000_000).astype(np.float32)\n\
 c = (re + 1j * im).astype(np.complex64)\n";
         let ns = PyDict::new(py);
-        py.run(std::ffi::CString::new(setup).unwrap().as_c_str(), Some(&ns), Some(&ns)).expect("setup");
+        py.run(
+            std::ffi::CString::new(setup).unwrap().as_c_str(),
+            Some(&ns),
+            Some(&ns),
+        )
+        .expect("setup");
         let cc = ns.get_item("c").expect("c");
         let fnp_u = module.getattr("unique").expect("fnp unique");
         let numpy_u = numpy.getattr("unique").expect("numpy unique");
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:7880:
         let eqf = numpy.getattr("array_equal").expect("np.array_equal");
         let f = fnp_u.call1((&cc,)).expect("fnp unique");
         let n = numpy_u.call1((&cc,)).expect("numpy unique");
-        assert!(eqf.call1((&f, &n)).unwrap().extract::<bool>().unwrap(), "complex64 unique mismatch");
-        group.bench_function("fnp_unique_c64_2m", |bn| bn.iter(|| black_box(fnp_u.call1((&cc,)).unwrap())));
-        group.bench_function("numpy_unique_c64_2m", |bn| bn.iter(|| black_box(numpy_u.call1((&cc,)).unwrap())));
+        assert!(
+            eqf.call1((&f, &n)).unwrap().extract::<bool>().unwrap(),
+            "complex64 unique mismatch"
+        );
+        group.bench_function("fnp_unique_c64_2m", |bn| {
+            bn.iter(|| black_box(fnp_u.call1((&cc,)).unwrap()))
+        });
+        group.bench_function("numpy_unique_c64_2m", |bn| {
+            bn.iter(|| black_box(numpy_u.call1((&cc,)).unwrap()))
+        });
     });
     group.finish();
 }
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:7906:
 h = np.sort(rng.integers(0, 1000, 2_000_000).astype(np.float64) + 1j * rng.integers(0, 1000, 2_000_000).astype(np.float64))\n\
 q = rng.integers(0, 1000, 2_000_000).astype(np.float64) + 1j * rng.integers(0, 1000, 2_000_000).astype(np.float64)\n";
         let ns = PyDict::new(py);
-        py.run(std::ffi::CString::new(setup).unwrap().as_c_str(), Some(&ns), Some(&ns)).expect("setup");
+        py.run(
+            std::ffi::CString::new(setup).unwrap().as_c_str(),
+            Some(&ns),
+            Some(&ns),
+        )
+        .expect("setup");
         let h = ns.get_item("h").expect("h");
         let q = ns.get_item("q").expect("q");
         let fnp_ss = module.getattr("searchsorted").expect("fnp searchsorted");
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:7917:
             kw.set_item("side", side).unwrap();
             let f = fnp_ss.call((&h, &q), Some(&kw)).expect("fnp ss");
             let n = numpy_ss.call((&h, &q), Some(&kw)).expect("numpy ss");
-            assert!(eqf.call1((&f, &n)).unwrap().extract::<bool>().unwrap(), "complex searchsorted mismatch side={side}");
+            assert!(
+                eqf.call1((&f, &n)).unwrap().extract::<bool>().unwrap(),
+                "complex searchsorted mismatch side={side}"
+            );
         }
-        group.bench_function("fnp_searchsorted_c128_2m", |bn| bn.iter(|| black_box(fnp_ss.call1((&h, &q)).unwrap())));
-        group.bench_function("numpy_searchsorted_c128_2m", |bn| bn.iter(|| black_box(numpy_ss.call1((&h, &q)).unwrap())));
+        group.bench_function("fnp_searchsorted_c128_2m", |bn| {
+            bn.iter(|| black_box(fnp_ss.call1((&h, &q)).unwrap()))
+        });
+        group.bench_function("numpy_searchsorted_c128_2m", |bn| {
+            bn.iter(|| black_box(numpy_ss.call1((&h, &q)).unwrap()))
+        });
     });
     group.finish();
 }
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:7945:
 trand = rng.integers(0, 1000, 100_000).astype(np.float64) + 1j * rng.integers(0, 1000, 100_000).astype(np.float64)\n\
 test = np.concatenate([a[:100_000], trand])\n";
         let ns = PyDict::new(py);
-        py.run(std::ffi::CString::new(setup).unwrap().as_c_str(), Some(&ns), Some(&ns)).expect("setup");
+        py.run(
+            std::ffi::CString::new(setup).unwrap().as_c_str(),
+            Some(&ns),
+            Some(&ns),
+        )
+        .expect("setup");
         let a = ns.get_item("a").expect("a");
         let test = ns.get_item("test").expect("test");
         let fnp_isin = module.getattr("isin").expect("fnp isin");
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:7956:
             kw.set_item("invert", inv).unwrap();
             let f = fnp_isin.call((&a, &test), Some(&kw)).expect("fnp isin");
             let n = numpy_isin.call((&a, &test), Some(&kw)).expect("numpy isin");
-            assert!(eqf.call1((&f, &n)).unwrap().extract::<bool>().unwrap(), "complex isin mismatch invert={inv}");
+            assert!(
+                eqf.call1((&f, &n)).unwrap().extract::<bool>().unwrap(),
+                "complex isin mismatch invert={inv}"
+            );
         }
-        group.bench_function("fnp_isin_c128_2m", |bn| bn.iter(|| black_box(fnp_isin.call1((&a, &test)).unwrap())));
-        group.bench_function("numpy_isin_c128_2m", |bn| bn.iter(|| black_box(numpy_isin.call1((&a, &test)).unwrap())));
+        group.bench_function("fnp_isin_c128_2m", |bn| {
+            bn.iter(|| black_box(fnp_isin.call1((&a, &test)).unwrap()))
+        });
+        group.bench_function("numpy_isin_c128_2m", |bn| {
+            bn.iter(|| black_box(numpy_isin.call1((&a, &test)).unwrap()))
+        });
     });
     group.finish();
 }
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:7985:
 trand = (rng.integers(0,1000,100_000)+1j*rng.integers(0,1000,100_000)).astype(np.complex64)\n\
 test = np.concatenate([a[:100_000], trand])\n";
         let ns = PyDict::new(py);
-        py.run(std::ffi::CString::new(setup).unwrap().as_c_str(), Some(&ns), Some(&ns)).expect("setup");
+        py.run(
+            std::ffi::CString::new(setup).unwrap().as_c_str(),
+            Some(&ns),
+            Some(&ns),
+        )
+        .expect("setup");
         let h = ns.get_item("h").unwrap();
         let q = ns.get_item("q").unwrap();
         let a = ns.get_item("a").unwrap();
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:7993:
         let eqf = numpy.getattr("array_equal").unwrap();
         // searchsorted correctness (left+right) + isin (default+invert).
         for side in ["left", "right"] {
-            let kw = PyDict::new(py); kw.set_item("side", side).unwrap();
-            let f = module.getattr("searchsorted").unwrap().call((&h, &q), Some(&kw)).unwrap();
-            let n = numpy.getattr("searchsorted").unwrap().call((&h, &q), Some(&kw)).unwrap();
-            assert!(eqf.call1((&f, &n)).unwrap().extract::<bool>().unwrap(), "c64 searchsorted mismatch side={side}");
+            let kw = PyDict::new(py);
+            kw.set_item("side", side).unwrap();
+            let f = module
+                .getattr("searchsorted")
+                .unwrap()
+                .call((&h, &q), Some(&kw))
+                .unwrap();
+            let n = numpy
+                .getattr("searchsorted")
+                .unwrap()
+                .call((&h, &q), Some(&kw))
+                .unwrap();
+            assert!(
+                eqf.call1((&f, &n)).unwrap().extract::<bool>().unwrap(),
+                "c64 searchsorted mismatch side={side}"
+            );
         }
         for inv in [false, true] {
-            let kw = PyDict::new(py); kw.set_item("invert", inv).unwrap();
-            let f = module.getattr("isin").unwrap().call((&a, &test), Some(&kw)).unwrap();
-            let n = numpy.getattr("isin").unwrap().call((&a, &test), Some(&kw)).unwrap();
-            assert!(eqf.call1((&f, &n)).unwrap().extract::<bool>().unwrap(), "c64 isin mismatch invert={inv}");
+            let kw = PyDict::new(py);
+            kw.set_item("invert", inv).unwrap();
+            let f = module
+                .getattr("isin")
+                .unwrap()
+                .call((&a, &test), Some(&kw))
+                .unwrap();
+            let n = numpy
+                .getattr("isin")
+                .unwrap()
+                .call((&a, &test), Some(&kw))
+                .unwrap();
+            assert!(
+                eqf.call1((&f, &n)).unwrap().extract::<bool>().unwrap(),
+                "c64 isin mismatch invert={inv}"
+            );
         }
         let ss_f = module.getattr("searchsorted").unwrap();
         let ss_n = numpy.getattr("searchsorted").unwrap();
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:8009:
         let is_f = module.getattr("isin").unwrap();
         let is_n = numpy.getattr("isin").unwrap();
-        group.bench_function("fnp_searchsorted_c64_2m", |bn| bn.iter(|| black_box(ss_f.call1((&h, &q)).unwrap())));
-        group.bench_function("numpy_searchsorted_c64_2m", |bn| bn.iter(|| black_box(ss_n.call1((&h, &q)).unwrap())));
-        group.bench_function("fnp_isin_c64_2m", |bn| bn.iter(|| black_box(is_f.call1((&a, &test)).unwrap())));
-        group.bench_function("numpy_isin_c64_2m", |bn| bn.iter(|| black_box(is_n.call1((&a, &test)).unwrap())));
+        group.bench_function("fnp_searchsorted_c64_2m", |bn| {
+            bn.iter(|| black_box(ss_f.call1((&h, &q)).unwrap()))
+        });
+        group.bench_function("numpy_searchsorted_c64_2m", |bn| {
+            bn.iter(|| black_box(ss_n.call1((&h, &q)).unwrap()))
+        });
+        group.bench_function("fnp_isin_c64_2m", |bn| {
+            bn.iter(|| black_box(is_f.call1((&a, &test)).unwrap()))
+        });
+        group.bench_function("numpy_isin_c64_2m", |bn| {
+            bn.iter(|| black_box(is_n.call1((&a, &test)).unwrap()))
+        });
     });
     group.finish();
 }
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:8035:
 dt = rng.integers(0, 100000, 2_000_000).astype('datetime64[s]')\n\
 td = rng.integers(0, 100000, 2_000_000).astype('timedelta64[s]')\n";
         let ns = PyDict::new(py);
-        py.run(std::ffi::CString::new(setup).unwrap().as_c_str(), Some(&ns), Some(&ns)).expect("setup");
+        py.run(
+            std::ffi::CString::new(setup).unwrap().as_c_str(),
+            Some(&ns),
+            Some(&ns),
+        )
+        .expect("setup");
         let dt = ns.get_item("dt").expect("dt");
         let td = ns.get_item("td").expect("td");
         let fnp_u = module.getattr("unique").expect("fnp unique");
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:8044:
         for (arr, label) in [(&dt, "datetime64"), (&td, "timedelta64")] {
             let f = fnp_u.call1((arr,)).expect("fnp unique");
             let n = numpy_u.call1((arr,)).expect("numpy unique");
-            assert!(eqf.call1((&f, &n)).unwrap().extract::<bool>().unwrap(), "{label} unique mismatch");
+            assert!(
+                eqf.call1((&f, &n)).unwrap().extract::<bool>().unwrap(),
+                "{label} unique mismatch"
+            );
         }
-        group.bench_function("fnp_unique_datetime64_2m", |bn| bn.iter(|| black_box(fnp_u.call1((&dt,)).unwrap())));
-        group.bench_function("numpy_unique_datetime64_2m", |bn| bn.iter(|| black_box(numpy_u.call1((&dt,)).unwrap())));
+        group.bench_function("fnp_unique_datetime64_2m", |bn| {
+            bn.iter(|| black_box(fnp_u.call1((&dt,)).unwrap()))
+        });
+        group.bench_function("numpy_unique_datetime64_2m", |bn| {
+            bn.iter(|| black_box(numpy_u.call1((&dt,)).unwrap()))
+        });
     });
     group.finish();
 }
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:8074:
 a['b'] = rng.integers(-50, 50, n)\n\
 a['c'] = rng.integers(-50, 50, n)\n";
         let ns = PyDict::new(py);
-        py.run(std::ffi::CString::new(setup).unwrap().as_c_str(), Some(&ns), Some(&ns)).expect("setup");
+        py.run(
+            std::ffi::CString::new(setup).unwrap().as_c_str(),
+            Some(&ns),
+            Some(&ns),
+        )
+        .expect("setup");
         let a = ns.get_item("a").expect("a");
         let fnp_u = module.getattr("unique").expect("fnp unique");
         let numpy_u = numpy.getattr("unique").expect("numpy unique");
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:8081:
         let eqf = numpy.getattr("array_equal").expect("np.array_equal");
         let f = fnp_u.call1((&a,)).expect("fnp unique");
         let n = numpy_u.call1((&a,)).expect("numpy unique");
-        assert!(eqf.call1((&f, &n)).unwrap().extract::<bool>().unwrap(), "unique struct mismatch");
-        group.bench_function("fnp_unique_struct_3xi8_1m", |bn| bn.iter(|| black_box(fnp_u.call1((&a,)).unwrap())));
-        group.bench_function("numpy_unique_struct_3xi8_1m", |bn| bn.iter(|| black_box(numpy_u.call1((&a,)).unwrap())));
+        assert!(
+            eqf.call1((&f, &n)).unwrap().extract::<bool>().unwrap(),
+            "unique struct mismatch"
+        );
+        group.bench_function("fnp_unique_struct_3xi8_1m", |bn| {
+            bn.iter(|| black_box(fnp_u.call1((&a,)).unwrap()))
+        });
+        group.bench_function("numpy_unique_struct_3xi8_1m", |bn| {
+            bn.iter(|| black_box(numpy_u.call1((&a,)).unwrap()))
+        });
     });
     group.finish();
 }
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:8111:
 a['val'] = rng.integers(-100000, 100000, n).astype(np.float64)\n\
 a['flag'] = rng.integers(0, 100, n)\n";
         let ns = PyDict::new(py);
-        py.run(std::ffi::CString::new(setup).unwrap().as_c_str(), Some(&ns), Some(&ns)).expect("setup");
+        py.run(
+            std::ffi::CString::new(setup).unwrap().as_c_str(),
+            Some(&ns),
+            Some(&ns),
+        )
+        .expect("setup");
         let a = ns.get_item("a").expect("a");
         let fnp_u = module.getattr("unique").expect("fnp unique");
         let numpy_u = numpy.getattr("unique").expect("numpy unique");
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:8118:
         let eqf = numpy.getattr("array_equal").expect("np.array_equal");
         let f = fnp_u.call1((&a,)).expect("fnp unique");
         let n = numpy_u.call1((&a,)).expect("numpy unique");
-        assert!(eqf.call1((&f, &n)).unwrap().extract::<bool>().unwrap(), "unique struct mixed mismatch");
-        group.bench_function("fnp_unique_struct_i8f8i4_1m", |bn| bn.iter(|| black_box(fnp_u.call1((&a,)).unwrap())));
-        group.bench_function("numpy_unique_struct_i8f8i4_1m", |bn| bn.iter(|| black_box(numpy_u.call1((&a,)).unwrap())));
+        assert!(
+            eqf.call1((&f, &n)).unwrap().extract::<bool>().unwrap(),
+            "unique struct mixed mismatch"
+        );
+        group.bench_function("fnp_unique_struct_i8f8i4_1m", |bn| {
+            bn.iter(|| black_box(fnp_u.call1((&a,)).unwrap()))
+        });
+        group.bench_function("numpy_unique_struct_i8f8i4_1m", |bn| {
+            bn.iter(|| black_box(numpy_u.call1((&a,)).unwrap()))
+        });
     });
     group.finish();
 }
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:8144:
 base = rng.integers(0, 100000, (250_000, 3)).astype('datetime64[s]')\n\
 a = np.concatenate([base, base])\n";
         let ns = PyDict::new(py);
-        py.run(std::ffi::CString::new(setup).unwrap().as_c_str(), Some(&ns), Some(&ns)).expect("setup");
+        py.run(
+            std::ffi::CString::new(setup).unwrap().as_c_str(),
+            Some(&ns),
+            Some(&ns),
+        )
+        .expect("setup");
         let a = ns.get_item("a").expect("a");
         let fnp_u = module.getattr("unique").expect("fnp unique");
         let numpy_u = numpy.getattr("unique").expect("numpy unique");
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:8151:
         let eqf = numpy.getattr("array_equal").expect("np.array_equal");
         // plain
-        let kw = PyDict::new(py); kw.set_item("axis", 0_i64).unwrap();
+        let kw = PyDict::new(py);
+        kw.set_item("axis", 0_i64).unwrap();
         let f = fnp_u.call((&a,), Some(&kw)).expect("fnp unique");
         let n = numpy_u.call((&a,), Some(&kw)).expect("numpy unique");
-        assert!(eqf.call1((&f, &n)).unwrap().extract::<bool>().unwrap(), "datetime rows plain mismatch");
+        assert!(
+            eqf.call1((&f, &n)).unwrap().extract::<bool>().unwrap(),
+            "datetime rows plain mismatch"
+        );
         // factorize
         let kwf = PyDict::new(py);
         kwf.set_item("axis", 0_i64).unwrap();
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:8160:
         kwf.set_item("return_index", true).unwrap();
         kwf.set_item("return_inverse", true).unwrap();
         kwf.set_item("return_counts", true).unwrap();
-        let ft = fnp_u.call((&a,), Some(&kwf)).expect("fnp full").downcast_into::<pyo3::types::PyTuple>().unwrap();
-        let nt = numpy_u.call((&a,), Some(&kwf)).expect("numpy full").downcast_into::<pyo3::types::PyTuple>().unwrap();
+        let ft = fnp_u
+            .call((&a,), Some(&kwf))
+            .expect("fnp full")
+            .downcast_into::<pyo3::types::PyTuple>()
+            .unwrap();
+        let nt = numpy_u
+            .call((&a,), Some(&kwf))
+            .expect("numpy full")
+            .downcast_into::<pyo3::types::PyTuple>()
+            .unwrap();
         for i in 0..4 {
-            let eq: bool = eqf.call1((ft.get_item(i).unwrap(), nt.get_item(i).unwrap())).unwrap().extract().unwrap();
+            let eq: bool = eqf
+                .call1((ft.get_item(i).unwrap(), nt.get_item(i).unwrap()))
+                .unwrap()
+                .extract()
+                .unwrap();
             assert!(eq, "datetime rows factorize element {i} mismatch");
         }
         group.bench_function("fnp_unique_rows_datetime_500kx3", |bn| {
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:8170:
-            let kw = PyDict::new(py); kw.set_item("axis", 0_i64).unwrap();
+            let kw = PyDict::new(py);
+            kw.set_item("axis", 0_i64).unwrap();
             bn.iter(|| black_box(fnp_u.call((&a,), Some(&kw)).unwrap()));
         });
         group.bench_function("numpy_unique_rows_datetime_500kx3", |bn| {
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:8174:
-            let kw = PyDict::new(py); kw.set_item("axis", 0_i64).unwrap();
+            let kw = PyDict::new(py);
+            kw.set_item("axis", 0_i64).unwrap();
             bn.iter(|| black_box(numpy_u.call((&a,), Some(&kw)).unwrap()));
         });
     });
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:8197:
 base = rng.integers(0, 50, (250_000, 4)).astype(np.float16)\n\
 a = np.concatenate([base, base])\n";
         let ns = PyDict::new(py);
-        py.run(std::ffi::CString::new(setup).unwrap().as_c_str(), Some(&ns), Some(&ns)).expect("setup");
+        py.run(
+            std::ffi::CString::new(setup).unwrap().as_c_str(),
+            Some(&ns),
+            Some(&ns),
+        )
+        .expect("setup");
         let a = ns.get_item("a").expect("a");
         let fnp_u = module.getattr("unique").expect("fnp unique");
         let numpy_u = numpy.getattr("unique").expect("numpy unique");
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:8204:
         let eqf = numpy.getattr("array_equal").expect("np.array_equal");
-        let kw = PyDict::new(py); kw.set_item("axis", 0_i64).unwrap();
+        let kw = PyDict::new(py);
+        kw.set_item("axis", 0_i64).unwrap();
         let f = fnp_u.call((&a,), Some(&kw)).expect("fnp unique");
         let n = numpy_u.call((&a,), Some(&kw)).expect("numpy unique");
-        assert!(eqf.call1((&f, &n)).unwrap().extract::<bool>().unwrap(), "f16 rows plain mismatch");
+        assert!(
+            eqf.call1((&f, &n)).unwrap().extract::<bool>().unwrap(),
+            "f16 rows plain mismatch"
+        );
         let kwf = PyDict::new(py);
         kwf.set_item("axis", 0_i64).unwrap();
         kwf.set_item("return_index", true).unwrap();
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:8212:
         kwf.set_item("return_inverse", true).unwrap();
         kwf.set_item("return_counts", true).unwrap();
-        let ft = fnp_u.call((&a,), Some(&kwf)).expect("fnp full").downcast_into::<pyo3::types::PyTuple>().unwrap();
-        let nt = numpy_u.call((&a,), Some(&kwf)).expect("numpy full").downcast_into::<pyo3::types::PyTuple>().unwrap();
+        let ft = fnp_u
+            .call((&a,), Some(&kwf))
+            .expect("fnp full")
+            .downcast_into::<pyo3::types::PyTuple>()
+            .unwrap();
+        let nt = numpy_u
+            .call((&a,), Some(&kwf))
+            .expect("numpy full")
+            .downcast_into::<pyo3::types::PyTuple>()
+            .unwrap();
         for i in 0..4 {
-            let eq: bool = eqf.call1((ft.get_item(i).unwrap(), nt.get_item(i).unwrap())).unwrap().extract().unwrap();
+            let eq: bool = eqf
+                .call1((ft.get_item(i).unwrap(), nt.get_item(i).unwrap()))
+                .unwrap()
+                .extract()
+                .unwrap();
             assert!(eq, "f16 rows factorize element {i} mismatch");
         }
         group.bench_function("fnp_unique_rows_f16_500kx4", |bn| {
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:8221:
-            let kw = PyDict::new(py); kw.set_item("axis", 0_i64).unwrap();
+            let kw = PyDict::new(py);
+            kw.set_item("axis", 0_i64).unwrap();
             bn.iter(|| black_box(fnp_u.call((&a,), Some(&kw)).unwrap()));
         });
         group.bench_function("numpy_unique_rows_f16_500kx4", |bn| {
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:8225:
-            let kw = PyDict::new(py); kw.set_item("axis", 0_i64).unwrap();
+            let kw = PyDict::new(py);
+            kw.set_item("axis", 0_i64).unwrap();
             bn.iter(|| black_box(numpy_u.call((&a,), Some(&kw)).unwrap()));
         });
     });
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:8250:
 a['b'] = rng.integers(-30, 30, n)\n\
 a['c'] = rng.integers(-30, 30, n)\n";
         let ns = PyDict::new(py);
-        py.run(std::ffi::CString::new(setup).unwrap().as_c_str(), Some(&ns), Some(&ns)).expect("setup");
+        py.run(
+            std::ffi::CString::new(setup).unwrap().as_c_str(),
+            Some(&ns),
+            Some(&ns),
+        )
+        .expect("setup");
         let a = ns.get_item("a").expect("a");
         let fnp_u = module.getattr("unique").expect("fnp unique");
         let numpy_u = numpy.getattr("unique").expect("numpy unique");
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:8259:
         kwfull.set_item("return_index", true).unwrap();
         kwfull.set_item("return_inverse", true).unwrap();
         kwfull.set_item("return_counts", true).unwrap();
-        let ft = fnp_u.call((&a,), Some(&kwfull)).expect("fnp full").downcast_into::<pyo3::types::PyTuple>().unwrap();
-        let nt = numpy_u.call((&a,), Some(&kwfull)).expect("numpy full").downcast_into::<pyo3::types::PyTuple>().unwrap();
+        let ft = fnp_u
+            .call((&a,), Some(&kwfull))
+            .expect("fnp full")
+            .downcast_into::<pyo3::types::PyTuple>()
+            .unwrap();
+        let nt = numpy_u
+            .call((&a,), Some(&kwfull))
+            .expect("numpy full")
+            .downcast_into::<pyo3::types::PyTuple>()
+            .unwrap();
         for i in 0..4 {
-            let eq: bool = eqf.call1((ft.get_item(i).unwrap(), nt.get_item(i).unwrap())).unwrap().extract().unwrap();
+            let eq: bool = eqf
+                .call1((ft.get_item(i).unwrap(), nt.get_item(i).unwrap()))
+                .unwrap()
+                .extract()
+                .unwrap();
             assert!(eq, "struct factorize element {i} mismatch");
         }
         group.bench_function("fnp_unique_struct_factorize_3xi8_1m", |bn| {
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:8302:
 dt = [('id','<i8'),('val','<f8')]\n\
 a = np.zeros(1_000_000, dtype=dt); a['id'] = rng.integers(0, 10000, 1_000_000); a['val'] = rng.integers(0, 10000, 1_000_000).astype(np.float64)\n";
         let ns = PyDict::new(py);
-        py.run(std::ffi::CString::new(setup).unwrap().as_c_str(), Some(&ns), Some(&ns)).expect("setup");
+        py.run(
+            std::ffi::CString::new(setup).unwrap().as_c_str(),
+            Some(&ns),
+            Some(&ns),
+        )
+        .expect("setup");
         let a = ns.get_item("a").expect("a");
         let fnp_u = module.getattr("unique").expect("fnp unique");
         let numpy_u = numpy.getattr("unique").expect("numpy unique");
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:8311:
         kwf.set_item("return_index", true).unwrap();
         kwf.set_item("return_inverse", true).unwrap();
         kwf.set_item("return_counts", true).unwrap();
-        let ft = fnp_u.call((&a,), Some(&kwf)).expect("fnp full").downcast_into::<pyo3::types::PyTuple>().unwrap();
-        let nt = numpy_u.call((&a,), Some(&kwf)).expect("numpy full").downcast_into::<pyo3::types::PyTuple>().unwrap();
+        let ft = fnp_u
+            .call((&a,), Some(&kwf))
+            .expect("fnp full")
+            .downcast_into::<pyo3::types::PyTuple>()
+            .unwrap();
+        let nt = numpy_u
+            .call((&a,), Some(&kwf))
+            .expect("numpy full")
+            .downcast_into::<pyo3::types::PyTuple>()
+            .unwrap();
         for i in 0..4 {
-            let eq: bool = eqf.call1((ft.get_item(i).unwrap(), nt.get_item(i).unwrap())).unwrap().extract().unwrap();
+            let eq: bool = eqf
+                .call1((ft.get_item(i).unwrap(), nt.get_item(i).unwrap()))
+                .unwrap()
+                .extract()
+                .unwrap();
             assert!(eq, "mixed struct factorize element {i} mismatch");
         }
         group.bench_function("fnp_unique_struct_mixed_factorize_1m", |bn| {
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:8357:
 keys2 = (k0, k1)\n\
 keys3 = (k0, k1, k2)\n";
         let ns = PyDict::new(py);
-        py.run(std::ffi::CString::new(setup).unwrap().as_c_str(), Some(&ns), Some(&ns)).expect("setup");
+        py.run(
+            std::ffi::CString::new(setup).unwrap().as_c_str(),
+            Some(&ns),
+            Some(&ns),
+        )
+        .expect("setup");
         let keys2 = ns.get_item("keys2").expect("keys2");
         let keys3 = ns.get_item("keys3").expect("keys3");
         let fnp_lx = module.getattr("lexsort").expect("fnp lexsort");
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:8366:
         for (keys, label) in [(&keys2, "2key"), (&keys3, "3key")] {
             let f = fnp_lx.call1((keys,)).expect("fnp lexsort");
             let n = numpy_lx.call1((keys,)).expect("numpy lexsort");
-            assert!(eqf.call1((&f, &n)).unwrap().extract::<bool>().unwrap(), "lexsort float {label} mismatch");
+            assert!(
+                eqf.call1((&f, &n)).unwrap().extract::<bool>().unwrap(),
+                "lexsort float {label} mismatch"
+            );
         }
-        group.bench_function("fnp_lexsort_f64_2key_2m", |bn| bn.iter(|| black_box(fnp_lx.call1((&keys2,)).unwrap())));
-        group.bench_function("numpy_lexsort_f64_2key_2m", |bn| bn.iter(|| black_box(numpy_lx.call1((&keys2,)).unwrap())));
-        group.bench_function("fnp_lexsort_f64_3key_2m", |bn| bn.iter(|| black_box(fnp_lx.call1((&keys3,)).unwrap())));
-        group.bench_function("numpy_lexsort_f64_3key_2m", |bn| bn.iter(|| black_box(numpy_lx.call1((&keys3,)).unwrap())));
+        group.bench_function("fnp_lexsort_f64_2key_2m", |bn| {
+            bn.iter(|| black_box(fnp_lx.call1((&keys2,)).unwrap()))
+        });
+        group.bench_function("numpy_lexsort_f64_2key_2m", |bn| {
+            bn.iter(|| black_box(numpy_lx.call1((&keys2,)).unwrap()))
+        });
+        group.bench_function("fnp_lexsort_f64_3key_2m", |bn| {
+            bn.iter(|| black_box(fnp_lx.call1((&keys3,)).unwrap()))
+        });
+        group.bench_function("numpy_lexsort_f64_3key_2m", |bn| {
+            bn.iter(|| black_box(numpy_lx.call1((&keys3,)).unwrap()))
+        });
     });
     group.finish();
 }
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:8398:
 a = np.zeros(1_000_000, dtype=dt); a['id'] = rng.integers(0, 10000, 1_000_000); a['val'] = rng.integers(0, 10000, 1_000_000).astype(np.float64)\n\
 a_argsort = np.zeros(1_000_000, dtype=dt); a_argsort['id'] = rng.permutation(1_000_000); a_argsort['val'] = rng.standard_normal(1_000_000)\n";
         let ns = PyDict::new(py);
-        py.run(std::ffi::CString::new(setup).unwrap().as_c_str(), Some(&ns), Some(&ns)).expect("setup");
+        py.run(
+            std::ffi::CString::new(setup).unwrap().as_c_str(),
+            Some(&ns),
+            Some(&ns),
+        )
+        .expect("setup");
         let a = ns.get_item("a").expect("a");
         let a_argsort = ns.get_item("a_argsort").expect("a_argsort");
         let fnp_s = module.getattr("sort").expect("fnp sort");
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:8408:
         let eqf = numpy.getattr("array_equal").expect("np.array_equal");
         let f = fnp_s.call1((&a,)).expect("fnp sort");
         let n = numpy_s.call1((&a,)).expect("numpy sort");
-        assert!(eqf.call1((&f, &n)).unwrap().extract::<bool>().unwrap(), "sort mixed struct mismatch");
+        assert!(
+            eqf.call1((&f, &n)).unwrap().extract::<bool>().unwrap(),
+            "sort mixed struct mismatch"
+        );
         let f_idx = fnp_as.call1((&a_argsort,)).expect("fnp argsort");
         let n_idx = numpy_as.call1((&a_argsort,)).expect("numpy argsort");
         assert!(
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:8415:
-            eqf.call1((&f_idx, &n_idx)).unwrap().extract::<bool>().unwrap(),
+            eqf.call1((&f_idx, &n_idx))
+                .unwrap()
+                .extract::<bool>()
+                .unwrap(),
             "argsort mixed struct mismatch"
         );
-        group.bench_function("fnp_sort_struct_i8f8_1m", |bn| bn.iter(|| black_box(fnp_s.call1((&a,)).unwrap())));
-        group.bench_function("numpy_sort_struct_i8f8_1m", |bn| bn.iter(|| black_box(numpy_s.call1((&a,)).unwrap())));
+        group.bench_function("fnp_sort_struct_i8f8_1m", |bn| {
+            bn.iter(|| black_box(fnp_s.call1((&a,)).unwrap()))
+        });
+        group.bench_function("numpy_sort_struct_i8f8_1m", |bn| {
+            bn.iter(|| black_box(numpy_s.call1((&a,)).unwrap()))
+        });
         group.bench_function("fnp_argsort_struct_i8f8_distinct_1m", |bn| {
             bn.iter(|| black_box(fnp_as.call1((&a_argsort,)).unwrap()))
         });
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:8446:
 di = rng.integers(0, 1000, 8_000_000).astype(np.int64)\n\
 df = rng.integers(0, 1000, 8_000_000).astype(np.float64)\n";
         let ns = PyDict::new(py);
-        py.run(std::ffi::CString::new(setup).unwrap().as_c_str(), Some(&ns), Some(&ns)).expect("setup");
+        py.run(
+            std::ffi::CString::new(setup).unwrap().as_c_str(),
+            Some(&ns),
+            Some(&ns),
+        )
+        .expect("setup");
         let di = ns.get_item("di").expect("di");
         let df = ns.get_item("df").expect("df");
         let fnp_as = module.getattr("argsort").expect("fnp argsort");
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:8453:
         let numpy_as = numpy.getattr("argsort").expect("numpy argsort");
         let eqf = numpy.getattr("array_equal").expect("np.array_equal");
         for (arr, label) in [(&di, "i64"), (&df, "f64")] {
-            let kw = PyDict::new(py); kw.set_item("kind", "stable").unwrap();
+            let kw = PyDict::new(py);
+            kw.set_item("kind", "stable").unwrap();
             let f = fnp_as.call((arr,), Some(&kw)).expect("fnp argsort");
             let n = numpy_as.call((arr,), Some(&kw)).expect("numpy argsort");
-            assert!(eqf.call1((&f, &n)).unwrap().extract::<bool>().unwrap(), "argsort {label} dense stable mismatch");
+            assert!(
+                eqf.call1((&f, &n)).unwrap().extract::<bool>().unwrap(),
+                "argsort {label} dense stable mismatch"
+            );
         }
         group.bench_function("fnp_argsort_i64_dense_stable_8m", |bn| {
-            let kw = PyDict::new(py); kw.set_item("kind", "stable").unwrap();
+            let kw = PyDict::new(py);
+            kw.set_item("kind", "stable").unwrap();
             bn.iter(|| black_box(fnp_as.call((&di,), Some(&kw)).unwrap()));
         });
         group.bench_function("numpy_argsort_i64_dense_stable_8m", |bn| {
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:8466:
-            let kw = PyDict::new(py); kw.set_item("kind", "stable").unwrap();
+            let kw = PyDict::new(py);
+            kw.set_item("kind", "stable").unwrap();
             bn.iter(|| black_box(numpy_as.call((&di,), Some(&kw)).unwrap()));
         });
         group.bench_function("fnp_argsort_f64_dense_stable_8m", |bn| {
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:8470:
-            let kw = PyDict::new(py); kw.set_item("kind", "stable").unwrap();
+            let kw = PyDict::new(py);
+            kw.set_item("kind", "stable").unwrap();
             bn.iter(|| black_box(fnp_as.call((&df,), Some(&kw)).unwrap()));
         });
         group.bench_function("numpy_argsort_f64_dense_stable_8m", |bn| {
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:8474:
-            let kw = PyDict::new(py); kw.set_item("kind", "stable").unwrap();
+            let kw = PyDict::new(py);
+            kw.set_item("kind", "stable").unwrap();
             bn.iter(|| black_box(numpy_as.call((&df,), Some(&kw)).unwrap()));
         });
     });
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:8498:
 mi = np.ascontiguousarray(rng.integers(0, 100, (4000, 2000)).astype(np.int64))\n\
 mf = np.ascontiguousarray(rng.integers(0, 100, (4000, 2000)).astype(np.float64))\n";
         let ns = PyDict::new(py);
-        py.run(std::ffi::CString::new(setup).unwrap().as_c_str(), Some(&ns), Some(&ns)).expect("setup");
+        py.run(
+            std::ffi::CString::new(setup).unwrap().as_c_str(),
+            Some(&ns),
+            Some(&ns),
+        )
+        .expect("setup");
         let mi = ns.get_item("mi").expect("mi");
         let mf = ns.get_item("mf").expect("mf");
         let fnp_as = module.getattr("argsort").expect("fnp argsort");
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:8505:
         let numpy_as = numpy.getattr("argsort").expect("numpy argsort");
         let eqf = numpy.getattr("array_equal").expect("np.array_equal");
         for (arr, label) in [(&mi, "i64"), (&mf, "f64")] {
-            let kw = PyDict::new(py); kw.set_item("axis", -1).unwrap(); kw.set_item("kind", "stable").unwrap();
+            let kw = PyDict::new(py);
+            kw.set_item("axis", -1).unwrap();
+            kw.set_item("kind", "stable").unwrap();
             let f = fnp_as.call((arr,), Some(&kw)).expect("fnp argsort");
             let n = numpy_as.call((arr,), Some(&kw)).expect("numpy argsort");
-            assert!(eqf.call1((&f, &n)).unwrap().extract::<bool>().unwrap(), "argsort 2D {label} axis=-1 stable mismatch");
+            assert!(
+                eqf.call1((&f, &n)).unwrap().extract::<bool>().unwrap(),
+                "argsort 2D {label} axis=-1 stable mismatch"
+            );
         }
         group.bench_function("fnp_argsort_i64_2d_lastaxis_stable_8m", |bn| {
-            let kw = PyDict::new(py); kw.set_item("axis", -1).unwrap(); kw.set_item("kind", "stable").unwrap();
+            let kw = PyDict::new(py);
+            kw.set_item("axis", -1).unwrap();
+            kw.set_item("kind", "stable").unwrap();
             bn.iter(|| black_box(fnp_as.call((&mi,), Some(&kw)).unwrap()));
         });
         group.bench_function("numpy_argsort_i64_2d_lastaxis_stable_8m", |bn| {
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:8518:
-            let kw = PyDict::new(py); kw.set_item("axis", -1).unwrap(); kw.set_item("kind", "stable").unwrap();
+            let kw = PyDict::new(py);
+            kw.set_item("axis", -1).unwrap();
+            kw.set_item("kind", "stable").unwrap();
             bn.iter(|| black_box(numpy_as.call((&mi,), Some(&kw)).unwrap()));
         });
         group.bench_function("fnp_argsort_f64_2d_lastaxis_stable_8m", |bn| {
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:8522:
-            let kw = PyDict::new(py); kw.set_item("axis", -1).unwrap(); kw.set_item("kind", "stable").unwrap();
+            let kw = PyDict::new(py);
+            kw.set_item("axis", -1).unwrap();
+            kw.set_item("kind", "stable").unwrap();
             bn.iter(|| black_box(fnp_as.call((&mf,), Some(&kw)).unwrap()));
         });
         group.bench_function("numpy_argsort_f64_2d_lastaxis_stable_8m", |bn| {
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:8526:
-            let kw = PyDict::new(py); kw.set_item("axis", -1).unwrap(); kw.set_item("kind", "stable").unwrap();
+            let kw = PyDict::new(py);
+            kw.set_item("axis", -1).unwrap();
+            kw.set_item("kind", "stable").unwrap();
             bn.iter(|| black_box(numpy_as.call((&mf,), Some(&kw)).unwrap()));
         });
     });
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:8550:
 wi = rng.integers(0, 2**30, 16_000_000).astype(np.int64)\n\
 wu = rng.integers(0, 2**52, 16_000_000).astype(np.uint64)\n";
         let ns = PyDict::new(py);
-        py.run(std::ffi::CString::new(setup).unwrap().as_c_str(), Some(&ns), Some(&ns)).expect("setup");
+        py.run(
+            std::ffi::CString::new(setup).unwrap().as_c_str(),
+            Some(&ns),
+            Some(&ns),
+        )
+        .expect("setup");
         let wi = ns.get_item("wi").expect("wi");
         let wu = ns.get_item("wu").expect("wu");
         let fnp_as = module.getattr("argsort").expect("fnp argsort");
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:8557:
         let numpy_as = numpy.getattr("argsort").expect("numpy argsort");
         let eqf = numpy.getattr("array_equal").expect("np.array_equal");
         for (arr, label) in [(&wi, "i64_2p30"), (&wu, "u64_2p52")] {
-            let kw = PyDict::new(py); kw.set_item("kind", "stable").unwrap();
+            let kw = PyDict::new(py);
+            kw.set_item("kind", "stable").unwrap();
             let f = fnp_as.call((arr,), Some(&kw)).expect("fnp argsort");
             let n = numpy_as.call((arr,), Some(&kw)).expect("numpy argsort");
-            assert!(eqf.call1((&f, &n)).unwrap().extract::<bool>().unwrap(), "argsort wide {label} stable mismatch");
+            assert!(
+                eqf.call1((&f, &n)).unwrap().extract::<bool>().unwrap(),
+                "argsort wide {label} stable mismatch"
+            );
         }
         group.bench_function("fnp_argsort_i64_wide_stable_16m", |bn| {
-            let kw = PyDict::new(py); kw.set_item("kind", "stable").unwrap();
+            let kw = PyDict::new(py);
+            kw.set_item("kind", "stable").unwrap();
             bn.iter(|| black_box(fnp_as.call((&wi,), Some(&kw)).unwrap()));
         });
         group.bench_function("numpy_argsort_i64_wide_stable_16m", |bn| {
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:8570:
-            let kw = PyDict::new(py); kw.set_item("kind", "stable").unwrap();
+            let kw = PyDict::new(py);
+            kw.set_item("kind", "stable").unwrap();
             bn.iter(|| black_box(numpy_as.call((&wi,), Some(&kw)).unwrap()));
         });
         group.bench_function("fnp_argsort_u64_wide_stable_16m", |bn| {
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:8574:
-            let kw = PyDict::new(py); kw.set_item("kind", "stable").unwrap();
+            let kw = PyDict::new(py);
+            kw.set_item("kind", "stable").unwrap();
             bn.iter(|| black_box(fnp_as.call((&wu,), Some(&kw)).unwrap()));
         });
         group.bench_function("numpy_argsort_u64_wide_stable_16m", |bn| {
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:8578:
-            let kw = PyDict::new(py); kw.set_item("kind", "stable").unwrap();
+            let kw = PyDict::new(py);
+            kw.set_item("kind", "stable").unwrap();
             bn.iter(|| black_box(numpy_as.call((&wu,), Some(&kw)).unwrap()));
         });
     });
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:8603:
 f64[rng.integers(0, 16_000_000, 1000)] *= -1.0\n\
 f32 = rng.integers(-100000, 100000, 16_000_000).astype(np.float32) / 7.0\n";
         let ns = PyDict::new(py);
-        py.run(std::ffi::CString::new(setup).unwrap().as_c_str(), Some(&ns), Some(&ns)).expect("setup");
+        py.run(
+            std::ffi::CString::new(setup).unwrap().as_c_str(),
+            Some(&ns),
+            Some(&ns),
+        )
+        .expect("setup");
         let f64a = ns.get_item("f64").expect("f64");
         let f32a = ns.get_item("f32").expect("f32");
         let fnp_as = module.getattr("argsort").expect("fnp argsort");
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:8610:
         let numpy_as = numpy.getattr("argsort").expect("numpy argsort");
         let eqf = numpy.getattr("array_equal").expect("np.array_equal");
         for (arr, label) in [(&f64a, "f64"), (&f32a, "f32")] {
-            let kw = PyDict::new(py); kw.set_item("kind", "stable").unwrap();
+            let kw = PyDict::new(py);
+            kw.set_item("kind", "stable").unwrap();
             let f = fnp_as.call((arr,), Some(&kw)).expect("fnp argsort");
             let n = numpy_as.call((arr,), Some(&kw)).expect("numpy argsort");
-            assert!(eqf.call1((&f, &n)).unwrap().extract::<bool>().unwrap(), "argsort float {label} stable mismatch");
+            assert!(
+                eqf.call1((&f, &n)).unwrap().extract::<bool>().unwrap(),
+                "argsort float {label} stable mismatch"
+            );
         }
         group.bench_function("fnp_argsort_f64_stable_16m", |bn| {
-            let kw = PyDict::new(py); kw.set_item("kind", "stable").unwrap();
+            let kw = PyDict::new(py);
+            kw.set_item("kind", "stable").unwrap();
             bn.iter(|| black_box(fnp_as.call((&f64a,), Some(&kw)).unwrap()));
         });
         group.bench_function("numpy_argsort_f64_stable_16m", |bn| {
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:8623:
-            let kw = PyDict::new(py); kw.set_item("kind", "stable").unwrap();
+            let kw = PyDict::new(py);
+            kw.set_item("kind", "stable").unwrap();
             bn.iter(|| black_box(numpy_as.call((&f64a,), Some(&kw)).unwrap()));
         });
         group.bench_function("fnp_argsort_f32_stable_16m", |bn| {
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:8627:
-            let kw = PyDict::new(py); kw.set_item("kind", "stable").unwrap();
+            let kw = PyDict::new(py);
+            kw.set_item("kind", "stable").unwrap();
             bn.iter(|| black_box(fnp_as.call((&f32a,), Some(&kw)).unwrap()));
         });
         group.bench_function("numpy_argsort_f32_stable_16m", |bn| {
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:8631:
-            let kw = PyDict::new(py); kw.set_item("kind", "stable").unwrap();
+            let kw = PyDict::new(py);
+            kw.set_item("kind", "stable").unwrap();
             bn.iter(|| black_box(numpy_as.call((&f32a,), Some(&kw)).unwrap()));
         });
     });
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:8657:
 du = rng.permutation(16_000_000).astype(np.uint64)\n\
 tied = rng.integers(0, 1000, 16_000_000).astype(np.int64)\n";
         let ns = PyDict::new(py);
-        py.run(std::ffi::CString::new(setup).unwrap().as_c_str(), Some(&ns), Some(&ns)).expect("setup");
+        py.run(
+            std::ffi::CString::new(setup).unwrap().as_c_str(),
+            Some(&ns),
+            Some(&ns),
+        )
+        .expect("setup");
         let di = ns.get_item("di").expect("di");
         let du = ns.get_item("du").expect("du");
         let tied = ns.get_item("tied").expect("tied");
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:8664:
         let fnp_as = module.getattr("argsort").expect("fnp argsort");
         let numpy_as = numpy.getattr("argsort").expect("numpy argsort");
         let eqf = numpy.getattr("array_equal").expect("np.array_equal");
-        for (arr, label) in [(&di, "i64_distinct"), (&du, "u64_distinct"), (&tied, "i64_tied")] {
+        for (arr, label) in [
+            (&di, "i64_distinct"),
+            (&du, "u64_distinct"),
+            (&tied, "i64_tied"),
+        ] {
             let f = fnp_as.call1((arr,)).expect("fnp argsort");
             let n = numpy_as.call1((arr,)).expect("numpy argsort");
-            assert!(eqf.call1((&f, &n)).unwrap().extract::<bool>().unwrap(), "default argsort {label} mismatch");
+            assert!(
+                eqf.call1((&f, &n)).unwrap().extract::<bool>().unwrap(),
+                "default argsort {label} mismatch"
+            );
         }
-        group.bench_function("fnp_argsort_i64_distinct_default_16m", |bn| bn.iter(|| black_box(fnp_as.call1((&di,)).unwrap())));
-        group.bench_function("numpy_argsort_i64_distinct_default_16m", |bn| bn.iter(|| black_box(numpy_as.call1((&di,)).unwrap())));
-        group.bench_function("fnp_argsort_u64_distinct_default_16m", |bn| bn.iter(|| black_box(fnp_as.call1((&du,)).unwrap())));
-        group.bench_function("numpy_argsort_u64_distinct_default_16m", |bn| bn.iter(|| black_box(numpy_as.call1((&du,)).unwrap())));
+        group.bench_function("fnp_argsort_i64_distinct_default_16m", |bn| {
+            bn.iter(|| black_box(fnp_as.call1((&di,)).unwrap()))
+        });
+        group.bench_function("numpy_argsort_i64_distinct_default_16m", |bn| {
+            bn.iter(|| black_box(numpy_as.call1((&di,)).unwrap()))
+        });
+        group.bench_function("fnp_argsort_u64_distinct_default_16m", |bn| {
+            bn.iter(|| black_box(fnp_as.call1((&du,)).unwrap()))
+        });
+        group.bench_function("numpy_argsort_u64_distinct_default_16m", |bn| {
+            bn.iter(|| black_box(numpy_as.call1((&du,)).unwrap()))
+        });
     });
     group.finish();
 }
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:8696:
 rng = np.random.default_rng(0)\n\
 f64d = rng.standard_normal(16_000_000)\n\
 f32d = rng.permutation(16_000_000).astype(np.float32)\n\
-tied = np.round(rng.standard_normal(16_000_000), 2)\n";  // f32d: ints < 2**24 exact -> distinct; tied: dense dups -> defers
+tied = np.round(rng.standard_normal(16_000_000), 2)\n"; // f32d: ints < 2**24 exact -> distinct; tied: dense dups -> defers
         let ns = PyDict::new(py);
-        py.run(std::ffi::CString::new(setup).unwrap().as_c_str(), Some(&ns), Some(&ns)).expect("setup");
+        py.run(
+            std::ffi::CString::new(setup).unwrap().as_c_str(),
+            Some(&ns),
+            Some(&ns),
+        )
+        .expect("setup");
         let f64d = ns.get_item("f64d").expect("f64d");
         let f32d = ns.get_item("f32d").expect("f32d");
         let tied = ns.get_item("tied").expect("tied");
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:8705:
         let fnp_as = module.getattr("argsort").expect("fnp argsort");
         let numpy_as = numpy.getattr("argsort").expect("numpy argsort");
         let eqf = numpy.getattr("array_equal").expect("np.array_equal");
-        for (arr, label) in [(&f64d, "f64_distinct"), (&f32d, "f32_distinct"), (&tied, "f64_tied")] {
+        for (arr, label) in [
+            (&f64d, "f64_distinct"),
+            (&f32d, "f32_distinct"),
+            (&tied, "f64_tied"),
+        ] {
             let f = fnp_as.call1((arr,)).expect("fnp argsort");
             let n = numpy_as.call1((arr,)).expect("numpy argsort");
-            assert!(eqf.call1((&f, &n)).unwrap().extract::<bool>().unwrap(), "default float argsort {label} mismatch");
+            assert!(
+                eqf.call1((&f, &n)).unwrap().extract::<bool>().unwrap(),
+                "default float argsort {label} mismatch"
+            );
         }
-        group.bench_function("fnp_argsort_f64_distinct_default_16m", |bn| bn.iter(|| black_box(fnp_as.call1((&f64d,)).unwrap())));
-        group.bench_function("numpy_argsort_f64_distinct_default_16m", |bn| bn.iter(|| black_box(numpy_as.call1((&f64d,)).unwrap())));
-        group.bench_function("fnp_argsort_f32_distinct_default_16m", |bn| bn.iter(|| black_box(fnp_as.call1((&f32d,)).unwrap())));
-        group.bench_function("numpy_argsort_f32_distinct_default_16m", |bn| bn.iter(|| black_box(numpy_as.call1((&f32d,)).unwrap())));
+        group.bench_function("fnp_argsort_f64_distinct_default_16m", |bn| {
+            bn.iter(|| black_box(fnp_as.call1((&f64d,)).unwrap()))
+        });
+        group.bench_function("numpy_argsort_f64_distinct_default_16m", |bn| {
+            bn.iter(|| black_box(numpy_as.call1((&f64d,)).unwrap()))
+        });
+        group.bench_function("fnp_argsort_f32_distinct_default_16m", |bn| {
+            bn.iter(|| black_box(fnp_as.call1((&f32d,)).unwrap()))
+        });
+        group.bench_function("numpy_argsort_f32_distinct_default_16m", |bn| {
+            bn.iter(|| black_box(numpy_as.call1((&f32d,)).unwrap()))
+        });
     });
     group.finish();
 }
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:8735:
         let setup = "import numpy as np\n\
 rng = np.random.default_rng(0)\n\
 dt = rng.permutation(16_000_000).astype('datetime64[s]')\n\
-dt_tied = rng.integers(0, 1000, 16_000_000).astype('datetime64[s]')\n";  // distinct + dense-tied
+dt_tied = rng.integers(0, 1000, 16_000_000).astype('datetime64[s]')\n"; // distinct + dense-tied
         let ns = PyDict::new(py);
-        py.run(std::ffi::CString::new(setup).unwrap().as_c_str(), Some(&ns), Some(&ns)).expect("setup");
+        py.run(
+            std::ffi::CString::new(setup).unwrap().as_c_str(),
+            Some(&ns),
+            Some(&ns),
+        )
+        .expect("setup");
         let dt = ns.get_item("dt").expect("dt");
         let dt_tied = ns.get_item("dt_tied").expect("dt_tied");
         let fnp_as = module.getattr("argsort").expect("fnp argsort");
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:8747:
         for (arr, label) in [(&dt, "distinct_default"), (&dt_tied, "tied_default")] {
             let f = fnp_as.call1((arr,)).expect("fnp argsort");
             let n = numpy_as.call1((arr,)).expect("numpy argsort");
-            assert!(eqf.call1((&f, &n)).unwrap().extract::<bool>().unwrap(), "datetime argsort {label} mismatch");
+            assert!(
+                eqf.call1((&f, &n)).unwrap().extract::<bool>().unwrap(),
+                "datetime argsort {label} mismatch"
+            );
         }
         // stable kind on tied datetime (dense) -> counting/radix, bit-exact
-        let kw = PyDict::new(py); kw.set_item("kind", "stable").unwrap();
+        let kw = PyDict::new(py);
+        kw.set_item("kind", "stable").unwrap();
         let fs = fnp_as.call((&dt_tied,), Some(&kw)).expect("fnp stable");
         let ns_ = numpy_as.call((&dt_tied,), Some(&kw)).expect("numpy stable");
-        assert!(eqf.call1((&fs, &ns_)).unwrap().extract::<bool>().unwrap(), "datetime argsort stable tied mismatch");
-        group.bench_function("fnp_argsort_datetime_distinct_default_16m", |bn| bn.iter(|| black_box(fnp_as.call1((&dt,)).unwrap())));
-        group.bench_function("numpy_argsort_datetime_distinct_default_16m", |bn| bn.iter(|| black_box(numpy_as.call1((&dt,)).unwrap())));
+        assert!(
+            eqf.call1((&fs, &ns_)).unwrap().extract::<bool>().unwrap(),
+            "datetime argsort stable tied mismatch"
+        );
+        group.bench_function("fnp_argsort_datetime_distinct_default_16m", |bn| {
+            bn.iter(|| black_box(fnp_as.call1((&dt,)).unwrap()))
+        });
+        group.bench_function("numpy_argsort_datetime_distinct_default_16m", |bn| {
+            bn.iter(|| black_box(numpy_as.call1((&dt,)).unwrap()))
+        });
         group.bench_function("fnp_argsort_datetime_tied_stable_16m", |bn| {
-            let kw = PyDict::new(py); kw.set_item("kind", "stable").unwrap();
+            let kw = PyDict::new(py);
+            kw.set_item("kind", "stable").unwrap();
             bn.iter(|| black_box(fnp_as.call((&dt_tied,), Some(&kw)).unwrap()));
         });
         group.bench_function("numpy_argsort_datetime_tied_stable_16m", |bn| {
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:8764:
-            let kw = PyDict::new(py); kw.set_item("kind", "stable").unwrap();
+            let kw = PyDict::new(py);
+            kw.set_item("kind", "stable").unwrap();
             bn.iter(|| black_box(numpy_as.call((&dt_tied,), Some(&kw)).unwrap()));
         });
     });
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:8789:
 odd_i64  = rng.integers(-500, 500, 16_000_001).astype(np.int64)\n\
 i16 = rng.integers(0, 30000, 16_000_000).astype(np.int16)\n";
         let ns = PyDict::new(py);
-        py.run(std::ffi::CString::new(setup).unwrap().as_c_str(), Some(&ns), Some(&ns)).expect("setup");
+        py.run(
+            std::ffi::CString::new(setup).unwrap().as_c_str(),
+            Some(&ns),
+            Some(&ns),
+        )
+        .expect("setup");
         let fnp_m = module.getattr("median").expect("fnp median");
         let numpy_m = numpy.getattr("median").expect("numpy median");
         for name in ["even_i64", "odd_i64", "i16"] {
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:8796:
             let arr = ns.get_item(name).expect("arr");
             let f = fnp_m.call1((&arr,)).expect("fnp median");
             let n = numpy_m.call1((&arr,)).expect("numpy median");
-            let eq: bool = numpy.getattr("equal").unwrap().call1((&f, &n)).unwrap().extract().unwrap();
+            let eq: bool = numpy
+                .getattr("equal")
+                .unwrap()
+                .call1((&f, &n))
+                .unwrap()
+                .extract()
+                .unwrap();
             assert!(eq, "median {name} mismatch: fnp {:?} numpy {:?}", f, n);
         }
         let ev = ns.get_item("even_i64").expect("ev");
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:8803:
         let i16a = ns.get_item("i16").expect("i16a");
-        group.bench_function("fnp_median_i64_dense_16m", |bn| bn.iter(|| black_box(fnp_m.call1((&ev,)).unwrap())));
-        group.bench_function("numpy_median_i64_dense_16m", |bn| bn.iter(|| black_box(numpy_m.call1((&ev,)).unwrap())));
-        group.bench_function("fnp_median_i16_dense_16m", |bn| bn.iter(|| black_box(fnp_m.call1((&i16a,)).unwrap())));
-        group.bench_function("numpy_median_i16_dense_16m", |bn| bn.iter(|| black_box(numpy_m.call1((&i16a,)).unwrap())));
+        group.bench_function("fnp_median_i64_dense_16m", |bn| {
+            bn.iter(|| black_box(fnp_m.call1((&ev,)).unwrap()))
+        });
+        group.bench_function("numpy_median_i64_dense_16m", |bn| {
+            bn.iter(|| black_box(numpy_m.call1((&ev,)).unwrap()))
+        });
+        group.bench_function("fnp_median_i16_dense_16m", |bn| {
+            bn.iter(|| black_box(fnp_m.call1((&i16a,)).unwrap()))
+        });
+        group.bench_function("numpy_median_i16_dense_16m", |bn| {
+            bn.iter(|| black_box(numpy_m.call1((&i16a,)).unwrap()))
+        });
     });
     group.finish();
 }
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:8829:
 i64 = rng.integers(-500, 500, 16_000_000).astype(np.int64)\n\
 u16 = rng.integers(0, 30000, 16_000_000).astype(np.uint16)\n";
         let ns = PyDict::new(py);
-        py.run(std::ffi::CString::new(setup).unwrap().as_c_str(), Some(&ns), Some(&ns)).expect("setup");
+        py.run(
+            std::ffi::CString::new(setup).unwrap().as_c_str(),
+            Some(&ns),
+            Some(&ns),
+        )
+        .expect("setup");
         let fnp_percentile = module.getattr("percentile").expect("fnp percentile");
         let numpy_percentile = numpy.getattr("percentile").expect("numpy percentile");
         let fnp_quantile = module.getattr("quantile").expect("fnp quantile");
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:8836:
         let numpy_quantile = numpy.getattr("quantile").expect("numpy quantile");
         let i64_arr = ns.get_item("i64").expect("i64");
         let u16_arr = ns.get_item("u16").expect("u16");
-        for (name, arr, p, q) in [("i64", &i64_arr, 12.5_f64, 0.125_f64), ("u16", &u16_arr, 75.0, 0.75)] {
+        for (name, arr, p, q) in [
+            ("i64", &i64_arr, 12.5_f64, 0.125_f64),
+            ("u16", &u16_arr, 75.0, 0.75),
+        ] {
             let fp = fnp_percentile.call1((arr, p)).expect("fnp percentile");
             let np = numpy_percentile.call1((arr, p)).expect("numpy percentile");
             let fq = fnp_quantile.call1((arr, q)).expect("fnp quantile");
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:8843:
             let nq = numpy_quantile.call1((arr, q)).expect("numpy quantile");
-            let eq_p: bool = numpy.getattr("array_equal").unwrap().call1((&fp, &np)).unwrap().extract().unwrap();
-            let eq_q: bool = numpy.getattr("array_equal").unwrap().call1((&fq, &nq)).unwrap().extract().unwrap();
-            assert!(eq_p, "percentile {name} mismatch: fnp {:?} numpy {:?}", fp, np);
-            assert!(eq_q, "quantile {name} mismatch: fnp {:?} numpy {:?}", fq, nq);
+            let eq_p: bool = numpy
+                .getattr("array_equal")
+                .unwrap()
+                .call1((&fp, &np))
+                .unwrap()
+                .extract()
+                .unwrap();
+            let eq_q: bool = numpy
+                .getattr("array_equal")
+                .unwrap()
+                .call1((&fq, &nq))
+                .unwrap()
+                .extract()
+                .unwrap();
+            assert!(
+                eq_p,
+                "percentile {name} mismatch: fnp {:?} numpy {:?}",
+                fp, np
+            );
+            assert!(
+                eq_q,
+                "quantile {name} mismatch: fnp {:?} numpy {:?}",
+                fq, nq
+            );
         }
         group.bench_function("fnp_percentile_i64_dense_16m_p12_5", |bn| {
             bn.iter(|| black_box(fnp_percentile.call1((&i64_arr, 12.5_f64)).unwrap()));
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:8881:
 dt = rng.integers(0, 1000, 8_000_000).astype('datetime64[s]')\n\
 cz = (rng.integers(0, 100, 8_000_000) + 1j*rng.integers(0, 100, 8_000_000)).astype(np.complex128)\n";
         let ns = PyDict::new(py);
-        py.run(std::ffi::CString::new(setup).unwrap().as_c_str(), Some(&ns), Some(&ns))
-            .expect("setup");
+        py.run(
+            std::ffi::CString::new(setup).unwrap().as_c_str(),
+            Some(&ns),
+            Some(&ns),
+        )
+        .expect("setup");
         let dt = ns.get_item("dt").expect("dt");
         let cz = ns.get_item("cz").expect("cz");
         let fnp_as = module.getattr("argsort").expect("fnp argsort");
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:8941:
 u = rng.integers(97, 123, (2_000_000, 6), dtype=np.uint32).reshape(-1).view('U6')\n\
 s = rng.integers(97, 123, (2_000_000, 6), dtype=np.uint8).reshape(-1).view('S6')\n";
         let ns = PyDict::new(py);
-        py.run(std::ffi::CString::new(setup).unwrap().as_c_str(), Some(&ns), Some(&ns)).expect("setup");
+        py.run(
+            std::ffi::CString::new(setup).unwrap().as_c_str(),
+            Some(&ns),
+            Some(&ns),
+        )
+        .expect("setup");
         let u = ns.get_item("u").expect("u");
         let s = ns.get_item("s").expect("s");
         let fnp_as = module.getattr("argsort").expect("fnp argsort");
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:8948:
         let numpy_as = numpy.getattr("argsort").expect("numpy argsort");
         let eqf = numpy.getattr("array_equal").expect("np.array_equal");
         for (arr, label) in [(&u, "U6"), (&s, "S6")] {
-            let kw = PyDict::new(py); kw.set_item("kind", "stable").unwrap();
+            let kw = PyDict::new(py);
+            kw.set_item("kind", "stable").unwrap();
             let f = fnp_as.call((arr,), Some(&kw)).expect("fnp argsort");
             let n = numpy_as.call((arr,), Some(&kw)).expect("numpy argsort");
-            assert!(eqf.call1((&f, &n)).unwrap().extract::<bool>().unwrap(), "argsort {label} stable mismatch");
+            assert!(
+                eqf.call1((&f, &n)).unwrap().extract::<bool>().unwrap(),
+                "argsort {label} stable mismatch"
+            );
         }
         group.bench_function("fnp_argsort_U6_stable_2m", |bn| {
-            let kw = PyDict::new(py); kw.set_item("kind", "stable").unwrap();
+            let kw = PyDict::new(py);
+            kw.set_item("kind", "stable").unwrap();
             bn.iter(|| black_box(fnp_as.call((&u,), Some(&kw)).unwrap()));
         });
         group.bench_function("numpy_argsort_U6_stable_2m", |bn| {
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:8961:
-            let kw = PyDict::new(py); kw.set_item("kind", "stable").unwrap();
+            let kw = PyDict::new(py);
+            kw.set_item("kind", "stable").unwrap();
             bn.iter(|| black_box(numpy_as.call((&u,), Some(&kw)).unwrap()));
         });
         group.bench_function("fnp_argsort_S6_stable_2m", |bn| {
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:8965:
-            let kw = PyDict::new(py); kw.set_item("kind", "stable").unwrap();
+            let kw = PyDict::new(py);
+            kw.set_item("kind", "stable").unwrap();
             bn.iter(|| black_box(fnp_as.call((&s,), Some(&kw)).unwrap()));
         });
         group.bench_function("numpy_argsort_S6_stable_2m", |bn| {
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:8969:
-            let kw = PyDict::new(py); kw.set_item("kind", "stable").unwrap();
+            let kw = PyDict::new(py);
+            kw.set_item("kind", "stable").unwrap();
             bn.iter(|| black_box(numpy_as.call((&s,), Some(&kw)).unwrap()));
         });
     });
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:8992:
 dt = [('id','<i8'),('val','<f8')]\n\
 a = np.zeros(2_000_000, dtype=dt); a['id'] = rng.integers(0, 100000, 2_000_000); a['val'] = rng.integers(0, 100000, 2_000_000).astype(np.float64)\n";
         let ns = PyDict::new(py);
-        py.run(std::ffi::CString::new(setup).unwrap().as_c_str(), Some(&ns), Some(&ns)).expect("setup");
+        py.run(
+            std::ffi::CString::new(setup).unwrap().as_c_str(),
+            Some(&ns),
+            Some(&ns),
+        )
+        .expect("setup");
         let a = ns.get_item("a").expect("a");
         let fnp_as = module.getattr("argsort").expect("fnp argsort");
         let numpy_as = numpy.getattr("argsort").expect("numpy argsort");
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:8999:
         let eqf = numpy.getattr("array_equal").expect("np.array_equal");
-        let kw = PyDict::new(py); kw.set_item("kind", "stable").unwrap();
+        let kw = PyDict::new(py);
+        kw.set_item("kind", "stable").unwrap();
         let f = fnp_as.call((&a,), Some(&kw)).expect("fnp argsort");
         let n = numpy_as.call((&a,), Some(&kw)).expect("numpy argsort");
-        assert!(eqf.call1((&f, &n)).unwrap().extract::<bool>().unwrap(), "argsort struct stable mismatch");
+        assert!(
+            eqf.call1((&f, &n)).unwrap().extract::<bool>().unwrap(),
+            "argsort struct stable mismatch"
+        );
         group.bench_function("fnp_argsort_struct_i8f8_2m", |bn| {
-            let kw = PyDict::new(py); kw.set_item("kind", "stable").unwrap();
+            let kw = PyDict::new(py);
+            kw.set_item("kind", "stable").unwrap();
             bn.iter(|| black_box(fnp_as.call((&a,), Some(&kw)).unwrap()));
         });
         group.bench_function("numpy_argsort_struct_i8f8_2m", |bn| {
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:9009:
-            let kw = PyDict::new(py); kw.set_item("kind", "stable").unwrap();
+            let kw = PyDict::new(py);
+            kw.set_item("kind", "stable").unwrap();
             bn.iter(|| black_box(numpy_as.call((&a,), Some(&kw)).unwrap()));
         });
     });
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:9031:
 rng = np.random.default_rng(0)\n\
 s = rng.integers(97, 123, (2_000_000, 8), dtype=np.uint32).reshape(-1).view('U8')\n";
         let ns = PyDict::new(py);
-        py.run(std::ffi::CString::new(setup).unwrap().as_c_str(), Some(&ns), Some(&ns)).expect("setup");
+        py.run(
+            std::ffi::CString::new(setup).unwrap().as_c_str(),
+            Some(&ns),
+            Some(&ns),
+        )
+        .expect("setup");
         let s = ns.get_item("s").expect("s");
         let eqf = numpy.getattr("array_equal").expect("np.array_equal");
         // correctness: compare each namedtuple field of unique_counts and unique_all
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:9038:
         for op in ["unique_counts", "unique_all"] {
             let fnp_fn = module.getattr(op).expect("fnp op");
             let np_fn = numpy.getattr(op).expect("numpy op");
-            let f = fnp_fn.call1((&s,)).expect("fnp").downcast_into::<pyo3::types::PyTuple>().unwrap();
-            let n = np_fn.call1((&s,)).expect("numpy").downcast_into::<pyo3::types::PyTuple>().unwrap();
+            let f = fnp_fn
+                .call1((&s,))
+                .expect("fnp")
+                .downcast_into::<pyo3::types::PyTuple>()
+                .unwrap();
+            let n = np_fn
+                .call1((&s,))
+                .expect("numpy")
+                .downcast_into::<pyo3::types::PyTuple>()
+                .unwrap();
             let nfields = if op == "unique_counts" { 2 } else { 4 };
             for i in 0..nfields {
-                let eq: bool = eqf.call1((f.get_item(i).unwrap(), n.get_item(i).unwrap())).unwrap().extract().unwrap();
+                let eq: bool = eqf
+                    .call1((f.get_item(i).unwrap(), n.get_item(i).unwrap()))
+                    .unwrap()
+                    .extract()
+                    .unwrap();
                 assert!(eq, "{op} field {i} mismatch");
             }
         }
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:9050:
         let np_uc = numpy.getattr("unique_counts").unwrap();
         let fnp_ua = module.getattr("unique_all").unwrap();
         let np_ua = numpy.getattr("unique_all").unwrap();
-        group.bench_function("fnp_unique_counts_U8_2m", |bn| bn.iter(|| black_box(fnp_uc.call1((&s,)).unwrap())));
-        group.bench_function("numpy_unique_counts_U8_2m", |bn| bn.iter(|| black_box(np_uc.call1((&s,)).unwrap())));
-        group.bench_function("fnp_unique_all_U8_2m", |bn| bn.iter(|| black_box(fnp_ua.call1((&s,)).unwrap())));
-        group.bench_function("numpy_unique_all_U8_2m", |bn| bn.iter(|| black_box(np_ua.call1((&s,)).unwrap())));
+        group.bench_function("fnp_unique_counts_U8_2m", |bn| {
+            bn.iter(|| black_box(fnp_uc.call1((&s,)).unwrap()))
+        });
+        group.bench_function("numpy_unique_counts_U8_2m", |bn| {
+            bn.iter(|| black_box(np_uc.call1((&s,)).unwrap()))
+        });
+        group.bench_function("fnp_unique_all_U8_2m", |bn| {
+            bn.iter(|| black_box(fnp_ua.call1((&s,)).unwrap()))
+        });
+        group.bench_function("numpy_unique_all_U8_2m", |bn| {
+            bn.iter(|| black_box(np_ua.call1((&s,)).unwrap()))
+        });
     });
     group.finish();
 }
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:9078:
 a = np.zeros(1_000_000, dtype=dt); a['a'] = rng.integers(0, 1000, 1_000_000); a['b'] = rng.integers(0, 1000, 1_000_000)\n\
 b = np.zeros(500_000, dtype=dt); b['a'] = rng.integers(0, 1000, 500_000); b['b'] = rng.integers(0, 1000, 500_000)\n";
         let ns = PyDict::new(py);
-        py.run(std::ffi::CString::new(setup).unwrap().as_c_str(), Some(&ns), Some(&ns)).expect("setup");
+        py.run(
+            std::ffi::CString::new(setup).unwrap().as_c_str(),
+            Some(&ns),
+            Some(&ns),
+        )
+        .expect("setup");
         let a = ns.get_item("a").expect("a");
         let b = ns.get_item("b").expect("b");
         let fnp_isin = module.getattr("isin").expect("fnp isin");
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:9086:
         let eqf = numpy.getattr("array_equal").expect("np.array_equal");
         let f = fnp_isin.call1((&a, &b)).expect("fnp isin");
         let n = numpy_isin.call1((&a, &b)).expect("numpy isin");
-        assert!(eqf.call1((&f, &n)).unwrap().extract::<bool>().unwrap(), "isin struct mismatch");
-        group.bench_function("fnp_isin_struct_2xi8_1m_500k", |bn| bn.iter(|| black_box(fnp_isin.call1((&a, &b)).unwrap())));
-        group.bench_function("numpy_isin_struct_2xi8_1m_500k", |bn| bn.iter(|| black_box(numpy_isin.call1((&a, &b)).unwrap())));
+        assert!(
+            eqf.call1((&f, &n)).unwrap().extract::<bool>().unwrap(),
+            "isin struct mismatch"
+        );
+        group.bench_function("fnp_isin_struct_2xi8_1m_500k", |bn| {
+            bn.iter(|| black_box(fnp_isin.call1((&a, &b)).unwrap()))
+        });
+        group.bench_function("numpy_isin_struct_2xi8_1m_500k", |bn| {
+            bn.iter(|| black_box(numpy_isin.call1((&a, &b)).unwrap()))
+        });
     });
     group.finish();
 }
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:9113:
 a = np.zeros(1_000_000, dtype=dt); a['id'] = rng.integers(0, 1000, 1_000_000); a['val'] = rng.integers(0, 1000, 1_000_000).astype(np.float64)\n\
 b = np.zeros(500_000, dtype=dt); b['id'] = rng.integers(0, 1000, 500_000); b['val'] = rng.integers(0, 1000, 500_000).astype(np.float64)\n";
         let ns = PyDict::new(py);
-        py.run(std::ffi::CString::new(setup).unwrap().as_c_str(), Some(&ns), Some(&ns)).expect("setup");
+        py.run(
+            std::ffi::CString::new(setup).unwrap().as_c_str(),
+            Some(&ns),
+            Some(&ns),
+        )
+        .expect("setup");
         let a = ns.get_item("a").expect("a");
         let b = ns.get_item("b").expect("b");
         let fnp_isin = module.getattr("isin").expect("fnp isin");
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:9121:
         let eqf = numpy.getattr("array_equal").expect("np.array_equal");
         let f = fnp_isin.call1((&a, &b)).expect("fnp isin");
         let n = numpy_isin.call1((&a, &b)).expect("numpy isin");
-        assert!(eqf.call1((&f, &n)).unwrap().extract::<bool>().unwrap(), "isin struct int+f8 mismatch");
-        group.bench_function("fnp_isin_struct_i8f8_1m_500k", |bn| bn.iter(|| black_box(fnp_isin.call1((&a, &b)).unwrap())));
-        group.bench_function("numpy_isin_struct_i8f8_1m_500k", |bn| bn.iter(|| black_box(numpy_isin.call1((&a, &b)).unwrap())));
+        assert!(
+            eqf.call1((&f, &n)).unwrap().extract::<bool>().unwrap(),
+            "isin struct int+f8 mismatch"
+        );
+        group.bench_function("fnp_isin_struct_i8f8_1m_500k", |bn| {
+            bn.iter(|| black_box(fnp_isin.call1((&a, &b)).unwrap()))
+        });
+        group.bench_function("numpy_isin_struct_i8f8_1m_500k", |bn| {
+            bn.iter(|| black_box(numpy_isin.call1((&a, &b)).unwrap()))
+        });
     });
     group.finish();
 }
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:9148:
 hay = np.zeros(2_000_000, dtype=dt); hay['a'] = rng.integers(0, 100000, 2_000_000); hay['b'] = rng.integers(0, 100000, 2_000_000); hay = np.sort(hay)\n\
 q = np.zeros(2_000_000, dtype=dt); q['a'] = rng.integers(0, 100000, 2_000_000); q['b'] = rng.integers(0, 100000, 2_000_000)\n";
         let ns = PyDict::new(py);
-        py.run(std::ffi::CString::new(setup).unwrap().as_c_str(), Some(&ns), Some(&ns)).expect("setup");
+        py.run(
+            std::ffi::CString::new(setup).unwrap().as_c_str(),
+            Some(&ns),
+            Some(&ns),
+        )
+        .expect("setup");
         let hay = ns.get_item("hay").expect("hay");
         let q = ns.get_item("q").expect("q");
         let fnp_ss = module.getattr("searchsorted").expect("fnp searchsorted");
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:9155:
         let numpy_ss = numpy.getattr("searchsorted").expect("numpy searchsorted");
         let eqf = numpy.getattr("array_equal").expect("np.array_equal");
         for side in ["left", "right"] {
-            let kw = PyDict::new(py); kw.set_item("side", side).unwrap();
-            let f = fnp_ss.call((&hay, &q), Some(&kw)).expect("fnp searchsorted");
-            let n = numpy_ss.call((&hay, &q), Some(&kw)).expect("numpy searchsorted");
-            assert!(eqf.call1((&f, &n)).unwrap().extract::<bool>().unwrap(), "searchsorted struct side={side} mismatch");
+            let kw = PyDict::new(py);
+            kw.set_item("side", side).unwrap();
+            let f = fnp_ss
+                .call((&hay, &q), Some(&kw))
+                .expect("fnp searchsorted");
+            let n = numpy_ss
+                .call((&hay, &q), Some(&kw))
+                .expect("numpy searchsorted");
+            assert!(
+                eqf.call1((&f, &n)).unwrap().extract::<bool>().unwrap(),
+                "searchsorted struct side={side} mismatch"
+            );
         }
-        group.bench_function("fnp_searchsorted_struct_2xi8_2m_2m", |bn| bn.iter(|| black_box(fnp_ss.call1((&hay, &q)).unwrap())));
-        group.bench_function("numpy_searchsorted_struct_2xi8_2m_2m", |bn| bn.iter(|| black_box(numpy_ss.call1((&hay, &q)).unwrap())));
+        group.bench_function("fnp_searchsorted_struct_2xi8_2m_2m", |bn| {
+            bn.iter(|| black_box(fnp_ss.call1((&hay, &q)).unwrap()))
+        });
+        group.bench_function("numpy_searchsorted_struct_2xi8_2m_2m", |bn| {
+            bn.iter(|| black_box(numpy_ss.call1((&hay, &q)).unwrap()))
+        });

         let setup_u64 = "import numpy as np\n\
 rng = np.random.default_rng(1)\n\
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:9175:
 qa = rng.integers(0, n, n, dtype=np.uint64)\n\
 q_u['a'] = qa // np.uint64(1000)\n\
 q_u['b'] = qa % np.uint64(1000)\n";
-        py.run(std::ffi::CString::new(setup_u64).unwrap().as_c_str(), Some(&ns), Some(&ns)).expect("u64 setup");
+        py.run(
+            std::ffi::CString::new(setup_u64).unwrap().as_c_str(),
+            Some(&ns),
+            Some(&ns),
+        )
+        .expect("u64 setup");
         let hay_u = ns.get_item("hay_u").expect("hay_u");
         let q_u = ns.get_item("q_u").expect("q_u");
         for side in ["left", "right"] {
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:9182:
-            let kw = PyDict::new(py); kw.set_item("side", side).unwrap();
-            let f = fnp_ss.call((&hay_u, &q_u), Some(&kw)).expect("fnp u64 searchsorted");
-            let n = numpy_ss.call((&hay_u, &q_u), Some(&kw)).expect("numpy u64 searchsorted");
-            assert!(eqf.call1((&f, &n)).unwrap().extract::<bool>().unwrap(), "u64 searchsorted struct side={side} mismatch");
+            let kw = PyDict::new(py);
+            kw.set_item("side", side).unwrap();
+            let f = fnp_ss
+                .call((&hay_u, &q_u), Some(&kw))
+                .expect("fnp u64 searchsorted");
+            let n = numpy_ss
+                .call((&hay_u, &q_u), Some(&kw))
+                .expect("numpy u64 searchsorted");
+            assert!(
+                eqf.call1((&f, &n)).unwrap().extract::<bool>().unwrap(),
+                "u64 searchsorted struct side={side} mismatch"
+            );
         }
-        group.bench_function("fnp_searchsorted_struct_2xu8_1m_1m", |bn| bn.iter(|| black_box(fnp_ss.call1((&hay_u, &q_u)).unwrap())));
-        group.bench_function("numpy_searchsorted_struct_2xu8_1m_1m", |bn| bn.iter(|| black_box(numpy_ss.call1((&hay_u, &q_u)).unwrap())));
+        group.bench_function("fnp_searchsorted_struct_2xu8_1m_1m", |bn| {
+            bn.iter(|| black_box(fnp_ss.call1((&hay_u, &q_u)).unwrap()))
+        });
+        group.bench_function("numpy_searchsorted_struct_2xu8_1m_1m", |bn| {
+            bn.iter(|| black_box(numpy_ss.call1((&hay_u, &q_u)).unwrap()))
+        });
     });
     group.finish();
 }
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:9210:
 hay = np.zeros(2_000_000, dtype=dt); hay['id'] = rng.integers(0, 100000, 2_000_000); hay['val'] = rng.integers(0, 100000, 2_000_000).astype(np.float64); hay = np.sort(hay)\n\
 q = np.zeros(2_000_000, dtype=dt); q['id'] = rng.integers(0, 100000, 2_000_000); q['val'] = rng.integers(0, 100000, 2_000_000).astype(np.float64)\n";
         let ns = PyDict::new(py);
-        py.run(std::ffi::CString::new(setup).unwrap().as_c_str(), Some(&ns), Some(&ns)).expect("setup");
+        py.run(
+            std::ffi::CString::new(setup).unwrap().as_c_str(),
+            Some(&ns),
+            Some(&ns),
+        )
+        .expect("setup");
         let hay = ns.get_item("hay").expect("hay");
         let q = ns.get_item("q").expect("q");
         let fnp_ss = module.getattr("searchsorted").expect("fnp searchsorted");
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:9217:
         let numpy_ss = numpy.getattr("searchsorted").expect("numpy searchsorted");
         let eqf = numpy.getattr("array_equal").expect("np.array_equal");
         for side in ["left", "right"] {
-            let kw = PyDict::new(py); kw.set_item("side", side).unwrap();
-            let f = fnp_ss.call((&hay, &q), Some(&kw)).expect("fnp searchsorted");
-            let n = numpy_ss.call((&hay, &q), Some(&kw)).expect("numpy searchsorted");
-            assert!(eqf.call1((&f, &n)).unwrap().extract::<bool>().unwrap(), "searchsorted mixed struct side={side} mismatch");
+            let kw = PyDict::new(py);
+            kw.set_item("side", side).unwrap();
+            let f = fnp_ss
+                .call((&hay, &q), Some(&kw))
+                .expect("fnp searchsorted");
+            let n = numpy_ss
+                .call((&hay, &q), Some(&kw))
+                .expect("numpy searchsorted");
+            assert!(
+                eqf.call1((&f, &n)).unwrap().extract::<bool>().unwrap(),
+                "searchsorted mixed struct side={side} mismatch"
+            );
         }
-        group.bench_function("fnp_searchsorted_struct_i8f8_2m_2m", |bn| bn.iter(|| black_box(fnp_ss.call1((&hay, &q)).unwrap())));
-        group.bench_function("numpy_searchsorted_struct_i8f8_2m_2m", |bn| bn.iter(|| black_box(numpy_ss.call1((&hay, &q)).unwrap())));
+        group.bench_function("fnp_searchsorted_struct_i8f8_2m_2m", |bn| {
+            bn.iter(|| black_box(fnp_ss.call1((&hay, &q)).unwrap()))
+        });
+        group.bench_function("numpy_searchsorted_struct_i8f8_2m_2m", |bn| {
+            bn.iter(|| black_box(numpy_ss.call1((&hay, &q)).unwrap()))
+        });
     });
     group.finish();
 }
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:9248:
 a = np.zeros(1_000_000, dtype=dt); a['a'] = rng.integers(0, 3000, 1_000_000); a['b'] = rng.integers(0, 3000, 1_000_000)\n\
 b = np.zeros(1_000_000, dtype=dt); b['a'] = rng.integers(0, 3000, 1_000_000); b['b'] = rng.integers(0, 3000, 1_000_000)\n";
         let ns = PyDict::new(py);
-        py.run(std::ffi::CString::new(setup).unwrap().as_c_str(), Some(&ns), Some(&ns)).expect("setup");
+        py.run(
+            std::ffi::CString::new(setup).unwrap().as_c_str(),
+            Some(&ns),
+            Some(&ns),
+        )
+        .expect("setup");
         let a = ns.get_item("a").expect("a");
         let b = ns.get_item("b").expect("b");
         let eqf = numpy.getattr("array_equal").expect("np.array_equal");
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:9257:
             let np_fn = numpy.getattr(op).expect("numpy op");
             let f = fnp_fn.call1((&a, &b)).expect("fnp setop");
             let n = np_fn.call1((&a, &b)).expect("numpy setop");
-            assert!(eqf.call1((&f, &n)).unwrap().extract::<bool>().unwrap(), "{op} struct mismatch");
-            group.bench_function(format!("fnp_{op}_struct_1m_1m"), |bn| bn.iter(|| black_box(fnp_fn.call1((&a, &b)).unwrap())));
-            group.bench_function(format!("numpy_{op}_struct_1m_1m"), |bn| bn.iter(|| black_box(np_fn.call1((&a, &b)).unwrap())));
+            assert!(
+                eqf.call1((&f, &n)).unwrap().extract::<bool>().unwrap(),
+                "{op} struct mismatch"
+            );
+            group.bench_function(format!("fnp_{op}_struct_1m_1m"), |bn| {
+                bn.iter(|| black_box(fnp_fn.call1((&a, &b)).unwrap()))
+            });
+            group.bench_function(format!("numpy_{op}_struct_1m_1m"), |bn| {
+                bn.iter(|| black_box(np_fn.call1((&a, &b)).unwrap()))
+            });
         }
     });
     group.finish();
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:9288:
 a32 = np.zeros(1_000_000, dtype=dt32); a32['id'] = rng.integers(0, 3000, 1_000_000, dtype=np.int32); a32['val'] = rng.integers(0, 3000, 1_000_000).astype(np.float32)\n\
 b32 = np.zeros(1_000_000, dtype=dt32); b32['id'] = rng.integers(0, 3000, 1_000_000, dtype=np.int32); b32['val'] = rng.integers(0, 3000, 1_000_000).astype(np.float32)\n";
         let ns = PyDict::new(py);
-        py.run(std::ffi::CString::new(setup).unwrap().as_c_str(), Some(&ns), Some(&ns)).expect("setup");
+        py.run(
+            std::ffi::CString::new(setup).unwrap().as_c_str(),
+            Some(&ns),
+            Some(&ns),
+        )
+        .expect("setup");
         let a = ns.get_item("a").expect("a");
         let b = ns.get_item("b").expect("b");
         let a32 = ns.get_item("a32").expect("a32");
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:9299:
             let np_fn = numpy.getattr(op).expect("numpy op");
             let f = fnp_fn.call1((&a, &b)).expect("fnp setop");
             let n = np_fn.call1((&a, &b)).expect("numpy setop");
-            assert!(eqf.call1((&f, &n)).unwrap().extract::<bool>().unwrap(), "{op} mixed struct mismatch");
-            group.bench_function(format!("fnp_{op}_struct_i8f8_1m_1m"), |bn| bn.iter(|| black_box(fnp_fn.call1((&a, &b)).unwrap())));
-            group.bench_function(format!("numpy_{op}_struct_i8f8_1m_1m"), |bn| bn.iter(|| black_box(np_fn.call1((&a, &b)).unwrap())));
+            assert!(
+                eqf.call1((&f, &n)).unwrap().extract::<bool>().unwrap(),
+                "{op} mixed struct mismatch"
+            );
+            group.bench_function(format!("fnp_{op}_struct_i8f8_1m_1m"), |bn| {
+                bn.iter(|| black_box(fnp_fn.call1((&a, &b)).unwrap()))
+            });
+            group.bench_function(format!("numpy_{op}_struct_i8f8_1m_1m"), |bn| {
+                bn.iter(|| black_box(np_fn.call1((&a, &b)).unwrap()))
+            });
             let f32 = fnp_fn.call1((&a32, &b32)).expect("fnp i4f4 setop");
             let n32 = np_fn.call1((&a32, &b32)).expect("numpy i4f4 setop");
             assert!(
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:9338:
 a = (rng.integers(0, 3000, 2_000_000) + 1j * rng.integers(0, 3000, 2_000_000)).astype(np.complex128)\n\
 b = (rng.integers(0, 3000, 2_000_000) + 1j * rng.integers(0, 3000, 2_000_000)).astype(np.complex128)\n";
         let ns = PyDict::new(py);
-        py.run(std::ffi::CString::new(setup).unwrap().as_c_str(), Some(&ns), Some(&ns)).expect("setup");
+        py.run(
+            std::ffi::CString::new(setup).unwrap().as_c_str(),
+            Some(&ns),
+            Some(&ns),
+        )
+        .expect("setup");
         let a = ns.get_item("a").expect("a");
         let b = ns.get_item("b").expect("b");
         let eqf = numpy.getattr("array_equal").expect("np.array_equal");
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:9348:
             let np_fn = numpy.getattr(op).expect("numpy op");
             let f = fnp_fn.call1((&a, &b)).expect("fnp setop");
             let n = np_fn.call1((&a, &b)).expect("numpy setop");
-            assert!(eqf.call1((&f, &n)).unwrap().extract::<bool>().unwrap(), "{op} c128 mismatch");
-            group.bench_function(format!("fnp_{op}_c128_2m_2m"), |bn| bn.iter(|| black_box(fnp_fn.call1((&a, &b)).unwrap())));
-            group.bench_function(format!("numpy_{op}_c128_2m_2m"), |bn| bn.iter(|| black_box(np_fn.call1((&a, &b)).unwrap())));
+            assert!(
+                eqf.call1((&f, &n)).unwrap().extract::<bool>().unwrap(),
+                "{op} c128 mismatch"
+            );
+            group.bench_function(format!("fnp_{op}_c128_2m_2m"), |bn| {
+                bn.iter(|| black_box(fnp_fn.call1((&a, &b)).unwrap()))
+            });
+            group.bench_function(format!("numpy_{op}_c128_2m_2m"), |bn| {
+                bn.iter(|| black_box(np_fn.call1((&a, &b)).unwrap()))
+            });
         }
     });
     group.finish();
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:9375:
 a = rng.integers(0, 3000, 2_000_000).astype('datetime64[s]')\n\
 b = rng.integers(0, 3000, 2_000_000).astype('datetime64[s]')\n";
         let ns = PyDict::new(py);
-        py.run(std::ffi::CString::new(setup).unwrap().as_c_str(), Some(&ns), Some(&ns)).expect("setup");
+        py.run(
+            std::ffi::CString::new(setup).unwrap().as_c_str(),
+            Some(&ns),
+            Some(&ns),
+        )
+        .expect("setup");
         let a = ns.get_item("a").expect("a");
         let b = ns.get_item("b").expect("b");
         let eqf = numpy.getattr("array_equal").expect("np.array_equal");
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:9384:
             let np_fn = numpy.getattr(op).expect("numpy op");
             let f = fnp_fn.call1((&a, &b)).expect("fnp setop");
             let n = np_fn.call1((&a, &b)).expect("numpy setop");
-            assert!(eqf.call1((&f, &n)).unwrap().extract::<bool>().unwrap(), "{op} datetime mismatch");
-            group.bench_function(format!("fnp_{op}_datetime_2m_2m"), |bn| bn.iter(|| black_box(fnp_fn.call1((&a, &b)).unwrap())));
-            group.bench_function(format!("numpy_{op}_datetime_2m_2m"), |bn| bn.iter(|| black_box(np_fn.call1((&a, &b)).unwrap())));
+            assert!(
+                eqf.call1((&f, &n)).unwrap().extract::<bool>().unwrap(),
+                "{op} datetime mismatch"
+            );
+            group.bench_function(format!("fnp_{op}_datetime_2m_2m"), |bn| {
+                bn.iter(|| black_box(fnp_fn.call1((&a, &b)).unwrap()))
+            });
+            group.bench_function(format!("numpy_{op}_datetime_2m_2m"), |bn| {
+                bn.iter(|| black_box(np_fn.call1((&a, &b)).unwrap()))
+            });
         }
     });
     group.finish();
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:9413:
 ia = rng.integers(0, 100000, 2_000_000).astype('datetime64[s]')\n\
 ib = rng.integers(0, 100000, 1_000_000).astype('datetime64[s]')\n";
         let ns = PyDict::new(py);
-        py.run(std::ffi::CString::new(setup).unwrap().as_c_str(), Some(&ns), Some(&ns)).expect("setup");
-        let (hay, q, ia, ib) = (ns.get_item("hay").unwrap(), ns.get_item("q").unwrap(), ns.get_item("ia").unwrap(), ns.get_item("ib").unwrap());
+        py.run(
+            std::ffi::CString::new(setup).unwrap().as_c_str(),
+            Some(&ns),
+            Some(&ns),
+        )
+        .expect("setup");
+        let (hay, q, ia, ib) = (
+            ns.get_item("hay").unwrap(),
+            ns.get_item("q").unwrap(),
+            ns.get_item("ia").unwrap(),
+            ns.get_item("ib").unwrap(),
+        );
         let eqf = numpy.getattr("array_equal").expect("np.array_equal");
         let fnp_ss = module.getattr("searchsorted").unwrap();
         let numpy_ss = numpy.getattr("searchsorted").unwrap();
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:9421:
         let fnp_isin = module.getattr("isin").unwrap();
         let numpy_isin = numpy.getattr("isin").unwrap();
         for side in ["left", "right"] {
-            let kw = PyDict::new(py); kw.set_item("side", side).unwrap();
+            let kw = PyDict::new(py);
+            kw.set_item("side", side).unwrap();
             let f = fnp_ss.call((&hay, &q), Some(&kw)).unwrap();
             let n = numpy_ss.call((&hay, &q), Some(&kw)).unwrap();
-            assert!(eqf.call1((&f, &n)).unwrap().extract::<bool>().unwrap(), "datetime searchsorted {side} mismatch");
+            assert!(
+                eqf.call1((&f, &n)).unwrap().extract::<bool>().unwrap(),
+                "datetime searchsorted {side} mismatch"
+            );
         }
         let fi = fnp_isin.call1((&ia, &ib)).unwrap();
         let ni = numpy_isin.call1((&ia, &ib)).unwrap();
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:9431:
-        assert!(eqf.call1((&fi, &ni)).unwrap().extract::<bool>().unwrap(), "datetime isin mismatch");
-        group.bench_function("fnp_searchsorted_datetime_2m_2m", |bn| bn.iter(|| black_box(fnp_ss.call1((&hay, &q)).unwrap())));
-        group.bench_function("numpy_searchsorted_datetime_2m_2m", |bn| bn.iter(|| black_box(numpy_ss.call1((&hay, &q)).unwrap())));
-        group.bench_function("fnp_isin_datetime_2m_1m", |bn| bn.iter(|| black_box(fnp_isin.call1((&ia, &ib)).unwrap())));
-        group.bench_function("numpy_isin_datetime_2m_1m", |bn| bn.iter(|| black_box(numpy_isin.call1((&ia, &ib)).unwrap())));
+        assert!(
+            eqf.call1((&fi, &ni)).unwrap().extract::<bool>().unwrap(),
+            "datetime isin mismatch"
+        );
+        group.bench_function("fnp_searchsorted_datetime_2m_2m", |bn| {
+            bn.iter(|| black_box(fnp_ss.call1((&hay, &q)).unwrap()))
+        });
+        group.bench_function("numpy_searchsorted_datetime_2m_2m", |bn| {
+            bn.iter(|| black_box(numpy_ss.call1((&hay, &q)).unwrap()))
+        });
+        group.bench_function("fnp_isin_datetime_2m_1m", |bn| {
+            bn.iter(|| black_box(fnp_isin.call1((&ia, &ib)).unwrap()))
+        });
+        group.bench_function("numpy_isin_datetime_2m_1m", |bn| {
+            bn.iter(|| black_box(numpy_isin.call1((&ia, &ib)).unwrap()))
+        });
     });
     group.finish();
 }
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:9459:
 ia = (rng.integers(0, 2000, 2_000_000) / 7).astype(np.float16)\n\
 ib = (rng.integers(0, 2000, 1_000_000) / 7).astype(np.float16)\n";
         let ns = PyDict::new(py);
-        py.run(std::ffi::CString::new(setup).unwrap().as_c_str(), Some(&ns), Some(&ns)).expect("setup");
-        let (u, hs, hq, ia, ib) = (ns.get_item("u").unwrap(), ns.get_item("hs").unwrap(), ns.get_item("hq").unwrap(), ns.get_item("ia").unwrap(), ns.get_item("ib").unwrap());
+        py.run(
+            std::ffi::CString::new(setup).unwrap().as_c_str(),
+            Some(&ns),
+            Some(&ns),
+        )
+        .expect("setup");
+        let (u, hs, hq, ia, ib) = (
+            ns.get_item("u").unwrap(),
+            ns.get_item("hs").unwrap(),
+            ns.get_item("hq").unwrap(),
+            ns.get_item("ia").unwrap(),
+            ns.get_item("ib").unwrap(),
+        );
         let eqf = numpy.getattr("array_equal").expect("np.array_equal");
-        let (fu, nu_) = (module.getattr("unique").unwrap(), numpy.getattr("unique").unwrap());
-        let (fss, nss) = (module.getattr("searchsorted").unwrap(), numpy.getattr("searchsorted").unwrap());
-        let (fi, ni) = (module.getattr("isin").unwrap(), numpy.getattr("isin").unwrap());
-        assert!(eqf.call1((fu.call1((&u,)).unwrap(), nu_.call1((&u,)).unwrap())).unwrap().extract::<bool>().unwrap(), "f16 unique mismatch");
-        assert!(eqf.call1((fss.call1((&hs, &hq)).unwrap(), nss.call1((&hs, &hq)).unwrap())).unwrap().extract::<bool>().unwrap(), "f16 searchsorted mismatch");
-        assert!(eqf.call1((fi.call1((&ia, &ib)).unwrap(), ni.call1((&ia, &ib)).unwrap())).unwrap().extract::<bool>().unwrap(), "f16 isin mismatch");
-        group.bench_function("fnp_unique_f16_4m", |bn| bn.iter(|| black_box(fu.call1((&u,)).unwrap())));
-        group.bench_function("numpy_unique_f16_4m", |bn| bn.iter(|| black_box(nu_.call1((&u,)).unwrap())));
-        group.bench_function("fnp_searchsorted_f16_2m_2m", |bn| bn.iter(|| black_box(fss.call1((&hs, &hq)).unwrap())));
-        group.bench_function("numpy_searchsorted_f16_2m_2m", |bn| bn.iter(|| black_box(nss.call1((&hs, &hq)).unwrap())));
-        group.bench_function("fnp_isin_f16_2m_1m", |bn| bn.iter(|| black_box(fi.call1((&ia, &ib)).unwrap())));
-        group.bench_function("numpy_isin_f16_2m_1m", |bn| bn.iter(|| black_box(ni.call1((&ia, &ib)).unwrap())));
+        let (fu, nu_) = (
+            module.getattr("unique").unwrap(),
+            numpy.getattr("unique").unwrap(),
+        );
+        let (fss, nss) = (
+            module.getattr("searchsorted").unwrap(),
+            numpy.getattr("searchsorted").unwrap(),
+        );
+        let (fi, ni) = (
+            module.getattr("isin").unwrap(),
+            numpy.getattr("isin").unwrap(),
+        );
+        assert!(
+            eqf.call1((fu.call1((&u,)).unwrap(), nu_.call1((&u,)).unwrap()))
+                .unwrap()
+                .extract::<bool>()
+                .unwrap(),
+            "f16 unique mismatch"
+        );
+        assert!(
+            eqf.call1((
+                fss.call1((&hs, &hq)).unwrap(),
+                nss.call1((&hs, &hq)).unwrap()
+            ))
+            .unwrap()
+            .extract::<bool>()
+            .unwrap(),
+            "f16 searchsorted mismatch"
+        );
+        assert!(
+            eqf.call1((fi.call1((&ia, &ib)).unwrap(), ni.call1((&ia, &ib)).unwrap()))
+                .unwrap()
+                .extract::<bool>()
+                .unwrap(),
+            "f16 isin mismatch"
+        );
+        group.bench_function("fnp_unique_f16_4m", |bn| {
+            bn.iter(|| black_box(fu.call1((&u,)).unwrap()))
+        });
+        group.bench_function("numpy_unique_f16_4m", |bn| {
+            bn.iter(|| black_box(nu_.call1((&u,)).unwrap()))
+        });
+        group.bench_function("fnp_searchsorted_f16_2m_2m", |bn| {
+            bn.iter(|| black_box(fss.call1((&hs, &hq)).unwrap()))
+        });
+        group.bench_function("numpy_searchsorted_f16_2m_2m", |bn| {
+            bn.iter(|| black_box(nss.call1((&hs, &hq)).unwrap()))
+        });
+        group.bench_function("fnp_isin_f16_2m_1m", |bn| {
+            bn.iter(|| black_box(fi.call1((&ia, &ib)).unwrap()))
+        });
+        group.bench_function("numpy_isin_f16_2m_1m", |bn| {
+            bn.iter(|| black_box(ni.call1((&ia, &ib)).unwrap()))
+        });
     });
     group.finish();
 }
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:9497:
 a = (rng.integers(0, 3000, 2_000_000) / 7).astype(np.float16)\n\
 b = (rng.integers(0, 3000, 2_000_000) / 7).astype(np.float16)\n";
         let ns = PyDict::new(py);
-        py.run(std::ffi::CString::new(setup).unwrap().as_c_str(), Some(&ns), Some(&ns)).expect("setup");
+        py.run(
+            std::ffi::CString::new(setup).unwrap().as_c_str(),
+            Some(&ns),
+            Some(&ns),
+        )
+        .expect("setup");
         let a = ns.get_item("a").expect("a");
         let b = ns.get_item("b").expect("b");
         let eqf = numpy.getattr("array_equal").expect("np.array_equal");
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:9506:
             let np_fn = numpy.getattr(op).expect("numpy op");
             let f = fnp_fn.call1((&a, &b)).expect("fnp setop");
             let n = np_fn.call1((&a, &b)).expect("numpy setop");
-            assert!(eqf.call1((&f, &n)).unwrap().extract::<bool>().unwrap(), "{op} f16 mismatch");
-            group.bench_function(format!("fnp_{op}_f16_2m_2m"), |bn| bn.iter(|| black_box(fnp_fn.call1((&a, &b)).unwrap())));
-            group.bench_function(format!("numpy_{op}_f16_2m_2m"), |bn| bn.iter(|| black_box(np_fn.call1((&a, &b)).unwrap())));
+            assert!(
+                eqf.call1((&f, &n)).unwrap().extract::<bool>().unwrap(),
+                "{op} f16 mismatch"
+            );
+            group.bench_function(format!("fnp_{op}_f16_2m_2m"), |bn| {
+                bn.iter(|| black_box(fnp_fn.call1((&a, &b)).unwrap()))
+            });
+            group.bench_function(format!("numpy_{op}_f16_2m_2m"), |bn| {
+                bn.iter(|| black_box(np_fn.call1((&a, &b)).unwrap()))
+            });
         }
     });
     group.finish();
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:9535:
 base = rng.integers(-10**15, 10**15, (250_000, 3)).astype(np.int64)\n\
 a = np.concatenate([base, base])\n";
         let ns = PyDict::new(py);
-        py.run(std::ffi::CString::new(setup).unwrap().as_c_str(), Some(&ns), Some(&ns)).expect("setup");
+        py.run(
+            std::ffi::CString::new(setup).unwrap().as_c_str(),
+            Some(&ns),
+            Some(&ns),
+        )
+        .expect("setup");
         let a = ns.get_item("a").expect("a");
         let fnp_u = module.getattr("unique").expect("fnp unique");
         let numpy_u = numpy.getattr("unique").expect("numpy unique");
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:9542:
         let eqf = numpy.getattr("array_equal").expect("np.array_equal");
-        let kw = PyDict::new(py); kw.set_item("axis", 0_i64).unwrap();
+        let kw = PyDict::new(py);
+        kw.set_item("axis", 0_i64).unwrap();
         let f = fnp_u.call((&a,), Some(&kw)).expect("fnp unique");
         let n = numpy_u.call((&a,), Some(&kw)).expect("numpy unique");
-        assert!(eqf.call1((&f, &n)).unwrap().extract::<bool>().unwrap(), "unique rows lexsort mismatch");
+        assert!(
+            eqf.call1((&f, &n)).unwrap().extract::<bool>().unwrap(),
+            "unique rows lexsort mismatch"
+        );
         group.bench_function("fnp_unique_rows_i64_500kx3", |bn| {
-            let kw = PyDict::new(py); kw.set_item("axis", 0_i64).unwrap();
+            let kw = PyDict::new(py);
+            kw.set_item("axis", 0_i64).unwrap();
             bn.iter(|| black_box(fnp_u.call((&a,), Some(&kw)).unwrap()));
         });
         group.bench_function("numpy_unique_rows_i64_500kx3", |bn| {
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:9552:
-            let kw = PyDict::new(py); kw.set_item("axis", 0_i64).unwrap();
+            let kw = PyDict::new(py);
+            kw.set_item("axis", 0_i64).unwrap();
             bn.iter(|| black_box(numpy_u.call((&a,), Some(&kw)).unwrap()));
         });
     });
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:9681:
 base = rng.integers(-10**15, 10**15, (250_000, 3)).astype(np.int64)\n\
 a = np.concatenate([base, base])\n";
         let ns = PyDict::new(py);
-        py.run(std::ffi::CString::new(setup).unwrap().as_c_str(), Some(&ns), Some(&ns)).expect("setup");
+        py.run(
+            std::ffi::CString::new(setup).unwrap().as_c_str(),
+            Some(&ns),
+            Some(&ns),
+        )
+        .expect("setup");
         let a = ns.get_item("a").expect("a");
         let fnp_u = module.getattr("unique").expect("fnp unique");
         let numpy_u = numpy.getattr("unique").expect("numpy unique");
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:9692:
         kwfull.set_item("return_inverse", true).unwrap();
         kwfull.set_item("return_counts", true).unwrap();
         // Correctness: compare each of the 4 tuple elements.
-        let ft = fnp_u.call((&a,), Some(&kwfull)).expect("fnp unique full").downcast_into::<pyo3::types::PyTuple>().unwrap();
-        let nt = numpy_u.call((&a,), Some(&kwfull)).expect("numpy unique full").downcast_into::<pyo3::types::PyTuple>().unwrap();
+        let ft = fnp_u
+            .call((&a,), Some(&kwfull))
+            .expect("fnp unique full")
+            .downcast_into::<pyo3::types::PyTuple>()
+            .unwrap();
+        let nt = numpy_u
+            .call((&a,), Some(&kwfull))
+            .expect("numpy unique full")
+            .downcast_into::<pyo3::types::PyTuple>()
+            .unwrap();
         for i in 0..4 {
-            let eq: bool = eqf.call1((ft.get_item(i).unwrap(), nt.get_item(i).unwrap())).unwrap().extract().unwrap();
+            let eq: bool = eqf
+                .call1((ft.get_item(i).unwrap(), nt.get_item(i).unwrap()))
+                .unwrap()
+                .extract()
+                .unwrap();
             assert!(eq, "unique rows factorize element {i} mismatch");
         }
         group.bench_function("fnp_unique_rows_factorize_500kx3", |bn| {
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:9738:
 base = rng.integers(-100, 100, (250_000, 4)).astype(np.float64)\n\
 a = np.concatenate([base, base])\n";
         let ns = PyDict::new(py);
-        py.run(std::ffi::CString::new(setup).unwrap().as_c_str(), Some(&ns), Some(&ns)).expect("setup");
+        py.run(
+            std::ffi::CString::new(setup).unwrap().as_c_str(),
+            Some(&ns),
+            Some(&ns),
+        )
+        .expect("setup");
         let a = ns.get_item("a").expect("a");
         let fnp_u = module.getattr("unique").expect("fnp unique");
         let numpy_u = numpy.getattr("unique").expect("numpy unique");
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:9745:
         let eqf = numpy.getattr("array_equal").expect("np.array_equal");
-        let kw = PyDict::new(py); kw.set_item("axis", 0_i64).unwrap();
+        let kw = PyDict::new(py);
+        kw.set_item("axis", 0_i64).unwrap();
         let f = fnp_u.call((&a,), Some(&kw)).expect("fnp unique");
         let n = numpy_u.call((&a,), Some(&kw)).expect("numpy unique");
-        assert!(eqf.call1((&f, &n)).unwrap().extract::<bool>().unwrap(), "unique rows f64 mismatch");
+        assert!(
+            eqf.call1((&f, &n)).unwrap().extract::<bool>().unwrap(),
+            "unique rows f64 mismatch"
+        );
         group.bench_function("fnp_unique_rows_f64_500kx4", |bn| {
-            let kw = PyDict::new(py); kw.set_item("axis", 0_i64).unwrap();
+            let kw = PyDict::new(py);
+            kw.set_item("axis", 0_i64).unwrap();
             bn.iter(|| black_box(fnp_u.call((&a,), Some(&kw)).unwrap()));
         });
         group.bench_function("numpy_unique_rows_f64_500kx4", |bn| {
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:9755:
-            let kw = PyDict::new(py); kw.set_item("axis", 0_i64).unwrap();
+            let kw = PyDict::new(py);
+            kw.set_item("axis", 0_i64).unwrap();
             bn.iter(|| black_box(numpy_u.call((&a,), Some(&kw)).unwrap()));
         });
     });
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:9777:
 base = rng.integers(-100, 100, (250_000, 4)).astype(np.float32)\n\
 a = np.concatenate([base, base])\n";
         let ns = PyDict::new(py);
-        py.run(std::ffi::CString::new(setup).unwrap().as_c_str(), Some(&ns), Some(&ns)).expect("setup");
+        py.run(
+            std::ffi::CString::new(setup).unwrap().as_c_str(),
+            Some(&ns),
+            Some(&ns),
+        )
+        .expect("setup");
         let a = ns.get_item("a").expect("a");
         let fnp_u = module.getattr("unique").expect("fnp unique");
         let numpy_u = numpy.getattr("unique").expect("numpy unique");
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:9784:
         let eqf = numpy.getattr("array_equal").expect("np.array_equal");
-        let kw = PyDict::new(py); kw.set_item("axis", 0_i64).unwrap();
+        let kw = PyDict::new(py);
+        kw.set_item("axis", 0_i64).unwrap();
         let f = fnp_u.call((&a,), Some(&kw)).expect("fnp unique");
         let n = numpy_u.call((&a,), Some(&kw)).expect("numpy unique");
-        assert!(eqf.call1((&f, &n)).unwrap().extract::<bool>().unwrap(), "unique rows f32 mismatch");
+        assert!(
+            eqf.call1((&f, &n)).unwrap().extract::<bool>().unwrap(),
+            "unique rows f32 mismatch"
+        );
         group.bench_function("fnp_unique_rows_f32_500kx4", |bn| {
-            let kw = PyDict::new(py); kw.set_item("axis", 0_i64).unwrap();
+            let kw = PyDict::new(py);
+            kw.set_item("axis", 0_i64).unwrap();
             bn.iter(|| black_box(fnp_u.call((&a,), Some(&kw)).unwrap()));
         });
         group.bench_function("numpy_unique_rows_f32_500kx4", |bn| {
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:9794:
-            let kw = PyDict::new(py); kw.set_item("axis", 0_i64).unwrap();
+            let kw = PyDict::new(py);
+            kw.set_item("axis", 0_i64).unwrap();
             bn.iter(|| black_box(numpy_u.call((&a,), Some(&kw)).unwrap()));
         });
     });
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:9816:
 base = rng.integers(-100, 100, (250_000, 4)).astype(np.float64)\n\
 a = np.concatenate([base, base])\n";
         let ns = PyDict::new(py);
-        py.run(std::ffi::CString::new(setup).unwrap().as_c_str(), Some(&ns), Some(&ns)).expect("setup");
+        py.run(
+            std::ffi::CString::new(setup).unwrap().as_c_str(),
+            Some(&ns),
+            Some(&ns),
+        )
+        .expect("setup");
         let a = ns.get_item("a").expect("a");
         let fnp_u = module.getattr("unique").expect("fnp unique");
         let numpy_u = numpy.getattr("unique").expect("numpy unique");
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:9826:
         kwfull.set_item("return_index", true).unwrap();
         kwfull.set_item("return_inverse", true).unwrap();
         kwfull.set_item("return_counts", true).unwrap();
-        let ft = fnp_u.call((&a,), Some(&kwfull)).expect("fnp full").downcast_into::<pyo3::types::PyTuple>().unwrap();
-        let nt = numpy_u.call((&a,), Some(&kwfull)).expect("numpy full").downcast_into::<pyo3::types::PyTuple>().unwrap();
+        let ft = fnp_u
+            .call((&a,), Some(&kwfull))
+            .expect("fnp full")
+            .downcast_into::<pyo3::types::PyTuple>()
+            .unwrap();
+        let nt = numpy_u
+            .call((&a,), Some(&kwfull))
+            .expect("numpy full")
+            .downcast_into::<pyo3::types::PyTuple>()
+            .unwrap();
         for i in 0..4 {
-            let eq: bool = eqf.call1((ft.get_item(i).unwrap(), nt.get_item(i).unwrap())).unwrap().extract().unwrap();
+            let eq: bool = eqf
+                .call1((ft.get_item(i).unwrap(), nt.get_item(i).unwrap()))
+                .unwrap()
+                .extract()
+                .unwrap();
             assert!(eq, "f64 factorize element {i} mismatch");
         }
         group.bench_function("fnp_unique_rows_f64_factorize_500kx4", |bn| {
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:9870:
 base = rng.integers(-100, 100, (250_000, 4)).astype(np.float32)\n\
 a = np.concatenate([base, base])\n";
         let ns = PyDict::new(py);
-        py.run(std::ffi::CString::new(setup).unwrap().as_c_str(), Some(&ns), Some(&ns)).expect("setup");
+        py.run(
+            std::ffi::CString::new(setup).unwrap().as_c_str(),
+            Some(&ns),
+            Some(&ns),
+        )
+        .expect("setup");
         let a = ns.get_item("a").expect("a");
         let fnp_u = module.getattr("unique").expect("fnp unique");
         let numpy_u = numpy.getattr("unique").expect("numpy unique");
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:9880:
         kwfull.set_item("return_index", true).unwrap();
         kwfull.set_item("return_inverse", true).unwrap();
         kwfull.set_item("return_counts", true).unwrap();
-        let ft = fnp_u.call((&a,), Some(&kwfull)).expect("fnp full").downcast_into::<pyo3::types::PyTuple>().unwrap();
-        let nt = numpy_u.call((&a,), Some(&kwfull)).expect("numpy full").downcast_into::<pyo3::types::PyTuple>().unwrap();
+        let ft = fnp_u
+            .call((&a,), Some(&kwfull))
+            .expect("fnp full")
+            .downcast_into::<pyo3::types::PyTuple>()
+            .unwrap();
+        let nt = numpy_u
+            .call((&a,), Some(&kwfull))
+            .expect("numpy full")
+            .downcast_into::<pyo3::types::PyTuple>()
+            .unwrap();
         for i in 0..4 {
-            let eq: bool = eqf.call1((ft.get_item(i).unwrap(), nt.get_item(i).unwrap())).unwrap().extract().unwrap();
+            let eq: bool = eqf
+                .call1((ft.get_item(i).unwrap(), nt.get_item(i).unwrap()))
+                .unwrap()
+                .extract()
+                .unwrap();
             assert!(eq, "f32 factorize element {i} mismatch");
         }
         group.bench_function("fnp_unique_rows_f32_factorize_500kx4", |bn| {
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:9924:
 base = (rng.integers(0, 50, (250_000, 3)) + 1j * rng.integers(0, 50, (250_000, 3))).astype(np.complex128)\n\
 a = np.concatenate([base, base])\n";
         let ns = PyDict::new(py);
-        py.run(std::ffi::CString::new(setup).unwrap().as_c_str(), Some(&ns), Some(&ns)).expect("setup");
+        py.run(
+            std::ffi::CString::new(setup).unwrap().as_c_str(),
+            Some(&ns),
+            Some(&ns),
+        )
+        .expect("setup");
         let a = ns.get_item("a").expect("a");
         let fnp_u = module.getattr("unique").expect("fnp unique");
         let numpy_u = numpy.getattr("unique").expect("numpy unique");
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:9931:
         let eqf = numpy.getattr("array_equal").expect("np.array_equal");
-        let kw = PyDict::new(py); kw.set_item("axis", 0_i64).unwrap();
+        let kw = PyDict::new(py);
+        kw.set_item("axis", 0_i64).unwrap();
         let f = fnp_u.call((&a,), Some(&kw)).expect("fnp unique");
         let n = numpy_u.call((&a,), Some(&kw)).expect("numpy unique");
-        assert!(eqf.call1((&f, &n)).unwrap().extract::<bool>().unwrap(), "unique rows c128 mismatch");
+        assert!(
+            eqf.call1((&f, &n)).unwrap().extract::<bool>().unwrap(),
+            "unique rows c128 mismatch"
+        );
         group.bench_function("fnp_unique_rows_c128_500kx3", |bn| {
-            let kw = PyDict::new(py); kw.set_item("axis", 0_i64).unwrap();
+            let kw = PyDict::new(py);
+            kw.set_item("axis", 0_i64).unwrap();
             bn.iter(|| black_box(fnp_u.call((&a,), Some(&kw)).unwrap()));
         });
         group.bench_function("numpy_unique_rows_c128_500kx3", |bn| {
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:9941:
-            let kw = PyDict::new(py); kw.set_item("axis", 0_i64).unwrap();
+            let kw = PyDict::new(py);
+            kw.set_item("axis", 0_i64).unwrap();
             bn.iter(|| black_box(numpy_u.call((&a,), Some(&kw)).unwrap()));
         });
     });
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:9964:
 base = (rng.integers(0, 50, (250_000, 3)) + 1j * rng.integers(0, 50, (250_000, 3))).astype(np.complex64)\n\
 a = np.concatenate([base, base])\n";
         let ns = PyDict::new(py);
-        py.run(std::ffi::CString::new(setup).unwrap().as_c_str(), Some(&ns), Some(&ns)).expect("setup");
+        py.run(
+            std::ffi::CString::new(setup).unwrap().as_c_str(),
+            Some(&ns),
+            Some(&ns),
+        )
+        .expect("setup");
         let a = ns.get_item("a").expect("a");
         let fnp_u = module.getattr("unique").expect("fnp unique");
         let numpy_u = numpy.getattr("unique").expect("numpy unique");
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:9971:
         let eqf = numpy.getattr("array_equal").expect("np.array_equal");
-        let kw = PyDict::new(py); kw.set_item("axis", 0_i64).unwrap();
+        let kw = PyDict::new(py);
+        kw.set_item("axis", 0_i64).unwrap();
         let f = fnp_u.call((&a,), Some(&kw)).expect("fnp unique");
         let n = numpy_u.call((&a,), Some(&kw)).expect("numpy unique");
-        assert!(eqf.call1((&f, &n)).unwrap().extract::<bool>().unwrap(), "c64 rows plain mismatch");
+        assert!(
+            eqf.call1((&f, &n)).unwrap().extract::<bool>().unwrap(),
+            "c64 rows plain mismatch"
+        );
         let kwf = PyDict::new(py);
         kwf.set_item("axis", 0_i64).unwrap();
         kwf.set_item("return_index", true).unwrap();
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:9979:
         kwf.set_item("return_inverse", true).unwrap();
         kwf.set_item("return_counts", true).unwrap();
-        let ft = fnp_u.call((&a,), Some(&kwf)).expect("fnp full").downcast_into::<pyo3::types::PyTuple>().unwrap();
-        let nt = numpy_u.call((&a,), Some(&kwf)).expect("numpy full").downcast_into::<pyo3::types::PyTuple>().unwrap();
+        let ft = fnp_u
+            .call((&a,), Some(&kwf))
+            .expect("fnp full")
+            .downcast_into::<pyo3::types::PyTuple>()
+            .unwrap();
+        let nt = numpy_u
+            .call((&a,), Some(&kwf))
+            .expect("numpy full")
+            .downcast_into::<pyo3::types::PyTuple>()
+            .unwrap();
         for i in 0..4 {
-            let eq: bool = eqf.call1((ft.get_item(i).unwrap(), nt.get_item(i).unwrap())).unwrap().extract().unwrap();
+            let eq: bool = eqf
+                .call1((ft.get_item(i).unwrap(), nt.get_item(i).unwrap()))
+                .unwrap()
+                .extract()
+                .unwrap();
             assert!(eq, "c64 rows factorize element {i} mismatch");
         }
         group.bench_function("fnp_unique_rows_c64_500kx3", |bn| {
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:9988:
-            let kw = PyDict::new(py); kw.set_item("axis", 0_i64).unwrap();
+            let kw = PyDict::new(py);
+            kw.set_item("axis", 0_i64).unwrap();
             bn.iter(|| black_box(fnp_u.call((&a,), Some(&kw)).unwrap()));
         });
         group.bench_function("numpy_unique_rows_c64_500kx3", |bn| {
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:9992:
-            let kw = PyDict::new(py); kw.set_item("axis", 0_i64).unwrap();
+            let kw = PyDict::new(py);
+            kw.set_item("axis", 0_i64).unwrap();
             bn.iter(|| black_box(numpy_u.call((&a,), Some(&kw)).unwrap()));
         });
     });
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:10014:
 base = (rng.integers(0, 50, (250_000, 3)) + 1j * rng.integers(0, 50, (250_000, 3))).astype(np.complex128)\n\
 a = np.concatenate([base, base])\n";
         let ns = PyDict::new(py);
-        py.run(std::ffi::CString::new(setup).unwrap().as_c_str(), Some(&ns), Some(&ns)).expect("setup");
+        py.run(
+            std::ffi::CString::new(setup).unwrap().as_c_str(),
+            Some(&ns),
+            Some(&ns),
+        )
+        .expect("setup");
         let a = ns.get_item("a").expect("a");
         let fnp_u = module.getattr("unique").expect("fnp unique");
         let numpy_u = numpy.getattr("unique").expect("numpy unique");
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:10024:
         kwfull.set_item("return_index", true).unwrap();
         kwfull.set_item("return_inverse", true).unwrap();
         kwfull.set_item("return_counts", true).unwrap();
-        let ft = fnp_u.call((&a,), Some(&kwfull)).expect("fnp full").downcast_into::<pyo3::types::PyTuple>().unwrap();
-        let nt = numpy_u.call((&a,), Some(&kwfull)).expect("numpy full").downcast_into::<pyo3::types::PyTuple>().unwrap();
+        let ft = fnp_u
+            .call((&a,), Some(&kwfull))
+            .expect("fnp full")
+            .downcast_into::<pyo3::types::PyTuple>()
+            .unwrap();
+        let nt = numpy_u
+            .call((&a,), Some(&kwfull))
+            .expect("numpy full")
+            .downcast_into::<pyo3::types::PyTuple>()
+            .unwrap();
         for i in 0..4 {
-            let eq: bool = eqf.call1((ft.get_item(i).unwrap(), nt.get_item(i).unwrap())).unwrap().extract().unwrap();
+            let eq: bool = eqf
+                .call1((ft.get_item(i).unwrap(), nt.get_item(i).unwrap()))
+                .unwrap()
+                .extract()
+                .unwrap();
             assert!(eq, "c128 factorize element {i} mismatch");
         }
         group.bench_function("fnp_unique_rows_c128_factorize_500kx3", |bn| {
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:10069:
 base = rng.integers(-10**15, 10**15, (3, 250_000)).astype(np.int64)\n\
 a = np.concatenate([base, base], axis=1)\n";
         let ns = PyDict::new(py);
-        py.run(std::ffi::CString::new(setup).unwrap().as_c_str(), Some(&ns), Some(&ns)).expect("setup");
+        py.run(
+            std::ffi::CString::new(setup).unwrap().as_c_str(),
+            Some(&ns),
+            Some(&ns),
+        )
+        .expect("setup");
         let a = ns.get_item("a").expect("a");
         let fnp_u = module.getattr("unique").expect("fnp unique");
         let numpy_u = numpy.getattr("unique").expect("numpy unique");
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:10076:
         let eqf = numpy.getattr("array_equal").expect("np.array_equal");
-        let kw = PyDict::new(py); kw.set_item("axis", 1_i64).unwrap();
+        let kw = PyDict::new(py);
+        kw.set_item("axis", 1_i64).unwrap();
         let f = fnp_u.call((&a,), Some(&kw)).expect("fnp unique");
         let n = numpy_u.call((&a,), Some(&kw)).expect("numpy unique");
-        assert!(eqf.call1((&f, &n)).unwrap().extract::<bool>().unwrap(), "unique cols axis=1 mismatch");
+        assert!(
+            eqf.call1((&f, &n)).unwrap().extract::<bool>().unwrap(),
+            "unique cols axis=1 mismatch"
+        );
         group.bench_function("fnp_unique_cols_i64_3x500k", |bn| {
-            let kw = PyDict::new(py); kw.set_item("axis", 1_i64).unwrap();
+            let kw = PyDict::new(py);
+            kw.set_item("axis", 1_i64).unwrap();
             bn.iter(|| black_box(fnp_u.call((&a,), Some(&kw)).unwrap()));
         });
         group.bench_function("numpy_unique_cols_i64_3x500k", |bn| {
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:10086:
-            let kw = PyDict::new(py); kw.set_item("axis", 1_i64).unwrap();
+            let kw = PyDict::new(py);
+            kw.set_item("axis", 1_i64).unwrap();
             bn.iter(|| black_box(numpy_u.call((&a,), Some(&kw)).unwrap()));
         });
     });
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:10230:
             b.iter(|| black_box(fnp_bc.call((&x_k1000,), Some(&kw)).expect("fnp wbincount")));
         });
         group.bench_function("numpy_bincount_weighted_4m_k1000", |b| {
-            b.iter(|| black_box(numpy_bc.call((&x_k1000,), Some(&kw2)).expect("np wbincount")));
+            b.iter(|| {
+                black_box(
+                    numpy_bc
+                        .call((&x_k1000,), Some(&kw2))
+                        .expect("np wbincount"),
+                )
+            });
         });
     });

Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:10306:
             b.iter(|| black_box(numpy_ss.call1((&a, &v)).expect("numpy searchsorted")));
         });
         group.bench_function("fnp_searchsorted_f64_4m_haystack4m", |b| {
-            b.iter(|| black_box(fnp_ss.call1((&a_big, &v_big)).expect("fnp searchsorted big")));
+            b.iter(|| {
+                black_box(
+                    fnp_ss
+                        .call1((&a_big, &v_big))
+                        .expect("fnp searchsorted big"),
+                )
+            });
         });
         group.bench_function("numpy_searchsorted_f64_4m_haystack4m", |b| {
-            b.iter(|| black_box(numpy_ss.call1((&a_big, &v_big)).expect("numpy searchsorted big")));
+            b.iter(|| {
+                black_box(
+                    numpy_ss
+                        .call1((&a_big, &v_big))
+                        .expect("numpy searchsorted big"),
+                )
+            });
         });

         // f32 twin: 4M sorted f32 haystack + 4M f32 queries. Correctness gate (both sides byte-identical
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:10332:
             for side in ["left", "right"] {
                 let kw = PyDict::new(py);
                 kw.set_item("side", side).expect("side");
-                let got = fnp_ss.call((&a_f32, &v_f32), Some(&kw)).expect("fnp ss f32");
-                let exp = numpy_ss.call((&a_f32, &v_f32), Some(&kw)).expect("np ss f32");
+                let got = fnp_ss
+                    .call((&a_f32, &v_f32), Some(&kw))
+                    .expect("fnp ss f32");
+                let exp = numpy_ss
+                    .call((&a_f32, &v_f32), Some(&kw))
+                    .expect("np ss f32");
                 let eq: bool = np_array_equal
                     .call1((&got, &exp))
                     .expect("array_equal")
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:10340:
                     .extract()
                     .expect("bool");
-                assert!(eq, "searchsorted f32 merge correctness mismatch: side={side}");
+                assert!(
+                    eq,
+                    "searchsorted f32 merge correctness mismatch: side={side}"
+                );
             }
         }
         group.bench_function("fnp_searchsorted_f32_4m_haystack4m", |b| {
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:10346:
-            b.iter(|| black_box(fnp_ss.call1((&a_f32, &v_f32)).expect("fnp searchsorted f32")));
+            b.iter(|| {
+                black_box(
+                    fnp_ss
+                        .call1((&a_f32, &v_f32))
+                        .expect("fnp searchsorted f32"),
+                )
+            });
         });
         group.bench_function("numpy_searchsorted_f32_4m_haystack4m", |b| {
-            b.iter(|| black_box(numpy_ss.call1((&a_f32, &v_f32)).expect("numpy searchsorted f32")));
+            b.iter(|| {
+                black_box(
+                    numpy_ss
+                        .call1((&a_f32, &v_f32))
+                        .expect("numpy searchsorted f32"),
+                )
+            });
         });

         // i64 twin: integer ordering is total, so the same query-sort + monotonic merge should
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:10369:
             for side in ["left", "right"] {
                 let kw = PyDict::new(py);
                 kw.set_item("side", side).expect("side");
-                let got = fnp_ss.call((&a_i64, &v_i64), Some(&kw)).expect("fnp ss i64");
-                let exp = numpy_ss.call((&a_i64, &v_i64), Some(&kw)).expect("np ss i64");
+                let got = fnp_ss
+                    .call((&a_i64, &v_i64), Some(&kw))
+                    .expect("fnp ss i64");
+                let exp = numpy_ss
+                    .call((&a_i64, &v_i64), Some(&kw))
+                    .expect("np ss i64");
                 let eq: bool = np_array_equal
                     .call1((&got, &exp))
                     .expect("array_equal")
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:10377:
                     .extract()
                     .expect("bool");
-                assert!(eq, "searchsorted i64 merge correctness mismatch: side={side}");
+                assert!(
+                    eq,
+                    "searchsorted i64 merge correctness mismatch: side={side}"
+                );
             }
         }
         group.bench_function("fnp_searchsorted_i64_4m_haystack4m", |b| {
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:10383:
-            b.iter(|| black_box(fnp_ss.call1((&a_i64, &v_i64)).expect("fnp searchsorted i64")));
+            b.iter(|| {
+                black_box(
+                    fnp_ss
+                        .call1((&a_i64, &v_i64))
+                        .expect("fnp searchsorted i64"),
+                )
+            });
         });
         group.bench_function("numpy_searchsorted_i64_4m_haystack4m", |b| {
-            b.iter(|| black_box(numpy_ss.call1((&a_i64, &v_i64)).expect("numpy searchsorted i64")));
+            b.iter(|| {
+                black_box(
+                    numpy_ss
+                        .call1((&a_i64, &v_i64))
+                        .expect("numpy searchsorted i64"),
+                )
+            });
         });
     });

Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:10426:
             b.iter(|| black_box(fnp_repeat.call((&m, 3_i64), Some(&kw)).expect("fnp repeat")));
         });
         group.bench_function("numpy_repeat_2d_axis1_c3", |b| {
-            b.iter(|| black_box(numpy_repeat.call((&m, 3_i64), Some(&kw2)).expect("np repeat")));
+            b.iter(|| {
+                black_box(
+                    numpy_repeat
+                        .call((&m, 3_i64), Some(&kw2))
+                        .expect("np repeat"),
+                )
+            });
         });
     });

Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:10584:
         .expect("take_along c64 setup");
         let x = ns.get_item("x").expect("x");
         let idx = ns.get_item("idx").expect("idx");
-        let fnp_ta = module.getattr("take_along_axis").expect("fnp take_along_axis");
-        let numpy_ta = numpy.getattr("take_along_axis").expect("numpy take_along_axis");
+        let fnp_ta = module
+            .getattr("take_along_axis")
+            .expect("fnp take_along_axis");
+        let numpy_ta = numpy
+            .getattr("take_along_axis")
+            .expect("numpy take_along_axis");
         group.bench_function("fnp_take_along_c64", |b| {
             b.iter(|| black_box(fnp_ta.call1((&x, &idx, 1_i64)).expect("fnp ta")));
         });
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:10623:
         .expect("take_along_axis setup");
         let a = ns.get_item("a").expect("a");
         let idx = ns.get_item("idx").expect("idx");
-        let fnp_t = module.getattr("take_along_axis").expect("fnp take_along_axis");
-        let numpy_t = numpy.getattr("take_along_axis").expect("numpy take_along_axis");
+        let fnp_t = module
+            .getattr("take_along_axis")
+            .expect("fnp take_along_axis");
+        let numpy_t = numpy
+            .getattr("take_along_axis")
+            .expect("numpy take_along_axis");
         let axis = 1_i64;
         group.bench_function("fnp_take_along_axis_f64_8m", |b| {
             b.iter(|| black_box(fnp_t.call1((&a, &idx, axis)).expect("fnp take_along_axis")));
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:10631:
         });
         group.bench_function("numpy_take_along_axis_f64_8m", |b| {
-            b.iter(|| black_box(numpy_t.call1((&a, &idx, axis)).expect("numpy take_along_axis")));
+            b.iter(|| {
+                black_box(
+                    numpy_t
+                        .call1((&a, &idx, axis))
+                        .expect("numpy take_along_axis"),
+                )
+            });
         });
     });

Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:10668:
         let kwargs = PyDict::new(py);
         kwargs.set_item("axis", 1_i64).expect("axis");
         group.bench_function("fnp_take_axis1_f64_8m", |b| {
-            b.iter(|| black_box(fnp_take.call((&a, &idx), Some(&kwargs)).expect("fnp take axis")));
+            b.iter(|| {
+                black_box(
+                    fnp_take
+                        .call((&a, &idx), Some(&kwargs))
+                        .expect("fnp take axis"),
+                )
+            });
         });
         group.bench_function("numpy_take_axis1_f64_8m", |b| {
-            b.iter(|| black_box(numpy_take.call((&a, &idx), Some(&kwargs)).expect("numpy take axis")));
+            b.iter(|| {
+                black_box(
+                    numpy_take
+                        .call((&a, &idx), Some(&kwargs))
+                        .expect("numpy take axis"),
+                )
+            });
         });
     });

Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:10756:
         // f16 fmod/remainder: numpy widens f16->f32 (~214ms / ~317ms @16M, slowest f16 binary).
         // hb has near-zero entries; replace them so divisors are non-zero (kernel engages).
         py.run(
-            std::ffi::CString::new("hbnz = np.where(np.abs(hb) < np.float16(0.05), np.float16(1.5), hb)")
-                .unwrap()
-                .as_c_str(),
+            std::ffi::CString::new(
+                "hbnz = np.where(np.abs(hb) < np.float16(0.05), np.float16(1.5), hb)",
+            )
+            .unwrap()
+            .as_c_str(),
             Some(&ns),
             Some(&ns),
         )
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:10788:
         }
         // f16 unary rounding ops floor/ceil/trunc/rint: numpy has no native f16 ALU and emulates
         // via widen->f32->op->narrow (compute-bound, ~77-126ms at 16M); native parallel wins ~15-30x.
-        for op in ["floor", "ceil", "trunc", "rint", "isnan", "isfinite", "signbit"] {
+        for op in [
+            "floor", "ceil", "trunc", "rint", "isnan", "isfinite", "signbit",
+        ] {
             let fnp_fn = module.getattr(op).expect("fnp unary op");
             let numpy_fn = numpy.getattr(op).expect("numpy unary op");
             group.bench_function(format!("fnp_{op}_f16_16m"), |bch| {
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:10840:
                 bch.iter(|| black_box(fnp_recip.call1((&hrecip,)).expect("fnp f16 reciprocal")));
             });
             group.bench_function("numpy_reciprocal_f16_16m", |bch| {
-                bch.iter(|| black_box(numpy_recip.call1((&hrecip,)).expect("numpy f16 reciprocal")));
+                bch.iter(|| {
+                    black_box(numpy_recip.call1((&hrecip,)).expect("numpy f16 reciprocal"))
+                });
             });
         }
         // f16 modf: numpy widens f16->f32, splits into (trunc, frac), narrows both — single-threaded
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:10870:
         // f16 ldexp: numpy widens f16->f32, scalbnf, narrows — single-threaded (~108ms@16M = ~1.2
         // GB/s, compute-bound). Native parallel exact-pow2-scale wins. (i32 exponent in [-5,5).)
         py.run(
-            std::ffi::CString::new(
-                "lde = rng.integers(-5, 5, 16_000_000, dtype=np.int32)",
-            )
-            .unwrap()
-            .as_c_str(),
+            std::ffi::CString::new("lde = rng.integers(-5, 5, 16_000_000, dtype=np.int32)")
+                .unwrap()
+                .as_c_str(),
             Some(&ns),
             Some(&ns),
         )
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:10911:
         for (arr, tag) in [(&cc128, "c128"), (&cc64, "c64")] {
             group.bench_function(format!("fnp_cumsum_lastaxis_{tag}_16m"), |bch| {
                 bch.iter(|| {
-                    black_box(fnp_cumsum.call((arr,), Some(&kw_ax1)).expect("fnp complex cumsum"))
+                    black_box(
+                        fnp_cumsum
+                            .call((arr,), Some(&kw_ax1))
+                            .expect("fnp complex cumsum"),
+                    )
                 });
             });
             group.bench_function(format!("numpy_cumsum_lastaxis_{tag}_16m"), |bch| {
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:10918:
                 bch.iter(|| {
-                    black_box(numpy_cumsum.call((arr,), Some(&kw_ax1)).expect("numpy complex cumsum"))
+                    black_box(
+                        numpy_cumsum
+                            .call((arr,), Some(&kw_ax1))
+                            .expect("numpy complex cumsum"),
+                    )
                 });
             });
         }
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:10925:
             let fnp_clip = module.getattr("clip").expect("fnp clip");
             let numpy_clip = numpy.getattr("clip").expect("numpy clip");
             group.bench_function("fnp_clip_f16_16m", |bch| {
-                bch.iter(|| black_box(fnp_clip.call1((&ha, -0.5f64, 0.5f64)).expect("fnp f16 clip")));
+                bch.iter(|| {
+                    black_box(
+                        fnp_clip
+                            .call1((&ha, -0.5f64, 0.5f64))
+                            .expect("fnp f16 clip"),
+                    )
+                });
             });
             group.bench_function("numpy_clip_f16_16m", |bch| {
                 bch.iter(|| {
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:10932:
-                    black_box(numpy_clip.call1((&ha, -0.5f64, 0.5f64)).expect("numpy f16 clip"))
+                    black_box(
+                        numpy_clip
+                            .call1((&ha, -0.5f64, 0.5f64))
+                            .expect("numpy f16 clip"),
+                    )
                 });
             });
         }
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:10970:
         }
         // f16 last-axis argmax/argmin: numpy widens f16->f32 per lane; native per-lane scan wins.
         py.run(
-            std::ffi::CString::new("hsq2 = hsq.reshape(4000, 4000)").unwrap().as_c_str(),
+            std::ffi::CString::new("hsq2 = hsq.reshape(4000, 4000)")
+                .unwrap()
+                .as_c_str(),
             Some(&ns),
             Some(&ns),
         )
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:10987:
             let numpy_fn = numpy.getattr(op).expect("numpy f16 minmax op");
             group.bench_function(format!("fnp_{op}_lastaxis_f16_16m"), |bch| {
                 bch.iter(|| {
-                    black_box(fnp_fn.call((&hsq2,), Some(&kw_axis)).expect("fnp f16 lastaxis minmax"))
+                    black_box(
+                        fnp_fn
+                            .call((&hsq2,), Some(&kw_axis))
+                            .expect("fnp f16 lastaxis minmax"),
+                    )
                 });
             });
             group.bench_function(format!("numpy_{op}_lastaxis_f16_16m"), |bch| {
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:10994:
                 bch.iter(|| {
                     black_box(
-                        numpy_fn.call((&hsq2,), Some(&kw_axis)).expect("numpy f16 lastaxis minmax"),
+                        numpy_fn
+                            .call((&hsq2,), Some(&kw_axis))
+                            .expect("numpy f16 lastaxis minmax"),
                     )
                 });
             });
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:11000:
             group.bench_function(format!("fnp_{op}_axis0_f16_16m"), |bch| {
                 bch.iter(|| {
-                    black_box(fnp_fn.call((&hsq2,), Some(&kw_axis0)).expect("fnp f16 axis0 minmax"))
+                    black_box(
+                        fnp_fn
+                            .call((&hsq2,), Some(&kw_axis0))
+                            .expect("fnp f16 axis0 minmax"),
+                    )
                 });
             });
             group.bench_function(format!("numpy_{op}_axis0_f16_16m"), |bch| {
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:11006:
                 bch.iter(|| {
                     black_box(
-                        numpy_fn.call((&hsq2,), Some(&kw_axis0)).expect("numpy f16 axis0 minmax"),
+                        numpy_fn
+                            .call((&hsq2,), Some(&kw_axis0))
+                            .expect("numpy f16 axis0 minmax"),
                     )
                 });
             });
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:11016:
         let numpy_ptp = numpy.getattr("ptp").expect("numpy ptp");
         group.bench_function("fnp_ptp_lastaxis_f16_16m", |bch| {
             bch.iter(|| {
-                black_box(fnp_ptp.call((&hsq2,), Some(&kw_axis)).expect("fnp f16 lastaxis ptp"))
+                black_box(
+                    fnp_ptp
+                        .call((&hsq2,), Some(&kw_axis))
+                        .expect("fnp f16 lastaxis ptp"),
+                )
             });
         });
         group.bench_function("numpy_ptp_lastaxis_f16_16m", |bch| {
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:11023:
             bch.iter(|| {
-                black_box(numpy_ptp.call((&hsq2,), Some(&kw_axis)).expect("numpy f16 lastaxis ptp"))
+                black_box(
+                    numpy_ptp
+                        .call((&hsq2,), Some(&kw_axis))
+                        .expect("numpy f16 lastaxis ptp"),
+                )
             });
         });
         group.bench_function("fnp_ptp_axis0_f16_16m", |bch| {
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:11028:
             bch.iter(|| {
-                black_box(fnp_ptp.call((&hsq2,), Some(&kw_axis0)).expect("fnp f16 axis0 ptp"))
+                black_box(
+                    fnp_ptp
+                        .call((&hsq2,), Some(&kw_axis0))
+                        .expect("fnp f16 axis0 ptp"),
+                )
             });
         });
         group.bench_function("numpy_ptp_axis0_f16_16m", |bch| {
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:11033:
             bch.iter(|| {
-                black_box(numpy_ptp.call((&hsq2,), Some(&kw_axis0)).expect("numpy f16 axis0 ptp"))
+                black_box(
+                    numpy_ptp
+                        .call((&hsq2,), Some(&kw_axis0))
+                        .expect("numpy f16 axis0 ptp"),
+                )
             });
         });
         // f16 nanmin/nanmax flat + last-axis + axis-0: numpy widens f16->f32 skip-NaN (~32ms@16M);
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:11060:
             });
             group.bench_function(format!("fnp_{op}_lastaxis_f16_16m"), |bch| {
                 bch.iter(|| {
-                    black_box(fnp_fn.call((&hsqn2,), Some(&kw_axis)).expect("fnp f16 lastaxis nan"))
+                    black_box(
+                        fnp_fn
+                            .call((&hsqn2,), Some(&kw_axis))
+                            .expect("fnp f16 lastaxis nan"),
+                    )
                 });
             });
             group.bench_function(format!("numpy_{op}_lastaxis_f16_16m"), |bch| {
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:11067:
                 bch.iter(|| {
                     black_box(
-                        numpy_fn.call((&hsqn2,), Some(&kw_axis)).expect("numpy f16 lastaxis nan"),
+                        numpy_fn
+                            .call((&hsqn2,), Some(&kw_axis))
+                            .expect("numpy f16 lastaxis nan"),
                     )
                 });
             });
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:11073:
             group.bench_function(format!("fnp_{op}_axis0_f16_16m"), |bch| {
                 bch.iter(|| {
-                    black_box(fnp_fn.call((&hsqn2,), Some(&kw_axis0)).expect("fnp f16 axis0 nan"))
+                    black_box(
+                        fnp_fn
+                            .call((&hsqn2,), Some(&kw_axis0))
+                            .expect("fnp f16 axis0 nan"),
+                    )
                 });
             });
             group.bench_function(format!("numpy_{op}_axis0_f16_16m"), |bch| {
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:11079:
                 bch.iter(|| {
                     black_box(
-                        numpy_fn.call((&hsqn2,), Some(&kw_axis0)).expect("numpy f16 axis0 nan"),
+                        numpy_fn
+                            .call((&hsqn2,), Some(&kw_axis0))
+                            .expect("numpy f16 axis0 nan"),
                     )
                 });
             });
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:11090:
             let numpy_fn = numpy.getattr(op).expect("numpy f16 cum op");
             group.bench_function(format!("fnp_{op}_lastaxis_f16_16m"), |bch| {
                 bch.iter(|| {
-                    black_box(fnp_fn.call((&hsq2,), Some(&kw_axis)).expect("fnp f16 lastaxis cum"))
+                    black_box(
+                        fnp_fn
+                            .call((&hsq2,), Some(&kw_axis))
+                            .expect("fnp f16 lastaxis cum"),
+                    )
                 });
             });
             group.bench_function(format!("numpy_{op}_lastaxis_f16_16m"), |bch| {
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:11097:
                 bch.iter(|| {
                     black_box(
-                        numpy_fn.call((&hsq2,), Some(&kw_axis)).expect("numpy f16 lastaxis cum"),
+                        numpy_fn
+                            .call((&hsq2,), Some(&kw_axis))
+                            .expect("numpy f16 lastaxis cum"),
                     )
                 });
             });
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:11103:
             group.bench_function(format!("fnp_{op}_axis0_f16_16m"), |bch| {
                 bch.iter(|| {
-                    black_box(fnp_fn.call((&hsq2,), Some(&kw_axis0)).expect("fnp f16 axis0 cum"))
+                    black_box(
+                        fnp_fn
+                            .call((&hsq2,), Some(&kw_axis0))
+                            .expect("fnp f16 axis0 cum"),
+                    )
                 });
             });
             group.bench_function(format!("numpy_{op}_axis0_f16_16m"), |bch| {
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:11109:
                 bch.iter(|| {
                     black_box(
-                        numpy_fn.call((&hsq2,), Some(&kw_axis0)).expect("numpy f16 axis0 cum"),
+                        numpy_fn
+                            .call((&hsq2,), Some(&kw_axis0))
+                            .expect("numpy f16 axis0 cum"),
                     )
                 });
             });
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:11121:
             group.bench_function(format!("fnp_{op}_lastaxis_f16_16m"), |bch| {
                 bch.iter(|| {
                     black_box(
-                        fnp_fn.call((&hsqn2,), Some(&kw_axis)).expect("fnp f16 lastaxis nancum"),
+                        fnp_fn
+                            .call((&hsqn2,), Some(&kw_axis))
+                            .expect("fnp f16 lastaxis nancum"),
                     )
                 });
             });
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:11128:
             group.bench_function(format!("numpy_{op}_lastaxis_f16_16m"), |bch| {
                 bch.iter(|| {
                     black_box(
-                        numpy_fn.call((&hsqn2,), Some(&kw_axis)).expect("numpy f16 lastaxis nancum"),
+                        numpy_fn
+                            .call((&hsqn2,), Some(&kw_axis))
+                            .expect("numpy f16 lastaxis nancum"),
                     )
                 });
             });
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:11135:
             group.bench_function(format!("fnp_{op}_axis0_f16_16m"), |bch| {
                 bch.iter(|| {
                     black_box(
-                        fnp_fn.call((&hsqn2,), Some(&kw_axis0)).expect("fnp f16 axis0 nancum"),
+                        fnp_fn
+                            .call((&hsqn2,), Some(&kw_axis0))
+                            .expect("fnp f16 axis0 nancum"),
                     )
                 });
             });
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:11142:
             group.bench_function(format!("numpy_{op}_axis0_f16_16m"), |bch| {
                 bch.iter(|| {
                     black_box(
-                        numpy_fn.call((&hsqn2,), Some(&kw_axis0)).expect("numpy f16 axis0 nancum"),
+                        numpy_fn
+                            .call((&hsqn2,), Some(&kw_axis0))
+                            .expect("numpy f16 axis0 nancum"),
                     )
                 });
             });
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:11167:
         for (arr, tag) in [(&c64, "f64"), (&ci64, "i64")] {
             group.bench_function(format!("fnp_cumsum_axis0_{tag}_16m"), |bch| {
                 bch.iter(|| {
-                    black_box(fnp_cumsum.call((arr,), Some(&kw_axis0)).expect("fnp cumsum axis0"))
+                    black_box(
+                        fnp_cumsum
+                            .call((arr,), Some(&kw_axis0))
+                            .expect("fnp cumsum axis0"),
+                    )
                 });
             });
             group.bench_function(format!("numpy_cumsum_axis0_{tag}_16m"), |bch| {
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:11174:
                 bch.iter(|| {
                     black_box(
-                        numpy_cumsum.call((arr,), Some(&kw_axis0)).expect("numpy cumsum axis0"),
+                        numpy_cumsum
+                            .call((arr,), Some(&kw_axis0))
+                            .expect("numpy cumsum axis0"),
                     )
                 });
             });
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:11208:
             let numpy_fn = numpy.getattr(op).expect("numpy arg op");
             group.bench_function(format!("fnp_{op}_axis0_f16_16m"), |bch| {
                 bch.iter(|| {
-                    black_box(fnp_fn.call((&hsq2,), Some(&kw_axis0)).expect("fnp f16 axis0 arg"))
+                    black_box(
+                        fnp_fn
+                            .call((&hsq2,), Some(&kw_axis0))
+                            .expect("fnp f16 axis0 arg"),
+                    )
                 });
             });
             group.bench_function(format!("numpy_{op}_axis0_f16_16m"), |bch| {
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:11215:
                 bch.iter(|| {
                     black_box(
-                        numpy_fn.call((&hsq2,), Some(&kw_axis0)).expect("numpy f16 axis0 arg"),
+                        numpy_fn
+                            .call((&hsq2,), Some(&kw_axis0))
+                            .expect("numpy f16 axis0 arg"),
                     )
                 });
             });
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:11697:
             let a = ns.get_item(format!("a{sz}")).expect("a");
             let b = ns.get_item(format!("b{sz}")).expect("b");
             group.bench_function(format!("fnp_tensordot_axes1_{sz}x{sz}"), |bch| {
-                bch.iter(|| {
-                    black_box(
-                        fnp_tensordot
-                            .call1((&a, &b, 1_i64))
-                            .expect("fnp call"),
-                    )
-                });
+                bch.iter(|| black_box(fnp_tensordot.call1((&a, &b, 1_i64)).expect("fnp call")));
             });
             group.bench_function(format!("numpy_tensordot_axes1_{sz}x{sz}"), |bch| {
-                bch.iter(|| {
-                    black_box(
-                        np_tensordot
-                            .call1((&a, &b, 1_i64))
-                            .expect("numpy call"),
-                    )
-                });
+                bch.iter(|| black_box(np_tensordot.call1((&a, &b, 1_i64)).expect("numpy call")));
             });
         }
         // Batched (3-D) matmul: native parallel-across-batch packed GEMM vs numpy slow BLAS.
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:11719:
-        for (tag, ak, bk) in [("64x256x256", "a3d", "b3d"), ("256x128x128", "a3db", "b3db")] {
+        for (tag, ak, bk) in [
+            ("64x256x256", "a3d", "b3d"),
+            ("256x128x128", "a3db", "b3db"),
+        ] {
             let a = ns.get_item(ak).expect("a3d");
             let b = ns.get_item(bk).expect("b3d");
             group.bench_function(format!("fnp_matmul_batched_{tag}"), |bch| {
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:12445:
                     .expect("array_equal")
                     .extract()
                     .expect("bool");
-                assert!(eq, "unique(axis=0) return_* correctness mismatch at tuple index {t}");
+                assert!(
+                    eq,
+                    "unique(axis=0) return_* correctness mismatch at tuple index {t}"
+                );
             }
         }
         group.bench_function("fnp_unique_rows_full_500k4", |b| {
Diff in /data/projects/franken_numpy/crates/fnp-python/benches/criterion_python_surface.rs:12452:
             b.iter(|| black_box(fnp_unique.call((&a,), Some(&kw)).expect("fnp unique full")));
         });
         group.bench_function("numpy_unique_rows_full_500k4", |b| {
-            b.iter(|| black_box(numpy_unique.call((&a,), Some(&kw)).expect("numpy unique full")));
+            b.iter(|| {
+                black_box(
+                    numpy_unique
+                        .call((&a,), Some(&kw))
+                        .expect("numpy unique full"),
+                )
+            });
         });
     });

Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:38:
 use fnp_ufunc::{
     BinaryOp, FromPyFuncReduceAxisSpec, FromPyFuncReduceError, FromPyFuncReduceIdentity,
     FromPyFuncReduceOptions, GridSpec, IntegerSidecar, MAError, MaskedArray, UFuncArray, UnaryOp,
-    bitwise_and as ufunc_bitwise_and,
-    bitwise_count as ufunc_bitwise_count, bitwise_or as ufunc_bitwise_or,
-    bitwise_xor as ufunc_bitwise_xor, divide as ufunc_divide,
-    divmod_arrays as ufunc_divmod, equal as ufunc_equal, fmax as ufunc_fmax,
-    fmin as ufunc_fmin, frexp as ufunc_frexp,
-    greater as ufunc_greater, greater_equal as ufunc_greater_equal,
-    isneginf as ufunc_isneginf,
-    isposinf as ufunc_isposinf, left_shift as ufunc_left_shift,
-    less as ufunc_less, less_equal as ufunc_less_equal,
-    logaddexp2 as ufunc_logaddexp2, logical_and as ufunc_logical_and,
-    logical_not as ufunc_logical_not, logical_or as ufunc_logical_or,
-    logical_xor as ufunc_logical_xor, ma_is_masked, ma_make_mask, ma_mask_or,
-    matmul_accumulate_serial, maximum as ufunc_maximum, minimum as ufunc_minimum, modf as ufunc_modf,
-    not_equal as ufunc_not_equal, power as ufunc_power,
+    bitwise_and as ufunc_bitwise_and, bitwise_count as ufunc_bitwise_count,
+    bitwise_or as ufunc_bitwise_or, bitwise_xor as ufunc_bitwise_xor, divide as ufunc_divide,
+    divmod_arrays as ufunc_divmod, equal as ufunc_equal, fmax as ufunc_fmax, fmin as ufunc_fmin,
+    frexp as ufunc_frexp, greater as ufunc_greater, greater_equal as ufunc_greater_equal,
+    isneginf as ufunc_isneginf, isposinf as ufunc_isposinf, left_shift as ufunc_left_shift,
+    less as ufunc_less, less_equal as ufunc_less_equal, logaddexp2 as ufunc_logaddexp2,
+    logical_and as ufunc_logical_and, logical_not as ufunc_logical_not,
+    logical_or as ufunc_logical_or, logical_xor as ufunc_logical_xor, ma_is_masked, ma_make_mask,
+    ma_mask_or, matmul_accumulate_serial, maximum as ufunc_maximum, minimum as ufunc_minimum,
+    modf as ufunc_modf, not_equal as ufunc_not_equal, power as ufunc_power,
     reduce_frompyfunc_values, remainder as ufunc_remainder, right_shift as ufunc_right_shift,
     signbit as ufunc_signbit, spacing as ufunc_spacing,
 };
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:362:
                         && let Some(b_slice) = b_buf.as_slice(py)
                     {
                         let b_raw: &[f64] = unsafe {
-                            std::slice::from_raw_parts(b_slice.as_ptr().cast::<f64>(), b_slice.len())
+                            std::slice::from_raw_parts(
+                                b_slice.as_ptr().cast::<f64>(),
+                                b_slice.len(),
+                            )
                         };
                         b_raw.iter().any(|&v| v == 0.0)
                     } else {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:462:
             // defers on zero divisor so numpy's RuntimeWarning + 0 surface. (FloorDivide is not in
             // the f64 binop fast-path set above, so this is the only native floor_divide path.)
             if matches!(self.kind, UFuncKind::FloorDivide)
-                && let Some(out_val) =
-                    try_native_int_floordiv(py, x1.bind(py), x2.bind(py))?
+                && let Some(out_val) = try_native_int_floordiv(py, x1.bind(py), x2.bind(py))?
             {
                 return Ok(out_val);
             }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:472:
             // int64 floor_divide of the raw counts; defers on any NaT (INT_MIN) or zero divisor
             // (numpy returns 0 there, not the floored quotient).
             if matches!(self.kind, UFuncKind::FloorDivide)
-                && let Some(out_val) =
-                    try_native_timedelta_floordiv(py, x1.bind(py), x2.bind(py))?
+                && let Some(out_val) = try_native_timedelta_floordiv(py, x1.bind(py), x2.bind(py))?
             {
                 return Ok(out_val);
             }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:531:
                 _ => None,
             };
             if let Some(op) = f16cmp
-                && let Some(out_val) =
-                    try_zerocopy_f16_compare(py, x1.bind(py), x2.bind(py), op)?
+                && let Some(out_val) = try_zerocopy_f16_compare(py, x1.bind(py), x2.bind(py), op)?
             {
                 return Ok(out_val);
             }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:1901:
                 // path `integers` uses) instead of the per-element numpy_bounded_uint64 loop
                 // + u64->i64 conversion pass — bit-exact (numpy choice == numpy integers,
                 // and fnp integers is bit-exact with numpy integers) and ~5x faster.
-                self.inner
-                    .integers(0, n, len)
-                    .map_err(map_random_error)?
+                self.inner.integers(0, n, len).map_err(map_random_error)?
             } else {
                 self.inner
                     .choice_indices_with_shuffle(population_len, len, replace, shuffle)
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:4647:
         // layout) and reconstruct via f16::from_bits. The old astype->tolist->Vec<f32>
         // path boxed one Python float per element — ~2500x slower than numpy on a 4M abs.
         DType::F16 => ArrayStorage::F16(
-            numpy_cast_contiguous_to_vec::<u16>(py, &flat.call_method1("view", ("uint16",))?, "uint16")?
-                .into_iter()
-                .map(f16::from_bits)
-                .collect(),
+            numpy_cast_contiguous_to_vec::<u16>(
+                py,
+                &flat.call_method1("view", ("uint16",))?,
+                "uint16",
+            )?
+            .into_iter()
+            .map(f16::from_bits)
+            .collect(),
         ),
         DType::F32 => ArrayStorage::F32(numpy_cast_contiguous_to_vec::<f32>(py, &flat, "float32")?),
         DType::F64 => ArrayStorage::F64(numpy_cast_contiguous_to_vec::<f64>(py, &flat, "float64")?),
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:5633:
                 // all-False mask to nomask, matching numpy exactly.
                 let kwargs = PyDict::new(py);
                 kwargs.set_item("copy", copy)?;
-                let result = numpy.getattr("ma")?.getattr("masked_where")?.call(
-                    (mask.bind(py), x.bind(py)),
-                    Some(&kwargs),
-                )?;
+                let result = numpy
+                    .getattr("ma")?
+                    .getattr("masked_where")?
+                    .call((mask.bind(py), x.bind(py)), Some(&kwargs))?;
                 if numpy_name == "masked_equal" {
                     result.setattr("fill_value", v)?;
                 }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:7534:
                 UnaryOp::Trunc => unary_map_f64(input, output, |x| UnaryOp::Trunc.apply(x)),
                 UnaryOp::Sign => unary_map_f64(input, output, |x| UnaryOp::Sign.apply(x)),
                 UnaryOp::Square => unary_map_f64(input, output, |x| UnaryOp::Square.apply(x)),
-                UnaryOp::Reciprocal => unary_map_f64(input, output, |x| UnaryOp::Reciprocal.apply(x)),
+                UnaryOp::Reciprocal => {
+                    unary_map_f64(input, output, |x| UnaryOp::Reciprocal.apply(x))
+                }
                 UnaryOp::Degrees => unary_map_f64(input, output, |x| UnaryOp::Degrees.apply(x)),
                 UnaryOp::Radians => unary_map_f64(input, output, |x| UnaryOp::Radians.apply(x)),
                 // The guard at the top of this fn restricts `op` to the arms above;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:8487:
     }
     let as_bool = bytes.call_method1("view", (numpy.getattr("bool_")?,))?;
     let output_shape = PyTuple::new(py, shape.iter().copied())?;
-    Ok(Some(as_bool.call_method1("reshape", (&output_shape,))?.unbind()))
+    Ok(Some(
+        as_bool.call_method1("reshape", (&output_shape,))?.unbind(),
+    ))
 }

 // f32 counterpart of try_zerocopy_f64_isclose_array_scalar. isclose(f32-array, finite scalar)
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:8565:
     }
     let as_bool = bytes.call_method1("view", (numpy.getattr("bool_")?,))?;
     let output_shape = PyTuple::new(py, shape.iter().copied())?;
-    Ok(Some(as_bool.call_method1("reshape", (&output_shape,))?.unbind()))
+    Ok(Some(
+        as_bool.call_method1("reshape", (&output_shape,))?.unbind(),
+    ))
 }

 // Wrap zerocopy_f64_isclose_flat with the shared reshape / 0-d scalar handling.
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:8680:
             // over f64; inputs are read-only under the GIL and `output` is a fresh buffer we
             // own (no alias). Bit-identical to the serial op.apply loop. (The old one-time
             // Vec copy predated this from_raw_parts trick and taxed the cheaper ops.)
-            let lhs: &[f64] =
-                unsafe { std::slice::from_raw_parts(a_in.as_ptr().cast::<f64>(), n) };
-            let rhs: &[f64] =
-                unsafe { std::slice::from_raw_parts(b_in.as_ptr().cast::<f64>(), n) };
+            let lhs: &[f64] = unsafe { std::slice::from_raw_parts(a_in.as_ptr().cast::<f64>(), n) };
+            let rhs: &[f64] = unsafe { std::slice::from_raw_parts(b_in.as_ptr().cast::<f64>(), n) };
             let out_data: &mut [f64] =
                 unsafe { std::slice::from_raw_parts_mut(output.as_ptr() as *mut f64, n) };
             let chunk = n.div_ceil(rayon::current_num_threads());
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:8778:
         return Ok(None);
     }
     let is_contig = |v: &Bound<'_, PyAny>| -> PyResult<bool> {
-        v.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()
+        v.getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()
     };
     if !is_contig(a)? || !is_contig(b)? {
         return Ok(None);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:8788:
         return Ok(None);
     }
     let u16t = numpy.getattr("uint16")?;
-    let (Ok(a16), Ok(b16)) = (a.call_method1("view", (&u16t,)), b.call_method1("view", (&u16t,)))
-    else {
+    let (Ok(a16), Ok(b16)) = (
+        a.call_method1("view", (&u16t,)),
+        b.call_method1("view", (&u16t,)),
+    ) else {
         return Ok(None);
     };
     let (Ok(a_buf), Ok(b_buf)) = (PyBuffer::<u16>::get(&a16), PyBuffer::<u16>::get(&b16)) else {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:8826:
         // NaN/inf — defer those to numpy so its overflow warning + special-value handling surface.
         // (Over-deferring the rare [32768, 46340) magnitudes is still correct — numpy computes them
         // exactly with no warning.) The test is a memory-bound integer scan, not an f32 widen.
-        if op == 16 && a_raw.iter().chain(b_raw.iter()).any(|&v| (v & 0x7C00) >= 0x7800) {
+        if op == 16
+            && a_raw
+                .iter()
+                .chain(b_raw.iter())
+                .any(|&v| (v & 0x7C00) >= 0x7800)
+        {
             return Ok(None);
         }
         // logaddexp/logaddexp2 (17/18): a NaN/inf operand can make numpy emit an "invalid" RuntimeWarning
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:8844:
         // power (14): defer any non-finite operand up front (numpy owns inf/nan results +
         // any warning). Remaining finite-input warning cases (0^neg divide, neg^non-int
         // invalid, finite->overflow) are detected during the compute and flagged below.
-        if op == 14 && a_raw.iter().chain(b_raw.iter()).any(|&v| (v & 0x7C00) == 0x7C00) {
+        if op == 14
+            && a_raw
+                .iter()
+                .chain(b_raw.iter())
+                .any(|&v| (v & 0x7C00) == 0x7C00)
+        {
             return Ok(None);
         }
         let out_raw: &mut [u16] =
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:8911:
                             let mut rem = av % bv;
                             if rem != 0.0 && rem.is_sign_negative() != bv.is_sign_negative() {
                                 rem += bv;
-                            } else if rem == 0.0 && rem.is_sign_negative() != bv.is_sign_negative() {
+                            } else if rem == 0.0 && rem.is_sign_negative() != bv.is_sign_negative()
+                            {
                                 rem = 0.0_f32.copysign(bv);
                             }
                             f16::from_f32(rem).to_bits()
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:9319:
     {
         return Ok(None);
     }
-    if !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()? {
+    if !a
+        .getattr("flags")?
+        .getattr("c_contiguous")?
+        .extract::<bool>()?
+    {
         return Ok(None);
     }
     let shape: Vec<usize> = a.getattr("shape")?.extract()?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:9359:
     let data: &[u16] = unsafe { std::slice::from_raw_parts(cells.as_ptr().cast::<u16>(), total) };
     use rayon::prelude::*;
     // Any NaN -> defer the whole reduction to numpy (first-NaN-index semantics differ).
-    if data.par_iter().any(|&b| (b & 0x7c00) == 0x7c00 && (b & 0x03ff) != 0) {
+    if data
+        .par_iter()
+        .any(|&b| (b & 0x7c00) == 0x7c00 && (b & 0x03ff) != 0)
+    {
         return Ok(None);
     }
     let out_len = outer * inner;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:9510:
     {
         return Ok(None);
     }
-    if !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()? {
+    if !a
+        .getattr("flags")?
+        .getattr("c_contiguous")?
+        .extract::<bool>()?
+    {
         return Ok(None);
     }
     let shape: Vec<usize> = a.getattr("shape")?.extract()?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:9550:
     let data: &[u16] = unsafe { std::slice::from_raw_parts(cells.as_ptr().cast::<u16>(), total) };
     use rayon::prelude::*;
     // Any NaN -> defer (numpy propagates a specific NaN's bits, fold-order-dependent).
-    if data.par_iter().any(|&b| (b & 0x7c00) == 0x7c00 && (b & 0x03ff) != 0) {
+    if data
+        .par_iter()
+        .any(|&b| (b & 0x7c00) == 0x7c00 && (b & 0x03ff) != 0)
+    {
         return Ok(None);
     }
     let out_len = outer * inner;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:9696:
     {
         return Ok(None);
     }
-    if !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()? {
+    if !a
+        .getattr("flags")?
+        .getattr("c_contiguous")?
+        .extract::<bool>()?
+    {
         return Ok(None);
     }
     let shape: Vec<usize> = a.getattr("shape")?.extract()?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:9736:
     let data: &[u16] = unsafe { std::slice::from_raw_parts(cells.as_ptr().cast::<u16>(), total) };
     use rayon::prelude::*;
     // Any NaN -> defer (numpy propagates a NaN into the lane's ptp).
-    if data.par_iter().any(|&b| (b & 0x7c00) == 0x7c00 && (b & 0x03ff) != 0) {
+    if data
+        .par_iter()
+        .any(|&b| (b & 0x7c00) == 0x7c00 && (b & 0x03ff) != 0)
+    {
         return Ok(None);
     }
     let out_len = outer * inner;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:9793:
     {
         return Ok(None);
     }
-    if !x.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()? {
+    if !x
+        .getattr("flags")?
+        .getattr("c_contiguous")?
+        .extract::<bool>()?
+    {
         return Ok(None);
     }
     let shape: Vec<usize> = x.getattr("shape")?.extract()?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:9814:
     let x_raw: &[u16] = unsafe { std::slice::from_raw_parts(x_in.as_ptr().cast::<u16>(), n) };
     use rayon::prelude::*;
     let chunk = n.div_ceil(rayon::current_num_threads());
-    let init = if is_max { f32::NEG_INFINITY } else { f32::INFINITY };
+    let init = if is_max {
+        f32::NEG_INFINITY
+    } else {
+        f32::INFINITY
+    };
     let (any_valid, ext) = x_raw
         .par_chunks(chunk)
         .map(|c| {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:9878:
     {
         return Ok(None);
     }
-    if !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()? {
+    if !a
+        .getattr("flags")?
+        .getattr("c_contiguous")?
+        .extract::<bool>()?
+    {
         return Ok(None);
     }
     let shape: Vec<usize> = a.getattr("shape")?.extract()?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:9917:
     }
     let data: &[u16] = unsafe { std::slice::from_raw_parts(cells.as_ptr().cast::<u16>(), total) };
     use rayon::prelude::*;
-    let init = if is_max { f32::NEG_INFINITY } else { f32::INFINITY };
+    let init = if is_max {
+        f32::NEG_INFINITY
+    } else {
+        f32::INFINITY
+    };
     let out_len = outer * inner;
     let bad = std::sync::atomic::AtomicBool::new(false); // any all-NaN OR zero-extremum lane -> defer
     let mut out = vec![0u16; out_len];
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:9996:
         return Ok(None);
     }
     let is_contig = |v: &Bound<'_, PyAny>| -> PyResult<bool> {
-        v.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()
+        v.getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()
     };
     if !is_contig(a)? || !is_contig(b)? {
         return Ok(None);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:10006:
         return Ok(None);
     }
     let u16t = numpy.getattr("uint16")?;
-    let (Ok(a16), Ok(b16)) = (a.call_method1("view", (&u16t,)), b.call_method1("view", (&u16t,)))
-    else {
+    let (Ok(a16), Ok(b16)) = (
+        a.call_method1("view", (&u16t,)),
+        b.call_method1("view", (&u16t,)),
+    ) else {
         return Ok(None);
     };
     let (Ok(a_buf), Ok(b_buf)) = (PyBuffer::<u16>::get(&a16), PyBuffer::<u16>::get(&b16)) else {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:10382:
         return Ok(None);
     }
     let is_contig = |v: &Bound<'_, PyAny>| -> PyResult<bool> {
-        v.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()
+        v.getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()
     };
     if !is_contig(a)? || !is_contig(b)? {
         return Ok(None);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:10404:
         };
     }
     match (kind.as_str(), itemsize) {
-        ("i", 1) => gk!(i8, "int8", |x: i8| x.unsigned_abs() as u64, |g: u64| g as i8),
-        ("i", 2) => gk!(i16, "int16", |x: i16| x.unsigned_abs() as u64, |g: u64| g as i16),
-        ("i", 4) => gk!(i32, "int32", |x: i32| x.unsigned_abs() as u64, |g: u64| g as i32),
+        ("i", 1) => gk!(i8, "int8", |x: i8| x.unsigned_abs() as u64, |g: u64| g
+            as i8),
+        ("i", 2) => gk!(i16, "int16", |x: i16| x.unsigned_abs() as u64, |g: u64| g
+            as i16),
+        ("i", 4) => gk!(i32, "int32", |x: i32| x.unsigned_abs() as u64, |g: u64| g
+            as i32),
         ("i", 8) => gk!(i64, "int64", |x: i64| x.unsigned_abs(), |g: u64| g as i64),
         ("u", 1) => gk!(u8, "uint8", |x: u8| x as u64, |g: u64| g as u8),
         ("u", 2) => gk!(u16, "uint16", |x: u16| x as u64, |g: u64| g as u16),
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:10507:
         return Ok(None);
     }
     let is_contig = |v: &Bound<'_, PyAny>| -> PyResult<bool> {
-        v.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()
+        v.getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()
     };
     if !is_contig(a)? || !is_contig(b)? {
         return Ok(None);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:10529:
         };
     }
     match (kind.as_str(), itemsize) {
-        ("i", 1) => lk!(i8, "int8", |x: i8| x.unsigned_abs() as u64, |g: u64| g as i8),
-        ("i", 2) => lk!(i16, "int16", |x: i16| x.unsigned_abs() as u64, |g: u64| g as i16),
-        ("i", 4) => lk!(i32, "int32", |x: i32| x.unsigned_abs() as u64, |g: u64| g as i32),
+        ("i", 1) => lk!(i8, "int8", |x: i8| x.unsigned_abs() as u64, |g: u64| g
+            as i8),
+        ("i", 2) => lk!(i16, "int16", |x: i16| x.unsigned_abs() as u64, |g: u64| g
+            as i16),
+        ("i", 4) => lk!(i32, "int32", |x: i32| x.unsigned_abs() as u64, |g: u64| g
+            as i32),
         ("i", 8) => lk!(i64, "int64", |x: i64| x.unsigned_abs(), |g: u64| g as i64),
         ("u", 1) => lk!(u8, "uint8", |x: u8| x as u64, |g: u64| g as u8),
         ("u", 2) => lk!(u16, "uint16", |x: u16| x as u64, |g: u64| g as u16),
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:10637:
         return Ok(None);
     }
     let is_contig = |v: &Bound<'_, PyAny>| -> PyResult<bool> {
-        v.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()
+        v.getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()
     };
     if !is_contig(a)? || !is_contig(b)? {
         return Ok(None);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:10790:
         return Ok(None);
     }
     let is_contig = |v: &Bound<'_, PyAny>| -> PyResult<bool> {
-        v.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()
+        v.getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()
     };
     if !is_contig(a)? || !is_contig(b)? {
         return Ok(None);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:10910:
         return Ok(None);
     }
     let result_dtype = match (ak.as_str(), bk.as_str(), is_add) {
-        ("m", "m", _) => a_dt.clone(), // td +/- td -> td
-        ("M", "m", _) => a_dt.clone(), // dt +/- td -> dt
+        ("m", "m", _) => a_dt.clone(),    // td +/- td -> td
+        ("M", "m", _) => a_dt.clone(),    // dt +/- td -> dt
         ("m", "M", true) => b_dt.clone(), // td + dt -> dt
         ("M", "M", false) => {
             // dt - dt -> timedelta of the (shared) unit; preserve a count>1 multiple.
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:10927:
         _ => return Ok(None), // dt+dt / td-dt are invalid -> let numpy raise
     };
     let is_contig = |v: &Bound<'_, PyAny>| -> PyResult<bool> {
-        v.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()
+        v.getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()
     };
     if !is_contig(a)? || !is_contig(b)? {
         return Ok(None);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:10941:
         return Ok(None);
     }
     let i64t = numpy.getattr("int64")?;
-    let (Ok(a_i), Ok(b_i)) = (a.call_method1("view", (&i64t,)), b.call_method1("view", (&i64t,)))
-    else {
+    let (Ok(a_i), Ok(b_i)) = (
+        a.call_method1("view", (&i64t,)),
+        b.call_method1("view", (&i64t,)),
+    ) else {
         return Ok(None);
     };
     let result = int_binary_map_typed::<i64, _>(py, &numpy, &a_i, &b_i, "int64", move |x, y| {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:10955:
         }
     })?;
     match result {
-        Some(r) => Ok(Some(r.bind(py).call_method1("view", (&result_dtype,))?.unbind())),
+        Some(r) => Ok(Some(
+            r.bind(py).call_method1("view", (&result_dtype,))?.unbind(),
+        )),
         None => Ok(None),
     }
 }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:10978:
         return Ok(None);
     }
     let is_contig = |v: &Bound<'_, PyAny>| -> PyResult<bool> {
-        v.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()
+        v.getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()
     };
     if !is_contig(a)? || !is_contig(b)? {
         return Ok(None);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:10993:
     }
     // View both as int64 (timedelta64 is int64 internally; NaT == i64::MIN).
     let i64t = numpy.getattr("int64")?;
-    let (Ok(a_i), Ok(b_i)) = (a.call_method1("view", (&i64t,)), b.call_method1("view", (&i64t,)))
-    else {
+    let (Ok(a_i), Ok(b_i)) = (
+        a.call_method1("view", (&i64t,)),
+        b.call_method1("view", (&i64t,)),
+    ) else {
         return Ok(None);
     };
     // Scan for NaT (either operand == i64::MIN) or a zero divisor; defer those to numpy.
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:11001:
     {
-        let (Ok(a_buf), Ok(b_buf)) = (PyBuffer::<i64>::get(&a_i), PyBuffer::<i64>::get(&b_i)) else {
+        let (Ok(a_buf), Ok(b_buf)) = (PyBuffer::<i64>::get(&a_i), PyBuffer::<i64>::get(&b_i))
+        else {
             return Ok(None);
         };
         let (Some(a_s), Some(b_s)) = (a_buf.as_slice(py), b_buf.as_slice(py)) else {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:11008:
         use rayon::prelude::*;
         let ar: &[i64] = unsafe { std::slice::from_raw_parts(a_s.as_ptr().cast::<i64>(), n) };
         let br: &[i64] = unsafe { std::slice::from_raw_parts(b_s.as_ptr().cast::<i64>(), n) };
-        if ar.par_iter().any(|&v| v == i64::MIN) || br.par_iter().any(|&v| v == i64::MIN || v == 0) {
+        if ar.par_iter().any(|&v| v == i64::MIN) || br.par_iter().any(|&v| v == i64::MIN || v == 0)
+        {
             return Ok(None);
         }
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:11045:
         return Ok(None);
     }
     let is_contig = |v: &Bound<'_, PyAny>| -> PyResult<bool> {
-        v.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()
+        v.getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()
     };
     if !is_contig(a)? || !is_contig(b)? {
         return Ok(None);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:11059:
         return Ok(None);
     }
     let i64t = numpy.getattr("int64")?;
-    let (Ok(a_i), Ok(b_i)) = (a.call_method1("view", (&i64t,)), b.call_method1("view", (&i64t,)))
-    else {
+    let (Ok(a_i), Ok(b_i)) = (
+        a.call_method1("view", (&i64t,)),
+        b.call_method1("view", (&i64t,)),
+    ) else {
         return Ok(None);
     };
     {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:11067:
-        let (Ok(a_buf), Ok(b_buf)) = (PyBuffer::<i64>::get(&a_i), PyBuffer::<i64>::get(&b_i)) else {
+        let (Ok(a_buf), Ok(b_buf)) = (PyBuffer::<i64>::get(&a_i), PyBuffer::<i64>::get(&b_i))
+        else {
             return Ok(None);
         };
         let (Some(a_s), Some(b_s)) = (a_buf.as_slice(py), b_buf.as_slice(py)) else {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:11073:
         use rayon::prelude::*;
         let ar: &[i64] = unsafe { std::slice::from_raw_parts(a_s.as_ptr().cast::<i64>(), n) };
         let br: &[i64] = unsafe { std::slice::from_raw_parts(b_s.as_ptr().cast::<i64>(), n) };
-        if ar.par_iter().any(|&v| v == i64::MIN) || br.par_iter().any(|&v| v == i64::MIN || v == 0) {
+        if ar.par_iter().any(|&v| v == i64::MIN) || br.par_iter().any(|&v| v == i64::MIN || v == 0)
+        {
             return Ok(None);
         }
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:11080:
     // Floored remainder (sign of divisor) of the raw counts -> int64, then view back to timedelta.
-    let int_res = int_binary_map_typed::<i64, _>(py, &numpy, &a_i, &b_i, "int64", |x: i64, y: i64| {
-        let r = x.wrapping_rem(y);
-        if r != 0 && (r < 0) != (y < 0) {
-            r.wrapping_add(y)
-        } else {
-            r
-        }
-    })?;
+    let int_res =
+        int_binary_map_typed::<i64, _>(py, &numpy, &a_i, &b_i, "int64", |x: i64, y: i64| {
+            let r = x.wrapping_rem(y);
+            if r != 0 && (r < 0) != (y < 0) {
+                r.wrapping_add(y)
+            } else {
+                r
+            }
+        })?;
     match int_res {
         Some(res) => Ok(Some(res.bind(py).call_method1("view", (dt,))?.unbind())),
         None => Ok(None),
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:11115:
         return Ok(None);
     }
     let is_contig = |v: &Bound<'_, PyAny>| -> PyResult<bool> {
-        v.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()
+        v.getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()
     };
     if !is_contig(a)? || !is_contig(b)? {
         return Ok(None);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:11224:
     let quotient = mk(name)?;
     let remainder = mk(name)?;
     {
-        let (Ok(qbuf), Ok(rbuf)) = (PyBuffer::<T>::get(&quotient), PyBuffer::<T>::get(&remainder))
-        else {
+        let (Ok(qbuf), Ok(rbuf)) = (
+            PyBuffer::<T>::get(&quotient),
+            PyBuffer::<T>::get(&remainder),
+        ) else {
             return Ok(None);
         };
         let (Some(qout), Some(rout)) = (qbuf.as_mut_slice(py), rbuf.as_mut_slice(py)) else {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:11243:
             .zip(lhs.par_chunks(chunk))
             .zip(rhs.par_chunks(chunk))
             .for_each(|(((qq, rr), aa), bb)| {
-                for (((qs, rs), &x), &y) in
-                    qq.iter_mut().zip(rr.iter_mut()).zip(aa.iter()).zip(bb.iter())
+                for (((qs, rs), &x), &y) in qq
+                    .iter_mut()
+                    .zip(rr.iter_mut())
+                    .zip(aa.iter())
+                    .zip(bb.iter())
                 {
                     let (qv, rv) = op(x, y);
                     *qs = qv;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:11261:
         return Ok(Some(PyTuple::new(py, [&qs, &rs])?.into_any().unbind()));
     }
     Ok(Some(
-        PyTuple::new(py, [&quotient, &remainder])?.into_any().unbind(),
+        PyTuple::new(py, [&quotient, &remainder])?
+            .into_any()
+            .unbind(),
     ))
 }

Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:11281:
         return Ok(None);
     }
     let is_contig = |v: &Bound<'_, PyAny>| -> PyResult<bool> {
-        v.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()
+        v.getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()
     };
     if !is_contig(a)? || !is_contig(b)? {
         return Ok(None);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:11823:
     if !numpy_dtype_is_f32(x1) || !ndarray_has_native_i32_dtype(x2)? {
         return Ok(None);
     }
-    let (Ok(x1_buffer), Ok(x2_buffer)) = (PyBuffer::<f32>::get(x1), PyBuffer::<i32>::get(x2)) else {
+    let (Ok(x1_buffer), Ok(x2_buffer)) = (PyBuffer::<f32>::get(x1), PyBuffer::<i32>::get(x2))
+    else {
         return Ok(None);
     };
     if x1_buffer.shape() != x2_buffer.shape() {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:11907:
     {
         return Ok(None);
     }
-    if !x1.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()? {
+    if !x1
+        .getattr("flags")?
+        .getattr("c_contiguous")?
+        .extract::<bool>()?
+    {
         return Ok(None);
     }
     let u16t = numpy.getattr("uint16")?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:11914:
     let Ok(x116) = x1.call_method1("view", (&u16t,)) else {
         return Ok(None);
     };
-    let (Ok(x1_buffer), Ok(x2_buffer)) = (PyBuffer::<u16>::get(&x116), PyBuffer::<i32>::get(x2)) else {
+    let (Ok(x1_buffer), Ok(x2_buffer)) = (PyBuffer::<u16>::get(&x116), PyBuffer::<i32>::get(x2))
+    else {
         return Ok(None);
     };
     if x1_buffer.shape() != x2_buffer.shape() {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:12640:
                         .zip(xd.par_chunks(chunk))
                         .zip(yd.par_chunks(chunk))
                         .for_each(|(((o, cc), xx), yy)| {
-                            for (((s, &cv), &xv), &yv) in
-                                o.iter_mut().zip(cc).zip(xx).zip(yy)
-                            {
+                            for (((s, &cv), &xv), &yv) in o.iter_mut().zip(cc).zip(xx).zip(yy) {
                                 *s = if cv != 0 { xv } else { yv };
                             }
                         });
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:12906:
     if x_is_arr == y_is_arr {
         return Ok(None); // need exactly one array + one scalar
     }
-    let (arr, scalar_obj, scalar_is_x) = if x_is_arr { (x, y, false) } else { (y, x, true) };
+    let (arr, scalar_obj, scalar_is_x) = if x_is_arr {
+        (x, y, false)
+    } else {
+        (y, x, true)
+    };
     // The non-array side must be a TRUE scalar. ndarray SUBCLASSES (e.g. an
     // __array_function__ override), 0-d arrays, lists, and tuples all expose __len__ and
     // must defer to numpy (which dispatches subclass overrides and broadcasts sequences);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:12998:
                 let Some(out_cells) = out_buf.as_mut_slice(py) else {
                     return Ok(None);
                 };
-                for ((slot, cc), av) in out_cells
-                    .iter()
-                    .zip(cond_cells.iter())
-                    .zip(arr_in.iter())
-                {
+                for ((slot, cc), av) in out_cells.iter().zip(cond_cells.iter()).zip(arr_in.iter()) {
                     // arr is x when scalar is y; arr is y when scalar is x.
                     let use_arr = (cc.get() != 0) != scalar_is_x;
                     slot.set(if use_arr { av.get() } else { scalar_u });
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:13177:
             .iter()
             .map(|s| unsafe { std::slice::from_raw_parts(s.as_ptr() as *const f64, n) })
             .collect();
-        let default_raw: Option<&[f64]> =
-            default_slice.map(|s| unsafe { std::slice::from_raw_parts(s.as_ptr() as *const f64, n) });
+        let default_raw: Option<&[f64]> = default_slice
+            .map(|s| unsafe { std::slice::from_raw_parts(s.as_ptr() as *const f64, n) });
         // First true condition wins (numpy.select picks the earliest match); else the per-element
         // (array) default, or the scalar default.
         let fill = |i: usize| -> f64 {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:13281:
         return Ok(None);
     };
     let n = input.len();
-    let out = numpy
-        .getattr("empty_like")?
-        .call1((a,))?;
+    let out = numpy.getattr("empty_like")?.call1((a,))?;
     if n > 0 {
         // Normalize the shift into [0, n); result[i] = input[(i - shift) mod n],
         // i.e. result = input[n-s..] ++ input[..n-s].
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:13467:
         // SAFETY: ReadOnlyCell/Cell<f64> are repr(transparent); input read-only under the GIL, output a fresh
         // owned buffer. Each outer lane is an independent within-lane roll -> parallelize over lanes, and copy
         // the two contiguous runs with memcpy (was a serial per-element Cell.set loop = the parity).
-        let in_raw: &[f64] = unsafe { std::slice::from_raw_parts(input.as_ptr().cast::<f64>(), total) };
+        let in_raw: &[f64] =
+            unsafe { std::slice::from_raw_parts(input.as_ptr().cast::<f64>(), total) };
         let out_raw: &mut [f64] =
             unsafe { std::slice::from_raw_parts_mut(output.as_ptr() as *mut f64, total) };
         use rayon::prelude::*;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:13566:
         // SAFETY: repr(transparent) u8 cells; input read-only under the GIL, output a fresh owned buffer. Each
         // lane is an independent within-lane byte roll -> parallelize over lanes + memcpy the two runs (was a
         // serial per-BYTE Cell.set loop = the parity).
-        let in_raw: &[u8] = unsafe { std::slice::from_raw_parts(input.as_ptr().cast::<u8>(), total_bytes) };
+        let in_raw: &[u8] =
+            unsafe { std::slice::from_raw_parts(input.as_ptr().cast::<u8>(), total_bytes) };
         let out_raw: &mut [u8] =
             unsafe { std::slice::from_raw_parts_mut(output.as_ptr() as *mut u8, total_bytes) };
         use rayon::prelude::*;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:13581:
                 });
         } else {
             // One / few big lanes (axis=0, outer==1): parallelize WITHIN each lane's two byte-run copies.
-            for (orow, irow) in out_raw.chunks_mut(lane_bytes).zip(in_raw.chunks(lane_bytes)) {
+            for (orow, irow) in out_raw
+                .chunks_mut(lane_bytes)
+                .zip(in_raw.chunks(lane_bytes))
+            {
                 par_copy_slice(&mut orow[..head], &irow[split..]);
                 par_copy_slice(&mut orow[head..], &irow[..split]);
             }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:13589:
     }
     let out_typed = out_u8.call_method1("view", (&a_dtype,))?;
     let output_shape = PyTuple::new(py, shape.iter().copied())?;
-    let output = out_typed.call_method1("reshape", (&output_shape,))?.unbind();
+    let output = out_typed
+        .call_method1("reshape", (&output_shape,))?
+        .unbind();
     Ok(Some(output))
 }

Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:13656:
         // (was a serial per-element Cell.set loop = the parity).
         let split = cols - sc;
         let total = rows * cols;
-        let in_raw: &[f64] = unsafe { std::slice::from_raw_parts(input.as_ptr().cast::<f64>(), total) };
+        let in_raw: &[f64] =
+            unsafe { std::slice::from_raw_parts(input.as_ptr().cast::<f64>(), total) };
         let out_raw: &mut [f64] =
             unsafe { std::slice::from_raw_parts_mut(output.as_ptr() as *mut f64, total) };
         use rayon::prelude::*;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:13719:
             _ => return Ok(None),
         }
     }
-    if !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()? {
+    if !a
+        .getattr("flags")?
+        .getattr("c_contiguous")?
+        .extract::<bool>()?
+    {
         return Ok(None);
     }
     let uint8 = numpy.getattr("uint8")?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:13748:
         // SAFETY: repr-transparent u8 cells; input read-only, output fresh+owned (empty_like). Output
         // rows are disjoint -> parallelize over output rows; each memcpys the two byte runs of one
         // (row-shifted) input row.
-        let in_raw: &[u8] = unsafe { std::slice::from_raw_parts(input.as_ptr().cast::<u8>(), total) };
+        let in_raw: &[u8] =
+            unsafe { std::slice::from_raw_parts(input.as_ptr().cast::<u8>(), total) };
         let out_raw: &mut [u8] =
             unsafe { std::slice::from_raw_parts_mut(output.as_ptr() as *mut u8, total) };
         use rayon::prelude::*;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:13876:
     {
         return Ok(None);
     }
-    if condition.getattr("dtype")?.getattr("kind")?.extract::<String>()? != "b" {
+    if condition
+        .getattr("dtype")?
+        .getattr("kind")?
+        .extract::<String>()?
+        != "b"
+    {
         return Ok(None);
     }
     let shape: Vec<usize> = a.getattr("shape")?.extract()?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:13951:
         }
     }
     let output_shape = PyTuple::new(py, out_shape.iter().copied())?;
-    Ok(Some(flat.call_method1("reshape", (&output_shape,))?.unbind()))
+    Ok(Some(
+        flat.call_method1("reshape", (&output_shape,))?.unbind(),
+    ))
 }

 // Generic typed core for boolean-mask stream compaction (np.compress / np.extract
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:14057:
     if !arr.is_exact_instance(&ndarray_type) || !cond.is_exact_instance(&ndarray_type) {
         return Ok(None);
     }
-    if cond.getattr("dtype")?.getattr("kind")?.extract::<String>()? != "b" {
+    if cond
+        .getattr("dtype")?
+        .getattr("kind")?
+        .extract::<String>()?
+        != "b"
+    {
         return Ok(None);
     }
     let a_dtype = arr.getattr("dtype")?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:14672:
     // over disjoint raw-slice chunks (in-place, no copy-back); numpy runs this single-threaded.
     // SAFETY: ReadOnlyCell<u8/f64>/Cell<f64> are repr(transparent); read-only mask/values under
     // the GIL, a's buffer written only at disjoint, non-overlapping chunk positions.
-    let a_slice: &mut [f64] = unsafe { std::slice::from_raw_parts_mut(a_out.as_ptr() as *mut f64, n) };
+    let a_slice: &mut [f64] =
+        unsafe { std::slice::from_raw_parts_mut(a_out.as_ptr() as *mut f64, n) };
     let m: &[u8] = unsafe { std::slice::from_raw_parts(mask_in.as_ptr().cast::<u8>(), n) };
     let vals: &[f64] = unsafe { std::slice::from_raw_parts(val_in.as_ptr().cast::<f64>(), v) };
     const PUTMASK_PARALLEL_MIN: usize = 1 << 19;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:14810:
     {
         return Ok(false);
     }
-    if mask.getattr("dtype")?.getattr("kind")?.extract::<String>()? != "b" {
+    if mask
+        .getattr("dtype")?
+        .getattr("kind")?
+        .extract::<String>()?
+        != "b"
+    {
         return Ok(false);
     }
     let a_dtype = a.getattr("dtype")?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:14915:
     {
         return Ok(false);
     }
-    if mask.getattr("dtype")?.getattr("kind")?.extract::<String>()? != "b" {
+    if mask
+        .getattr("dtype")?
+        .getattr("kind")?
+        .extract::<String>()?
+        != "b"
+    {
         return Ok(false);
     }
     let a_dtype = arr.getattr("dtype")?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:15407:
             use rayon::prelude::*;
             // SAFETY: ReadOnlyCell<T>/Cell<A> are repr(transparent) over their value;
             // input is read-only under the GIL and `flat` is a fresh numpy.empty we own.
-            let in_raw: &[T] =
-                unsafe { std::slice::from_raw_parts(input.as_ptr().cast::<T>(), n) };
+            let in_raw: &[T] = unsafe { std::slice::from_raw_parts(input.as_ptr().cast::<T>(), n) };
             let out_raw: &mut [A] =
                 unsafe { std::slice::from_raw_parts_mut(output.as_ptr() as *mut A, n) };
             let chunk = n.div_ceil(rayon::current_num_threads());
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:15534:
             |a, b| a.wrapping_add(b),
             true,
         ),
-        ("i", 8) => {
-            cumsum_typed::<i64, i64, _, _>(py, &numpy, a, "int64", |v| v, |a, b| a.wrapping_add(b), true)
-        }
+        ("i", 8) => cumsum_typed::<i64, i64, _, _>(
+            py,
+            &numpy,
+            a,
+            "int64",
+            |v| v,
+            |a, b| a.wrapping_add(b),
+            true,
+        ),
         ("u", 1) => cumsum_typed::<u8, u64, _, _>(
             py,
             &numpy,
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:15564:
             |a, b| a.wrapping_add(b),
             true,
         ),
-        ("u", 8) => {
-            cumsum_typed::<u64, u64, _, _>(py, &numpy, a, "uint64", |v| v, |a, b| a.wrapping_add(b), true)
-        }
+        ("u", 8) => cumsum_typed::<u64, u64, _, _>(
+            py,
+            &numpy,
+            a,
+            "uint64",
+            |v| v,
+            |a, b| a.wrapping_add(b),
+            true,
+        ),
         _ => Ok(None),
     }
 }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:15811:
                 unsafe { std::slice::from_raw_parts_mut(output.as_ptr() as *mut f64, total) };
             let scan = |(orow, irow): (&mut [f64], &[f64])| {
                 let first = irow[0];
-                let mut acc = if skip_nan && first.is_nan() { ident } else { first };
+                let mut acc = if skip_nan && first.is_nan() {
+                    ident
+                } else {
+                    first
+                };
                 orow[0] = acc;
                 for k in 1..axis_len {
                     let value = irow[k];
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:15862:
                     for i in 0..inner {
                         let carried = oblk[prev + i];
                         let value = iblk[cur + i];
-                        let contrib = if skip_nan && value.is_nan() { ident } else { value };
+                        let contrib = if skip_nan && value.is_nan() {
+                            ident
+                        } else {
+                            value
+                        };
                         oblk[cur + i] = if is_prod {
                             carried * contrib
                         } else {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:15889:
                 // (carrying the skip_nan/-0.0 semantics), transpose back. Bit-identical per column;
                 // columns disjoint. numpy runs cumsum single-threaded, so axis-0 was the serial gap.
                 let mut scratch = vec![0.0f64; total];
-                scratch.par_chunks_mut(axis_len).enumerate().for_each(|(c, col)| {
-                    let first = in_raw[c];
-                    let mut acc = if skip_nan && first.is_nan() { ident } else { first };
-                    col[0] = acc;
-                    for r in 1..axis_len {
-                        let value = in_raw[r * inner + c];
-                        if !(skip_nan && value.is_nan()) {
-                            acc = if is_prod { acc * value } else { acc + value };
+                scratch
+                    .par_chunks_mut(axis_len)
+                    .enumerate()
+                    .for_each(|(c, col)| {
+                        let first = in_raw[c];
+                        let mut acc = if skip_nan && first.is_nan() {
+                            ident
+                        } else {
+                            first
+                        };
+                        col[0] = acc;
+                        for r in 1..axis_len {
+                            let value = in_raw[r * inner + c];
+                            if !(skip_nan && value.is_nan()) {
+                                acc = if is_prod { acc * value } else { acc + value };
+                            }
+                            col[r] = acc;
                         }
-                        col[r] = acc;
-                    }
-                });
-                out_raw.par_chunks_mut(inner).enumerate().for_each(|(r, orow)| {
-                    for (c, slot) in orow.iter_mut().enumerate() {
-                        *slot = scratch[c * axis_len + r];
-                    }
-                });
+                    });
+                out_raw
+                    .par_chunks_mut(inner)
+                    .enumerate()
+                    .for_each(|(r, orow)| {
+                        for (c, slot) in orow.iter_mut().enumerate() {
+                            *slot = scratch[c * axis_len + r];
+                        }
+                    });
             } else {
                 out_raw
                     .chunks_mut(lane)
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:15977:
         // contiguous PyBuffer slices held under the GIL. The output is fresh numpy.empty
         // (cannot alias the input) and each lane/block writes a disjoint range, so the
         // &[T] / &mut [A] views are sound for the parallel folds below.
-        let in_raw: &[T] =
-            unsafe { std::slice::from_raw_parts(input.as_ptr().cast::<T>(), total) };
+        let in_raw: &[T] = unsafe { std::slice::from_raw_parts(input.as_ptr().cast::<T>(), total) };
         let out_raw: &mut [A] =
             unsafe { std::slice::from_raw_parts_mut(output.as_ptr() as *mut A, total) };
         use rayon::prelude::*;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:16039:
                 // transpose back. Each per-column scan is bit-identical; columns are disjoint. numpy
                 // runs cumsum single-threaded for every dtype, so axis-0 was the last serial gap.
                 let mut scratch: Vec<A> = vec![convert(in_raw[0]); total];
-                scratch.par_chunks_mut(axis_len).enumerate().for_each(|(c, col)| {
-                    let mut acc = convert(in_raw[c]);
-                    col[0] = acc;
-                    for r in 1..axis_len {
-                        acc = op(acc, convert(in_raw[r * inner + c]));
-                        col[r] = acc;
-                    }
-                });
-                out_raw.par_chunks_mut(inner).enumerate().for_each(|(r, orow)| {
-                    for (c, slot) in orow.iter_mut().enumerate() {
-                        *slot = scratch[c * axis_len + r];
-                    }
-                });
+                scratch
+                    .par_chunks_mut(axis_len)
+                    .enumerate()
+                    .for_each(|(c, col)| {
+                        let mut acc = convert(in_raw[c]);
+                        col[0] = acc;
+                        for r in 1..axis_len {
+                            acc = op(acc, convert(in_raw[r * inner + c]));
+                            col[r] = acc;
+                        }
+                    });
+                out_raw
+                    .par_chunks_mut(inner)
+                    .enumerate()
+                    .for_each(|(r, orow)| {
+                        for (c, slot) in orow.iter_mut().enumerate() {
+                            *slot = scratch[c * axis_len + r];
+                        }
+                    });
             } else {
                 out_raw
                     .chunks_mut(lane)
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:16089:
     // bool -> int64 accumulator (uint8 view of the 0/1 bytes; see try_zerocopy_int_cumsum).
     let result = if kind == "b" {
         let viewed = a.call_method1("view", (numpy.getattr("uint8")?,))?;
-        cumsum_axis_typed::<u8, i64, _, _>(py, &numpy, &viewed, axis, "int64", |v| v as i64, add_i64)?
+        cumsum_axis_typed::<u8, i64, _, _>(
+            py,
+            &numpy,
+            &viewed,
+            axis,
+            "int64",
+            |v| v as i64,
+            add_i64,
+        )?
     } else {
         match (kind.as_str(), itemsize) {
-        ("i", 1) => cumsum_axis_typed::<i8, i64, _, _>(py, &numpy, a, axis, "int64", |v| v as i64, add_i64)?,
-        ("i", 2) => cumsum_axis_typed::<i16, i64, _, _>(py, &numpy, a, axis, "int64", |v| v as i64, add_i64)?,
-        ("i", 4) => cumsum_axis_typed::<i32, i64, _, _>(py, &numpy, a, axis, "int64", |v| v as i64, add_i64)?,
-        ("i", 8) => cumsum_axis_typed::<i64, i64, _, _>(py, &numpy, a, axis, "int64", |v| v, add_i64)?,
-        ("u", 1) => cumsum_axis_typed::<u8, u64, _, _>(py, &numpy, a, axis, "uint64", |v| v as u64, add_u64)?,
-        ("u", 2) => cumsum_axis_typed::<u16, u64, _, _>(py, &numpy, a, axis, "uint64", |v| v as u64, add_u64)?,
-        ("u", 4) => cumsum_axis_typed::<u32, u64, _, _>(py, &numpy, a, axis, "uint64", |v| v as u64, add_u64)?,
-        ("u", 8) => cumsum_axis_typed::<u64, u64, _, _>(py, &numpy, a, axis, "uint64", |v| v, add_u64)?,
-        _ => return Ok(None),
+            ("i", 1) => cumsum_axis_typed::<i8, i64, _, _>(
+                py,
+                &numpy,
+                a,
+                axis,
+                "int64",
+                |v| v as i64,
+                add_i64,
+            )?,
+            ("i", 2) => cumsum_axis_typed::<i16, i64, _, _>(
+                py,
+                &numpy,
+                a,
+                axis,
+                "int64",
+                |v| v as i64,
+                add_i64,
+            )?,
+            ("i", 4) => cumsum_axis_typed::<i32, i64, _, _>(
+                py,
+                &numpy,
+                a,
+                axis,
+                "int64",
+                |v| v as i64,
+                add_i64,
+            )?,
+            ("i", 8) => {
+                cumsum_axis_typed::<i64, i64, _, _>(py, &numpy, a, axis, "int64", |v| v, add_i64)?
+            }
+            ("u", 1) => cumsum_axis_typed::<u8, u64, _, _>(
+                py,
+                &numpy,
+                a,
+                axis,
+                "uint64",
+                |v| v as u64,
+                add_u64,
+            )?,
+            ("u", 2) => cumsum_axis_typed::<u16, u64, _, _>(
+                py,
+                &numpy,
+                a,
+                axis,
+                "uint64",
+                |v| v as u64,
+                add_u64,
+            )?,
+            ("u", 4) => cumsum_axis_typed::<u32, u64, _, _>(
+                py,
+                &numpy,
+                a,
+                axis,
+                "uint64",
+                |v| v as u64,
+                add_u64,
+            )?,
+            ("u", 8) => {
+                cumsum_axis_typed::<u64, u64, _, _>(py, &numpy, a, axis, "uint64", |v| v, add_u64)?
+            }
+            _ => return Ok(None),
         }
     };
     let Some((flat, shape)) = result else {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:16204:
     {
         return Ok(None);
     }
-    if !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()? {
+    if !a
+        .getattr("flags")?
+        .getattr("c_contiguous")?
+        .extract::<bool>()?
+    {
         return Ok(None);
     }
     let shape: Vec<usize> = a.getattr("shape")?.extract()?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:16279:
             }
         };
         if total >= CUM_AXIS_PARALLEL_MIN && rayon::current_num_threads() >= 2 {
-            out_raw.par_chunks_mut(axis_len).zip(in_raw.par_chunks(axis_len)).for_each(scan);
+            out_raw
+                .par_chunks_mut(axis_len)
+                .zip(in_raw.par_chunks(axis_len))
+                .for_each(scan);
         } else {
-            out_raw.chunks_mut(axis_len).zip(in_raw.chunks(axis_len)).for_each(scan);
+            out_raw
+                .chunks_mut(axis_len)
+                .zip(in_raw.chunks(axis_len))
+                .for_each(scan);
         }
     } else {
         // Non-last axis: each OUTER block is an independent (axis_len, inner) cumulative over its
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:16289:
         let scan_block = |(oblk, iblk): (&mut [u16], &[u16])| {
             for i in 0..inner {
                 let f0 = f16::from_bits(iblk[i]).to_f32();
-                oblk[i] = if skip_nan && f0.is_nan() { ident_bits } else { iblk[i] };
+                oblk[i] = if skip_nan && f0.is_nan() {
+                    ident_bits
+                } else {
+                    iblk[i]
+                };
             }
             for a_idx in 1..axis_len {
                 let cur = a_idx * inner;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:16304:
             }
         };
         if outer >= 2 && total >= CUM_AXIS_PARALLEL_MIN && rayon::current_num_threads() >= 2 {
-            out_raw.par_chunks_mut(lane).zip(in_raw.par_chunks(lane)).for_each(scan_block);
+            out_raw
+                .par_chunks_mut(lane)
+                .zip(in_raw.par_chunks(lane))
+                .for_each(scan_block);
         } else if outer == 1
             && inner >= 2
             && total >= CUM_AXIS_PARALLEL_MIN
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:16315:
             // inner) slab into (inner, axis_len) scratch, scan each contiguous column (par across
             // columns), transpose back. Each per-column scan is bit-identical; columns are disjoint.
             let mut scratch = vec![0u16; total];
-            scratch.par_chunks_mut(axis_len).enumerate().for_each(|(c, col)| {
-                for (r, slot) in col.iter_mut().enumerate() {
-                    *slot = in_raw[r * inner + c];
-                }
-                let f0 = f16::from_bits(col[0]).to_f32();
-                let nan0 = skip_nan && f0.is_nan();
-                let mut acc = if nan0 { ident } else { f0 };
-                if nan0 {
-                    col[0] = ident_bits;
-                }
-                for r in 1..axis_len {
-                    let raw = f16::from_bits(col[r]).to_f32();
-                    let v = if skip_nan && raw.is_nan() { ident } else { raw };
-                    let h = f16::from_f32(if is_prod { acc * v } else { acc + v });
-                    col[r] = h.to_bits();
-                    acc = h.to_f32();
-                }
-            });
-            out_raw.par_chunks_mut(inner).enumerate().for_each(|(r, orow)| {
-                for (c, slot) in orow.iter_mut().enumerate() {
-                    *slot = scratch[c * axis_len + r];
-                }
-            });
+            scratch
+                .par_chunks_mut(axis_len)
+                .enumerate()
+                .for_each(|(c, col)| {
+                    for (r, slot) in col.iter_mut().enumerate() {
+                        *slot = in_raw[r * inner + c];
+                    }
+                    let f0 = f16::from_bits(col[0]).to_f32();
+                    let nan0 = skip_nan && f0.is_nan();
+                    let mut acc = if nan0 { ident } else { f0 };
+                    if nan0 {
+                        col[0] = ident_bits;
+                    }
+                    for r in 1..axis_len {
+                        let raw = f16::from_bits(col[r]).to_f32();
+                        let v = if skip_nan && raw.is_nan() { ident } else { raw };
+                        let h = f16::from_f32(if is_prod { acc * v } else { acc + v });
+                        col[r] = h.to_bits();
+                        acc = h.to_f32();
+                    }
+                });
+            out_raw
+                .par_chunks_mut(inner)
+                .enumerate()
+                .for_each(|(r, orow)| {
+                    for (c, slot) in orow.iter_mut().enumerate() {
+                        *slot = scratch[c * axis_len + r];
+                    }
+                });
         } else {
-            out_raw.chunks_mut(lane).zip(in_raw.chunks(lane)).for_each(scan_block);
+            out_raw
+                .chunks_mut(lane)
+                .zip(in_raw.chunks(lane))
+                .for_each(scan_block);
         }
     }
     Ok(Some(out.unbind()))
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:16437:
     // extract path (~15x slower than numpy at 4M).
     if kind == "b" {
         let viewed = a.call_method1("view", (numpy.getattr("uint8")?,))?;
-        return cumsum_typed::<u8, i64, _, _>(py, &numpy, &viewed, "int64", |v| v as i64, mul_i64, true);
+        return cumsum_typed::<u8, i64, _, _>(
+            py,
+            &numpy,
+            &viewed,
+            "int64",
+            |v| v as i64,
+            mul_i64,
+            true,
+        );
     }
     match (kind.as_str(), itemsize) {
-        ("i", 1) => cumsum_typed::<i8, i64, _, _>(py, &numpy, a, "int64", |v| v as i64, mul_i64, true),
-        ("i", 2) => cumsum_typed::<i16, i64, _, _>(py, &numpy, a, "int64", |v| v as i64, mul_i64, true),
-        ("i", 4) => cumsum_typed::<i32, i64, _, _>(py, &numpy, a, "int64", |v| v as i64, mul_i64, true),
+        ("i", 1) => {
+            cumsum_typed::<i8, i64, _, _>(py, &numpy, a, "int64", |v| v as i64, mul_i64, true)
+        }
+        ("i", 2) => {
+            cumsum_typed::<i16, i64, _, _>(py, &numpy, a, "int64", |v| v as i64, mul_i64, true)
+        }
+        ("i", 4) => {
+            cumsum_typed::<i32, i64, _, _>(py, &numpy, a, "int64", |v| v as i64, mul_i64, true)
+        }
         ("i", 8) => cumsum_typed::<i64, i64, _, _>(py, &numpy, a, "int64", |v| v, mul_i64, true),
-        ("u", 1) => cumsum_typed::<u8, u64, _, _>(py, &numpy, a, "uint64", |v| v as u64, mul_u64, true),
-        ("u", 2) => cumsum_typed::<u16, u64, _, _>(py, &numpy, a, "uint64", |v| v as u64, mul_u64, true),
-        ("u", 4) => cumsum_typed::<u32, u64, _, _>(py, &numpy, a, "uint64", |v| v as u64, mul_u64, true),
+        ("u", 1) => {
+            cumsum_typed::<u8, u64, _, _>(py, &numpy, a, "uint64", |v| v as u64, mul_u64, true)
+        }
+        ("u", 2) => {
+            cumsum_typed::<u16, u64, _, _>(py, &numpy, a, "uint64", |v| v as u64, mul_u64, true)
+        }
+        ("u", 4) => {
+            cumsum_typed::<u32, u64, _, _>(py, &numpy, a, "uint64", |v| v as u64, mul_u64, true)
+        }
         ("u", 8) => cumsum_typed::<u64, u64, _, _>(py, &numpy, a, "uint64", |v| v, mul_u64, true),
         _ => Ok(None),
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:16477:
     // bool -> int64 accumulator (uint8 view of the 0/1 bytes; see try_zerocopy_int_cumprod).
     let result = if kind == "b" {
         let viewed = a.call_method1("view", (numpy.getattr("uint8")?,))?;
-        cumsum_axis_typed::<u8, i64, _, _>(py, &numpy, &viewed, axis, "int64", |v| v as i64, mul_i64)?
+        cumsum_axis_typed::<u8, i64, _, _>(
+            py,
+            &numpy,
+            &viewed,
+            axis,
+            "int64",
+            |v| v as i64,
+            mul_i64,
+        )?
     } else {
         match (kind.as_str(), itemsize) {
-        ("i", 1) => cumsum_axis_typed::<i8, i64, _, _>(py, &numpy, a, axis, "int64", |v| v as i64, mul_i64)?,
-        ("i", 2) => cumsum_axis_typed::<i16, i64, _, _>(py, &numpy, a, axis, "int64", |v| v as i64, mul_i64)?,
-        ("i", 4) => cumsum_axis_typed::<i32, i64, _, _>(py, &numpy, a, axis, "int64", |v| v as i64, mul_i64)?,
-        ("i", 8) => cumsum_axis_typed::<i64, i64, _, _>(py, &numpy, a, axis, "int64", |v| v, mul_i64)?,
-        ("u", 1) => cumsum_axis_typed::<u8, u64, _, _>(py, &numpy, a, axis, "uint64", |v| v as u64, mul_u64)?,
-        ("u", 2) => cumsum_axis_typed::<u16, u64, _, _>(py, &numpy, a, axis, "uint64", |v| v as u64, mul_u64)?,
-        ("u", 4) => cumsum_axis_typed::<u32, u64, _, _>(py, &numpy, a, axis, "uint64", |v| v as u64, mul_u64)?,
-        ("u", 8) => cumsum_axis_typed::<u64, u64, _, _>(py, &numpy, a, axis, "uint64", |v| v, mul_u64)?,
-        _ => return Ok(None),
+            ("i", 1) => cumsum_axis_typed::<i8, i64, _, _>(
+                py,
+                &numpy,
+                a,
+                axis,
+                "int64",
+                |v| v as i64,
+                mul_i64,
+            )?,
+            ("i", 2) => cumsum_axis_typed::<i16, i64, _, _>(
+                py,
+                &numpy,
+                a,
+                axis,
+                "int64",
+                |v| v as i64,
+                mul_i64,
+            )?,
+            ("i", 4) => cumsum_axis_typed::<i32, i64, _, _>(
+                py,
+                &numpy,
+                a,
+                axis,
+                "int64",
+                |v| v as i64,
+                mul_i64,
+            )?,
+            ("i", 8) => {
+                cumsum_axis_typed::<i64, i64, _, _>(py, &numpy, a, axis, "int64", |v| v, mul_i64)?
+            }
+            ("u", 1) => cumsum_axis_typed::<u8, u64, _, _>(
+                py,
+                &numpy,
+                a,
+                axis,
+                "uint64",
+                |v| v as u64,
+                mul_u64,
+            )?,
+            ("u", 2) => cumsum_axis_typed::<u16, u64, _, _>(
+                py,
+                &numpy,
+                a,
+                axis,
+                "uint64",
+                |v| v as u64,
+                mul_u64,
+            )?,
+            ("u", 4) => cumsum_axis_typed::<u32, u64, _, _>(
+                py,
+                &numpy,
+                a,
+                axis,
+                "uint64",
+                |v| v as u64,
+                mul_u64,
+            )?,
+            ("u", 8) => {
+                cumsum_axis_typed::<u64, u64, _, _>(py, &numpy, a, axis, "uint64", |v| v, mul_u64)?
+            }
+            _ => return Ok(None),
         }
     };
     let Some((flat, shape)) = result else {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:16747:
     out_shape[last] = out_shape[last].saturating_mul(n_elems);
     let output_shape = PyTuple::new(py, out_shape.iter().copied())?;
     Ok(Some(
-        out_typed.call_method1("reshape", (&output_shape,))?.unbind(),
+        out_typed
+            .call_method1("reshape", (&output_shape,))?
+            .unbind(),
     ))
 }

Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:16900:
     let out_typed = out_u8.call_method1("view", (&dtype,))?;
     let output_shape = PyTuple::new(py, out_shape.iter().copied())?;
     Ok(Some(
-        out_typed.call_method1("reshape", (&output_shape,))?.unbind(),
+        out_typed
+            .call_method1("reshape", (&output_shape,))?
+            .unbind(),
     ))
 }

Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:16960:
         if n_out >= DIFF1D_PARALLEL_MIN && rayon::current_num_threads() >= 2 {
             use rayon::prelude::*;
             let chunk = n_out.div_ceil(rayon::current_num_threads());
-            out_raw.par_chunks_mut(chunk).enumerate().for_each(|(ci, o)| {
-                let base = ci * chunk;
-                for (j, slot) in o.iter_mut().enumerate() {
-                    let i = base + j;
-                    *slot = in_raw[i + 1] - in_raw[i];
-                }
-            });
+            out_raw
+                .par_chunks_mut(chunk)
+                .enumerate()
+                .for_each(|(ci, o)| {
+                    let base = ci * chunk;
+                    for (j, slot) in o.iter_mut().enumerate() {
+                        let i = base + j;
+                        *slot = in_raw[i + 1] - in_raw[i];
+                    }
+                });
         } else {
             for (i, slot) in out_raw.iter_mut().enumerate() {
                 *slot = in_raw[i + 1] - in_raw[i];
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:17051:
         let par = total_out >= DIFF_AXIS_PARALLEL_MIN && rayon::current_num_threads() >= 2;
         if inner == 1 {
             if par && outer >= 2 {
-                out_raw.par_chunks_mut(out_axis_len).enumerate().for_each(|(o, outl)| {
-                    let base = o * in_lane;
-                    for (a, slot) in outl.iter_mut().enumerate() {
-                        *slot = in_raw[base + a + 1] - in_raw[base + a];
-                    }
-                });
+                out_raw
+                    .par_chunks_mut(out_axis_len)
+                    .enumerate()
+                    .for_each(|(o, outl)| {
+                        let base = o * in_lane;
+                        for (a, slot) in outl.iter_mut().enumerate() {
+                            *slot = in_raw[base + a + 1] - in_raw[base + a];
+                        }
+                    });
             } else if par {
                 let chunk = out_axis_len.div_ceil(rayon::current_num_threads());
-                out_raw.par_chunks_mut(chunk).enumerate().for_each(|(ci, outl)| {
-                    let base = ci * chunk;
-                    for (j, slot) in outl.iter_mut().enumerate() {
-                        let i = base + j;
-                        *slot = in_raw[i + 1] - in_raw[i];
-                    }
-                });
+                out_raw
+                    .par_chunks_mut(chunk)
+                    .enumerate()
+                    .for_each(|(ci, outl)| {
+                        let base = ci * chunk;
+                        for (j, slot) in outl.iter_mut().enumerate() {
+                            let i = base + j;
+                            *slot = in_raw[i + 1] - in_raw[i];
+                        }
+                    });
             } else {
                 for o in 0..outer {
                     let base = o * in_lane;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:17076:
                 }
             }
         } else if par {
-            out_raw.par_chunks_mut(inner).enumerate().for_each(|(c, row)| {
-                let o = c / out_axis_len;
-                let a_out = c % out_axis_len;
-                let cur = o * in_lane + a_out * inner;
-                let nxt = cur + inner;
-                for i in 0..inner {
-                    row[i] = in_raw[nxt + i] - in_raw[cur + i];
-                }
-            });
+            out_raw
+                .par_chunks_mut(inner)
+                .enumerate()
+                .for_each(|(c, row)| {
+                    let o = c / out_axis_len;
+                    let a_out = c % out_axis_len;
+                    let cur = o * in_lane + a_out * inner;
+                    let nxt = cur + inner;
+                    for i in 0..inner {
+                        row[i] = in_raw[nxt + i] - in_raw[cur + i];
+                    }
+                });
         } else {
             for o in 0..outer {
                 let ibase = o * in_lane;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:17175:
             // Last-axis (or 1-D) lanes are contiguous.
             if par && outer >= 2 {
                 // One lane per chunk (out_lane == out_axis_len when inner == 1).
-                out_raw.par_chunks_mut(out_axis_len).enumerate().for_each(|(o, outl)| {
-                    let base = o * in_lane;
-                    for (a, slot) in outl.iter_mut().enumerate() {
-                        *slot = sub(in_raw[base + a + 1], in_raw[base + a]);
-                    }
-                });
+                out_raw
+                    .par_chunks_mut(out_axis_len)
+                    .enumerate()
+                    .for_each(|(o, outl)| {
+                        let base = o * in_lane;
+                        for (a, slot) in outl.iter_mut().enumerate() {
+                            *slot = sub(in_raw[base + a + 1], in_raw[base + a]);
+                        }
+                    });
             } else if par {
                 // Single lane (outer == 1): chunk it across threads (mirrors ediff1d).
                 let chunk = out_axis_len.div_ceil(rayon::current_num_threads());
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:17187:
-                out_raw.par_chunks_mut(chunk).enumerate().for_each(|(ci, outl)| {
-                    let base = ci * chunk;
-                    for (j, slot) in outl.iter_mut().enumerate() {
-                        let i = base + j;
-                        *slot = sub(in_raw[i + 1], in_raw[i]);
-                    }
-                });
+                out_raw
+                    .par_chunks_mut(chunk)
+                    .enumerate()
+                    .for_each(|(ci, outl)| {
+                        let base = ci * chunk;
+                        for (j, slot) in outl.iter_mut().enumerate() {
+                            let i = base + j;
+                            *slot = sub(in_raw[i + 1], in_raw[i]);
+                        }
+                    });
             } else {
                 for o in 0..outer {
                     let base = o * in_lane;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:17203:
         } else if par {
             // Strided: one output row (inner contiguous elems) per chunk; chunk index c maps to
             // (outer o, axis position a_out) = (c / out_axis_len, c % out_axis_len).
-            out_raw.par_chunks_mut(inner).enumerate().for_each(|(c, row)| {
-                let o = c / out_axis_len;
-                let a_out = c % out_axis_len;
-                let cur = o * in_lane + a_out * inner;
-                let nxt = cur + inner;
-                for i in 0..inner {
-                    row[i] = sub(in_raw[nxt + i], in_raw[cur + i]);
-                }
-            });
+            out_raw
+                .par_chunks_mut(inner)
+                .enumerate()
+                .for_each(|(c, row)| {
+                    let o = c / out_axis_len;
+                    let a_out = c % out_axis_len;
+                    let cur = o * in_lane + a_out * inner;
+                    let nxt = cur + inner;
+                    for i in 0..inner {
+                        row[i] = sub(in_raw[nxt + i], in_raw[cur + i]);
+                    }
+                });
         } else {
             for o in 0..outer {
                 let ibase = o * in_lane;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:17454:
         if n_out >= EDIFF1D_PARALLEL_MIN && rayon::current_num_threads() >= 2 {
             use rayon::prelude::*;
             let chunk = n_out.div_ceil(rayon::current_num_threads());
-            out_raw.par_chunks_mut(chunk).enumerate().for_each(|(ci, o)| {
-                let base = ci * chunk;
-                for (j, slot) in o.iter_mut().enumerate() {
-                    let i = base + j;
-                    *slot = sub(in_raw[i + 1], in_raw[i]);
-                }
-            });
+            out_raw
+                .par_chunks_mut(chunk)
+                .enumerate()
+                .for_each(|(ci, o)| {
+                    let base = ci * chunk;
+                    for (j, slot) in o.iter_mut().enumerate() {
+                        let i = base + j;
+                        *slot = sub(in_raw[i + 1], in_raw[i]);
+                    }
+                });
         } else {
             for (i, slot) in out_raw.iter_mut().enumerate() {
                 *slot = sub(in_raw[i + 1], in_raw[i]);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:17475:
 // Otherwise these extracted to an f64 Vec (~57x slower, lossy for wide ints).
 // Returns None when to_begin/to_end are present (handled by the general path), a
 // non-integer dtype, or a non-ndarray input.
-fn try_zerocopy_int_ediff1d(
-    py: Python<'_>,
-    ary: &Bound<'_, PyAny>,
-) -> PyResult<Option<Py<PyAny>>> {
+fn try_zerocopy_int_ediff1d(py: Python<'_>, ary: &Bound<'_, PyAny>) -> PyResult<Option<Py<PyAny>>> {
     let numpy = py.import("numpy")?;
     let ndarray_type = numpy.getattr("ndarray")?;
     if !ary.is_exact_instance(&ndarray_type) {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:17509:
 // Vec (~311x slower). Reuses the generic ediff1d_typed core with plain IEEE-754
 // single-precision subtraction x - y (bit-identical; nan/inf/-0.0 propagate exactly).
 // Returns None for a non-float32 dtype or a non-ndarray input.
-fn try_zerocopy_f32_ediff1d(
-    py: Python<'_>,
-    ary: &Bound<'_, PyAny>,
-) -> PyResult<Option<Py<PyAny>>> {
+fn try_zerocopy_f32_ediff1d(py: Python<'_>, ary: &Bound<'_, PyAny>) -> PyResult<Option<Py<PyAny>>> {
     let numpy = py.import("numpy")?;
     let ndarray_type = numpy.getattr("ndarray")?;
     if !ary.is_exact_instance(&ndarray_type) {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:18827:
         let half = len / 2;
         let mid = left + half;
         let probe = bins[mid];
-        let cond = if right_probe { probe <= key } else { probe < key };
+        let cond = if right_probe {
+            probe <= key
+        } else {
+            probe < key
+        };
         if cond {
             left = mid + 1;
             len -= half + 1;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:18922:
     if !x.is_exact_instance(&ndarray_type) || !bins.is_exact_instance(&ndarray_type) {
         return Ok(None);
     }
-    if x.getattr("ndim")?.extract::<usize>()? < 1
-        || bins.getattr("ndim")?.extract::<usize>()? != 1
+    if x.getattr("ndim")?.extract::<usize>()? < 1 || bins.getattr("ndim")?.extract::<usize>()? != 1
     {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:19114:
     // Pass 1: max value + non-negativity (a negative value defers so numpy raises).
     let (max_val, any_neg) = if parallel_scan {
         data.par_iter()
-            .fold(
-                || (-1i64, false),
-                |(mx, neg), &v| (mx.max(v), neg || v < 0),
-            )
+            .fold(|| (-1i64, false), |(mx, neg), &v| (mx.max(v), neg || v < 0))
             .reduce(|| (-1i64, false), |a, b| (a.0.max(b.0), a.1 || b.1))
     } else {
         // Branchless max + min fold (no data-dependent branch / early break) so LLVM
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:19151:
         // the K_MAX cap bounds privatized-array memory, and n >= length*32 ensures the
         // length*nthreads merge/alloc is a small fraction of the count (large-K needs a
         // proportionally larger n — e.g. K=65536 wins from ~4M, K=1000 from ~512K).
-        let parallel_count = parallel_scan
-            && length <= BINCOUNT_PARALLEL_K_MAX
-            && n >= length.saturating_mul(32);
+        let parallel_count =
+            parallel_scan && length <= BINCOUNT_PARALLEL_K_MAX && n >= length.saturating_mul(32);
         if parallel_count {
             // Privatized parallel tally: each chunk counts into a thread-local array,
             // then the locals are summed element-wise (bit-identical to serial counts).
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:19244:
     // float64 array (a no-op view when already f64). Defer on any cast failure.
     let kw = PyDict::new(py);
     kw.set_item("dtype", "float64")?;
-    let Ok(w_arr) = numpy.getattr("ascontiguousarray")?.call((weights,), Some(&kw)) else {
+    let Ok(w_arr) = numpy
+        .getattr("ascontiguousarray")?
+        .call((weights,), Some(&kw))
+    else {
         return Ok(None);
     };
     let Ok(w_buffer) = PyBuffer::<f64>::get(&w_arr) else {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:19444:
         let as_f64_kw = PyDict::new(py);
         as_f64_kw.set_item("dtype", &f64_dt)?;
         let fp_probe = numpy.call_method1("asarray", (fp.bind(py),))?;
-        if fp_probe.getattr("dtype")?.getattr("kind")?.extract::<String>()? == "c" {
+        if fp_probe
+            .getattr("dtype")?
+            .getattr("kind")?
+            .extract::<String>()?
+            == "c"
+        {
             return fallback(); // complex fp -> numpy (component-wise)
         }
         let x_arr = numpy.call_method("asarray", (x.bind(py),), Some(&as_f64_kw))?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:19460:
         let fp_s = fp_arr.get_item(&asort)?;
         let last = pyo3::types::PySlice::new(py, -1, isize::MAX, 1);
         let first = pyo3::types::PySlice::new(py, 0, 1, 1);
-        let xp_lo = xp_s.get_item(&last)?.call_method1("__sub__", (period_val,))?;
-        let xp_hi = xp_s.get_item(&first)?.call_method1("__add__", (period_val,))?;
+        let xp_lo = xp_s
+            .get_item(&last)?
+            .call_method1("__sub__", (period_val,))?;
+        let xp_hi = xp_s
+            .get_item(&first)?
+            .call_method1("__add__", (period_val,))?;
         let xp_aug =
             numpy.call_method1("concatenate", (PyTuple::new(py, [&xp_lo, &xp_s, &xp_hi])?,))?;
         let fp_lo = fp_s.get_item(&last)?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:19537:
     if xp_buf.shape().len() != 1 || fp_buf.shape().len() != 1 {
         return Ok(None);
     }
-    let (Some(x_cells), Some(xp_cells), Some(fp_cells)) = (
-        x_buf.as_slice(py),
-        xp_buf.as_slice(py),
-        fp_buf.as_slice(py),
-    ) else {
+    let (Some(x_cells), Some(xp_cells), Some(fp_cells)) =
+        (x_buf.as_slice(py), xp_buf.as_slice(py), fp_buf.as_slice(py))
+    else {
         return Ok(None);
     };
     let nx = x_cells.len();
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:19571:
     }
     let shape: Vec<usize> = x_buf.shape().to_vec();
     let output_shape = PyTuple::new(py, shape.iter().copied())?;
-    Ok(Some(flat.call_method1("reshape", (&output_shape,))?.unbind()))
+    Ok(Some(
+        flat.call_method1("reshape", (&output_shape,))?.unbind(),
+    ))
 }

 // Zero-copy parallel trapezoid for a 1-D f64 C-contiguous ndarray with uniform dx.
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:19721:
         }
     }
     let shape_tuple = PyTuple::new(py, shape[..ndim - 1].iter().copied())?;
-    Ok(Some(flat.call_method1("reshape", (&shape_tuple,))?.unbind()))
+    Ok(Some(
+        flat.call_method1("reshape", (&shape_tuple,))?.unbind(),
+    ))
 }

 // Zero-copy trapezoid along the LAST axis for an f32 C-contiguous ndarray (1-D -> f32
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:19819:
         }
     }
     let shape_tuple = PyTuple::new(py, shape[..ndim - 1].iter().copied())?;
-    Ok(Some(flat.call_method1("reshape", (&shape_tuple,))?.unbind()))
+    Ok(Some(
+        flat.call_method1("reshape", (&shape_tuple,))?.unbind(),
+    ))
 }

 fn trapezoid_impl(
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:19843:
         }
         kwargs.set_item("dx", dx)?;
         kwargs.set_item("axis", axis)?;
-        return Ok(numpy.getattr(name)?.call((y.bind(py),), Some(&kwargs))?.unbind());
+        return Ok(numpy
+            .getattr(name)?
+            .call((y.bind(py),), Some(&kwargs))?
+            .unbind());
     }
     // Zero-copy fast path: 1-D f64 contiguous y with uniform dx (no x). numpy's
     // trapezoid is single-threaded and allocates temporaries; read the buffer directly
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:20075:
     let count = input.iter().filter(|cell| pred(cell.get())).count();
     let kwargs = PyDict::new(py);
     kwargs.set_item("dtype", "int64")?;
-    let out = numpy.call_method("empty", (count,), Some(&kwargs))?.unbind();
+    let out = numpy
+        .call_method("empty", (count,), Some(&kwargs))?
+        .unbind();
     if count > 0 {
         let Ok(out_buffer) = PyBuffer::<i64>::get(out.bind(py)) else {
             return Ok(None);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:20359:
     let dtype = arr.getattr("dtype")?;
     let dtype_kind = dtype.getattr("kind")?.extract::<String>()?;
     let itemsize = dtype.getattr("itemsize")?.extract::<usize>()?;
-    if !(matches!(dtype_kind.as_str(), "b" | "i" | "u" | "f" | "c") && matches!(itemsize, 1 | 2 | 4 | 8))
+    if !(matches!(dtype_kind.as_str(), "b" | "i" | "u" | "f" | "c")
+        && matches!(itemsize, 1 | 2 | 4 | 8))
     {
         return fallback();
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:21069:
             // hazard), so the shared block / row-wise folds autovectorize. The
             // fallback widened ints to an f64 Vec and ran the strided reduce
             // (~17-23x slower). Dispatch by width to the right buffer element type.
-            let itemsize = a.getattr("dtype")?.getattr("itemsize")?.extract::<usize>()?;
+            let itemsize = a
+                .getattr("dtype")?
+                .getattr("itemsize")?
+                .extract::<usize>()?;
             match (kind.as_str(), itemsize) {
-                ("i", 1) => zerocopy_any_all_buf::<i8, _>(py, &numpy, a, axis, is_all, &shape, |v| v != 0),
-                ("i", 2) => zerocopy_any_all_buf::<i16, _>(py, &numpy, a, axis, is_all, &shape, |v| v != 0),
-                ("i", 4) => zerocopy_any_all_buf::<i32, _>(py, &numpy, a, axis, is_all, &shape, |v| v != 0),
-                ("i", 8) => zerocopy_any_all_buf::<i64, _>(py, &numpy, a, axis, is_all, &shape, |v| v != 0),
-                ("u", 1) => zerocopy_any_all_buf::<u8, _>(py, &numpy, a, axis, is_all, &shape, |v| v != 0),
-                ("u", 2) => zerocopy_any_all_buf::<u16, _>(py, &numpy, a, axis, is_all, &shape, |v| v != 0),
-                ("u", 4) => zerocopy_any_all_buf::<u32, _>(py, &numpy, a, axis, is_all, &shape, |v| v != 0),
-                ("u", 8) => zerocopy_any_all_buf::<u64, _>(py, &numpy, a, axis, is_all, &shape, |v| v != 0),
+                ("i", 1) => {
+                    zerocopy_any_all_buf::<i8, _>(py, &numpy, a, axis, is_all, &shape, |v| v != 0)
+                }
+                ("i", 2) => {
+                    zerocopy_any_all_buf::<i16, _>(py, &numpy, a, axis, is_all, &shape, |v| v != 0)
+                }
+                ("i", 4) => {
+                    zerocopy_any_all_buf::<i32, _>(py, &numpy, a, axis, is_all, &shape, |v| v != 0)
+                }
+                ("i", 8) => {
+                    zerocopy_any_all_buf::<i64, _>(py, &numpy, a, axis, is_all, &shape, |v| v != 0)
+                }
+                ("u", 1) => {
+                    zerocopy_any_all_buf::<u8, _>(py, &numpy, a, axis, is_all, &shape, |v| v != 0)
+                }
+                ("u", 2) => {
+                    zerocopy_any_all_buf::<u16, _>(py, &numpy, a, axis, is_all, &shape, |v| v != 0)
+                }
+                ("u", 4) => {
+                    zerocopy_any_all_buf::<u32, _>(py, &numpy, a, axis, is_all, &shape, |v| v != 0)
+                }
+                ("u", 8) => {
+                    zerocopy_any_all_buf::<u64, _>(py, &numpy, a, axis, is_all, &shape, |v| v != 0)
+                }
                 _ => Ok(None),
             }
         }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:21086:
             // float32 any/all (the f64 arm above is guarded): truthiness is v != 0.0
             // (NaN truthy, -0.0 falsy, matching numpy). The fallback widened to an
             // f64 Vec and ran the strided reduce (~28-40x slower).
-            let itemsize = a.getattr("dtype")?.getattr("itemsize")?.extract::<usize>()?;
+            let itemsize = a
+                .getattr("dtype")?
+                .getattr("itemsize")?
+                .extract::<usize>()?;
             if itemsize == 4 {
                 zerocopy_any_all_buf::<f32, _>(py, &numpy, a, axis, is_all, &shape, |v| v != 0.0)
             } else {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:21797:
     // not engage. bool input: numpy.clip is the parity reference (the native f64 bridge is ~680x
     // slower) — defer.
     if a.bind(py).is_exact_instance(&numpy.getattr("ndarray")?) {
-        let kind = a.bind(py).getattr("dtype")?.getattr("kind")?.extract::<String>()?;
-        let itemsize = a.bind(py).getattr("dtype")?.getattr("itemsize")?.extract::<usize>()?;
+        let kind = a
+            .bind(py)
+            .getattr("dtype")?
+            .getattr("kind")?
+            .extract::<String>()?;
+        let itemsize = a
+            .bind(py)
+            .getattr("dtype")?
+            .getattr("itemsize")?
+            .extract::<usize>()?;
         if kind == "f" && itemsize == 2 {
             if let Some(out) = try_zerocopy_f16_clip(py, a.bind(py), min_val, max_val)? {
                 return Ok(out);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:21819:

     // Zero-copy float32 clip with scalar bounds: clamp in f32, gated on a no-op
     // promotion so result dtype matches numpy; skips the cold f64 round-trip.
-    if let Some(out) =
-        try_zerocopy_f32_clip(py, a.bind(py), a_min.bind(py), a_max.bind(py), min_val, max_val)?
-    {
+    if let Some(out) = try_zerocopy_f32_clip(
+        py,
+        a.bind(py),
+        a_min.bind(py),
+        a_max.bind(py),
+        min_val,
+        max_val,
+    )? {
         return Ok(out);
     }

Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:21914:
         return Ok(None);
     }
     let rdt = repeats.getattr("dtype")?;
-    if !matches!(rdt.getattr("kind")?.extract::<String>()?.as_str(), "i" | "u")
-        || rdt.getattr("itemsize")?.extract::<usize>()? != 8
+    if !matches!(
+        rdt.getattr("kind")?.extract::<String>()?.as_str(),
+        "i" | "u"
+    ) || rdt.getattr("itemsize")?.extract::<usize>()? != 8
         || !repeats
             .getattr("flags")?
             .getattr("c_contiguous")?
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:21948:
             if axv != 0 {
                 return Ok(None);
             }
-            (shape[0], shape[1..].iter().product::<usize>() * itemsize, true)
+            (
+                shape[0],
+                shape[1..].iter().product::<usize>() * itemsize,
+                true,
+            )
         }
     };
     let Ok(c_buf) = PyBuffer::<i64>::get(repeats) else {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:22069:
     if !matches!(kind.as_str(), "b" | "i" | "u" | "f" | "c") {
         return Ok(None);
     }
-    if !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()? {
+    if !a
+        .getattr("flags")?
+        .getattr("c_contiguous")?
+        .extract::<bool>()?
+    {
         return Ok(None);
     }
     let shape: Vec<usize> = a.getattr("shape")?.extract()?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:22122:
     }
     let kw = PyDict::new(py);
     kw.set_item("dtype", &dtype)?;
-    let out = numpy.call_method("empty", (PyTuple::new(py, out_shape.iter().copied())?,), Some(&kw))?;
+    let out = numpy.call_method(
+        "empty",
+        (PyTuple::new(py, out_shape.iter().copied())?,),
+        Some(&kw),
+    )?;
     let out_bytes = out.call_method1("view", (&uint8,))?;
     let Ok(out_buf) = PyBuffer::<u8>::get(&out_bytes) else {
         return Ok(None);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:22136:
     use rayon::prelude::*;
     // SAFETY: ReadOnlyCell<u8>/Cell<u8> are repr(transparent) over u8; `a` is read-only under the GIL and
     // `out` is a fresh numpy.empty we own. Each output block (k copies of one unit) is disjoint.
-    let in_data: &[u8] = unsafe { std::slice::from_raw_parts(a_in.as_ptr().cast::<u8>(), a_in.len()) };
+    let in_data: &[u8] =
+        unsafe { std::slice::from_raw_parts(a_in.as_ptr().cast::<u8>(), a_in.len()) };
     let out_data: &mut [u8] =
         unsafe { std::slice::from_raw_parts_mut(output.as_ptr() as *mut u8, out_bytes_total) };
     out_data
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:22283:
             out_raw[na..].copy_from_slice(v_raw);
         }
     }
-    Ok(Some(out_bytes.call_method1("view", (&arr_dtype,))?.unbind()))
+    Ok(Some(
+        out_bytes.call_method1("view", (&arr_dtype,))?.unbind(),
+    ))
 }

 #[pyfunction]
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:22421:
         return Ok(None);
     };
     let n = cells.len();
-    let idx = if idx_raw < 0 { idx_raw + n as i64 } else { idx_raw };
+    let idx = if idx_raw < 0 {
+        idx_raw + n as i64
+    } else {
+        idx_raw
+    };
     if idx < 0 || idx > n as i64 {
         return Ok(None); // idx==n appends; out of range -> numpy raises
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:22514:
     }
     let n: usize = arr.getattr("shape")?.extract::<Vec<usize>>()?[0];
     let m: usize = values.getattr("shape")?.extract::<Vec<usize>>()?[0];
-    let idx = if idx_raw < 0 { idx_raw + n as i64 } else { idx_raw };
+    let idx = if idx_raw < 0 {
+        idx_raw + n as i64
+    } else {
+        idx_raw
+    };
     if idx < 0 || idx > n as i64 {
         return Ok(None); // out of range -> numpy raises IndexError
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:22528:
     let uint8 = numpy.getattr("uint8")?;
     let a_bytes = arr.call_method1("view", (&uint8,))?;
     let v_bytes = values.call_method1("view", (&uint8,))?;
-    let (Ok(a_buf), Ok(v_buf)) = (PyBuffer::<u8>::get(&a_bytes), PyBuffer::<u8>::get(&v_bytes)) else {
+    let (Ok(a_buf), Ok(v_buf)) = (PyBuffer::<u8>::get(&a_bytes), PyBuffer::<u8>::get(&v_bytes))
+    else {
         return Ok(None);
     };
     let (Some(a_in), Some(v_in)) = (a_buf.as_slice(py), v_buf.as_slice(py)) else {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:22555:
     let mb = m * itemsize;
     // SAFETY: ReadOnlyCell<u8>/Cell<u8> are repr(transparent) over u8; inputs read-only under the
     // GIL, output is a fresh numpy.empty. split_at_mut yields three disjoint output runs.
-    let a_data: &[u8] = unsafe { std::slice::from_raw_parts(a_in.as_ptr().cast::<u8>(), a_in.len()) };
-    let v_data: &[u8] = unsafe { std::slice::from_raw_parts(v_in.as_ptr().cast::<u8>(), v_in.len()) };
+    let a_data: &[u8] =
+        unsafe { std::slice::from_raw_parts(a_in.as_ptr().cast::<u8>(), a_in.len()) };
+    let v_data: &[u8] =
+        unsafe { std::slice::from_raw_parts(v_in.as_ptr().cast::<u8>(), v_in.len()) };
     let o_data: &mut [u8] =
         unsafe { std::slice::from_raw_parts_mut(output.as_ptr() as *mut u8, out_total_bytes) };
     let (head, rest) = o_data.split_at_mut(ib);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:22667:
         return Ok(None);
     };
     let n = cells.len();
-    let idx = if idx_raw < 0 { idx_raw + n as i64 } else { idx_raw };
+    let idx = if idx_raw < 0 {
+        idx_raw + n as i64
+    } else {
+        idx_raw
+    };
     if idx < 0 || idx >= n as i64 {
         return Ok(None); // numpy raises IndexError -> defer
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:22730:
     if obj_arr.getattr("ndim")?.extract::<usize>()? != 1 {
         return Ok(None);
     }
-    let okind = obj_arr.getattr("dtype")?.getattr("kind")?.extract::<String>()?;
+    let okind = obj_arr
+        .getattr("dtype")?
+        .getattr("kind")?
+        .extract::<String>()?;
     let keep = match okind.as_str() {
         "b" => {
             // bool mask: length must match; delete-where-True == keep-where-False.
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:22742:
         "i" | "u" => {
             // int index array: keep all, then mark deleted indices False (numpy fancy-assign handles
             // negatives + duplicates; an out-of-bounds index raises -> defer so numpy owns the error).
-            let keep = numpy
-                .getattr("ones")?
-                .call1((n, numpy.getattr("bool_")?))?;
+            let keep = numpy.getattr("ones")?.call1((n, numpy.getattr("bool_")?))?;
             if keep.set_item(&obj_arr, false).is_err() {
                 return Ok(None);
             }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:22849:
     }
     // SAFETY: each job writes a disjoint output range (blocks disjoint, grains within a block disjoint);
     // src is an input view that never overlaps the fresh output -> copy_nonoverlapping is sound.
-    jobs.par_iter().for_each(|&(out_off, k, in_off, len)| unsafe {
-        let dst = (out_ptr as *mut T).add(out_off);
-        let src = (src_ptrs[k] as *const T).add(in_off);
-        std::ptr::copy_nonoverlapping(src, dst, len);
-    });
+    jobs.par_iter()
+        .for_each(|&(out_off, k, in_off, len)| unsafe {
+            let dst = (out_ptr as *mut T).add(out_off);
+            let src = (src_ptrs[k] as *const T).add(in_off);
+            std::ptr::copy_nonoverlapping(src, dst, len);
+        });
 }

 const CONCAT_PARALLEL_MIN_BYTES: usize = 1 << 23; // 8MB output -> parallelize the block copy
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:22933:
         {
             let out_ptr = output.as_ptr() as usize;
             let src_ptrs: Vec<usize> = slices.iter().map(|s| s.as_ptr() as usize).collect();
-            concat_copy_blocks_parallel::<f64>(out_ptr, &src_ptrs, &shapes, ax, outer, inner, out_slab);
+            concat_copy_blocks_parallel::<f64>(
+                out_ptr, &src_ptrs, &shapes, ax, outer, inner, out_slab,
+            );
         } else {
             for o in 0..outer {
                 let mut axis_off = 0usize;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:23008:
         {
             let out_ptr = output.as_ptr() as usize;
             let src_ptrs: Vec<usize> = slices.iter().map(|s| s.as_ptr() as usize).collect();
-            concat_copy_blocks_parallel::<T>(out_ptr, &src_ptrs, &shapes, ax, outer, inner, out_slab);
+            concat_copy_blocks_parallel::<T>(
+                out_ptr, &src_ptrs, &shapes, ax, outer, inner, out_slab,
+            );
         } else {
             for o in 0..outer {
                 let mut axis_off = 0usize;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:23033:
     out_shape[ax] = out_axis;
     let restored = flat.call_method1("view", (out_dtype,))?;
     let output_shape = PyTuple::new(py, out_shape.iter().copied())?;
-    Ok(Some(restored.call_method1("reshape", (&output_shape,))?.unbind()))
+    Ok(Some(
+        restored.call_method1("reshape", (&output_shape,))?.unbind(),
+    ))
 }

 // Typed-by-itemsize concatenate for the dtypes the f64 helper leaves to the cold
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:23251:
         let items: PyResult<Vec<_>> = iter.collect();
         if let Ok(items) = items
             && !items.is_empty()
-            && items
-                .iter()
-                .all(|it| it.is_exact_instance(&ndarray_type))
+            && items.iter().all(|it| it.is_exact_instance(&ndarray_type))
         {
             let shape_of = |it: &Bound<'_, PyAny>| -> Option<Vec<usize>> {
-                it.getattr("shape").ok().and_then(|s| s.extract::<Vec<usize>>().ok())
+                it.getattr("shape")
+                    .ok()
+                    .and_then(|s| s.extract::<Vec<usize>>().ok())
             };
             let first = shape_of(&items[0]);
             if let Some(s0) = first.as_ref()
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:23267:
                 out_shape.extend_from_slice(s0);
                 let shape_tuple = PyTuple::new(py, out_shape.iter().copied())?;
                 if let Some(out) = try_zerocopy_f64_concatenate(py, &seq, 0)? {
-                    return Ok(out.bind(py).call_method1("reshape", (&shape_tuple,))?.unbind());
+                    return Ok(out
+                        .bind(py)
+                        .call_method1("reshape", (&shape_tuple,))?
+                        .unbind());
                 }
                 if let Some(out) = try_zerocopy_bytes_concatenate(py, &seq, 0)? {
-                    return Ok(out.bind(py).call_method1("reshape", (&shape_tuple,))?.unbind());
+                    return Ok(out
+                        .bind(py)
+                        .call_method1("reshape", (&shape_tuple,))?
+                        .unbind());
                 }
             }
         }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:23395:
     let trim_f = mode.contains('f');
     let trim_b = mode.contains('b');
     let (lo, hi) = match bounds {
-        Some((first, last)) => (if trim_f { first } else { 0 }, if trim_b { last } else { n }),
+        Some((first, last)) => (
+            if trim_f { first } else { 0 },
+            if trim_b { last } else { n },
+        ),
         None => (0usize, 0usize), // all-zero / empty → empty result
     };
     let slice = pyo3::types::PySlice::new(py, lo as isize, hi as isize, 1);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:23784:
         let m = shape[shape.len() - 2];
         let n = shape[shape.len() - 1];
         let owned_shape = shape.to_vec();
-        if let Ok(values) =
-            fnp_linalg::batch_pinv(array.values(), &owned_shape, rcond.as_rcond(), rtol.as_rtol())
-        {
+        if let Ok(values) = fnp_linalg::batch_pinv(
+            array.values(),
+            &owned_shape,
+            rcond.as_rcond(),
+            rtol.as_rtol(),
+        ) {
             let mut out_shape: Vec<usize> = owned_shape[..owned_shape.len() - 2].to_vec();
             out_shape.push(n);
             out_shape.push(m);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:23793:
-            let result =
-                UFuncArray::new(out_shape, values, DType::F64).map_err(map_ufunc_error)?;
+            let result = UFuncArray::new(out_shape, values, DType::F64).map_err(map_ufunc_error)?;
             return build_numpy_array_from_ufunc(py, &result);
         }
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:23897:
         let n = shape[shape.len() - 1];
         if let Some(rcond) = matrix_rank_default_rcond(array.dtype(), n) {
             let owned_shape = shape.to_vec();
-            if let Ok(ranks) =
-                fnp_linalg::batch_matrix_rank(array.values(), &owned_shape, rcond)
-            {
+            if let Ok(ranks) = fnp_linalg::batch_matrix_rank(array.values(), &owned_shape, rcond) {
                 let out_shape: Vec<usize> = owned_shape[..owned_shape.len() - 2].to_vec();
                 let vals: Vec<i64> = ranks.iter().map(|&r| r as i64).collect();
                 let result = UFuncArray::from_storage(out_shape, ArrayStorage::I64(vals))
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:24210:
             .and_then(|s| s.extract::<Vec<usize>>())
         && shape.len() == 2
         && shape[0] == shape[1]
-        && a
-            .bind(py)
+        && a.bind(py)
             .getattr("dtype")
             .and_then(|d| d.getattr("kind"))
             .and_then(|k| k.extract::<String>())
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:24236:
     // parallel batch_slogdet, reducing the two matrix axes. On any Err (singular
     // is still valid: sign=0, logabsdet=-inf; only shape errors out) fall back to
     // numpy. Mirrors the 2-D guards.
-    if shape.len() >= 3
-        && shape[shape.len() - 1] == shape[shape.len() - 2]
-        && real_f64_finite
-    {
+    if shape.len() >= 3 && shape[shape.len() - 1] == shape[shape.len() - 2] && real_f64_finite {
         let owned_shape = shape.to_vec();
         if let Ok((signs, logs)) = fnp_linalg::batch_slogdet(array.values(), &owned_shape) {
             let out_shape: Vec<usize> = owned_shape[..owned_shape.len() - 2].to_vec();
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:24246:
-            let sign_arr = UFuncArray::new(out_shape.clone(), signs, DType::F64)
-                .map_err(map_ufunc_error)?;
-            let log_arr =
-                UFuncArray::new(out_shape, logs, DType::F64).map_err(map_ufunc_error)?;
+            let sign_arr =
+                UFuncArray::new(out_shape.clone(), signs, DType::F64).map_err(map_ufunc_error)?;
+            let log_arr = UFuncArray::new(out_shape, logs, DType::F64).map_err(map_ufunc_error)?;
             return build_numpy_slogdet_result_arrays(py, &sign_arr, &log_arr);
         }
         return fallback();
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:24680:
     // [n, k]. A defensive element-count check + Err fall back to numpy for any case
     // batch_solve does not accept (non-trivial broadcast, singular, etc.). Mirrors the
     // det/inv/eigh/matrix_rank batched wirings.
-    if a_shape.len() >= 3 && a_shape[a_shape.len() - 1] == a_shape[a_shape.len() - 2] && real_f64_finite
+    if a_shape.len() >= 3
+        && a_shape[a_shape.len() - 1] == a_shape[a_shape.len() - 2]
+        && real_f64_finite
     {
         let n = a_shape[a_shape.len() - 1];
         // NumPy 2.0+ treats `b` as a single vector ONLY when b.ndim == 1; any
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:24700:
                     out_shape.push(b_shape[b_shape.len() - 1]);
                 }
                 let expected: usize = out_shape.iter().product();
-                if let Ok(values) = fnp_linalg::batch_solve(
-                    a.values(),
-                    a_shape,
-                    b.values(),
-                    b_shape,
-                    vector_rhs,
-                ) && values.len() == expected
+                if let Ok(values) =
+                    fnp_linalg::batch_solve(a.values(), a_shape, b.values(), b_shape, vector_rhs)
+                    && values.len() == expected
                     && let Ok(result) = UFuncArray::new(out_shape, values, DType::F64)
                 {
                     return build_numpy_array_from_ufunc(py, &result);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:24716:
         return fallback();
     }

-    if a_shape.len() != 2
-        || a_shape[0] != a_shape[1]
-        || !real_f64_finite
-    {
+    if a_shape.len() != 2 || a_shape[0] != a_shape[1] || !real_f64_finite {
         return fallback();
     }

Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:24844:
             .and_then(|s| s.extract::<Vec<usize>>())
         && shape.len() == 2
         && shape[0] == shape[1]
-        && a
-            .bind(py)
+        && a.bind(py)
             .getattr("dtype")
             .and_then(|d| d.getattr("kind"))
             .and_then(|k| k.extract::<String>())
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:24899:
         if let Ok(values) = fnp_linalg::batch_eigvalsh(&sym, &owned_shape) {
             let mut out_shape: Vec<usize> = owned_shape[..owned_shape.len() - 2].to_vec();
             out_shape.push(n);
-            let result =
-                UFuncArray::new(out_shape, values, DType::F64).map_err(map_ufunc_error)?;
+            let result = UFuncArray::new(out_shape, values, DType::F64).map_err(map_ufunc_error)?;
             return build_numpy_array_from_ufunc(py, &result);
         }
         return fallback();
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:24954:
             .map(|k| k == "f")
             .unwrap_or(false)
     {
-        return Ok(numpy.getattr("linalg")?.getattr("det")?.call1((bound,))?.unbind());
+        return Ok(numpy
+            .getattr("linalg")?
+            .getattr("det")?
+            .call1((bound,))?
+            .unbind());
     }
     if let Ok(array) = extract_numeric_array(py, bound, "det(a)") {
         let shape = array.shape();
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:25006:
     // (>=3-D) batch_inv below still wins (numpy loops serial per lane). Re-enable
     // native 2-D only if a future NumPy/BLAS reintroduces the getri cliff.
     if bound.is_exact_instance(&numpy.getattr("ndarray")?)
-        && let Ok(shape) = bound.getattr("shape").and_then(|s| s.extract::<Vec<usize>>())
+        && let Ok(shape) = bound
+            .getattr("shape")
+            .and_then(|s| s.extract::<Vec<usize>>())
         && shape.len() == 2
         && shape[0] == shape[1]
     {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:25197:
     kwargs.set_item("dtype", "bool")?;
     // np.zeros (calloc) / np.ones beat np.full(value) for the constant bool fill.
     let ctor = if value { "ones" } else { "zeros" };
-    Ok(Some(numpy.call_method(ctor, (shape,), Some(&kwargs))?.unbind()))
+    Ok(Some(
+        numpy.call_method(ctor, (shape,), Some(&kwargs))?.unbind(),
+    ))
 }

 // Zero-copy np.isposinf / np.isneginf for a float (f32/f64) c-contiguous ndarray.
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:25260:
         let Some(in_cells) = in_buf.as_slice(py) else {
             return Ok(None);
         };
-        let target = if positive { f64::INFINITY } else { f64::NEG_INFINITY };
+        let target = if positive {
+            f64::INFINITY
+        } else {
+            f64::NEG_INFINITY
+        };
         // SAFETY: ReadOnlyCell<f64> is repr(transparent) over f64; read-only under the GIL.
         let xin: &[f64] = unsafe { std::slice::from_raw_parts(in_cells.as_ptr().cast::<f64>(), n) };
         if parallel {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:25285:
         let Some(in_cells) = in_buf.as_slice(py) else {
             return Ok(None);
         };
-        let target = if positive { f32::INFINITY } else { f32::NEG_INFINITY };
+        let target = if positive {
+            f32::INFINITY
+        } else {
+            f32::NEG_INFINITY
+        };
         // SAFETY: ReadOnlyCell<f32> is repr(transparent) over f32; read-only under the GIL.
         let xin: &[f32] = unsafe { std::slice::from_raw_parts(in_cells.as_ptr().cast::<f32>(), n) };
         if parallel {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:25383:
             let kind: String = x.getattr("dtype")?.getattr("kind")?.extract()?;
             // int/uint canonicalize lossy; non-contiguous bails the fast path into the
             // transpose-copy extract (~55x slower). Delegate both to numpy.
-            let c_contiguous = x.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()?;
+            let c_contiguous = x
+                .getattr("flags")?
+                .getattr("c_contiguous")?
+                .extract::<bool>()?;
             // unsigned / bool can never be negative -> signbit is identically False;
             // return np.zeros(bool) in memset time. bool otherwise hit the cold f64
             // widen extract (~19ms/1M); uint went to numpy. (signed int is NOT
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:25413:
 ) -> PyResult<Py<PyAny>> {
     if kwargs.is_none_or(|kwargs| kwargs.is_empty()) && args.len() == 1 {
         let x_arg = args.get_item(0)?;
-        signbit_native(py, &x_arg)
-            .or_else(|_| core_numpy_passthrough(py, "signbit", args, kwargs))
+        signbit_native(py, &x_arg).or_else(|_| core_numpy_passthrough(py, "signbit", args, kwargs))
     } else {
         core_numpy_passthrough(py, "signbit", args, kwargs)
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:25588:
     if !x.is_exact_instance(&ndarray_type) || !numpy_dtype_is_f32(x) {
         return Ok(None);
     }
-    if !x.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()? {
+    if !x
+        .getattr("flags")?
+        .getattr("c_contiguous")?
+        .extract::<bool>()?
+    {
         return Ok(None);
     }
     let Ok(in_buf) = PyBuffer::<f32>::get(x) else {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:25927:
     // native path maps every integer to float64 (widening int8/int16), so defer all
     // non-float64 inputs to numpy for exact dtype + value parity.
     if !numpy_dtype_is_f64(py, x) {
-        return Ok(py.import("numpy")?.getattr(numpy_name)?.call1((x,))?.unbind());
+        return Ok(py
+            .import("numpy")?
+            .getattr(numpy_name)?
+            .call1((x,))?
+            .unbind());
     }
     let x = extract_precise_numeric_array(py, x, extract_label)?;
     build_numpy_scalar_or_array(py, &x.elementwise_unary(op))
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:25952:
         if let Some(out) = try_zerocopy_f16_unary_widen(py, &x_arg, UnaryOp::Degrees)? {
             return Ok(out);
         }
-        degrees_native(py, &x_arg)
-            .or_else(|_| core_numpy_passthrough(py, "degrees", args, kwargs))
+        degrees_native(py, &x_arg).or_else(|_| core_numpy_passthrough(py, "degrees", args, kwargs))
     } else {
         core_numpy_passthrough(py, "degrees", args, kwargs)
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:25978:
         if let Some(out) = try_zerocopy_f16_unary_widen(py, &x_arg, UnaryOp::Radians)? {
             return Ok(out);
         }
-        radians_native(py, &x_arg)
-            .or_else(|_| core_numpy_passthrough(py, "radians", args, kwargs))
+        radians_native(py, &x_arg).or_else(|_| core_numpy_passthrough(py, "radians", args, kwargs))
     } else {
         core_numpy_passthrough(py, "radians", args, kwargs)
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:26055:
         Ok(Some(flat.unbind()))
     } else {
         let shape_tuple = PyTuple::new(py, shape.iter().copied())?;
-        Ok(Some(flat.call_method1("reshape", (&shape_tuple,))?.unbind()))
+        Ok(Some(
+            flat.call_method1("reshape", (&shape_tuple,))?.unbind(),
+        ))
     }
 }

Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:26130:
         Ok(Some(flat.unbind()))
     } else {
         let shape_tuple = PyTuple::new(py, shape.iter().copied())?;
-        Ok(Some(flat.call_method1("reshape", (&shape_tuple,))?.unbind()))
+        Ok(Some(
+            flat.call_method1("reshape", (&shape_tuple,))?.unbind(),
+        ))
     }
 }

Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:26216:
     // incl specials) and aggregates cores. Same-shape C-contiguous f32; else fall through to numpy.
     if numpy_dtype_is_f32(x1.bind(py))
         && numpy_dtype_is_f32(x2.bind(py))
-        && let Some(out) = try_zerocopy_f32_binary(py, x1.bind(py), x2.bind(py), BinaryOp::Nextafter)?
+        && let Some(out) =
+            try_zerocopy_f32_binary(py, x1.bind(py), x2.bind(py), BinaryOp::Nextafter)?
     {
         return Ok(out);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:26356:
             .call1((x1.bind(py), x2.bind(py)))?
             .unbind());
     }
-    if let Some(out) =
-        try_zerocopy_f64_binary(py, x1.bind(py), x2.bind(py), BinaryOp::Logaddexp)?
-    {
+    if let Some(out) = try_zerocopy_f64_binary(py, x1.bind(py), x2.bind(py), BinaryOp::Logaddexp)? {
         return Ok(out);
     }
     if noncontiguous_ndarray(&numpy, x1.bind(py))? || noncontiguous_ndarray(&numpy, x2.bind(py))? {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:26404:
             .call1((x1.bind(py), x2.bind(py)))?
             .unbind());
     }
-    if let Some(out) =
-        try_zerocopy_f64_binary(py, x1.bind(py), x2.bind(py), BinaryOp::Logaddexp2)?
+    if let Some(out) = try_zerocopy_f64_binary(py, x1.bind(py), x2.bind(py), BinaryOp::Logaddexp2)?
     {
         return Ok(out);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:26431:
             && !is_arr(x2b)
             && let Ok(h) = x2b.extract::<f64>()
         {
-            let full = numpy.getattr("full")?.call((x1b.getattr("shape")?, h), Some(&kw))?;
+            let full = numpy
+                .getattr("full")?
+                .call((x1b.getattr("shape")?, h), Some(&kw))?;
             if let Some(out) = try_zerocopy_f64_binary(py, x1b, &full, BinaryOp::Logaddexp2)? {
                 return Ok(out);
             }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:26439:
             && !is_arr(x1b)
             && let Ok(h) = x1b.extract::<f64>()
         {
-            let full = numpy.getattr("full")?.call((x2b.getattr("shape")?, h), Some(&kw))?;
+            let full = numpy
+                .getattr("full")?
+                .call((x2b.getattr("shape")?, h), Some(&kw))?;
             if let Some(out) = try_zerocopy_f64_binary(py, &full, x2b, BinaryOp::Logaddexp2)? {
                 return Ok(out);
             }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:26637:
     let mantissa = mantissa.call_method1("reshape", (&output_shape,))?;
     let exponent = exponent.call_method1("reshape", (&output_shape,))?;
     Ok(Some(
-        PyTuple::new(py, [&mantissa, &exponent])?.into_any().unbind(),
+        PyTuple::new(py, [&mantissa, &exponent])?
+            .into_any()
+            .unbind(),
     ))
 }

Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:26660:
     {
         return Ok(None);
     }
-    if !x.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()? {
+    if !x
+        .getattr("flags")?
+        .getattr("c_contiguous")?
+        .extract::<bool>()?
+    {
         return Ok(None);
     }
     let shape: Vec<usize> = x.getattr("shape")?.extract()?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:26723:
         }
     }
     Ok(Some(
-        PyTuple::new(py, [&mantissa, &exponent])?.into_any().unbind(),
+        PyTuple::new(py, [&mantissa, &exponent])?
+            .into_any()
+            .unbind(),
     ))
 }

Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:26939:
         return Ok(None);
     }
     if x.getattr("ndim")?.extract::<usize>()? == 0
-        || !x.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()?
+        || !x
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
     {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:27149:
     {
         let numpy = py.import("numpy")?;
         if x.bind(py).is_exact_instance(&numpy.getattr("ndarray")?)
-            && x.bind(py).getattr("dtype")?.getattr("kind")?.extract::<String>()? == "b"
+            && x.bind(py)
+                .getattr("dtype")?
+                .getattr("kind")?
+                .extract::<String>()?
+                == "b"
         {
             let kwargs = PyDict::new(py);
             kwargs.set_item("nan", nan)?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:27370:
             // A Python real scalar (int/float, e.g. the ubiquitous default=0) promotes to f64 against
             // f64 choices and try_zerocopy_f64_select casts it via extract::<f64> — accept it so the
             // common `select(conds, f64_choices, 0)` call takes the fast path. Reject complex/other.
-            if !bound.is_none() && !numpy_dtype_is_f64(py, bound) && bound.extract::<f64>().is_err() {
+            if !bound.is_none() && !numpy_dtype_is_f64(py, bound) && bound.extract::<f64>().is_err()
+            {
                 return Ok(false);
             }
         }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:27736:
     // Sorted MIXED-field structured haystack: byte-transform value-lex record binary search (numpy ~10s @2M+2M).
     if a_kind == "V"
         && sorter.is_none()
-        && let Some(out) = try_native_searchsorted_struct_valuelex(py, &numpy, &a_arr, v.bind(py), side)?
+        && let Some(out) =
+            try_native_searchsorted_struct_valuelex(py, &numpy, &a_arr, v.bind(py), side)?
     {
         return Ok(out);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:28155:
             return Ok(None);
         };
         // SAFETY: fresh numpy.empty output, cannot alias inputs; each index written exactly once.
-        let out: &mut [i64] = unsafe { std::slice::from_raw_parts_mut(output.as_ptr() as *mut i64, m) };
+        let out: &mut [i64] =
+            unsafe { std::slice::from_raw_parts_mut(output.as_ptr() as *mut i64, m) };
         // Monotonic merge: sorted queries -> insertion index is non-decreasing, so one forward pointer
         // through the haystack answers every query in a single sequential pass. Scatter to input order.
         let mut p = 0usize;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:28177:
     }
     let shape: Vec<usize> = v.getattr("shape")?.extract()?;
     let output_shape = PyTuple::new(py, shape.iter().copied())?;
-    Ok(Some(flat.call_method1("reshape", (&output_shape,))?.unbind()))
+    Ok(Some(
+        flat.call_method1("reshape", (&output_shape,))?.unbind(),
+    ))
 }

 fn try_zerocopy_f64_searchsorted(
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:28230:
             // we own (no alias). Raw &[f64] is Sync so the search runs on worker threads.
             let a_raw: &[f64] =
                 unsafe { std::slice::from_raw_parts(a_s.as_ptr().cast::<f64>(), a_s.len()) };
-            let v_raw: &[f64] = unsafe { std::slice::from_raw_parts(v_s.as_ptr().cast::<f64>(), m) };
+            let v_raw: &[f64] =
+                unsafe { std::slice::from_raw_parts(v_s.as_ptr().cast::<f64>(), m) };
             let out_data: &mut [i64] =
                 unsafe { std::slice::from_raw_parts_mut(output.as_ptr() as *mut i64, m) };
             let chunk = m.div_ceil(rayon::current_num_threads());
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:28259:
     }
     let shape: Vec<usize> = v.getattr("shape")?.extract()?;
     let output_shape = PyTuple::new(py, shape.iter().copied())?;
-    Ok(Some(flat.call_method1("reshape", (&output_shape,))?.unbind()))
+    Ok(Some(
+        flat.call_method1("reshape", (&output_shape,))?.unbind(),
+    ))
 }

 // Typed binary-search core for np.searchsorted over a sorted haystack. For each
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:28346:
     Ok(Some(flat.call_method1("reshape", (&output_shape,))?))
 }

-fn searchsorted_int_merge_typed<
-    'py,
-    T: pyo3::buffer::Element + Copy + Ord + Send + Sync,
->(
+fn searchsorted_int_merge_typed<'py, T: pyo3::buffer::Element + Copy + Ord + Send + Sync>(
     py: Python<'py>,
     numpy: &Bound<'py, PyModule>,
     a: &Bound<'py, PyAny>,
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:28399:
             return Ok(None);
         };
         // SAFETY: fresh numpy.empty output, cannot alias inputs; each original-index slot is written once.
-        let out: &mut [i64] = unsafe { std::slice::from_raw_parts_mut(output.as_ptr() as *mut i64, m) };
+        let out: &mut [i64] =
+            unsafe { std::slice::from_raw_parts_mut(output.as_ptr() as *mut i64, m) };
         let mut p = 0usize;
         if right {
             for &(q, idx) in &pairs {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:28583:
             return Ok(None);
         };
         // SAFETY: fresh numpy.empty output, cannot alias inputs; each index written exactly once.
-        let out: &mut [i64] = unsafe { std::slice::from_raw_parts_mut(output.as_ptr() as *mut i64, m) };
+        let out: &mut [i64] =
+            unsafe { std::slice::from_raw_parts_mut(output.as_ptr() as *mut i64, m) };
         let mut p = 0usize;
         if right {
             for &(q, idx) in &pairs {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:28603:
     }
     let shape: Vec<usize> = v.getattr("shape")?.extract()?;
     let output_shape = PyTuple::new(py, shape.iter().copied())?;
-    Ok(Some(flat.call_method1("reshape", (&output_shape,))?.unbind()))
+    Ok(Some(
+        flat.call_method1("reshape", (&output_shape,))?.unbind(),
+    ))
 }

 fn try_zerocopy_f32_searchsorted(
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:28786:
         let kwargs = PyDict::new(py);
         kwargs.set_item("dtype", "int64")?;
         let counts = numpy.call_method("zeros", (nbins,), Some(&kwargs))?;
-        return Ok(Some(
-            PyTuple::new(py, [counts, edges])?.into_any().unbind(),
-        ));
+        return Ok(Some(PyTuple::new(py, [counts, edges])?.into_any().unbind()));
     }
     let Some(s) = buf.as_slice(py) else {
         let histogram_fn = numpy.getattr("histogram")?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:28829:
             .par_iter()
             .map(|&raw| {
                 let v = to_f64(raw);
-                (
-                    v,
-                    v,
-                    value_supported(raw),
-                    !check_finite || v.is_finite(),
-                )
+                (v, v, value_supported(raw), !check_finite || v.is_finite())
             })
             .reduce(
                 || (f64::INFINITY, f64::NEG_INFINITY, true, true),
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:28935:
         for (slot, value) in cs.iter().zip(local_counts) {
             slot.set(value);
         }
-        return Ok(Some(
-            PyTuple::new(py, [counts, edges])?.into_any().unbind(),
-        ));
+        return Ok(Some(PyTuple::new(py, [counts, edges])?.into_any().unbind()));
     }

     let mut mn = to_f64(s[0].get());
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:29013:
             slot.set(slot.get() + 1);
         }
     }
-    Ok(Some(
-        PyTuple::new(py, [counts, edges])?.into_any().unbind(),
-    ))
+    Ok(Some(PyTuple::new(py, [counts, edges])?.into_any().unbind()))
 }

 fn histogram_f32(
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:29045:
         let kwargs = PyDict::new(py);
         kwargs.set_item("dtype", "int64")?;
         let counts = numpy.call_method("zeros", (nbins,), Some(&kwargs))?;
-        return Ok(Some(
-            PyTuple::new(py, [counts, edges])?.into_any().unbind(),
-        ));
+        return Ok(Some(PyTuple::new(py, [counts, edges])?.into_any().unbind()));
     }
     let Some(s) = buf.as_slice(py) else {
         let histogram_fn = numpy.getattr("histogram")?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:29169:
             }
         }
     }
-    Ok(Some(
-        PyTuple::new(py, [counts, edges])?.into_any().unbind(),
-    ))
+    Ok(Some(PyTuple::new(py, [counts, edges])?.into_any().unbind()))
 }

 // O(n) uniform-bin np.histogram for exact 1-D f64/f32/integer ndarrays with
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:29196:
     match (kind.as_str(), itemsize) {
         ("f", 4) => histogram_f32(py, &numpy, a, nbins),
         ("f", 8) => histogram_typed::<f64>(py, &numpy, a, nbins, |x| x, true, |_| true),
-        ("i", 1) => {
-            histogram_typed::<i8>(py, &numpy, a, nbins, |x| x as f64, false, |_| true)
+        ("i", 1) => histogram_typed::<i8>(py, &numpy, a, nbins, |x| x as f64, false, |_| true),
+        ("i", 2) => histogram_typed::<i16>(py, &numpy, a, nbins, |x| x as f64, false, |_| true),
+        ("i", 4) => histogram_typed::<i32>(py, &numpy, a, nbins, |x| x as f64, false, |_| true),
+        ("i", 8) => {
+            histogram_typed::<i64>(py, &numpy, a, nbins, |x| x as f64, false, i64_is_f64_exact)
         }
-        ("i", 2) => {
-            histogram_typed::<i16>(py, &numpy, a, nbins, |x| x as f64, false, |_| true)
+        ("u", 1) => histogram_typed::<u8>(py, &numpy, a, nbins, |x| x as f64, false, |_| true),
+        ("u", 2) => histogram_typed::<u16>(py, &numpy, a, nbins, |x| x as f64, false, |_| true),
+        ("u", 4) => histogram_typed::<u32>(py, &numpy, a, nbins, |x| x as f64, false, |_| true),
+        ("u", 8) => {
+            histogram_typed::<u64>(py, &numpy, a, nbins, |x| x as f64, false, u64_is_f64_exact)
         }
-        ("i", 4) => {
-            histogram_typed::<i32>(py, &numpy, a, nbins, |x| x as f64, false, |_| true)
-        }
-        ("i", 8) => histogram_typed::<i64>(
-            py,
-            &numpy,
-            a,
-            nbins,
-            |x| x as f64,
-            false,
-            i64_is_f64_exact,
-        ),
-        ("u", 1) => {
-            histogram_typed::<u8>(py, &numpy, a, nbins, |x| x as f64, false, |_| true)
-        }
-        ("u", 2) => {
-            histogram_typed::<u16>(py, &numpy, a, nbins, |x| x as f64, false, |_| true)
-        }
-        ("u", 4) => {
-            histogram_typed::<u32>(py, &numpy, a, nbins, |x| x as f64, false, |_| true)
-        }
-        ("u", 8) => histogram_typed::<u64>(
-            py,
-            &numpy,
-            a,
-            nbins,
-            |x| x as f64,
-            false,
-            u64_is_f64_exact,
-        ),
         _ => Ok(None),
     }
 }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:29333:
             let (b0, bn) = bnd(data);
             o[0] = b0;
             o[l - 1] = bn;
-            o[1..l - 1].par_iter_mut().enumerate().for_each(|(j, slot)| {
-                let i = j + 1;
-                *slot = (data[i + 1] - data[i - 1]) / (2.0 * dx);
-            });
+            o[1..l - 1]
+                .par_iter_mut()
+                .enumerate()
+                .for_each(|(j, slot)| {
+                    let i = j + 1;
+                    *slot = (data[i + 1] - data[i - 1]) / (2.0 * dx);
+                });
         } else if parallel {
             // N-D last axis: each contiguous row is independent -> parallelize over rows.
             o.par_chunks_mut(l)
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:29418:
             return Ok(None);
         };
         // SAFETY: freshly allocated numpy.empty output, cannot alias f/x; written once.
-        let o: &mut [f64] = unsafe { std::slice::from_raw_parts_mut(ocells.as_ptr() as *mut f64, n) };
+        let o: &mut [f64] =
+            unsafe { std::slice::from_raw_parts_mut(ocells.as_ptr() as *mut f64, n) };
         o[0] = (fd[1] - fd[0]) / (xd[1] - xd[0]);
         o[n - 1] = (fd[n - 1] - fd[n - 2]) / (xd[n - 1] - xd[n - 2]);
         let interior = |i: usize| -> f64 {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:29514:
             return Ok(None);
         };
         // SAFETY: fresh numpy.empty outputs, cannot alias f/cy/cx; each element written once.
-        let o0: &mut [f64] = unsafe { std::slice::from_raw_parts_mut(g0c.as_ptr() as *mut f64, total) };
-        let o1: &mut [f64] = unsafe { std::slice::from_raw_parts_mut(g1c.as_ptr() as *mut f64, total) };
+        let o0: &mut [f64] =
+            unsafe { std::slice::from_raw_parts_mut(g0c.as_ptr() as *mut f64, total) };
+        let o1: &mut [f64] =
+            unsafe { std::slice::from_raw_parts_mut(g1c.as_ptr() as *mut f64, total) };
         use rayon::prelude::*;
         // g1: last-axis (columns) per-row 1-D coord stencil with cx.
         let row_x = |orow: &mut [f64], frow: &[f64]| {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:29662:
             return Ok(None);
         };
         // SAFETY: fresh numpy.empty output, cannot alias f/coord; every element written once.
-        let o: &mut [f64] = unsafe { std::slice::from_raw_parts_mut(ocells.as_ptr() as *mut f64, total) };
+        let o: &mut [f64] =
+            unsafe { std::slice::from_raw_parts_mut(ocells.as_ptr() as *mut f64, total) };
         use rayon::prelude::*;
         // One output "plane" = the inner slab at (outer o, axis position i). Plane index p = o*la + i.
         let fill = |p: usize, plane: &mut [f64]| {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:29676:
             } else if i == la - 1 {
                 let d = cd[la - 1] - cd[la - 2];
                 for j in 0..inner {
-                    plane[j] = (fd[o_blk + (la - 1) * inner + j] - fd[o_blk + (la - 2) * inner + j]) / d;
+                    plane[j] =
+                        (fd[o_blk + (la - 1) * inner + j] - fd[o_blk + (la - 2) * inner + j]) / d;
                 }
             } else {
                 let hd = cd[i] - cd[i - 1];
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:29684:
                 let a = -(hs) / (hd * (hd + hs));
                 let b = (hs - hd) / (hd * hs);
                 let c = hd / (hs * (hd + hs));
-                let (rm, r0, rp) = (o_blk + (i - 1) * inner, o_blk + i * inner, o_blk + (i + 1) * inner);
+                let (rm, r0, rp) = (
+                    o_blk + (i - 1) * inner,
+                    o_blk + i * inner,
+                    o_blk + (i + 1) * inner,
+                );
                 for j in 0..inner {
                     plane[j] = a * fd[rm + j] + b * fd[r0 + j] + c * fd[rp + j];
                 }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:29803:
             // 1-D: parallelize the interior (a single row has no row-level parallelism).
             o[0] = (data[1] - data[0]) / dxf;
             o[l - 1] = (data[l - 1] - data[l - 2]) / dxf;
-            o[1..l - 1].par_iter_mut().enumerate().for_each(|(j, slot)| {
-                let i = j + 1;
-                *slot = (data[i + 1] - data[i - 1]) / div;
-            });
+            o[1..l - 1]
+                .par_iter_mut()
+                .enumerate()
+                .for_each(|(j, slot)| {
+                    let i = j + 1;
+                    *slot = (data[i + 1] - data[i - 1]) / div;
+                });
         } else if parallel {
             o.par_chunks_mut(l)
                 .zip(data.par_chunks(l))
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:30090:
     // with no axis on N-D it returns a list. So take native only when the target axis is
     // the last one: axis=None requires ndim==1; axis=k requires k to normalize to ndim-1.
     let axis_is_last = uniform_dx.is_some()
-        && match f.bind(py).getattr("ndim").and_then(|n| n.extract::<usize>()) {
+        && match f
+            .bind(py)
+            .getattr("ndim")
+            .and_then(|n| n.extract::<usize>())
+        {
             Ok(ndim) if ndim >= 1 => match axis.as_ref() {
                 None => ndim == 1,
                 Some(a) => a
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:30147:
     // non-f64/non-contiguous) aborts to the numpy passthrough so the result matches exactly.
     if axis.as_ref().is_none_or(|v| v.bind(py).is_none())
         && let Some(dx) = uniform_dx
-        && let Ok(ndim) = f.bind(py).getattr("ndim").and_then(|n| n.extract::<usize>())
+        && let Ok(ndim) = f
+            .bind(py)
+            .getattr("ndim")
+            .and_then(|n| n.extract::<usize>())
         && ndim >= 2
     {
         let mut grads: Vec<Py<PyAny>> = Vec::with_capacity(ndim);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:30219:
     if varargs.len() == 1
         && axis.as_ref().is_none_or(|v| {
             let ab = v.bind(py);
-            ab.is_none() || ab.extract::<isize>().map(|k| k == 0 || k == -1).unwrap_or(false)
+            ab.is_none()
+                || ab
+                    .extract::<isize>()
+                    .map(|k| k == 0 || k == -1)
+                    .unwrap_or(false)
         })
         && f.bind(py)
             .getattr("ndim")
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:30228:
             .unwrap_or(false)
     {
         let x = varargs.get_item(0)?;
-        if let Some(out) = try_zerocopy_f64_gradient_1d_coords(py, &numpy, f.bind(py), &x, edge_order)?
+        if let Some(out) =
+            try_zerocopy_f64_gradient_1d_coords(py, &numpy, f.bind(py), &x, edge_order)?
         {
             return Ok(out);
         }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:30242:
     if varargs.len() == 1
         && let Some(ax_obj) = axis.as_ref()
         && let Ok(ax_raw) = ax_obj.bind(py).extract::<isize>()
-        && let Ok(ndim) = f.bind(py).getattr("ndim").and_then(|nd| nd.extract::<usize>())
+        && let Ok(ndim) = f
+            .bind(py)
+            .getattr("ndim")
+            .and_then(|nd| nd.extract::<usize>())
         && ndim >= 2
     {
-        let norm = if ax_raw < 0 { ax_raw + ndim as isize } else { ax_raw };
+        let norm = if ax_raw < 0 {
+            ax_raw + ndim as isize
+        } else {
+            ax_raw
+        };
         if norm >= 0 && (norm as usize) < ndim {
             let coord = varargs.get_item(0)?;
             if let Some(out) = try_zerocopy_f64_gradient_axis_coords(
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:30359:
                 }
                 if all_ok {
                     let td_dtype = numpy.call_method1("dtype", (td_str,))?;
-                    return Ok(current.bind(py).call_method1("view", (&td_dtype,))?.unbind());
+                    return Ok(current
+                        .bind(py)
+                        .call_method1("view", (&td_dtype,))?
+                        .unbind());
                 }
                 return fallback();
             }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:30416:
     // pass); the extract path below bridges bool->f64 and runs ~700x slower (85ms vs
     // 122us @4M). numpy is the parity reference, so defer bool to it. (Checked only on
     // the fallthrough — f64/int/f32 already returned.)
-    if a
-        .bind(py)
-        .is_exact_instance(&numpy.getattr("ndarray")?)
-        && a.bind(py).getattr("dtype")?.getattr("kind")?.extract::<String>()? == "b"
+    if a.bind(py).is_exact_instance(&numpy.getattr("ndarray")?)
+        && a.bind(py)
+            .getattr("dtype")?
+            .getattr("kind")?
+            .extract::<String>()?
+            == "b"
     {
         return fallback();
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:30840:
     // view-semantics divergence). Delegate to numpy.rot90 for the exact view,
     // dtype, and error surface.
     let numpy = py.import("numpy")?;
-    Ok(numpy.getattr("rot90")?.call1((m.bind(py), k, axes))?.unbind())
+    Ok(numpy
+        .getattr("rot90")?
+        .call1((m.bind(py), k, axes))?
+        .unbind())
 }

 #[pyfunction]
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:31336:
     // Normalize indices to a contiguous int64 array; only integer index dtypes
     // are accepted (float indices raise in numpy — defer).
     let ind_arr = numpy.call_method1("asarray", (ind,))?;
-    let ind_kind = ind_arr.getattr("dtype")?.getattr("kind")?.extract::<String>()?;
+    let ind_kind = ind_arr
+        .getattr("dtype")?
+        .getattr("kind")?
+        .extract::<String>()?;
     if ind_kind != "i" && ind_kind != "u" {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:31492:
     if !dst.is_exact_instance(&ndarray_type) || !numpy_dtype_is_f64(py, dst) {
         return Ok(false);
     }
-    if !dst.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()? {
+    if !dst
+        .getattr("flags")?
+        .getattr("c_contiguous")?
+        .extract::<bool>()?
+    {
         return Ok(false);
     }
     if !where_obj.is_exact_instance(&ndarray_type)
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:31499:
-        || where_obj.getattr("dtype")?.getattr("kind")?.extract::<String>()? != "b"
+        || where_obj
+            .getattr("dtype")?
+            .getattr("kind")?
+            .extract::<String>()?
+            != "b"
         || !where_obj
             .getattr("flags")?
             .getattr("c_contiguous")?
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:31547:
     // array src: same-shape f64 C-contiguous, not overlapping dst (identity buffer is a safe i==i no-op).
     if !src.is_exact_instance(&ndarray_type)
         || !numpy_dtype_is_f64(py, src)
-        || !src.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()?
+        || !src
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
     {
         return Ok(false);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:31757:
 // Generic fill of np.indices' k-th coordinate slab: output[f] = (f / inner) % dim_k,
 // written as contiguous blocks of `inner` equal values cycling 0..dim_k (a memset-like
 // block fill that vectorizes).
-fn fill_indices_typed<'py, T: pyo3::buffer::Element + Copy + Send + Sync, F: Fn(usize) -> T + Sync>(
+fn fill_indices_typed<
+    'py,
+    T: pyo3::buffer::Element + Copy + Send + Sync,
+    F: Fn(usize) -> T + Sync,
+>(
     py: Python<'py>,
     out: &Bound<'py, PyAny>,
     dims: &[usize],
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:31858:
                 np_dt,
             )
         }
-        _ => ("i".to_string(), 8, numpy.getattr("dtype")?.call1(("int64",))?),
+        _ => (
+            "i".to_string(),
+            8,
+            numpy.getattr("dtype")?.call1(("int64",))?,
+        ),
     };
     if kind != "i" && kind != "u" {
         return Ok(None);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:32536:
             Err(_) => return Ok(None),
         },
     };
-    let Ok(shape) = value.getattr("shape").and_then(|s| s.extract::<Vec<usize>>()) else {
+    let Ok(shape) = value
+        .getattr("shape")
+        .and_then(|s| s.extract::<Vec<usize>>())
+    else {
         return Ok(None);
     };
     let total: usize = shape.iter().product();
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:32581:
         }
     }
     let shape_tuple = PyTuple::new(py, shape)?;
-    Ok(Some(flat.call_method1("reshape", (&shape_tuple,))?.unbind()))
+    Ok(Some(
+        flat.call_method1("reshape", (&shape_tuple,))?.unbind(),
+    ))
 }

 // Generic-dtype counterpart of try_zerocopy_ma_filled_f64: the f64 path is f64-only, so int/uint/
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:32619:
             }
         }
     };
-    let Ok(shape) = value.getattr("shape").and_then(|s| s.extract::<Vec<usize>>()) else {
+    let Ok(shape) = value
+        .getattr("shape")
+        .and_then(|s| s.extract::<Vec<usize>>())
+    else {
         return Ok(None);
     };
     let total: usize = shape.iter().product();
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:32661:
         }
     }
     let shape_tuple = PyTuple::new(py, shape)?;
-    Ok(Some(flat.call_method1("reshape", (&shape_tuple,))?.unbind()))
+    Ok(Some(
+        flat.call_method1("reshape", (&shape_tuple,))?.unbind(),
+    ))
 }

 #[pyfunction]
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:32686:
     // (out[i] = mask[i] ? fill : data[i]). Pure element selection, so bit-identical to the
     // generic path. Non-f64 data, non-contiguous, structured/non-float fill, or any buffer
     // failure fall through to the generic extraction.
-    if let Some(out) = try_zerocopy_ma_filled_f64(py, a.bind(py), fill_value.as_ref().map(|v| v.bind(py)))? {
+    if let Some(out) =
+        try_zerocopy_ma_filled_f64(py, a.bind(py), fill_value.as_ref().map(|v| v.bind(py)))?
+    {
         return Ok(out);
     }

Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:32884:
     let (mn, mx) = data
         .par_iter()
         .copied()
-        .map(|v| { let i: i128 = v.into(); (i, i) })
-        .reduce(|| { let i: i128 = data[0].into(); (i, i) }, |(a0, a1), (b0, b1)| (a0.min(b0), a1.max(b1)));
+        .map(|v| {
+            let i: i128 = v.into();
+            (i, i)
+        })
+        .reduce(
+            || {
+                let i: i128 = data[0].into();
+                (i, i)
+            },
+            |(a0, a1), (b0, b1)| (a0.min(b0), a1.max(b1)),
+        );
     let span = mx - mn;
     if span < 0 || span > RANGE_MAX as i128 {
         return Ok(None);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:32955:
     let (mn, mx) = data
         .par_iter()
         .copied()
-        .map(|v| { let i: i128 = v.into(); (i, i) })
-        .reduce(|| { let i: i128 = data[0].into(); (i, i) }, |(a0, a1), (b0, b1)| (a0.min(b0), a1.max(b1)));
+        .map(|v| {
+            let i: i128 = v.into();
+            (i, i)
+        })
+        .reduce(
+            || {
+                let i: i128 = data[0].into();
+                (i, i)
+            },
+            |(a0, a1), (b0, b1)| (a0.min(b0), a1.max(b1)),
+        );
     let span = mx - mn;
     if span < 0 || span > RANGE_MAX as i128 {
         return Ok(None);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:32990:
     let lo_rank = pos.floor() as u64;
     let hi_rank = pos.ceil() as u64;
     let lo = val(lo_rank) as f64;
-    let hi = if hi_rank == lo_rank { lo } else { val(hi_rank) as f64 };
+    let hi = if hi_rank == lo_rank {
+        lo
+    } else {
+        val(hi_rank) as f64
+    };
     let out = lo + (hi - lo) * (pos - lo_rank as f64);
     Ok(Some(numpy.getattr("float64")?.call1((out,))?.unbind()))
 }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:33007:
         return Ok(None);
     }
     let flat = a.call_method0("ravel")?;
-    if !flat.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()? {
+    if !flat
+        .getattr("flags")?
+        .getattr("c_contiguous")?
+        .extract::<bool>()?
+    {
         return Ok(None);
     }
     let n = flat.len()?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:33041:
         return Ok(None);
     }
     let flat = a.call_method0("ravel")?;
-    if !flat.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()? {
+    if !flat
+        .getattr("flags")?
+        .getattr("c_contiguous")?
+        .extract::<bool>()?
+    {
         return Ok(None);
     }
     let n = flat.len()?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:33856:
     // two-1-D-operand stay on the fast Gram below).
     if !rowvar
         && y_binding.as_ref().is_none_or(|y_val| y_val.is_none())
-        && m_bound.getattr("ndim").ok().and_then(|n| n.extract::<usize>().ok()) == Some(2)
+        && m_bound
+            .getattr("ndim")
+            .ok()
+            .and_then(|n| n.extract::<usize>().ok())
+            == Some(2)
     {
         return fallback(py);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:33997:
     // it). numpy.corrcoef is faster, so delegate for parity (rowvar=True / two-1-D-operand stay fast).
     if !rowvar
         && y_binding.as_ref().is_none_or(|y_val| y_val.is_none())
-        && x_bound.getattr("ndim").ok().and_then(|n| n.extract::<usize>().ok()) == Some(2)
+        && x_bound
+            .getattr("ndim")
+            .ok()
+            .and_then(|n| n.extract::<usize>().ok())
+            == Some(2)
     {
         return fallback(py);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:34140:
         if (a_is_ma || a_b.is_exact_instance(&ndarray_type))
             && (b_is_ma || b_b.is_exact_instance(&ndarray_type))
         {
-            let a_data = if a_is_ma { a_b.getattr("data")? } else { a_b.clone() };
-            let b_data = if b_is_ma { b_b.getattr("data")? } else { b_b.clone() };
+            let a_data = if a_is_ma {
+                a_b.getattr("data")?
+            } else {
+                a_b.clone()
+            };
+            let b_data = if b_is_ma {
+                b_b.getattr("data")?
+            } else {
+                b_b.clone()
+            };
             if numpy_dtype_is_f64(py, &a_data) && numpy_dtype_is_f64(py, &b_data) {
                 let ashape = a_b.getattr("shape").and_then(|s| s.extract::<Vec<usize>>());
                 let bshape = b_b.getattr("shape").and_then(|s| s.extract::<Vec<usize>>());
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:34168:
                     } else {
                         None
                     };
-                    let a_mask_buf = a_mask_view.as_ref().and_then(|v| PyBuffer::<u8>::get(v).ok());
-                    let b_mask_buf = b_mask_view.as_ref().and_then(|v| PyBuffer::<u8>::get(v).ok());
+                    let a_mask_buf = a_mask_view
+                        .as_ref()
+                        .and_then(|v| PyBuffer::<u8>::get(v).ok());
+                    let b_mask_buf = b_mask_view
+                        .as_ref()
+                        .and_then(|v| PyBuffer::<u8>::get(v).ok());
                     let a_mask = a_mask_buf.as_ref().and_then(|buf| buf.as_slice(py));
                     let b_mask = b_mask_buf.as_ref().and_then(|buf| buf.as_slice(py));
                     let n = ad.len();
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:34205:
                             for i in 0..n {
                                 let masked = a_mask.is_some_and(|s| s[i].get() != 0)
                                     || b_mask.is_some_and(|s| s[i].get() != 0);
-                                let res = if masked { fill_value } else { ad[i].get() == bd[i].get() };
+                                let res = if masked {
+                                    fill_value
+                                } else {
+                                    ad[i].get() == bd[i].get()
+                                };
                                 if !res {
                                     equal = false;
                                     break;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:34377:
                 let mut accs = vec![0.0f64; inner];
                 let base = o * axis_len * inner;
                 for axis_index in 0..axis_len {
-                    let slab = &in_raw[base + axis_index * inner..base + axis_index * inner + inner];
+                    let slab =
+                        &in_raw[base + axis_index * inner..base + axis_index * inner + inner];
                     for (acc, &v) in accs.iter_mut().zip(slab.iter()) {
                         if !v.is_nan() {
                             *acc += v;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:34493:
     // Native parallel f16 nanmean along a NON-LAST axis (per-lane sequential f16-narrow nansum + count).
     if let Some(ax) = axis.as_ref().filter(|v| !v.bind(py).is_none())
         && let Ok(ax_i) = ax.bind(py).extract::<isize>()
-        && let Some(out) = try_zerocopy_f16_nanmean_nonlast_axis(py, a.bind(py), Some(ax_i), keepdims)?
+        && let Some(out) =
+            try_zerocopy_f16_nanmean_nonlast_axis(py, a.bind(py), Some(ax_i), keepdims)?
     {
         return Ok(out);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:34535:
     // materializes a NaN->0 copy + mask + two reduces. See try_zerocopy_f64_nanmean_axis0.
     if let Some(axis_val) = axis.as_ref()
         && !axis_val.bind(py).is_none()
-        && let Some(out) = try_zerocopy_f64_nanmean_axis0(py, a.bind(py), axis_val.bind(py), keepdims)?
+        && let Some(out) =
+            try_zerocopy_f64_nanmean_axis0(py, a.bind(py), axis_val.bind(py), keepdims)?
     {
         return Ok(out);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:34563:
     }
     // Multi-axis (tuple) reductions extract the whole array then fall back to numpy
     // on the axis re-parse below (wasteful 8x bool-bridge / f64 copy). Delegate up front.
-    if axis.as_ref().is_some_and(|ax| ax.bind(py).cast::<PyTuple>().is_ok()) {
+    if axis
+        .as_ref()
+        .is_some_and(|ax| ax.bind(py).cast::<PyTuple>().is_ok())
+    {
         return fallback();
     }
     let a = match extract_numeric_array(py, a.bind(py), "nanmean(a)") {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:34780:
 // Bit-exact flat mean (axis=None) = numpy's pairwise sum / n. numpy's unweighted
 // np.average and np.mean both reduce to umr_sum (pairwise) / n, so this matches
 // both. Empty input defers (numpy raises / warns). C-contiguous f64 only.
-fn try_zerocopy_f64_mean_flat(
-    py: Python<'_>,
-    a: &Bound<'_, PyAny>,
-) -> PyResult<Option<Py<PyAny>>> {
+fn try_zerocopy_f64_mean_flat(py: Python<'_>, a: &Bound<'_, PyAny>) -> PyResult<Option<Py<PyAny>>> {
     let numpy = py.import("numpy")?;
     let Some(in_buffer) = f64_contiguous_cells(py, a, &numpy)? else {
         return Ok(None);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:34983:
     }
     let mut n2 = n / 2;
     n2 -= n2 % 8;
-    pairwise_nansum_f16_widen(data, off, n2, buf) + pairwise_nansum_f16_widen(data, off + n2, n - n2, buf)
+    pairwise_nansum_f16_widen(data, off, n2, buf)
+        + pairwise_nansum_f16_widen(data, off + n2, n - n2, buf)
 }

 // Pairwise sum of the SQUARED DEVIATIONS (x-avg)^2 for the last-axis f16 var-family. numpy computes the
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:35036:
 // is single-threaded compute-bound (~50ms@16M, ~2x the plain sum from the per-element NaN test). Parallel
 // bit-exact nan-skip widen-pairwise (see helpers). Returns Ok(None) for non-f16 / non-contiguous / below
 // the gate. Unlike sum, NEVER defers on NaN (nansum defines NaN as 0).
-fn try_zerocopy_f16_nansum_flat(py: Python<'_>, x: &Bound<'_, PyAny>) -> PyResult<Option<Py<PyAny>>> {
+fn try_zerocopy_f16_nansum_flat(
+    py: Python<'_>,
+    x: &Bound<'_, PyAny>,
+) -> PyResult<Option<Py<PyAny>>> {
     // Much lower gate than the plain sum (1<<22): numpy's f16 nansum is ~6x slower per element than its
     // f16 sum (it builds an isnan mask + a where'd copy + sums = temps), ~9ms/M even at small N, so the
     // parallel path (no NaN pre-scan needed) wins from ~130K up. Measured crossover ~1<<17.
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:35052:
     {
         return Ok(None);
     }
-    if !x.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()? {
+    if !x
+        .getattr("flags")?
+        .getattr("c_contiguous")?
+        .extract::<bool>()?
+    {
         return Ok(None);
     }
     let shape: Vec<usize> = x.getattr("shape")?.extract()?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:35086:
 // (np.sum's dtype), re-widened, then divided (verified byte-exact incl its inf-overflow when the f16 sum
 // overflows). So: parallel nan-skip pairwise sum -> narrow to f16 -> re-widen -> divide by the non-NaN
 // count -> narrow. count==0 (all NaN) defers to numpy for its "Mean of empty slice" warning + NaN.
-fn try_zerocopy_f16_nanmean_flat(py: Python<'_>, x: &Bound<'_, PyAny>) -> PyResult<Option<Py<PyAny>>> {
+fn try_zerocopy_f16_nanmean_flat(
+    py: Python<'_>,
+    x: &Bound<'_, PyAny>,
+) -> PyResult<Option<Py<PyAny>>> {
     // Higher gate than nansum (1<<17): nanmean adds a full non-NaN count pass + the narrow/re-widen, so
     // its fixed overhead is larger; measured crossover ~1<<19 (262K is 1.04x, 1M is 0.58x win).
     const F16_NANMEAN_PARALLEL_MIN: usize = 1 << 19;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:35101:
     {
         return Ok(None);
     }
-    if !x.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()? {
+    if !x
+        .getattr("flags")?
+        .getattr("c_contiguous")?
+        .extract::<bool>()?
+    {
         return Ok(None);
     }
     let shape: Vec<usize> = x.getattr("shape")?.extract()?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:35170:
     {
         return Ok(None);
     }
-    if !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()? {
+    if !a
+        .getattr("flags")?
+        .getattr("c_contiguous")?
+        .extract::<bool>()?
+    {
         return Ok(None);
     }
     let shape: Vec<usize> = a.getattr("shape")?.extract()?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:35207:
     }
     let data: &[u16] = unsafe { std::slice::from_raw_parts(cells.as_ptr().cast::<u16>(), total) };
     use rayon::prelude::*;
-    if !nan_skip && data.par_iter().any(|&b| (b & 0x7c00) == 0x7c00 && (b & 0x03ff) != 0) {
+    if !nan_skip
+        && data
+            .par_iter()
+            .any(|&b| (b & 0x7c00) == 0x7c00 && (b & 0x03ff) != 0)
+    {
         return Ok(None); // plain sum defers on NaN (numpy propagates a fold-order-dependent NaN)
     }
     let mut out = vec![0u16; outer];
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:35258:
     {
         return Ok(None);
     }
-    if !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()? {
+    if !a
+        .getattr("flags")?
+        .getattr("c_contiguous")?
+        .extract::<bool>()?
+    {
         return Ok(None);
     }
     let shape: Vec<usize> = a.getattr("shape")?.extract()?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:35355:
     {
         return Ok(None);
     }
-    if !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()? {
+    if !a
+        .getattr("flags")?
+        .getattr("c_contiguous")?
+        .extract::<bool>()?
+    {
         return Ok(None);
     }
     let shape: Vec<usize> = a.getattr("shape")?.extract()?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:35452:
     {
         return Ok(None);
     }
-    if !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()? {
+    if !a
+        .getattr("flags")?
+        .getattr("c_contiguous")?
+        .extract::<bool>()?
+    {
         return Ok(None);
     }
     let shape: Vec<usize> = a.getattr("shape")?.extract()?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:35560:
     {
         return Ok(None);
     }
-    if !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()? {
+    if !a
+        .getattr("flags")?
+        .getattr("c_contiguous")?
+        .extract::<bool>()?
+    {
         return Ok(None);
     }
     let shape: Vec<usize> = a.getattr("shape")?.extract()?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:35689:
     {
         return Ok(None);
     }
-    if !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()? {
+    if !a
+        .getattr("flags")?
+        .getattr("c_contiguous")?
+        .extract::<bool>()?
+    {
         return Ok(None);
     }
     let shape: Vec<usize> = a.getattr("shape")?.extract()?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:35733:
         let base = r * cols;
         let mut buf = [0.0f32; 128];
         let cnt = if nan_skip {
-            data[base..base + cols].iter().filter(|&&b| !is_nan(b)).count()
+            data[base..base + cols]
+                .iter()
+                .filter(|&&b| !is_nan(b))
+                .count()
         } else {
             cols
         };
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:35799:
     {
         return Ok(None);
     }
-    if !x.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()? {
+    if !x
+        .getattr("flags")?
+        .getattr("c_contiguous")?
+        .extract::<bool>()?
+    {
         return Ok(None);
     }
     let shape: Vec<usize> = x.getattr("shape")?.extract()?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:35854:
     {
         return Ok(None);
     }
-    if !x.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()? {
+    if !x
+        .getattr("flags")?
+        .getattr("c_contiguous")?
+        .extract::<bool>()?
+    {
         return Ok(None);
     }
     let shape: Vec<usize> = x.getattr("shape")?.extract()?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:36106:
     // ABOVE the f64 guard. Single int axis; non-last/small/non-f16 defer inside (keepdims handled there).
     if let Some(ax) = axis.as_ref().filter(|v| !v.bind(py).is_none())
         && let Ok(ax_i) = ax.bind(py).extract::<isize>()
-        && let Some(out) = try_zerocopy_f16_sum_lastaxis(py, a.bind(py), Some(ax_i), keepdims, true)?
+        && let Some(out) =
+            try_zerocopy_f16_sum_lastaxis(py, a.bind(py), Some(ax_i), keepdims, true)?
     {
         return Ok(out);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:36114:
     // the outer*inner lanes). numpy's strided f16 nansum is slow (mask/where + narrow-each-step).
     if let Some(ax) = axis.as_ref().filter(|v| !v.bind(py).is_none())
         && let Ok(ax_i) = ax.bind(py).extract::<isize>()
-        && let Some(out) = try_zerocopy_f16_sum_nonlast_axis(py, a.bind(py), Some(ax_i), keepdims, true)?
+        && let Some(out) =
+            try_zerocopy_f16_sum_nonlast_axis(py, a.bind(py), Some(ax_i), keepdims, true)?
     {
         return Ok(out);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:36181:
     }
     // Multi-axis (tuple) reductions extract the whole array then fall back to numpy
     // on the axis re-parse below (wasteful 8x bool-bridge / f64 copy). Delegate up front.
-    if axis.as_ref().is_some_and(|ax| ax.bind(py).cast::<PyTuple>().is_ok()) {
+    if axis
+        .as_ref()
+        .is_some_and(|ax| ax.bind(py).cast::<PyTuple>().is_ok())
+    {
         return fallback();
     }
     let a = match extract_numeric_array(py, a.bind(py), "nansum(a)") {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:36269:
             Some(v) => v.bind(py).extract::<isize>().ok(),
         };
         if let Some(k) = ax_isize
-            && let Some(out) = try_zerocopy_complex_nanprod_lastaxis(
-                py,
-                a.bind(py),
-                k,
-                keepdims.unwrap_or(false),
-            )?
+            && let Some(out) =
+                try_zerocopy_complex_nanprod_lastaxis(py, a.bind(py), k, keepdims.unwrap_or(false))?
         {
             return Ok(out);
         }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:36335:
     }
     // Multi-axis (tuple) reductions extract the whole array then fall back to numpy
     // on the axis re-parse below (wasteful 8x bool-bridge / f64 copy). Delegate up front.
-    if axis.as_ref().is_some_and(|ax| ax.bind(py).cast::<PyTuple>().is_ok()) {
+    if axis
+        .as_ref()
+        .is_some_and(|ax| ax.bind(py).cast::<PyTuple>().is_ok())
+    {
         return fallback();
     }
     let a = match extract_numeric_array(py, a.bind(py), "nanprod(a)") {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:36467:
             }
             acc
         };
-        let parallel =
-            outer >= 2 && outer * axis_len * inner >= (1 << 16) && rayon::current_num_threads() >= 2;
+        let parallel = outer >= 2
+            && outer * axis_len * inner >= (1 << 16)
+            && rayon::current_num_threads() >= 2;
         let mut out = vec![0.0f64; outer * inner];
         if parallel {
             let planes: Vec<Vec<f64>> = (0..outer).into_par_iter().map(group_prod).collect();
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:36486:
     let mut out_shape = shape.clone();
     out_shape.remove(ax);
     let flat = numpy_array_from_slice(py, &numpy, &out, "float64")?;
-    let reshaped =
-        flat.call_method1("reshape", (PyTuple::new(py, out_shape.iter().copied())?,))?;
+    let reshaped = flat.call_method1("reshape", (PyTuple::new(py, out_shape.iter().copied())?,))?;
     if out_shape.is_empty() {
         return Ok(Some(reshaped.get_item(())?.unbind()));
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:36528:
     while off < full {
         let c = V::from_slice(&data[off..off + L]);
         vsaw |= c.simd_eq(c); // non-NaN lanes (c == c)
-        acc = if take_max { acc.simd_max(c) } else { acc.simd_min(c) };
+        acc = if take_max {
+            acc.simd_max(c)
+        } else {
+            acc.simd_min(c)
+        };
         off += L;
     }
     let mut m = init;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:36551:
 // loop). The caller detects the all-NaN / empty case via `m == init` (defer),
 // which it must do anyway for the ±inf-extreme tie — so no accuracy is lost.
 fn simd_nanextreme_value(data: &[f64], take_max: bool) -> f64 {
-    use std::simd::num::SimdFloat;
     use std::simd::Simd;
+    use std::simd::num::SimdFloat;
     const L: usize = 8;
     type V = Simd<f64, L>;
     let init = if take_max {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:36566:
     let mut off = 0;
     while off < full {
         let c = V::from_slice(&data[off..off + L]);
-        acc = if take_max { acc.simd_max(c) } else { acc.simd_min(c) };
+        acc = if take_max {
+            acc.simd_max(c)
+        } else {
+            acc.simd_min(c)
+        };
         off += L;
     }
     let mut m = init;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:36586:
 // scalar f64::max is NaN-aware and does NOT autovectorize, leaving the down-axis
 // reduction 1.5-3x slower than numpy (which copy-replaces NaN then runs raw vmaxpd).
 fn fold_row_extreme_simd(acc: &mut [f64], row: &[f64], take_max: bool) {
-    use std::simd::num::SimdFloat;
     use std::simd::Simd;
+    use std::simd::num::SimdFloat;
     const L: usize = 8;
     type V = Simd<f64, L>;
     let n = acc.len();
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:36596:
     while i < full {
         let a = V::from_slice(&acc[i..i + L]);
         let v = V::from_slice(&row[i..i + L]);
-        let m = if take_max { a.simd_max(v) } else { a.simd_min(v) };
+        let m = if take_max {
+            a.simd_max(v)
+        } else {
+            a.simd_min(v)
+        };
         m.copy_to_slice(&mut acc[i..i + L]);
         i += L;
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:36603:
     for k in full..n {
-        acc[k] = if take_max { acc[k].max(row[k]) } else { acc[k].min(row[k]) };
+        acc[k] = if take_max {
+            acc[k].max(row[k])
+        } else {
+            acc[k].min(row[k])
+        };
     }
 }

Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:36827:
         };
         let mut out = vec![init; outer * inner];
         if parallel && outer >= 2 {
-            let planes: Vec<Vec<f64>> =
-                (0..outer).into_par_iter().map(|o| row_fold(o * lane)).collect();
+            let planes: Vec<Vec<f64>> = (0..outer)
+                .into_par_iter()
+                .map(|o| row_fold(o * lane))
+                .collect();
             for (o, acc) in planes.into_iter().enumerate() {
                 out[o * inner..o * inner + inner].copy_from_slice(&acc);
             }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:37017:
         };
         let mut out = vec![init; outer * inner];
         if parallel && outer >= 2 {
-            let planes: Vec<Vec<f32>> =
-                (0..outer).into_par_iter().map(|o| row_fold(o * lane)).collect();
+            let planes: Vec<Vec<f32>> = (0..outer)
+                .into_par_iter()
+                .map(|o| row_fold(o * lane))
+                .collect();
             for (o, acc) in planes.into_iter().enumerate() {
                 out[o * inner..o * inner + inner].copy_from_slice(&acc);
             }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:37308:
     use rayon::prelude::*;
     use std::sync::atomic::{AtomicBool, Ordering};
     const NANMEAN_AXIS_PARALLEL_MIN: usize = 98_304;
-    let parallel = outer * axis_len * inner >= NANMEAN_AXIS_PARALLEL_MIN
-        && rayon::current_num_threads() >= 2;
+    let parallel =
+        outer * axis_len * inner >= NANMEAN_AXIS_PARALLEL_MIN && rayon::current_num_threads() >= 2;
     let mut out = vec![0.0f64; outer * inner];
     let any_empty = AtomicBool::new(false);
     if parallel {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:38028:
     // Same crossover as nanvar_axis: per-lane two-pass fold is cheap, so rayon fan-out
     // only pays off above ~1e5 total elements.
     const VAR_AXIS_PARALLEL_MIN: usize = 98_304;
-    let parallel =
-        outer * axis_len >= VAR_AXIS_PARALLEL_MIN && rayon::current_num_threads() >= 2;
+    let parallel = outer * axis_len >= VAR_AXIS_PARALLEL_MIN && rayon::current_num_threads() >= 2;
     let results: Vec<Option<f64>> = if parallel {
         data.par_chunks_exact(axis_len).map(lane_var).collect()
     } else {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:38240:
     };
     use rayon::prelude::*;
     const VAR_AXIS_PARALLEL_MIN: usize = 98_304;
-    let parallel = outer * axis_len * inner >= VAR_AXIS_PARALLEL_MIN
-        && rayon::current_num_threads() >= 2;
+    let parallel =
+        outer * axis_len * inner >= VAR_AXIS_PARALLEL_MIN && rayon::current_num_threads() >= 2;
     let mut out = vec![0.0f64; outer * inner];
     if parallel {
         out.par_chunks_mut(inner).enumerate().for_each(|(o, dst)| {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:38587:
     use rayon::prelude::*;
     use std::sync::atomic::{AtomicBool, Ordering};
     const VAR_AXIS_PARALLEL_MIN: usize = 98_304;
-    let parallel = outer * axis_len * inner >= VAR_AXIS_PARALLEL_MIN
-        && rayon::current_num_threads() >= 2;
+    let parallel =
+        outer * axis_len * inner >= VAR_AXIS_PARALLEL_MIN && rayon::current_num_threads() >= 2;
     let mut out = vec![0.0f64; outer * inner];
     let defer = AtomicBool::new(false);
     if parallel {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:38796:
             acc = a;
         }
     }
-    if saw_nan {
-        f64::NAN
-    } else {
-        acc
-    }
+    if saw_nan { f64::NAN } else { acc }
 }

 // np.linalg.norm(x, ord in {None, 1, 2}, axis=<int last axis>) - a vector L1 or L2
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:39209:
         if outer >= 2 {
             let block_elems = alen * inner;
             if parallel {
-                out_vec.par_chunks_mut(inner).enumerate().for_each(|(o, dst)| {
-                    reduce_rows(&data[o * block_elems..(o + 1) * block_elems], dst, alen);
-                });
+                out_vec
+                    .par_chunks_mut(inner)
+                    .enumerate()
+                    .for_each(|(o, dst)| {
+                        reduce_rows(&data[o * block_elems..(o + 1) * block_elems], dst, alen);
+                    });
             } else {
                 for (o, dst) in out_vec.chunks_mut(inner).enumerate() {
                     reduce_rows(&data[o * block_elems..(o + 1) * block_elems], dst, alen);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:39308:
     };
     use rayon::prelude::*;
     const NORM_AXIS_PARALLEL_MIN: usize = 98_304;
-    let parallel =
-        outer * block >= NORM_AXIS_PARALLEL_MIN && rayon::current_num_threads() >= 2;
+    let parallel = outer * block >= NORM_AXIS_PARALLEL_MIN && rayon::current_num_threads() >= 2;
     let out: Vec<f64> = if parallel {
         data.par_chunks_exact(block).map(lane_norm).collect()
     } else {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:39354:
             acc = s;
         }
     }
-    if saw_nan {
-        f64::NAN
-    } else {
-        acc
-    }
+    if saw_nan { f64::NAN } else { acc }
 }

 // np.linalg.norm(x, ord in {1,-1,+inf,-inf}, axis=(<-2>,<-1>)) - an induced matrix
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:39443:
     };
     use rayon::prelude::*;
     const NORM_AXIS_PARALLEL_MIN: usize = 98_304;
-    let parallel =
-        outer * block >= NORM_AXIS_PARALLEL_MIN && rayon::current_num_threads() >= 2;
+    let parallel = outer * block >= NORM_AXIS_PARALLEL_MIN && rayon::current_num_threads() >= 2;
     let out: Vec<f64> = if parallel {
         data.par_chunks_exact(block).map(&block_norm).collect()
     } else {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:39525:
     // temp when NaN is present, ~13-20x slower). MUST sit ABOVE the dtype guard below (else dead).
     if let Some(axis_val) = axis.as_ref()
         && !axis_val.bind(py).is_none()
-        && let Some(out) = try_zerocopy_f32_nanextreme_axis(py, a.bind(py), axis_val.bind(py), true)?
+        && let Some(out) =
+            try_zerocopy_f32_nanextreme_axis(py, a.bind(py), axis_val.bind(py), true)?
     {
         if keepdims.unwrap_or(false) {
             let ndim = a.bind(py).getattr("ndim")?.extract::<usize>()?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:39554:
         if keepdims.unwrap_or(false) {
             let ndim = a.bind(py).getattr("ndim")?.extract::<usize>()?;
             let arr = numpy.call_method1("asarray", (out.bind(py),))?;
-            let reshaped =
-                arr.call_method1("reshape", (PyTuple::new(py, vec![1usize; ndim])?,))?;
+            let reshaped = arr.call_method1("reshape", (PyTuple::new(py, vec![1usize; ndim])?,))?;
             return Ok(reshaped.unbind());
         }
         return Ok(out);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:39564:
     // than the extract → native scan; all-NaN/±0 lanes defer to numpy).
     if let Some(axis_val) = axis.as_ref()
         && !axis_val.bind(py).is_none()
-        && let Some(out) = try_zerocopy_f64_nanextreme_axis(py, a.bind(py), axis_val.bind(py), true)?
+        && let Some(out) =
+            try_zerocopy_f64_nanextreme_axis(py, a.bind(py), axis_val.bind(py), true)?
     {
         if keepdims.unwrap_or(false) {
             let ndim = a.bind(py).getattr("ndim")?.extract::<usize>()?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:39588:
     }
     // Multi-axis (tuple) reductions extract the whole array then fall back to numpy
     // on the axis re-parse below (wasteful 8x bool-bridge / f64 copy). Delegate up front.
-    if axis.as_ref().is_some_and(|ax| ax.bind(py).cast::<PyTuple>().is_ok()) {
+    if axis
+        .as_ref()
+        .is_some_and(|ax| ax.bind(py).cast::<PyTuple>().is_ok())
+    {
         return fallback();
     }
     let a = match extract_numeric_array(py, a.bind(py), "nanmax(a)") {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:39671:
     // f32 per-axis nanmin: no f32 nanextreme-axis kernel -> delegated. ABOVE the dtype guard below.
     if let Some(axis_val) = axis.as_ref()
         && !axis_val.bind(py).is_none()
-        && let Some(out) = try_zerocopy_f32_nanextreme_axis(py, a.bind(py), axis_val.bind(py), false)?
+        && let Some(out) =
+            try_zerocopy_f32_nanextreme_axis(py, a.bind(py), axis_val.bind(py), false)?
     {
         if keepdims.unwrap_or(false) {
             let ndim = a.bind(py).getattr("ndim")?.extract::<usize>()?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:39700:
         if keepdims.unwrap_or(false) {
             let ndim = a.bind(py).getattr("ndim")?.extract::<usize>()?;
             let arr = numpy.call_method1("asarray", (out.bind(py),))?;
-            let reshaped =
-                arr.call_method1("reshape", (PyTuple::new(py, vec![1usize; ndim])?,))?;
+            let reshaped = arr.call_method1("reshape", (PyTuple::new(py, vec![1usize; ndim])?,))?;
             return Ok(reshaped.unbind());
         }
         return Ok(out);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:39710:
     // than the extract → native scan; all-NaN/±0 lanes defer to numpy).
     if let Some(axis_val) = axis.as_ref()
         && !axis_val.bind(py).is_none()
-        && let Some(out) = try_zerocopy_f64_nanextreme_axis(py, a.bind(py), axis_val.bind(py), false)?
+        && let Some(out) =
+            try_zerocopy_f64_nanextreme_axis(py, a.bind(py), axis_val.bind(py), false)?
     {
         if keepdims.unwrap_or(false) {
             let ndim = a.bind(py).getattr("ndim")?.extract::<usize>()?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:39734:
     }
     // Multi-axis (tuple) reductions extract the whole array then fall back to numpy
     // on the axis re-parse below (wasteful 8x bool-bridge / f64 copy). Delegate up front.
-    if axis.as_ref().is_some_and(|ax| ax.bind(py).cast::<PyTuple>().is_ok()) {
+    if axis
+        .as_ref()
+        .is_some_and(|ax| ax.bind(py).cast::<PyTuple>().is_ok())
+    {
         return fallback();
     }
     let a = match extract_numeric_array(py, a.bind(py), "nanmin(a)") {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:39978:
     }
     // Multi-axis (tuple) reductions extract the whole array then fall back to numpy
     // on the axis re-parse below (wasteful 8x bool-bridge / f64 copy). Delegate up front.
-    if axis.as_ref().is_some_and(|ax| ax.bind(py).cast::<PyTuple>().is_ok()) {
+    if axis
+        .as_ref()
+        .is_some_and(|ax| ax.bind(py).cast::<PyTuple>().is_ok())
+    {
         return fallback();
     }
     let a = match extract_numeric_array(py, a.bind(py), "nanstd(a)") {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:40236:
     }
     // Multi-axis (tuple) reductions extract the whole array then fall back to numpy
     // on the axis re-parse below (wasteful 8x bool-bridge / f64 copy). Delegate up front.
-    if axis.as_ref().is_some_and(|ax| ax.bind(py).cast::<PyTuple>().is_ok()) {
+    if axis
+        .as_ref()
+        .is_some_and(|ax| ax.bind(py).cast::<PyTuple>().is_ok())
+    {
         return fallback();
     }
     let a = match extract_numeric_array(py, a.bind(py), "nanvar(a)") {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:40283:
     if !a.is_exact_instance(&numpy.getattr("ndarray")?) || !numpy_dtype_is_f64(py, a) {
         return Ok(None);
     }
-    if !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()? {
+    if !a
+        .getattr("flags")?
+        .getattr("c_contiguous")?
+        .extract::<bool>()?
+    {
         return Ok(None);
     }
     let Ok(buffer) = PyBuffer::<f64>::get(a) else {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:40362:
     if !a.is_exact_instance(&numpy.getattr("ndarray")?) || !numpy_dtype_is_f32(a) {
         return Ok(None);
     }
-    if !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()? {
+    if !a
+        .getattr("flags")?
+        .getattr("c_contiguous")?
+        .extract::<bool>()?
+    {
         return Ok(None);
     }
     let Ok(buffer) = PyBuffer::<f32>::get(a) else {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:40447:
     if !a.is_exact_instance(&numpy.getattr("ndarray")?) || !numpy_dtype_is_f32(a) {
         return Ok(None);
     }
-    if !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()? {
+    if !a
+        .getattr("flags")?
+        .getattr("c_contiguous")?
+        .extract::<bool>()?
+    {
         return Ok(None);
     }
     let shape: Vec<usize> = a.getattr("shape")?.extract()?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:40474:
         return Ok(None);
     }
     // SAFETY: ReadOnlyCell<f32> is repr(transparent) over f32; read-only under the GIL.
-    let data: &[f32] = unsafe { std::slice::from_raw_parts(cells.as_ptr().cast::<f32>(), cells.len()) };
+    let data: &[f32] =
+        unsafe { std::slice::from_raw_parts(cells.as_ptr().cast::<f32>(), cells.len()) };
     let per_lane = |l: &[f32]| -> Option<usize> {
         let mut best: Option<(usize, f32)> = None;
         for (i, &v) in l.iter().enumerate() {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:40530:
     if !a.is_exact_instance(&numpy.getattr("ndarray")?) || !numpy_dtype_is_f64(py, a) {
         return Ok(None);
     }
-    if !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()? {
+    if !a
+        .getattr("flags")?
+        .getattr("c_contiguous")?
+        .extract::<bool>()?
+    {
         return Ok(None);
     }
     let shape: Vec<usize> = a.getattr("shape")?.extract()?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:41369:
         let dt = arr.getattr("dtype")?;
         let kind = dt.getattr("kind")?.extract::<String>()?;
         let itemsize = dt.getattr("itemsize")?.extract::<usize>()?;
-        let is_int_like = matches!(kind.as_str(), "i" | "u" | "b") && !(kind == "u" && itemsize >= 8);
+        let is_int_like =
+            matches!(kind.as_str(), "i" | "u" | "b") && !(kind == "u" && itemsize >= 8);
         let is_integral_float = kind == "f" && matches!(itemsize, 4 | 8);
         if !is_int_like && !is_integral_float {
             return Ok(None);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:41529:
             return Ok(None);
         };
         // SAFETY: fresh numpy.empty output, cannot alias inputs; each slot written exactly once.
-        let out: &mut [i64] = unsafe { std::slice::from_raw_parts_mut(output.as_ptr() as *mut i64, n) };
+        let out: &mut [i64] =
+            unsafe { std::slice::from_raw_parts_mut(output.as_ptr() as *mut i64, n) };
         out.par_iter_mut()
             .zip(pairs.par_iter())
             .for_each(|(o, &(_, idx))| *o = idx as i64);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:41587:
             return Ok(None);
         }
         if item.getattr("ndim")?.extract::<usize>()? != 1
-            || !item.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()?
+            || !item
+                .getattr("flags")?
+                .getattr("c_contiguous")?
+                .extract::<bool>()?
         {
             return Ok(None);
         }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:41609:
     if has_float {
         for (i, item) in items.iter().enumerate() {
             if kinds_widths[i].0 == b'f' {
-                if numpy.call_method1("isnan", (item,))?.call_method0("any")?.extract::<bool>()? {
+                if numpy
+                    .call_method1("isnan", (item,))?
+                    .call_method0("any")?
+                    .extract::<bool>()?
+                {
                     return Ok(None);
                 }
                 let sb = numpy.call_method1("signbit", (item,))?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:41616:
                 let eq0 = numpy.call_method1("equal", (item, 0.0_f64))?;
-                if numpy.call_method1("logical_and", (sb, eq0))?.call_method0("any")?.extract::<bool>()? {
+                if numpy
+                    .call_method1("logical_and", (sb, eq0))?
+                    .call_method0("any")?
+                    .extract::<bool>()?
+                {
                     return Ok(None);
                 }
             }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:41643:
             return Ok(None);
         }
         // SAFETY: ReadOnlyCell<u8> repr(transparent); read-only under the GIL; buffer outlives the slice.
-        key_data.push(unsafe { std::slice::from_raw_parts(cells.as_ptr().cast::<u8>(), cells.len()) });
+        key_data
+            .push(unsafe { std::slice::from_raw_parts(cells.as_ptr().cast::<u8>(), cells.len()) });
     }
     // Record layout: PRIMARY-FIRST (numpy's last key is primary) -> iterate keys in reverse, accumulate offset.
     let total_width: usize = kinds_widths.iter().map(|(_, w)| *w).sum();
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:41655:
         off += w;
     }
     let mut records: Vec<u8> = vec![0; n * total_width];
-    records.par_chunks_exact_mut(total_width).enumerate().for_each(|(i, rec)| {
-        for &(kj, kb, w, roff) in &layout {
-            transform_field(&key_data[kj][i * w..(i + 1) * w], &mut rec[roff..roff + w], kb, w);
-        }
-    });
+    records
+        .par_chunks_exact_mut(total_width)
+        .enumerate()
+        .for_each(|(i, rec)| {
+            for &(kj, kb, w, roff) in &layout {
+                transform_field(
+                    &key_data[kj][i * w..(i + 1) * w],
+                    &mut rec[roff..roff + w],
+                    kb,
+                    w,
+                );
+            }
+        });
     let reck = |r: u32| -> &[u8] {
         let b = r as usize * total_width;
         &records[b..b + total_width]
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:41675:
         return Ok(None);
     }
     // SAFETY: fresh numpy.empty intp output, no alias.
-    let o: &mut [i64] = unsafe { std::slice::from_raw_parts_mut(out_cells.as_ptr() as *mut i64, n) };
-    o.par_iter_mut().zip(perm.par_iter()).for_each(|(dst, &p)| *dst = p as i64);
+    let o: &mut [i64] =
+        unsafe { std::slice::from_raw_parts_mut(out_cells.as_ptr() as *mut i64, n) };
+    o.par_iter_mut()
+        .zip(perm.par_iter())
+        .for_each(|(dst, &p)| *dst = p as i64);
     Ok(Some(out.unbind()))
 }

Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:42304:
     if !matches!(
         parsed,
         Some(
-            DType::I8
-                | DType::U8
-                | DType::I16
-                | DType::U16
-                | DType::I32
-                | DType::U32
-                | DType::I64
+            DType::I8 | DType::U8 | DType::I16 | DType::U16 | DType::I32 | DType::U32 | DType::I64
         )
     ) {
         return Ok(None);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:42444:
             return Ok(None);
         };
         // SAFETY: ReadOnlyCell<i64> repr(transparent); read-only under the GIL.
-        let data: &[i64] = unsafe { std::slice::from_raw_parts(cells.as_ptr().cast::<i64>(), cells.len()) };
+        let data: &[i64] =
+            unsafe { std::slice::from_raw_parts(cells.as_ptr().cast::<i64>(), cells.len()) };
         if data.par_iter().any(|&v| v == i64::MIN) {
             return Ok(None);
         }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:42525:
     // datetime64/timedelta64 intersect via the int64 view -> fast int64 intersect (~2.0s numpy delegate).
     if !assume_unique
         && !return_indices
-        && let Some(r) = try_native_datetime_setop(py, ar1.bind(py), ar2.bind(py), DtSetOp::Intersect)?
+        && let Some(r) =
+            try_native_datetime_setop(py, ar1.bind(py), ar2.bind(py), DtSetOp::Intersect)?
     {
         return Ok(r);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:42532:
     // float16 intersect via exact f32 widening (numpy f16 set-ops ~172ms, no SIMD).
     if !assume_unique
         && !return_indices
-        && let Some(r) = try_native_f16_setop(py, &numpy, ar1.bind(py), ar2.bind(py), DtSetOp::Intersect)?
+        && let Some(r) =
+            try_native_f16_setop(py, &numpy, ar1.bind(py), ar2.bind(py), DtSetOp::Intersect)?
     {
         return Ok(r);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:42680:
         return Ok(r);
     }
     // float16 setdiff via exact f32 widening (numpy f16 set-ops ~172ms, no SIMD).
-    if let Some(r) = try_native_f16_setop(py, &numpy, ar1.bind(py), ar2.bind(py), DtSetOp::Setdiff)? {
+    if let Some(r) = try_native_f16_setop(py, &numpy, ar1.bind(py), ar2.bind(py), DtSetOp::Setdiff)?
+    {
         return Ok(r);
     }
     if setop_inputs_are_float(py, ar1.bind(py), ar2.bind(py))? {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:42755:
         return Ok(r);
     }
     // float16 setxor via exact f32 widening (numpy f16 set-ops ~172ms, no SIMD).
-    if let Some(r) = try_native_f16_setop(py, &numpy, ar1.bind(py), ar2.bind(py), DtSetOp::Setxor)? {
+    if let Some(r) = try_native_f16_setop(py, &numpy, ar1.bind(py), ar2.bind(py), DtSetOp::Setxor)?
+    {
         return Ok(r);
     }
     // complex128 symmetric difference on a dense integer-valued grid (numpy delegates c128 to a serial sort).
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:42831:
     }
     // Fast hashed-set membership for matched integer dtypes — runs BEFORE the cold
     // extract→UFuncArray path (~25x slower) so common id-membership skips it.
-    if let Some(out) = try_zerocopy_int_isin(py, element.bind(py), test_elements.bind(py), invert)? {
+    if let Some(out) = try_zerocopy_int_isin(py, element.bind(py), test_elements.bind(py), invert)?
+    {
         return Ok(out);
     }
     // Fast zero-copy parallel hashed-set membership for matched real-float dtypes —
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:42838:
     // runs BEFORE the extract→UFuncArray path (which copies both operands). numpy's
     // float isin sorts the concatenation serially (~2.7 s for 16M f64); this is O(n+m).
-    if let Some(out) = try_zerocopy_float_isin(py, element.bind(py), test_elements.bind(py), invert)?
+    if let Some(out) =
+        try_zerocopy_float_isin(py, element.bind(py), test_elements.bind(py), invert)?
     {
         return Ok(out);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:42844:
     // Fixed-width unicode ('U') membership via a hashed record-byte set (equality is byte-equality
     // for ANY codepoint, so no Latin-1 gate is needed). numpy's string isin sorts |elem|+|test| (~730ms
     // @2M). O(n+m).
-    if let Some(out) = try_native_string_isin(py, element.bind(py), test_elements.bind(py), invert)? {
+    if let Some(out) = try_native_string_isin(py, element.bind(py), test_elements.bind(py), invert)?
+    {
         return Ok(out);
     }
     // complex128 membership via a hashed 16-byte-pattern set (byte-equality == value-equality for finite
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:42851:
     // non-(-0.0) values; NaN and -0.0 defer). numpy's complex isin sorts |elem|+|test| (~529ms @2M).
-    if let Some(out) = try_zerocopy_c128_isin(py, element.bind(py), test_elements.bind(py), invert)? {
+    if let Some(out) = try_zerocopy_c128_isin(py, element.bind(py), test_elements.bind(py), invert)?
+    {
         return Ok(out);
     }
-    if let Some(out) = try_zerocopy_c64_isin(py, element.bind(py), test_elements.bind(py), invert)? {
+    if let Some(out) = try_zerocopy_c64_isin(py, element.bind(py), test_elements.bind(py), invert)?
+    {
         return Ok(out);
     }
     // Structured/record membership via a hashed record-byte set (equality is byte-equality for integer-kind
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:42859:
     // fields with no padding). numpy's structured isin falls back to a serial O((n+m)log) sort (~4 s @1M+500k).
-    if let Some(out) = try_native_struct_isin(py, element.bind(py), test_elements.bind(py), invert)? {
+    if let Some(out) = try_native_struct_isin(py, element.bind(py), test_elements.bind(py), invert)?
+    {
         return Ok(out);
     }
     // datetime64/timedelta64 membership via the int64 view -> fast int isin (numpy delegates, ~341ms); NaT defer.
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:42864:
-    if let Some(out) = try_native_datetime_isin(py, &numpy, element.bind(py), test_elements.bind(py), invert)? {
+    if let Some(out) =
+        try_native_datetime_isin(py, &numpy, element.bind(py), test_elements.bind(py), invert)?
+    {
         return Ok(out);
     }
     // float16 membership: widen exact to f32, route to the fast f32 hashed isin (numpy f16 ~168ms).
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:42868:
-    if let Some(out) = try_native_f16_isin(py, &numpy, element.bind(py), test_elements.bind(py), invert)? {
+    if let Some(out) =
+        try_native_f16_isin(py, &numpy, element.bind(py), test_elements.bind(py), invert)?
+    {
         return Ok(out);
     }
     let ar1 = match extract_precise_numeric_array(py, element.bind(py), "isin(element)") {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:42926:
         return Ok(None);
     }
     if element.getattr("ndim")?.extract::<usize>()? != 1
-        || !element.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()?
+        || !element
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
         || test.getattr("ndim")?.extract::<usize>()? != 1
-        || !test.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()?
+        || !test
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
     {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:42935:
-    let n: usize = element.getattr("shape")?.extract::<Vec<usize>>()?.iter().product();
-    let m: usize = test.getattr("shape")?.extract::<Vec<usize>>()?.iter().product();
+    let n: usize = element
+        .getattr("shape")?
+        .extract::<Vec<usize>>()?
+        .iter()
+        .product();
+    let m: usize = test
+        .getattr("shape")?
+        .extract::<Vec<usize>>()?
+        .iter()
+        .product();
     if n < ISIN_MIN || rayon::current_num_threads() < 2 {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:42951:
     }
     use rayon::prelude::*;
     // SAFETY: ReadOnlyCell<u8> repr(transparent) over u8; read-only under the GIL.
-    let e_data: &[u8] = unsafe { std::slice::from_raw_parts(e_cells.as_ptr().cast::<u8>(), n * itemsize) };
-    let t_data: &[u8] = unsafe { std::slice::from_raw_parts(t_cells.as_ptr().cast::<u8>(), m * itemsize) };
+    let e_data: &[u8] =
+        unsafe { std::slice::from_raw_parts(e_cells.as_ptr().cast::<u8>(), n * itemsize) };
+    let t_data: &[u8] =
+        unsafe { std::slice::from_raw_parts(t_cells.as_ptr().cast::<u8>(), m * itemsize) };
     // Exact byte-record set of the test elements (FNV byte hash; membership verified on lookup).
     let mut set: std::collections::HashSet<&[u8], FastIntBuildHasher> =
         std::collections::HashSet::with_capacity_and_hasher(m, FastIntBuildHasher);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:43034:
     for name in &float_fields {
         for arr in [element, test] {
             let f = arr.get_item(name.as_str())?;
-            if numpy.call_method1("isnan", (&f,))?.call_method0("any")?.extract::<bool>()? {
+            if numpy
+                .call_method1("isnan", (&f,))?
+                .call_method0("any")?
+                .extract::<bool>()?
+            {
                 return Ok(None);
             }
             let sb = numpy.call_method1("signbit", (&f,))?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:43049:
         }
     }
     if element.getattr("ndim")?.extract::<usize>()? != 1
-        || !element.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()?
+        || !element
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
         || test.getattr("ndim")?.extract::<usize>()? != 1
-        || !test.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()?
+        || !test
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
     {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:43058:
-    let n: usize = element.getattr("shape")?.extract::<Vec<usize>>()?.iter().product();
-    let m: usize = test.getattr("shape")?.extract::<Vec<usize>>()?.iter().product();
+    let n: usize = element
+        .getattr("shape")?
+        .extract::<Vec<usize>>()?
+        .iter()
+        .product();
+    let m: usize = test
+        .getattr("shape")?
+        .extract::<Vec<usize>>()?
+        .iter()
+        .product();
     if n < ISIN_MIN || rayon::current_num_threads() < 2 {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:43074:
     }
     use rayon::prelude::*;
     // SAFETY: ReadOnlyCell<u8> repr(transparent) over u8; read-only under the GIL.
-    let e_data: &[u8] = unsafe { std::slice::from_raw_parts(e_cells.as_ptr().cast::<u8>(), n * itemsize) };
-    let t_data: &[u8] = unsafe { std::slice::from_raw_parts(t_cells.as_ptr().cast::<u8>(), m * itemsize) };
+    let e_data: &[u8] =
+        unsafe { std::slice::from_raw_parts(e_cells.as_ptr().cast::<u8>(), n * itemsize) };
+    let t_data: &[u8] =
+        unsafe { std::slice::from_raw_parts(t_cells.as_ptr().cast::<u8>(), m * itemsize) };
     let mut set: std::collections::HashSet<&[u8], FastIntBuildHasher> =
         std::collections::HashSet::with_capacity_and_hasher(m, FastIntBuildHasher);
     for k in 0..m {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:43130:
         return Ok(None);
     }
     // SAFETY: ReadOnlyCell<u8> repr(transparent); read-only under the GIL.
-    let base_data: &[u8] = unsafe { std::slice::from_raw_parts(base_cells.as_ptr().cast::<u8>(), nu * itemsize) };
+    let base_data: &[u8] =
+        unsafe { std::slice::from_raw_parts(base_cells.as_ptr().cast::<u8>(), nu * itemsize) };
     let mut set: std::collections::HashSet<&[u8], FastIntBuildHasher> =
         std::collections::HashSet::with_capacity_and_hasher(m, FastIntBuildHasher);
     for k in 0..m {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:43144:
             return Ok(None);
         };
         // SAFETY: fresh numpy.empty bool mask (1 byte/elem), no alias with base/set.
-        let mo: &mut [u8] = unsafe { std::slice::from_raw_parts_mut(mask_cells.as_ptr() as *mut u8, nu) };
+        let mo: &mut [u8] =
+            unsafe { std::slice::from_raw_parts_mut(mask_cells.as_ptr() as *mut u8, nu) };
         mo.par_iter_mut().enumerate().for_each(|(k, slot)| {
             let rec = &base_data[k * itemsize..(k + 1) * itemsize];
             *slot = (set.contains(rec) == keep_in_set) as u8;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:43154:
 }

 // Gate helper: both are same-dtype structured ndarrays. Returns Some(itemsize) if so.
-fn struct_setop_gate(a: &Bound<'_, PyAny>, b: &Bound<'_, PyAny>, nd: &Bound<'_, PyAny>) -> PyResult<Option<usize>> {
+fn struct_setop_gate(
+    a: &Bound<'_, PyAny>,
+    b: &Bound<'_, PyAny>,
+    nd: &Bound<'_, PyAny>,
+) -> PyResult<Option<usize>> {
     if !a.is_exact_instance(nd) || !b.is_exact_instance(nd) {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:43180:

 // True if every float field of a structured array is finite and non-(-0.0) — required for the record-byte hash
 // (membership) to be byte-exact. All-integer records trivially pass (no float fields).
-fn struct_all_floats_finite(py: Python<'_>, numpy: &Bound<'_, PyModule>, arr: &Bound<'_, PyAny>) -> PyResult<bool> {
+fn struct_all_floats_finite(
+    py: Python<'_>,
+    numpy: &Bound<'_, PyModule>,
+    arr: &Bound<'_, PyAny>,
+) -> PyResult<bool> {
     let dtype = arr.getattr("dtype")?;
     let names_obj = dtype.getattr("names")?;
     if names_obj.is_none() {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:43192:
         let ftype = fields.get_item(name.as_str())?.get_item(0)?;
         if ftype.getattr("kind")?.extract::<String>()? == "f" {
             let f = arr.get_item(name.as_str())?;
-            if numpy.call_method1("isnan", (&f,))?.call_method0("any")?.extract::<bool>()? {
+            if numpy
+                .call_method1("isnan", (&f,))?
+                .call_method0("any")?
+                .extract::<bool>()?
+            {
                 return Ok(false);
             }
             let sb = numpy.call_method1("signbit", (&f,))?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:43199:
             let eq0 = numpy.call_method1("equal", (&f, 0.0_f64))?;
-            if numpy.call_method1("logical_and", (sb, eq0))?.call_method0("any")?.extract::<bool>()? {
+            if numpy
+                .call_method1("logical_and", (sb, eq0))?
+                .call_method0("any")?
+                .extract::<bool>()?
+            {
                 return Ok(false);
             }
         }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:43238:
     let a_flat = a.call_method0("ravel")?;
     let b_flat = b.call_method0("ravel")?;
     if let Some(out) = try_native_struct_intersect_setdiff_int_float_bitplanes(
-        py,
-        numpy,
-        &a_flat,
-        &b_flat,
-        itemsize,
-        !is_diff,
+        py, numpy, &a_flat, &b_flat, itemsize, !is_diff,
     )? {
         return Ok(Some(out));
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:43269:
         return Ok(None);
     }
     // SAFETY: ReadOnlyCell<u8> repr(transparent); read-only under the GIL.
-    let b_data: &[u8] = unsafe { std::slice::from_raw_parts(b_cells.as_ptr().cast::<u8>(), nb * itemsize) };
+    let b_data: &[u8] =
+        unsafe { std::slice::from_raw_parts(b_cells.as_ptr().cast::<u8>(), nb * itemsize) };
     // intersect keeps ua records IN set(b); setdiff keeps those NOT in set(b).
     struct_filter_by_set(py, numpy, &ua, b_data, nb, itemsize, !is_diff)
 }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:43282:
 }

 fn native_little_or_not_applicable(byteorder: &str, width: usize) -> bool {
-    byteorder == "<" || (byteorder == "=" && cfg!(target_endian = "little")) || (width == 1 && byteorder == "|")
+    byteorder == "<"
+        || (byteorder == "=" && cfg!(target_endian = "little"))
+        || (width == 1 && byteorder == "|")
 }

-fn struct_dense_int_float_dtype(dtype: &Bound<'_, PyAny>) -> PyResult<Option<StructDenseIntFloatDesc>> {
+fn struct_dense_int_float_dtype(
+    dtype: &Bound<'_, PyAny>,
+) -> PyResult<Option<StructDenseIntFloatDesc>> {
     let names_obj = dtype.getattr("names")?;
     if names_obj.is_none() {
         return Ok(None);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:43378:
     Some((id, val_i))
 }

-fn write_dense_int_float_record(dst: &mut [u8], desc: StructDenseIntFloatDesc, id: i128, val: i64) -> Option<()> {
+fn write_dense_int_float_record(
+    dst: &mut [u8],
+    desc: StructDenseIntFloatDesc,
+    id: i128,
+    val: i64,
+) -> Option<()> {
     match (desc.int_kind, desc.int_width) {
         (b'i', 1) => dst[0] = i8::try_from(id).ok()? as u8,
         (b'i', 2) => dst[..2].copy_from_slice(&i16::try_from(id).ok()?.to_le_bytes()),
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:43435:
         return Ok(None);
     }
     // SAFETY: ReadOnlyCell<u8> repr(transparent); read-only under the GIL.
-    let a_data: &[u8] = unsafe { std::slice::from_raw_parts(a_cells.as_ptr().cast::<u8>(), na * itemsize) };
-    let b_data: &[u8] = unsafe { std::slice::from_raw_parts(b_cells.as_ptr().cast::<u8>(), nb * itemsize) };
+    let a_data: &[u8] =
+        unsafe { std::slice::from_raw_parts(a_cells.as_ptr().cast::<u8>(), na * itemsize) };
+    let b_data: &[u8] =
+        unsafe { std::slice::from_raw_parts(b_cells.as_ptr().cast::<u8>(), nb * itemsize) };
     let mut min_id = i128::MAX;
     let mut max_id = i128::MIN;
     let mut min_val = i64::MAX;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:43521:
         return Ok(None);
     }
     // SAFETY: fresh structured output viewed as u8, no alias with inputs.
-    let out_data: &mut [u8] =
-        unsafe { std::slice::from_raw_parts_mut(out_cells.as_ptr() as *mut u8, out_len * itemsize) };
+    let out_data: &mut [u8] = unsafe {
+        std::slice::from_raw_parts_mut(out_cells.as_ptr() as *mut u8, out_len * itemsize)
+    };
     let mut write = 0usize;
     for (word_idx, (&a, &b)) in a_bits.iter().zip(&b_bits).enumerate() {
         let mut keep = if keep_in_set { a & b } else { a & !b };
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:43582:
         return Ok(None);
     }
     // SAFETY: ReadOnlyCell<u8> repr(transparent); read-only under the GIL.
-    let a_data: &[u8] = unsafe { std::slice::from_raw_parts(a_cells.as_ptr().cast::<u8>(), na * itemsize) };
-    let b_data: &[u8] = unsafe { std::slice::from_raw_parts(b_cells.as_ptr().cast::<u8>(), nb * itemsize) };
+    let a_data: &[u8] =
+        unsafe { std::slice::from_raw_parts(a_cells.as_ptr().cast::<u8>(), na * itemsize) };
+    let b_data: &[u8] =
+        unsafe { std::slice::from_raw_parts(b_cells.as_ptr().cast::<u8>(), nb * itemsize) };
     let mut min_id = i128::MAX;
     let mut max_id = i128::MIN;
     let mut min_val = i64::MAX;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:43590:
     let mut max_val = i64::MIN;
-    for rec in a_data.chunks_exact(itemsize).chain(b_data.chunks_exact(itemsize)) {
+    for rec in a_data
+        .chunks_exact(itemsize)
+        .chain(b_data.chunks_exact(itemsize))
+    {
         let Some((id, val)) = dense_int_float_record_key(rec, desc) else {
             return Ok(None);
         };
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:43644:
         return Ok(None);
     }
     // SAFETY: fresh structured output viewed as u8, no alias with inputs.
-    let out_data: &mut [u8] =
-        unsafe { std::slice::from_raw_parts_mut(out_cells.as_ptr() as *mut u8, out_len * itemsize) };
+    let out_data: &mut [u8] = unsafe {
+        std::slice::from_raw_parts_mut(out_cells.as_ptr() as *mut u8, out_len * itemsize)
+    };
     let mut write = 0usize;
     for id_off in 0..id_span {
         let id = min_id + id_off as i128;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:43677:
     };
     let a_flat = a.call_method0("ravel")?;
     let b_flat = b.call_method0("ravel")?;
-    if let Some(out) = try_native_struct_setxor1d_int_float_dense(py, numpy, &a_flat, &b_flat, itemsize)? {
+    if let Some(out) =
+        try_native_struct_setxor1d_int_float_dense(py, numpy, &a_flat, &b_flat, itemsize)?
+    {
         return Ok(Some(out));
     }
     // uc = sorted-unique of the union (enforces the all-int64 gate).
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:43694:
     let a_u8 = a_flat.call_method1("view", (&uint8,))?;
     let b_u8 = b_flat.call_method1("view", (&uint8,))?;
     let uc_u8 = uc.call_method1("view", (&uint8,))?;
-    let (Ok(a_buf), Ok(b_buf), Ok(uc_buf)) =
-        (PyBuffer::<u8>::get(&a_u8), PyBuffer::<u8>::get(&b_u8), PyBuffer::<u8>::get(&uc_u8))
-    else {
+    let (Ok(a_buf), Ok(b_buf), Ok(uc_buf)) = (
+        PyBuffer::<u8>::get(&a_u8),
+        PyBuffer::<u8>::get(&b_u8),
+        PyBuffer::<u8>::get(&uc_u8),
+    ) else {
         return Ok(None);
     };
     let (Some(a_cells), Some(b_cells), Some(uc_cells)) =
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:43704:
     else {
         return Ok(None);
     };
-    if a_cells.len() != na * itemsize || b_cells.len() != nb * itemsize || uc_cells.len() != nu * itemsize {
+    if a_cells.len() != na * itemsize
+        || b_cells.len() != nb * itemsize
+        || uc_cells.len() != nu * itemsize
+    {
         return Ok(None);
     }
     // SAFETY: ReadOnlyCell<u8> repr(transparent); read-only under the GIL.
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:43711:
-    let a_data: &[u8] = unsafe { std::slice::from_raw_parts(a_cells.as_ptr().cast::<u8>(), na * itemsize) };
-    let b_data: &[u8] = unsafe { std::slice::from_raw_parts(b_cells.as_ptr().cast::<u8>(), nb * itemsize) };
-    let uc_data: &[u8] = unsafe { std::slice::from_raw_parts(uc_cells.as_ptr().cast::<u8>(), nu * itemsize) };
+    let a_data: &[u8] =
+        unsafe { std::slice::from_raw_parts(a_cells.as_ptr().cast::<u8>(), na * itemsize) };
+    let b_data: &[u8] =
+        unsafe { std::slice::from_raw_parts(b_cells.as_ptr().cast::<u8>(), nb * itemsize) };
+    let uc_data: &[u8] =
+        unsafe { std::slice::from_raw_parts(uc_cells.as_ptr().cast::<u8>(), nu * itemsize) };
     let mut set_a: std::collections::HashSet<&[u8], FastIntBuildHasher> =
         std::collections::HashSet::with_capacity_and_hasher(na, FastIntBuildHasher);
     for k in 0..na {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:43730:
             return Ok(None);
         };
         // SAFETY: fresh numpy.empty bool mask, no alias.
-        let mo: &mut [u8] = unsafe { std::slice::from_raw_parts_mut(mask_cells.as_ptr() as *mut u8, nu) };
+        let mo: &mut [u8] =
+            unsafe { std::slice::from_raw_parts_mut(mask_cells.as_ptr() as *mut u8, nu) };
         mo.par_iter_mut().enumerate().for_each(|(k, slot)| {
             let rec = &uc_data[k * itemsize..(k + 1) * itemsize];
             *slot = (set_a.contains(rec) ^ set_b.contains(rec)) as u8;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:43757:
         return Ok(true);
     };
     // SAFETY: ReadOnlyCell<f64> repr(transparent); read-only under the GIL.
-    let data: &[f64] = unsafe { std::slice::from_raw_parts(cells.as_ptr().cast::<f64>(), cells.len()) };
+    let data: &[f64] =
+        unsafe { std::slice::from_raw_parts(cells.as_ptr().cast::<f64>(), cells.len()) };
     Ok(data
         .par_iter()
         .any(|v| v.is_nan() || v.to_bits() == 0x8000_0000_0000_0000))
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:43764:
 }

 // Gate: both are same-dtype complex128 ndarrays.
-fn c128_setop_gate(a: &Bound<'_, PyAny>, b: &Bound<'_, PyAny>, nd: &Bound<'_, PyAny>) -> PyResult<bool> {
+fn c128_setop_gate(
+    a: &Bound<'_, PyAny>,
+    b: &Bound<'_, PyAny>,
+    nd: &Bound<'_, PyAny>,
+) -> PyResult<bool> {
     if !a.is_exact_instance(nd) || !b.is_exact_instance(nd) {
         return Ok(false);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:43817:
     }
     use rayon::prelude::*;
     // SAFETY: ReadOnlyCell<f64> is repr(transparent) over f64; inputs are read-only under the GIL.
-    let a_data: &[f64] = unsafe { std::slice::from_raw_parts(a_cells.as_ptr().cast::<f64>(), 2 * na) };
-    let b_data: &[f64] = unsafe { std::slice::from_raw_parts(b_cells.as_ptr().cast::<f64>(), 2 * nb) };
+    let a_data: &[f64] =
+        unsafe { std::slice::from_raw_parts(a_cells.as_ptr().cast::<f64>(), 2 * na) };
+    let b_data: &[f64] =
+        unsafe { std::slice::from_raw_parts(b_cells.as_ptr().cast::<f64>(), 2 * nb) };
     let summarize = |data: &[f64]| {
         data.par_chunks_exact(2)
             .map(|z| {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:43832:
             })
             .reduce(
                 || (true, i128::MAX, i128::MIN, i128::MAX, i128::MIN),
-                |x, y| (x.0 && y.0, x.1.min(y.1), x.2.max(y.2), x.3.min(y.3), x.4.max(y.4)),
+                |x, y| {
+                    (
+                        x.0 && y.0,
+                        x.1.min(y.1),
+                        x.2.max(y.2),
+                        x.3.min(y.3),
+                        x.4.max(y.4),
+                    )
+                },
             )
     };
     let ar = summarize(a_data);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:43943:
     }
     use rayon::prelude::*;
     // SAFETY: ReadOnlyCell<f64> is repr(transparent) over f64; inputs are read-only under the GIL.
-    let a_data: &[f64] = unsafe { std::slice::from_raw_parts(a_cells.as_ptr().cast::<f64>(), 2 * na) };
-    let b_data: &[f64] = unsafe { std::slice::from_raw_parts(b_cells.as_ptr().cast::<f64>(), 2 * nb) };
+    let a_data: &[f64] =
+        unsafe { std::slice::from_raw_parts(a_cells.as_ptr().cast::<f64>(), 2 * na) };
+    let b_data: &[f64] =
+        unsafe { std::slice::from_raw_parts(b_cells.as_ptr().cast::<f64>(), 2 * nb) };
     let summarize = |data: &[f64]| {
         data.par_chunks_exact(2)
-            .map(|z| match (
-                c128_dense_integral_component_key(z[0]),
-                c128_dense_integral_component_key(z[1]),
-            ) {
-                (Some(r), Some(i)) => (true, r, r, i, i),
-                _ => (false, 0, 0, 0, 0),
+            .map(|z| {
+                match (
+                    c128_dense_integral_component_key(z[0]),
+                    c128_dense_integral_component_key(z[1]),
+                ) {
+                    (Some(r), Some(i)) => (true, r, r, i, i),
+                    _ => (false, 0, 0, 0, 0),
+                }
             })
             .reduce(
                 || (true, i128::MAX, i128::MIN, i128::MAX, i128::MIN),
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:43959:
-                |x, y| (x.0 && y.0, x.1.min(y.1), x.2.max(y.2), x.3.min(y.3), x.4.max(y.4)),
+                |x, y| {
+                    (
+                        x.0 && y.0,
+                        x.1.min(y.1),
+                        x.2.max(y.2),
+                        x.3.min(y.3),
+                        x.4.max(y.4),
+                    )
+                },
             )
     };
     let ar = summarize(a_data);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:43971:
     if max_r < min_r || max_i < min_i {
         return Ok(None);
     }
-    let (Ok(real_bins), Ok(imag_bins)) =
-        (usize::try_from(max_r - min_r + 1), usize::try_from(max_i - min_i + 1))
-    else {
+    let (Ok(real_bins), Ok(imag_bins)) = (
+        usize::try_from(max_r - min_r + 1),
+        usize::try_from(max_i - min_i + 1),
+    ) else {
         return Ok(None);
     };
     let Some(domain) = real_bins.checked_mul(imag_bins) else {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:44080:
     let nb = ub.len()?;
     let ua_view = ua.call_method1("view", ("float64",))?;
     let ub_view = ub.call_method1("view", ("float64",))?;
-    let (Ok(ua_buf), Ok(ub_buf)) = (PyBuffer::<f64>::get(&ua_view), PyBuffer::<f64>::get(&ub_view))
-    else {
+    let (Ok(ua_buf), Ok(ub_buf)) = (
+        PyBuffer::<f64>::get(&ua_view),
+        PyBuffer::<f64>::get(&ub_view),
+    ) else {
         return Ok(None);
     };
     let (Some(ua_cells), Some(ub_cells)) = (ua_buf.as_slice(py), ub_buf.as_slice(py)) else {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:44091:
         return Ok(None);
     }
     // SAFETY: ReadOnlyCell<f64> is repr(transparent) over f64; arrays are read-only under the GIL.
-    let a_data: &[f64] = unsafe { std::slice::from_raw_parts(ua_cells.as_ptr().cast::<f64>(), 2 * na) };
-    let b_data: &[f64] = unsafe { std::slice::from_raw_parts(ub_cells.as_ptr().cast::<f64>(), 2 * nb) };
+    let a_data: &[f64] =
+        unsafe { std::slice::from_raw_parts(ua_cells.as_ptr().cast::<f64>(), 2 * na) };
+    let b_data: &[f64] =
+        unsafe { std::slice::from_raw_parts(ub_cells.as_ptr().cast::<f64>(), 2 * nb) };

     let mut out_pairs: Vec<[f64; 2]> = Vec::with_capacity(if is_diff { na } else { na.min(nb) });
     let mut i = 0usize;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:44136:
         return Ok(None);
     }
     // SAFETY: fresh numpy.empty complex128 storage viewed as f64.
-    let dst: &mut [f64] =
-        unsafe { std::slice::from_raw_parts_mut(out_cells.as_ptr() as *mut f64, 2 * out_pairs.len()) };
+    let dst: &mut [f64] = unsafe {
+        std::slice::from_raw_parts_mut(out_cells.as_ptr() as *mut f64, 2 * out_pairs.len())
+    };
     use rayon::prelude::*;
     dst.par_chunks_exact_mut(2)
         .zip(out_pairs.par_iter())
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:44185:
     }
     use rayon::prelude::*;
     // SAFETY: ReadOnlyCell<f64> is repr(transparent) over f64; inputs read-only under the GIL.
-    let a_data: &[f64] = unsafe { std::slice::from_raw_parts(a_cells.as_ptr().cast::<f64>(), 2 * na) };
-    let b_data: &[f64] = unsafe { std::slice::from_raw_parts(b_cells.as_ptr().cast::<f64>(), 2 * nb) };
+    let a_data: &[f64] =
+        unsafe { std::slice::from_raw_parts(a_cells.as_ptr().cast::<f64>(), 2 * na) };
+    let b_data: &[f64] =
+        unsafe { std::slice::from_raw_parts(b_cells.as_ptr().cast::<f64>(), 2 * nb) };
     let summarize = |data: &[f64]| {
         data.par_chunks_exact(2)
-            .map(|z| match (
-                c128_dense_integral_component_key(z[0]),
-                c128_dense_integral_component_key(z[1]),
-            ) {
-                (Some(r), Some(i)) => (true, r, r, i, i),
-                _ => (false, 0, 0, 0, 0),
+            .map(|z| {
+                match (
+                    c128_dense_integral_component_key(z[0]),
+                    c128_dense_integral_component_key(z[1]),
+                ) {
+                    (Some(r), Some(i)) => (true, r, r, i, i),
+                    _ => (false, 0, 0, 0, 0),
+                }
             })
             .reduce(
                 || (true, i128::MAX, i128::MIN, i128::MAX, i128::MIN),
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:44201:
-                |x, y| (x.0 && y.0, x.1.min(y.1), x.2.max(y.2), x.3.min(y.3), x.4.max(y.4)),
+                |x, y| {
+                    (
+                        x.0 && y.0,
+                        x.1.min(y.1),
+                        x.2.max(y.2),
+                        x.3.min(y.3),
+                        x.4.max(y.4),
+                    )
+                },
             )
     };
     let ar = summarize(a_data);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:44213:
     if max_r < min_r || max_i < min_i {
         return Ok(None);
     }
-    let (Ok(real_bins), Ok(imag_bins)) =
-        (usize::try_from(max_r - min_r + 1), usize::try_from(max_i - min_i + 1))
-    else {
+    let (Ok(real_bins), Ok(imag_bins)) = (
+        usize::try_from(max_r - min_r + 1),
+        usize::try_from(max_i - min_i + 1),
+    ) else {
         return Ok(None);
     };
     let Some(domain) = real_bins.checked_mul(imag_bins) else {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:44322:
         return Ok(None);
     }
     // SAFETY: ReadOnlyCell<u8> repr(transparent); read-only under the GIL.
-    let b_data: &[u8] = unsafe { std::slice::from_raw_parts(b_cells.as_ptr().cast::<u8>(), nb * 16) };
+    let b_data: &[u8] =
+        unsafe { std::slice::from_raw_parts(b_cells.as_ptr().cast::<u8>(), nb * 16) };
     struct_filter_by_set(py, numpy, &ua, b_data, nb, 16, !is_diff)
 }

Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:44570:
     }
     #[inline]
     fn fk_key(self) -> u64 {
-        if self == 0.0 {
-            0
-        } else {
-            self.to_bits()
-        }
+        if self == 0.0 { 0 } else { self.to_bits() }
     }
 }
 impl FloatKey for f32 {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:44585:
     }
     #[inline]
     fn fk_key(self) -> u32 {
-        if self == 0.0 {
-            0
-        } else {
-            self.to_bits()
-        }
+        if self == 0.0 { 0 } else { self.to_bits() }
     }
 }

Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:45123:
     }
     let ndarray_type = numpy.getattr("ndarray")?;
     if !source.is_exact_instance(&ndarray_type)
-        || !source.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()?
+        || !source
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
     {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:45366:
     let _ = subok;
     let numpy = py.import("numpy")?;
     if let Some(out) = try_native_full_like_parallel(
-        py, &numpy, a_bound, fill_bound, dtype_bound, order, shape_bound,
+        py,
+        &numpy,
+        a_bound,
+        fill_bound,
+        dtype_bound,
+        order,
+        shape_bound,
         device_bound,
     )? {
         return Ok(out);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:45409:
     let numpy = py.import("numpy")?;
     let zero = 0i64.into_pyobject(py)?.into_any();
     if let Some(out) = try_native_full_like_parallel(
-        py, &numpy, a_bound, &zero, dtype_bound, order, shape_bound,
+        py,
+        &numpy,
+        a_bound,
+        &zero,
+        dtype_bound,
+        order,
+        shape_bound,
         device_bound,
     )? {
         return Ok(out);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:45450:
     let numpy = py.import("numpy")?;
     let one = 1i64.into_pyobject(py)?.into_any();
     if let Some(out) = try_native_full_like_parallel(
-        py, &numpy, a_bound, &one, dtype_bound, order, shape_bound,
+        py,
+        &numpy,
+        a_bound,
+        &one,
+        dtype_bound,
+        order,
+        shape_bound,
         device_bound,
     )? {
         return Ok(out);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:45991:
                 use rayon::prelude::*;
                 let threads = rayon::current_num_threads().max(1);
                 let chunk = n.div_ceil(threads).max(1) * 2;
-                o.par_chunks_mut(chunk).zip(lx.par_chunks(chunk)).for_each(|(oc, xc)| {
-                    let m = oc.len() / 2;
-                    for j in 0..m {
-                        let re = xc[2 * j];
-                        let im = xc[2 * j + 1];
-                        let (o_re, o_im) = match op {
-                            ComplexUnaryOp::Exp => {
-                                let e = re.exp();
-                                (e * im.cos(), e * im.sin())
-                            }
-                            ComplexUnaryOp::Sin => (re.sin() * im.cosh(), re.cos() * im.sinh()),
-                            ComplexUnaryOp::Cos => (re.cos() * im.cosh(), -re.sin() * im.sinh()),
-                            ComplexUnaryOp::Sinh => (re.sinh() * im.cos(), re.cosh() * im.sin()),
-                            ComplexUnaryOp::Cosh => (re.cosh() * im.cos(), re.sinh() * im.sin()),
-                            ComplexUnaryOp::Sign => {
-                                let h = re.hypot(im);
-                                if h == 0.0 as $ty {
-                                    (0.0 as $ty, 0.0 as $ty)
-                                } else {
-                                    (re / h, im / h)
+                o.par_chunks_mut(chunk)
+                    .zip(lx.par_chunks(chunk))
+                    .for_each(|(oc, xc)| {
+                        let m = oc.len() / 2;
+                        for j in 0..m {
+                            let re = xc[2 * j];
+                            let im = xc[2 * j + 1];
+                            let (o_re, o_im) = match op {
+                                ComplexUnaryOp::Exp => {
+                                    let e = re.exp();
+                                    (e * im.cos(), e * im.sin())
                                 }
-                            }
-                        };
-                        oc[2 * j] = o_re;
-                        oc[2 * j + 1] = o_im;
-                    }
-                });
+                                ComplexUnaryOp::Sin => (re.sin() * im.cosh(), re.cos() * im.sinh()),
+                                ComplexUnaryOp::Cos => {
+                                    (re.cos() * im.cosh(), -re.sin() * im.sinh())
+                                }
+                                ComplexUnaryOp::Sinh => {
+                                    (re.sinh() * im.cos(), re.cosh() * im.sin())
+                                }
+                                ComplexUnaryOp::Cosh => {
+                                    (re.cosh() * im.cos(), re.sinh() * im.sin())
+                                }
+                                ComplexUnaryOp::Sign => {
+                                    let h = re.hypot(im);
+                                    if h == 0.0 as $ty {
+                                        (0.0 as $ty, 0.0 as $ty)
+                                    } else {
+                                        (re / h, im / h)
+                                    }
+                                }
+                            };
+                            oc[2 * j] = o_re;
+                            oc[2 * j + 1] = o_im;
+                        }
+                    });
             }
             Ok(Some(out.unbind()))
         }};
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:46262:
         Ok(Some(flat.unbind()))
     } else {
         let shape_tuple = PyTuple::new(py, shape.iter().copied())?;
-        Ok(Some(flat.call_method1("reshape", (&shape_tuple,))?.unbind()))
+        Ok(Some(
+            flat.call_method1("reshape", (&shape_tuple,))?.unbind(),
+        ))
     }
 }

Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:46494:
             } else {
                 false
             };
-            if !zero_divisor
-                && let Some(out) = try_zerocopy_f64_binary(py, &a, &b, BinaryOp::Fmod)?
+            if !zero_divisor && let Some(out) = try_zerocopy_f64_binary(py, &a, &b, BinaryOp::Fmod)?
             {
                 return Ok(out);
             }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:46514:
             } else {
                 false
             };
-            if !zero_divisor
-                && let Some(out) = try_zerocopy_f32_binary(py, &a, &b, BinaryOp::Fmod)?
+            if !zero_divisor && let Some(out) = try_zerocopy_f32_binary(py, &a, &b, BinaryOp::Fmod)?
             {
                 return Ok(out);
             }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:46838:
         // straight off the borrowed buffers with a rayon-parallel atan2, skipping the
         // extract_numeric_array copies (the serial extract+build marshalling, not the
         // atan2, dominated the old path — ~26ms of a 47ms 1M call).
-        if let Some(out) =
-            try_zerocopy_f64_binary(py, &args.get_item(0)?, &args.get_item(1)?, BinaryOp::Arctan2)?
-        {
+        if let Some(out) = try_zerocopy_f64_binary(
+            py,
+            &args.get_item(0)?,
+            &args.get_item(1)?,
+            BinaryOp::Arctan2,
+        )? {
             return Ok(out);
         }
         let numpy = py.import("numpy")?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:47045:
     // numpy (its vectorized f64 pow beats the extract path there). (BlackThrush 2026-06-25:
     // the old "delegate everything" note predated the no-copy from_raw_parts parallel kernel.)
     if kwargs.is_none_or(|kwargs| kwargs.is_empty()) && args.len() == 2 {
-        if numpy_dtype_is_f64(py, &args.get_item(0)?) && numpy_dtype_is_f64(py, &args.get_item(1)?)
+        if numpy_dtype_is_f64(py, &args.get_item(0)?)
+            && numpy_dtype_is_f64(py, &args.get_item(1)?)
             && let Some(out) = try_zerocopy_f64_binary(
                 py,
                 &args.get_item(0)?,
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:47633:
         }
     }
     let output_shape = PyTuple::new(py, shape.iter().copied())?;
-    Ok(Some(flat.call_method1("reshape", (&output_shape,))?.unbind()))
+    Ok(Some(
+        flat.call_method1("reshape", (&output_shape,))?.unbind(),
+    ))
 }

 // Dispatch the narrow-width integer shift across i8/i16/i32 and u8/u16/u32/u64.
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:47656:
     let itemsize = dtype.getattr("itemsize")?.extract::<usize>()?;
     match (kind.as_str(), itemsize, is_left) {
         ("i", 1, true) => shift_typed_nw::<i8, _>(py, &numpy, a, b, "int8", "i", 1, |a, b| {
-            if (0..8).contains(&b) { a.wrapping_shl(b as u32) } else { 0 }
+            if (0..8).contains(&b) {
+                a.wrapping_shl(b as u32)
+            } else {
+                0
+            }
         }),
         ("i", 1, false) => shift_typed_nw::<i8, _>(py, &numpy, a, b, "int8", "i", 1, |a, b| {
-            if (0..8).contains(&b) { a.wrapping_shr(b as u32) } else if a < 0 { -1 } else { 0 }
+            if (0..8).contains(&b) {
+                a.wrapping_shr(b as u32)
+            } else if a < 0 {
+                -1
+            } else {
+                0
+            }
         }),
         ("i", 2, true) => shift_typed_nw::<i16, _>(py, &numpy, a, b, "int16", "i", 2, |a, b| {
-            if (0..16).contains(&b) { a.wrapping_shl(b as u32) } else { 0 }
+            if (0..16).contains(&b) {
+                a.wrapping_shl(b as u32)
+            } else {
+                0
+            }
         }),
         ("i", 2, false) => shift_typed_nw::<i16, _>(py, &numpy, a, b, "int16", "i", 2, |a, b| {
-            if (0..16).contains(&b) { a.wrapping_shr(b as u32) } else if a < 0 { -1 } else { 0 }
+            if (0..16).contains(&b) {
+                a.wrapping_shr(b as u32)
+            } else if a < 0 {
+                -1
+            } else {
+                0
+            }
         }),
         ("i", 4, true) => shift_typed_nw::<i32, _>(py, &numpy, a, b, "int32", "i", 4, |a, b| {
-            if (0..32).contains(&b) { a.wrapping_shl(b as u32) } else { 0 }
+            if (0..32).contains(&b) {
+                a.wrapping_shl(b as u32)
+            } else {
+                0
+            }
         }),
         ("i", 4, false) => shift_typed_nw::<i32, _>(py, &numpy, a, b, "int32", "i", 4, |a, b| {
-            if (0..32).contains(&b) { a.wrapping_shr(b as u32) } else if a < 0 { -1 } else { 0 }
+            if (0..32).contains(&b) {
+                a.wrapping_shr(b as u32)
+            } else if a < 0 {
+                -1
+            } else {
+                0
+            }
         }),
         ("u", 1, true) => shift_typed_nw::<u8, _>(py, &numpy, a, b, "uint8", "u", 1, |a, b| {
             if b < 8 { a.wrapping_shl(b as u32) } else { 0 }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:47928:
     // every size (measured c128 16M 1.91x slower). Complex square delegates to numpy.
     // Bare call → native fast path; out=/where=/dtype= etc → numpy passthrough (full
     // ufunc kwarg surface, matching np.square which accepts out=).
-    native_unary_elementwise_or_passthrough(py, args, kwargs, UnaryOp::Square, "square", "square(x)")
+    native_unary_elementwise_or_passthrough(
+        py,
+        args,
+        kwargs,
+        UnaryOp::Square,
+        "square",
+        "square(x)",
+    )
 }

 #[pyfunction]
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:48226:
         // SAFETY: ReadOnlyCell<T>/Cell<T> are repr(transparent) over T; the input
         // is read-only under the GIL and `output` is a fresh numpy.empty buffer
         // (no alias). Each row maps to a disjoint output chunk.
-        let in_data: &[T] =
-            unsafe { std::slice::from_raw_parts(p_in.as_ptr().cast::<T>(), n) };
+        let in_data: &[T] = unsafe { std::slice::from_raw_parts(p_in.as_ptr().cast::<T>(), n) };
         let out_data: &mut [T] =
             unsafe { std::slice::from_raw_parts_mut(output.as_ptr() as *mut T, n) };
         out_data
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:48236:
             .for_each(|(row_out, row_in)| unwrap_row(row_in, row_out));
     } else {
         // SAFETY: same repr(transparent) T view; serial single-threaded fill.
-        let in_data: &[T] =
-            unsafe { std::slice::from_raw_parts(p_in.as_ptr().cast::<T>(), n) };
+        let in_data: &[T] = unsafe { std::slice::from_raw_parts(p_in.as_ptr().cast::<T>(), n) };
         let out_data: &mut [T] =
             unsafe { std::slice::from_raw_parts_mut(output.as_ptr() as *mut T, n) };
         for (row_out, row_in) in out_data.chunks_mut(row).zip(in_data.chunks(row)) {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:48586:
     a2: &Bound<'_, PyAny>,
 ) -> PyResult<Option<bool>> {
     let u8t = numpy.getattr("uint8")?;
-    let (Ok(v1), Ok(v2)) = (a1.call_method1("view", (&u8t,)), a2.call_method1("view", (&u8t,)))
-    else {
+    let (Ok(v1), Ok(v2)) = (
+        a1.call_method1("view", (&u8t,)),
+        a2.call_method1("view", (&u8t,)),
+    ) else {
         return Ok(None);
     };
     let (Ok(b1), Ok(b2)) = (PyBuffer::<u8>::get(&v1), PyBuffer::<u8>::get(&v2)) else {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:48599:
     let (Some(s1), Some(s2)) = (b1.as_slice(py), b2.as_slice(py)) else {
         return Ok(None);
     };
-    Ok(Some(s1.iter().zip(s2.iter()).all(|(x, y)| x.get() == y.get())))
+    Ok(Some(
+        s1.iter().zip(s2.iter()).all(|(x, y)| x.get() == y.get()),
+    ))
 }

 // Zero-copy np.array_equal for two SAME-dtype numeric ndarrays (int/uint/bool of
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:50169:
     let ndarray_type = numpy.getattr("ndarray")?;
     let a_arr = a.is_exact_instance(&ndarray_type) && numpy_dtype_is_f64(py, a);
     let b_arr = b.is_exact_instance(&ndarray_type) && numpy_dtype_is_f64(py, b);
-    let a_buf = if a_arr { PyBuffer::<f64>::get(a).ok() } else { None };
-    let b_buf = if b_arr { PyBuffer::<f64>::get(b).ok() } else { None };
-    let a_sc = if a_buf.is_none() { a.extract::<f64>().ok() } else { None };
-    let b_sc = if b_buf.is_none() { b.extract::<f64>().ok() } else { None };
+    let a_buf = if a_arr {
+        PyBuffer::<f64>::get(a).ok()
+    } else {
+        None
+    };
+    let b_buf = if b_arr {
+        PyBuffer::<f64>::get(b).ok()
+    } else {
+        None
+    };
+    let a_sc = if a_buf.is_none() {
+        a.extract::<f64>().ok()
+    } else {
+        None
+    };
+    let b_sc = if b_buf.is_none() {
+        b.extract::<f64>().ok()
+    } else {
+        None
+    };
     let verdict = match (&a_buf, &b_buf) {
         (Some(ab), Some(bb)) => {
             if ab.shape() != bb.shape() {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:50235:
     if !a_arr && !b_arr {
         return Ok(None); // need at least one f32 array
     }
-    let a_buf = if a_arr { PyBuffer::<f32>::get(a).ok() } else { None };
-    let b_buf = if b_arr { PyBuffer::<f32>::get(b).ok() } else { None };
-    let a_sc = if a_buf.is_none() { a.extract::<f64>().ok() } else { None };
-    let b_sc = if b_buf.is_none() { b.extract::<f64>().ok() } else { None };
+    let a_buf = if a_arr {
+        PyBuffer::<f32>::get(a).ok()
+    } else {
+        None
+    };
+    let b_buf = if b_arr {
+        PyBuffer::<f32>::get(b).ok()
+    } else {
+        None
+    };
+    let a_sc = if a_buf.is_none() {
+        a.extract::<f64>().ok()
+    } else {
+        None
+    };
+    let b_sc = if b_buf.is_none() {
+        b.extract::<f64>().ok()
+    } else {
+        None
+    };
     let verdict = match (&a_buf, &b_buf) {
         (Some(ab), Some(bb)) => {
             if ab.shape() != bb.shape() {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:50247:
             let (Some(sa), Some(sb)) = (ab.as_slice(py), bb.as_slice(py)) else {
                 return Ok(None);
             };
-            sa.iter().zip(sb.iter()).all(|(x, y)| {
-                allclose_pair(x.get() as f64, y.get() as f64, rtol, atol, equal_nan)
-            })
+            sa.iter()
+                .zip(sb.iter())
+                .all(|(x, y)| allclose_pair(x.get() as f64, y.get() as f64, rtol, atol, equal_nan))
         }
         (Some(ab), None) => {
             let Some(bs) = b_sc else { return Ok(None) };
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:53017:
         return Ok(None);
     }
     // SAFETY: ReadOnlyCell<u8> is repr(transparent) over u8; read-only under the GIL.
-    let in_data: &[u8] = unsafe { std::slice::from_raw_parts(in_cells.as_ptr().cast::<u8>(), nbytes) };
+    let in_data: &[u8] =
+        unsafe { std::slice::from_raw_parts(in_cells.as_ptr().cast::<u8>(), nbytes) };
     let total = before + n + after;
     let out_kwargs = PyDict::new(py);
     out_kwargs.set_item("dtype", &dtype)?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:53118:
         return Ok(None);
     }
     // SAFETY: ReadOnlyCell<u8> is repr(transparent) over u8; read-only under the GIL.
-    let in_data: &[u8] = unsafe { std::slice::from_raw_parts(in_cells.as_ptr().cast::<u8>(), nbytes) };
+    let in_data: &[u8] =
+        unsafe { std::slice::from_raw_parts(in_cells.as_ptr().cast::<u8>(), nbytes) };
     let total = before + n + after;
     let out_kwargs = PyDict::new(py);
     out_kwargs.set_item("dtype", &dtype)?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:53253:
         return Ok(None);
     }
     // SAFETY: ReadOnlyCell<u8> is repr(transparent) over u8; read-only under the GIL.
-    let in_data: &[u8] = unsafe { std::slice::from_raw_parts(in_cells.as_ptr().cast::<u8>(), nbytes) };
+    let in_data: &[u8] =
+        unsafe { std::slice::from_raw_parts(in_cells.as_ptr().cast::<u8>(), nbytes) };
     let total = before + n + after;
     let out_kwargs = PyDict::new(py);
     out_kwargs.set_item("dtype", &dtype)?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:53369:
         return Ok(None);
     }
     // SAFETY: ReadOnlyCell<u8> is repr(transparent) over u8; read-only under the GIL.
-    let in_data: &[u8] = unsafe { std::slice::from_raw_parts(in_cells.as_ptr().cast::<u8>(), nbytes) };
+    let in_data: &[u8] =
+        unsafe { std::slice::from_raw_parts(in_cells.as_ptr().cast::<u8>(), nbytes) };
     let total = before + n + after;
     let out_kwargs = PyDict::new(py);
     out_kwargs.set_item("dtype", &dtype)?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:53429:
     // numpy surface is exposed.
     let numpy = py.import("numpy")?;
     // Native 1-D constant-mode fast path (bypasses np.pad's ~9us Python dispatch).
-    if let Some(out) =
-        try_zerocopy_f64_pad_1d_constant(py, &numpy, array.bind(py), pad_width.bind(py), mode, kwargs)?
-    {
+    if let Some(out) = try_zerocopy_f64_pad_1d_constant(
+        py,
+        &numpy,
+        array.bind(py),
+        pad_width.bind(py),
+        mode,
+        kwargs,
+    )? {
         return Ok(out);
     }
     // Same fast path for non-f64 numeric dtypes (f32/int*/complex/bool) via a byte copy.
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:53438:
-    if let Some(out) =
-        try_zerocopy_pad_bytes_1d_constant(py, &numpy, array.bind(py), pad_width.bind(py), mode, kwargs)?
-    {
+    if let Some(out) = try_zerocopy_pad_bytes_1d_constant(
+        py,
+        &numpy,
+        array.bind(py),
+        pad_width.bind(py),
+        mode,
+        kwargs,
+    )? {
         return Ok(out);
     }
     // Native 1-D edge-mode fast path (first/last element replication) for all numeric dtypes.
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:53444:
-    if let Some(out) =
-        try_zerocopy_pad_bytes_1d_edge(py, &numpy, array.bind(py), pad_width.bind(py), mode, kwargs)?
-    {
+    if let Some(out) = try_zerocopy_pad_bytes_1d_edge(
+        py,
+        &numpy,
+        array.bind(py),
+        pad_width.bind(py),
+        mode,
+        kwargs,
+    )? {
         return Ok(out);
     }
     // Native 1-D wrap-mode fast path (periodic tiling, before<=n & after<=n) for all numeric dtypes.
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:53450:
-    if let Some(out) =
-        try_zerocopy_pad_bytes_1d_wrap(py, &numpy, array.bind(py), pad_width.bind(py), mode, kwargs)?
-    {
+    if let Some(out) = try_zerocopy_pad_bytes_1d_wrap(
+        py,
+        &numpy,
+        array.bind(py),
+        pad_width.bind(py),
+        mode,
+        kwargs,
+    )? {
         return Ok(out);
     }
     // Native 1-D reflect/symmetric fast path (single-reflection mirror) for all numeric dtypes.
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:53456:
-    if let Some(out) =
-        try_zerocopy_pad_bytes_1d_reflect(py, &numpy, array.bind(py), pad_width.bind(py), mode, kwargs)?
-    {
+    if let Some(out) = try_zerocopy_pad_bytes_1d_reflect(
+        py,
+        &numpy,
+        array.bind(py),
+        pad_width.bind(py),
+        mode,
+        kwargs,
+    )? {
         return Ok(out);
     }
     let call_kwargs = PyDict::new(py);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:53542:
     let svd_mode: Option<u8> = ord.as_ref().and_then(|ord_val| {
         let bound = ord_val.bind(py);
         if let Ok(text) = bound.extract::<String>() {
-            if text == "nuc" {
-                Some(2)
-            } else {
-                None
-            }
+            if text == "nuc" { Some(2) } else { None }
         } else if let Ok(f) = bound.extract::<f64>() {
             if f == 2.0 {
                 Some(0)
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:53589:
                             out_shape.push(1);
                             out_shape.push(1);
                         }
-                        let result = UFuncArray::new(out_shape, out, DType::F64)
-                            .map_err(map_ufunc_error)?;
+                        let result =
+                            UFuncArray::new(out_shape, out, DType::F64).map_err(map_ufunc_error)?;
                         return build_numpy_array_from_ufunc(py, &result);
                     }
                 }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:53891:
     // above; this only catches the losing non-last full-shape case.
     if let Some(w) = weights.as_ref()
         && !w.bind(py).is_none()
-        && let Ok(a_shape) = a.bind(py).getattr("shape").and_then(|s| s.extract::<Vec<usize>>())
-        && let Ok(w_shape) = w.bind(py).getattr("shape").and_then(|s| s.extract::<Vec<usize>>())
+        && let Ok(a_shape) = a
+            .bind(py)
+            .getattr("shape")
+            .and_then(|s| s.extract::<Vec<usize>>())
+        && let Ok(w_shape) = w
+            .bind(py)
+            .getattr("shape")
+            .and_then(|s| s.extract::<Vec<usize>>())
         && a_shape == w_shape
         && a_shape.len() >= 2
         && let Some(ax_obj) = axis.as_ref()
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:54218:
         let out_shape: Vec<usize> = shape[..ax].to_vec();
         let avg: Vec<f64> = pairs.iter().map(|(n, d)| n / d).collect();
         let avg_flat = numpy_array_from_slice(py, &numpy, &avg, "float64")?;
-        let avg_arr = avg_flat
-            .call_method1("reshape", (PyTuple::new(py, out_shape.iter().copied())?,))?;
+        let avg_arr =
+            avg_flat.call_method1("reshape", (PyTuple::new(py, out_shape.iter().copied())?,))?;
         let avg_output = if out_shape.is_empty() {
             avg_arr.get_item(())?
         } else {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:54236:
                 sow_arr
             };
             return Ok(Some(
-                PyTuple::new(py, [avg_output, sow_output])?.into_any().unbind(),
+                PyTuple::new(py, [avg_output, sow_output])?
+                    .into_any()
+                    .unbind(),
             ));
         }
         return Ok(Some(avg_output.unbind()));
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:54781:
         if let Ok(values) = fnp_linalg::batch_svd(x.values(), &owned_shape) {
             let mut out_shape: Vec<usize> = owned_shape[..owned_shape.len() - 2].to_vec();
             out_shape.push(k);
-            let result =
-                UFuncArray::new(out_shape, values, DType::F64).map_err(map_ufunc_error)?;
+            let result = UFuncArray::new(out_shape, values, DType::F64).map_err(map_ufunc_error)?;
             return build_numpy_array_from_ufunc(py, &result);
         }
         return fallback();
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:55255:
         return Ok(None);
     }
     if a.getattr("ndim")?.extract::<usize>()? != 1
-        || !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()?
+        || !a
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
     {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:55319:
         return Ok(None);
     }
     if a.getattr("ndim")?.extract::<usize>()? != 1
-        || !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()?
+        || !a
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
     {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:55341:
     use rayon::prelude::*;
     // SAFETY: ReadOnlyCell<f64> is repr(transparent) over f64; read-only under the GIL.
     let src: &[f64] = unsafe { std::slice::from_raw_parts(cells.as_ptr().cast::<f64>(), 2 * n) };
-    if src.par_iter().any(|v| v.is_nan() || v.to_bits() == 0x8000_0000_0000_0000) {
+    if src
+        .par_iter()
+        .any(|v| v.is_nan() || v.to_bits() == 0x8000_0000_0000_0000)
+    {
         return Ok(None);
     }
     let kwargs = PyDict::new(py);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:55390:
         return Ok(None);
     }
     if a.getattr("ndim")?.extract::<usize>()? != 1
-        || !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()?
+        || !a
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
     {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:55413:
     // SAFETY: ReadOnlyCell<f64> is repr(transparent) over f64; read-only under the GIL.
     let src: &[f64] = unsafe { std::slice::from_raw_parts(cells.as_ptr().cast::<f64>(), 2 * n) };
     // Defer on NaN or -0.0 (numpy's complex sort/unique ordering of those is subtle).
-    if src.par_iter().any(|v| v.is_nan() || v.to_bits() == 0x8000_0000_0000_0000) {
+    if src
+        .par_iter()
+        .any(|v| v.is_nan() || v.to_bits() == 0x8000_0000_0000_0000)
+    {
         return Ok(None);
     }
     // Sort [re, im] pairs lexicographically (numpy's complex order), then dedup adjacent equal.
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:55474:
         return Ok(None);
     }
     if a_arr.getattr("ndim")?.extract::<usize>()? != 1
-        || !a_arr.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()?
+        || !a_arr
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
     {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:55488:
         return Ok(None);
     }
     if v.getattr("ndim")?.extract::<usize>()? != 1
-        || !v.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()?
+        || !v
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
     {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:55495:
-    let n: usize = a_arr.getattr("shape")?.extract::<Vec<usize>>()?.iter().product();
-    let m: usize = v.getattr("shape")?.extract::<Vec<usize>>()?.iter().product();
+    let n: usize = a_arr
+        .getattr("shape")?
+        .extract::<Vec<usize>>()?
+        .iter()
+        .product();
+    let m: usize = v
+        .getattr("shape")?
+        .extract::<Vec<usize>>()?
+        .iter()
+        .product();
     if n < 1 || m < QUERY_MIN || rayon::current_num_threads() < 2 {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:55500:
     let a_view = a_arr.call_method1("view", ("float64",))?;
     let v_view = v.call_method1("view", ("float64",))?;
-    let (Ok(a_buf), Ok(v_buf)) = (PyBuffer::<f64>::get(&a_view), PyBuffer::<f64>::get(&v_view)) else {
+    let (Ok(a_buf), Ok(v_buf)) = (PyBuffer::<f64>::get(&a_view), PyBuffer::<f64>::get(&v_view))
+    else {
         return Ok(None);
     };
     let (Some(a_cells), Some(v_cells)) = (a_buf.as_slice(py), v_buf.as_slice(py)) else {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:55510:
     }
     use rayon::prelude::*;
     // SAFETY: ReadOnlyCell<f64> repr(transparent) over f64; read-only under the GIL.
-    let a_data: &[f64] = unsafe { std::slice::from_raw_parts(a_cells.as_ptr().cast::<f64>(), 2 * n) };
-    let v_data: &[f64] = unsafe { std::slice::from_raw_parts(v_cells.as_ptr().cast::<f64>(), 2 * m) };
-    let bad = |d: &[f64]| d.par_iter().any(|x| x.is_nan() || x.to_bits() == 0x8000_0000_0000_0000);
+    let a_data: &[f64] =
+        unsafe { std::slice::from_raw_parts(a_cells.as_ptr().cast::<f64>(), 2 * n) };
+    let v_data: &[f64] =
+        unsafe { std::slice::from_raw_parts(v_cells.as_ptr().cast::<f64>(), 2 * m) };
+    let bad = |d: &[f64]| {
+        d.par_iter()
+            .any(|x| x.is_nan() || x.to_bits() == 0x8000_0000_0000_0000)
+    };
     if bad(a_data) || bad(v_data) {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:55526:
         return Ok(None);
     }
     // SAFETY: fresh numpy.empty int64 output, no alias with a_data/v_data.
-    let o: &mut [i64] = unsafe { std::slice::from_raw_parts_mut(out_cells.as_ptr() as *mut i64, m) };
+    let o: &mut [i64] =
+        unsafe { std::slice::from_raw_parts_mut(out_cells.as_ptr() as *mut i64, m) };
     o.par_iter_mut().enumerate().for_each(|(j, slot)| {
         let (qr, qi) = (v_data[2 * j], v_data[2 * j + 1]);
         let (mut lo, mut hi) = (0usize, n);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:55584:
     }
     let contig = |x: &Bound<'_, PyAny>| -> PyResult<bool> {
         Ok(x.getattr("ndim")?.extract::<usize>()? == 1
-            && x.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()?)
+            && x.getattr("flags")?
+                .getattr("c_contiguous")?
+                .extract::<bool>()?)
     };
     if !contig(element)? || !contig(test)? {
         return Ok(None);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:55591:
     }
-    let n: usize = element.getattr("shape")?.extract::<Vec<usize>>()?.iter().product();
-    let m: usize = test.getattr("shape")?.extract::<Vec<usize>>()?.iter().product();
+    let n: usize = element
+        .getattr("shape")?
+        .extract::<Vec<usize>>()?
+        .iter()
+        .product();
+    let m: usize = test
+        .getattr("shape")?
+        .extract::<Vec<usize>>()?
+        .iter()
+        .product();
     if n < ISIN_MIN || rayon::current_num_threads() < 2 {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:55608:
             return Ok(true);
         }
         // SAFETY: ReadOnlyCell<f64> repr(transparent); read-only under the GIL.
-        let d: &[f64] = unsafe { std::slice::from_raw_parts(cells.as_ptr().cast::<f64>(), 2 * count) };
-        Ok(d.par_iter().any(|v| v.is_nan() || v.to_bits() == 0x8000_0000_0000_0000))
+        let d: &[f64] =
+            unsafe { std::slice::from_raw_parts(cells.as_ptr().cast::<f64>(), 2 * count) };
+        Ok(d.par_iter()
+            .any(|v| v.is_nan() || v.to_bits() == 0x8000_0000_0000_0000))
     };
     if bad(element, n)? || bad(test, m)? {
         return Ok(None);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:55628:
         return Ok(None);
     }
     // SAFETY: ReadOnlyCell<u8> repr(transparent); read-only under the GIL.
-    let e_data: &[u8] = unsafe { std::slice::from_raw_parts(e_cells.as_ptr().cast::<u8>(), 16 * n) };
-    let t_data: &[u8] = unsafe { std::slice::from_raw_parts(t_cells.as_ptr().cast::<u8>(), 16 * m) };
+    let e_data: &[u8] =
+        unsafe { std::slice::from_raw_parts(e_cells.as_ptr().cast::<u8>(), 16 * n) };
+    let t_data: &[u8] =
+        unsafe { std::slice::from_raw_parts(t_cells.as_ptr().cast::<u8>(), 16 * m) };
     let mut set: std::collections::HashSet<&[u8], FastIntBuildHasher> =
         std::collections::HashSet::with_capacity_and_hasher(m, FastIntBuildHasher);
     for k in 0..m {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:55669:
         return Ok(None);
     }
     if a_arr.getattr("ndim")?.extract::<usize>()? != 1
-        || !a_arr.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()?
+        || !a_arr
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
     {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:55683:
         return Ok(None);
     }
     if v.getattr("ndim")?.extract::<usize>()? != 1
-        || !v.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()?
+        || !v
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
     {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:55690:
-    let n: usize = a_arr.getattr("shape")?.extract::<Vec<usize>>()?.iter().product();
-    let m: usize = v.getattr("shape")?.extract::<Vec<usize>>()?.iter().product();
+    let n: usize = a_arr
+        .getattr("shape")?
+        .extract::<Vec<usize>>()?
+        .iter()
+        .product();
+    let m: usize = v
+        .getattr("shape")?
+        .extract::<Vec<usize>>()?
+        .iter()
+        .product();
     if n < 1 || m < QUERY_MIN || rayon::current_num_threads() < 2 {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:55695:
     let a_view = a_arr.call_method1("view", ("float32",))?;
     let v_view = v.call_method1("view", ("float32",))?;
-    let (Ok(a_buf), Ok(v_buf)) = (PyBuffer::<f32>::get(&a_view), PyBuffer::<f32>::get(&v_view)) else {
+    let (Ok(a_buf), Ok(v_buf)) = (PyBuffer::<f32>::get(&a_view), PyBuffer::<f32>::get(&v_view))
+    else {
         return Ok(None);
     };
     let (Some(a_cells), Some(v_cells)) = (a_buf.as_slice(py), v_buf.as_slice(py)) else {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:55705:
     }
     use rayon::prelude::*;
     // SAFETY: ReadOnlyCell<f32> repr(transparent); read-only under the GIL.
-    let a_data: &[f32] = unsafe { std::slice::from_raw_parts(a_cells.as_ptr().cast::<f32>(), 2 * n) };
-    let v_data: &[f32] = unsafe { std::slice::from_raw_parts(v_cells.as_ptr().cast::<f32>(), 2 * m) };
-    let bad = |d: &[f32]| d.par_iter().any(|x| x.is_nan() || x.to_bits() == 0x8000_0000);
+    let a_data: &[f32] =
+        unsafe { std::slice::from_raw_parts(a_cells.as_ptr().cast::<f32>(), 2 * n) };
+    let v_data: &[f32] =
+        unsafe { std::slice::from_raw_parts(v_cells.as_ptr().cast::<f32>(), 2 * m) };
+    let bad = |d: &[f32]| {
+        d.par_iter()
+            .any(|x| x.is_nan() || x.to_bits() == 0x8000_0000)
+    };
     if bad(a_data) || bad(v_data) {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:55721:
         return Ok(None);
     }
     // SAFETY: fresh numpy.empty int64 output, no alias.
-    let o: &mut [i64] = unsafe { std::slice::from_raw_parts_mut(out_cells.as_ptr() as *mut i64, m) };
+    let o: &mut [i64] =
+        unsafe { std::slice::from_raw_parts_mut(out_cells.as_ptr() as *mut i64, m) };
     o.par_iter_mut().enumerate().for_each(|(j, slot)| {
         let (qr, qi) = (v_data[2 * j], v_data[2 * j + 1]);
         let (mut lo, mut hi) = (0usize, n);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:55771:
     }
     let contig = |x: &Bound<'_, PyAny>| -> PyResult<bool> {
         Ok(x.getattr("ndim")?.extract::<usize>()? == 1
-            && x.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()?)
+            && x.getattr("flags")?
+                .getattr("c_contiguous")?
+                .extract::<bool>()?)
     };
     if !contig(element)? || !contig(test)? {
         return Ok(None);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:55778:
     }
-    let n: usize = element.getattr("shape")?.extract::<Vec<usize>>()?.iter().product();
-    let m: usize = test.getattr("shape")?.extract::<Vec<usize>>()?.iter().product();
+    let n: usize = element
+        .getattr("shape")?
+        .extract::<Vec<usize>>()?
+        .iter()
+        .product();
+    let m: usize = test
+        .getattr("shape")?
+        .extract::<Vec<usize>>()?
+        .iter()
+        .product();
     if n < ISIN_MIN || rayon::current_num_threads() < 2 {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:55794:
             return Ok(true);
         }
         // SAFETY: ReadOnlyCell<f32> repr(transparent); read-only under the GIL.
-        let d: &[f32] = unsafe { std::slice::from_raw_parts(cells.as_ptr().cast::<f32>(), 2 * count) };
-        Ok(d.par_iter().any(|v| v.is_nan() || v.to_bits() == 0x8000_0000))
+        let d: &[f32] =
+            unsafe { std::slice::from_raw_parts(cells.as_ptr().cast::<f32>(), 2 * count) };
+        Ok(d.par_iter()
+            .any(|v| v.is_nan() || v.to_bits() == 0x8000_0000))
     };
     if bad(element, n)? || bad(test, m)? {
         return Ok(None);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:55856:
         return Ok(None);
     }
     if a.getattr("ndim")?.extract::<usize>()? != 1
-        || !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()?
+        || !a
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
     {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:55878:
     use rayon::prelude::*;
     // SAFETY: ReadOnlyCell<f32> is repr(transparent) over f32; read-only under the GIL.
     let src: &[f32] = unsafe { std::slice::from_raw_parts(cells.as_ptr().cast::<f32>(), 2 * n) };
-    if src.par_iter().any(|v| v.is_nan() || v.to_bits() == 0x8000_0000) {
+    if src
+        .par_iter()
+        .any(|v| v.is_nan() || v.to_bits() == 0x8000_0000)
+    {
         return Ok(None);
     }
     let mut pairs: Vec<[f32; 2]> = (0..n).map(|i| [src[2 * i], src[2 * i + 1]]).collect();
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:55938:
         return Ok(None);
     }
     if a.getattr("ndim")?.extract::<usize>()? != 1
-        || !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()?
+        || !a
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
     {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:55981:
         return Ok(None);
     }
     // SAFETY: fresh numpy.empty datetime storage viewed as int64 (no alias with src).
-    let dst: &mut [i64] = unsafe { std::slice::from_raw_parts_mut(out_cells.as_ptr() as *mut i64, nu) };
-    dst.par_iter_mut().zip(ticks.par_iter()).for_each(|(o, &t)| *o = t);
+    let dst: &mut [i64] =
+        unsafe { std::slice::from_raw_parts_mut(out_cells.as_ptr() as *mut i64, nu) };
+    dst.par_iter_mut()
+        .zip(ticks.par_iter())
+        .for_each(|(o, &t)| *o = t);
     Ok(Some(out.unbind()))
 }

Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:56005:
         return Ok(None);
     }
     let ndim = a.getattr("ndim")?.extract::<usize>()?;
-    if ndim < 2 || !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()? {
+    if ndim < 2
+        || !a
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
+    {
         return Ok(None);
     }
     if !axis_spec_is_last(axis_spec, ndim) {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:56015:
     let cols = shape[ndim - 1];
     let rows: usize = shape[..ndim - 1].iter().product();
     const SORT_PARALLEL_MIN: usize = 1 << 20;
-    if rows < 2 || cols < 2 || rows * cols < SORT_PARALLEL_MIN
-        || rayon::current_num_threads() < 2
-    {
+    if rows < 2 || cols < 2 || rows * cols < SORT_PARALLEL_MIN || rayon::current_num_threads() < 2 {
         return Ok(None);
     }
     let view = a.call_method1("view", ("float64",))?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:56034:
     use rayon::prelude::*;
     // SAFETY: ReadOnlyCell<f64> is repr(transparent) over f64; read-only under the GIL.
     let src: &[f64] = unsafe { std::slice::from_raw_parts(cells.as_ptr().cast::<f64>(), 2 * n) };
-    if src.par_iter().any(|v| v.is_nan() || v.to_bits() == 0x8000_0000_0000_0000) {
+    if src
+        .par_iter()
+        .any(|v| v.is_nan() || v.to_bits() == 0x8000_0000_0000_0000)
+    {
         return Ok(None);
     }
     let shape_tuple = PyTuple::new(py, shape.iter().copied())?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:56087:
     let ndim = a.getattr("ndim")?.extract::<usize>()?;
     if ndim < 2
         || !axis_spec_is_first(axis_spec, ndim)
-        || !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()?
+        || !a
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
     {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:56095:
     let rows = shape[0];
     let cols: usize = shape[1..].iter().product();
     const SORT_PARALLEL_MIN: usize = 1 << 20;
-    if rows < 2 || cols < 2 || rows * cols < SORT_PARALLEL_MIN
-        || rayon::current_num_threads() < 2
-    {
+    if rows < 2 || cols < 2 || rows * cols < SORT_PARALLEL_MIN || rayon::current_num_threads() < 2 {
         return Ok(None);
     }
     let view = a.call_method1("view", ("float64",))?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:56114:
     use rayon::prelude::*;
     // SAFETY: ReadOnlyCell<f64> is repr(transparent) over f64; read-only under the GIL.
     let src: &[f64] = unsafe { std::slice::from_raw_parts(cells.as_ptr().cast::<f64>(), 2 * n) };
-    if src.par_iter().any(|v| v.is_nan() || v.to_bits() == 0x8000_0000_0000_0000) {
+    if src
+        .par_iter()
+        .any(|v| v.is_nan() || v.to_bits() == 0x8000_0000_0000_0000)
+    {
         return Ok(None);
     }
     // Gather each column's pairs into a contiguous lane (2*rows f64), then sort the lane's pairs in place.
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:56121:
     let mut vals = vec![0.0f64; 2 * n];
-    vals.par_chunks_mut(2 * rows).enumerate().for_each(|(j, vlane)| {
-        for i in 0..rows {
-            let s = (i * cols + j) * 2;
-            vlane[2 * i] = src[s];
-            vlane[2 * i + 1] = src[s + 1];
-        }
-        // SAFETY: 2*rows f64 reinterpreted as rows [re,im] pairs.
-        let pairs: &mut [[f64; 2]] =
-            unsafe { std::slice::from_raw_parts_mut(vlane.as_mut_ptr().cast::<[f64; 2]>(), rows) };
-        pairs.sort_unstable_by(|x, y| {
-            x[0].partial_cmp(&y[0])
-                .expect("no NaN after check")
-                .then_with(|| x[1].partial_cmp(&y[1]).expect("no NaN"))
+    vals.par_chunks_mut(2 * rows)
+        .enumerate()
+        .for_each(|(j, vlane)| {
+            for i in 0..rows {
+                let s = (i * cols + j) * 2;
+                vlane[2 * i] = src[s];
+                vlane[2 * i + 1] = src[s + 1];
+            }
+            // SAFETY: 2*rows f64 reinterpreted as rows [re,im] pairs.
+            let pairs: &mut [[f64; 2]] = unsafe {
+                std::slice::from_raw_parts_mut(vlane.as_mut_ptr().cast::<[f64; 2]>(), rows)
+            };
+            pairs.sort_unstable_by(|x, y| {
+                x[0].partial_cmp(&y[0])
+                    .expect("no NaN after check")
+                    .then_with(|| x[1].partial_cmp(&y[1]).expect("no NaN"))
+            });
         });
-    });
     let out = numpy.call_method(
         "empty",
         (PyTuple::new(py, shape.iter().copied())?,),
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:56157:
     // SAFETY: vals is 2*n f64 = n [re,im] pairs.
     let vals_pairs: &[[f64; 2]] =
         unsafe { std::slice::from_raw_parts(vals.as_ptr().cast::<[f64; 2]>(), n) };
-    dst_pairs.par_chunks_mut(cols).enumerate().for_each(|(i, orow)| {
-        for (j, slot) in orow.iter_mut().enumerate() {
-            *slot = vals_pairs[j * rows + i];
-        }
-    });
+    dst_pairs
+        .par_chunks_mut(cols)
+        .enumerate()
+        .for_each(|(i, orow)| {
+            for (j, slot) in orow.iter_mut().enumerate() {
+                *slot = vals_pairs[j * rows + i];
+            }
+        });
     Ok(Some(out.unbind()))
 }

Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:56184:
         return Ok(None);
     }
     let ndim = a.getattr("ndim")?.extract::<usize>()?;
-    if ndim < 3 || !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()? {
+    if ndim < 3
+        || !a
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
+    {
         return Ok(None);
     }
     let Some(ax) = axis_spec_middle(axis_spec, ndim) else {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:56196:
     let inner: usize = shape[ax + 1..].iter().product();
     let lanes = outer * inner;
     const SORT_PARALLEL_MIN: usize = 1 << 20;
-    if alen < 2 || lanes < 2 || outer * alen * inner < SORT_PARALLEL_MIN
+    if alen < 2
+        || lanes < 2
+        || outer * alen * inner < SORT_PARALLEL_MIN
         || rayon::current_num_threads() < 2
     {
         return Ok(None);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:56215:
     use rayon::prelude::*;
     // SAFETY: ReadOnlyCell<f64> is repr(transparent) over f64; read-only under the GIL.
     let src: &[f64] = unsafe { std::slice::from_raw_parts(cells.as_ptr().cast::<f64>(), 2 * n) };
-    if src.par_iter().any(|v| v.is_nan() || v.to_bits() == 0x8000_0000_0000_0000) {
+    if src
+        .par_iter()
+        .any(|v| v.is_nan() || v.to_bits() == 0x8000_0000_0000_0000)
+    {
         return Ok(None);
     }
     // Gather each lane's pairs into a contiguous lane (2*alen f64), then sort the lane's pairs in place.
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:56222:
     let mut vals = vec![0.0f64; 2 * n];
-    vals.par_chunks_mut(2 * alen).enumerate().for_each(|(lane, vlane)| {
-        let o = lane / inner;
-        let t = lane % inner;
-        let base = o * alen * inner + t;
-        for j in 0..alen {
-            let s = (base + j * inner) * 2;
-            vlane[2 * j] = src[s];
-            vlane[2 * j + 1] = src[s + 1];
-        }
-        // SAFETY: 2*alen f64 reinterpreted as alen [re,im] pairs.
-        let pairs: &mut [[f64; 2]] =
-            unsafe { std::slice::from_raw_parts_mut(vlane.as_mut_ptr().cast::<[f64; 2]>(), alen) };
-        pairs.sort_unstable_by(|x, y| {
-            x[0].partial_cmp(&y[0])
-                .expect("no NaN after check")
-                .then_with(|| x[1].partial_cmp(&y[1]).expect("no NaN"))
+    vals.par_chunks_mut(2 * alen)
+        .enumerate()
+        .for_each(|(lane, vlane)| {
+            let o = lane / inner;
+            let t = lane % inner;
+            let base = o * alen * inner + t;
+            for j in 0..alen {
+                let s = (base + j * inner) * 2;
+                vlane[2 * j] = src[s];
+                vlane[2 * j + 1] = src[s + 1];
+            }
+            // SAFETY: 2*alen f64 reinterpreted as alen [re,im] pairs.
+            let pairs: &mut [[f64; 2]] = unsafe {
+                std::slice::from_raw_parts_mut(vlane.as_mut_ptr().cast::<[f64; 2]>(), alen)
+            };
+            pairs.sort_unstable_by(|x, y| {
+                x[0].partial_cmp(&y[0])
+                    .expect("no NaN after check")
+                    .then_with(|| x[1].partial_cmp(&y[1]).expect("no NaN"))
+            });
         });
-    });
     let out = numpy.call_method(
         "empty",
         (PyTuple::new(py, shape.iter().copied())?,),
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:56262:
     let vals_pairs: &[[f64; 2]] =
         unsafe { std::slice::from_raw_parts(vals.as_ptr().cast::<[f64; 2]>(), n) };
     // Scatter: output chunk c (length inner) holds fixed (o, j) varying t. c = o*alen + j.
-    dst_pairs.par_chunks_mut(inner).enumerate().for_each(|(c, ochunk)| {
-        let o = c / alen;
-        let j = c % alen;
-        for (t, slot) in ochunk.iter_mut().enumerate() {
-            *slot = vals_pairs[(o * inner + t) * alen + j];
-        }
-    });
+    dst_pairs
+        .par_chunks_mut(inner)
+        .enumerate()
+        .for_each(|(c, ochunk)| {
+            let o = c / alen;
+            let j = c % alen;
+            for (t, slot) in ochunk.iter_mut().enumerate() {
+                *slot = vals_pairs[(o * inner + t) * alen + j];
+            }
+        });
     Ok(Some(out.unbind()))
 }

Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:56292:
         return Ok(None);
     }
     if a.getattr("ndim")?.extract::<usize>()? != 1
-        || !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()?
+        || !a
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
     {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:56314:
     use rayon::prelude::*;
     // SAFETY: ReadOnlyCell<f32> is repr(transparent) over f32; read-only under the GIL.
     let src: &[f32] = unsafe { std::slice::from_raw_parts(cells.as_ptr().cast::<f32>(), 2 * n) };
-    if src.par_iter().any(|v| v.is_nan() || v.to_bits() == 0x8000_0000) {
+    if src
+        .par_iter()
+        .any(|v| v.is_nan() || v.to_bits() == 0x8000_0000)
+    {
         return Ok(None);
     }
     let kwargs = PyDict::new(py);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:56362:
         return Ok(None);
     }
     let ndim = a.getattr("ndim")?.extract::<usize>()?;
-    if ndim < 2 || !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()? {
+    if ndim < 2
+        || !a
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
+    {
         return Ok(None);
     }
     if !axis_spec_is_last(axis_spec, ndim) {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:56372:
     let cols = shape[ndim - 1];
     let rows: usize = shape[..ndim - 1].iter().product();
     const SORT_PARALLEL_MIN: usize = 1 << 20;
-    if rows < 2 || cols < 2 || rows * cols < SORT_PARALLEL_MIN
-        || rayon::current_num_threads() < 2
-    {
+    if rows < 2 || cols < 2 || rows * cols < SORT_PARALLEL_MIN || rayon::current_num_threads() < 2 {
         return Ok(None);
     }
     let view = a.call_method1("view", ("float32",))?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:56391:
     use rayon::prelude::*;
     // SAFETY: ReadOnlyCell<f32> is repr(transparent) over f32; read-only under the GIL.
     let src: &[f32] = unsafe { std::slice::from_raw_parts(cells.as_ptr().cast::<f32>(), 2 * n) };
-    if src.par_iter().any(|v| v.is_nan() || v.to_bits() == 0x8000_0000) {
+    if src
+        .par_iter()
+        .any(|v| v.is_nan() || v.to_bits() == 0x8000_0000)
+    {
         return Ok(None);
     }
     let shape_tuple = PyTuple::new(py, shape.iter().copied())?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:56442:
     let ndim = a.getattr("ndim")?.extract::<usize>()?;
     if ndim < 2
         || !axis_spec_is_first(axis_spec, ndim)
-        || !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()?
+        || !a
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
     {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:56450:
     let rows = shape[0];
     let cols: usize = shape[1..].iter().product();
     const SORT_PARALLEL_MIN: usize = 1 << 20;
-    if rows < 2 || cols < 2 || rows * cols < SORT_PARALLEL_MIN
-        || rayon::current_num_threads() < 2
-    {
+    if rows < 2 || cols < 2 || rows * cols < SORT_PARALLEL_MIN || rayon::current_num_threads() < 2 {
         return Ok(None);
     }
     let view = a.call_method1("view", ("float32",))?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:56469:
     use rayon::prelude::*;
     // SAFETY: ReadOnlyCell<f32> is repr(transparent) over f32; read-only under the GIL.
     let src: &[f32] = unsafe { std::slice::from_raw_parts(cells.as_ptr().cast::<f32>(), 2 * n) };
-    if src.par_iter().any(|v| v.is_nan() || v.to_bits() == 0x8000_0000) {
+    if src
+        .par_iter()
+        .any(|v| v.is_nan() || v.to_bits() == 0x8000_0000)
+    {
         return Ok(None);
     }
     let mut vals = vec![0.0f32; 2 * n];
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:56476:
-    vals.par_chunks_mut(2 * rows).enumerate().for_each(|(j, vlane)| {
-        for i in 0..rows {
-            let s = (i * cols + j) * 2;
-            vlane[2 * i] = src[s];
-            vlane[2 * i + 1] = src[s + 1];
-        }
-        // SAFETY: 2*rows f32 reinterpreted as rows [re,im] pairs.
-        let pairs: &mut [[f32; 2]] =
-            unsafe { std::slice::from_raw_parts_mut(vlane.as_mut_ptr().cast::<[f32; 2]>(), rows) };
-        pairs.sort_unstable_by(|x, y| {
-            x[0].partial_cmp(&y[0])
-                .expect("no NaN after check")
-                .then_with(|| x[1].partial_cmp(&y[1]).expect("no NaN"))
+    vals.par_chunks_mut(2 * rows)
+        .enumerate()
+        .for_each(|(j, vlane)| {
+            for i in 0..rows {
+                let s = (i * cols + j) * 2;
+                vlane[2 * i] = src[s];
+                vlane[2 * i + 1] = src[s + 1];
+            }
+            // SAFETY: 2*rows f32 reinterpreted as rows [re,im] pairs.
+            let pairs: &mut [[f32; 2]] = unsafe {
+                std::slice::from_raw_parts_mut(vlane.as_mut_ptr().cast::<[f32; 2]>(), rows)
+            };
+            pairs.sort_unstable_by(|x, y| {
+                x[0].partial_cmp(&y[0])
+                    .expect("no NaN after check")
+                    .then_with(|| x[1].partial_cmp(&y[1]).expect("no NaN"))
+            });
         });
-    });
     let out = numpy.call_method(
         "empty",
         (PyTuple::new(py, shape.iter().copied())?,),
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:56511:
     // SAFETY: vals is 2*n f32 = n [re,im] pairs.
     let vals_pairs: &[[f32; 2]] =
         unsafe { std::slice::from_raw_parts(vals.as_ptr().cast::<[f32; 2]>(), n) };
-    dst_pairs.par_chunks_mut(cols).enumerate().for_each(|(i, orow)| {
-        for (j, slot) in orow.iter_mut().enumerate() {
-            *slot = vals_pairs[j * rows + i];
-        }
-    });
+    dst_pairs
+        .par_chunks_mut(cols)
+        .enumerate()
+        .for_each(|(i, orow)| {
+            for (j, slot) in orow.iter_mut().enumerate() {
+                *slot = vals_pairs[j * rows + i];
+            }
+        });
     Ok(Some(out.unbind()))
 }

Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:56536:
         return Ok(None);
     }
     let ndim = a.getattr("ndim")?.extract::<usize>()?;
-    if ndim < 3 || !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()? {
+    if ndim < 3
+        || !a
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
+    {
         return Ok(None);
     }
     let Some(ax) = axis_spec_middle(axis_spec, ndim) else {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:56548:
     let inner: usize = shape[ax + 1..].iter().product();
     let lanes = outer * inner;
     const SORT_PARALLEL_MIN: usize = 1 << 20;
-    if alen < 2 || lanes < 2 || outer * alen * inner < SORT_PARALLEL_MIN
+    if alen < 2
+        || lanes < 2
+        || outer * alen * inner < SORT_PARALLEL_MIN
         || rayon::current_num_threads() < 2
     {
         return Ok(None);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:56567:
     use rayon::prelude::*;
     // SAFETY: ReadOnlyCell<f32> is repr(transparent) over f32; read-only under the GIL.
     let src: &[f32] = unsafe { std::slice::from_raw_parts(cells.as_ptr().cast::<f32>(), 2 * n) };
-    if src.par_iter().any(|v| v.is_nan() || v.to_bits() == 0x8000_0000) {
+    if src
+        .par_iter()
+        .any(|v| v.is_nan() || v.to_bits() == 0x8000_0000)
+    {
         return Ok(None);
     }
     let mut vals = vec![0.0f32; 2 * n];
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:56574:
-    vals.par_chunks_mut(2 * alen).enumerate().for_each(|(lane, vlane)| {
-        let o = lane / inner;
-        let t = lane % inner;
-        let base = o * alen * inner + t;
-        for j in 0..alen {
-            let s = (base + j * inner) * 2;
-            vlane[2 * j] = src[s];
-            vlane[2 * j + 1] = src[s + 1];
-        }
-        // SAFETY: 2*alen f32 reinterpreted as alen [re,im] pairs.
-        let pairs: &mut [[f32; 2]] =
-            unsafe { std::slice::from_raw_parts_mut(vlane.as_mut_ptr().cast::<[f32; 2]>(), alen) };
-        pairs.sort_unstable_by(|x, y| {
-            x[0].partial_cmp(&y[0])
-                .expect("no NaN after check")
-                .then_with(|| x[1].partial_cmp(&y[1]).expect("no NaN"))
+    vals.par_chunks_mut(2 * alen)
+        .enumerate()
+        .for_each(|(lane, vlane)| {
+            let o = lane / inner;
+            let t = lane % inner;
+            let base = o * alen * inner + t;
+            for j in 0..alen {
+                let s = (base + j * inner) * 2;
+                vlane[2 * j] = src[s];
+                vlane[2 * j + 1] = src[s + 1];
+            }
+            // SAFETY: 2*alen f32 reinterpreted as alen [re,im] pairs.
+            let pairs: &mut [[f32; 2]] = unsafe {
+                std::slice::from_raw_parts_mut(vlane.as_mut_ptr().cast::<[f32; 2]>(), alen)
+            };
+            pairs.sort_unstable_by(|x, y| {
+                x[0].partial_cmp(&y[0])
+                    .expect("no NaN after check")
+                    .then_with(|| x[1].partial_cmp(&y[1]).expect("no NaN"))
+            });
         });
-    });
     let out = numpy.call_method(
         "empty",
         (PyTuple::new(py, shape.iter().copied())?,),
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:56612:
     // SAFETY: vals is 2*n f32 = n [re,im] pairs.
     let vals_pairs: &[[f32; 2]] =
         unsafe { std::slice::from_raw_parts(vals.as_ptr().cast::<[f32; 2]>(), n) };
-    dst_pairs.par_chunks_mut(inner).enumerate().for_each(|(c, ochunk)| {
-        let o = c / alen;
-        let j = c % alen;
-        for (t, slot) in ochunk.iter_mut().enumerate() {
-            *slot = vals_pairs[(o * inner + t) * alen + j];
-        }
-    });
+    dst_pairs
+        .par_chunks_mut(inner)
+        .enumerate()
+        .for_each(|(c, ochunk)| {
+            let o = c / alen;
+            let j = c % alen;
+            for (t, slot) in ochunk.iter_mut().enumerate() {
+                *slot = vals_pairs[(o * inner + t) * alen + j];
+            }
+        });
     Ok(Some(out.unbind()))
 }

Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:56653:
         return Ok(None);
     };
     // SAFETY: fresh numpy.empty buffer we own (no alias with src).
-    let dst: &mut [T] =
-        unsafe { std::slice::from_raw_parts_mut(out_cells.as_ptr() as *mut T, n) };
+    let dst: &mut [T] = unsafe { std::slice::from_raw_parts_mut(out_cells.as_ptr() as *mut T, n) };
     dst.copy_from_slice(src);
     dst.par_sort_unstable();
     Ok(Some(out.unbind()))
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:56672:
         return Ok(None);
     }
     if a.getattr("ndim")?.extract::<usize>()? != 1
-        || !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()?
+        || !a
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
     {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:56681:
     if kind != "i" && kind != "u" {
         return Ok(None);
     }
-    let n: usize = a.getattr("shape")?.extract::<Vec<usize>>()?.iter().product();
+    let n: usize = a
+        .getattr("shape")?
+        .extract::<Vec<usize>>()?
+        .iter()
+        .product();
     if n < SORT_PARALLEL_MIN || rayon::current_num_threads() < 2 {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:56718:
         return Ok(None);
     }
     if a.getattr("ndim")?.extract::<usize>()? != 1
-        || !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()?
+        || !a
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
     {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:56732:
     if itemsize == 0 || itemsize > 4096 || (!is_bytes && itemsize % 4 != 0) {
         return Ok(None);
     }
-    let n: usize = a.getattr("shape")?.extract::<Vec<usize>>()?.iter().product();
+    let n: usize = a
+        .getattr("shape")?
+        .extract::<Vec<usize>>()?
+        .iter()
+        .product();
     if n < STRING_SORT_MIN || rayon::current_num_threads() < 2 {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:56750:
     }
     use rayon::prelude::*;
     // SAFETY: ReadOnlyCell<u8> is repr(transparent) over u8; read-only under the GIL.
-    let in_data: &[u8] = unsafe { std::slice::from_raw_parts(in_cells.as_ptr().cast::<u8>(), nbytes) };
+    let in_data: &[u8] =
+        unsafe { std::slice::from_raw_parts(in_cells.as_ptr().cast::<u8>(), nbytes) };
     // 'U' only: defer if any codepoint >= 0x100 (a UCS4 char has a nonzero high byte) since byte
     // order would then diverge from numpy's codepoint order. 'S' bytes are always in byte order.
     if !is_bytes
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:56826:
         return Ok(None);
     }
     if a.getattr("ndim")?.extract::<usize>()? != 1
-        || !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()?
+        || !a
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
     {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:56840:
     if itemsize == 0 || itemsize > 4096 || (!is_bytes && itemsize % 4 != 0) {
         return Ok(None);
     }
-    let n: usize = a.getattr("shape")?.extract::<Vec<usize>>()?.iter().product();
+    let n: usize = a
+        .getattr("shape")?
+        .extract::<Vec<usize>>()?
+        .iter()
+        .product();
     if n < STRING_ARGSORT_MIN || rayon::current_num_threads() < 2 {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:56858:
     }
     use rayon::prelude::*;
     // SAFETY: ReadOnlyCell<u8> is repr(transparent) over u8; read-only under the GIL.
-    let in_data: &[u8] = unsafe { std::slice::from_raw_parts(in_cells.as_ptr().cast::<u8>(), nbytes) };
+    let in_data: &[u8] =
+        unsafe { std::slice::from_raw_parts(in_cells.as_ptr().cast::<u8>(), nbytes) };
     // Narrow fixed-width Latin-1 records (S1..8 / U1..8) pack losslessly + ORDER-PRESERVINGLY into a big-endian
     // u64 (top bytes = leading codepoints), so a gather-free (key, index) pair sort reproduces the memcmp order
     // without chasing two cache-cold records per compare. The packed key captures every codepoint of the record,
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:56865:
     // so key equality == record equality and the index tie-break gives numpy's stable argsort exactly. Wider
     // records / wide (>0xFF) codepoints fall through to the memcmp path below (pack returns None on wide bytes).
     if let Some(key_width) = packed_string_key_width(&kind, itemsize) {
-        if let Some(keys) = pack_fixed_width_string_keys(in_data, n, itemsize, key_width, is_bytes) {
+        if let Some(keys) = pack_fixed_width_string_keys(in_data, n, itemsize, key_width, is_bytes)
+        {
             let mut pairs: Vec<(u64, u32)> = (0..n as u32).map(|i| (keys[i as usize], i)).collect();
             pairs.par_sort_unstable();
             let out = numpy.call_method("empty", ((n,), "intp"), None)?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:56896:
     let mut perm: Vec<u32> = (0..n as u32).collect();
     perm.par_sort_unstable_by(|&i, &j| {
         let (bi, bj) = (i as usize * itemsize, j as usize * itemsize);
-        in_data[bi..bi + itemsize].cmp(&in_data[bj..bj + itemsize]).then_with(|| i.cmp(&j))
+        in_data[bi..bi + itemsize]
+            .cmp(&in_data[bj..bj + itemsize])
+            .then_with(|| i.cmp(&j))
     });
     let out = numpy.call_method("empty", ((n,), "intp"), None)?;
     let out_buf = PyBuffer::<i64>::get(&out)?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:56907:
         return Ok(None);
     }
     // SAFETY: fresh numpy.empty intp output, no alias.
-    let o: &mut [i64] = unsafe { std::slice::from_raw_parts_mut(out_cells.as_ptr() as *mut i64, n) };
-    o.par_iter_mut().zip(perm.par_iter()).for_each(|(dst, &p)| *dst = p as i64);
+    let o: &mut [i64] =
+        unsafe { std::slice::from_raw_parts_mut(out_cells.as_ptr() as *mut i64, n) };
+    o.par_iter_mut()
+        .zip(perm.par_iter())
+        .for_each(|(dst, &p)| *dst = p as i64);
     Ok(Some(out.unbind()))
 }

Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:57021:
         return Ok(None);
     };
     let is_bytes = kind == "S";
-    let na: usize = a.getattr("shape")?.extract::<Vec<usize>>()?.iter().product();
-    let nb: usize = b.getattr("shape")?.extract::<Vec<usize>>()?.iter().product();
+    let na: usize = a
+        .getattr("shape")?
+        .extract::<Vec<usize>>()?
+        .iter()
+        .product();
+    let nb: usize = b
+        .getattr("shape")?
+        .extract::<Vec<usize>>()?
+        .iter()
+        .product();
     let nc = na + nb;
     if nc < MIN || rayon::current_num_threads() < 2 {
         return Ok(None);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:57079:
         return Ok(None);
     };
     let is_bytes = kind == "S";
-    let na: usize = a.getattr("shape")?.extract::<Vec<usize>>()?.iter().product();
-    let nb: usize = b.getattr("shape")?.extract::<Vec<usize>>()?.iter().product();
+    let na: usize = a
+        .getattr("shape")?
+        .extract::<Vec<usize>>()?
+        .iter()
+        .product();
+    let nb: usize = b
+        .getattr("shape")?
+        .extract::<Vec<usize>>()?
+        .iter()
+        .product();
     let nc = na + nb;
     if nc < MIN || rayon::current_num_threads() < 2 {
         return Ok(None);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:57111:
         None => return Ok(None),
     };
     let mut tagged = Vec::with_capacity(nc);
-    tagged.extend(a_keys.into_iter().map(|key| PackedStringTaggedKey { key, from_a: true }));
-    tagged.extend(b_keys.into_iter().map(|key| PackedStringTaggedKey { key, from_a: false }));
+    tagged.extend(
+        a_keys
+            .into_iter()
+            .map(|key| PackedStringTaggedKey { key, from_a: true }),
+    );
+    tagged.extend(
+        b_keys
+            .into_iter()
+            .map(|key| PackedStringTaggedKey { key, from_a: false }),
+    );
     use rayon::prelude::*;
     tagged.par_sort_unstable_by_key(|entry| entry.key);

Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:57212:
     }
     let contig = |x: &Bound<'_, PyAny>| -> PyResult<bool> {
         Ok(x.getattr("ndim")?.extract::<usize>()? == 1
-            && x.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()?)
+            && x.getattr("flags")?
+                .getattr("c_contiguous")?
+                .extract::<bool>()?)
     };
     if !contig(a)? || !contig(b)? {
         return Ok(None);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:57219:
     }
-    let na: usize = a.getattr("shape")?.extract::<Vec<usize>>()?.iter().product();
-    let nb: usize = b.getattr("shape")?.extract::<Vec<usize>>()?.iter().product();
+    let na: usize = a
+        .getattr("shape")?
+        .extract::<Vec<usize>>()?
+        .iter()
+        .product();
+    let nb: usize = b
+        .getattr("shape")?
+        .extract::<Vec<usize>>()?
+        .iter()
+        .product();
     if na < MIN || rayon::current_num_threads() < 2 {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:57236:
     }
     use rayon::prelude::*;
     // SAFETY: ReadOnlyCell<u8> repr(transparent) over u8; read-only under the GIL.
-    let a_data: &[u8] = unsafe { std::slice::from_raw_parts(a_cells.as_ptr().cast::<u8>(), na * itemsize) };
-    let b_data: &[u8] = unsafe { std::slice::from_raw_parts(b_cells.as_ptr().cast::<u8>(), nb * itemsize) };
+    let a_data: &[u8] =
+        unsafe { std::slice::from_raw_parts(a_cells.as_ptr().cast::<u8>(), na * itemsize) };
+    let b_data: &[u8] =
+        unsafe { std::slice::from_raw_parts(b_cells.as_ptr().cast::<u8>(), nb * itemsize) };
     // 'U' only: wide codepoints break byte==codepoint order; 'S' bytes are always in byte order.
-    let wide = |d: &[u8]| d.par_chunks_exact(4).any(|c| c[1] != 0 || c[2] != 0 || c[3] != 0);
+    let wide = |d: &[u8]| {
+        d.par_chunks_exact(4)
+            .any(|c| c[1] != 0 || c[2] != 0 || c[3] != 0)
+    };
     if !is_bytes && (wide(a_data) || wide(b_data)) {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:57285:
                 }
             }
         }
-        return packed_string_keys_to_numpy(
-            py, numpy, &a_dt, &keep, itemsize, key_width, is_bytes,
-        );
+        return packed_string_keys_to_numpy(py, numpy, &a_dt, &keep, itemsize, key_width, is_bytes);
     }
     // FNV set of b records (exact membership; verified on lookup).
     let mut set_b: std::collections::HashSet<&[u8], FastIntBuildHasher> =
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:57387:
     }
     let contig = |x: &Bound<'_, PyAny>| -> PyResult<bool> {
         Ok(x.getattr("ndim")?.extract::<usize>()? == 1
-            && x.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()?)
+            && x.getattr("flags")?
+                .getattr("c_contiguous")?
+                .extract::<bool>()?)
     };
     if !contig(a)? || !contig(b)? {
         return Ok(None);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:57395:
     if let Some(out) = try_packed_string_setxor(py, numpy, a, b, &a_dt, &a_kind, itemsize)? {
         return Ok(Some(out));
     }
-    let na: usize = a.getattr("shape")?.extract::<Vec<usize>>()?.iter().product();
-    let nb: usize = b.getattr("shape")?.extract::<Vec<usize>>()?.iter().product();
+    let na: usize = a
+        .getattr("shape")?
+        .extract::<Vec<usize>>()?
+        .iter()
+        .product();
+    let nb: usize = b
+        .getattr("shape")?
+        .extract::<Vec<usize>>()?
+        .iter()
+        .product();
     let nc = na + nb;
     if nc < MIN || rayon::current_num_threads() < 2 {
         return Ok(None);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:57404:
     // Concatenate a then b so the source array is recoverable from the combined index (< na => a).
     let combined = numpy.call_method1(
         "concatenate",
-        (PyTuple::new(py, [a.call_method0("ravel")?, b.call_method0("ravel")?])?,),
+        (PyTuple::new(
+            py,
+            [a.call_method0("ravel")?, b.call_method0("ravel")?],
+        )?,),
     )?;
     let uint8 = numpy.getattr("uint8")?;
     let c_u8 = combined.call_method1("view", (&uint8,))?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:57419:
     }
     use rayon::prelude::*;
     // SAFETY: ReadOnlyCell<u8> repr(transparent) over u8; read-only under the GIL.
-    let c_data: &[u8] = unsafe { std::slice::from_raw_parts(c_cells.as_ptr().cast::<u8>(), nc * itemsize) };
+    let c_data: &[u8] =
+        unsafe { std::slice::from_raw_parts(c_cells.as_ptr().cast::<u8>(), nc * itemsize) };
     // 'U' only: wide codepoints break byte==codepoint order; 'S' bytes are always in byte order.
     if !is_bytes
         && c_data
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:57508:
         return Ok(None);
     }
     if a.getattr("ndim")?.extract::<usize>()? != 1
-        || !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()?
+        || !a
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
     {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:57522:
     if itemsize == 0 || itemsize > 4096 || (!is_bytes && itemsize % 4 != 0) {
         return Ok(None);
     }
-    let n: usize = a.getattr("shape")?.extract::<Vec<usize>>()?.iter().product();
+    let n: usize = a
+        .getattr("shape")?
+        .extract::<Vec<usize>>()?
+        .iter()
+        .product();
     if n < STRING_UNIQUE_MIN || rayon::current_num_threads() < 2 {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:57540:
     }
     use rayon::prelude::*;
     // SAFETY: ReadOnlyCell<u8> is repr(transparent) over u8; read-only under the GIL.
-    let in_data: &[u8] = unsafe { std::slice::from_raw_parts(in_cells.as_ptr().cast::<u8>(), nbytes) };
+    let in_data: &[u8] =
+        unsafe { std::slice::from_raw_parts(in_cells.as_ptr().cast::<u8>(), nbytes) };
     // 'U' only: wide codepoints break byte==codepoint order; 'S' bytes are always in byte order.
     if !is_bytes
         && in_data
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:57636:
         return Ok(None);
     }
     if a.getattr("ndim")?.extract::<usize>()? != 1
-        || !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()?
+        || !a
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
     {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:57650:
     if itemsize == 0 || itemsize > 4096 || (!is_bytes && itemsize % 4 != 0) {
         return Ok(None);
     }
-    let n: usize = a.getattr("shape")?.extract::<Vec<usize>>()?.iter().product();
+    let n: usize = a
+        .getattr("shape")?
+        .extract::<Vec<usize>>()?
+        .iter()
+        .product();
     if n < STRING_UNIQUE_FULL_MIN || n > u32::MAX as usize || rayon::current_num_threads() < 2 {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:57668:
     }
     use rayon::prelude::*;
     // SAFETY: ReadOnlyCell<u8> is repr(transparent) over u8; read-only under the GIL.
-    let in_data: &[u8] = unsafe { std::slice::from_raw_parts(in_cells.as_ptr().cast::<u8>(), nbytes) };
+    let in_data: &[u8] =
+        unsafe { std::slice::from_raw_parts(in_cells.as_ptr().cast::<u8>(), nbytes) };
     if !is_bytes
         && in_data
             .par_chunks_exact(4)
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:57764:
             return Ok(None);
         };
         // SAFETY: fresh numpy.empty intp output; each slot written once.
-        let idx: &mut [i64] = unsafe { std::slice::from_raw_parts_mut(cells.as_ptr() as *mut i64, nu) };
+        let idx: &mut [i64] =
+            unsafe { std::slice::from_raw_parts_mut(cells.as_ptr() as *mut i64, nu) };
         idx.par_iter_mut()
             .zip(group_starts.par_iter())
             .for_each(|(slot, &gs)| *slot = orig[gs] as i64);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:57781:
             return Ok(None);
         };
         // SAFETY: fresh numpy.empty intp output; each original index is assigned exactly once.
-        let inv: &mut [i64] = unsafe { std::slice::from_raw_parts_mut(cells.as_ptr() as *mut i64, n) };
+        let inv: &mut [i64] =
+            unsafe { std::slice::from_raw_parts_mut(cells.as_ptr() as *mut i64, n) };
         for (group, &gs) in group_starts.iter().enumerate() {
-            let end = if group + 1 < nu { group_starts[group + 1] } else { n };
+            let end = if group + 1 < nu {
+                group_starts[group + 1]
+            } else {
+                n
+            };
             for &original in &orig[gs..end] {
                 inv[original as usize] = group as i64;
             }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:57805:
         // SAFETY: fresh numpy.empty intp output; each slot written once.
         let counts: &mut [i64] =
             unsafe { std::slice::from_raw_parts_mut(cells.as_ptr() as *mut i64, nu) };
-        counts
-            .par_iter_mut()
-            .enumerate()
-            .for_each(|(group, slot)| {
-                let end = if group + 1 < nu { group_starts[group + 1] } else { n };
-                *slot = (end - group_starts[group]) as i64;
-            });
+        counts.par_iter_mut().enumerate().for_each(|(group, slot)| {
+            let end = if group + 1 < nu {
+                group_starts[group + 1]
+            } else {
+                n
+            };
+            *slot = (end - group_starts[group]) as i64;
+        });
         outs.push(cnt_arr);
     }
     Ok(Some(PyTuple::new(py, outs)?.into_any().unbind()))
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:57828:
         return Ok(true);
     };
     // SAFETY: ReadOnlyCell<i64> repr(transparent); read-only under the GIL.
-    let data: &[i64] = unsafe { std::slice::from_raw_parts(cells.as_ptr().cast::<i64>(), cells.len()) };
+    let data: &[i64] =
+        unsafe { std::slice::from_raw_parts(cells.as_ptr().cast::<i64>(), cells.len()) };
     Ok(data.par_iter().any(|&v| v == i64::MIN))
 }

Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:57913:
     // Widen exact -> f32 unique (fnp fast path or numpy) -> narrow the sorted-unique result back to f16.
     let f32 = item.call_method1("astype", ("float32",))?;
     let u = unique(py, &PyTuple::new(py, [f32])?, None)?;
-    Ok(Some(u.bind(py).call_method1("astype", ("float16",))?.unbind()))
+    Ok(Some(
+        u.bind(py).call_method1("astype", ("float16",))?.unbind(),
+    ))
 }

 fn try_native_f16_isin(
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:57972:
         DtSetOp::Setdiff => setdiff1d(py, a32, b32, false)?,
         DtSetOp::Setxor => setxor1d(py, a32, b32, false)?,
     };
-    Ok(Some(r.bind(py).call_method1("astype", ("float16",))?.unbind()))
+    Ok(Some(
+        r.bind(py).call_method1("astype", ("float16",))?.unbind(),
+    ))
 }

 // np.searchsorted into a SORTED 1-D fixed-width unicode ('U') haystack with a 1-D 'U' query array of the
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:58024:
         }
     }
     if a_arr.getattr("ndim")?.extract::<usize>()? != 1
-        || !a_arr.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()?
+        || !a_arr
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
     {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:58032:
         return Ok(None);
     }
     if v.getattr("ndim")?.extract::<usize>()? != 1
-        || !v.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()?
+        || !v
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
     {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:58039:
-    let n: usize = a_arr.getattr("shape")?.extract::<Vec<usize>>()?.iter().product();
-    let m: usize = v.getattr("shape")?.extract::<Vec<usize>>()?.iter().product();
+    let n: usize = a_arr
+        .getattr("shape")?
+        .extract::<Vec<usize>>()?
+        .iter()
+        .product();
+    let m: usize = v
+        .getattr("shape")?
+        .extract::<Vec<usize>>()?
+        .iter()
+        .product();
     if n < 1 || m < QUERY_MIN || rayon::current_num_threads() < 2 {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:58084:
     }
     for arr in [a_arr, v] {
         if arr.getattr("ndim")?.extract::<usize>()? != 1
-            || !arr.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()?
+            || !arr
+                .getattr("flags")?
+                .getattr("c_contiguous")?
+                .extract::<bool>()?
         {
             return Ok(None);
         }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:58122:
     if expected_off != itemsize {
         return Ok(None);
     }
-    if has_float && (!struct_all_floats_finite(py, numpy, a_arr)? || !struct_all_floats_finite(py, numpy, v)?) {
+    if has_float
+        && (!struct_all_floats_finite(py, numpy, a_arr)?
+            || !struct_all_floats_finite(py, numpy, v)?)
+    {
         return Ok(None);
     }
-    let n: usize = a_arr.getattr("shape")?.extract::<Vec<usize>>()?.iter().product();
-    let m: usize = v.getattr("shape")?.extract::<Vec<usize>>()?.iter().product();
+    let n: usize = a_arr
+        .getattr("shape")?
+        .extract::<Vec<usize>>()?
+        .iter()
+        .product();
+    let m: usize = v
+        .getattr("shape")?
+        .extract::<Vec<usize>>()?
+        .iter()
+        .product();
     if n < 1 || m < QUERY_MIN || rayon::current_num_threads() < 2 {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:58144:
     }
     use rayon::prelude::*;
     // SAFETY: ReadOnlyCell<u8> repr(transparent); read-only under the GIL.
-    let a_data: &[u8] = unsafe { std::slice::from_raw_parts(a_cells.as_ptr().cast::<u8>(), n * itemsize) };
-    let v_data: &[u8] = unsafe { std::slice::from_raw_parts(v_cells.as_ptr().cast::<u8>(), m * itemsize) };
+    let a_data: &[u8] =
+        unsafe { std::slice::from_raw_parts(a_cells.as_ptr().cast::<u8>(), n * itemsize) };
+    let v_data: &[u8] =
+        unsafe { std::slice::from_raw_parts(v_cells.as_ptr().cast::<u8>(), m * itemsize) };
     let build_keys = |data: &[u8], count: usize| -> Vec<u8> {
         let mut keys = vec![0u8; count * itemsize];
         keys.par_chunks_exact_mut(itemsize)
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:58169:
         return Ok(None);
     }
     // SAFETY: fresh numpy.empty int64 output, no alias with hkeys/qkeys.
-    let o: &mut [i64] = unsafe { std::slice::from_raw_parts_mut(out_cells.as_ptr() as *mut i64, m) };
+    let o: &mut [i64] =
+        unsafe { std::slice::from_raw_parts_mut(out_cells.as_ptr() as *mut i64, m) };
     o.par_iter_mut().enumerate().for_each(|(j, slot)| {
         let qk = &qkeys[j * itemsize..(j + 1) * itemsize];
         let (mut lo, mut hi) = (0usize, n);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:58218:
     }
     use rayon::prelude::*;
     // SAFETY: ReadOnlyCell<T> repr(transparent); read-only under the GIL.
-    let a_data: &[T] = unsafe { std::slice::from_raw_parts(a_cells.as_ptr().cast::<T>(), n * nfields) };
-    let v_data: &[T] = unsafe { std::slice::from_raw_parts(v_cells.as_ptr().cast::<T>(), m * nfields) };
+    let a_data: &[T] =
+        unsafe { std::slice::from_raw_parts(a_cells.as_ptr().cast::<T>(), n * nfields) };
+    let v_data: &[T] =
+        unsafe { std::slice::from_raw_parts(v_cells.as_ptr().cast::<T>(), m * nfields) };
     if nfields == 2 {
         if let Some(out) =
             searchsorted_struct_pair_prefix_index(py, numpy, a_data, v_data, side, n, m)?
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:58246:
         return Ok(None);
     }
     // SAFETY: fresh numpy.empty int64 output, no alias with a_data/v_data.
-    let o: &mut [i64] = unsafe { std::slice::from_raw_parts_mut(out_cells.as_ptr() as *mut i64, m) };
+    let o: &mut [i64] =
+        unsafe { std::slice::from_raw_parts_mut(out_cells.as_ptr() as *mut i64, m) };
     o.par_iter_mut().enumerate().for_each(|(j, slot)| {
         let q = &v_data[j * nfields..(j + 1) * nfields];
         let (mut lo, mut hi) = (0usize, n);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:58325:
     for i in 1..n {
         let p = (i - 1) * 2;
         let c = i * 2;
-        if a_data[p] > a_data[c]
-            || (a_data[p] == a_data[c] && a_data[p + 1] > a_data[c + 1])
-        {
+        if a_data[p] > a_data[c] || (a_data[p] == a_data[c] && a_data[p + 1] > a_data[c + 1]) {
             return Ok(None);
         }
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:58423:
         return Ok(None);
     }
     if a_arr.getattr("ndim")?.extract::<usize>()? != 1
-        || !a_arr.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()?
+        || !a_arr
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
     {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:58438:
         return Ok(None);
     }
     if v.getattr("ndim")?.extract::<usize>()? != 1
-        || !v.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()?
+        || !v
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
     {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:58445:
-    let n: usize = a_arr.getattr("shape")?.extract::<Vec<usize>>()?.iter().product();
-    let m: usize = v.getattr("shape")?.extract::<Vec<usize>>()?.iter().product();
+    let n: usize = a_arr
+        .getattr("shape")?
+        .extract::<Vec<usize>>()?
+        .iter()
+        .product();
+    let m: usize = v
+        .getattr("shape")?
+        .extract::<Vec<usize>>()?
+        .iter()
+        .product();
     if n < 1 || m < QUERY_MIN || rayon::current_num_threads() < 2 {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:58461:
     }
     use rayon::prelude::*;
     // SAFETY: ReadOnlyCell<u8> repr(transparent) over u8; read-only under the GIL.
-    let a_data: &[u8] = unsafe { std::slice::from_raw_parts(a_cells.as_ptr().cast::<u8>(), n * itemsize) };
-    let v_data: &[u8] = unsafe { std::slice::from_raw_parts(v_cells.as_ptr().cast::<u8>(), m * itemsize) };
+    let a_data: &[u8] =
+        unsafe { std::slice::from_raw_parts(a_cells.as_ptr().cast::<u8>(), n * itemsize) };
+    let v_data: &[u8] =
+        unsafe { std::slice::from_raw_parts(v_cells.as_ptr().cast::<u8>(), m * itemsize) };
     let right = side == "right";
     if m <= u32::MAX as usize {
         if let Some(key_width) = packed_string_key_width(&a_kind, itemsize) {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:58482:
                         return Ok(None);
                     }
                     // SAFETY: fresh numpy.empty int64 output, no alias with inputs.
-                    let o: &mut [i64] =
-                        unsafe { std::slice::from_raw_parts_mut(out_cells.as_ptr() as *mut i64, m) };
+                    let o: &mut [i64] = unsafe {
+                        std::slice::from_raw_parts_mut(out_cells.as_ptr() as *mut i64, m)
+                    };
                     let mut p = 0usize;
                     if right {
                         for &(key, idx) in &pairs {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:58506:
         }
     }
     // 'U' only: wide codepoints break byte==codepoint order; 'S' bytes are always in byte order.
-    let wide = |d: &[u8]| d.par_chunks_exact(4).any(|c| c[1] != 0 || c[2] != 0 || c[3] != 0);
+    let wide = |d: &[u8]| {
+        d.par_chunks_exact(4)
+            .any(|c| c[1] != 0 || c[2] != 0 || c[3] != 0)
+    };
     if !is_bytes && (wide(a_data) || wide(v_data)) {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:58519:
         return Ok(None);
     }
     // SAFETY: fresh numpy.empty int64 output, no alias with a_data/v_data.
-    let o: &mut [i64] = unsafe { std::slice::from_raw_parts_mut(out_cells.as_ptr() as *mut i64, m) };
+    let o: &mut [i64] =
+        unsafe { std::slice::from_raw_parts_mut(out_cells.as_ptr() as *mut i64, m) };
     o.par_iter_mut().enumerate().for_each(|(j, slot)| {
         let q = &v_data[j * itemsize..(j + 1) * itemsize];
         let (mut lo, mut hi) = (0usize, n);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:58558:
         return Ok(None);
     }
     if a.getattr("ndim")?.extract::<usize>()? != 1
-        || !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()?
+        || !a
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
     {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:58567:
     if (kind != "M" && kind != "m") || dtype.getattr("itemsize")?.extract::<usize>()? != 8 {
         return Ok(None);
     }
-    let n: usize = a.getattr("shape")?.extract::<Vec<usize>>()?.iter().product();
+    let n: usize = a
+        .getattr("shape")?
+        .extract::<Vec<usize>>()?
+        .iter()
+        .product();
     if n < SORT_PARALLEL_MIN || rayon::current_num_threads() < 2 {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:58631:
         return Ok(None);
     }
     let ndim = a.getattr("ndim")?.extract::<usize>()?;
-    if ndim < 2 || !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()? {
+    if ndim < 2
+        || !a
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
+    {
         return Ok(None);
     }
     let is_last = axis_spec_is_last(axis_spec, ndim);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:58638:
     let is_first = axis_spec_is_first(axis_spec, ndim);
-    let mid_ax = if is_last || is_first { None } else { axis_spec_middle(axis_spec, ndim) };
+    let mid_ax = if is_last || is_first {
+        None
+    } else {
+        axis_spec_middle(axis_spec, ndim)
+    };
     if !is_last && !is_first && mid_ax.is_none() {
         return Ok(None); // flatten / unsupported -> defer to numpy
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:58712:
         DtMode::Last(cols) => {
             // Each contiguous lane is an independent sort distributed across the rayon pool.
             dst.copy_from_slice(src);
-            dst.par_chunks_mut(cols).for_each(|lane| lane.sort_unstable());
+            dst.par_chunks_mut(cols)
+                .for_each(|lane| lane.sort_unstable());
         }
         DtMode::First(rows, cols) => {
             // axis 0: gather each strided column into a transposed scratch lane, sort, scatter back.
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:58719:
             let mut scratch = vec![0i64; n];
-            scratch.par_chunks_mut(rows).enumerate().for_each(|(j, lane)| {
-                for (i, slot) in lane.iter_mut().enumerate() {
-                    *slot = src[i * cols + j];
-                }
-                lane.sort_unstable();
-            });
+            scratch
+                .par_chunks_mut(rows)
+                .enumerate()
+                .for_each(|(j, lane)| {
+                    for (i, slot) in lane.iter_mut().enumerate() {
+                        *slot = src[i * cols + j];
+                    }
+                    lane.sort_unstable();
+                });
             dst.par_chunks_mut(cols).enumerate().for_each(|(i, orow)| {
                 for (j, slot) in orow.iter_mut().enumerate() {
                     *slot = scratch[j * rows + i];
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:58732:
         DtMode::Mid(_outer, alen, inner) => {
             // middle axis: gather each strided lane into a contiguous scratch lane, sort, scatter.
             let mut scratch = vec![0i64; n];
-            scratch.par_chunks_mut(alen).enumerate().for_each(|(lane, vlane)| {
-                let o = lane / inner;
-                let t = lane % inner;
-                let base = o * alen * inner + t;
-                for (j, slot) in vlane.iter_mut().enumerate() {
-                    *slot = src[base + j * inner];
-                }
-                vlane.sort_unstable();
-            });
+            scratch
+                .par_chunks_mut(alen)
+                .enumerate()
+                .for_each(|(lane, vlane)| {
+                    let o = lane / inner;
+                    let t = lane % inner;
+                    let base = o * alen * inner + t;
+                    for (j, slot) in vlane.iter_mut().enumerate() {
+                        *slot = src[base + j * inner];
+                    }
+                    vlane.sort_unstable();
+                });
             // Output chunk C (length inner) holds fixed (o, j) varying t; C = o*alen + j.
-            dst.par_chunks_mut(inner).enumerate().for_each(|(c, ochunk)| {
-                let o = c / alen;
-                let j = c % alen;
-                for (t, slot) in ochunk.iter_mut().enumerate() {
-                    *slot = scratch[(o * inner + t) * alen + j];
-                }
-            });
+            dst.par_chunks_mut(inner)
+                .enumerate()
+                .for_each(|(c, ochunk)| {
+                    let o = c / alen;
+                    let j = c % alen;
+                    for (t, slot) in ochunk.iter_mut().enumerate() {
+                        *slot = scratch[(o * inner + t) * alen + j];
+                    }
+                });
         }
     }
     Ok(Some(out.unbind()))
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:58790:
         return Ok(None);
     };
     // SAFETY: fresh numpy.empty buffer we own (no alias with src).
-    let dst: &mut [T] =
-        unsafe { std::slice::from_raw_parts_mut(out_cells.as_ptr() as *mut T, n) };
+    let dst: &mut [T] = unsafe { std::slice::from_raw_parts_mut(out_cells.as_ptr() as *mut T, n) };
     dst.copy_from_slice(src);
-    dst.par_chunks_mut(cols).for_each(|lane| lane.sort_unstable());
+    dst.par_chunks_mut(cols)
+        .for_each(|lane| lane.sort_unstable());
     Ok(Some(out.unbind()))
 }

Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:58811:
         return Ok(None);
     }
     let ndim = a.getattr("ndim")?.extract::<usize>()?;
-    if ndim < 2 || !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()? {
+    if ndim < 2
+        || !a
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
+    {
         return Ok(None);
     }
     if !axis_spec_is_last(axis_spec, ndim) {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:58872:
     // Transposed scratch: cols lanes of rows each. Each lane gathers its axis-0 column (strided),
     // sorts it, fully parallel across lanes.
     let mut scratch = vec![T::default(); n];
-    scratch.par_chunks_mut(rows).enumerate().for_each(|(j, lane)| {
-        for (i, slot) in lane.iter_mut().enumerate() {
-            *slot = src[i * cols + j];
-        }
-        lane.sort_unstable();
-    });
+    scratch
+        .par_chunks_mut(rows)
+        .enumerate()
+        .for_each(|(j, lane)| {
+            for (i, slot) in lane.iter_mut().enumerate() {
+                *slot = src[i * cols + j];
+            }
+            lane.sort_unstable();
+        });
     let kwargs = PyDict::new(py);
     kwargs.set_item("dtype", dtype_name)?;
     let shape_tuple = PyTuple::new(py, shape.iter().copied())?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:58887:
         return Ok(None);
     };
     // SAFETY: fresh numpy.empty buffer we own (no alias with src/scratch).
-    let dst: &mut [T] =
-        unsafe { std::slice::from_raw_parts_mut(out_cells.as_ptr() as *mut T, n) };
+    let dst: &mut [T] = unsafe { std::slice::from_raw_parts_mut(out_cells.as_ptr() as *mut T, n) };
     // Scatter the sorted columns back into the C-contiguous result, one output row per chunk.
     dst.par_chunks_mut(cols).enumerate().for_each(|(i, orow)| {
         for (j, slot) in orow.iter_mut().enumerate() {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:58913:
     let ndim = a.getattr("ndim")?.extract::<usize>()?;
     if ndim < 2
         || !axis_spec_is_first(axis_spec, ndim)
-        || !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()?
+        || !a
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
     {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:58925:
     let shape: Vec<usize> = a.getattr("shape")?.extract()?;
     let rows = shape[0];
     let cols: usize = shape[1..].iter().product();
-    if rows < 2 || cols < 2 || rows * cols < SORT_AXIS0_PARALLEL_MIN
+    if rows < 2
+        || cols < 2
+        || rows * cols < SORT_AXIS0_PARALLEL_MIN
         || rayon::current_num_threads() < 2
     {
         return Ok(None);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:58971:
     let src: &[T] = unsafe { std::slice::from_raw_parts(cells.as_ptr().cast::<T>(), n) };
     // Gather each lane's values into a contiguous scratch lane (strided read), then sort it.
     let mut vals = vec![T::default(); n];
-    vals.par_chunks_mut(alen).enumerate().for_each(|(lane, vlane)| {
-        let o = lane / inner;
-        let t = lane % inner;
-        let base = o * alen * inner + t;
-        for (j, slot) in vlane.iter_mut().enumerate() {
-            *slot = src[base + j * inner];
-        }
-        vlane.sort_unstable();
-    });
+    vals.par_chunks_mut(alen)
+        .enumerate()
+        .for_each(|(lane, vlane)| {
+            let o = lane / inner;
+            let t = lane % inner;
+            let base = o * alen * inner + t;
+            for (j, slot) in vlane.iter_mut().enumerate() {
+                *slot = src[base + j * inner];
+            }
+            vlane.sort_unstable();
+        });
     let kwargs = PyDict::new(py);
     kwargs.set_item("dtype", dtype_name)?;
     let shape_tuple = PyTuple::new(py, shape.iter().copied())?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:58989:
         return Ok(None);
     };
     // SAFETY: fresh numpy.empty buffer we own (no alias with src/vals).
-    let dst: &mut [T] =
-        unsafe { std::slice::from_raw_parts_mut(out_cells.as_ptr() as *mut T, n) };
+    let dst: &mut [T] = unsafe { std::slice::from_raw_parts_mut(out_cells.as_ptr() as *mut T, n) };
     // Scatter: output chunk C (length inner) holds fixed (o, j) varying t; C = o*alen + j.
-    dst.par_chunks_mut(inner).enumerate().for_each(|(c, ochunk)| {
-        let o = c / alen;
-        let j = c % alen;
-        for (t, slot) in ochunk.iter_mut().enumerate() {
-            *slot = vals[(o * inner + t) * alen + j];
-        }
-    });
+    dst.par_chunks_mut(inner)
+        .enumerate()
+        .for_each(|(c, ochunk)| {
+            let o = c / alen;
+            let j = c % alen;
+            for (t, slot) in ochunk.iter_mut().enumerate() {
+                *slot = vals[(o * inner + t) * alen + j];
+            }
+        });
     Ok(Some(out.unbind()))
 }

Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:59015:
         return Ok(None);
     }
     let ndim = a.getattr("ndim")?.extract::<usize>()?;
-    if ndim < 3 || !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()? {
+    if ndim < 3
+        || !a
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
+    {
         return Ok(None);
     }
     let Some(ax) = axis_spec_middle(axis_spec, ndim) else {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:59030:
     let outer: usize = shape[..ax].iter().product();
     let alen = shape[ax];
     let inner: usize = shape[ax + 1..].iter().product();
-    if alen < 2 || outer * inner < 2 || outer * alen * inner < SORT_MIDAXIS_PARALLEL_MIN
+    if alen < 2
+        || outer * inner < 2
+        || outer * alen * inner < SORT_MIDAXIS_PARALLEL_MIN
         || rayon::current_num_threads() < 2
     {
         return Ok(None);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:59059:
 // axis=None (flatten — NOT the last axis); Some(Some(k)) = explicit integer axis.
 fn axis_spec_is_last(axis_spec: Option<Option<isize>>, ndim: usize) -> bool {
     match axis_spec {
-        None => true,                       // missing -> default -1 (last)
-        Some(None) => false,                // axis=None -> flatten (handled elsewhere)
+        None => true,        // missing -> default -1 (last)
+        Some(None) => false, // axis=None -> flatten (handled elsewhere)
         Some(Some(k)) => k == -1 || k == (ndim as isize) - 1,
     }
 }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:59082:
         return Ok(None);
     }
     let ndim = a.getattr("ndim")?.extract::<usize>()?;
-    if ndim < 2 || !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()? {
+    if ndim < 2
+        || !a
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
+    {
         return Ok(None);
     }
     if !axis_spec_is_last(axis_spec, ndim) {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:59101:
     // width at 256: wide lanes fan out across the rayon pool (win), short lanes -> numpy (parity).
     const SORT_AXIS_PARALLEL_MIN: usize = 1 << 20;
     const SORT_LANE_PARALLEL_MIN: usize = 256;
-    if rows < 2 || cols < SORT_LANE_PARALLEL_MIN || rows * cols < SORT_AXIS_PARALLEL_MIN
+    if rows < 2
+        || cols < SORT_LANE_PARALLEL_MIN
+        || rows * cols < SORT_AXIS_PARALLEL_MIN
         || rayon::current_num_threads() < 2
     {
         return Ok(None);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:59174:
     let ndim = a.getattr("ndim")?.extract::<usize>()?;
     if ndim < 2
         || !axis_spec_is_first(axis_spec, ndim)
-        || !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()?
+        || !a
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
     {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:59182:
     let rows = shape[0];
     let cols: usize = shape[1..].iter().product();
     const SORT_AXIS0_PARALLEL_MIN: usize = 1 << 20;
-    if rows < 2 || cols < 2 || rows * cols < SORT_AXIS0_PARALLEL_MIN
+    if rows < 2
+        || cols < 2
+        || rows * cols < SORT_AXIS0_PARALLEL_MIN
         || rayon::current_num_threads() < 2
     {
         return Ok(None);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:59206:
     // Transposed scratch: column-major lanes (cols lanes of rows each). Each lane gathers its
     // axis-0 column from the strided source and sorts it — fully parallel across lanes.
     let mut scratch = vec![0.0f64; n];
-    scratch.par_chunks_mut(rows).enumerate().for_each(|(j, lane)| {
-        for (i, slot) in lane.iter_mut().enumerate() {
-            *slot = src[i * cols + j];
-        }
-        lane.sort_unstable_by(|x, y| x.partial_cmp(y).expect("no NaN after check"));
-    });
+    scratch
+        .par_chunks_mut(rows)
+        .enumerate()
+        .for_each(|(j, lane)| {
+            for (i, slot) in lane.iter_mut().enumerate() {
+                *slot = src[i * cols + j];
+            }
+            lane.sort_unstable_by(|x, y| x.partial_cmp(y).expect("no NaN after check"));
+        });
     let kwargs = PyDict::new(py);
     kwargs.set_item("dtype", "float64")?;
     let shape_tuple = PyTuple::new(py, shape.iter().copied())?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:59267:
         return Ok(None);
     }
     let ndim = a.getattr("ndim")?.extract::<usize>()?;
-    if ndim < 3 || !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()? {
+    if ndim < 3
+        || !a
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
+    {
         return Ok(None);
     }
     let Some(ax) = axis_spec_middle(axis_spec, ndim) else {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:59279:
     let inner: usize = shape[ax + 1..].iter().product();
     let lanes = outer * inner;
     const SORT_MIDAXIS_PARALLEL_MIN: usize = 1 << 20;
-    if alen < 2 || lanes < 2 || outer * alen * inner < SORT_MIDAXIS_PARALLEL_MIN
+    if alen < 2
+        || lanes < 2
+        || outer * alen * inner < SORT_MIDAXIS_PARALLEL_MIN
         || rayon::current_num_threads() < 2
     {
         return Ok(None);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:59327:
     let dst: &mut [f64] =
         unsafe { std::slice::from_raw_parts_mut(out_cells.as_ptr() as *mut f64, n) };
     // Scatter back: output chunk C (length inner) holds fixed (o, j) varying t. C = o*alen + j.
-    dst.par_chunks_mut(inner).enumerate().for_each(|(c, ochunk)| {
-        let o = c / alen;
-        let j = c % alen;
-        for (t, slot) in ochunk.iter_mut().enumerate() {
-            *slot = scratch[(o * inner + t) * alen + j];
-        }
-    });
+    dst.par_chunks_mut(inner)
+        .enumerate()
+        .for_each(|(c, ochunk)| {
+            let o = c / alen;
+            let j = c % alen;
+            for (t, slot) in ochunk.iter_mut().enumerate() {
+                *slot = scratch[(o * inner + t) * alen + j];
+            }
+        });
     Ok(Some(out.unbind()))
 }

Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:59374:
     if shape.len() != 1 {
         return Ok(None); // n-D structured sort (per-axis) -> numpy
     }
-    if !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()? {
+    if !a
+        .getattr("flags")?
+        .getattr("c_contiguous")?
+        .extract::<bool>()?
+    {
         return Ok(None);
     }
     // axis must collapse to the single axis of a 1-D array.
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:59447:
         if let Some(kw) = kwargs {
             for (k, v) in kw.iter() {
                 if k.extract::<String>().ok().as_deref() == Some("axis") {
-                    axis_spec = Some(if v.is_none() { None } else { Some(v.extract::<isize>()?) });
+                    axis_spec = Some(if v.is_none() {
+                        None
+                    } else {
+                        Some(v.extract::<isize>()?)
+                    });
                 } else if k.extract::<String>().ok().as_deref() == Some("kind") {
                     if !v.is_none() {
                         kind_spec = Some(v.extract::<String>()?);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:59486:
             let numpy = py.import("numpy")?;
             let a = args.get_item(0)?;
             // 1-D: axis missing/None/-1/0 all collapse to the single axis.
-            if matches!(axis_spec, None | Some(None) | Some(Some(-1)) | Some(Some(0))) {
+            if matches!(
+                axis_spec,
+                None | Some(None) | Some(Some(-1)) | Some(Some(0))
+            ) {
                 if let Some(out) = try_zerocopy_f64_sort_flat(py, &numpy, &a, require_distinct)? {
                     return Ok(out);
                 }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:59595:
     let stride = (n / k).max(1); // spread the sample across the whole array, not just a prefix
     let mut sample: Vec<T> = (0..k).map(|i| data[i * stride]).collect();
     sample.sort_unstable_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
-    (1..sample.len()).any(|i| sample[i].partial_cmp(&sample[i - 1]) == Some(std::cmp::Ordering::Equal))
+    (1..sample.len())
+        .any(|i| sample[i].partial_cmp(&sample[i - 1]) == Some(std::cmp::Ordering::Equal))
 }

 // Per-lane sampled tie oracle for the AXIS argsort fast paths (per-lane mirror of argsort_sample_has_tie).
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:59645:
         return Ok(None);
     }
     if a.getattr("ndim")?.extract::<usize>()? != 1
-        || !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()?
+        || !a
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
     {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:59679:
     // SAFETY: fresh numpy.empty intp buffer we own (no alias).
     let perm: &mut [i64] =
         unsafe { std::slice::from_raw_parts_mut(out_cells.as_ptr() as *mut i64, n) };
-    perm.par_iter_mut().enumerate().for_each(|(i, s)| *s = i as i64);
+    perm.par_iter_mut()
+        .enumerate()
+        .for_each(|(i, s)| *s = i as i64);
     // No NaN -> partial_cmp is a total order on the values.
     perm.par_sort_unstable_by(|&x, &y| {
         data[x as usize]
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:59712:
         return Ok(None);
     }
     if a.getattr("ndim")?.extract::<usize>()? != 1
-        || !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()?
+        || !a
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
     {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:59746:
     // SAFETY: fresh numpy.empty intp buffer we own (no alias).
     let perm: &mut [i64] =
         unsafe { std::slice::from_raw_parts_mut(out_cells.as_ptr() as *mut i64, n) };
-    perm.par_iter_mut().enumerate().for_each(|(i, s)| *s = i as i64);
+    perm.par_iter_mut()
+        .enumerate()
+        .for_each(|(i, s)| *s = i as i64);
     // No NaN -> partial_cmp is a total order on the values.
     perm.par_sort_unstable_by(|&x, &y| {
         data[x as usize]
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:59784:
         return Ok(None);
     }
     if a.getattr("ndim")?.extract::<usize>()? != 1
-        || !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()?
+        || !a
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
     {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:59840:
     // SAFETY: fresh numpy.empty intp buffer we own (no alias).
     let perm: &mut [i64] =
         unsafe { std::slice::from_raw_parts_mut(out_cells.as_ptr() as *mut i64, n) };
-    perm.par_iter_mut().enumerate().for_each(|(i, s)| *s = i as i64);
+    perm.par_iter_mut()
+        .enumerate()
+        .for_each(|(i, s)| *s = i as i64);
     // No NaN -> partial_cmp is a total order on each component. Lexicographic: real then imag.
     perm.par_sort_unstable_by(|&x, &y| {
         let (xr, xi) = (data[2 * x as usize], data[2 * x as usize + 1]);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:59880:
         return Ok(None);
     }
     if a.getattr("ndim")?.extract::<usize>()? != 1
-        || !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()?
+        || !a
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
     {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:59932:
     // SAFETY: fresh numpy.empty intp buffer we own (no alias).
     let perm: &mut [i64] =
         unsafe { std::slice::from_raw_parts_mut(out_cells.as_ptr() as *mut i64, n) };
-    perm.par_iter_mut().enumerate().for_each(|(i, s)| *s = i as i64);
+    perm.par_iter_mut()
+        .enumerate()
+        .for_each(|(i, s)| *s = i as i64);
     perm.par_sort_unstable_by(|&x, &y| {
         let (xr, xi) = (data[2 * x as usize], data[2 * x as usize + 1]);
         let (yr, yi) = (data[2 * y as usize], data[2 * y as usize + 1]);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:59970:
         return Ok(None);
     }
     let ndim = a.getattr("ndim")?.extract::<usize>()?;
-    if ndim < 2 || !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()? {
+    if ndim < 2
+        || !a
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
+    {
         return Ok(None);
     }
     if !axis_spec_is_last(axis_spec, ndim) {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:59980:
     let cols = shape[ndim - 1];
     let rows: usize = shape[..ndim - 1].iter().product();
     const ARGSORT_AXIS_PARALLEL_MIN: usize = 1 << 20;
-    if rows < 2 || cols < 2 || rows * cols < ARGSORT_AXIS_PARALLEL_MIN
+    if rows < 2
+        || cols < 2
+        || rows * cols < ARGSORT_AXIS_PARALLEL_MIN
         || rayon::current_num_threads() < 2
     {
         return Ok(None);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:60036:
     let has_tie = sorted.par_chunks(cols).enumerate().any(|(r, prow)| {
         let base = r * cols;
         (1..cols).any(|j| {
-            let (p, q) = ((base + prow[j] as usize) * 2, (base + prow[j - 1] as usize) * 2);
+            let (p, q) = (
+                (base + prow[j] as usize) * 2,
+                (base + prow[j - 1] as usize) * 2,
+            );
             data[p] == data[q] && data[p + 1] == data[q + 1]
         })
     });
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:60063:
     {
         return Ok(None);
     }
-    if !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()? {
+    if !a
+        .getattr("flags")?
+        .getattr("c_contiguous")?
+        .extract::<bool>()?
+    {
         return Ok(None);
     }
     let view = a.call_method1("view", ("float64",))?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:60093:
     let cols = shape[ndim - 1];
     let rows: usize = shape[..ndim - 1].iter().product();
     const ARGSORT_AXIS_PARALLEL_MIN: usize = 1 << 20;
-    if rows < 2 || cols < 2 || rows * cols < ARGSORT_AXIS_PARALLEL_MIN
+    if rows < 2
+        || cols < 2
+        || rows * cols < ARGSORT_AXIS_PARALLEL_MIN
         || rayon::current_num_threads() < 2
     {
         return Ok(None);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:60148:
     let has_tie = sorted.par_chunks(cols).enumerate().any(|(r, prow)| {
         let base = r * cols;
         (1..cols).any(|j| {
-            let (p, q) = ((base + prow[j] as usize) * 2, (base + prow[j - 1] as usize) * 2);
+            let (p, q) = (
+                (base + prow[j] as usize) * 2,
+                (base + prow[j - 1] as usize) * 2,
+            );
             data[p] == data[q] && data[p + 1] == data[q + 1]
         })
     });
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:60175:
     let rows = shape[0];
     let cols: usize = shape[1..].iter().product();
     const ARGSORT_AXIS0_PARALLEL_MIN: usize = 1 << 20;
-    if rows < 2 || cols < 2 || rows * cols < ARGSORT_AXIS0_PARALLEL_MIN
+    if rows < 2
+        || cols < 2
+        || rows * cols < ARGSORT_AXIS0_PARALLEL_MIN
         || rayon::current_num_threads() < 2
     {
         return Ok(None);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:60205:
     }
     // Gather column j's (re,im) pairs into a contiguous lane of 2*rows f64.
     let mut vals = vec![0.0f64; 2 * n];
-    vals.par_chunks_mut(2 * rows).enumerate().for_each(|(j, vlane)| {
-        for i in 0..rows {
-            let src = (i * cols + j) * 2;
-            vlane[2 * i] = data[src];
-            vlane[2 * i + 1] = data[src + 1];
-        }
-    });
+    vals.par_chunks_mut(2 * rows)
+        .enumerate()
+        .for_each(|(j, vlane)| {
+            for i in 0..rows {
+                let src = (i * cols + j) * 2;
+                vlane[2 * i] = data[src];
+                vlane[2 * i + 1] = data[src + 1];
+            }
+        });
     let mut idx = vec![0i64; n];
     let tie = std::sync::atomic::AtomicBool::new(false);
     idx.par_chunks_mut(rows)
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:60240:
     }
     let out = numpy.call_method1(
         "empty",
-        (PyTuple::new(py, shape.iter().copied())?, numpy.getattr("intp")?),
+        (
+            PyTuple::new(py, shape.iter().copied())?,
+            numpy.getattr("intp")?,
+        ),
     )?;
     let out_buffer = PyBuffer::<i64>::get(&out)?;
     let Some(out_cells) = out_buffer.as_mut_slice(py) else {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:60279:
     let inner: usize = shape[ax + 1..].iter().product();
     let lanes = outer * inner;
     const ARGSORT_MIDAXIS_PARALLEL_MIN: usize = 1 << 20;
-    if alen < 2 || lanes < 2 || outer * alen * inner < ARGSORT_MIDAXIS_PARALLEL_MIN
+    if alen < 2
+        || lanes < 2
+        || outer * alen * inner < ARGSORT_MIDAXIS_PARALLEL_MIN
         || rayon::current_num_threads() < 2
     {
         return Ok(None);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:60311:
     }
     // Gather each lane's (re,im) pairs into a contiguous lane of 2*alen f64.
     let mut vals = vec![0.0f64; 2 * n];
-    vals.par_chunks_mut(2 * alen).enumerate().for_each(|(lane, vlane)| {
-        let o = lane / inner;
-        let t = lane % inner;
-        let base = o * alen * inner + t; // complex-element base
-        for j in 0..alen {
-            let src = (base + j * inner) * 2;
-            vlane[2 * j] = data[src];
-            vlane[2 * j + 1] = data[src + 1];
-        }
-    });
+    vals.par_chunks_mut(2 * alen)
+        .enumerate()
+        .for_each(|(lane, vlane)| {
+            let o = lane / inner;
+            let t = lane % inner;
+            let base = o * alen * inner + t; // complex-element base
+            for j in 0..alen {
+                let src = (base + j * inner) * 2;
+                vlane[2 * j] = data[src];
+                vlane[2 * j + 1] = data[src + 1];
+            }
+        });
     let mut idx = vec![0i64; n];
     let tie = std::sync::atomic::AtomicBool::new(false);
     idx.par_chunks_mut(alen)
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:60349:
     }
     let out = numpy.call_method1(
         "empty",
-        (PyTuple::new(py, shape.iter().copied())?, numpy.getattr("intp")?),
+        (
+            PyTuple::new(py, shape.iter().copied())?,
+            numpy.getattr("intp")?,
+        ),
     )?;
     let out_buffer = PyBuffer::<i64>::get(&out)?;
     let Some(out_cells) = out_buffer.as_mut_slice(py) else {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:60358:
     // SAFETY: fresh numpy.empty intp buffer we own (no alias).
     let dst: &mut [i64] =
         unsafe { std::slice::from_raw_parts_mut(out_cells.as_ptr() as *mut i64, n) };
-    dst.par_chunks_mut(inner).enumerate().for_each(|(c, ochunk)| {
-        let o = c / alen;
-        let j = c % alen;
-        for (t, slot) in ochunk.iter_mut().enumerate() {
-            *slot = idx[(o * inner + t) * alen + j];
-        }
-    });
+    dst.par_chunks_mut(inner)
+        .enumerate()
+        .for_each(|(c, ochunk)| {
+            let o = c / alen;
+            let j = c % alen;
+            for (t, slot) in ochunk.iter_mut().enumerate() {
+                *slot = idx[(o * inner + t) * alen + j];
+            }
+        });
     Ok(Some(out.unbind()))
 }

Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:60383:
     {
         return Ok(None);
     }
-    if !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()? {
+    if !a
+        .getattr("flags")?
+        .getattr("c_contiguous")?
+        .extract::<bool>()?
+    {
         return Ok(None);
     }
     let view = a.call_method1("view", ("float32",))?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:60408:
     let rows = shape[0];
     let cols: usize = shape[1..].iter().product();
     const ARGSORT_AXIS0_PARALLEL_MIN: usize = 1 << 20;
-    if rows < 2 || cols < 2 || rows * cols < ARGSORT_AXIS0_PARALLEL_MIN
+    if rows < 2
+        || cols < 2
+        || rows * cols < ARGSORT_AXIS0_PARALLEL_MIN
         || rayon::current_num_threads() < 2
     {
         return Ok(None);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:60437:
         return Ok(None);
     }
     let mut vals = vec![0.0f32; 2 * n];
-    vals.par_chunks_mut(2 * rows).enumerate().for_each(|(j, vlane)| {
-        for i in 0..rows {
-            let src = (i * cols + j) * 2;
-            vlane[2 * i] = data[src];
-            vlane[2 * i + 1] = data[src + 1];
-        }
-    });
+    vals.par_chunks_mut(2 * rows)
+        .enumerate()
+        .for_each(|(j, vlane)| {
+            for i in 0..rows {
+                let src = (i * cols + j) * 2;
+                vlane[2 * i] = data[src];
+                vlane[2 * i + 1] = data[src + 1];
+            }
+        });
     let mut idx = vec![0i64; n];
     let tie = std::sync::atomic::AtomicBool::new(false);
     idx.par_chunks_mut(rows)
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:60472:
     }
     let out = numpy.call_method1(
         "empty",
-        (PyTuple::new(py, shape.iter().copied())?, numpy.getattr("intp")?),
+        (
+            PyTuple::new(py, shape.iter().copied())?,
+            numpy.getattr("intp")?,
+        ),
     )?;
     let out_buffer = PyBuffer::<i64>::get(&out)?;
     let Some(out_cells) = out_buffer.as_mut_slice(py) else {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:60509:
     let inner: usize = shape[ax + 1..].iter().product();
     let lanes = outer * inner;
     const ARGSORT_MIDAXIS_PARALLEL_MIN: usize = 1 << 20;
-    if alen < 2 || lanes < 2 || outer * alen * inner < ARGSORT_MIDAXIS_PARALLEL_MIN
+    if alen < 2
+        || lanes < 2
+        || outer * alen * inner < ARGSORT_MIDAXIS_PARALLEL_MIN
         || rayon::current_num_threads() < 2
     {
         return Ok(None);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:60540:
         return Ok(None);
     }
     let mut vals = vec![0.0f32; 2 * n];
-    vals.par_chunks_mut(2 * alen).enumerate().for_each(|(lane, vlane)| {
-        let o = lane / inner;
-        let t = lane % inner;
-        let base = o * alen * inner + t;
-        for j in 0..alen {
-            let src = (base + j * inner) * 2;
-            vlane[2 * j] = data[src];
-            vlane[2 * j + 1] = data[src + 1];
-        }
-    });
+    vals.par_chunks_mut(2 * alen)
+        .enumerate()
+        .for_each(|(lane, vlane)| {
+            let o = lane / inner;
+            let t = lane % inner;
+            let base = o * alen * inner + t;
+            for j in 0..alen {
+                let src = (base + j * inner) * 2;
+                vlane[2 * j] = data[src];
+                vlane[2 * j + 1] = data[src + 1];
+            }
+        });
     let mut idx = vec![0i64; n];
     let tie = std::sync::atomic::AtomicBool::new(false);
     idx.par_chunks_mut(alen)
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:60578:
     }
     let out = numpy.call_method1(
         "empty",
-        (PyTuple::new(py, shape.iter().copied())?, numpy.getattr("intp")?),
+        (
+            PyTuple::new(py, shape.iter().copied())?,
+            numpy.getattr("intp")?,
+        ),
     )?;
     let out_buffer = PyBuffer::<i64>::get(&out)?;
     let Some(out_cells) = out_buffer.as_mut_slice(py) else {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:60587:
     // SAFETY: fresh numpy.empty intp buffer we own (no alias).
     let dst: &mut [i64] =
         unsafe { std::slice::from_raw_parts_mut(out_cells.as_ptr() as *mut i64, n) };
-    dst.par_chunks_mut(inner).enumerate().for_each(|(c, ochunk)| {
-        let o = c / alen;
-        let j = c % alen;
-        for (t, slot) in ochunk.iter_mut().enumerate() {
-            *slot = idx[(o * inner + t) * alen + j];
-        }
-    });
+    dst.par_chunks_mut(inner)
+        .enumerate()
+        .for_each(|(c, ochunk)| {
+            let o = c / alen;
+            let j = c % alen;
+            for (t, slot) in ochunk.iter_mut().enumerate() {
+                *slot = idx[(o * inner + t) * alen + j];
+            }
+        });
     Ok(Some(out.unbind()))
 }

Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:60670:
     // SAFETY: fresh numpy.empty intp buffer we own (no alias).
     let perm: &mut [i64] =
         unsafe { std::slice::from_raw_parts_mut(out_cells.as_ptr() as *mut i64, n) };
-    perm.par_iter_mut().enumerate().for_each(|(i, s)| *s = i as i64);
+    perm.par_iter_mut()
+        .enumerate()
+        .for_each(|(i, s)| *s = i as i64);
     perm.par_sort_unstable_by(|&x, &y| data[x as usize].cmp(&data[y as usize]));
     // Defer on any tie (adjacent equal values in sorted order); distinct -> unique permutation.
     let sorted: &[i64] = perm;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:60713:
     use rayon::prelude::*;
     // SAFETY: ReadOnlyCell<T> is repr(transparent) over T; read-only under the GIL.
     let data: &[T] = unsafe { std::slice::from_raw_parts(cells.as_ptr().cast::<T>(), n) };
-    let (mn, mx) = data
-        .par_iter()
-        .copied()
-        .map(|v| (v, v))
-        .reduce(|| (data[0], data[0]), |(a0, a1), (b0, b1)| (a0.min(b0), a1.max(b1)));
+    let (mn, mx) = data.par_iter().copied().map(|v| (v, v)).reduce(
+        || (data[0], data[0]),
+        |(a0, a1), (b0, b1)| (a0.min(b0), a1.max(b1)),
+    );
     let min_i: i128 = mn.into();
     let max_i: i128 = mx.into();
     let span = max_i - min_i + 1;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:60828:
         .par_iter()
         .copied()
         .reduce_with(|a, b| (a.min(b)))
-        .map(|min| (min, keys.par_iter().copied().reduce_with(u64::max).unwrap_or(min)))
+        .map(|min| {
+            (
+                min,
+                keys.par_iter()
+                    .copied()
+                    .reduce_with(u64::max)
+                    .unwrap_or(min),
+            )
+        })
         .unwrap_or((0, 0));
     let span = mx.wrapping_sub(mn);
     if span == 0 {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:60835:
         if distinct_only && n > 1 {
             return Ok(None); // all values equal -> not distinct -> default-kind tie order is numpy's to pick
         }
-        o.par_iter_mut().enumerate().for_each(|(i, s)| *s = i as i64);
+        o.par_iter_mut()
+            .enumerate()
+            .for_each(|(i, s)| *s = i as i64);
         return Ok(Some(out.unbind()));
     }
     if mn != 0 {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:60917:
             return Ok(None);
         }
     }
-    o.par_iter_mut().zip(idx.par_iter()).for_each(|(dst, &p)| *dst = p as i64);
+    o.par_iter_mut()
+        .zip(idx.par_iter())
+        .for_each(|(dst, &p)| *dst = p as i64);
     Ok(Some(out.unbind()))
 }

Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:60946:
     let (min_i, max_i) = data
         .par_iter()
         .copied()
-        .map(|v| { let i: i128 = v.into(); (i, i) })
-        .reduce(|| { let i: i128 = data[0].into(); (i, i) }, |(a0, a1), (b0, b1)| (a0.min(b0), a1.max(b1)));
+        .map(|v| {
+            let i: i128 = v.into();
+            (i, i)
+        })
+        .reduce(
+            || {
+                let i: i128 = data[0].into();
+                (i, i)
+            },
+            |(a0, a1), (b0, b1)| (a0.min(b0), a1.max(b1)),
+        );
     // Pigeonhole: fewer distinct values than elements guarantees a tie -> for the default-kind (distinct-only)
     // caller, skip the radix and defer to numpy immediately (numpy owns the unstable tie order).
     if distinct_only && (max_i - min_i + 1) < n as i128 {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:60954:
         return Ok(None);
     }
     // Monotonic keys = value - min (non-negative u64, preserves value order for signed/unsigned).
-    let keys: Vec<u64> = data.par_iter().map(|&v| (Into::<i128>::into(v) - min_i) as u64).collect();
+    let keys: Vec<u64> = data
+        .par_iter()
+        .map(|&v| (Into::<i128>::into(v) - min_i) as u64)
+        .collect();
     radix_perm_from_keys(py, numpy, keys, n, distinct_only)
 }

Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:61054:
         return Ok(None);
     }
     if a.getattr("ndim")?.extract::<usize>()? != 1
-        || !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()?
+        || !a
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
     {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:61105:
         return Ok(None);
     }
     if a.getattr("ndim")?.extract::<usize>()? != 1
-        || !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()?
+        || !a
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
     {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:61163:
         return Ok(None);
     };
     // SAFETY: fresh numpy.empty intp buffer we own (no alias).
-    let perm: &mut [i64] = unsafe { std::slice::from_raw_parts_mut(out_cells.as_ptr() as *mut i64, n) };
-    perm.par_iter_mut().enumerate().for_each(|(i, s)| *s = i as i64);
+    let perm: &mut [i64] =
+        unsafe { std::slice::from_raw_parts_mut(out_cells.as_ptr() as *mut i64, n) };
+    perm.par_iter_mut()
+        .enumerate()
+        .for_each(|(i, s)| *s = i as i64);
     // STABLE: order by value, ties by original index (== numpy stable argsort). No NaN present -> partial_cmp
     // is a total order (Equal for -0.0 vs +0.0, which numpy also ties by index).
     perm.par_sort_unstable_by(|&x, &y| {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:61186:
         return Ok(None);
     }
     if a.getattr("ndim")?.extract::<usize>()? != 1
-        || !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()?
+        || !a
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
     {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:61195:
     if kind != "i" && kind != "u" && kind != "f" {
         return Ok(None);
     }
-    let n: usize = a.getattr("shape")?.extract::<Vec<usize>>()?.iter().product();
+    let n: usize = a
+        .getattr("shape")?
+        .extract::<Vec<usize>>()?
+        .iter()
+        .product();
     if n < ARGSORT_PARALLEL_MIN || rayon::current_num_threads() < 2 {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:61237:
         return Ok(None);
     }
     if a.getattr("ndim")?.extract::<usize>()? != 1
-        || !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()?
+        || !a
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
     {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:61245:
     if n < ARGSORT_PARALLEL_MIN || rayon::current_num_threads() < 2 {
         return Ok(None);
     }
-    if numpy.call_method1("isnat", (a,))?.call_method0("any")?.extract::<bool>()? {
+    if numpy
+        .call_method1("isnat", (a,))?
+        .call_method0("any")?
+        .extract::<bool>()?
+    {
         return Ok(None);
     }
     let iview = a.call_method1("view", ("int64",))?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:61307:
         })
         .reduce(
             || (true, i128::MAX, i128::MIN, i128::MAX, i128::MIN),
-            |a, b| (a.0 && b.0, a.1.min(b.1), a.2.max(b.2), a.3.min(b.3), a.4.max(b.4)),
+            |a, b| {
+                (
+                    a.0 && b.0,
+                    a.1.min(b.1),
+                    a.2.max(b.2),
+                    a.3.min(b.3),
+                    a.4.max(b.4),
+                )
+            },
         );
     if !ok || max_r < min_r || max_i < min_i {
         return Ok(None);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:61423:
     // SAFETY: fresh numpy.empty intp buffer we own.
     let perm: &mut [i64] =
         unsafe { std::slice::from_raw_parts_mut(out_cells.as_ptr() as *mut i64, n) };
-    perm.par_iter_mut().enumerate().for_each(|(i, slot)| *slot = i as i64);
+    perm.par_iter_mut()
+        .enumerate()
+        .for_each(|(i, slot)| *slot = i as i64);
     perm.par_sort_unstable_by(|&x, &y| {
         let (xr, xi) = (data[2 * x as usize], data[2 * x as usize + 1]);
         let (yr, yi) = (data[2 * y as usize], data[2 * y as usize + 1]);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:61449:
         return Ok(None);
     }
     if a.getattr("ndim")?.extract::<usize>()? != 1
-        || !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()?
+        || !a
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
     {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:61490:
         return Ok(None);
     }
     if a.getattr("ndim")?.extract::<usize>()? != 1
-        || !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()?
+        || !a
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
     {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:61499:
     if kind != "i" && kind != "u" {
         return Ok(None);
     }
-    let n: usize = a.getattr("shape")?.extract::<Vec<usize>>()?.iter().product();
+    let n: usize = a
+        .getattr("shape")?
+        .extract::<Vec<usize>>()?
+        .iter()
+        .product();
     if n < ARGSORT_PARALLEL_MIN || rayon::current_num_threads() < 2 {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:61528:
         return Ok(None);
     }
     if a.getattr("ndim")?.extract::<usize>()? != 1
-        || !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()?
+        || !a
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
     {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:61537:
     if (kind != "M" && kind != "m") || dt.getattr("itemsize")?.extract::<usize>()? != 8 {
         return Ok(None);
     }
-    let n: usize = a.getattr("shape")?.extract::<Vec<usize>>()?.iter().product();
+    let n: usize = a
+        .getattr("shape")?
+        .extract::<Vec<usize>>()?
+        .iter()
+        .product();
     if n < ARGSORT_PARALLEL_MIN || rayon::current_num_threads() < 2 {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:61587:
         return Ok(None);
     }
     if a.getattr("ndim")?.extract::<usize>()? < 2
-        || !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()?
+        || !a
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
     {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:61713:
         return Ok(None);
     };
     // SAFETY: fresh numpy.empty intp buffer we own (no alias).
-    let perm: &mut [i64] = unsafe { std::slice::from_raw_parts_mut(out_cells.as_ptr() as *mut i64, n) };
+    let perm: &mut [i64] =
+        unsafe { std::slice::from_raw_parts_mut(out_cells.as_ptr() as *mut i64, n) };
     perm.par_chunks_mut(cols).enumerate().for_each(|(r, prow)| {
         let base = r * cols;
         let vrow = &data[base..base + cols];
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:61742:
         return Ok(None);
     }
     let ndim = a.getattr("ndim")?.extract::<usize>()?;
-    if ndim < 2 || !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()? {
+    if ndim < 2
+        || !a
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
+    {
         return Ok(None);
     }
     if !axis_spec_is_last(axis_spec, ndim) {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:61756:
     let shape: Vec<usize> = a.getattr("shape")?.extract()?;
     let cols = shape[ndim - 1];
     let rows: usize = shape[..ndim - 1].iter().product();
-    if rows < 2 || cols < 2 || rows * cols < ARGSORT_AXIS_PARALLEL_MIN || rayon::current_num_threads() < 2 {
+    if rows < 2
+        || cols < 2
+        || rows * cols < ARGSORT_AXIS_PARALLEL_MIN
+        || rayon::current_num_threads() < 2
+    {
         return Ok(None);
     }
     let n = rows * cols;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:61763:
     let itemsize = dt.getattr("itemsize")?.extract::<usize>()?;
     match (kind.as_str(), itemsize) {
-        ("i", 4) => argsort_stable_lastaxis_typed::<i32>(py, numpy, a, &shape, rows, cols, n, false),
-        ("i", 8) => argsort_stable_lastaxis_typed::<i64>(py, numpy, a, &shape, rows, cols, n, false),
-        ("u", 4) => argsort_stable_lastaxis_typed::<u32>(py, numpy, a, &shape, rows, cols, n, false),
-        ("u", 8) => argsort_stable_lastaxis_typed::<u64>(py, numpy, a, &shape, rows, cols, n, false),
+        ("i", 4) => {
+            argsort_stable_lastaxis_typed::<i32>(py, numpy, a, &shape, rows, cols, n, false)
+        }
+        ("i", 8) => {
+            argsort_stable_lastaxis_typed::<i64>(py, numpy, a, &shape, rows, cols, n, false)
+        }
+        ("u", 4) => {
+            argsort_stable_lastaxis_typed::<u32>(py, numpy, a, &shape, rows, cols, n, false)
+        }
+        ("u", 8) => {
+            argsort_stable_lastaxis_typed::<u64>(py, numpy, a, &shape, rows, cols, n, false)
+        }
         ("f", 4) => argsort_stable_lastaxis_typed::<f32>(py, numpy, a, &shape, rows, cols, n, true),
         ("f", 8) => argsort_stable_lastaxis_typed::<f64>(py, numpy, a, &shape, rows, cols, n, true),
         _ => Ok(None),
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:61785:
         return Ok(None);
     }
     let ndim = a.getattr("ndim")?.extract::<usize>()?;
-    if ndim < 2 || !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()? {
+    if ndim < 2
+        || !a
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
+    {
         return Ok(None);
     }
     if !axis_spec_is_last(axis_spec, ndim) {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:61799:
     let shape: Vec<usize> = a.getattr("shape")?.extract()?;
     let cols = shape[ndim - 1];
     let rows: usize = shape[..ndim - 1].iter().product();
-    if rows < 2 || cols < 2 || rows * cols < ARGSORT_AXIS_PARALLEL_MIN
+    if rows < 2
+        || cols < 2
+        || rows * cols < ARGSORT_AXIS_PARALLEL_MIN
         || rayon::current_num_threads() < 2
     {
         return Ok(None);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:61821:
 // by value parallel across columns, then scattered into the C-contiguous intp output. DEFER the whole
 // op on ANY per-column tie (numpy's unstable introsort order is algorithm-specific). 4-/8-byte ints.
 // numpy argsort axis 0 is single-threaded per column (compute-bound). BYTE-EXACT for tie-free columns.
-fn int_argsort_axis0_typed<T: pyo3::buffer::Element + Copy + Ord + Send + Sync + Default + Into<i128>>(
+fn int_argsort_axis0_typed<
+    T: pyo3::buffer::Element + Copy + Ord + Send + Sync + Default + Into<i128>,
+>(
     py: Python<'_>,
     numpy: &Bound<'_, PyModule>,
     a: &Bound<'_, PyAny>,
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:61849:
     }
     // Transposed value scratch: cols lanes of rows each (gather each axis-0 column, strided read).
     let mut vals = vec![T::default(); n];
-    vals.par_chunks_mut(rows).enumerate().for_each(|(j, vlane)| {
-        for (i, slot) in vlane.iter_mut().enumerate() {
-            *slot = src[i * cols + j];
-        }
-    });
+    vals.par_chunks_mut(rows)
+        .enumerate()
+        .for_each(|(j, vlane)| {
+            for (i, slot) in vlane.iter_mut().enumerate() {
+                *slot = src[i * cols + j];
+            }
+        });
     // Per-column local row-indices (0..rows), sorted by the contiguous lane values; flag any tie.
     let mut idx = vec![0i64; n];
     let tie = std::sync::atomic::AtomicBool::new(false);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:61876:
     }
     let out = numpy.call_method1(
         "empty",
-        (PyTuple::new(py, shape.iter().copied())?, numpy.getattr("intp")?),
+        (
+            PyTuple::new(py, shape.iter().copied())?,
+            numpy.getattr("intp")?,
+        ),
     )?;
     let out_buffer = PyBuffer::<i64>::get(&out)?;
     let Some(out_cells) = out_buffer.as_mut_slice(py) else {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:61908:
     let ndim = a.getattr("ndim")?.extract::<usize>()?;
     if ndim < 2
         || !axis_spec_is_first(axis_spec, ndim)
-        || !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()?
+        || !a
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
     {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:61920:
     let shape: Vec<usize> = a.getattr("shape")?.extract()?;
     let rows = shape[0];
     let cols: usize = shape[1..].iter().product();
-    if rows < 2 || cols < 2 || rows * cols < ARGSORT_AXIS0_PARALLEL_MIN
+    if rows < 2
+        || cols < 2
+        || rows * cols < ARGSORT_AXIS0_PARALLEL_MIN
         || rayon::current_num_threads() < 2
     {
         return Ok(None);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:61942:
 // sorted by value parallel across lanes, then scattered into the C-contiguous intp output. DEFER the
 // whole op on ANY per-lane tie (numpy's unstable introsort order is algorithm-specific). 4-/8-byte
 // ints. numpy argsort along a middle axis is single-threaded per lane. BYTE-EXACT for tie-free lanes.
-fn int_argsort_midaxis_typed<T: pyo3::buffer::Element + Copy + Ord + Send + Sync + Default + Into<i128>>(
+fn int_argsort_midaxis_typed<
+    T: pyo3::buffer::Element + Copy + Ord + Send + Sync + Default + Into<i128>,
+>(
     py: Python<'_>,
     numpy: &Bound<'_, PyModule>,
     a: &Bound<'_, PyAny>,
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:61971:
     }
     // Gather each lane's VALUES into a contiguous lane FIRST (strided read), then sort local indices.
     let mut vals = vec![T::default(); n];
-    vals.par_chunks_mut(alen).enumerate().for_each(|(lane, vlane)| {
-        let o = lane / inner;
-        let t = lane % inner;
-        let base = o * alen * inner + t;
-        for (j, slot) in vlane.iter_mut().enumerate() {
-            *slot = src[base + j * inner];
-        }
-    });
+    vals.par_chunks_mut(alen)
+        .enumerate()
+        .for_each(|(lane, vlane)| {
+            let o = lane / inner;
+            let t = lane % inner;
+            let base = o * alen * inner + t;
+            for (j, slot) in vlane.iter_mut().enumerate() {
+                *slot = src[base + j * inner];
+            }
+        });
     let mut idx = vec![0i64; n];
     let tie = std::sync::atomic::AtomicBool::new(false);
     idx.par_chunks_mut(alen)
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:62000:
     }
     let out = numpy.call_method1(
         "empty",
-        (PyTuple::new(py, shape.iter().copied())?, numpy.getattr("intp")?),
+        (
+            PyTuple::new(py, shape.iter().copied())?,
+            numpy.getattr("intp")?,
+        ),
     )?;
     let out_buffer = PyBuffer::<i64>::get(&out)?;
     let Some(out_cells) = out_buffer.as_mut_slice(py) else {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:62010:
     let dst: &mut [i64] =
         unsafe { std::slice::from_raw_parts_mut(out_cells.as_ptr() as *mut i64, n) };
     // Scatter back: output chunk C (length inner) holds fixed (o, j) varying t. C = o*alen + j.
-    dst.par_chunks_mut(inner).enumerate().for_each(|(c, ochunk)| {
-        let o = c / alen;
-        let j = c % alen;
-        for (t, slot) in ochunk.iter_mut().enumerate() {
-            *slot = idx[(o * inner + t) * alen + j];
-        }
-    });
+    dst.par_chunks_mut(inner)
+        .enumerate()
+        .for_each(|(c, ochunk)| {
+            let o = c / alen;
+            let j = c % alen;
+            for (t, slot) in ochunk.iter_mut().enumerate() {
+                *slot = idx[(o * inner + t) * alen + j];
+            }
+        });
     Ok(Some(out.unbind()))
 }

Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:62033:
         return Ok(None);
     }
     let ndim = a.getattr("ndim")?.extract::<usize>()?;
-    if ndim < 3 || !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()? {
+    if ndim < 3
+        || !a
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
+    {
         return Ok(None);
     }
     let Some(ax) = axis_spec_middle(axis_spec, ndim) else {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:62049:
     let alen = shape[ax];
     let inner: usize = shape[ax + 1..].iter().product();
     let lanes = outer * inner;
-    if alen < 2 || lanes < 2 || outer * alen * inner < ARGSORT_MIDAXIS_PARALLEL_MIN
+    if alen < 2
+        || lanes < 2
+        || outer * alen * inner < ARGSORT_MIDAXIS_PARALLEL_MIN
         || rayon::current_num_threads() < 2
     {
         return Ok(None);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:62080:
         return Ok(None);
     }
     let ndim = a.getattr("ndim")?.extract::<usize>()?;
-    if ndim < 2 || !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()? {
+    if ndim < 2
+        || !a
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
+    {
         return Ok(None);
     }
     if !axis_spec_is_last(axis_spec, ndim) {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:62090:
     let cols = shape[ndim - 1];
     let rows: usize = shape[..ndim - 1].iter().product();
     const ARGSORT_AXIS_PARALLEL_MIN: usize = 1 << 20;
-    if rows < 2 || cols < 2 || rows * cols < ARGSORT_AXIS_PARALLEL_MIN
+    if rows < 2
+        || cols < 2
+        || rows * cols < ARGSORT_AXIS_PARALLEL_MIN
         || rayon::current_num_threads() < 2
     {
         return Ok(None);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:62168:
     let ndim = a.getattr("ndim")?.extract::<usize>()?;
     if ndim < 2
         || !axis_spec_is_first(axis_spec, ndim)
-        || !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()?
+        || !a
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
     {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:62176:
     let rows = shape[0];
     let cols: usize = shape[1..].iter().product();
     const ARGSORT_AXIS0_PARALLEL_MIN: usize = 1 << 20;
-    if rows < 2 || cols < 2 || rows * cols < ARGSORT_AXIS0_PARALLEL_MIN
+    if rows < 2
+        || cols < 2
+        || rows * cols < ARGSORT_AXIS0_PARALLEL_MIN
         || rayon::current_num_threads() < 2
     {
         return Ok(None);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:62206:
     // data[idx*cols+j] directly would cache-miss on every comparison — fine when the array
     // fits in cache but catastrophic under cache pressure.)
     let mut vals = vec![0.0f64; n];
-    vals.par_chunks_mut(rows).enumerate().for_each(|(j, vlane)| {
-        for (i, slot) in vlane.iter_mut().enumerate() {
-            *slot = data[i * cols + j];
-        }
-    });
+    vals.par_chunks_mut(rows)
+        .enumerate()
+        .for_each(|(j, vlane)| {
+            for (i, slot) in vlane.iter_mut().enumerate() {
+                *slot = data[i * cols + j];
+            }
+        });
     // Per-column scratch of local row-indices (0..rows), sorted by the contiguous lane values.
     let mut idx = vec![0i64; n];
     let tie = std::sync::atomic::AtomicBool::new(false);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:62238:
     }
     let out = numpy.call_method1(
         "empty",
-        (PyTuple::new(py, shape.iter().copied())?, numpy.getattr("intp")?),
+        (
+            PyTuple::new(py, shape.iter().copied())?,
+            numpy.getattr("intp")?,
+        ),
     )?;
     let out_buffer = PyBuffer::<i64>::get(&out)?;
     let Some(out_cells) = out_buffer.as_mut_slice(py) else {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:62272:
         return Ok(None);
     }
     let ndim = a.getattr("ndim")?.extract::<usize>()?;
-    if ndim < 3 || !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()? {
+    if ndim < 3
+        || !a
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
+    {
         return Ok(None);
     }
     let Some(ax) = axis_spec_middle(axis_spec, ndim) else {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:62284:
     let inner: usize = shape[ax + 1..].iter().product();
     let lanes = outer * inner;
     const ARGSORT_MIDAXIS_PARALLEL_MIN: usize = 1 << 20;
-    if alen < 2 || lanes < 2 || outer * alen * inner < ARGSORT_MIDAXIS_PARALLEL_MIN
+    if alen < 2
+        || lanes < 2
+        || outer * alen * inner < ARGSORT_MIDAXIS_PARALLEL_MIN
         || rayon::current_num_threads() < 2
     {
         return Ok(None);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:62317:
     // per-index sort comparator reads contiguous memory (strided data[base+j*inner] per comparison
     // would cache-miss on every compare). Then sort local 0..alen indices by the contiguous values.
     let mut vals = vec![0.0f64; n];
-    vals.par_chunks_mut(alen).enumerate().for_each(|(lane, vlane)| {
-        let o = lane / inner;
-        let t = lane % inner;
-        let base = o * alen * inner + t;
-        for (j, slot) in vlane.iter_mut().enumerate() {
-            *slot = src[base + j * inner];
-        }
-    });
+    vals.par_chunks_mut(alen)
+        .enumerate()
+        .for_each(|(lane, vlane)| {
+            let o = lane / inner;
+            let t = lane % inner;
+            let base = o * alen * inner + t;
+            for (j, slot) in vlane.iter_mut().enumerate() {
+                *slot = src[base + j * inner];
+            }
+        });
     let mut idx = vec![0i64; n];
     let tie = std::sync::atomic::AtomicBool::new(false);
     idx.par_chunks_mut(alen)
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:62351:
     }
     let out = numpy.call_method1(
         "empty",
-        (PyTuple::new(py, shape.iter().copied())?, numpy.getattr("intp")?),
+        (
+            PyTuple::new(py, shape.iter().copied())?,
+            numpy.getattr("intp")?,
+        ),
     )?;
     let out_buffer = PyBuffer::<i64>::get(&out)?;
     let Some(out_cells) = out_buffer.as_mut_slice(py) else {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:62361:
     let dst: &mut [i64] =
         unsafe { std::slice::from_raw_parts_mut(out_cells.as_ptr() as *mut i64, n) };
     // Scatter back: output chunk C (length inner) holds fixed (o, j) varying t. C = o*alen + j.
-    dst.par_chunks_mut(inner).enumerate().for_each(|(c, ochunk)| {
-        let o = c / alen;
-        let j = c % alen;
-        for (t, slot) in ochunk.iter_mut().enumerate() {
-            *slot = idx[(o * inner + t) * alen + j];
-        }
-    });
+    dst.par_chunks_mut(inner)
+        .enumerate()
+        .for_each(|(c, ochunk)| {
+            let o = c / alen;
+            let j = c % alen;
+            for (t, slot) in ochunk.iter_mut().enumerate() {
+                *slot = idx[(o * inner + t) * alen + j];
+            }
+        });
     Ok(Some(out.unbind()))
 }

Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:62384:
         return Ok(None);
     }
     let ndim = a.getattr("ndim")?.extract::<usize>()?;
-    if ndim < 2 || !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()? {
+    if ndim < 2
+        || !a
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
+    {
         return Ok(None);
     }
     if !axis_spec_is_last(axis_spec, ndim) {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:62394:
     let cols = shape[ndim - 1];
     let rows: usize = shape[..ndim - 1].iter().product();
     const ARGSORT_AXIS_PARALLEL_MIN: usize = 1 << 20;
-    if rows < 2 || cols < 2 || rows * cols < ARGSORT_AXIS_PARALLEL_MIN
+    if rows < 2
+        || cols < 2
+        || rows * cols < ARGSORT_AXIS_PARALLEL_MIN
         || rayon::current_num_threads() < 2
     {
         return Ok(None);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:62467:
     let ndim = a.getattr("ndim")?.extract::<usize>()?;
     if ndim < 2
         || !axis_spec_is_first(axis_spec, ndim)
-        || !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()?
+        || !a
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
     {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:62475:
     let rows = shape[0];
     let cols: usize = shape[1..].iter().product();
     const ARGSORT_AXIS0_PARALLEL_MIN: usize = 1 << 20;
-    if rows < 2 || cols < 2 || rows * cols < ARGSORT_AXIS0_PARALLEL_MIN
+    if rows < 2
+        || cols < 2
+        || rows * cols < ARGSORT_AXIS0_PARALLEL_MIN
         || rayon::current_num_threads() < 2
     {
         return Ok(None);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:62501:
         return Ok(None);
     }
     let mut vals = vec![0.0f32; n];
-    vals.par_chunks_mut(rows).enumerate().for_each(|(j, vlane)| {
-        for (i, slot) in vlane.iter_mut().enumerate() {
-            *slot = data[i * cols + j];
-        }
-    });
+    vals.par_chunks_mut(rows)
+        .enumerate()
+        .for_each(|(j, vlane)| {
+            for (i, slot) in vlane.iter_mut().enumerate() {
+                *slot = data[i * cols + j];
+            }
+        });
     let mut idx = vec![0i64; n];
     let tie = std::sync::atomic::AtomicBool::new(false);
     idx.par_chunks_mut(rows)
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:62531:
     }
     let out = numpy.call_method1(
         "empty",
-        (PyTuple::new(py, shape.iter().copied())?, numpy.getattr("intp")?),
+        (
+            PyTuple::new(py, shape.iter().copied())?,
+            numpy.getattr("intp")?,
+        ),
     )?;
     let out_buffer = PyBuffer::<i64>::get(&out)?;
     let Some(out_cells) = out_buffer.as_mut_slice(py) else {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:62561:
         return Ok(None);
     }
     let ndim = a.getattr("ndim")?.extract::<usize>()?;
-    if ndim < 3 || !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()? {
+    if ndim < 3
+        || !a
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
+    {
         return Ok(None);
     }
     let Some(ax) = axis_spec_middle(axis_spec, ndim) else {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:62573:
     let inner: usize = shape[ax + 1..].iter().product();
     let lanes = outer * inner;
     const ARGSORT_MIDAXIS_PARALLEL_MIN: usize = 1 << 20;
-    if alen < 2 || lanes < 2 || outer * alen * inner < ARGSORT_MIDAXIS_PARALLEL_MIN
+    if alen < 2
+        || lanes < 2
+        || outer * alen * inner < ARGSORT_MIDAXIS_PARALLEL_MIN
         || rayon::current_num_threads() < 2
     {
         return Ok(None);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:62603:
         return Ok(None);
     }
     let mut vals = vec![0.0f32; n];
-    vals.par_chunks_mut(alen).enumerate().for_each(|(lane, vlane)| {
-        let o = lane / inner;
-        let t = lane % inner;
-        let base = o * alen * inner + t;
-        for (j, slot) in vlane.iter_mut().enumerate() {
-            *slot = src[base + j * inner];
-        }
-    });
+    vals.par_chunks_mut(alen)
+        .enumerate()
+        .for_each(|(lane, vlane)| {
+            let o = lane / inner;
+            let t = lane % inner;
+            let base = o * alen * inner + t;
+            for (j, slot) in vlane.iter_mut().enumerate() {
+                *slot = src[base + j * inner];
+            }
+        });
     let mut idx = vec![0i64; n];
     let tie = std::sync::atomic::AtomicBool::new(false);
     idx.par_chunks_mut(alen)
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:62636:
     }
     let out = numpy.call_method1(
         "empty",
-        (PyTuple::new(py, shape.iter().copied())?, numpy.getattr("intp")?),
+        (
+            PyTuple::new(py, shape.iter().copied())?,
+            numpy.getattr("intp")?,
+        ),
     )?;
     let out_buffer = PyBuffer::<i64>::get(&out)?;
     let Some(out_cells) = out_buffer.as_mut_slice(py) else {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:62645:
     // SAFETY: fresh numpy.empty intp buffer we own (no alias).
     let dst: &mut [i64] =
         unsafe { std::slice::from_raw_parts_mut(out_cells.as_ptr() as *mut i64, n) };
-    dst.par_chunks_mut(inner).enumerate().for_each(|(c, ochunk)| {
-        let o = c / alen;
-        let j = c % alen;
-        for (t, slot) in ochunk.iter_mut().enumerate() {
-            *slot = idx[(o * inner + t) * alen + j];
-        }
-    });
+    dst.par_chunks_mut(inner)
+        .enumerate()
+        .for_each(|(c, ochunk)| {
+            let o = c / alen;
+            let j = c % alen;
+            for (t, slot) in ochunk.iter_mut().enumerate() {
+                *slot = idx[(o * inner + t) * alen + j];
+            }
+        });
     Ok(Some(out.unbind()))
 }

Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:62670:
         if let Some(kw) = kwargs {
             for (k, v) in kw.iter() {
                 if k.extract::<String>().ok().as_deref() == Some("axis") {
-                    axis_spec = Some(if v.is_none() { None } else { Some(v.extract::<isize>()?) });
+                    axis_spec = Some(if v.is_none() {
+                        None
+                    } else {
+                        Some(v.extract::<isize>()?)
+                    });
                 } else if k.extract::<String>().ok().as_deref() == Some("kind") {
                     if !v.is_none() {
                         kind_spec = Some(v.extract::<String>()?);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:62683:
         if !other_kwarg && sort_kind_fast_path(kind_spec.as_deref()).is_some() {
             let numpy = py.import("numpy")?;
             let a = args.get_item(0)?;
-            if matches!(axis_spec, None | Some(None) | Some(Some(-1)) | Some(Some(0))) {
+            if matches!(
+                axis_spec,
+                None | Some(None) | Some(Some(-1)) | Some(Some(0))
+            ) {
                 // kind='stable'/'mergesort' int/uint/float: (value, orig-index) stable argsort handles TIES
                 // byte-exactly (the default-kind paths below defer on ties). numpy ~0.9-1.2s @8M with repeats.
                 if matches!(kind_spec.as_deref(), Some("stable") | Some("mergesort"))
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:62758:
                 // structured default/quicksort/heapsort: byte-transform value-lex argsort for distinct records.
                 // Tie order is algorithm-specific, so this path detects adjacent equal keys and defers.
                 if !matches!(kind_spec.as_deref(), Some("stable") | Some("mergesort"))
-                    && let Some(out) = try_native_struct_argsort_valuelex(py, &numpy, &a, axis_spec)?
+                    && let Some(out) =
+                        try_native_struct_argsort_valuelex(py, &numpy, &a, axis_spec)?
                 {
                     return Ok(out);
                 }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:62845:
         return Ok(None);
     }
     if a.getattr("ndim")?.extract::<usize>()? != 1
-        || !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()?
+        || !a
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
     {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:62916:
     if has_negative_zero {
         output[..n].par_sort_by(|left, right| left.partial_cmp(right).expect("no NaN after check"));
     } else {
-        output[..n]
-            .par_sort_unstable_by(|left, right| left.partial_cmp(right).expect("no NaN after check"));
+        output[..n].par_sort_unstable_by(|left, right| {
+            left.partial_cmp(right).expect("no NaN after check")
+        });
     }

     for index in (0..n).rev() {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:63605:
     // Coefficients must be real (int/uint/float); complex defers. Cast to f64 (numpy
     // promotes int/float coeffs against an f64 x to f64).
     let p_arr = numpy.call_method1("asarray", (p,))?;
-    let pkind = p_arr.getattr("dtype")?.getattr("kind")?.extract::<String>()?;
+    let pkind = p_arr
+        .getattr("dtype")?
+        .getattr("kind")?
+        .extract::<String>()?;
     if pkind != "f" && pkind != "i" && pkind != "u" {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:63805:
             && let Ok(mask) = arr.bind(py).getattr("mask")
             && mask.is_instance(&numpy.getattr("ndarray")?)?
             && let (Ok(mshape), Ok(dshape)) = (
-                mask.getattr("shape").and_then(|s| s.extract::<Vec<usize>>()),
-                arr.bind(py).getattr("shape").and_then(|s| s.extract::<Vec<usize>>()),
+                mask.getattr("shape")
+                    .and_then(|s| s.extract::<Vec<usize>>()),
+                arr.bind(py)
+                    .getattr("shape")
+                    .and_then(|s| s.extract::<Vec<usize>>()),
             )
             && mshape == dshape
-            && mask.getattr("dtype")?.getattr("kind")?.extract::<String>()? == "b"
+            && mask
+                .getattr("dtype")?
+                .getattr("kind")?
+                .extract::<String>()?
+                == "b"
         {
             return Ok(mask.unbind());
         }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:63935:
     if !numpy_dtype_is_f64(py, &data_obj) {
         return Ok(None);
     }
-    let Ok(shape) = value.getattr("shape").and_then(|s| s.extract::<Vec<usize>>()) else {
+    let Ok(shape) = value
+        .getattr("shape")
+        .and_then(|s| s.extract::<Vec<usize>>())
+    else {
         return Ok(None);
     };
     let total: usize = shape.iter().product();
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:64206:
             .and_then(|s| s.extract::<Vec<usize>>())
         && shape.len() == 2
         && shape[0] == shape[1]
-        && a
-            .bind(py)
+        && a.bind(py)
             .getattr("dtype")
             .and_then(|d| d.getattr("kind"))
             .and_then(|k| k.extract::<String>())
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:64266:
             val_shape.push(n);
             let eval_arr =
                 UFuncArray::new(val_shape, eigenvalues, DType::F64).map_err(map_ufunc_error)?;
-            let evec_arr = UFuncArray::new(owned_shape, eigenvectors, DType::F64)
-                .map_err(map_ufunc_error)?;
+            let evec_arr =
+                UFuncArray::new(owned_shape, eigenvectors, DType::F64).map_err(map_ufunc_error)?;
             return build_numpy_eigh_result(py, &eval_arr, &evec_arr);
         }
         return fallback();
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:64686:
         }
         // SAFETY: ReadOnlyCell<T>/Cell<T> are repr(transparent) over T; the two inputs are read-only
         // under the GIL and `out` is a fresh numpy.empty we own (no alias). Rows are disjoint sub-slices.
-        let a_data: &[T] =
-            unsafe { std::slice::from_raw_parts(a_in.as_ptr().cast::<T>(), total) };
-        let b_data: &[T] =
-            unsafe { std::slice::from_raw_parts(b_in.as_ptr().cast::<T>(), total) };
+        let a_data: &[T] = unsafe { std::slice::from_raw_parts(a_in.as_ptr().cast::<T>(), total) };
+        let b_data: &[T] = unsafe { std::slice::from_raw_parts(b_in.as_ptr().cast::<T>(), total) };
         let out_data: &mut [T] =
             unsafe { std::slice::from_raw_parts_mut(output.as_ptr() as *mut T, total) };
         let (a0, a1, a2) = (&a_data[0..n], &a_data[n..2 * n], &a_data[2 * n..3 * n]);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:64745:
     if a_shape.len() != 2 || a_shape[0] != 3 || a_shape != b_shape {
         return Ok(None);
     }
-    if !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()?
-        || !b.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()?
+    if !a
+        .getattr("flags")?
+        .getattr("c_contiguous")?
+        .extract::<bool>()?
+        || !b
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
     {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:64793:
     {
         let axis_is_first_2d = |k: i64| k == 0 || k == -2;
         let is_axis0_case = match axis.as_ref() {
-            Some(v) => v.bind(py).extract::<i64>().map(axis_is_first_2d).unwrap_or(false),
+            Some(v) => v
+                .bind(py)
+                .extract::<i64>()
+                .map(axis_is_first_2d)
+                .unwrap_or(false),
             None => axis_is_first_2d(axisa) && axis_is_first_2d(axisb) && axis_is_first_2d(axisc),
         };
-        if is_axis0_case
-            && let Some(out) = try_zerocopy_cross_axis0_3n(py, a.bind(py), b.bind(py))?
+        if is_axis0_case && let Some(out) = try_zerocopy_cross_axis0_3n(py, a.bind(py), b.bind(py))?
         {
             return Ok(out);
         }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:65274:
             // own (no alias). Each par_chunks_mut(m) chunk is exactly one output row (a[i]*b).
             let out_raw: &mut [f64] =
                 unsafe { std::slice::from_raw_parts_mut(output.as_ptr() as *mut f64, total) };
-            out_raw.par_chunks_mut(m).enumerate().for_each(|(i, out_row)| {
-                let ai = avals[i];
-                for (o, &bv) in out_row.iter_mut().zip(bvals.iter()) {
-                    *o = ai * bv;
-                }
-            });
+            out_raw
+                .par_chunks_mut(m)
+                .enumerate()
+                .for_each(|(i, out_row)| {
+                    let ai = avals[i];
+                    for (o, &bv) in out_row.iter_mut().zip(bvals.iter()) {
+                        *o = ai * bv;
+                    }
+                });
         } else {
             for (i, ai_cell) in a_in.iter().enumerate() {
                 let ai = ai_cell.get();
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:66083:
                             // all-zero / fully singular matrix) to +Inf whenever the
                             // input had no NaN entries. Our gate already requires
                             // all-finite input, so every NaN here becomes Inf.
-                            out.push(if ratio.is_nan() {
-                                f64::INFINITY
-                            } else {
-                                ratio
-                            });
+                            out.push(if ratio.is_nan() { f64::INFINITY } else { ratio });
                         }
-                        let out_shape: Vec<usize> =
-                            owned_shape[..owned_shape.len() - 2].to_vec();
-                        let result = UFuncArray::new(out_shape, out, DType::F64)
-                            .map_err(map_ufunc_error)?;
+                        let out_shape: Vec<usize> = owned_shape[..owned_shape.len() - 2].to_vec();
+                        let result =
+                            UFuncArray::new(out_shape, out, DType::F64).map_err(map_ufunc_error)?;
                         return build_numpy_array_from_ufunc(py, &result);
                     }
                 }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:66112:
 fn svd_matrix_norm_mode(py: Python<'_>, ord: &Py<PyAny>) -> Option<u8> {
     let bound = ord.bind(py);
     if let Ok(text) = bound.extract::<String>() {
-        if text == "nuc" {
-            Some(2)
-        } else {
-            None
-        }
+        if text == "nuc" { Some(2) } else { None }
     } else if let Ok(f) = bound.extract::<f64>() {
         if f == 2.0 {
             Some(0)
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:67189:
             for dd in 0..d {
                 let raw = in_raw[dd][i];
                 let coord = match mode_kind {
-                    1 => raw.clamp(0, dims[dd] - 1),  // clip
-                    2 => raw.rem_euclid(dims[dd]),    // wrap
+                    1 => raw.clamp(0, dims[dd] - 1), // clip
+                    2 => raw.rem_euclid(dims[dd]),   // wrap
                     _ => {
                         if raw < 0 || raw >= dims[dd] {
                             oob.store(true, Ordering::Relaxed);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:67560:
     }
     use rayon::prelude::*;
     // SAFETY: ReadOnlyCell<T>/<i64> are repr(transparent); read-only under the GIL.
-    let idx_raw: &[i64] = unsafe { std::slice::from_raw_parts(idx_in.as_ptr() as *const i64, total_idx) };
-    let val_raw: &[T] = unsafe { std::slice::from_raw_parts(val_in.as_ptr() as *const T, total_idx) };
+    let idx_raw: &[i64] =
+        unsafe { std::slice::from_raw_parts(idx_in.as_ptr() as *const i64, total_idx) };
+    let val_raw: &[T] =
+        unsafe { std::slice::from_raw_parts(val_in.as_ptr() as *const T, total_idx) };
     let normalize = |k: i64| if k < 0 { k + la_i } else { k };
     const SCATTER_PAR_MIN: usize = 1 << 16;
     let par_gate = total_idx >= SCATTER_PAR_MIN && outer >= 2 && rayon::current_num_threads() >= 2;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:67958:
     };
     let restored = flat.call_method1("view", (numpy.getattr(orig_name.as_str())?,))?;
     let output_shape = PyTuple::new(py, s_idx.iter().copied())?;
-    Ok(Some(restored.call_method1("reshape", (&output_shape,))?.unbind()))
+    Ok(Some(
+        restored.call_method1("reshape", (&output_shape,))?.unbind(),
+    ))
 }

 #[pyfunction]
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:67992:

     // Check for complex dtype and fallback to numpy
     let numpy = py.import("numpy")?;
-    let arr_dtype_o = numpy.call_method1("asarray", (arr.bind(py),))?.getattr("dtype")?;
+    let arr_dtype_o = numpy
+        .call_method1("asarray", (arr.bind(py),))?
+        .getattr("dtype")?;
     let dtype_kind = arr_dtype_o.getattr("kind")?.extract::<String>()?;
     let dtype_itemsize = arr_dtype_o.getattr("itemsize")?.extract::<usize>()?;
     // complex128 (16-byte, no primitive mover) delegates; complex64 (8-byte) is handled by the
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:68005:
     // indices); covers int/float/bool/complex64 via a uintN bit-view. Skips the cold extract +
     // rebuild. Bit-identical; broadcast, axis=None, OOB indices, and other layouts
     // fall through.
-    if let Some(out) =
-        try_zerocopy_take_along_axis(py, arr.bind(py), indices.bind(py), axis)?
-    {
+    if let Some(out) = try_zerocopy_take_along_axis(py, arr.bind(py), indices.bind(py), axis)? {
         return Ok(out);
     }

Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:68043:
     // (same module, same getattr+call). Large-array ops are unaffected (amortized).
     static NUMPY_MODULE: PyOnceLock<Py<PyModule>> = PyOnceLock::new();
     let numpy = NUMPY_MODULE
-        .get_or_try_init(py, || -> PyResult<Py<PyModule>> { Ok(py.import("numpy")?.unbind()) })?
+        .get_or_try_init(py, || -> PyResult<Py<PyModule>> {
+            Ok(py.import("numpy")?.unbind())
+        })?
         .bind(py);
     Ok(numpy.getattr(name)?.call(args, kwargs)?.unbind())
 }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:68217:
     };
     use rayon::prelude::*;
     const SUM_AXIS_PARALLEL_MIN: usize = 98_304;
-    let parallel =
-        outer * axis_len >= SUM_AXIS_PARALLEL_MIN && rayon::current_num_threads() >= 2;
+    let parallel = outer * axis_len >= SUM_AXIS_PARALLEL_MIN && rayon::current_num_threads() >= 2;
     let out: Vec<f64> = if parallel {
         data.par_chunks_exact(axis_len).map(lane_sum).collect()
     } else {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:68289:
         && initial.as_ref().is_none_or(|v| v.bind(py).is_none())
         && let Some(ax) = axis.as_ref().filter(|v| !v.bind(py).is_none())
         && let Ok(ax_i) = ax.bind(py).extract::<isize>()
-        && let Some(o) = try_zerocopy_f16_sum_nonlast_axis(py, a.bind(py), Some(ax_i), keepdims, false)?
+        && let Some(o) =
+            try_zerocopy_f16_sum_nonlast_axis(py, a.bind(py), Some(ax_i), keepdims, false)?
     {
         return Ok(o);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:68413:
                 };
                 use rayon::prelude::*;
                 const PROD_AXIS_PARALLEL_MIN: usize = 1 << 18;
-                if outer * axis_len >= PROD_AXIS_PARALLEL_MIN
-                    && rayon::current_num_threads() >= 2
-                {
+                if outer * axis_len >= PROD_AXIS_PARALLEL_MIN && rayon::current_num_threads() >= 2 {
                     out_raw
                         .par_iter_mut()
                         .zip(in_raw.par_chunks(axis_len))
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:68586:
         return Ok(Some(zerod.get_item(())?.unbind()));
     }
     let output_shape = PyTuple::new(py, out_shape.iter().copied())?;
-    Ok(Some(flat.call_method1("reshape", (&output_shape,))?.unbind()))
+    Ok(Some(
+        flat.call_method1("reshape", (&output_shape,))?.unbind(),
+    ))
 }

 fn try_zerocopy_f64_minmax(
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:69065:
         return Ok(Some(zerod.get_item(())?.unbind()));
     }
     let output_shape = PyTuple::new(py, out_shape.iter().copied())?;
-    let output = flat_bool.call_method1("reshape", (&output_shape,))?.unbind();
+    let output = flat_bool
+        .call_method1("reshape", (&output_shape,))?
+        .unbind();
     Ok(Some(output))
 }

Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:69361:
         && out.as_ref().is_none_or(|v| v.bind(py).is_none())
         && let DdofArg::Native(d) = &ddof
         && let Some(ax) = axis.as_ref().filter(|v| !v.bind(py).is_none())
-        && let Some(o) =
-            try_zerocopy_f64_var_axis(py, a.bind(py), ax.bind(py), *d, true, keepdims)?
+        && let Some(o) = try_zerocopy_f64_var_axis(py, a.bind(py), ax.bind(py), *d, true, keepdims)?
     {
         return Ok(o);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:69373:
         && out.as_ref().is_none_or(|v| v.bind(py).is_none())
         && let DdofArg::Native(d) = &ddof
         && let Some(ax) = axis.as_ref().filter(|v| !v.bind(py).is_none())
-        && let Some(o) = try_zerocopy_f64_var_axis0(py, a.bind(py), ax.bind(py), *d, true, keepdims)?
+        && let Some(o) =
+            try_zerocopy_f64_var_axis0(py, a.bind(py), ax.bind(py), *d, true, keepdims)?
     {
         return Ok(o);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:69419:
         && let DdofArg::Native(d) = &ddof
         && let Some(ax) = axis.as_ref().filter(|v| !v.bind(py).is_none())
         && let Ok(ax_i) = ax.bind(py).extract::<isize>()
-        && let Some(o) =
-            try_zerocopy_f16_nanvar_nonlast_axis(py, a.bind(py), Some(ax_i), *d, true, keepdims, false)?
+        && let Some(o) = try_zerocopy_f16_nanvar_nonlast_axis(
+            py,
+            a.bind(py),
+            Some(ax_i),
+            *d,
+            true,
+            keepdims,
+            false,
+        )?
     {
         return Ok(o);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:69485:
         && out.as_ref().is_none_or(|v| v.bind(py).is_none())
         && let DdofArg::Native(d) = &ddof
         && let Some(ax) = axis.as_ref().filter(|v| !v.bind(py).is_none())
-        && let Some(o) = try_zerocopy_f64_var_axis0(py, a.bind(py), ax.bind(py), *d, false, keepdims)?
+        && let Some(o) =
+            try_zerocopy_f64_var_axis0(py, a.bind(py), ax.bind(py), *d, false, keepdims)?
     {
         return Ok(o);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:69518:
         && let DdofArg::Native(d) = &ddof
         && let Some(ax) = axis.as_ref().filter(|v| !v.bind(py).is_none())
         && let Ok(ax_i) = ax.bind(py).extract::<isize>()
-        && let Some(o) =
-            try_zerocopy_f16_nanvar_lastaxis(py, a.bind(py), Some(ax_i), *d, false, keepdims, false)?
+        && let Some(o) = try_zerocopy_f16_nanvar_lastaxis(
+            py,
+            a.bind(py),
+            Some(ax_i),
+            *d,
+            false,
+            keepdims,
+            false,
+        )?
     {
         return Ok(o);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:69531:
         && let DdofArg::Native(d) = &ddof
         && let Some(ax) = axis.as_ref().filter(|v| !v.bind(py).is_none())
         && let Ok(ax_i) = ax.bind(py).extract::<isize>()
-        && let Some(o) =
-            try_zerocopy_f16_nanvar_nonlast_axis(py, a.bind(py), Some(ax_i), *d, false, keepdims, false)?
+        && let Some(o) = try_zerocopy_f16_nanvar_nonlast_axis(
+            py,
+            a.bind(py),
+            Some(ax_i),
+            *d,
+            false,
+            keepdims,
+            false,
+        )?
     {
         return Ok(o);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:69975:
     // scalar fold. Delegate (axis=None bool and f64 stay native).
     if axis_val.is_some()
         && a.bind(py).is_exact_instance(&numpy.getattr("ndarray")?)
-        && a.bind(py).getattr("dtype")?.getattr("kind")?.extract::<String>()? == "b"
+        && a.bind(py)
+            .getattr("dtype")?
+            .getattr("kind")?
+            .extract::<String>()?
+            == "b"
     {
         return fallback();
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:70069:
     // scalar fold. Delegate (axis=None bool and f64 stay native).
     if axis_val.is_some()
         && a.bind(py).is_exact_instance(&numpy.getattr("ndarray")?)
-        && a.bind(py).getattr("dtype")?.getattr("kind")?.extract::<String>()? == "b"
+        && a.bind(py)
+            .getattr("dtype")?
+            .getattr("kind")?
+            .extract::<String>()?
+            == "b"
     {
         return fallback();
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:70151:
         unsafe { std::slice::from_raw_parts_mut(out_cells.as_ptr() as *mut T, total) };
     use rayon::prelude::*;
     let lane = cols * 2;
-    dst.par_chunks_mut(lane).zip(src.par_chunks(lane)).for_each(|(orow, irow)| {
-        let mut re = T::default();
-        let mut im = T::default();
-        for c in 0..cols {
-            re = re + irow[2 * c];
-            orow[2 * c] = re;
-            im = im + irow[2 * c + 1];
-            orow[2 * c + 1] = im;
-        }
-    });
+    dst.par_chunks_mut(lane)
+        .zip(src.par_chunks(lane))
+        .for_each(|(orow, irow)| {
+            let mut re = T::default();
+            let mut im = T::default();
+            for c in 0..cols {
+                re = re + irow[2 * c];
+                orow[2 * c] = re;
+                im = im + irow[2 * c + 1];
+                orow[2 * c + 1] = im;
+            }
+        });
     Ok(Some(out.unbind()))
 }

Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:70182:
     }
     let itemsize = dt.getattr("itemsize")?.extract::<usize>()?;
     let ndim = a.getattr("ndim")?.extract::<usize>()?;
-    if ndim < 2 || !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()? {
+    if ndim < 2
+        || !a
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
+    {
         return Ok(None);
     }
     if !axis_spec_is_last(axis.map(Some), ndim) {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:70191:
     let shape: Vec<usize> = a.getattr("shape")?.extract()?;
     let cols = shape[ndim - 1];
     let rows: usize = shape[..ndim - 1].iter().product();
-    if rows < 2 || cols < 2 || rows * cols < COMPLEX_CUMSUM_PARALLEL_MIN
+    if rows < 2
+        || cols < 2
+        || rows * cols < COMPLEX_CUMSUM_PARALLEL_MIN
         || rayon::current_num_threads() < 2
     {
         return Ok(None);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:70198:
     }
     match itemsize {
         16 => complex_cumsum_lastaxis_typed::<f64>(
-            py, &numpy, a, "float64", "complex128", &shape, rows, cols,
+            py,
+            &numpy,
+            a,
+            "float64",
+            "complex128",
+            &shape,
+            rows,
+            cols,
         ),
         8 => complex_cumsum_lastaxis_typed::<f32>(
-            py, &numpy, a, "float32", "complex64", &shape, rows, cols,
+            py,
+            &numpy,
+            a,
+            "float32",
+            "complex64",
+            &shape,
+            rows,
+            cols,
         ),
         _ => Ok(None),
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:70267:
         unsafe { std::slice::from_raw_parts_mut(out_cells.as_ptr() as *mut T, total) };
     use rayon::prelude::*;
     let lane = cols * 2;
-    dst.par_chunks_mut(lane).zip(src.par_chunks(lane)).for_each(|(orow, irow)| {
-        // accumulate semantics: out[0] = in[0]; out[c] = out[c-1] * in[c] (naive cmul, acc on left)
-        let mut re = irow[0];
-        let mut im = irow[1];
-        orow[0] = re;
-        orow[1] = im;
-        for c in 1..cols {
-            let xr = irow[2 * c];
-            let xi = irow[2 * c + 1];
-            let nr = re * xr - im * xi;
-            let ni = re * xi + im * xr;
-            re = nr;
-            im = ni;
-            orow[2 * c] = re;
-            orow[2 * c + 1] = im;
-        }
-    });
+    dst.par_chunks_mut(lane)
+        .zip(src.par_chunks(lane))
+        .for_each(|(orow, irow)| {
+            // accumulate semantics: out[0] = in[0]; out[c] = out[c-1] * in[c] (naive cmul, acc on left)
+            let mut re = irow[0];
+            let mut im = irow[1];
+            orow[0] = re;
+            orow[1] = im;
+            for c in 1..cols {
+                let xr = irow[2 * c];
+                let xi = irow[2 * c + 1];
+                let nr = re * xr - im * xi;
+                let ni = re * xi + im * xr;
+                re = nr;
+                im = ni;
+                orow[2 * c] = re;
+                orow[2 * c + 1] = im;
+            }
+        });
     Ok(Some(out.unbind()))
 }

Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:70305:
     }
     let itemsize = dt.getattr("itemsize")?.extract::<usize>()?;
     let ndim = a.getattr("ndim")?.extract::<usize>()?;
-    if ndim < 2 || !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()? {
+    if ndim < 2
+        || !a
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
+    {
         return Ok(None);
     }
     if !axis_spec_is_last(axis.map(Some), ndim) {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:70314:
     let shape: Vec<usize> = a.getattr("shape")?.extract()?;
     let cols = shape[ndim - 1];
     let rows: usize = shape[..ndim - 1].iter().product();
-    if rows < 2 || cols < 2 || rows * cols < COMPLEX_CUMPROD_PARALLEL_MIN
+    if rows < 2
+        || cols < 2
+        || rows * cols < COMPLEX_CUMPROD_PARALLEL_MIN
         || rayon::current_num_threads() < 2
     {
         return Ok(None);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:70321:
     }
     match itemsize {
         16 => complex_cumprod_lastaxis_typed::<f64>(
-            py, &numpy, a, "float64", "complex128", &shape, rows, cols,
+            py,
+            &numpy,
+            a,
+            "float64",
+            "complex128",
+            &shape,
+            rows,
+            cols,
         ),
         8 => complex_cumprod_lastaxis_typed::<f32>(
-            py, &numpy, a, "float32", "complex64", &shape, rows, cols,
+            py,
+            &numpy,
+            a,
+            "float32",
+            "complex64",
+            &shape,
+            rows,
+            cols,
         ),
         _ => Ok(None),
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:70399:
         unsafe { std::slice::from_raw_parts_mut(out_cells.as_ptr() as *mut T, rows * 2) };
     use rayon::prelude::*;
     let lane = cols * 2;
-    dst.par_chunks_mut(2).zip(src.par_chunks(lane)).for_each(|(ocell, irow)| {
-        // reduce semantics: acc = in[0]; acc = acc * in[c] for c in 1..cols (naive cmul, acc on left)
-        let mut re = irow[0];
-        let mut im = irow[1];
-        for c in 1..cols {
-            let xr = irow[2 * c];
-            let xi = irow[2 * c + 1];
-            let nr = re * xr - im * xi;
-            let ni = re * xi + im * xr;
-            re = nr;
-            im = ni;
-        }
-        ocell[0] = re;
-        ocell[1] = im;
-    });
+    dst.par_chunks_mut(2)
+        .zip(src.par_chunks(lane))
+        .for_each(|(ocell, irow)| {
+            // reduce semantics: acc = in[0]; acc = acc * in[c] for c in 1..cols (naive cmul, acc on left)
+            let mut re = irow[0];
+            let mut im = irow[1];
+            for c in 1..cols {
+                let xr = irow[2 * c];
+                let xi = irow[2 * c + 1];
+                let nr = re * xr - im * xi;
+                let ni = re * xi + im * xr;
+                re = nr;
+                im = ni;
+            }
+            ocell[0] = re;
+            ocell[1] = im;
+        });
     Ok(Some(out.unbind()))
 }

Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:70437:
     }
     let itemsize = dt.getattr("itemsize")?.extract::<usize>()?;
     let ndim = a.getattr("ndim")?.extract::<usize>()?;
-    if ndim < 2 || !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()? {
+    if ndim < 2
+        || !a
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
+    {
         return Ok(None);
     }
     // `axis` is None ONLY for a flatten reduction (np.prod(a) / axis=None -> scalar), which this
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:70449:
     let shape: Vec<usize> = a.getattr("shape")?.extract()?;
     let cols = shape[ndim - 1];
     let rows: usize = shape[..ndim - 1].iter().product();
-    if rows < 2 || cols < 2 || rows * cols < COMPLEX_PROD_PARALLEL_MIN
+    if rows < 2
+        || cols < 2
+        || rows * cols < COMPLEX_PROD_PARALLEL_MIN
         || rayon::current_num_threads() < 2
     {
         return Ok(None);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:70461:
     }
     match itemsize {
         16 => complex_prod_lastaxis_typed::<f64>(
-            py, &numpy, a, "float64", "complex128", &out_shape, rows, cols,
+            py,
+            &numpy,
+            a,
+            "float64",
+            "complex128",
+            &out_shape,
+            rows,
+            cols,
         ),
         8 => complex_prod_lastaxis_typed::<f32>(
-            py, &numpy, a, "float32", "complex64", &out_shape, rows, cols,
+            py,
+            &numpy,
+            a,
+            "float32",
+            "complex64",
+            &out_shape,
+            rows,
+            cols,
         ),
         _ => Ok(None),
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:70537:
     use rayon::prelude::*;
     let lane = cols * 2;
     let zero = T::default();
-    dst.par_chunks_mut(2).zip(src.par_chunks(lane)).for_each(|(ocell, irow)| {
-        // NaN-complex (re OR im NaN) -> (ident=1, 0); else the value verbatim. Sequential naive cmul.
-        let rep = |r: T, i: T| -> (T, T) {
-            if is_nan(r) || is_nan(i) { (ident, zero) } else { (r, i) }
-        };
-        let (mut re, mut im) = rep(irow[0], irow[1]);
-        for c in 1..cols {
-            let (xr, xi) = rep(irow[2 * c], irow[2 * c + 1]);
-            let nr = re * xr - im * xi;
-            let ni = re * xi + im * xr;
-            re = nr;
-            im = ni;
-        }
-        ocell[0] = re;
-        ocell[1] = im;
-    });
+    dst.par_chunks_mut(2)
+        .zip(src.par_chunks(lane))
+        .for_each(|(ocell, irow)| {
+            // NaN-complex (re OR im NaN) -> (ident=1, 0); else the value verbatim. Sequential naive cmul.
+            let rep = |r: T, i: T| -> (T, T) {
+                if is_nan(r) || is_nan(i) {
+                    (ident, zero)
+                } else {
+                    (r, i)
+                }
+            };
+            let (mut re, mut im) = rep(irow[0], irow[1]);
+            for c in 1..cols {
+                let (xr, xi) = rep(irow[2 * c], irow[2 * c + 1]);
+                let nr = re * xr - im * xi;
+                let ni = re * xi + im * xr;
+                re = nr;
+                im = ni;
+            }
+            ocell[0] = re;
+            ocell[1] = im;
+        });
     Ok(Some(out.unbind()))
 }

Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:70576:
     }
     let itemsize = dt.getattr("itemsize")?.extract::<usize>()?;
     let ndim = a.getattr("ndim")?.extract::<usize>()?;
-    if ndim < 2 || !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()? {
+    if ndim < 2
+        || !a
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
+    {
         return Ok(None);
     }
     if !axis_spec_is_last(Some(Some(axis)), ndim) {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:70585:
     let shape: Vec<usize> = a.getattr("shape")?.extract()?;
     let cols = shape[ndim - 1];
     let rows: usize = shape[..ndim - 1].iter().product();
-    if rows < 2 || cols < 2 || rows * cols < COMPLEX_NANPROD_PARALLEL_MIN
+    if rows < 2
+        || cols < 2
+        || rows * cols < COMPLEX_NANPROD_PARALLEL_MIN
         || rayon::current_num_threads() < 2
     {
         return Ok(None);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:70596:
     }
     match itemsize {
         16 => complex_nanprod_lastaxis_typed::<f64>(
-            py, &numpy, a, "float64", "complex128", &out_shape, rows, cols, 1.0_f64, f64::is_nan,
+            py,
+            &numpy,
+            a,
+            "float64",
+            "complex128",
+            &out_shape,
+            rows,
+            cols,
+            1.0_f64,
+            f64::is_nan,
         ),
         8 => complex_nanprod_lastaxis_typed::<f32>(
-            py, &numpy, a, "float32", "complex64", &out_shape, rows, cols, 1.0_f32, f32::is_nan,
+            py,
+            &numpy,
+            a,
+            "float32",
+            "complex64",
+            &out_shape,
+            rows,
+            cols,
+            1.0_f32,
+            f32::is_nan,
         ),
         _ => Ok(None),
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:70668:
     use rayon::prelude::*;
     let lane = cols * 2;
     let zero = T::default();
-    dst.par_chunks_mut(lane).zip(src.par_chunks(lane)).for_each(|(orow, irow)| {
-        // NaN-complex (re OR im NaN) -> (ident, 0); else the value verbatim.
-        let rep = |r: T, i: T| -> (T, T) {
-            if is_nan(r) || is_nan(i) { (ident, zero) } else { (r, i) }
-        };
-        let (mut re, mut im) = rep(irow[0], irow[1]);
-        orow[0] = re;
-        orow[1] = im;
-        for c in 1..cols {
-            let (xr, xi) = rep(irow[2 * c], irow[2 * c + 1]);
-            if is_prod {
-                let nr = re * xr - im * xi;
-                let ni = re * xi + im * xr;
-                re = nr;
-                im = ni;
-            } else {
-                re = re + xr;
-                im = im + xi;
+    dst.par_chunks_mut(lane)
+        .zip(src.par_chunks(lane))
+        .for_each(|(orow, irow)| {
+            // NaN-complex (re OR im NaN) -> (ident, 0); else the value verbatim.
+            let rep = |r: T, i: T| -> (T, T) {
+                if is_nan(r) || is_nan(i) {
+                    (ident, zero)
+                } else {
+                    (r, i)
+                }
+            };
+            let (mut re, mut im) = rep(irow[0], irow[1]);
+            orow[0] = re;
+            orow[1] = im;
+            for c in 1..cols {
+                let (xr, xi) = rep(irow[2 * c], irow[2 * c + 1]);
+                if is_prod {
+                    let nr = re * xr - im * xi;
+                    let ni = re * xi + im * xr;
+                    re = nr;
+                    im = ni;
+                } else {
+                    re = re + xr;
+                    im = im + xi;
+                }
+                orow[2 * c] = re;
+                orow[2 * c + 1] = im;
             }
-            orow[2 * c] = re;
-            orow[2 * c + 1] = im;
-        }
-    });
+        });
     Ok(Some(out.unbind()))
 }

Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:70713:
     }
     let itemsize = dt.getattr("itemsize")?.extract::<usize>()?;
     let ndim = a.getattr("ndim")?.extract::<usize>()?;
-    if ndim < 2 || !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()? {
+    if ndim < 2
+        || !a
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
+    {
         return Ok(None);
     }
     if !axis_spec_is_last(axis.map(Some), ndim) {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:70722:
     let shape: Vec<usize> = a.getattr("shape")?.extract()?;
     let cols = shape[ndim - 1];
     let rows: usize = shape[..ndim - 1].iter().product();
-    if rows < 2 || cols < 2 || rows * cols < COMPLEX_NANCUM_PARALLEL_MIN
+    if rows < 2
+        || cols < 2
+        || rows * cols < COMPLEX_NANCUM_PARALLEL_MIN
         || rayon::current_num_threads() < 2
     {
         return Ok(None);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:70729:
     }
     match itemsize {
         16 => complex_nancumulative_lastaxis_typed::<f64>(
-            py, &numpy, a, "float64", "complex128", &shape, rows, cols, is_prod,
-            if is_prod { 1.0_f64 } else { 0.0_f64 }, f64::is_nan,
+            py,
+            &numpy,
+            a,
+            "float64",
+            "complex128",
+            &shape,
+            rows,
+            cols,
+            is_prod,
+            if is_prod { 1.0_f64 } else { 0.0_f64 },
+            f64::is_nan,
         ),
         8 => complex_nancumulative_lastaxis_typed::<f32>(
-            py, &numpy, a, "float32", "complex64", &shape, rows, cols, is_prod,
-            if is_prod { 1.0_f32 } else { 0.0_f32 }, f32::is_nan,
+            py,
+            &numpy,
+            a,
+            "float32",
+            "complex64",
+            &shape,
+            rows,
+            cols,
+            is_prod,
+            if is_prod { 1.0_f32 } else { 0.0_f32 },
+            f32::is_nan,
         ),
         _ => Ok(None),
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:70804:
     let zero = T::default();
     let scan_block = |(oblk, iblk): (&mut [T], &[T])| {
         let rep = |r: T, i: T| -> (T, T) {
-            if is_nan(r) || is_nan(i) { (ident, zero) } else { (r, i) }
+            if is_nan(r) || is_nan(i) {
+                (ident, zero)
+            } else {
+                (r, i)
+            }
         };
         // First slab: NaN-replaced verbatim.
         for k in 0..inner {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:70829:
             }
         }
     };
-    dst.par_chunks_mut(lane).zip(src.par_chunks(lane)).for_each(scan_block);
+    dst.par_chunks_mut(lane)
+        .zip(src.par_chunks(lane))
+        .for_each(scan_block);
     Ok(Some(out.unbind()))
 }

Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:70852:
     }
     let itemsize = dt.getattr("itemsize")?.extract::<usize>()?;
     let ndim = a.getattr("ndim")?.extract::<usize>()?;
-    if ndim < 3 || !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()? {
+    if ndim < 3
+        || !a
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
+    {
         return Ok(None);
     }
     let Some(ax_raw) = axis else {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:70859:
         return Ok(None);
     };
-    let norm = if ax_raw < 0 { ax_raw + ndim as isize } else { ax_raw };
+    let norm = if ax_raw < 0 {
+        ax_raw + ndim as isize
+    } else {
+        ax_raw
+    };
     if norm <= 0 || norm >= ndim as isize - 1 {
         return Ok(None); // only MIDDLE axes (outer >= 2); axis-0 and last defer
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:70877:
     }
     match itemsize {
         16 => complex_nancumulative_nonlast_typed::<f64>(
-            py, &numpy, a, "float64", "complex128", &shape, outer, axis_len, inner, is_prod,
-            if is_prod { 1.0_f64 } else { 0.0_f64 }, f64::is_nan,
+            py,
+            &numpy,
+            a,
+            "float64",
+            "complex128",
+            &shape,
+            outer,
+            axis_len,
+            inner,
+            is_prod,
+            if is_prod { 1.0_f64 } else { 0.0_f64 },
+            f64::is_nan,
         ),
         8 => complex_nancumulative_nonlast_typed::<f32>(
-            py, &numpy, a, "float32", "complex64", &shape, outer, axis_len, inner, is_prod,
-            if is_prod { 1.0_f32 } else { 0.0_f32 }, f32::is_nan,
+            py,
+            &numpy,
+            a,
+            "float32",
+            "complex64",
+            &shape,
+            outer,
+            axis_len,
+            inner,
+            is_prod,
+            if is_prod { 1.0_f32 } else { 0.0_f32 },
+            f32::is_nan,
         ),
         _ => Ok(None),
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:70971:
             }
         }
     };
-    dst.par_chunks_mut(lane).zip(src.par_chunks(lane)).for_each(scan_block);
+    dst.par_chunks_mut(lane)
+        .zip(src.par_chunks(lane))
+        .for_each(scan_block);
     Ok(Some(out.unbind()))
 }

Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:71023:
     use rayon::prelude::*;
     // Gather each column j into a contiguous lane (2*rows reals), then scan the lane in place.
     let mut vals = vec![T::default(); total];
-    vals.par_chunks_mut(2 * rows).enumerate().for_each(|(j, vlane)| {
-        for i in 0..rows {
-            let s = (i * cols + j) * 2;
-            vlane[2 * i] = src[s];
-            vlane[2 * i + 1] = src[s + 1];
-        }
-        let mut re = vlane[0];
-        let mut im = vlane[1];
-        for i in 1..rows {
-            let xr = vlane[2 * i];
-            let xi = vlane[2 * i + 1];
-            if is_prod {
-                let nr = re * xr - im * xi;
-                let ni = re * xi + im * xr;
-                re = nr;
-                im = ni;
-            } else {
-                re = re + xr;
-                im = im + xi;
+    vals.par_chunks_mut(2 * rows)
+        .enumerate()
+        .for_each(|(j, vlane)| {
+            for i in 0..rows {
+                let s = (i * cols + j) * 2;
+                vlane[2 * i] = src[s];
+                vlane[2 * i + 1] = src[s + 1];
             }
-            vlane[2 * i] = re;
-            vlane[2 * i + 1] = im;
-        }
-    });
+            let mut re = vlane[0];
+            let mut im = vlane[1];
+            for i in 1..rows {
+                let xr = vlane[2 * i];
+                let xi = vlane[2 * i + 1];
+                if is_prod {
+                    let nr = re * xr - im * xi;
+                    let ni = re * xi + im * xr;
+                    re = nr;
+                    im = ni;
+                } else {
+                    re = re + xr;
+                    im = im + xi;
+                }
+                vlane[2 * i] = re;
+                vlane[2 * i + 1] = im;
+            }
+        });
     let kwargs = PyDict::new(py);
     kwargs.set_item("dtype", complex_name)?;
     let shape_tuple = PyTuple::new(py, shape.iter().copied())?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:71062:
     // disjoint contiguous `2*cols` range.
     let dst: &mut [T] =
         unsafe { std::slice::from_raw_parts_mut(out_cells.as_ptr() as *mut T, total) };
-    dst.par_chunks_mut(cols * 2).enumerate().for_each(|(i, orow)| {
-        for j in 0..cols {
-            let v = (j * rows + i) * 2;
-            orow[2 * j] = vals[v];
-            orow[2 * j + 1] = vals[v + 1];
-        }
-    });
+    dst.par_chunks_mut(cols * 2)
+        .enumerate()
+        .for_each(|(i, orow)| {
+            for j in 0..cols {
+                let v = (j * rows + i) * 2;
+                orow[2 * j] = vals[v];
+                orow[2 * j + 1] = vals[v + 1];
+            }
+        });
     Ok(Some(out.unbind()))
 }

Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:71093:
     }
     let itemsize = dt.getattr("itemsize")?.extract::<usize>()?;
     let ndim = a.getattr("ndim")?.extract::<usize>()?;
-    if ndim < 2 || !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()? {
+    if ndim < 2
+        || !a
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
+    {
         return Ok(None);
     }
     let Some(ax_raw) = axis else {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:71100:
         return Ok(None);
     };
-    let norm = if ax_raw < 0 { ax_raw + ndim as isize } else { ax_raw };
+    let norm = if ax_raw < 0 {
+        ax_raw + ndim as isize
+    } else {
+        ax_raw
+    };
     if norm < 0 || norm >= ndim as isize {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:71131:
         }
         return match itemsize {
             16 => complex_cumulative_axis0_typed::<f64>(
-                py, &numpy, a, "float64", "complex128", &shape, axis_len, inner, is_prod,
+                py,
+                &numpy,
+                a,
+                "float64",
+                "complex128",
+                &shape,
+                axis_len,
+                inner,
+                is_prod,
             ),
             8 => complex_cumulative_axis0_typed::<f32>(
-                py, &numpy, a, "float32", "complex64", &shape, axis_len, inner, is_prod,
+                py,
+                &numpy,
+                a,
+                "float32",
+                "complex64",
+                &shape,
+                axis_len,
+                inner,
+                is_prod,
             ),
             _ => Ok(None),
         };
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:71142:
     // MIDDLE axis (0 < ax < ndim-1): outer >= 2 independent blocks -> per-block slab scan.
     match itemsize {
         16 => complex_cumulative_nonlast_typed::<f64>(
-            py, &numpy, a, "float64", "complex128", &shape, outer, axis_len, inner, is_prod,
+            py,
+            &numpy,
+            a,
+            "float64",
+            "complex128",
+            &shape,
+            outer,
+            axis_len,
+            inner,
+            is_prod,
         ),
         8 => complex_cumulative_nonlast_typed::<f32>(
-            py, &numpy, a, "float32", "complex64", &shape, outer, axis_len, inner, is_prod,
+            py,
+            &numpy,
+            a,
+            "float32",
+            "complex64",
+            &shape,
+            outer,
+            axis_len,
+            inner,
+            is_prod,
         ),
         _ => Ok(None),
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:71917:
     if !a.is_exact_instance(&ndarray_type) {
         return Ok(None);
     }
-    if !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()? {
+    if !a
+        .getattr("flags")?
+        .getattr("c_contiguous")?
+        .extract::<bool>()?
+    {
         return Ok(None);
     }
     let shape: Vec<usize> = a.getattr("shape")?.extract()?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:71965:
         // beats numpy) and 4M is ~8x. Gate at 1<<20. Lane scans are independent ->
         // serial/parallel bit-identical.
         const ARGEXTREME_LASTAXIS_PARALLEL_MIN: usize = 1 << 20;
-        let parallel = outer * lane >= ARGEXTREME_LASTAXIS_PARALLEL_MIN
-            && rayon::current_num_threads() >= 2;
+        let parallel =
+            outer * lane >= ARGEXTREME_LASTAXIS_PARALLEL_MIN && rayon::current_num_threads() >= 2;
         let per: Vec<Option<usize>> = if parallel {
             data.par_chunks_exact(lane)
                 .map(|l| simd_argextreme_f64(l, take_max))
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:72140:
         }
     };
     use rayon::prelude::*;
-    let parallel =
-        outer >= 2 && outer * lane >= (1 << 16) && rayon::current_num_threads() >= 2;
+    let parallel = outer >= 2 && outer * lane >= (1 << 16) && rayon::current_num_threads() >= 2;
     if parallel {
         indices
             .par_chunks_mut(inner)
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:72242:
         }
     };
     use rayon::prelude::*;
-    let parallel =
-        outer >= 2 && outer * lane >= (1 << 16) && rayon::current_num_threads() >= 2;
+    let parallel = outer >= 2 && outer * lane >= (1 << 16) && rayon::current_num_threads() >= 2;
     if parallel {
         indices
             .par_chunks_mut(inner)
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:72305:
     let inner: usize = shape[k + 1..].iter().product();
     let lane = axis_len * inner;
     // SAFETY: ReadOnlyCell<T> is repr(transparent) over T; read-only under the GIL.
-    let data: &[T] =
-        unsafe { std::slice::from_raw_parts(cells.as_ptr().cast::<T>(), cells.len()) };
+    let data: &[T] = unsafe { std::slice::from_raw_parts(cells.as_ptr().cast::<T>(), cells.len()) };
     let mut indices = vec![0i64; outer * inner];
     // Per-outer-block running best (each block seeds its own best_val from row 0), so the outer
     // blocks are INDEPENDENT -> fan across the pool (previously SERIAL). Strict-better update =>
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:72327:
         }
     };
     use rayon::prelude::*;
-    let parallel =
-        outer >= 2 && outer * lane >= (1 << 16) && rayon::current_num_threads() >= 2;
+    let parallel = outer >= 2 && outer * lane >= (1 << 16) && rayon::current_num_threads() >= 2;
     if parallel {
         indices
             .par_chunks_mut(inner)
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:72857:
 // artificially serialized BLAS is a false win for real users.
 fn blas_is_single_threaded() -> bool {
     // OpenBLAS/MKL/BLIS honor these; any pinned to 1 => numpy's BLAS is serial and our parallel GEMM wins.
-    ["OPENBLAS_NUM_THREADS", "OMP_NUM_THREADS", "MKL_NUM_THREADS", "BLIS_NUM_THREADS"]
-        .iter()
-        .any(|var| matches!(std::env::var(var), Ok(ref v) if v.trim() == "1"))
+    [
+        "OPENBLAS_NUM_THREADS",
+        "OMP_NUM_THREADS",
+        "MKL_NUM_THREADS",
+        "BLIS_NUM_THREADS",
+    ]
+    .iter()
+    .any(|var| matches!(std::env::var(var), Ok(ref v) if v.trim() == "1"))
 }

 const PY_NATIVE_GEMM_MIN_FLOPS: usize = 320 * 320 * 320;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:72909:
     let (m, k1) = (a.shape()[0], a.shape()[1]);
     let (k2, n) = (b.shape()[0], b.shape()[1]);
     let max_dim = python_native_gemm_max_dim_for_op(op);
-    if k1 != k2
-        || [m, k1, n]
-            .iter()
-            .any(|&dim| dim == 0 || dim > max_dim)
-    {
+    if k1 != k2 || [m, k1, n].iter().any(|&dim| dim == 0 || dim > max_dim) {
         return false;
     }
     if a.values()
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:72962:
     let (k2, n) = (b_shape[0], b_shape[1]);
     let max_dim = python_native_gemm_max_dim_for_op(op);
     if k1 != k2
-        || [m, k1, n]
-            .iter()
-            .any(|&dim| dim == 0 || dim > max_dim)
+        || [m, k1, n].iter().any(|&dim| dim == 0 || dim > max_dim)
         || m.saturating_mul(k1).saturating_mul(n) < PY_NATIVE_GEMM_MIN_FLOPS
     {
         return Ok(false);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:73102:
         return Ok(None);
     }
     let batch: usize = batch_dims.iter().product();
-    let total_flops = batch
-        .saturating_mul(m)
-        .saturating_mul(k)
-        .saturating_mul(n);
+    let total_flops = batch.saturating_mul(m).saturating_mul(k).saturating_mul(n);
     // ~16.7M flops: below this, the extract-free fan-out doesn't beat numpy's per-call overhead.
     const BATCHED_GEMM_MIN_FLOPS: usize = 1 << 24;
     if batch < 2 || total_flops < BATCHED_GEMM_MIN_FLOPS || rayon::current_num_threads() < 2 {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:73274:
         return Ok(None);
     }
     let is_contig = |v: &Bound<'_, PyAny>| -> PyResult<bool> {
-        v.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()
+        v.getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()
     };
     if !is_contig(x1)? || !is_contig(x2)? {
         return Ok(None);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:73345:
         return Ok(None);
     }
     let is_contig = |v: &Bound<'_, PyAny>| -> PyResult<bool> {
-        v.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()
+        v.getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()
     };
     if !is_contig(x1)? || !is_contig(x2)? {
         return Ok(None);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:73365:
         return Ok(None);
     }
     let u16t = numpy.getattr("uint16")?;
-    let (Ok(va), Ok(vb)) = (x1.call_method1("view", (&u16t,)), x2.call_method1("view", (&u16t,)))
-    else {
+    let (Ok(va), Ok(vb)) = (
+        x1.call_method1("view", (&u16t,)),
+        x2.call_method1("view", (&u16t,)),
+    ) else {
         return Ok(None);
     };
     let (Ok(a_buf), Ok(b_buf)) = (PyBuffer::<u16>::get(&va), PyBuffer::<u16>::get(&vb)) else {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:73478:
         let out_raw: &mut [T] =
             unsafe { std::slice::from_raw_parts_mut(output.as_ptr() as *mut T, batch * m * n) };
         // Each chunk is one output row out[batch_b, i, :]; row index = b*m + i.
-        out_raw.par_chunks_mut(n).enumerate().for_each(|(row, orow)| {
-            let bb = row / m;
-            let i = row % m;
-            for slot in orow.iter_mut() {
-                *slot = zero;
-            }
-            let a_off = bb * m * k + i * k;
-            let arow = &a_raw[a_off..a_off + k];
-            let b_base = bb * k * n;
-            for (kk, &aik) in arow.iter().enumerate() {
-                let brow = &b_raw[b_base + kk * n..b_base + kk * n + n];
-                for (slot, &bkj) in orow.iter_mut().zip(brow) {
-                    *slot = add(*slot, mul(aik, bkj));
+        out_raw
+            .par_chunks_mut(n)
+            .enumerate()
+            .for_each(|(row, orow)| {
+                let bb = row / m;
+                let i = row % m;
+                for slot in orow.iter_mut() {
+                    *slot = zero;
                 }
-            }
-        });
+                let a_off = bb * m * k + i * k;
+                let arow = &a_raw[a_off..a_off + k];
+                let b_base = bb * k * n;
+                for (kk, &aik) in arow.iter().enumerate() {
+                    let brow = &b_raw[b_base + kk * n..b_base + kk * n + n];
+                    for (slot, &bkj) in orow.iter_mut().zip(brow) {
+                        *slot = add(*slot, mul(aik, bkj));
+                    }
+                }
+            });
     } else if batch * m * n > 0 {
         let Ok(out_buf) = PyBuffer::<T>::get(&flat) else {
             return Ok(None);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:73533:
         return Ok(None);
     }
     let is_contig = |v: &Bound<'_, PyAny>| -> PyResult<bool> {
-        v.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()
+        v.getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()
     };
     if !is_contig(x1)? || !is_contig(x2)? {
         return Ok(None);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:73546:
     let itemsize = dt.getattr("itemsize")?.extract::<usize>()?;
     let batch: usize = a_shape[..nd - 2].iter().product();
     let (m, k, n) = (a_shape[nd - 2], a_shape[nd - 1], b_shape[nd - 1]);
-    if batch
-        .saturating_mul(m)
-        .saturating_mul(k)
-        .saturating_mul(n)
-        < INT_MATMUL_MIN_WORK
+    if batch.saturating_mul(m).saturating_mul(k).saturating_mul(n) < INT_MATMUL_MIN_WORK
         || rayon::current_num_threads() < 2
     {
         return Ok(None);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:73615:
         return Ok(None);
     }
     let is_contig = |v: &Bound<'_, PyAny>| -> PyResult<bool> {
-        v.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()
+        v.getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()
     };
     if !is_contig(x1)? || !is_contig(x2)? {
         return Ok(None);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:73630:
     }
     let batch: usize = a_shape[..nd - 2].iter().product();
     let (m, k, n) = (a_shape[nd - 2], a_shape[nd - 1], b_shape[nd - 1]);
-    if batch
-        .saturating_mul(m)
-        .saturating_mul(k)
-        .saturating_mul(n)
-        < F16_MATMUL_MIN_WORK
+    if batch.saturating_mul(m).saturating_mul(k).saturating_mul(n) < F16_MATMUL_MIN_WORK
         || rayon::current_num_threads() < 2
     {
         return Ok(None);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:73643:
     out_shape.push(m);
     out_shape.push(n);
     let u16t = numpy.getattr("uint16")?;
-    let (Ok(va), Ok(vb)) = (x1.call_method1("view", (&u16t,)), x2.call_method1("view", (&u16t,)))
-    else {
+    let (Ok(va), Ok(vb)) = (
+        x1.call_method1("view", (&u16t,)),
+        x2.call_method1("view", (&u16t,)),
+    ) else {
         return Ok(None);
     };
     let (Ok(a_buf), Ok(b_buf)) = (PyBuffer::<u16>::get(&va), PyBuffer::<u16>::get(&vb)) else {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:73677:
             unsafe { std::slice::from_raw_parts_mut(output.as_ptr() as *mut u16, batch * m * n) };
         use rayon::prelude::*;
         // Each chunk is one output row out[batch_b, i, :]; row index = b*m + i.
-        out_raw.par_chunks_mut(n).enumerate().for_each(|(row, orow)| {
-            let bb = row / m;
-            let i = row % m;
-            let mut acc = vec![0.0f32; n];
-            let a_off = bb * m * k + i * k;
-            let arow = &a_raw[a_off..a_off + k];
-            let b_base = bb * k * n;
-            for (kk, &aik_bits) in arow.iter().enumerate() {
-                let aik = f16::from_bits(aik_bits).to_f32();
-                let brow = &b_raw[b_base + kk * n..b_base + kk * n + n];
-                for (a_acc, &bkj) in acc.iter_mut().zip(brow) {
-                    *a_acc += aik * f16::from_bits(bkj).to_f32();
+        out_raw
+            .par_chunks_mut(n)
+            .enumerate()
+            .for_each(|(row, orow)| {
+                let bb = row / m;
+                let i = row % m;
+                let mut acc = vec![0.0f32; n];
+                let a_off = bb * m * k + i * k;
+                let arow = &a_raw[a_off..a_off + k];
+                let b_base = bb * k * n;
+                for (kk, &aik_bits) in arow.iter().enumerate() {
+                    let aik = f16::from_bits(aik_bits).to_f32();
+                    let brow = &b_raw[b_base + kk * n..b_base + kk * n + n];
+                    for (a_acc, &bkj) in acc.iter_mut().zip(brow) {
+                        *a_acc += aik * f16::from_bits(bkj).to_f32();
+                    }
                 }
-            }
-            for (slot, &v) in orow.iter_mut().zip(acc.iter()) {
-                *slot = f16::from_f32(v).to_bits();
-            }
-        });
+                for (slot, &v) in orow.iter_mut().zip(acc.iter()) {
+                    *slot = f16::from_f32(v).to_bits();
+                }
+            });
     } else if batch * m * n > 0 {
         let out_view = out.call_method1("view", (&u16t,))?;
         let Ok(out_buf) = PyBuffer::<u16>::get(&out_view) else {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:73739:
         return Ok(None);
     };
     let is_contig = |v: &Bound<'_, PyAny>| -> PyResult<bool> {
-        v.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()
+        v.getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()
     };
     if !is_contig(x1)? || !is_contig(x2)? {
         return Ok(None);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:73777:
         os.push(n);
         (batch, m, k, n, os)
     };
-    if batch
-        .saturating_mul(m)
-        .saturating_mul(k)
-        .saturating_mul(n)
-        < F16_MATMUL_MIN_WORK
+    if batch.saturating_mul(m).saturating_mul(k).saturating_mul(n) < F16_MATMUL_MIN_WORK
         || rayon::current_num_threads() < 2
     {
         return Ok(None);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:73788:
     }
     let u16t = numpy.getattr("uint16")?;
-    let (Ok(va), Ok(vb)) = (x1.call_method1("view", (&u16t,)), x2.call_method1("view", (&u16t,)))
-    else {
+    let (Ok(va), Ok(vb)) = (
+        x1.call_method1("view", (&u16t,)),
+        x2.call_method1("view", (&u16t,)),
+    ) else {
         return Ok(None);
     };
     let (Ok(a_buf), Ok(b_buf)) = (PyBuffer::<u16>::get(&va), PyBuffer::<u16>::get(&vb)) else {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:73822:
         let out_raw: &mut [u16] =
             unsafe { std::slice::from_raw_parts_mut(output.as_ptr() as *mut u16, batch * m * n) };
         use rayon::prelude::*;
-        out_raw.par_chunks_mut(n).enumerate().for_each(|(row, orow)| {
-            let bb = row / m;
-            let i = row % m;
-            // Shared operand reuses slice 0; batched operand indexes by bb.
-            let a_off = if a_batched { bb * m * k + i * k } else { i * k };
-            let b_base = if a_batched { 0 } else { bb * k * n };
-            let mut acc = vec![0.0f32; n];
-            let arow = &a_raw[a_off..a_off + k];
-            for (kk, &aik_bits) in arow.iter().enumerate() {
-                let aik = f16::from_bits(aik_bits).to_f32();
-                let brow = &b_raw[b_base + kk * n..b_base + kk * n + n];
-                for (a_acc, &bkj) in acc.iter_mut().zip(brow) {
-                    *a_acc += aik * f16::from_bits(bkj).to_f32();
+        out_raw
+            .par_chunks_mut(n)
+            .enumerate()
+            .for_each(|(row, orow)| {
+                let bb = row / m;
+                let i = row % m;
+                // Shared operand reuses slice 0; batched operand indexes by bb.
+                let a_off = if a_batched { bb * m * k + i * k } else { i * k };
+                let b_base = if a_batched { 0 } else { bb * k * n };
+                let mut acc = vec![0.0f32; n];
+                let arow = &a_raw[a_off..a_off + k];
+                for (kk, &aik_bits) in arow.iter().enumerate() {
+                    let aik = f16::from_bits(aik_bits).to_f32();
+                    let brow = &b_raw[b_base + kk * n..b_base + kk * n + n];
+                    for (a_acc, &bkj) in acc.iter_mut().zip(brow) {
+                        *a_acc += aik * f16::from_bits(bkj).to_f32();
+                    }
                 }
-            }
-            for (slot, &v) in orow.iter_mut().zip(acc.iter()) {
-                *slot = f16::from_f32(v).to_bits();
-            }
-        });
+                for (slot, &v) in orow.iter_mut().zip(acc.iter()) {
+                    *slot = f16::from_f32(v).to_bits();
+                }
+            });
     }
     Ok(Some(out.unbind()))
 }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:73885:
         return Ok(None);
     }
     let is_contig = |v: &Bound<'_, PyAny>| -> PyResult<bool> {
-        v.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()
+        v.getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()
     };
     if !is_contig(a)? || !is_contig(b)? {
         return Ok(None);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:73947:
         return Ok(None);
     }
     let is_contig = |v: &Bound<'_, PyAny>| -> PyResult<bool> {
-        v.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()
+        v.getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()
     };
     if !is_contig(a)? || !is_contig(b)? {
         return Ok(None);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:74007:
         return Ok(None);
     }
     let is_contig = |v: &Bound<'_, PyAny>| -> PyResult<bool> {
-        v.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()
+        v.getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()
     };
     if !is_contig(a)? || !is_contig(b)? {
         return Ok(None);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:74064:
         return Ok(None);
     }
     let is_contig = |v: &Bound<'_, PyAny>| -> PyResult<bool> {
-        v.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()
+        v.getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()
     };
     if !is_contig(a)? || !is_contig(b)? {
         return Ok(None);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:74116:
         && python_explicit_out_is_absent_or_none(py, out.as_ref())
     {
         let numpy = py.import("numpy")?;
-        if let Some(result) =
-            try_zerocopy_f64_batched_matmul(py, &numpy, x1.bind(py), x2.bind(py))?
+        if let Some(result) = try_zerocopy_f64_batched_matmul(py, &numpy, x1.bind(py), x2.bind(py))?
         {
             return Ok(result);
         }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:74236:
     // the result, so defer those.
     if let Some(kw) = kwargs {
         for key in kw.keys() {
-            if key.extract::<String>().map(|k| k != "optimize").unwrap_or(true) {
+            if key
+                .extract::<String>()
+                .map(|k| k != "optimize")
+                .unwrap_or(true)
+            {
                 return Ok(None);
             }
         }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:74249:
     };
     let inb: Vec<char> = inp.trim().chars().collect();
     let oub: Vec<char> = outp.trim().chars().collect();
-    let distinct = |s: &[char]| {
-        s.iter().collect::<std::collections::HashSet<_>>().len() == s.len()
-    };
+    let distinct = |s: &[char]| s.iter().collect::<std::collections::HashSet<_>>().len() == s.len();
     // Must be a permutation of ALL labels: same length, all alphabetic, both sides
     // distinct, and same label set.
     if inb.is_empty()
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:74279:
         .iter()
         .map(|c| inb.iter().position(|x| x == c).unwrap())
         .collect();
-    Ok(Some(
-        operand.call_method1("transpose", (perm,))?.unbind(),
-    ))
+    Ok(Some(operand.call_method1("transpose", (perm,))?.unbind()))
 }

 // True for a single-operand pure REDUCTION spec ("ijk->k", "ij->", "ijk->ik"):
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:75689:
     let outc: Vec<char> = out.chars().collect();
     if !labels.iter().all(|c| c.is_ascii_alphabetic())
         || outc.len() >= labels.len()
-        || labels.iter().collect::<std::collections::HashSet<_>>().len() != labels.len()
+        || labels
+            .iter()
+            .collect::<std::collections::HashSet<_>>()
+            .len()
+            != labels.len()
     {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:75732:
         return Ok(None);
     }
     // Contiguous-block 8-accumulator dot — bit-stable per block, allclose vs numpy.
-    let block_dot = |ra: &[pyo3::buffer::ReadOnlyCell<f64>],
-                     rb: &[pyo3::buffer::ReadOnlyCell<f64>]|
-     -> f64 {
-        let mut acc = [0.0f64; 8];
-        let mut ca = ra.chunks_exact(8);
-        let mut cb = rb.chunks_exact(8);
-        for (ga, gb) in ca.by_ref().zip(cb.by_ref()) {
-            acc[0] += ga[0].get() * gb[0].get();
-            acc[1] += ga[1].get() * gb[1].get();
-            acc[2] += ga[2].get() * gb[2].get();
-            acc[3] += ga[3].get() * gb[3].get();
-            acc[4] += ga[4].get() * gb[4].get();
-            acc[5] += ga[5].get() * gb[5].get();
-            acc[6] += ga[6].get() * gb[6].get();
-            acc[7] += ga[7].get() * gb[7].get();
-        }
-        let mut s = acc.iter().sum::<f64>();
-        for (x, y) in ca.remainder().iter().zip(cb.remainder()) {
-            s += x.get() * y.get();
-        }
-        s
-    };
+    let block_dot =
+        |ra: &[pyo3::buffer::ReadOnlyCell<f64>], rb: &[pyo3::buffer::ReadOnlyCell<f64>]| -> f64 {
+            let mut acc = [0.0f64; 8];
+            let mut ca = ra.chunks_exact(8);
+            let mut cb = rb.chunks_exact(8);
+            for (ga, gb) in ca.by_ref().zip(cb.by_ref()) {
+                acc[0] += ga[0].get() * gb[0].get();
+                acc[1] += ga[1].get() * gb[1].get();
+                acc[2] += ga[2].get() * gb[2].get();
+                acc[3] += ga[3].get() * gb[3].get();
+                acc[4] += ga[4].get() * gb[4].get();
+                acc[5] += ga[5].get() * gb[5].get();
+                acc[6] += ga[6].get() * gb[6].get();
+                acc[7] += ga[7].get() * gb[7].get();
+            }
+            let mut s = acc.iter().sum::<f64>();
+            for (x, y) in ca.remainder().iter().zip(cb.remainder()) {
+                s += x.get() * y.get();
+            }
+            s
+        };
     let (out_values, out_shape): (Vec<f64>, Vec<usize>) = if is_prefix {
         // Keep leading outc.len() axes; sum the trailing (contiguous) block.
         let n_groups: usize = sa[..outc.len()].iter().product();
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:75760:
         let block = if n_groups == 0 { 0 } else { total / n_groups };
         let vals = (0..n_groups)
-            .map(|g| block_dot(&va[g * block..(g + 1) * block], &vb[g * block..(g + 1) * block]))
+            .map(|g| {
+                block_dot(
+                    &va[g * block..(g + 1) * block],
+                    &vb[g * block..(g + 1) * block],
+                )
+            })
             .collect();
         (vals, sa[..outc.len()].to_vec())
     } else {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:75894:
     ) else {
         return Ok(None);
     };
-    let (nrows, ncols) = if sa.len() == 2 { (sa[0], sa[1]) } else { return Ok(None) };
+    let (nrows, ncols) = if sa.len() == 2 {
+        (sa[0], sa[1])
+    } else {
+        return Ok(None);
+    };
     // Vector length must match the contracted axis: column→ncols, row→nrows.
     let want_vlen = if contract_col { ncols } else { nrows };
     if sb.len() != 1 || sb[0] != want_vlen {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:75934:
             s
         };
         if keep_axis {
-            ((0..nrows).map(|i| row_dot(&va[i * ncols..(i + 1) * ncols])).collect(), vec![nrows])
+            (
+                (0..nrows)
+                    .map(|i| row_dot(&va[i * ncols..(i + 1) * ncols]))
+                    .collect(),
+                vec![nrows],
+            )
         } else {
             let mut total = 0.0f64;
             for i in 0..nrows {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:76009:
         return Ok(Some(result));
     }
     if args.len() == 2
-        && let Some(result) = try_zerocopy_f64_einsum_single_reduction_2d(
-            py,
-            &subscripts,
-            &args.get_item(1)?,
-        )?
+        && let Some(result) =
+            try_zerocopy_f64_einsum_single_reduction_2d(py, &subscripts, &args.get_item(1)?)?
     {
         return Ok(Some(result));
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:76055:
     }
     // Two-operand matrix·vector contraction ('ij,j->i' / 'ij,j->'): zero-copy fused scan.
     if args.len() == 3
-        && let Some(result) = try_zerocopy_f64_einsum_matvec(
-            py,
-            &subscripts,
-            &args.get_item(1)?,
-            &args.get_item(2)?,
-        )?
+        && let Some(result) =
+            try_zerocopy_f64_einsum_matvec(py, &subscripts, &args.get_item(1)?, &args.get_item(2)?)?
     {
         return Ok(Some(result));
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:76095:
         let op = &operands[0];
         let inb: Vec<char> = inp.chars().collect();
         let oub: Vec<char> = outp.chars().collect();
-        let distinct = |s: &[char]| {
-            s.iter()
-                .collect::<std::collections::HashSet<_>>()
-                .len()
-                == s.len()
-        };
+        let distinct =
+            |s: &[char]| s.iter().collect::<std::collections::HashSet<_>>().len() == s.len();
         if op.dtype() == DType::F64
             && !inp.contains('.')
             && !outp.contains('.')
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:76159:
     if !item.is_exact_instance(&numpy.getattr("ndarray")?) || !numpy_dtype_is_f64(py, item) {
         return Ok(None);
     }
-    if !item.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()? {
+    if !item
+        .getattr("flags")?
+        .getattr("c_contiguous")?
+        .extract::<bool>()?
+    {
         return Ok(None);
     }
     let Ok(buffer) = PyBuffer::<f64>::get(item) else {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:76252:
     if !item.is_exact_instance(&numpy.getattr("ndarray")?) || !numpy_dtype_is_f64(py, item) {
         return Ok(None);
     }
-    if !item.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()? {
+    if !item
+        .getattr("flags")?
+        .getattr("c_contiguous")?
+        .extract::<bool>()?
+    {
         return Ok(None);
     }
     let Ok(buffer) = PyBuffer::<f64>::get(item) else {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:76304:
 fn sortable_u16(u: u16, kind: u8) -> u16 {
     match kind {
         b'i' => u ^ 0x8000,
-        b'f' => if u & 0x8000 != 0 { !u } else { u | 0x8000 },
+        b'f' => {
+            if u & 0x8000 != 0 {
+                !u
+            } else {
+                u | 0x8000
+            }
+        }
         _ => u,
     }
 }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:76312:
 fn sortable_u32(u: u32, kind: u8) -> u32 {
     match kind {
         b'i' => u ^ 0x8000_0000,
-        b'f' => if u & 0x8000_0000 != 0 { !u } else { u | 0x8000_0000 },
+        b'f' => {
+            if u & 0x8000_0000 != 0 {
+                !u
+            } else {
+                u | 0x8000_0000
+            }
+        }
         _ => u,
     }
 }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:76320:
 fn sortable_u64(u: u64, kind: u8) -> u64 {
     match kind {
         b'i' => u ^ 0x8000_0000_0000_0000,
-        b'f' => if u & 0x8000_0000_0000_0000 != 0 { !u } else { u | 0x8000_0000_0000_0000 },
+        b'f' => {
+            if u & 0x8000_0000_0000_0000 != 0 {
+                !u
+            } else {
+                u | 0x8000_0000_0000_0000
+            }
+        }
         _ => u,
     }
 }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:76328:
 fn transform_field(src: &[u8], dst: &mut [u8], kind: u8, w: usize) {
     match w {
         1 => dst[0] = sortable_u8(src[0], kind),
-        2 => dst[..2].copy_from_slice(&sortable_u16(u16::from_le_bytes([src[0], src[1]]), kind).to_be_bytes()),
+        2 => dst[..2].copy_from_slice(
+            &sortable_u16(u16::from_le_bytes([src[0], src[1]]), kind).to_be_bytes(),
+        ),
         4 => dst[..4].copy_from_slice(
             &sortable_u32(u32::from_le_bytes([src[0], src[1], src[2], src[3]]), kind).to_be_bytes(),
         ),
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:76370:
         return Ok(None);
     }
     if item.getattr("ndim")?.extract::<usize>()? != 1
-        || !item.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()?
+        || !item
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
     {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:76414:
     // Any NaN or -0.0 in a float field -> defer (transform diverges from numpy's ±0.0/NaN semantics).
     for name in &float_names {
         let f = item.get_item(name.as_str())?;
-        if numpy.call_method1("isnan", (&f,))?.call_method0("any")?.extract::<bool>()? {
+        if numpy
+            .call_method1("isnan", (&f,))?
+            .call_method0("any")?
+            .extract::<bool>()?
+        {
             return Ok(None);
         }
         let sb = numpy.call_method1("signbit", (&f,))?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:76421:
         let eq0 = numpy.call_method1("equal", (&f, 0.0_f64))?;
-        if numpy.call_method1("logical_and", (sb, eq0))?.call_method0("any")?.extract::<bool>()? {
+        if numpy
+            .call_method1("logical_and", (sb, eq0))?
+            .call_method0("any")?
+            .extract::<bool>()?
+        {
             return Ok(None);
         }
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:76457:
     let mut srt: Vec<u8> = vec![0; total];
     srt.par_chunks_exact_mut(itemsize)
         .zip(perm.par_iter())
-        .for_each(|(dst, &r)| dst.copy_from_slice(&data[r as usize * itemsize..r as usize * itemsize + itemsize]));
+        .for_each(|(dst, &r)| {
+            dst.copy_from_slice(&data[r as usize * itemsize..r as usize * itemsize + itemsize])
+        });
     let mut keep: Vec<usize> = Vec::with_capacity(64);
     keep.push(0);
     for k in 1..n {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:76481:
             return Ok(None);
         }
         // SAFETY: fresh numpy.empty structured storage viewed as u8, no alias with srt.
-        let o: &mut [u8] = unsafe { std::slice::from_raw_parts_mut(os.as_ptr() as *mut u8, nu * itemsize) };
+        let o: &mut [u8] =
+            unsafe { std::slice::from_raw_parts_mut(os.as_ptr() as *mut u8, nu * itemsize) };
         o.par_chunks_exact_mut(itemsize)
             .zip(keep.par_iter())
             .for_each(|(orec, &k)| orec.copy_from_slice(&srt[k * itemsize..(k + 1) * itemsize]));
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:76508:
         return Ok(None);
     }
     if a.getattr("ndim")?.extract::<usize>()? != 1
-        || !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()?
+        || !a
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
     {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:76515:
     // 1-D: axis missing / None / -1 / 0 all collapse to the single axis.
-    if !matches!(axis_spec, None | Some(None) | Some(Some(-1)) | Some(Some(0))) {
+    if !matches!(
+        axis_spec,
+        None | Some(None) | Some(Some(-1)) | Some(Some(0))
+    ) {
         return Ok(None);
     }
     let dtype = a.getattr("dtype")?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:76568:
     }
     for name in &float_names {
         let f = a.get_item(name.as_str())?;
-        if numpy.call_method1("isnan", (&f,))?.call_method0("any")?.extract::<bool>()? {
+        if numpy
+            .call_method1("isnan", (&f,))?
+            .call_method0("any")?
+            .extract::<bool>()?
+        {
             return Ok(None);
         }
         let sb = numpy.call_method1("signbit", (&f,))?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:76575:
         let eq0 = numpy.call_method1("equal", (&f, 0.0_f64))?;
-        if numpy.call_method1("logical_and", (sb, eq0))?.call_method0("any")?.extract::<bool>()? {
+        if numpy
+            .call_method1("logical_and", (sb, eq0))?
+            .call_method0("any")?
+            .extract::<bool>()?
+        {
             return Ok(None);
         }
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:76643:
         return Ok(None);
     }
     if a.getattr("ndim")?.extract::<usize>()? != 1
-        || !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()?
+        || !a
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
     {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:76699:
     }
     for name in &float_names {
         let f = a.get_item(name.as_str())?;
-        if numpy.call_method1("isnan", (&f,))?.call_method0("any")?.extract::<bool>()? {
+        if numpy
+            .call_method1("isnan", (&f,))?
+            .call_method0("any")?
+            .extract::<bool>()?
+        {
             return Ok(None);
         }
         let sb = numpy.call_method1("signbit", (&f,))?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:76706:
         let eq0 = numpy.call_method1("equal", (&f, 0.0_f64))?;
-        if numpy.call_method1("logical_and", (sb, eq0))?.call_method0("any")?.extract::<bool>()? {
+        if numpy
+            .call_method1("logical_and", (sb, eq0))?
+            .call_method0("any")?
+            .extract::<bool>()?
+        {
             return Ok(None);
         }
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:76746:
         return Ok(None);
     }
     // SAFETY: fresh numpy.empty intp output, no alias.
-    let o: &mut [i64] = unsafe { std::slice::from_raw_parts_mut(out_cells.as_ptr() as *mut i64, n) };
-    o.par_iter_mut().zip(perm.par_iter()).for_each(|(dst, &p)| *dst = p as i64);
+    let o: &mut [i64] =
+        unsafe { std::slice::from_raw_parts_mut(out_cells.as_ptr() as *mut i64, n) };
+    o.par_iter_mut()
+        .zip(perm.par_iter())
+        .for_each(|(dst, &p)| *dst = p as i64);
     Ok(Some(out.unbind()))
 }

Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:76768:
         return Ok(None);
     }
     if a.getattr("ndim")?.extract::<usize>()? != 1
-        || !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()?
+        || !a
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
     {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:76775:
-    if !matches!(axis_spec, None | Some(None) | Some(Some(-1)) | Some(Some(0))) {
+    if !matches!(
+        axis_spec,
+        None | Some(None) | Some(Some(-1)) | Some(Some(0))
+    ) {
         return Ok(None);
     }
     let dtype = a.getattr("dtype")?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:76827:
     }
     for name in &float_names {
         let f = a.get_item(name.as_str())?;
-        if numpy.call_method1("isnan", (&f,))?.call_method0("any")?.extract::<bool>()? {
+        if numpy
+            .call_method1("isnan", (&f,))?
+            .call_method0("any")?
+            .extract::<bool>()?
+        {
             return Ok(None);
         }
         let sb = numpy.call_method1("signbit", (&f,))?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:76834:
         let eq0 = numpy.call_method1("equal", (&f, 0.0_f64))?;
-        if numpy.call_method1("logical_and", (sb, eq0))?.call_method0("any")?.extract::<bool>()? {
+        if numpy
+            .call_method1("logical_and", (sb, eq0))?
+            .call_method0("any")?
+            .extract::<bool>()?
+        {
             return Ok(None);
         }
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:76877:
         return Ok(None);
     }
     // SAFETY: fresh numpy.empty intp output, no alias with keys/perm.
-    let o: &mut [i64] = unsafe { std::slice::from_raw_parts_mut(out_cells.as_ptr() as *mut i64, n) };
+    let o: &mut [i64] =
+        unsafe { std::slice::from_raw_parts_mut(out_cells.as_ptr() as *mut i64, n) };
     o.par_iter_mut()
         .zip(perm.par_iter())
         .for_each(|(dst, &p)| *dst = p as i64);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:76916:
         return Ok(None);
     }
     if item.getattr("ndim")?.extract::<usize>()? != 1
-        || !item.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()?
+        || !item
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
     {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:76959:
     }
     for name in &float_names {
         let f = item.get_item(name.as_str())?;
-        if numpy.call_method1("isnan", (&f,))?.call_method0("any")?.extract::<bool>()? {
+        if numpy
+            .call_method1("isnan", (&f,))?
+            .call_method0("any")?
+            .extract::<bool>()?
+        {
             return Ok(None);
         }
         let sb = numpy.call_method1("signbit", (&f,))?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:76966:
         let eq0 = numpy.call_method1("equal", (&f, 0.0_f64))?;
-        if numpy.call_method1("logical_and", (sb, eq0))?.call_method0("any")?.extract::<bool>()? {
+        if numpy
+            .call_method1("logical_and", (sb, eq0))?
+            .call_method0("any")?
+            .extract::<bool>()?
+        {
             return Ok(None);
         }
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:77001:
     let mut srt: Vec<u8> = vec![0; total];
     srt.par_chunks_exact_mut(itemsize)
         .zip(perm.par_iter())
-        .for_each(|(dst, &r)| dst.copy_from_slice(&data[r as usize * itemsize..r as usize * itemsize + itemsize]));
+        .for_each(|(dst, &r)| {
+            dst.copy_from_slice(&data[r as usize * itemsize..r as usize * itemsize + itemsize])
+        });
     let mut group_starts: Vec<usize> = Vec::with_capacity(64);
     group_starts.push(0);
     for k in 1..n {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:77022:
             return Ok(None);
         };
         // SAFETY: fresh numpy.empty structured storage viewed as u8, no alias with srt.
-        let o: &mut [u8] = unsafe { std::slice::from_raw_parts_mut(os.as_ptr() as *mut u8, u * itemsize) };
+        let o: &mut [u8] =
+            unsafe { std::slice::from_raw_parts_mut(os.as_ptr() as *mut u8, u * itemsize) };
         o.par_chunks_exact_mut(itemsize)
             .zip(group_starts.par_iter())
             .for_each(|(orec, &gs)| orec.copy_from_slice(&srt[gs * itemsize..(gs + 1) * itemsize]));
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:77038:
         let Some(is_) = ib.as_mut_slice(py) else {
             return Ok(None);
         };
-        let out: &mut [i64] = unsafe { std::slice::from_raw_parts_mut(is_.as_ptr() as *mut i64, u) };
+        let out: &mut [i64] =
+            unsafe { std::slice::from_raw_parts_mut(is_.as_ptr() as *mut i64, u) };
         out.par_iter_mut()
             .zip(group_starts.par_iter())
             .for_each(|(o, &gs)| *o = perm[gs] as i64);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:77112:
         return Ok(None); // padding or non-8-byte fields
     }
     if item.getattr("ndim")?.extract::<usize>()? != 1
-        || !item.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()?
+        || !item
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
     {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:77178:
         return Ok(None);
     }
     if item.getattr("ndim")?.extract::<usize>()? != 1
-        || !item.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()?
+        || !item
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
     {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:77328:
             }
             {
                 let numpy = py.import("numpy")?;
-                if let Some(out) = try_native_string_unique_full(py, &numpy, &item, ri, rinv, rc)?
-                {
+                if let Some(out) = try_native_string_unique_full(py, &numpy, &item, ri, rinv, rc)? {
                     return Ok(out);
                 }
             }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:77391:
                             return Ok(out);
                         }
                         // complex64 rows: view as f32 (n, 2*ncols), route to the f32 row-unique, view back.
-                        if let Some(out) = try_native_unique_rows_complex64(py, &item, false, false, false)? {
+                        if let Some(out) =
+                            try_native_unique_rows_complex64(py, &item, false, false, false)?
+                        {
                             return Ok(out);
                         }
                         // datetime64/timedelta64 rows: view int64, route to the int row-unique, view back (NaT defer).
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:77398:
-                        if let Some(out) = try_native_unique_rows_datetime(py, &item, false, false, false)? {
+                        if let Some(out) =
+                            try_native_unique_rows_datetime(py, &item, false, false, false)?
+                        {
                             return Ok(out);
                         }
                         // float16 rows: widen exact to f32, route to the f32 row-unique, narrow back (NaN/-0.0 defer).
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:77402:
-                        if let Some(out) = try_native_unique_rows_f16(py, &item, false, false, false)? {
+                        if let Some(out) =
+                            try_native_unique_rows_f16(py, &item, false, false, false)?
+                        {
                             return Ok(out);
                         }
                     } else if let Some(out) =
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:77575:
     // Unique rows (decode) in original dtype.
     let out_kw = PyDict::new(py);
     out_kw.set_item("dtype", "int64")?;
-    let flat = numpy.call_method("empty", ((u, k), ), Some(&out_kw))?;
+    let flat = numpy.call_method("empty", ((u, k),), Some(&out_kw))?;
     {
         let Ok(o_buf) = PyBuffer::<i64>::get(&flat) else {
             return Ok(None);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:77584:
             return Ok(None);
         };
         // SAFETY: fresh numpy.empty output; each element written once.
-        let o: &mut [i64] = unsafe { std::slice::from_raw_parts_mut(o_s.as_ptr() as *mut i64, u * k) };
+        let o: &mut [i64] =
+            unsafe { std::slice::from_raw_parts_mut(o_s.as_ptr() as *mut i64, u * k) };
         o.par_chunks_mut(k)
             .zip(group_starts.par_iter())
             .for_each(|(orow, &gs)| {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:77609:
         let Some(is_) = ib.as_mut_slice(py) else {
             return Ok(None);
         };
-        let out: &mut [i64] = unsafe { std::slice::from_raw_parts_mut(is_.as_ptr() as *mut i64, u) };
+        let out: &mut [i64] =
+            unsafe { std::slice::from_raw_parts_mut(is_.as_ptr() as *mut i64, u) };
         out.par_iter_mut()
             .zip(group_starts.par_iter())
             .for_each(|(o, &gs)| *o = pairs[gs].1 as i64);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:77629:
         let inv: &mut [i64] = unsafe { std::slice::from_raw_parts_mut(vs.as_ptr() as *mut i64, n) };
         // group id per sorted position, then scatter to original index.
         for (g, &gs) in group_starts.iter().enumerate() {
-            let end = if g + 1 < u { group_starts[g + 1] } else { pairs.len() };
+            let end = if g + 1 < u {
+                group_starts[g + 1]
+            } else {
+                pairs.len()
+            };
             for &(_, orig) in &pairs[gs..end] {
                 inv[orig as usize] = g as i64;
             }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:77649:
         };
         let cnt: &mut [i64] = unsafe { std::slice::from_raw_parts_mut(cs.as_ptr() as *mut i64, u) };
         for g in 0..u {
-            let end = if g + 1 < u { group_starts[g + 1] } else { pairs.len() };
+            let end = if g + 1 < u {
+                group_starts[g + 1]
+            } else {
+                pairs.len()
+            };
             cnt[g] = (end - group_starts[g]) as i64;
         }
         outs.push(cnt_arr);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:77678:
         return Ok(None);
     }
     if item.getattr("ndim")?.extract::<usize>()? != 2
-        || !item.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()?
+        || !item
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
     {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:77698:
     }
     use rayon::prelude::*;
     // SAFETY: ReadOnlyCell<f64> repr(transparent); read-only under the GIL.
-    let data: &[f64] = unsafe { std::slice::from_raw_parts(cells.as_ptr().cast::<f64>(), rows * ncols) };
+    let data: &[f64] =
+        unsafe { std::slice::from_raw_parts(cells.as_ptr().cast::<f64>(), rows * ncols) };
     // Defer on NaN or -0.0 (numpy collapses +0.0/-0.0 rows; NaN ordering subtle).
     if data
         .par_iter()
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:77780:
         return Ok(None);
     }
     if item.getattr("ndim")?.extract::<usize>()? != 2
-        || !item.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()?
+        || !item
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
     {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:77800:
     }
     use rayon::prelude::*;
     // SAFETY: ReadOnlyCell<f32> repr(transparent); read-only under the GIL.
-    let data: &[f32] = unsafe { std::slice::from_raw_parts(cells.as_ptr().cast::<f32>(), rows * ncols) };
+    let data: &[f32] =
+        unsafe { std::slice::from_raw_parts(cells.as_ptr().cast::<f32>(), rows * ncols) };
     if data
         .par_iter()
         .any(|v| v.is_nan() || v.to_bits() == 0x8000_0000)
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:77882:
         return Ok(None);
     }
     if item.getattr("ndim")?.extract::<usize>()? != 2
-        || !item.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()?
+        || !item
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
     {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:77919:
         return Ok(None);
     }
     if item.getattr("ndim")?.extract::<usize>()? != 2
-        || !item.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()?
+        || !item
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
     {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:77926:
     let f_view = item.call_method1("view", ("float64",))?;
-    match try_native_unique_rows_lexsort_f64_full(py, &f_view, ret_index, ret_inverse, ret_counts)? {
+    match try_native_unique_rows_lexsort_f64_full(py, &f_view, ret_index, ret_inverse, ret_counts)?
+    {
         Some(f_out) => {
             let tup = f_out.bind(py).downcast::<PyTuple>()?;
             // tuple = [unique_f64, index?, inverse?, counts?]; re-view element 0 back to complex128.
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:77960:
     if dtype.getattr("kind")?.extract::<String>()? != "c"
         || dtype.getattr("itemsize")?.extract::<usize>()? != 8
         || item.getattr("ndim")?.extract::<usize>()? != 2
-        || !item.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()?
+        || !item
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
     {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:77967:
     let f_view = item.call_method1("view", ("float32",))?;
     if !ret_index && !ret_inverse && !ret_counts {
         match try_native_unique_rows_lexsort_f32(py, &f_view)? {
-            Some(f_out) => Ok(Some(f_out.bind(py).call_method1("view", ("complex64",))?.unbind())),
+            Some(f_out) => Ok(Some(
+                f_out
+                    .bind(py)
+                    .call_method1("view", ("complex64",))?
+                    .unbind(),
+            )),
             None => Ok(None),
         }
     } else {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:77974:
-        match try_native_unique_rows_lexsort_f32_full(py, &f_view, ret_index, ret_inverse, ret_counts)? {
+        match try_native_unique_rows_lexsort_f32_full(
+            py,
+            &f_view,
+            ret_index,
+            ret_inverse,
+            ret_counts,
+        )? {
             Some(t) => {
                 let tup = t.bind(py).downcast::<PyTuple>()?;
                 let count = 1 + ret_index as usize + ret_inverse as usize + ret_counts as usize;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:78010:
         return Ok(None);
     }
     if item.getattr("ndim")?.extract::<usize>()? != 2
-        || !item.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()?
+        || !item
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
     {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:78025:
             return Ok(None);
         };
         // SAFETY: ReadOnlyCell<i64> repr(transparent); read-only under the GIL.
-        let data: &[i64] = unsafe { std::slice::from_raw_parts(cells.as_ptr().cast::<i64>(), cells.len()) };
+        let data: &[i64] =
+            unsafe { std::slice::from_raw_parts(cells.as_ptr().cast::<i64>(), cells.len()) };
         if data.par_iter().any(|&v| v == i64::MIN) {
             return Ok(None);
         }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:78036:
             None => Ok(None),
         }
     } else {
-        match try_native_unique_rows_lexsort_int_full(py, &iview, ret_index, ret_inverse, ret_counts)? {
+        match try_native_unique_rows_lexsort_int_full(
+            py,
+            &iview,
+            ret_index,
+            ret_inverse,
+            ret_counts,
+        )? {
             Some(t) => {
                 let tup = t.bind(py).downcast::<PyTuple>()?;
                 // tuple[0] = unique int64 rows -> view back to M8/m8; extras (index/inverse/counts) pass through.
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:78073:
     if dtype.getattr("kind")?.extract::<String>()? != "f"
         || dtype.getattr("itemsize")?.extract::<usize>()? != 2
         || item.getattr("ndim")?.extract::<usize>()? != 2
-        || !item.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()?
+        || !item
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
     {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:78080:
     let f32 = item.call_method1("astype", ("float32",))?;
     if !ret_index && !ret_inverse && !ret_counts {
         match try_native_unique_rows_lexsort_f32(py, &f32)? {
-            Some(u) => Ok(Some(u.bind(py).call_method1("astype", ("float16",))?.unbind())),
+            Some(u) => Ok(Some(
+                u.bind(py).call_method1("astype", ("float16",))?.unbind(),
+            )),
             None => Ok(None),
         }
     } else {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:78087:
-        match try_native_unique_rows_lexsort_f32_full(py, &f32, ret_index, ret_inverse, ret_counts)? {
+        match try_native_unique_rows_lexsort_f32_full(py, &f32, ret_index, ret_inverse, ret_counts)?
+        {
             Some(t) => {
                 let tup = t.bind(py).downcast::<PyTuple>()?;
                 let count = 1 + ret_index as usize + ret_inverse as usize + ret_counts as usize;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:78164:
         return Ok(None);
     }
     if item.getattr("ndim")?.extract::<usize>()? != 2
-        || !item.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()?
+        || !item
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
     {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:78214:
         return Ok(None);
     }
     if item.getattr("ndim")?.extract::<usize>()? != 2
-        || !item.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()?
+        || !item
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
     {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:78227:
     let kw = PyDict::new(py);
     kw.set_item("dtype", "int64")?;
     let widened = numpy.call_method("ascontiguousarray", (item,), Some(&kw))?;
-    match try_native_unique_rows_lexsort_int_full(py, &widened, ret_index, ret_inverse, ret_counts)? {
+    match try_native_unique_rows_lexsort_int_full(py, &widened, ret_index, ret_inverse, ret_counts)?
+    {
         Some(t) => {
             let tup = t.bind(py).cast::<PyTuple>()?;
             let count = 1 + ret_index as usize + ret_inverse as usize + ret_counts as usize;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:78264:
         return Ok(None);
     }
     if item.getattr("ndim")?.extract::<usize>()? != 2
-        || !item.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()?
+        || !item
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
     {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:78286:
     }
     use rayon::prelude::*;
     // SAFETY: ReadOnlyCell<i64> repr(transparent); read-only under the GIL.
-    let data: &[i64] = unsafe { std::slice::from_raw_parts(cells.as_ptr().cast::<i64>(), rows * ncols) };
+    let data: &[i64] =
+        unsafe { std::slice::from_raw_parts(cells.as_ptr().cast::<i64>(), rows * ncols) };
     let row = |r: u32| -> &[i64] {
         let b = r as usize * ncols;
         &data[b..b + ncols]
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:78373:
         return Ok(None);
     }
     if item.getattr("ndim")?.extract::<usize>()? != 2
-        || !item.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()?
+        || !item
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
     {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:78393:
     }
     use rayon::prelude::*;
     // SAFETY: ReadOnlyCell<f64> repr(transparent); read-only under the GIL.
-    let data: &[f64] = unsafe { std::slice::from_raw_parts(cells.as_ptr().cast::<f64>(), rows * ncols) };
+    let data: &[f64] =
+        unsafe { std::slice::from_raw_parts(cells.as_ptr().cast::<f64>(), rows * ncols) };
     if data
         .par_iter()
         .any(|v| v.is_nan() || v.to_bits() == 0x8000_0000_0000_0000)
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:78440:
         let Some(os) = ob.as_mut_slice(py) else {
             return Ok(None);
         };
-        let o: &mut [f64] = unsafe { std::slice::from_raw_parts_mut(os.as_ptr() as *mut f64, u * ncols) };
+        let o: &mut [f64] =
+            unsafe { std::slice::from_raw_parts_mut(os.as_ptr() as *mut f64, u * ncols) };
         o.par_chunks_exact_mut(ncols)
             .zip(group_starts.par_iter())
             .for_each(|(orow, &gs)| {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:78458:
         let Some(is_) = ib.as_mut_slice(py) else {
             return Ok(None);
         };
-        let out: &mut [i64] = unsafe { std::slice::from_raw_parts_mut(is_.as_ptr() as *mut i64, u) };
+        let out: &mut [i64] =
+            unsafe { std::slice::from_raw_parts_mut(is_.as_ptr() as *mut i64, u) };
         out.par_iter_mut()
             .zip(group_starts.par_iter())
             .for_each(|(o, &gs)| *o = perm[gs] as i64);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:78474:
         let Some(vs) = vb.as_mut_slice(py) else {
             return Ok(None);
         };
-        let inv: &mut [i64] = unsafe { std::slice::from_raw_parts_mut(vs.as_ptr() as *mut i64, rows) };
+        let inv: &mut [i64] =
+            unsafe { std::slice::from_raw_parts_mut(vs.as_ptr() as *mut i64, rows) };
         for (g, &gs) in group_starts.iter().enumerate() {
             let end = if g + 1 < u { group_starts[g + 1] } else { rows };
             for &orig in &perm[gs..end] {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:78525:
         return Ok(None);
     }
     if item.getattr("ndim")?.extract::<usize>()? != 2
-        || !item.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()?
+        || !item
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
     {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:78545:
     }
     use rayon::prelude::*;
     // SAFETY: ReadOnlyCell<f32> repr(transparent); read-only under the GIL.
-    let data: &[f32] = unsafe { std::slice::from_raw_parts(cells.as_ptr().cast::<f32>(), rows * ncols) };
-    if data.par_iter().any(|v| v.is_nan() || v.to_bits() == 0x8000_0000) {
+    let data: &[f32] =
+        unsafe { std::slice::from_raw_parts(cells.as_ptr().cast::<f32>(), rows * ncols) };
+    if data
+        .par_iter()
+        .any(|v| v.is_nan() || v.to_bits() == 0x8000_0000)
+    {
         return Ok(None);
     }
     let row = |r: u32| -> &[f32] {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:78589:
         let Some(os) = ob.as_mut_slice(py) else {
             return Ok(None);
         };
-        let o: &mut [f32] = unsafe { std::slice::from_raw_parts_mut(os.as_ptr() as *mut f32, u * ncols) };
+        let o: &mut [f32] =
+            unsafe { std::slice::from_raw_parts_mut(os.as_ptr() as *mut f32, u * ncols) };
         o.par_chunks_exact_mut(ncols)
             .zip(group_starts.par_iter())
             .for_each(|(orow, &gs)| {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:78607:
         let Some(is_) = ib.as_mut_slice(py) else {
             return Ok(None);
         };
-        let out: &mut [i64] = unsafe { std::slice::from_raw_parts_mut(is_.as_ptr() as *mut i64, u) };
+        let out: &mut [i64] =
+            unsafe { std::slice::from_raw_parts_mut(is_.as_ptr() as *mut i64, u) };
         out.par_iter_mut()
             .zip(group_starts.par_iter())
             .for_each(|(o, &gs)| *o = perm[gs] as i64);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:78623:
         let Some(vs) = vb.as_mut_slice(py) else {
             return Ok(None);
         };
-        let inv: &mut [i64] = unsafe { std::slice::from_raw_parts_mut(vs.as_ptr() as *mut i64, rows) };
+        let inv: &mut [i64] =
+            unsafe { std::slice::from_raw_parts_mut(vs.as_ptr() as *mut i64, rows) };
         for (g, &gs) in group_starts.iter().enumerate() {
             let end = if g + 1 < u { group_starts[g + 1] } else { rows };
             for &orig in &perm[gs..end] {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:78677:
         return Ok(None);
     }
     if item.getattr("ndim")?.extract::<usize>()? != 2
-        || !item.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()?
+        || !item
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
     {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:78699:
     }
     use rayon::prelude::*;
     // SAFETY: ReadOnlyCell<i64> repr(transparent); read-only under the GIL.
-    let data: &[i64] = unsafe { std::slice::from_raw_parts(cells.as_ptr().cast::<i64>(), rows * ncols) };
+    let data: &[i64] =
+        unsafe { std::slice::from_raw_parts(cells.as_ptr().cast::<i64>(), rows * ncols) };
     let row = |r: u32| -> &[i64] {
         let b = r as usize * ncols;
         &data[b..b + ncols]
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:78748:
         let Some(os) = ob.as_mut_slice(py) else {
             return Ok(None);
         };
-        let o: &mut [i64] = unsafe { std::slice::from_raw_parts_mut(os.as_ptr() as *mut i64, u * ncols) };
+        let o: &mut [i64] =
+            unsafe { std::slice::from_raw_parts_mut(os.as_ptr() as *mut i64, u * ncols) };
         o.par_chunks_exact_mut(ncols)
             .zip(group_starts.par_iter())
             .for_each(|(orow, &gs)| {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:78767:
         let Some(is_) = ib.as_mut_slice(py) else {
             return Ok(None);
         };
-        let out: &mut [i64] = unsafe { std::slice::from_raw_parts_mut(is_.as_ptr() as *mut i64, u) };
+        let out: &mut [i64] =
+            unsafe { std::slice::from_raw_parts_mut(is_.as_ptr() as *mut i64, u) };
         out.par_iter_mut()
             .zip(group_starts.par_iter())
             .for_each(|(o, &gs)| *o = perm[gs] as i64);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:78784:
         let Some(vs) = vb.as_mut_slice(py) else {
             return Ok(None);
         };
-        let inv: &mut [i64] = unsafe { std::slice::from_raw_parts_mut(vs.as_ptr() as *mut i64, rows) };
+        let inv: &mut [i64] =
+            unsafe { std::slice::from_raw_parts_mut(vs.as_ptr() as *mut i64, rows) };
         for (g, &gs) in group_starts.iter().enumerate() {
             let end = if g + 1 < u { group_starts[g + 1] } else { rows };
             for &orig in &perm[gs..end] {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:78924:
             return Ok(None);
         };
         // SAFETY: fresh numpy.empty output, cannot alias input; each element written exactly once.
-        let o: &mut [i64] = unsafe { std::slice::from_raw_parts_mut(o_s.as_ptr() as *mut i64, u * k) };
+        let o: &mut [i64] =
+            unsafe { std::slice::from_raw_parts_mut(o_s.as_ptr() as *mut i64, u * k) };
         o.par_chunks_mut(k)
             .zip(comp.par_iter())
             .for_each(|(orow, &c)| {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:78936:
     // Restore the input dtype exactly (values are the originals, so the narrowing cast is lossless).
     let cast_kw = PyDict::new(py);
     cast_kw.set_item("copy", false)?;
-    Ok(Some(flat.call_method("astype", (&dt,), Some(&cast_kw))?.unbind()))
+    Ok(Some(
+        flat.call_method("astype", (&dt,), Some(&cast_kw))?.unbind(),
+    ))
 }

 // unique(2-D small-range integer array, axis=1) via a packed composite column key. This is the column
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:79041:
     }
     let cast_kw = PyDict::new(py);
     cast_kw.set_item("copy", false)?;
-    Ok(Some(flat.call_method("astype", (&dt,), Some(&cast_kw))?.unbind()))
+    Ok(Some(
+        flat.call_method("astype", (&dt,), Some(&cast_kw))?.unbind(),
+    ))
 }

 // Counting-sort dedup core for np.unique over a small-range integer array. numpy
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:79120:
 // (O(n+range) vs the sort path's O(n log n)). Gated to a C-contiguous integer
 // ndarray; dispatched by (kind, itemsize). Wide ranges / non-contiguous / empty
 // inputs return None so the caller keeps the existing sort path.
-fn try_zerocopy_int_unique(
-    py: Python<'_>,
-    item: &Bound<'_, PyAny>,
-) -> PyResult<Option<Py<PyAny>>> {
+fn try_zerocopy_int_unique(py: Python<'_>, item: &Bound<'_, PyAny>) -> PyResult<Option<Py<PyAny>>> {
     let numpy = py.import("numpy")?;
     let ndarray_type = numpy.getattr("ndarray")?;
     if !item.is_exact_instance(&ndarray_type) {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:79225:
     }
     let size = range as usize + 1;
     let mut seen = vec![false; size];
-    let mut first_idx = if want_index { vec![0i64; size] } else { Vec::new() };
+    let mut first_idx = if want_index {
+        vec![0i64; size]
+    } else {
+        Vec::new()
+    };
     let mut counts = if want_counts || want_inverse {
         vec![0i64; size]
     } else {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:79247:
     let mut uvals: Vec<i128> = Vec::new();
     let mut idxv: Vec<i64> = Vec::new();
     let mut cntv: Vec<i64> = Vec::new();
-    let mut rank = if want_inverse { vec![0i64; size] } else { Vec::new() };
+    let mut rank = if want_inverse {
+        vec![0i64; size]
+    } else {
+        Vec::new()
+    };
     let mut w: i64 = 0;
     for (i, &b) in seen.iter().enumerate() {
         if b {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:79709:
         use rayon::prelude::*;
         // SAFETY: ReadOnlyCell<T> is repr(transparent) over T (read-only under the GIL); `out` is a fresh
         // numpy.empty(uint8) we own, disjoint chunks written exactly once.
-        let in_raw: &[T] = unsafe { std::slice::from_raw_parts(in_slice.as_ptr().cast::<T>(), total) };
+        let in_raw: &[T] =
+            unsafe { std::slice::from_raw_parts(in_slice.as_ptr().cast::<T>(), total) };
         let out_raw: &mut [u8] =
             unsafe { std::slice::from_raw_parts_mut(out_slice.as_ptr() as *mut u8, total) };
         let chunk = total.div_ceil(rayon::current_num_threads()).max(1);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:79729:
         ("u", 2) => pop_fill::<u16>(py, array, &out, total, |v| v.count_ones() as u8),
         ("u", 4) => pop_fill::<u32>(py, array, &out, total, |v| v.count_ones() as u8),
         ("u", 8) => pop_fill::<u64>(py, array, &out, total, |v| v.count_ones() as u8),
-        ("i", 1) => pop_fill::<i8>(py, array, &out, total, |v| v.unsigned_abs().count_ones() as u8),
-        ("i", 2) => pop_fill::<i16>(py, array, &out, total, |v| v.unsigned_abs().count_ones() as u8),
-        ("i", 4) => pop_fill::<i32>(py, array, &out, total, |v| v.unsigned_abs().count_ones() as u8),
-        ("i", 8) => pop_fill::<i64>(py, array, &out, total, |v| v.unsigned_abs().count_ones() as u8),
+        ("i", 1) => pop_fill::<i8>(py, array, &out, total, |v| {
+            v.unsigned_abs().count_ones() as u8
+        }),
+        ("i", 2) => pop_fill::<i16>(py, array, &out, total, |v| {
+            v.unsigned_abs().count_ones() as u8
+        }),
+        ("i", 4) => pop_fill::<i32>(py, array, &out, total, |v| {
+            v.unsigned_abs().count_ones() as u8
+        }),
+        ("i", 8) => pop_fill::<i64>(py, array, &out, total, |v| {
+            v.unsigned_abs().count_ones() as u8
+        }),
         _ => return Ok(None),
     };
     match done {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:79821:
     if dt.getattr("kind")?.extract::<String>()? != "u"
         || dt.getattr("itemsize")?.extract::<usize>()? != 1
         || x.getattr("ndim")?.extract::<usize>()? != 1
-        || !x.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()?
+        || !x
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
     {
         return delegate();
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:79828:
-    let n: usize = x.getattr("shape")?.extract::<Vec<usize>>()?.iter().product();
+    let n: usize = x
+        .getattr("shape")?
+        .extract::<Vec<usize>>()?
+        .iter()
+        .product();
     if n < UNPACKBITS_PARALLEL_MIN || rayon::current_num_threads() < 2 {
         return delegate();
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:79851:
         use rayon::prelude::*;
         // SAFETY: ReadOnlyCell<u8> is repr(transparent) over u8 (read-only under the GIL); `out` is a
         // fresh numpy.empty(uint8) we own; each output group of 8 is written exactly once.
-        let in_raw: &[u8] = unsafe { std::slice::from_raw_parts(in_slice.as_ptr().cast::<u8>(), n) };
+        let in_raw: &[u8] =
+            unsafe { std::slice::from_raw_parts(in_slice.as_ptr().cast::<u8>(), n) };
         let out_raw: &mut [u8] =
             unsafe { std::slice::from_raw_parts_mut(out_slice.as_ptr() as *mut u8, 8 * n) };
         let chunk = n.div_ceil(rayon::current_num_threads()).max(1);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:80243:
     let n = codepoints_in.len();
     use rayon::prelude::*;
     // SAFETY: ReadOnlyCell<u32> is repr(transparent) over u32; read-only under the GIL.
-    let cin: &[u32] = unsafe { std::slice::from_raw_parts(codepoints_in.as_ptr().cast::<u32>(), n) };
+    let cin: &[u32] =
+        unsafe { std::slice::from_raw_parts(codepoints_in.as_ptr().cast::<u32>(), n) };
     // numpy.char/strings upper/lower/swapcase use full-Unicode casing (e.g. 'ß'.upper()=='SS' changes
     // width); only ASCII codepoints map cleanly. Defer the whole call to numpy on ANY non-ASCII
     // codepoint. The all-ASCII scan + the case map are both O(total codepoints) — parallelize both
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:80351:
             return Ok(None);
         };
         // SAFETY: fresh empty_like buffer we own (no alias with the input view).
-        let cout: &mut [u8] = unsafe { std::slice::from_raw_parts_mut(output.as_ptr() as *mut u8, n) };
+        let cout: &mut [u8] =
+            unsafe { std::slice::from_raw_parts_mut(output.as_ptr() as *mut u8, n) };
         let map = |b: u8| -> u8 {
             match method {
                 "upper" if b.is_ascii_lowercase() => b - 32,
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:80417:
     }
     use rayon::prelude::*;
     // SAFETY: ReadOnlyCell<u32> is repr(transparent) over u32; read-only under the GIL.
-    let cin: &[u32] = unsafe { std::slice::from_raw_parts(codepoints_in.as_ptr().cast::<u32>(), n) };
+    let cin: &[u32] =
+        unsafe { std::slice::from_raw_parts(codepoints_in.as_ptr().cast::<u32>(), n) };
     // numpy capitalize/title use full-Unicode casing; defer on any non-ASCII codepoint. Both the
     // all-ASCII scan and the per-slot map are O(total codepoints) and parallelize across the (whole-
     // string) slots — each fixed-width slot is independent (title's prev_cased state is per-slot).
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:80441:
     // SAFETY: fresh empty_like buffer we own (no alias with the input view).
     let cout: &mut [u32] =
         unsafe { std::slice::from_raw_parts_mut(output.as_ptr() as *mut u32, n) };
-    let ascii_upper =
-        |c: u32| if (u32::from(b'a')..=u32::from(b'z')).contains(&c) { c - 32 } else { c };
-    let ascii_lower =
-        |c: u32| if (u32::from(b'A')..=u32::from(b'Z')).contains(&c) { c + 32 } else { c };
+    let ascii_upper = |c: u32| {
+        if (u32::from(b'a')..=u32::from(b'z')).contains(&c) {
+            c - 32
+        } else {
+            c
+        }
+    };
+    let ascii_lower = |c: u32| {
+        if (u32::from(b'A')..=u32::from(b'Z')).contains(&c) {
+            c + 32
+        } else {
+            c
+        }
+    };
     let is_letter = |c: u32| {
         (u32::from(b'a')..=u32::from(b'z')).contains(&c)
             || (u32::from(b'A')..=u32::from(b'Z')).contains(&c)
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:80455:
             for (s, d) in slot_in.iter().zip(slot_out.iter_mut()) {
                 let c = *s;
                 if is_letter(c) {
-                    *d = if prev_cased { ascii_lower(c) } else { ascii_upper(c) };
+                    *d = if prev_cased {
+                        ascii_lower(c)
+                    } else {
+                        ascii_upper(c)
+                    };
                     prev_cased = true;
                 } else {
                     *d = c;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:80465:
         } else {
             for (j, (s, d)) in slot_in.iter().zip(slot_out.iter_mut()).enumerate() {
                 let c = *s;
-                *d = if j == 0 { ascii_upper(c) } else { ascii_lower(c) };
+                *d = if j == 0 {
+                    ascii_upper(c)
+                } else {
+                    ascii_lower(c)
+                };
             }
         }
     };
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:80478:
             process_slot(i, o);
         }
     }
-    Ok(Some(codepoints_out.call_method1("view", (&dtype,))?.unbind()))
+    Ok(Some(
+        codepoints_out.call_method1("view", (&dtype,))?.unbind(),
+    ))
 }

 // ASCII capitalize/title for 'S' (bytes) arrays — bytes.capitalize()/title() are ASCII-only (non-ASCII bytes
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:80535:
             return Ok(None);
         };
         // SAFETY: fresh empty_like buffer we own (no alias with the input view).
-        let cout: &mut [u8] = unsafe { std::slice::from_raw_parts_mut(output.as_ptr() as *mut u8, n) };
+        let cout: &mut [u8] =
+            unsafe { std::slice::from_raw_parts_mut(output.as_ptr() as *mut u8, n) };
         let process_slot = |slot_in: &[u8], slot_out: &mut [u8]| {
             if is_title {
                 let mut prev_cased = false;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:80627:
     let n = codepoints_in.len();
     use rayon::prelude::*;
     // SAFETY: ReadOnlyCell<u32> is repr(transparent) over u32; read-only under the GIL.
-    let cin: &[u32] = unsafe { std::slice::from_raw_parts(codepoints_in.as_ptr().cast::<u32>(), n) };
+    let cin: &[u32] =
+        unsafe { std::slice::from_raw_parts(codepoints_in.as_ptr().cast::<u32>(), n) };
     // numpy char.translate uses str.translate (full Unicode); defer on any non-ASCII codepoint.
     // The scan + the 1:1 lookup remap are O(total codepoints); parallelize both (numpy runs
     // str.translate single-threaded per element). Gate the codepoint count.
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:80666:
             *d = lookup[s as usize];
         }
     }
-    Ok(Some(codepoints_out.call_method1("view", (&dtype,))?.unbind()))
+    Ok(Some(
+        codepoints_out.call_method1("view", (&dtype,))?.unbind(),
+    ))
 }

 // numpy.char/strings.translate(a, table) for 'S' (bytes): table is a 256-byte translation (from
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:80859:
         dtb.getattr("kind")?.extract::<String>()?,
     );
     let is_contig = |v: &Bound<'_, PyAny>| -> PyResult<bool> {
-        v.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()
+        v.getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()
     };
     if !is_contig(a)? || !is_contig(b)? {
         return Ok(None);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:80990:
                 }
             }
             Some(lo) => {
-                let hi = content.iter().rposition(|&c| !is_ascii_strip_ws(c)).unwrap() + 1;
+                let hi = content
+                    .iter()
+                    .rposition(|&c| !is_ascii_strip_ws(c))
+                    .unwrap()
+                    + 1;
                 let (s, e) = match mode {
                     1 => (lo, content_len), // lstrip
                     2 => (0, hi),           // rstrip
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:81013:
             strip_slot(i, o);
         }
     }
-    Ok(Some(codepoints_out.call_method1("view", (&dtype,))?.unbind()))
+    Ok(Some(
+        codepoints_out.call_method1("view", (&dtype,))?.unbind(),
+    ))
 }

 // char/strings strip/lstrip/rstrip: native ASCII whitespace strip (no `chars` arg), else numpy.
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:81026:
     namespace: &str,
 ) -> PyResult<Py<PyAny>> {
     let chars_given = chars.as_ref().is_some_and(|c| !c.bind(py).is_none());
-    if !chars_given
-        && let Some(result) = try_zerocopy_unicode_strip(py, a.bind(py), mode)?
-    {
+    if !chars_given && let Some(result) = try_zerocopy_unicode_strip(py, a.bind(py), mode)? {
         return Ok(result);
     }
     let numpy = py.import("numpy")?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:81183:
     }
     let dtype = a.getattr("dtype")?;
     let kind = dtype.getattr("kind")?.extract::<String>()?;
-    if !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()? {
+    if !a
+        .getattr("flags")?
+        .getattr("c_contiguous")?
+        .extract::<bool>()?
+    {
         return Ok(None);
     }
     let itemsize = dtype.getattr("itemsize")?.extract::<usize>()?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:81209:
                 return Ok(None);
             }
             run_replace::<u32>(
-                py, &numpy, a, &shape, itemsize / 4, &old_cp, &new_cp, "U", "uint32", true,
+                py,
+                &numpy,
+                a,
+                &shape,
+                itemsize / 4,
+                &old_cp,
+                &new_cp,
+                "U",
+                "uint32",
+                true,
             )
         }
         "S" => {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:81216:
-            let (Ok(old_b), Ok(new_b)) = (old.extract::<Vec<u8>>(), new.extract::<Vec<u8>>()) else {
+            let (Ok(old_b), Ok(new_b)) = (old.extract::<Vec<u8>>(), new.extract::<Vec<u8>>())
+            else {
                 return Ok(None); // array old/new / str-not-bytes -> numpy
             };
             if old_b.is_empty() {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:81222:
             if itemsize == 0 {
                 return Ok(None);
             }
-            run_replace::<u8>(py, &numpy, a, &shape, itemsize, &old_b, &new_b, "S", "uint8", false)
+            run_replace::<u8>(
+                py, &numpy, a, &shape, itemsize, &old_b, &new_b, "S", "uint8", false,
+            )
         }
         _ => Ok(None),
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:81288:
     let in_class = |c: u32| -> bool {
         match mode {
             0 => (0x41..=0x5a).contains(&c) || (0x61..=0x7a).contains(&c), // isalpha
-            1 => (0x30..=0x39).contains(&c),                              // isdigit
+            1 => (0x30..=0x39).contains(&c),                               // isdigit
             2 => {
                 (0x41..=0x5a).contains(&c)
                     || (0x61..=0x7a).contains(&c)
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:81295:
                     || (0x30..=0x39).contains(&c)
             } // isalnum
-            _ => is_ascii_strip_ws(c),                                    // isspace
+            _ => is_ascii_strip_ws(c),                                     // isspace
         }
     };
     let n = total / w;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:81514:
     }
     let dtype = a.getattr("dtype")?;
     let kind = dtype.getattr("kind")?.extract::<String>()?;
-    if !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()? {
+    if !a
+        .getattr("flags")?
+        .getattr("c_contiguous")?
+        .extract::<bool>()?
+    {
         return Ok(None);
     }
     let itemsize = dtype.getattr("itemsize")?.extract::<usize>()?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:81548:
 // both encodings. `oslot` is pre-sized to out_w >= width; content (clen) is copied verbatim (non-ASCII bytes
 // pass through) and the remainder filled per mode.
 #[inline]
-fn pad_build<E: Copy + PartialEq + From<u8>>(slot: &[E], oslot: &mut [E], mode: u8, width: usize, fill: E) {
+fn pad_build<E: Copy + PartialEq + From<u8>>(
+    slot: &[E],
+    oslot: &mut [E],
+    mode: u8,
+    width: usize,
+    fill: E,
+) {
     let zero = E::from(0u8);
     for s in oslot.iter_mut() {
         *s = zero;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:81700:
     }
     let dtype = a.getattr("dtype")?;
     let kind = dtype.getattr("kind")?.extract::<String>()?;
-    if !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()? {
+    if !a
+        .getattr("flags")?
+        .getattr("c_contiguous")?
+        .extract::<bool>()?
+    {
         return Ok(None);
     }
     let itemsize = dtype.getattr("itemsize")?.extract::<usize>()?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:81763:
     namespace: &str,
     method: &str,
 ) -> PyResult<Py<PyAny>> {
-    if let Some(out) =
-        try_zerocopy_unicode_pad(py, a.bind(py), width.bind(py), fillchar.as_ref().map(|f| f.bind(py)), mode)?
-    {
+    if let Some(out) = try_zerocopy_unicode_pad(
+        py,
+        a.bind(py),
+        width.bind(py),
+        fillchar.as_ref().map(|f| f.bind(py)),
+        mode,
+    )? {
         return Ok(out);
     }
     let numpy = py.import("numpy")?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:81834:
         let (mut col, mut buf) = (0usize, 0usize);
         for &c in &slot[..clen] {
             if c == tab {
-                let inc = if tabsize > 0 { tabsize - col % tabsize } else { 0 };
+                let inc = if tabsize > 0 {
+                    tabsize - col % tabsize
+                } else {
+                    0
+                };
                 buf += inc;
                 col += inc;
             } else if c == nl || c == cr {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:81872:
             let (mut col, mut pos) = (0usize, 0usize);
             for &c in &slot[..clen] {
                 if c == tab {
-                    let inc = if tabsize > 0 { tabsize - col % tabsize } else { 0 };
+                    let inc = if tabsize > 0 {
+                        tabsize - col % tabsize
+                    } else {
+                        0
+                    };
                     for s in oslot.iter_mut().skip(pos).take(inc) {
                         *s = space;
                     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:81917:
     }
     let dtype = a.getattr("dtype")?;
     let kind = dtype.getattr("kind")?.extract::<String>()?;
-    if !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()? {
+    if !a
+        .getattr("flags")?
+        .getattr("c_contiguous")?
+        .extract::<bool>()?
+    {
         return Ok(None);
     }
     let itemsize = dtype.getattr("itemsize")?.extract::<usize>()?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:81969:
     }
     let dtype = a.getattr("dtype")?;
     if dtype.getattr("kind")?.extract::<String>()? != "U"
-        || !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()?
+        || !a
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
     {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:82057:
         // index/rindex (require_found) raise ValueError on a miss. Flag any -1 during the fill; if seen,
         // return None so the caller delegates to numpy for its EXACT ValueError (rare error path).
         let missing = std::sync::atomic::AtomicBool::new(false);
-        cout.par_iter_mut().zip(cin.par_chunks(w)).for_each(|(o, i)| {
-            let r = search(i);
-            if require_found && r < 0 {
-                missing.store(true, std::sync::atomic::Ordering::Relaxed);
-            }
-            *o = r;
-        });
+        cout.par_iter_mut()
+            .zip(cin.par_chunks(w))
+            .for_each(|(o, i)| {
+                let r = search(i);
+                if require_found && r < 0 {
+                    missing.store(true, std::sync::atomic::Ordering::Relaxed);
+                }
+                *o = r;
+            });
         if require_found && missing.load(std::sync::atomic::Ordering::Relaxed) {
             return Ok(None);
         }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:82090:
     }
     let dtype = a.getattr("dtype")?;
     if dtype.getattr("kind")?.extract::<String>()? != "U"
-        || !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()?
+        || !a
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
     {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:82122:
     let content_len = |slot: &[u32]| slot.iter().rposition(|&c| c != 0).map_or(0, |p| p + 1);
     // CPython PySlice_GetIndicesEx clamping -> (start_idx, stop_idx) for this step and string length.
     let indices = |len_: i64| -> (i64, i64) {
-        let (lower, upper) = if step < 0 { (-1i64, len_ - 1) } else { (0i64, len_) };
+        let (lower, upper) = if step < 0 {
+            (-1i64, len_ - 1)
+        } else {
+            (0i64, len_)
+        };
         let clamp = |v: i64| -> i64 {
             if v < 0 {
                 (v + len_).max(lower)
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:82511:
             return None; // flags/width/other -> numpy
         }
         let digs: String = chars[start..j].iter().collect();
-        if digs.is_empty() { 0 } else { digs.parse().ok()? }
+        if digs.is_empty() {
+            0
+        } else {
+            digs.parse().ok()?
+        }
     } else {
         return None;
     };
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:82623:
             return Ok(None);
         }
         let kwargs = PyDict::new(py);
-        kwargs.set_item("dtype", format!("{}{out_w}", if as_bytes { "S" } else { "U" }))?;
+        kwargs.set_item(
+            "dtype",
+            format!("{}{out_w}", if as_bytes { "S" } else { "U" }),
+        )?;
         let shape_tuple = PyTuple::new(py, shape.iter().copied())?;
         let out = numpy.call_method("empty", (shape_tuple,), Some(&kwargs))?;
         {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:82702:

     if itemsize == 8 {
         fill::<f64>(
-            py, values, &numpy, &prefix, &suffix, &ffmt, |v| v, &shape, n, as_bytes,
+            py,
+            values,
+            &numpy,
+            &prefix,
+            &suffix,
+            &ffmt,
+            |v| v,
+            &shape,
+            n,
+            as_bytes,
         )
     } else {
         fill::<f32>(
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:82807:
             return Ok(None);
         }
         let kwargs = PyDict::new(py);
-        kwargs.set_item("dtype", format!("{}{out_w}", if as_bytes { "S" } else { "U" }))?;
+        kwargs.set_item(
+            "dtype",
+            format!("{}{out_w}", if as_bytes { "S" } else { "U" }),
+        )?;
         let shape_tuple = PyTuple::new(py, shape.iter().copied())?;
         let out = numpy.call_method("empty", (shape_tuple,), Some(&kwargs))?;
         {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:82874:
     }

     match (kind.as_str(), itemsize) {
-        ("i", 1) => fill::<i8>(py, values, &numpy, &prefix, &suffix, &ifmt, &shape, n, as_bytes),
-        ("i", 2) => fill::<i16>(py, values, &numpy, &prefix, &suffix, &ifmt, &shape, n, as_bytes),
-        ("i", 4) => fill::<i32>(py, values, &numpy, &prefix, &suffix, &ifmt, &shape, n, as_bytes),
-        ("i", 8) => fill::<i64>(py, values, &numpy, &prefix, &suffix, &ifmt, &shape, n, as_bytes),
-        ("u", 1) => fill::<u8>(py, values, &numpy, &prefix, &suffix, &ifmt, &shape, n, as_bytes),
-        ("u", 2) => fill::<u16>(py, values, &numpy, &prefix, &suffix, &ifmt, &shape, n, as_bytes),
-        ("u", 4) => fill::<u32>(py, values, &numpy, &prefix, &suffix, &ifmt, &shape, n, as_bytes),
-        ("u", 8) => fill::<u64>(py, values, &numpy, &prefix, &suffix, &ifmt, &shape, n, as_bytes),
+        ("i", 1) => fill::<i8>(
+            py, values, &numpy, &prefix, &suffix, &ifmt, &shape, n, as_bytes,
+        ),
+        ("i", 2) => fill::<i16>(
+            py, values, &numpy, &prefix, &suffix, &ifmt, &shape, n, as_bytes,
+        ),
+        ("i", 4) => fill::<i32>(
+            py, values, &numpy, &prefix, &suffix, &ifmt, &shape, n, as_bytes,
+        ),
+        ("i", 8) => fill::<i64>(
+            py, values, &numpy, &prefix, &suffix, &ifmt, &shape, n, as_bytes,
+        ),
+        ("u", 1) => fill::<u8>(
+            py, values, &numpy, &prefix, &suffix, &ifmt, &shape, n, as_bytes,
+        ),
+        ("u", 2) => fill::<u16>(
+            py, values, &numpy, &prefix, &suffix, &ifmt, &shape, n, as_bytes,
+        ),
+        ("u", 4) => fill::<u32>(
+            py, values, &numpy, &prefix, &suffix, &ifmt, &shape, n, as_bytes,
+        ),
+        ("u", 8) => fill::<u64>(
+            py, values, &numpy, &prefix, &suffix, &ifmt, &shape, n, as_bytes,
+        ),
         _ => Ok(None),
     }
 }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:82901:
     // start/end restrict the search window -> defer (native handles the whole-string case only).
     if !non_default(&start)
         && !non_default(&end)
-        && let Some(out) = try_zerocopy_unicode_search(py, a.bind(py), sub.bind(py), mode, require_found)?
+        && let Some(out) =
+            try_zerocopy_unicode_search(py, a.bind(py), sub.bind(py), mode, require_found)?
     {
         return Ok(out);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:82948:
         Some(c) => c.bind(py).is_none() || c.bind(py).extract::<i64>().is_ok_and(|v| v == -1),
     };
     if count_default
-        && let Some(result) = try_zerocopy_unicode_replace(py, a.bind(py), old.bind(py), new.bind(py))?
+        && let Some(result) =
+            try_zerocopy_unicode_replace(py, a.bind(py), old.bind(py), new.bind(py))?
     {
         return Ok(result);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:82980:
             return Ok(result);
         }
     }
-    let func = py.import("numpy")?.getattr(namespace)?.getattr("translate")?;
+    let func = py
+        .import("numpy")?
+        .getattr(namespace)?
+        .getattr("translate")?;
     match deletechars {
         Some(d) => Ok(func
             .call1((input.bind(py), table.bind(py), d.bind(py)))?
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:83071:
 }

 #[pyfunction(name = "strip", signature = (a, chars=None))]
-fn char_strip_native(py: Python<'_>, a: Py<PyAny>, chars: Option<Py<PyAny>>) -> PyResult<Py<PyAny>> {
+fn char_strip_native(
+    py: Python<'_>,
+    a: Py<PyAny>,
+    chars: Option<Py<PyAny>>,
+) -> PyResult<Py<PyAny>> {
     unicode_strip_or_numpy(py, a, chars, 0, "strip", "char")
 }

Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:83078:
 #[pyfunction(name = "lstrip", signature = (a, chars=None))]
-fn char_lstrip_native(py: Python<'_>, a: Py<PyAny>, chars: Option<Py<PyAny>>) -> PyResult<Py<PyAny>> {
+fn char_lstrip_native(
+    py: Python<'_>,
+    a: Py<PyAny>,
+    chars: Option<Py<PyAny>>,
+) -> PyResult<Py<PyAny>> {
     unicode_strip_or_numpy(py, a, chars, 1, "lstrip", "char")
 }

Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:83083:
 #[pyfunction(name = "rstrip", signature = (a, chars=None))]
-fn char_rstrip_native(py: Python<'_>, a: Py<PyAny>, chars: Option<Py<PyAny>>) -> PyResult<Py<PyAny>> {
+fn char_rstrip_native(
+    py: Python<'_>,
+    a: Py<PyAny>,
+    chars: Option<Py<PyAny>>,
+) -> PyResult<Py<PyAny>> {
     unicode_strip_or_numpy(py, a, chars, 2, "rstrip", "char")
 }

Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:83159:
 }

 #[pyfunction(name = "strip", signature = (a, chars=None))]
-fn strings_strip_native(py: Python<'_>, a: Py<PyAny>, chars: Option<Py<PyAny>>) -> PyResult<Py<PyAny>> {
+fn strings_strip_native(
+    py: Python<'_>,
+    a: Py<PyAny>,
+    chars: Option<Py<PyAny>>,
+) -> PyResult<Py<PyAny>> {
     unicode_strip_or_numpy(py, a, chars, 0, "strip", "strings")
 }

Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:83166:
 #[pyfunction(name = "lstrip", signature = (a, chars=None))]
-fn strings_lstrip_native(py: Python<'_>, a: Py<PyAny>, chars: Option<Py<PyAny>>) -> PyResult<Py<PyAny>> {
+fn strings_lstrip_native(
+    py: Python<'_>,
+    a: Py<PyAny>,
+    chars: Option<Py<PyAny>>,
+) -> PyResult<Py<PyAny>> {
     unicode_strip_or_numpy(py, a, chars, 1, "lstrip", "strings")
 }

Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:83171:
 #[pyfunction(name = "rstrip", signature = (a, chars=None))]
-fn strings_rstrip_native(py: Python<'_>, a: Py<PyAny>, chars: Option<Py<PyAny>>) -> PyResult<Py<PyAny>> {
+fn strings_rstrip_native(
+    py: Python<'_>,
+    a: Py<PyAny>,
+    chars: Option<Py<PyAny>>,
+) -> PyResult<Py<PyAny>> {
     unicode_strip_or_numpy(py, a, chars, 2, "rstrip", "strings")
 }

Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:83362:
         return Ok(None);
     }
     // Build one {U|S}{width} output array from a per-element writer.
-    let build = |width: usize,
-                 writer: &(dyn Fn(usize, &[E], &mut [E]) + Sync)|
-     -> PyResult<Py<PyAny>> {
-        let kwargs = PyDict::new(py);
-        kwargs.set_item("dtype", format!("{kind_prefix}{width}"))?; // width >= 1 (0-width parts deferred)
-        let shape_tuple = PyTuple::new(py, shape.iter().copied())?;
-        let out = numpy.call_method("empty", (shape_tuple,), Some(&kwargs))?;
-        let wout = width;
-        {
-            let out_view = out.call_method1("view", (&view_dtype,))?;
-            let ob = PyBuffer::<E>::get(&out_view)
-                .map_err(|e| PyValueError::new_err(e.to_string()))?;
-            let co = ob
-                .as_mut_slice(py)
-                .ok_or_else(|| PyValueError::new_err("output not contiguous"))?;
-            // SAFETY: fresh numpy.empty buffer we own; disjoint wout-cell slots.
-            let cout: &mut [E] =
-                unsafe { std::slice::from_raw_parts_mut(co.as_ptr() as *mut E, n * wout) };
-            cout.par_chunks_mut(wout)
-                .zip(cin.par_chunks(w))
-                .enumerate()
-                .for_each(|(i, (o, slot))| {
-                    for s in o.iter_mut() {
-                        *s = zero;
-                    }
-                    writer(i, slot, o);
-                });
-        }
-        Ok(out.unbind())
-    };
+    let build =
+        |width: usize, writer: &(dyn Fn(usize, &[E], &mut [E]) + Sync)| -> PyResult<Py<PyAny>> {
+            let kwargs = PyDict::new(py);
+            kwargs.set_item("dtype", format!("{kind_prefix}{width}"))?; // width >= 1 (0-width parts deferred)
+            let shape_tuple = PyTuple::new(py, shape.iter().copied())?;
+            let out = numpy.call_method("empty", (shape_tuple,), Some(&kwargs))?;
+            let wout = width;
+            {
+                let out_view = out.call_method1("view", (&view_dtype,))?;
+                let ob = PyBuffer::<E>::get(&out_view)
+                    .map_err(|e| PyValueError::new_err(e.to_string()))?;
+                let co = ob
+                    .as_mut_slice(py)
+                    .ok_or_else(|| PyValueError::new_err("output not contiguous"))?;
+                // SAFETY: fresh numpy.empty buffer we own; disjoint wout-cell slots.
+                let cout: &mut [E] =
+                    unsafe { std::slice::from_raw_parts_mut(co.as_ptr() as *mut E, n * wout) };
+                cout.par_chunks_mut(wout)
+                    .zip(cin.par_chunks(w))
+                    .enumerate()
+                    .for_each(|(i, (o, slot))| {
+                        for s in o.iter_mut() {
+                            *s = zero;
+                        }
+                        writer(i, slot, o);
+                    });
+            }
+            Ok(out.unbind())
+        };
     let info_ref = &info;
     let before = build(bw, &|i, slot, o| {
         let (pos, blen, _) = info_ref[i];
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:83415:
         }
     })?;
     Ok(Some(
-        PyTuple::new(py, [before, sep_out, after])?.into_any().unbind(),
+        PyTuple::new(py, [before, sep_out, after])?
+            .into_any()
+            .unbind(),
     ))
 }

Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:83431:
     }
     let dtype = a.getattr("dtype")?;
     let kind = dtype.getattr("kind")?.extract::<String>()?;
-    if !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()? {
+    if !a
+        .getattr("flags")?
+        .getattr("c_contiguous")?
+        .extract::<bool>()?
+    {
         return Ok(None);
     }
     let itemsize = dtype.getattr("itemsize")?.extract::<usize>()?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:83449:
             if itemsize == 0 || itemsize % 4 != 0 {
                 return Ok(None);
             }
-            run_partition::<u32>(py, &numpy, a, itemsize / 4, &sep_cp, from_right, "U", "uint32")
+            run_partition::<u32>(
+                py,
+                &numpy,
+                a,
+                itemsize / 4,
+                &sep_cp,
+                from_right,
+                "U",
+                "uint32",
+            )
         }
         "S" => {
             let Ok(sep_b) = sep.extract::<Vec<u8>>() else {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:83484:
     }
     let dtype = a.getattr("dtype")?;
     if dtype.getattr("kind")?.extract::<String>()? != "S"
-        || !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()?
+        || !a
+            .getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()?
     {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:83591:
     }
     let numpy = py.import("numpy")?;
     let f = numpy.getattr("strings")?.getattr("decode")?;
-    Ok(f.call1((a.bind(py), encoding.as_ref().map(|e| e.bind(py)), errors.as_ref().map(|e| e.bind(py))))?
-        .unbind())
+    Ok(f.call1((
+        a.bind(py),
+        encoding.as_ref().map(|e| e.bind(py)),
+        errors.as_ref().map(|e| e.bind(py)),
+    ))?
+    .unbind())
 }

 #[pyfunction(name = "mod", signature = (a, values))]
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:83599:
-fn strings_mod_native(
-    py: Python<'_>,
-    a: Py<PyAny>,
-    values: Py<PyAny>,
-) -> PyResult<Py<PyAny>> {
+fn strings_mod_native(py: Python<'_>, a: Py<PyAny>, values: Py<PyAny>) -> PyResult<Py<PyAny>> {
     // Native fast paths: plain %d/%i/%x/%X/%o over ints, %[.N]f over floats; everything else -> numpy.
     if let Some(out) = try_native_strings_mod_int(py, a.bind(py), values.bind(py))? {
         return Ok(out);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:83847:
         return Ok(None);
     }
     let c_contig = |a: &Bound<'_, PyAny>| -> PyResult<bool> {
-        a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()
+        a.getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()
     };
     if !c_contig(x1)? || !c_contig(x2)? {
         return Ok(None);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:83937:
         return Ok(Some(PyTuple::new(py, [&qs, &rs])?.into_any().unbind()));
     }
     Ok(Some(
-        PyTuple::new(py, [&quotient, &remainder])?.into_any().unbind(),
+        PyTuple::new(py, [&quotient, &remainder])?
+            .into_any()
+            .unbind(),
     ))
 }

Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:84085:
         ("i", 1) => {
             sign_typed::<i8, _>(py, &numpy, a, "int8", |v| i8::from(v > 0) - i8::from(v < 0))?
         }
-        ("i", 2) => {
-            sign_typed::<i16, _>(py, &numpy, a, "int16", |v| i16::from(v > 0) - i16::from(v < 0))?
-        }
-        ("i", 4) => {
-            sign_typed::<i32, _>(py, &numpy, a, "int32", |v| i32::from(v > 0) - i32::from(v < 0))?
-        }
+        ("i", 2) => sign_typed::<i16, _>(py, &numpy, a, "int16", |v| {
+            i16::from(v > 0) - i16::from(v < 0)
+        })?,
+        ("i", 4) => sign_typed::<i32, _>(py, &numpy, a, "int32", |v| {
+            i32::from(v > 0) - i32::from(v < 0)
+        })?,
         ("i", 8) => sign_typed::<i64, _>(py, &numpy, a, "int64", |v| {
             i64::from(v > 0) - i64::from(v < 0)
         })?,
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:84268:
         let mul = |x: i64, y: i64| x.wrapping_mul(y);
         return match axis {
             None => prod_typed::<u8, i64, _, _>(py, &numpy, &viewed, "int64", 1, |v| v as i64, mul),
-            Some(ax) => {
-                prod_axis_typed::<u8, i64, _, _>(py, &numpy, &viewed, "int64", 1, |v| v as i64, mul, ax)
-            }
+            Some(ax) => prod_axis_typed::<u8, i64, _, _>(
+                py,
+                &numpy,
+                &viewed,
+                "int64",
+                1,
+                |v| v as i64,
+                mul,
+                ax,
+            ),
         };
     }
     // Dispatch each (input type T, accumulator type A) pair to either the full
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:84278:
     macro_rules! dispatch {
         ($t:ty, $a:ty, $dt:literal, $conv:expr) => {
             match axis {
-                None => prod_typed::<$t, $a, _, _>(
-                    py,
-                    &numpy,
-                    a,
-                    $dt,
-                    1,
-                    $conv,
-                    |x: $a, y: $a| x.wrapping_mul(y),
-                ),
+                None => prod_typed::<$t, $a, _, _>(py, &numpy, a, $dt, 1, $conv, |x: $a, y: $a| {
+                    x.wrapping_mul(y)
+                }),
                 Some(ax) => prod_axis_typed::<$t, $a, _, _>(
                     py,
                     &numpy,
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:84737:
     if itemsize <= 2 || size < (1 << 23) {
         let kwargs = PyDict::new(py);
         kwargs.set_item("axis", axis)?;
-        return Ok(Some(numpy.getattr("ptp")?.call((a,), Some(&kwargs))?.unbind()));
+        return Ok(Some(
+            numpy.getattr("ptp")?.call((a,), Some(&kwargs))?.unbind(),
+        ));
     }
     let result = match (kind.as_str(), itemsize) {
         ("i", 1) => ptp_axis_typed::<i8, _>(py, &numpy, a, axis, "int8", |x, y| x.wrapping_sub(y))?,
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:84744:
-        ("i", 2) => ptp_axis_typed::<i16, _>(py, &numpy, a, axis, "int16", |x, y| x.wrapping_sub(y))?,
-        ("i", 4) => ptp_axis_typed::<i32, _>(py, &numpy, a, axis, "int32", |x, y| x.wrapping_sub(y))?,
-        ("i", 8) => ptp_axis_typed::<i64, _>(py, &numpy, a, axis, "int64", |x, y| x.wrapping_sub(y))?,
-        ("u", 1) => ptp_axis_typed::<u8, _>(py, &numpy, a, axis, "uint8", |x, y| x.wrapping_sub(y))?,
-        ("u", 2) => ptp_axis_typed::<u16, _>(py, &numpy, a, axis, "uint16", |x, y| x.wrapping_sub(y))?,
-        ("u", 4) => ptp_axis_typed::<u32, _>(py, &numpy, a, axis, "uint32", |x, y| x.wrapping_sub(y))?,
-        ("u", 8) => ptp_axis_typed::<u64, _>(py, &numpy, a, axis, "uint64", |x, y| x.wrapping_sub(y))?,
+        ("i", 2) => {
+            ptp_axis_typed::<i16, _>(py, &numpy, a, axis, "int16", |x, y| x.wrapping_sub(y))?
+        }
+        ("i", 4) => {
+            ptp_axis_typed::<i32, _>(py, &numpy, a, axis, "int32", |x, y| x.wrapping_sub(y))?
+        }
+        ("i", 8) => {
+            ptp_axis_typed::<i64, _>(py, &numpy, a, axis, "int64", |x, y| x.wrapping_sub(y))?
+        }
+        ("u", 1) => {
+            ptp_axis_typed::<u8, _>(py, &numpy, a, axis, "uint8", |x, y| x.wrapping_sub(y))?
+        }
+        ("u", 2) => {
+            ptp_axis_typed::<u16, _>(py, &numpy, a, axis, "uint16", |x, y| x.wrapping_sub(y))?
+        }
+        ("u", 4) => {
+            ptp_axis_typed::<u32, _>(py, &numpy, a, axis, "uint32", |x, y| x.wrapping_sub(y))?
+        }
+        ("u", 8) => {
+            ptp_axis_typed::<u64, _>(py, &numpy, a, axis, "uint64", |x, y| x.wrapping_sub(y))?
+        }
         _ => return Ok(None),
     };
     let Some((flat, out_shape)) = result else {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:84833:
             unsafe { std::slice::from_raw_parts(input.as_ptr().cast::<f64>(), input.len()) };
         use rayon::prelude::*;
         const PTP_AXIS_PARALLEL_MIN: usize = 1 << 21;
-        let parallel = outer * axis_len * inner >= PTP_AXIS_PARALLEL_MIN
-            && rayon::current_num_threads() >= 2;
+        let parallel =
+            outer * axis_len * inner >= PTP_AXIS_PARALLEL_MIN && rayon::current_num_threads() >= 2;
         let lane = axis_len * inner;
         // ptp tracks NaN-presence SEPARATELY and emits NaN for any nan lane, so the
         // running max/min can use branchless NaN-skipping f64::max/min (vectorizes;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:84875:
             let group_plane = |obase: usize| -> (Vec<f64>, Vec<f64>, Vec<bool>) {
                 let mut mxv = data[obase..obase + inner].to_vec();
                 let mut mnv = mxv.clone();
-                let mut nanv: Vec<bool> = data[obase..obase + inner].iter().map(|&v| v != v).collect();
+                let mut nanv: Vec<bool> =
+                    data[obase..obase + inner].iter().map(|&v| v != v).collect();
                 for a_idx in 1..axis_len {
                     let slab = &data[obase + a_idx * inner..obase + a_idx * inner + inner];
                     for (((m, n), nf), &v) in mxv
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:85022:
             unsafe { std::slice::from_raw_parts(input.as_ptr().cast::<f32>(), input.len()) };
         use rayon::prelude::*;
         const PTP_AXIS_PARALLEL_MIN: usize = 1 << 21;
-        let parallel = outer * axis_len * inner >= PTP_AXIS_PARALLEL_MIN
-            && rayon::current_num_threads() >= 2;
+        let parallel =
+            outer * axis_len * inner >= PTP_AXIS_PARALLEL_MIN && rayon::current_num_threads() >= 2;
         let lane = axis_len * inner;
         let result: Vec<f32> = if inner == 1 {
             let lane_ptp = |l: &[f32]| -> f32 {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:86043:
 // ndim>1 (axis=None raises a ValueError instead of flattening). Route the pure case to the
 // native parallel `cum{sum,prod}` (which beats numpy's single-threaded scan ~3.7x at 4M
 // f64), and defer `include_initial` / `dtype` / `out` / the axis=None-on-ND error to numpy.
-type CumulativeNative =
-    fn(Python<'_>, Py<PyAny>, Option<Py<PyAny>>, Option<Py<PyAny>>, Option<Py<PyAny>>) -> PyResult<Py<PyAny>>;
+type CumulativeNative = fn(
+    Python<'_>,
+    Py<PyAny>,
+    Option<Py<PyAny>>,
+    Option<Py<PyAny>>,
+    Option<Py<PyAny>>,
+) -> PyResult<Py<PyAny>>;

 fn cumulative_dispatch(
     py: Python<'_>,
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:86163:
     args: &Bound<'_, PyTuple>,
     kwargs: Option<&Bound<'_, PyDict>>,
 ) -> PyResult<Py<PyAny>> {
-    array_api_unique_route(py, args, kwargs, "unique_all", true, true, true, Some("UniqueAllResult"))
+    array_api_unique_route(
+        py,
+        args,
+        kwargs,
+        "unique_all",
+        true,
+        true,
+        true,
+        Some("UniqueAllResult"),
+    )
 }

 #[pyfunction]
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:86173:
     args: &Bound<'_, PyTuple>,
     kwargs: Option<&Bound<'_, PyDict>>,
 ) -> PyResult<Py<PyAny>> {
-    array_api_unique_route(py, args, kwargs, "unique_counts", false, false, true, Some("UniqueCountsResult"))
+    array_api_unique_route(
+        py,
+        args,
+        kwargs,
+        "unique_counts",
+        false,
+        false,
+        true,
+        Some("UniqueCountsResult"),
+    )
 }

 #[pyfunction]
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:86183:
     args: &Bound<'_, PyTuple>,
     kwargs: Option<&Bound<'_, PyDict>>,
 ) -> PyResult<Py<PyAny>> {
-    array_api_unique_route(py, args, kwargs, "unique_inverse", false, true, false, Some("UniqueInverseResult"))
+    array_api_unique_route(
+        py,
+        args,
+        kwargs,
+        "unique_inverse",
+        false,
+        true,
+        false,
+        Some("UniqueInverseResult"),
+    )
 }

 #[pyfunction]
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:86454:
         return Ok(None);
     }
     let is_contig = |x: &Bound<'_, PyAny>| -> PyResult<bool> {
-        x.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()
+        x.getattr("flags")?
+            .getattr("c_contiguous")?
+            .extract::<bool>()
     };
     if !is_contig(a)? || !is_contig(v)? {
         return Ok(None);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:86465:
     }
     let kind = dt.getattr("kind")?.extract::<String>()?;
     let itemsize = dt.getattr("itemsize")?.extract::<usize>()?;
-    if a_shape[0].saturating_mul(v_shape[0]) < INT_CONV_MIN_WORK
-        || rayon::current_num_threads() < 2
+    if a_shape[0].saturating_mul(v_shape[0]) < INT_CONV_MIN_WORK || rayon::current_num_threads() < 2
     {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:86740:
                 .getattr("kind")?
                 .extract::<String>()?;
             if kind == "i" || kind == "u" || kind == "b" {
-                let a_f64 =
-                    numpy.call_method1("asarray", (a_bound, numpy.getattr("float64")?))?;
+                let a_f64 = numpy.call_method1("asarray", (a_bound, numpy.getattr("float64")?))?;
                 if let Some(out) =
                     try_zerocopy_f64_isclose_array_scalar(py, &a_f64, b_bound, rtol, atol)?
                 {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:87006:
         };
         cond_bufs.push(cb);
     }
-    let cond_slices: Vec<&[pyo3::buffer::ReadOnlyCell<u8>]> =
-        match cond_bufs.iter().map(|b| b.as_slice(py)).collect::<Option<Vec<_>>>() {
-            Some(s) => s,
-            None => return Ok(None),
-        };
+    let cond_slices: Vec<&[pyo3::buffer::ReadOnlyCell<u8>]> = match cond_bufs
+        .iter()
+        .map(|b| b.as_slice(py))
+        .collect::<Option<Vec<_>>>()
+    {
+        Some(s) => s,
+        None => return Ok(None),
+    };
     let n: usize = x_shape.iter().product();
     let out_arr = numpy.call_method1("empty_like", (&x,))?;
     let Ok(out_buf) = PyBuffer::<f64>::get(&out_arr) else {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:87459:
     let days = v.div_euclid(u.per_day);
     let rem = v.rem_euclid(u.per_day);
     let (secofday, frac) = match u.level {
-        0 => (0, 0),          // D
-        1 => (rem * 3600, 0), // h: rem = hour-of-day
-        2 => (rem * 60, 0),   // m: rem = minute-of-day
-        3 => (rem, 0),        // s: rem = second-of-day
+        0 => (0, 0),                           // D
+        1 => (rem * 3600, 0),                  // h: rem = hour-of-day
+        2 => (rem * 60, 0),                    // m: rem = minute-of-day
+        3 => (rem, 0),                         // s: rem = second-of-day
         _ => (rem / u.subper, rem % u.subper), // ms/us/ns: rem = sub-second-of-day
     };
     let (y, mo, d) = civil_from_days(days);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:87552:
     match dtunit.kind {
         1 => {
             let lim = i64::MAX / 7;
-            if cin.par_iter().any(|&v| v != i64::MIN && (v > lim || v < -lim)) {
+            if cin
+                .par_iter()
+                .any(|&v| v != i64::MIN && (v > lim || v < -lim))
+            {
                 return Ok(None);
             }
         }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:89324:
             // kernels. (Their numpy fallback goes to numpy.strings == numpy.char, so defers stay correct.)
             if let Ok(fnp_strings) = m.getattr("strings") {
                 for op in [
-                    "ljust", "rjust", "center", "zfill", "expandtabs", "count", "find", "rfind", "index",
-                    "rindex", "isupper", "islower", "istitle", "mod", "decode",
+                    "ljust",
+                    "rjust",
+                    "center",
+                    "zfill",
+                    "expandtabs",
+                    "count",
+                    "find",
+                    "rfind",
+                    "index",
+                    "rindex",
+                    "isupper",
+                    "islower",
+                    "istitle",
+                    "mod",
+                    "decode",
                 ] {
                     if char_mod.hasattr(op)?
                         && let Ok(native) = fnp_strings.getattr(op)
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:90178:
 #[cfg(test)]
 mod tests {
     use super::{
-        PyFromPyFunc, PyVectorize, PythonNativeGemmOp, argwhere, bincount,
+        NarrowSetOp, PyFromPyFunc, PyVectorize, PythonNativeGemmOp, argwhere, bincount,
         build_numpy_array_from_ufunc, ceil_native, choose, compress, copysign, count_nonzero,
-        degrees_native, diag, diag_indices, diag_indices_from, diagflat, diagonal, digitize, extract,
-        extract_numeric_array, extract_precise_numeric_array, fill_diagonal, flatnonzero, flip,
-        fliplr, flipud, floor_native, fnp_python, frexp, hypot, indices, interp, isfinite_native,
-        isinf_native, isnan_native, isneginf_native, isposinf_native, ix_, ldexp, logaddexp,
-        logaddexp2, meshgrid, modf, nan_to_num, NarrowSetOp, narrow_bitmap_setop,
+        degrees_native, diag, diag_indices, diag_indices_from, diagflat, diagonal, digitize,
+        extract, extract_numeric_array, extract_precise_numeric_array, fill_diagonal, flatnonzero,
+        flip, fliplr, flipud, floor_native, fnp_python, frexp, hypot, indices, interp,
+        isfinite_native, isinf_native, isnan_native, isneginf_native, isposinf_native, ix_, ldexp,
+        logaddexp, logaddexp2, meshgrid, modf, nan_to_num, narrow_bitmap_setop, nextafter, place,
+        put, put_along_axis, putmask, python_native_gemm_f64_2d,
+        python_native_gemm_f64_2d_eligible, python_native_gemm_f64_2d_metadata_gate,
+        radians_native, ravel_multi_index, required_dict_item, rfftfreq, rint_native, searchsorted,
+        select, sign, signbit_native, sinc, solve_triangular, spacing, take, take_along_axis,
+        tensorinv, tensorsolve, trapezoid, trapz, tri, tril_indices, tril_indices_from,
+        triu_indices, triu_indices_from, trunc_native, unravel_index, where_py,
         wide_int_table_bounds,
-        nextafter, place, put, put_along_axis, putmask, python_native_gemm_f64_2d,
-        python_native_gemm_f64_2d_eligible, python_native_gemm_f64_2d_metadata_gate, radians_native,
-        ravel_multi_index, required_dict_item, rfftfreq, rint_native, searchsorted, select, sign,
-        signbit_native, sinc, solve_triangular, spacing, take, take_along_axis, tensorinv,
-        tensorsolve, trapezoid, trapz, tri, tril_indices, tril_indices_from, triu_indices,
-        triu_indices_from, trunc_native, unravel_index, where_py,
     };
     use fnp_dtype::{ArrayStorage, DType};
     use fnp_ufunc::UFuncArray;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:94697:
             int16_kwargs.set_item("dtype", numpy.getattr("int16")?)?;
             let (ours, theirs) = random_generator_pair(&random, &numpy_random, 275)?;
             assert_random_sample_matches_numpy(
-                &ours.call_method("integers", (-300_i64, 300_i64, 6_usize), Some(&int16_kwargs))?,
+                &ours.call_method(
+                    "integers",
+                    (-300_i64, 300_i64, 6_usize),
+                    Some(&int16_kwargs),
+                )?,
                 &theirs.call_method(
                     "integers",
                     (-300_i64, 300_i64, 6_usize),
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:98655:
             let np_where = numpy.getattr("where")?;
             let fnp_where = module.getattr("where")?;
             let array_equal = numpy.getattr("array_equal")?;
-            let rng = numpy.getattr("random")?.getattr("default_rng")?.call1((8_u64,))?;
+            let rng = numpy
+                .getattr("random")?
+                .getattr("default_rng")?
+                .call1((8_u64,))?;
             let v = rng.getattr("standard_normal")?.call1((4096_i64,))?;
             let cond = v.call_method1("__gt__", (0.0,))?;
             // (description, x, y) where x/y are arbitrary python objects.
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:98663:
             let i = |x: i64| -> Py<PyAny> { x.into_pyobject(py).unwrap().into_any().unbind() };
             let arr = || v.clone().unbind();
             let cases: Vec<(Py<PyAny>, Py<PyAny>)> = vec![
-                (arr(), f(0.0)),       // arr, float scalar -> f64
-                (f(5.0), arr()),       // float scalar, arr -> f64
-                (arr(), i(0)),         // arr, int scalar -> f64 (array dominates)
-                (f(1.0), f(0.0)),      // float, float -> f64
-                (f(1.0), i(0)),        // float, int -> f64
-                (i(1), i(0)),          // int, int -> int64 (MUST defer / match numpy)
-                (arr(), f(f64::NAN)),  // nan scalar
+                (arr(), f(0.0)),      // arr, float scalar -> f64
+                (f(5.0), arr()),      // float scalar, arr -> f64
+                (arr(), i(0)),        // arr, int scalar -> f64 (array dominates)
+                (f(1.0), f(0.0)),     // float, float -> f64
+                (f(1.0), i(0)),       // float, int -> f64
+                (i(1), i(0)),         // int, int -> int64 (MUST defer / match numpy)
+                (arr(), f(f64::NAN)), // nan scalar
             ];
             for (x, y) in cases {
                 let ours = fnp_where.call1((cond.clone(), x.bind(py), y.bind(py)))?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:98928:
                     h = h.wrapping_mul(0x100000001b3);
                 }
             }
-            assert_eq!(h, 0x9c25806588f3a004, "argwhere zero-copy golden FNV drifted");
+            assert_eq!(
+                h, 0x9c25806588f3a004,
+                "argwhere zero-copy golden FNV drifted"
+            );
             Ok(())
         });
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:101228:
             let a_stack = np_eye
                 .call1((3_i64,))?
                 .call_method1("__mul__", (2.0_f64,))?
-                .call_method1("__add__", (np_arange.call1((9_i64,))?.call_method1(
-                    "reshape",
-                    ((3_i64, 3_i64),),
-                )?.call_method1("__mul__", (0.1_f64,))?,))?;
+                .call_method1(
+                    "__add__",
+                    (np_arange
+                        .call1((9_i64,))?
+                        .call_method1("reshape", ((3_i64, 3_i64),))?
+                        .call_method1("__mul__", (0.1_f64,))?,),
+                )?;
             let a_batched2 = numpy
                 .getattr("stack")?
                 .call1((vec![a_stack.clone(), a_stack.clone()],))?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:105175:
             // (op, dtypes-to-check). Angle ops add narrow ints; sign adds float16;
             // sinc/nanmedian cover narrow floats.
             let cases: [(&str, &[&str]); 7] = [
-                ("degrees", &["float32", "float16", "int8", "int16", "uint8", "int32"]),
-                ("radians", &["float32", "float16", "int8", "int16", "uint8", "int32"]),
+                (
+                    "degrees",
+                    &["float32", "float16", "int8", "int16", "uint8", "int32"],
+                ),
+                (
+                    "radians",
+                    &["float32", "float16", "int8", "int16", "uint8", "int32"],
+                ),
                 ("deg2rad", &["float32", "float16", "int8", "int16", "uint8"]),
                 ("rad2deg", &["float32", "float16", "int8", "int16", "uint8"]),
                 ("sinc", &["float32", "float16"]),
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:109002:
                 h = h.wrapping_mul(0x100000001b3);
             }
             assert_eq!(
-                h, 0x2a9a4ef553c1d16c,
+                h,
+                0x2a9a4ef553c1d16c,
                 "extract f64 branchless golden FNV drifted (len={})",
                 actual_bytes.len()
             );
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:109229:
             let numpy = py.import("numpy")?;
             let np_select = numpy.getattr("select")?;
             let fnp_select = module.getattr("select")?;
-            let rng = numpy.getattr("random")?.getattr("default_rng")?.call1((4_u64,))?;
+            let rng = numpy
+                .getattr("random")?
+                .getattr("default_rng")?
+                .call1((4_u64,))?;
             let v = rng.getattr("standard_normal")?.call1((5000_i64,))?;
             // conditions and choices and the array default are all derived from v.
-            let lt = |t: f64| -> PyResult<Bound<'_, PyAny>> {
-                Ok(v.call_method1("__lt__", (t,))?)
-            };
-            let scaled = |s: f64| -> PyResult<Bound<'_, PyAny>> {
-                Ok(v.call_method1("__mul__", (s,))?)
-            };
+            let lt = |t: f64| -> PyResult<Bound<'_, PyAny>> { Ok(v.call_method1("__lt__", (t,))?) };
+            let scaled =
+                |s: f64| -> PyResult<Bound<'_, PyAny>> { Ok(v.call_method1("__mul__", (s,))?) };
             let array_equal = numpy.getattr("array_equal")?;
             for (conds, choices) in [
                 (vec![lt(-1.0)?, lt(1.0)?], vec![scaled(2.0)?, scaled(3.0)?]),
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:111680:
             let cases: &[(&str, Vec<i64>, Vec<i64>)] = &[
                 ("int8", vec![100, 100], vec![100, 100]), // 20000 wraps int8 -> 32
                 ("int16", vec![1, 2, 3], vec![4, 5, 6]),
-                ("int32", vec![1 << 30, 1 << 30, 7], vec![1 << 30, 1 << 30, 3]), // 2^61+21 -> int32 21
+                (
+                    "int32",
+                    vec![1 << 30, 1 << 30, 7],
+                    vec![1 << 30, 1 << 30, 3],
+                ), // 2^61+21 -> int32 21
                 ("uint8", vec![200, 200], vec![200, 200]),
                 ("uint32", vec![1 << 30, 1 << 30], vec![4, 4]),
             ];
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:118584:
             // float32 nanstd previously returned a float64 scalar.
             for narrow in ["float32", "float16"] {
                 let dtype_obj = numpy.getattr(narrow)?;
-                let base = numpy
-                    .getattr("array")?
-                    .call1((vec![1.0_f64, 2.0, f64::NAN, 4.0, 8.0],))?;
+                let base =
+                    numpy
+                        .getattr("array")?
+                        .call1((vec![1.0_f64, 2.0, f64::NAN, 4.0, 8.0],))?;
                 let narrow_arr = base.call_method1("astype", (dtype_obj,))?;
                 let ours_n = nanstd_fn.call1((narrow_arr.clone(),))?;
                 let theirs_n = numpy_nanstd.call1((narrow_arr.clone(),))?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:118740:
             // float32 nanvar previously returned a float64 scalar.
             for narrow in ["float32", "float16"] {
                 let dtype_obj = numpy.getattr(narrow)?;
-                let base = numpy
-                    .getattr("array")?
-                    .call1((vec![1.0_f64, 2.0, f64::NAN, 4.0, 8.0],))?;
+                let base =
+                    numpy
+                        .getattr("array")?
+                        .call1((vec![1.0_f64, 2.0, f64::NAN, 4.0, 8.0],))?;
                 let narrow_arr = base.call_method1("astype", (dtype_obj,))?;
                 let ours_n = nanvar_fn.call1((narrow_arr.clone(),))?;
                 let theirs_n = numpy_nanvar.call1((narrow_arr.clone(),))?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:120644:
             (-5_000, 4096, DType::I64),
         ];
         let lanes: &[(NarrowSetOp, fn(&BTreeSet<i64>, &BTreeSet<i64>) -> Vec<i64>)] = &[
-            (NarrowSetOp::Intersect, |a, b| a.intersection(b).copied().collect()),
+            (NarrowSetOp::Intersect, |a, b| {
+                a.intersection(b).copied().collect()
+            }),
             (NarrowSetOp::Union, |a, b| a.union(b).copied().collect()),
-            (NarrowSetOp::Setdiff, |a, b| a.difference(b).copied().collect()),
-            (NarrowSetOp::Setxor, |a, b| a.symmetric_difference(b).copied().collect()),
+            (NarrowSetOp::Setdiff, |a, b| {
+                a.difference(b).copied().collect()
+            }),
+            (NarrowSetOp::Setxor, |a, b| {
+                a.symmetric_difference(b).copied().collect()
+            }),
         ];
         for &(min, size, dt) in dims {
             let hi = min + size as i64;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:120786:
                     }),
                 )?;
                 for &(op_name, _op) in &ops {
-                    let ours = module.getattr(op_name)?.call1((left_arr.clone(), right_arr.clone()))?;
-                    let theirs = numpy.getattr(op_name)?.call1((left_arr.clone(), right_arr.clone()))?;
+                    let ours = module
+                        .getattr(op_name)?
+                        .call1((left_arr.clone(), right_arr.clone()))?;
+                    let theirs = numpy
+                        .getattr(op_name)?
+                        .call1((left_arr.clone(), right_arr.clone()))?;
                     assert_array_matches_numpy(&ours, &theirs)?;
                     proof_bytes.extend_from_slice(case.as_bytes());
                     proof_bytes.push(0);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:120794:
                     proof_bytes.extend_from_slice(op_name.as_bytes());
                     proof_bytes.push(0);
-                    proof_bytes.extend_from_slice(ours.getattr("dtype")?.str()?.to_string().as_bytes());
+                    proof_bytes
+                        .extend_from_slice(ours.getattr("dtype")?.str()?.to_string().as_bytes());
                     proof_bytes.push(0);
-                    proof_bytes.extend_from_slice(ours.getattr("shape")?.str()?.to_string().as_bytes());
+                    proof_bytes
+                        .extend_from_slice(ours.getattr("shape")?.str()?.to_string().as_bytes());
                     proof_bytes.push(0);
                     let raw: Vec<u8> = ours.call_method0("tobytes")?.extract()?;
                     proof_bytes.extend_from_slice(&raw);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:120805:

             let digest = py_sha256_hex(py, &proof_bytes)?;
             assert_eq!(
-                digest,
-                "acd30f91b7d4d0a28e13171ea131ce4f34cd24693be4b30372fd7329f4e05f0b",
+                digest, "acd30f91b7d4d0a28e13171ea131ce4f34cd24693be4b30372fd7329f4e05f0b",
                 "wide-int range bitmap set-op golden sha256 changed",
             );
             Ok(())
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:120879:
                     proof_bytes.push(0);
                     proof_bytes.extend_from_slice(op_name.as_bytes());
                     proof_bytes.push(0);
-                    proof_bytes.extend_from_slice(
-                        ours.getattr("dtype")?.str()?.to_string().as_bytes(),
-                    );
+                    proof_bytes
+                        .extend_from_slice(ours.getattr("dtype")?.str()?.to_string().as_bytes());
                     proof_bytes.push(0);
-                    proof_bytes.extend_from_slice(
-                        ours.getattr("shape")?.str()?.to_string().as_bytes(),
-                    );
+                    proof_bytes
+                        .extend_from_slice(ours.getattr("shape")?.str()?.to_string().as_bytes());
                     proof_bytes.push(0);
                     let raw: Vec<u8> = ours.call_method0("tobytes")?.extract()?;
                     proof_bytes.extend_from_slice(&raw);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:120895:

             let digest = py_sha256_hex(py, &proof_bytes)?;
             assert_eq!(
-                digest,
-                "ec0d35d19fdfc33accacb0c9a96cfd020ccd007989c970f193948b449a5a1e4b",
+                digest, "ec0d35d19fdfc33accacb0c9a96cfd020ccd007989c970f193948b449a5a1e4b",
                 "float set-op numpy passthrough golden sha256 changed",
             );
             Ok(())
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:120936:
             )?;

             let two_d_left = array.call(
-                (
-                    vec![
-                        vec![0.0_f32, 0.125, 0.25],
-                        vec![0.25_f32, 8.0, 9.125],
-                    ],
-                ),
+                (vec![vec![0.0_f32, 0.125, 0.25], vec![0.25_f32, 8.0, 9.125]],),
                 Some(&dtype_kwargs()?),
             )?;
             let two_d_right = array.call(
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:120948:
-                (
-                    vec![
-                        vec![0.125_f32, 1.0, 8.0],
-                        vec![5.5_f32, 9.125, 10.0],
-                    ],
-                ),
+                (vec![vec![0.125_f32, 1.0, 8.0], vec![5.5_f32, 9.125, 10.0]],),
                 Some(&dtype_kwargs()?),
             )?;
             assert_array_matches_numpy(
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:120958:
                 &numpy_setxor1d.call1((two_d_left.clone(), two_d_right.clone()))?,
             )?;

-            let arbitrary_left = array.call(
-                (vec![0.1_f32, 0.2, 0.2],),
-                Some(&dtype_kwargs()?),
-            )?;
-            let arbitrary_right = array.call(
-                (vec![0.2_f32, 0.3],),
-                Some(&dtype_kwargs()?),
-            )?;
+            let arbitrary_left = array.call((vec![0.1_f32, 0.2, 0.2],), Some(&dtype_kwargs()?))?;
+            let arbitrary_right = array.call((vec![0.2_f32, 0.3],), Some(&dtype_kwargs()?))?;
             assert_array_matches_numpy(
                 &setxor1d_fn.call1((arbitrary_left.clone(), arbitrary_right.clone()))?,
                 &numpy_setxor1d.call1((arbitrary_left.clone(), arbitrary_right.clone()))?,
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:128602:

             // Rectangular batched (5, 4, 3): exercises the parallel batch_svd path on
             // tall lanes; output shape must be (5, 3) = batch dims + min(M,N).
-            let rng = numpy.getattr("random")?.getattr("default_rng")?.call1((7_u64,))?;
-            let rect = rng.getattr("standard_normal")?.call1(((5_i64, 4_i64, 3_i64),))?;
+            let rng = numpy
+                .getattr("random")?
+                .getattr("default_rng")?
+                .call1((7_u64,))?;
+            let rect = rng
+                .getattr("standard_normal")?
+                .call1(((5_i64, 4_i64, 3_i64),))?;
             let ours_r = sv_fn.call1((rect.clone(),))?;
             let theirs_r = numpy_sv.call1((rect.clone(),))?;
             let ok_r: bool = allclose.call1((&ours_r, &theirs_r))?.extract()?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:128612:
             assert_eq!(r_shape, vec![5, 3], "svdvals(5,4,3) shape must be (5,3)");

             // 4-D batched (2, 3, 6, 6): nested batch dims through the same path.
-            let big = rng.getattr("standard_normal")?.call1(((2_i64, 3_i64, 6_i64, 6_i64),))?;
+            let big = rng
+                .getattr("standard_normal")?
+                .call1(((2_i64, 3_i64, 6_i64, 6_i64),))?;
             let ours_g = sv_fn.call1((big.clone(),))?;
             let theirs_g = numpy_sv.call1((big.clone(),))?;
             let ok_g: bool = allclose.call1((&ours_g, &theirs_g))?.extract()?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:128619:
             assert!(ok_g, "svdvals 4-D batched mismatch");
             let g_shape: Vec<usize> = ours_g.getattr("shape")?.extract()?;
-            assert_eq!(g_shape, vec![2, 3, 6], "svdvals(2,3,6,6) shape must be (2,3,6)");
+            assert_eq!(
+                g_shape,
+                vec![2, 3, 6],
+                "svdvals(2,3,6,6) shape must be (2,3,6)"
+            );

             // Complex input stays on the numpy fallback path but must still
             // match exactly.
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:128682:
             // Integer lists must defer to numpy (int result dtype) and still work.
             let ai = PyList::new(py, [1_i64, 2, 3])?;
             let vi = PyList::new(py, [0_i64, 1, 2])?;
-            let ours_i = module.getattr("convolve")?.call((ai.clone(), vi.clone(), "full"), None)?;
+            let ours_i = module
+                .getattr("convolve")?
+                .call((ai.clone(), vi.clone(), "full"), None)?;
             let theirs_i = numpy.getattr("convolve")?.call1((ai.clone(), vi.clone()))?;
             let eq_i: bool = numpy
                 .getattr("array_equal")?
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:128706:

             // choose must accept array_like (Python list) choices, not just ndarrays
             // — the int fast path used to read items[0].dtype unguarded.
-            let idx = numpy.getattr("array")?.call1((PyList::new(py, [0_i64, 1, 2, 0])?,))?;
+            let idx = numpy
+                .getattr("array")?
+                .call1((PyList::new(py, [0_i64, 1, 2, 0])?,))?;
             let choices = PyList::new(
                 py,
                 [
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:128715:
                     PyList::new(py, [30_i64, 31, 32, 33])?,
                 ],
             )?;
-            let ours_ch = module.getattr("choose")?.call1((idx.clone(), choices.clone()))?;
-            let theirs_ch = numpy.getattr("choose")?.call1((idx.clone(), choices.clone()))?;
+            let ours_ch = module
+                .getattr("choose")?
+                .call1((idx.clone(), choices.clone()))?;
+            let theirs_ch = numpy
+                .getattr("choose")?
+                .call1((idx.clone(), choices.clone()))?;
             let okch: bool = numpy
                 .getattr("array_equal")?
                 .call1((&ours_ch, &theirs_ch))?
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:128737:
             fnp_python(&module)?;
             let numpy = py.import("numpy")?;
             let allclose = numpy.getattr("allclose")?;
-            let rng = numpy.getattr("random")?.getattr("default_rng")?.call1((9_u64,))?;
+            let rng = numpy
+                .getattr("random")?
+                .getattr("default_rng")?
+                .call1((9_u64,))?;
             let x = rng.getattr("standard_normal")?.call1((300_i64,))?;
             let y = rng.getattr("standard_normal")?.call1((300_i64,))?;

Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:128744:
             // histogram2d: numpy returns float64 counts even unweighted.
-            let h2 = module.getattr("histogram2d")?.call((x.clone(), y.clone(), 7_i64), None)?;
+            let h2 = module
+                .getattr("histogram2d")?
+                .call((x.clone(), y.clone(), 7_i64), None)?;
             let our_h = h2.get_item(0)?;
             let np_h = numpy
                 .getattr("histogram2d")?
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:128757:
             let sample = numpy
                 .getattr("column_stack")?
                 .call1((PyList::new(py, [&x, &y])?,))?;
-            let hdd = module.getattr("histogramdd")?.call((sample.clone(), 6_i64), None)?;
+            let hdd = module
+                .getattr("histogramdd")?
+                .call((sample.clone(), 6_i64), None)?;
             let our_hdd = hdd.get_item(0)?;
             let np_hdd = numpy
                 .getattr("histogramdd")?
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:128784:
             let numpy = py.import("numpy")?;
             let numpy_cond = numpy.getattr("linalg")?.getattr("cond")?;
             let allclose = numpy.getattr("allclose")?;
-            let rng = numpy.getattr("random")?.getattr("default_rng")?.call1((11_u64,))?;
+            let rng = numpy
+                .getattr("random")?
+                .getattr("default_rng")?
+                .call1((11_u64,))?;

             // Batched square / rectangular / 4-D, default p (None==2-norm).
             for shp in [
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:128801:
             }

             // Explicit p=2 and p=-2 selectors must also hit the native path.
-            let a = rng.getattr("standard_normal")?.call1((vec![32_i64, 8, 8],))?;
+            let a = rng
+                .getattr("standard_normal")?
+                .call1((vec![32_i64, 8, 8],))?;
             for p in [2_i64, -2] {
                 let ours = cond_fn.call1((a.clone(), p))?;
                 let theirs = numpy_cond.call1((a.clone(), p))?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:128810:
             }

             // Non-2-norm p stays on numpy passthrough (p='fro', square only).
-            let sq = rng.getattr("standard_normal")?.call1((vec![16_i64, 5, 5],))?;
+            let sq = rng
+                .getattr("standard_normal")?
+                .call1((vec![16_i64, 5, 5],))?;
             let ours_f = cond_fn.call1((sq.clone(), "fro"))?;
             let theirs_f = numpy_cond.call1((sq.clone(), "fro"))?;
             let ok_f: bool = allclose.call1((&ours_f, &theirs_f))?.extract()?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:128822:
             let ours_z = cond_fn.call1((zeros.clone(),))?;
             let theirs_z = numpy_cond.call1((zeros.clone(),))?;
             let isinf = numpy.getattr("isinf")?;
-            let all_inf_ours: bool = isinf
-                .call1((&ours_z,))?
-                .call_method0("all")?
-                .extract()?;
+            let all_inf_ours: bool = isinf.call1((&ours_z,))?.call_method0("all")?.extract()?;
             assert!(all_inf_ours, "cond(zeros) must be +Inf, got non-inf");
             let eq_z: bool = numpy
                 .getattr("array_equal")?
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:128849:
             let numpy = py.import("numpy")?;
             let numpy_pinv = numpy.getattr("linalg")?.getattr("pinv")?;
             let allclose = numpy.getattr("allclose")?;
-            let rng = numpy.getattr("random")?.getattr("default_rng")?.call1((23_u64,))?;
+            let rng = numpy
+                .getattr("random")?
+                .getattr("default_rng")?
+                .call1((23_u64,))?;

             // Square / tall / wide / 4-D batched. Output is (..., N, M).
             for (shp, out) in [
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:128869:
             }

             // hermitian=True batched stays on the numpy passthrough but must match.
-            let sq = rng.getattr("standard_normal")?.call1((vec![8_i64, 5, 5],))?;
+            let sq = rng
+                .getattr("standard_normal")?
+                .call1((vec![8_i64, 5, 5],))?;
             let kw = PyDict::new(py);
             kw.set_item("hermitian", true)?;
             let ours_h = pinv_fn.call((sq.clone(),), Some(&kw))?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:128893:
             let numpy = py.import("numpy")?;
             let numpy_mn = numpy.getattr("linalg")?.getattr("matrix_norm")?;
             let allclose = numpy.getattr("allclose")?;
-            let rng = numpy.getattr("random")?.getattr("default_rng")?.call1((41_u64,))?;
+            let rng = numpy
+                .getattr("random")?
+                .getattr("default_rng")?
+                .call1((41_u64,))?;

             // SVD-derived orders 2 / -2 / 'nuc' across square / tall / wide / 4-D.
             for shp in [
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:128917:
             }

             // keepdims=True must keep the reduced last two axes as (1, 1).
-            let a = rng.getattr("standard_normal")?.call1((vec![4_i64, 3, 5, 5],))?;
+            let a = rng
+                .getattr("standard_normal")?
+                .call1((vec![4_i64, 3, 5, 5],))?;
             let kw = PyDict::new(py);
             kw.set_item("ord", 2_i64)?;
             kw.set_item("keepdims", true)?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:128956:
             let numpy = py.import("numpy")?;
             let numpy_norm = numpy.getattr("linalg")?.getattr("norm")?;
             let allclose = numpy.getattr("allclose")?;
-            let rng = numpy.getattr("random")?.getattr("default_rng")?.call1((57_u64,))?;
+            let rng = numpy
+                .getattr("random")?
+                .getattr("default_rng")?
+                .call1((57_u64,))?;

             // ord 2 / -2 / 'nuc' over the trailing two axes, both axis orders.
             for shp in [vec![32_i64, 6, 6], vec![16, 7, 4], vec![2, 3, 5, 5]] {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:128978:
             }

             // keepdims preserves the reduced trailing axes as (1, 1).
-            let a = rng.getattr("standard_normal")?.call1((vec![4_i64, 3, 5, 5],))?;
+            let a = rng
+                .getattr("standard_normal")?
+                .call1((vec![4_i64, 3, 5, 5],))?;
             let kw = PyDict::new(py);
             kw.set_item("ord", 2_i64)?;
             kw.set_item("axis", (-2_i64, -1_i64))?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:131902:

             // (subscripts, operand (n, shape) list)
             let cases: &[EinsumCase<'_>] = &[
-                ("ij,jk,kl->il", &[(20, &[4, 5]), (30, &[5, 6]), (18, &[6, 3])]),
+                (
+                    "ij,jk,kl->il",
+                    &[(20, &[4, 5]), (30, &[5, 6]), (18, &[6, 3])],
+                ),
                 ("ij,jk,ki->", &[(35, &[5, 7]), (42, &[7, 6]), (30, &[6, 5])]),
-                ("ijk,jkl,kl->il", &[(24, &[2, 3, 4]), (24, &[3, 4, 2]), (8, &[4, 2])]),
-                ("bij,bjk,bkl->bil", &[(24, &[2, 3, 4]), (16, &[2, 4, 2]), (12, &[2, 2, 3])]),
-                ("ij,jk,kl,lm->im", &[(6, &[2, 3]), (6, &[3, 2]), (8, &[2, 4]), (8, &[4, 2])]),
+                (
+                    "ijk,jkl,kl->il",
+                    &[(24, &[2, 3, 4]), (24, &[3, 4, 2]), (8, &[4, 2])],
+                ),
+                (
+                    "bij,bjk,bkl->bil",
+                    &[(24, &[2, 3, 4]), (16, &[2, 4, 2]), (12, &[2, 2, 3])],
+                ),
+                (
+                    "ij,jk,kl,lm->im",
+                    &[(6, &[2, 3]), (6, &[3, 2]), (8, &[2, 4]), (8, &[4, 2])],
+                ),
                 ("i,ij,j->", &[(5, &[5]), (30, &[5, 6]), (6, &[6])]),
                 ("a,abc,c->b", &[(4, &[4]), (60, &[4, 5, 3]), (3, &[3])]),
-                ("bi,bij,bj->b", &[(20, &[4, 5]), (120, &[4, 5, 6]), (24, &[4, 6])]),
+                (
+                    "bi,bij,bj->b",
+                    &[(20, &[4, 5]), (120, &[4, 5, 6]), (24, &[4, 6])],
+                ),
             ];
             for (subs, ops) in cases {
-                let built: Vec<Bound<'_, PyAny>> =
-                    ops.iter().map(|(n, s)| mk(*n, s)).collect::<PyResult<_>>()?;
+                let built: Vec<Bound<'_, PyAny>> = ops
+                    .iter()
+                    .map(|(n, s)| mk(*n, s))
+                    .collect::<PyResult<_>>()?;
                 let subs_obj = pyo3::types::PyString::new(py, subs).into_any();
                 let mut args: Vec<Bound<'_, PyAny>> = vec![subs_obj];
                 for b in &built {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:131921:
                 }
                 let ours = ours_fn.call1(PyTuple::new(py, &args)?)?;
                 let theirs = np_einsum.call1(PyTuple::new(py, &args)?)?;
-                let close: bool = allclose
-                    .call1((ours, theirs))?
-                    .extract()?;
+                let close: bool = allclose.call1((ours, theirs))?.extract()?;
                 assert!(close, "einsum '{subs}' diverges from numpy beyond allclose");
             }
             Ok(())
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:131963:

             // (subscripts, n, shape) — fast-path cases AND deferred cases (same harness).
             let cases: &[(&str, i64, &[i64])] = &[
-                ("ij,ij->", 4225, &[65, 65]),  // full contraction (fast path)
-                ("i,i->", 9000, &[9000]),      // 1-D inner product (fast path)
-                ("abc,abc->", 5040, &[14, 12, 30]), // 3-D full contraction (fast path)
-                ("ij,ij->i", 4002, &[58, 69]), // 2-D partial keep-row (fast path)
-                ("ij,ij->j", 4002, &[58, 69]), // 2-D partial keep-col (fast path)
-                ("ijk,ijk->i", 1680, &[8, 14, 15]), // N-D prefix reduce keep-1 (fast path)
-                ("ijk,ijk->ij", 1680, &[8, 14, 15]), // N-D prefix reduce keep-2 (fast path)
+                ("ij,ij->", 4225, &[65, 65]),            // full contraction (fast path)
+                ("i,i->", 9000, &[9000]),                // 1-D inner product (fast path)
+                ("abc,abc->", 5040, &[14, 12, 30]),      // 3-D full contraction (fast path)
+                ("ij,ij->i", 4002, &[58, 69]),           // 2-D partial keep-row (fast path)
+                ("ij,ij->j", 4002, &[58, 69]),           // 2-D partial keep-col (fast path)
+                ("ijk,ijk->i", 1680, &[8, 14, 15]),      // N-D prefix reduce keep-1 (fast path)
+                ("ijk,ijk->ij", 1680, &[8, 14, 15]),     // N-D prefix reduce keep-2 (fast path)
                 ("ijkl,ijkl->ij", 1680, &[4, 6, 7, 10]), // 4-D prefix reduce (fast path)
-                ("i,j->ij", 60, &[60]),        // outer product (fast path)
-                ("ij,ji->", 4225, &[65, 65]),  // transpose contraction (deferred)
-                ("ij,ji->i", 4225, &[65, 65]), // transposed partial (deferred)
-                ("i,j->ji", 60, &[60]),        // transposed outer (deferred)
-                ("ii,ii->", 2500, &[50, 50]),  // repeated labels (deferred)
+                ("i,j->ij", 60, &[60]),                  // outer product (fast path)
+                ("ij,ji->", 4225, &[65, 65]),            // transpose contraction (deferred)
+                ("ij,ji->i", 4225, &[65, 65]),           // transposed partial (deferred)
+                ("i,j->ji", 60, &[60]),                  // transposed outer (deferred)
+                ("ii,ii->", 2500, &[50, 50]),            // repeated labels (deferred)
             ];
             for (subs, n, shape) in cases {
                 let a = mk(*n, shape)?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:136532:

             for dt in ["float64", "float32"] {
                 let mk = |vals: Vec<f64>| -> PyResult<Py<PyAny>> {
-                    Ok(numpy
-                        .getattr("array")?
-                        .call1((vals, dt))?
-                        .unbind())
+                    Ok(numpy.getattr("array")?.call1((vals, dt))?.unbind())
                 };
                 let nan = f64::NAN;
                 let inf = f64::INFINITY;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:136554:
                     for inv in [false, true] {
                         let kw = PyDict::new(py);
                         kw.set_item("invert", inv)?;
-                        let fnp_r =
-                            asarray.call1((fnp_isin.call((a.bind(py), b.bind(py)), Some(&kw))?,))?;
+                        let fnp_r = asarray
+                            .call1((fnp_isin.call((a.bind(py), b.bind(py)), Some(&kw))?,))?;
                         let np_r = np_isin.call((a.bind(py), b.bind(py)), Some(&kw))?;
                         assert!(
                             array_equal.call1((&fnp_r, &np_r))?.extract::<bool>()?,
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:136562:
                             "isin {dt} mismatch a={a_vals:?} b={b_vals:?} invert={inv}"
                         );
-                        let kind: String =
-                            fnp_r.getattr("dtype")?.getattr("kind")?.extract()?;
+                        let kind: String = fnp_r.getattr("dtype")?.getattr("kind")?.extract()?;
                         assert_eq!(kind, "b", "isin {dt} output not bool");
                     }
                 }
Diff in /data/projects/franken_numpy/crates/fnp-python/tests/conformance_histogram_bincount.rs:281:
     ];

     for (label, setup, call_expr) in cases {
-        let numpy_result =
-            numpy_oracle(&numpy_outcome_script("np.histogram_bin_edges", setup, call_expr))?;
+        let numpy_result = numpy_oracle(&numpy_outcome_script(
+            "np.histogram_bin_edges",
+            setup,
+            call_expr,
+        ))?;
         let rust_result =
             numpy_oracle(&fnp_outcome_script("histogram_bin_edges", setup, call_expr))?;

Diff in /data/projects/franken_numpy/crates/fnp-python/tests/conformance_histogram_bincount.rs:314:
             "",
             "op([0.5, 1.5, 3.5], [4, 3, 2, 1], right=True)",
         ),
-        (
-            "nonmonotonic bins error type",
-            "",
-            "op([1, 2], [0, 2, 1])",
-        ),
+        ("nonmonotonic bins error type", "", "op([1, 2], [0, 2, 1])"),
     ];

     for (label, setup, call_expr) in cases {
Diff in /data/projects/franken_numpy/crates/fnp-python/tests/conformance_integer_overflow.rs:54:
     let g_bytes = g.call_method0("tobytes")?.extract::<Vec<u8>>()?;
     let x_bytes = x.call_method0("tobytes")?.extract::<Vec<u8>>()?;
     assert_eq!(
-        g_bytes, x_bytes,
+        g_bytes,
+        x_bytes,
         "{label}: bytes mismatch (fnp={:?} numpy={:?})",
         g.call_method0("tolist")?.str()?.to_string(),
         x.call_method0("tolist")?.str()?.to_string()
Diff in /data/projects/franken_numpy/crates/fnp-python/tests/conformance_integer_overflow.rs:126:
         }

         // ── cross: preserves the promoted integer dtype with wraparound ──
-        for dt in ["int8", "int16", "int32", "int64", "uint8", "uint16", "uint32", "uint64"] {
+        for dt in [
+            "int8", "int16", "int32", "int64", "uint8", "uint16", "uint32", "uint64",
+        ] {
             // Values fit int8 (so they construct for every dtype) but their cross
             // component products (e.g. 120*110, 100*90) overflow int8/int16 → wrap.
             let a = np_int_array(py, &numpy, &[120, 100, 3], &[3], dt)?;
Diff in /data/projects/franken_numpy/crates/fnp-python/tests/conformance_interp_trapz.rs:103:
             "xp = np.array([0.0, 2.0, 4.0])\nfp = np.array([0.0, 20.0, 40.0])",
             "op((1.0, 3.0), xp, fp)",
         ),
-        (
-            "missing fp error type",
-            "",
-            "op([0.0], [0.0])",
-        ),
+        ("missing fp error type", "", "op([0.0], [0.0])"),
         (
             "xp fp length mismatch error type",
             "",
Diff in /data/projects/franken_numpy/crates/fnp-python/tests/conformance_mean.rs:136:
 #[test]
 fn mean_python_container_keyword_outcomes_match_numpy() -> Result<(), String> {
     let cases = [
+        ("list input scalar", "result = MODULE.mean([1, 2, 3])"),
         (
-            "list input scalar",
-            "result = MODULE.mean([1, 2, 3])",
-        ),
-        (
             "tuple input axis keepdims",
             "result = MODULE.mean(((1, 2, 3), (4, 5, 6)), axis=1, keepdims=True)",
         ),
Diff in /data/projects/franken_numpy/crates/fnp-python/tests/conformance_mean.rs:160:
             "out = np.empty((2,), dtype=np.float64)
 result = MODULE.mean(np.array([[1.0, 2.0], [3.0, 4.0]]), axis=0, out=out)",
         ),
-        (
-            "axis error type",
-            "result = MODULE.mean([1, 2, 3], axis=2)",
-        ),
+        ("axis error type", "result = MODULE.mean([1, 2, 3], axis=2)"),
     ];

     for (name, body) in cases {
Diff in /data/projects/franken_numpy/crates/fnp-python/tests/conformance_setops.rs:126:
             let ours = module.getattr(op)?.call1((&a, &b))?;
             let theirs = numpy.getattr(op)?.call1((&a, &b))?;
             let equal: bool = array_equal.call1((&ours, &theirs))?.extract()?;
-            assert!(
-                equal,
-                "complex128 sorted-merge {op} diverged from numpy"
-            );
+            assert!(equal, "complex128 sorted-merge {op} diverged from numpy");
             let dtype = ours.getattr("dtype")?.str()?.to_string();
             assert_eq!(dtype, "complex128");
         }
Diff in /data/projects/franken_numpy/crates/fnp-python/tests/conformance_setops.rs:894:
                 let o = ours.get_item(k)?;
                 let t = theirs.get_item(k)?;
                 let equal: bool = array_equal.call1((&o, &t))?.extract()?;
-                assert!(equal, "packed Latin-1 {name} unique_full field {k} diverged from numpy");
+                assert!(
+                    equal,
+                    "packed Latin-1 {name} unique_full field {k} diverged from numpy"
+                );
             }
         }
         Ok(())
Diff in /data/projects/franken_numpy/crates/fnp-python/tests/conformance_setops.rs:928:
             .ok_or_else(|| pyo3::exceptions::PyAssertionError::new_err("missing b"))?;
         let ours = module.getattr("setxor1d")?.call1((&a, &b))?;
         let theirs = numpy.getattr("setxor1d")?.call1((&a, &b))?;
-        let equal: bool = numpy.getattr("array_equal")?.call1((&ours, &theirs))?.extract()?;
-        assert!(equal, "dense integral complex128 setxor1d diverged from numpy");
+        let equal: bool = numpy
+            .getattr("array_equal")?
+            .call1((&ours, &theirs))?
+            .extract()?;
+        assert!(
+            equal,
+            "dense integral complex128 setxor1d diverged from numpy"
+        );
         let dtype = ours.getattr("dtype")?.str()?.to_string();
         assert_eq!(dtype, "complex128");
         Ok(())
Diff in /data/projects/franken_numpy/crates/fnp-python/tests/conformance_sorting.rs:469:
             let ours = module.getattr("argsort")?.call((&arr,), Some(&kw))?;
             let theirs = numpy.getattr("argsort")?.call((&arr,), Some(&kw))?;
             let equal: bool = array_equal.call1((&ours, &theirs))?.extract()?;
-            assert!(equal, "packed Latin-1 {name} stable argsort diverged from numpy");
+            assert!(
+                equal,
+                "packed Latin-1 {name} stable argsort diverged from numpy"
+            );
         }
         Ok(())
     });
Diff in /data/projects/franken_numpy/crates/fnp-python/tests/conformance_statistics.rs:287:
 #[test]
 fn average_python_container_keyword_outcomes_match_numpy() -> Result<(), String> {
     let cases = [
-        (
-            "list input scalar",
-            "result = MODULE.average([1, 2, 3, 4])",
-        ),
+        ("list input scalar", "result = MODULE.average([1, 2, 3, 4])"),
         (
             "tuple input axis weights returned",
             "result = MODULE.average(
Diff in /data/projects/franken_numpy/crates/fnp-python/tests/conformance_std.rs:138:
 #[test]
 fn std_python_container_keyword_outcomes_match_numpy() -> Result<(), String> {
     let cases = [
+        ("list input scalar", "result = MODULE.std([1, 2, 3])"),
         (
-            "list input scalar",
-            "result = MODULE.std([1, 2, 3])",
-        ),
-        (
             "tuple input axis keepdims",
             "result = MODULE.std(((1, 2, 3), (4, 5, 6)), axis=1, keepdims=True)",
         ),
Diff in /data/projects/franken_numpy/crates/fnp-python/tests/conformance_std.rs:166:
             "out = np.empty((2,), dtype=np.float64)
 result = MODULE.std(np.array([[1.0, 2.0], [3.0, 4.0]]), axis=0, out=out)",
         ),
-        (
-            "axis error type",
-            "result = MODULE.std([1, 2, 3], axis=2)",
-        ),
+        ("axis error type", "result = MODULE.std([1, 2, 3], axis=2)"),
     ];

     for (name, body) in cases {
Diff in /data/projects/franken_numpy/crates/fnp-python/tests/conformance_trim_zeros.rs:18:
         .stdin
         .take()
         .ok_or_else(|| format!("python3 stdin should be available\nScript: {script}"))?;
-    stdin
-        .write_all(script.as_bytes())
-        .map_err(|error| format!("failed to write Python oracle script: {error}\nScript: {script}"))?;
+    stdin.write_all(script.as_bytes()).map_err(|error| {
+        format!("failed to write Python oracle script: {error}\nScript: {script}")
+    })?;
     drop(stdin);

-    let output = child
-        .wait_with_output()
-        .map_err(|error| format!("failed to read Python oracle output: {error}\nScript: {script}"))?;
+    let output = child.wait_with_output().map_err(|error| {
+        format!("failed to read Python oracle output: {error}\nScript: {script}")
+    })?;
     if !output.status.success() {
         let stderr = String::from_utf8_lossy(&output.stderr);
         return Err(format!("NumPy oracle failed: {stderr}\nScript: {script}"));
Diff in /data/projects/franken_numpy/crates/fnp-python/tests/conformance_ufunc_edge.rs:2571:
 }

 #[test]
-fn f16_unary_floor_ceil_trunc_rint_parallel_full_domain_bit_exact_matches_numpy() -> Result<(), String>
-{
+fn f16_unary_floor_ceil_trunc_rint_parallel_full_domain_bit_exact_matches_numpy()
+-> Result<(), String> {
     // numpy has no native f16 ALU: floor/ceil/trunc/rint widen->f32->op->narrow (compute-bound).
     // The native parallel widen path must be byte-identical to numpy over EVERY f16 bit pattern
     // (all 65536, incl. every nan/inf/subnormal/-0.0), tiled past the 1<<20 gate to engage the
Diff in /data/projects/franken_numpy/crates/fnp-python/tests/conformance_var.rs:138:
 #[test]
 fn var_python_container_keyword_outcomes_match_numpy() -> Result<(), String> {
     let cases = [
+        ("list input scalar", "result = MODULE.var([1, 2, 3])"),
         (
-            "list input scalar",
-            "result = MODULE.var([1, 2, 3])",
-        ),
-        (
             "tuple input axis keepdims",
             "result = MODULE.var(((1, 2, 3), (4, 5, 6)), axis=1, keepdims=True)",
         ),
Diff in /data/projects/franken_numpy/crates/fnp-python/tests/conformance_var.rs:166:
             "out = np.empty((2,), dtype=np.float64)
 result = MODULE.var(np.array([[1.0, 2.0], [3.0, 4.0]]), axis=0, out=out)",
         ),
-        (
-            "axis error type",
-            "result = MODULE.var([1, 2, 3], axis=2)",
-        ),
+        ("axis error type", "result = MODULE.var([1, 2, 3], axis=2)"),
     ];

     for (name, body) in cases {
