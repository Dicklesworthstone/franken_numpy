Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:40:
     FromPyFuncReduceOptions, GridSpec, IntegerSidecar, MAError, MaskedArray, UFuncArray, UnaryOp,
     arctan2 as ufunc_arctan2, bitwise_and as ufunc_bitwise_and,
     bitwise_count as ufunc_bitwise_count, bitwise_or as ufunc_bitwise_or,
[31m-    bitwise_xor as ufunc_bitwise_xor, divide as ufunc_divide,
[m[31m-    divmod_arrays as ufunc_divmod, equal as ufunc_equal, fmax as ufunc_fmax,
[m[31m-    fmin as ufunc_fmin, frexp as ufunc_frexp,
[m[31m-    greater as ufunc_greater, greater_equal as ufunc_greater_equal,
[m[31m-    hypot as ufunc_hypot, isneginf as ufunc_isneginf,
[m[31m-    isposinf as ufunc_isposinf, left_shift as ufunc_left_shift,
[m[32m+    bitwise_xor as ufunc_bitwise_xor, divide as ufunc_divide, divmod_arrays as ufunc_divmod,
[m[32m+    equal as ufunc_equal, fmax as ufunc_fmax, fmin as ufunc_fmin, frexp as ufunc_frexp,
[m[32m+    greater as ufunc_greater, greater_equal as ufunc_greater_equal, hypot as ufunc_hypot,
[m[32m+    isneginf as ufunc_isneginf, isposinf as ufunc_isposinf, left_shift as ufunc_left_shift,
[m     less as ufunc_less, less_equal as ufunc_less_equal, logaddexp as ufunc_logaddexp,
     logaddexp2 as ufunc_logaddexp2, logical_and as ufunc_logical_and,
     logical_not as ufunc_logical_not, logical_or as ufunc_logical_or,
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:4271:
         // layout) and reconstruct via f16::from_bits. The old astype->tolist->Vec<f32>
         // path boxed one Python float per element — ~2500x slower than numpy on a 4M abs.
         DType::F16 => ArrayStorage::F16(
[31m-            numpy_cast_contiguous_to_vec::<u16>(py, &flat.call_method1("view", ("uint16",))?, "uint16")?
[m[31m-                .into_iter()
[m[31m-                .map(f16::from_bits)
[m[31m-                .collect(),
[m[32m+            numpy_cast_contiguous_to_vec::<u16>(
[m[32m+                py,
[m[32m+                &flat.call_method1("view", ("uint16",))?,
[m[32m+                "uint16",
[m[32m+            )?
[m[32m+            .into_iter()
[m[32m+            .map(f16::from_bits)
[m[32m+            .collect(),
[m         ),
         DType::F32 => ArrayStorage::F32(numpy_cast_contiguous_to_vec::<f32>(py, &flat, "float32")?),
         DType::F64 => ArrayStorage::F64(numpy_cast_contiguous_to_vec::<f64>(py, &flat, "float64")?),
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:5257:
                 // all-False mask to nomask, matching numpy exactly.
                 let kwargs = PyDict::new(py);
                 kwargs.set_item("copy", copy)?;
[31m-                let result = numpy.getattr("ma")?.getattr("masked_where")?.call(
[m[31m-                    (mask.bind(py), x.bind(py)),
[m[31m-                    Some(&kwargs),
[m[31m-                )?;
[m[32m+                let result = numpy
[m[32m+                    .getattr("ma")?
[m[32m+                    .getattr("masked_where")?
[m[32m+                    .call((mask.bind(py), x.bind(py)), Some(&kwargs))?;
[m                 if numpy_name == "masked_equal" {
                     result.setattr("fill_value", v)?;
                 }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:7195:
                 UnaryOp::Trunc => unary_map_f64(input, output, |x| UnaryOp::Trunc.apply(x)),
                 UnaryOp::Sign => unary_map_f64(input, output, |x| UnaryOp::Sign.apply(x)),
                 UnaryOp::Square => unary_map_f64(input, output, |x| UnaryOp::Square.apply(x)),
[31m-                UnaryOp::Reciprocal => unary_map_f64(input, output, |x| UnaryOp::Reciprocal.apply(x)),
[m[32m+                UnaryOp::Reciprocal => {
[m[32m+                    unary_map_f64(input, output, |x| UnaryOp::Reciprocal.apply(x))
[m[32m+                }
[m                 UnaryOp::Degrees => unary_map_f64(input, output, |x| UnaryOp::Degrees.apply(x)),
                 UnaryOp::Radians => unary_map_f64(input, output, |x| UnaryOp::Radians.apply(x)),
                 // The guard at the top of this fn restricts `op` to the arms above;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:8045:
     }
     let as_bool = bytes.call_method1("view", (numpy.getattr("bool_")?,))?;
     let output_shape = PyTuple::new(py, shape.iter().copied())?;
[31m-    Ok(Some(as_bool.call_method1("reshape", (&output_shape,))?.unbind()))
[m[32m+    Ok(Some(
[m[32m+        as_bool.call_method1("reshape", (&output_shape,))?.unbind(),
[m[32m+    ))
[m }

 // f32 counterpart of try_zerocopy_f64_isclose_array_scalar. isclose(f32-array, finite scalar)
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:8123:
     }
     let as_bool = bytes.call_method1("view", (numpy.getattr("bool_")?,))?;
     let output_shape = PyTuple::new(py, shape.iter().copied())?;
[31m-    Ok(Some(as_bool.call_method1("reshape", (&output_shape,))?.unbind()))
[m[32m+    Ok(Some(
[m[32m+        as_bool.call_method1("reshape", (&output_shape,))?.unbind(),
[m[32m+    ))
[m }

 // Wrap zerocopy_f64_isclose_flat with the shared reshape / 0-d scalar handling.
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:8221:
             // over f64; inputs are read-only under the GIL and `output` is a fresh buffer we
             // own (no alias). Bit-identical to the serial op.apply loop. (The old one-time
             // Vec copy predated this from_raw_parts trick and taxed the cheaper ops.)
[31m-            let lhs: &[f64] =
[m[31m-                unsafe { std::slice::from_raw_parts(a_in.as_ptr().cast::<f64>(), n) };
[m[31m-            let rhs: &[f64] =
[m[31m-                unsafe { std::slice::from_raw_parts(b_in.as_ptr().cast::<f64>(), n) };
[m[32m+            let lhs: &[f64] = unsafe { std::slice::from_raw_parts(a_in.as_ptr().cast::<f64>(), n) };
[m[32m+            let rhs: &[f64] = unsafe { std::slice::from_raw_parts(b_in.as_ptr().cast::<f64>(), n) };
[m             let out_data: &mut [f64] =
                 unsafe { std::slice::from_raw_parts_mut(output.as_ptr() as *mut f64, n) };
             let chunk = n.div_ceil(rayon::current_num_threads());
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:9121:
     if x_is_arr == y_is_arr {
         return Ok(None); // need exactly one array + one scalar
     }
[31m-    let (arr, scalar_obj, scalar_is_x) = if x_is_arr { (x, y, false) } else { (y, x, true) };
[m[32m+    let (arr, scalar_obj, scalar_is_x) = if x_is_arr {
[m[32m+        (x, y, false)
[m[32m+    } else {
[m[32m+        (y, x, true)
[m[32m+    };
[m     // The non-array side must be a TRUE scalar. ndarray SUBCLASSES (e.g. an
     // __array_function__ override), 0-d arrays, lists, and tuples all expose __len__ and
     // must defer to numpy (which dispatches subclass overrides and broadcasts sequences);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:9213:
                 let Some(out_cells) = out_buf.as_mut_slice(py) else {
                     return Ok(None);
                 };
[31m-                for ((slot, cc), av) in out_cells
[m[31m-                    .iter()
[m[31m-                    .zip(cond_cells.iter())
[m[31m-                    .zip(arr_in.iter())
[m[31m-                {
[m[32m+                for ((slot, cc), av) in out_cells.iter().zip(cond_cells.iter()).zip(arr_in.iter()) {
[m                     // arr is x when scalar is y; arr is y when scalar is x.
                     let use_arr = (cc.get() != 0) != scalar_is_x;
                     slot.set(if use_arr { av.get() } else { scalar_u });
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:9451:
         return Ok(None);
     };
     let n = input.len();
[31m-    let out = numpy
[m[31m-        .getattr("empty_like")?
[m[31m-        .call1((a,))?;
[m[32m+    let out = numpy.getattr("empty_like")?.call1((a,))?;
[m     if n > 0 {
         // Normalize the shift into [0, n); result[i] = input[(i - shift) mod n],
         // i.e. result = input[n-s..] ++ input[..n-s].
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:9730:
     }
     let out_typed = out_u8.call_method1("view", (&a_dtype,))?;
     let output_shape = PyTuple::new(py, shape.iter().copied())?;
[31m-    let output = out_typed.call_method1("reshape", (&output_shape,))?.unbind();
[m[32m+    let output = out_typed
[m[32m+        .call_method1("reshape", (&output_shape,))?
[m[32m+        .unbind();
[m     Ok(Some(output))
 }

Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:9912:
     {
         return Ok(None);
     }
[31m-    if condition.getattr("dtype")?.getattr("kind")?.extract::<String>()? != "b" {
[m[32m+    if condition
[m[32m+        .getattr("dtype")?
[m[32m+        .getattr("kind")?
[m[32m+        .extract::<String>()?
[m[32m+        != "b"
[m[32m+    {
[m         return Ok(None);
     }
     let shape: Vec<usize> = a.getattr("shape")?.extract()?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:9979:
         }
     }
     let output_shape = PyTuple::new(py, out_shape.iter().copied())?;
[31m-    Ok(Some(flat.call_method1("reshape", (&output_shape,))?.unbind()))
[m[32m+    Ok(Some(
[m[32m+        flat.call_method1("reshape", (&output_shape,))?.unbind(),
[m[32m+    ))
[m }

 // Generic typed core for boolean-mask stream compaction (np.compress / np.extract
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:10085:
     if !arr.is_exact_instance(&ndarray_type) || !cond.is_exact_instance(&ndarray_type) {
         return Ok(None);
     }
[31m-    if cond.getattr("dtype")?.getattr("kind")?.extract::<String>()? != "b" {
[m[32m+    if cond
[m[32m+        .getattr("dtype")?
[m[32m+        .getattr("kind")?
[m[32m+        .extract::<String>()?
[m[32m+        != "b"
[m[32m+    {
[m         return Ok(None);
     }
     let a_dtype = arr.getattr("dtype")?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:10558:
     // over disjoint raw-slice chunks (in-place, no copy-back); numpy runs this single-threaded.
     // SAFETY: ReadOnlyCell<u8/f64>/Cell<f64> are repr(transparent); read-only mask/values under
     // the GIL, a's buffer written only at disjoint, non-overlapping chunk positions.
[31m-    let a_slice: &mut [f64] = unsafe { std::slice::from_raw_parts_mut(a_out.as_ptr() as *mut f64, n) };
[m[32m+    let a_slice: &mut [f64] =
[m[32m+        unsafe { std::slice::from_raw_parts_mut(a_out.as_ptr() as *mut f64, n) };
[m     let m: &[u8] = unsafe { std::slice::from_raw_parts(mask_in.as_ptr().cast::<u8>(), n) };
     let vals: &[f64] = unsafe { std::slice::from_raw_parts(val_in.as_ptr().cast::<f64>(), v) };
     const PUTMASK_PARALLEL_MIN: usize = 1 << 19;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:10670:
     {
         return Ok(false);
     }
[31m-    if mask.getattr("dtype")?.getattr("kind")?.extract::<String>()? != "b" {
[m[32m+    if mask
[m[32m+        .getattr("dtype")?
[m[32m+        .getattr("kind")?
[m[32m+        .extract::<String>()?
[m[32m+        != "b"
[m[32m+    {
[m         return Ok(false);
     }
     let a_dtype = a.getattr("dtype")?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:10775:
     {
         return Ok(false);
     }
[31m-    if mask.getattr("dtype")?.getattr("kind")?.extract::<String>()? != "b" {
[m[32m+    if mask
[m[32m+        .getattr("dtype")?
[m[32m+        .getattr("kind")?
[m[32m+        .extract::<String>()?
[m[32m+        != "b"
[m[32m+    {
[m         return Ok(false);
     }
     let a_dtype = arr.getattr("dtype")?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:11457:
     // bool -> int64 accumulator (uint8 view of the 0/1 bytes; see try_zerocopy_int_cumsum).
     let result = if kind == "b" {
         let viewed = a.call_method1("view", (numpy.getattr("uint8")?,))?;
[31m-        cumsum_axis_typed::<u8, i64, _, _>(py, &numpy, &viewed, axis, "int64", |v| v as i64, add_i64)?
[m[32m+        cumsum_axis_typed::<u8, i64, _, _>(
[m[32m+            py,
[m[32m+            &numpy,
[m[32m+            &viewed,
[m[32m+            axis,
[m[32m+            "int64",
[m[32m+            |v| v as i64,
[m[32m+            add_i64,
[m[32m+        )?
[m     } else {
         match (kind.as_str(), itemsize) {
[31m-        ("i", 1) => cumsum_axis_typed::<i8, i64, _, _>(py, &numpy, a, axis, "int64", |v| v as i64, add_i64)?,
[m[31m-        ("i", 2) => cumsum_axis_typed::<i16, i64, _, _>(py, &numpy, a, axis, "int64", |v| v as i64, add_i64)?,
[m[31m-        ("i", 4) => cumsum_axis_typed::<i32, i64, _, _>(py, &numpy, a, axis, "int64", |v| v as i64, add_i64)?,
[m[31m-        ("i", 8) => cumsum_axis_typed::<i64, i64, _, _>(py, &numpy, a, axis, "int64", |v| v, add_i64)?,
[m[31m-        ("u", 1) => cumsum_axis_typed::<u8, u64, _, _>(py, &numpy, a, axis, "uint64", |v| v as u64, add_u64)?,
[m[31m-        ("u", 2) => cumsum_axis_typed::<u16, u64, _, _>(py, &numpy, a, axis, "uint64", |v| v as u64, add_u64)?,
[m[31m-        ("u", 4) => cumsum_axis_typed::<u32, u64, _, _>(py, &numpy, a, axis, "uint64", |v| v as u64, add_u64)?,
[m[31m-        ("u", 8) => cumsum_axis_typed::<u64, u64, _, _>(py, &numpy, a, axis, "uint64", |v| v, add_u64)?,
[m[31m-        _ => return Ok(None),
[m[32m+            ("i", 1) => cumsum_axis_typed::<i8, i64, _, _>(
[m[32m+                py,
[m[32m+                &numpy,
[m[32m+                a,
[m[32m+                axis,
[m[32m+                "int64",
[m[32m+                |v| v as i64,
[m[32m+                add_i64,
[m[32m+            )?,
[m[32m+            ("i", 2) => cumsum_axis_typed::<i16, i64, _, _>(
[m[32m+                py,
[m[32m+                &numpy,
[m[32m+                a,
[m[32m+                axis,
[m[32m+                "int64",
[m[32m+                |v| v as i64,
[m[32m+                add_i64,
[m[32m+            )?,
[m[32m+            ("i", 4) => cumsum_axis_typed::<i32, i64, _, _>(
[m[32m+                py,
[m[32m+                &numpy,
[m[32m+                a,
[m[32m+                axis,
[m[32m+                "int64",
[m[32m+                |v| v as i64,
[m[32m+                add_i64,
[m[32m+            )?,
[m[32m+            ("i", 8) => {
[m[32m+                cumsum_axis_typed::<i64, i64, _, _>(py, &numpy, a, axis, "int64", |v| v, add_i64)?
[m[32m+            }
[m[32m+            ("u", 1) => cumsum_axis_typed::<u8, u64, _, _>(
[m[32m+                py,
[m[32m+                &numpy,
[m[32m+                a,
[m[32m+                axis,
[m[32m+                "uint64",
[m[32m+                |v| v as u64,
[m[32m+                add_u64,
[m[32m+            )?,
[m[32m+            ("u", 2) => cumsum_axis_typed::<u16, u64, _, _>(
[m[32m+                py,
[m[32m+                &numpy,
[m[32m+                a,
[m[32m+                axis,
[m[32m+                "uint64",
[m[32m+                |v| v as u64,
[m[32m+                add_u64,
[m[32m+            )?,
[m[32m+            ("u", 4) => cumsum_axis_typed::<u32, u64, _, _>(
[m[32m+                py,
[m[32m+                &numpy,
[m[32m+                a,
[m[32m+                axis,
[m[32m+                "uint64",
[m[32m+                |v| v as u64,
[m[32m+                add_u64,
[m[32m+            )?,
[m[32m+            ("u", 8) => {
[m[32m+                cumsum_axis_typed::<u64, u64, _, _>(py, &numpy, a, axis, "uint64", |v| v, add_u64)?
[m[32m+            }
[m[32m+            _ => return Ok(None),
[m         }
     };
     let Some((flat, shape)) = result else {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:11627:
     // bool -> int64 accumulator (uint8 view of the 0/1 bytes; see try_zerocopy_int_cumprod).
     let result = if kind == "b" {
         let viewed = a.call_method1("view", (numpy.getattr("uint8")?,))?;
[31m-        cumsum_axis_typed::<u8, i64, _, _>(py, &numpy, &viewed, axis, "int64", |v| v as i64, mul_i64)?
[m[32m+        cumsum_axis_typed::<u8, i64, _, _>(
[m[32m+            py,
[m[32m+            &numpy,
[m[32m+            &viewed,
[m[32m+            axis,
[m[32m+            "int64",
[m[32m+            |v| v as i64,
[m[32m+            mul_i64,
[m[32m+        )?
[m     } else {
         match (kind.as_str(), itemsize) {
[31m-        ("i", 1) => cumsum_axis_typed::<i8, i64, _, _>(py, &numpy, a, axis, "int64", |v| v as i64, mul_i64)?,
[m[31m-        ("i", 2) => cumsum_axis_typed::<i16, i64, _, _>(py, &numpy, a, axis, "int64", |v| v as i64, mul_i64)?,
[m[31m-        ("i", 4) => cumsum_axis_typed::<i32, i64, _, _>(py, &numpy, a, axis, "int64", |v| v as i64, mul_i64)?,
[m[31m-        ("i", 8) => cumsum_axis_typed::<i64, i64, _, _>(py, &numpy, a, axis, "int64", |v| v, mul_i64)?,
[m[31m-        ("u", 1) => cumsum_axis_typed::<u8, u64, _, _>(py, &numpy, a, axis, "uint64", |v| v as u64, mul_u64)?,
[m[31m-        ("u", 2) => cumsum_axis_typed::<u16, u64, _, _>(py, &numpy, a, axis, "uint64", |v| v as u64, mul_u64)?,
[m[31m-        ("u", 4) => cumsum_axis_typed::<u32, u64, _, _>(py, &numpy, a, axis, "uint64", |v| v as u64, mul_u64)?,
[m[31m-        ("u", 8) => cumsum_axis_typed::<u64, u64, _, _>(py, &numpy, a, axis, "uint64", |v| v, mul_u64)?,
[m[31m-        _ => return Ok(None),
[m[32m+            ("i", 1) => cumsum_axis_typed::<i8, i64, _, _>(
[m[32m+                py,
[m[32m+                &numpy,
[m[32m+                a,
[m[32m+                axis,
[m[32m+                "int64",
[m[32m+                |v| v as i64,
[m[32m+                mul_i64,
[m[32m+            )?,
[m[32m+            ("i", 2) => cumsum_axis_typed::<i16, i64, _, _>(
[m[32m+                py,
[m[32m+                &numpy,
[m[32m+                a,
[m[32m+                axis,
[m[32m+                "int64",
[m[32m+                |v| v as i64,
[m[32m+                mul_i64,
[m[32m+            )?,
[m[32m+            ("i", 4) => cumsum_axis_typed::<i32, i64, _, _>(
[m[32m+                py,
[m[32m+                &numpy,
[m[32m+                a,
[m[32m+                axis,
[m[32m+                "int64",
[m[32m+                |v| v as i64,
[m[32m+                mul_i64,
[m[32m+            )?,
[m[32m+            ("i", 8) => {
[m[32m+                cumsum_axis_typed::<i64, i64, _, _>(py, &numpy, a, axis, "int64", |v| v, mul_i64)?
[m[32m+            }
[m[32m+            ("u", 1) => cumsum_axis_typed::<u8, u64, _, _>(
[m[32m+                py,
[m[32m+                &numpy,
[m[32m+                a,
[m[32m+                axis,
[m[32m+                "uint64",
[m[32m+                |v| v as u64,
[m[32m+                mul_u64,
[m[32m+            )?,
[m[32m+            ("u", 2) => cumsum_axis_typed::<u16, u64, _, _>(
[m[32m+                py,
[m[32m+                &numpy,
[m[32m+                a,
[m[32m+                axis,
[m[32m+                "uint64",
[m[32m+                |v| v as u64,
[m[32m+                mul_u64,
[m[32m+            )?,
[m[32m+            ("u", 4) => cumsum_axis_typed::<u32, u64, _, _>(
[m[32m+                py,
[m[32m+                &numpy,
[m[32m+                a,
[m[32m+                axis,
[m[32m+                "uint64",
[m[32m+                |v| v as u64,
[m[32m+                mul_u64,
[m[32m+            )?,
[m[32m+            ("u", 8) => {
[m[32m+                cumsum_axis_typed::<u64, u64, _, _>(py, &numpy, a, axis, "uint64", |v| v, mul_u64)?
[m[32m+            }
[m[32m+            _ => return Ok(None),
[m         }
     };
     let Some((flat, shape)) = result else {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:11865:
     out_shape[last] = out_shape[last].saturating_mul(n_elems);
     let output_shape = PyTuple::new(py, out_shape.iter().copied())?;
     Ok(Some(
[31m-        out_typed.call_method1("reshape", (&output_shape,))?.unbind(),
[m[32m+        out_typed
[m[32m+            .call_method1("reshape", (&output_shape,))?
[m[32m+            .unbind(),
[m     ))
 }

Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:11979:
     let out_typed = out_u8.call_method1("view", (&dtype,))?;
     let output_shape = PyTuple::new(py, out_shape.iter().copied())?;
     Ok(Some(
[31m-        out_typed.call_method1("reshape", (&output_shape,))?.unbind(),
[m[32m+        out_typed
[m[32m+            .call_method1("reshape", (&output_shape,))?
[m[32m+            .unbind(),
[m     ))
 }

Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:12421:
 // Otherwise these extracted to an f64 Vec (~57x slower, lossy for wide ints).
 // Returns None when to_begin/to_end are present (handled by the general path), a
 // non-integer dtype, or a non-ndarray input.
[31m-fn try_zerocopy_int_ediff1d(
[m[31m-    py: Python<'_>,
[m[31m-    ary: &Bound<'_, PyAny>,
[m[31m-) -> PyResult<Option<Py<PyAny>>> {
[m[32m+fn try_zerocopy_int_ediff1d(py: Python<'_>, ary: &Bound<'_, PyAny>) -> PyResult<Option<Py<PyAny>>> {
[m     let numpy = py.import("numpy")?;
     let ndarray_type = numpy.getattr("ndarray")?;
     if !ary.is_exact_instance(&ndarray_type) {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:12455:
 // Vec (~311x slower). Reuses the generic ediff1d_typed core with plain IEEE-754
 // single-precision subtraction x - y (bit-identical; nan/inf/-0.0 propagate exactly).
 // Returns None for a non-float32 dtype or a non-ndarray input.
[31m-fn try_zerocopy_f32_ediff1d(
[m[31m-    py: Python<'_>,
[m[31m-    ary: &Bound<'_, PyAny>,
[m[31m-) -> PyResult<Option<Py<PyAny>>> {
[m[32m+fn try_zerocopy_f32_ediff1d(py: Python<'_>, ary: &Bound<'_, PyAny>) -> PyResult<Option<Py<PyAny>>> {
[m     let numpy = py.import("numpy")?;
     let ndarray_type = numpy.getattr("ndarray")?;
     if !ary.is_exact_instance(&ndarray_type) {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:13803:
                     let half = len / 2;
                     let mid = left + half;
                     let probe = bs[mid].get();
[31m-                    let cond = if right_probe { probe <= key } else { probe < key };
[m[32m+                    let cond = if right_probe {
[m[32m+                        probe <= key
[m[32m+                    } else {
[m[32m+                        probe < key
[m[32m+                    };
[m                     if cond {
                         left = mid + 1;
                         len -= half + 1;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:13836:
     if !x.is_exact_instance(&ndarray_type) || !bins.is_exact_instance(&ndarray_type) {
         return Ok(None);
     }
[31m-    if x.getattr("ndim")?.extract::<usize>()? < 1
[m[31m-        || bins.getattr("ndim")?.extract::<usize>()? != 1
[m[32m+    if x.getattr("ndim")?.extract::<usize>()? < 1 || bins.getattr("ndim")?.extract::<usize>()? != 1
[m     {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:13915:
     // Pass 1: max value + non-negativity (a negative value defers so numpy raises).
     let (max_val, any_neg) = if parallel_scan {
         data.par_iter()
[31m-            .fold(
[m[31m-                || (-1i64, false),
[m[31m-                |(mx, neg), &v| (mx.max(v), neg || v < 0),
[m[31m-            )
[m[32m+            .fold(|| (-1i64, false), |(mx, neg), &v| (mx.max(v), neg || v < 0))
[m             .reduce(|| (-1i64, false), |a, b| (a.0.max(b.0), a.1 || b.1))
     } else {
         let mut mx = -1i64;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:13952:
         // the K_MAX cap bounds privatized-array memory, and n >= length*32 ensures the
         // length*nthreads merge/alloc is a small fraction of the count (large-K needs a
         // proportionally larger n — e.g. K=65536 wins from ~4M, K=1000 from ~512K).
[31m-        let parallel_count = parallel_scan
[m[31m-            && length <= BINCOUNT_PARALLEL_K_MAX
[m[31m-            && n >= length.saturating_mul(32);
[m[32m+        let parallel_count =
[m[32m+            parallel_scan && length <= BINCOUNT_PARALLEL_K_MAX && n >= length.saturating_mul(32);
[m         if parallel_count {
             // Privatized parallel tally: each chunk counts into a thread-local array,
             // then the locals are summed element-wise (bit-identical to serial counts).
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:14041:
     // float64 array (a no-op view when already f64). Defer on any cast failure.
     let kw = PyDict::new(py);
     kw.set_item("dtype", "float64")?;
[31m-    let Ok(w_arr) = numpy.getattr("ascontiguousarray")?.call((weights,), Some(&kw)) else {
[m[32m+    let Ok(w_arr) = numpy
[m[32m+        .getattr("ascontiguousarray")?
[m[32m+        .call((weights,), Some(&kw))
[m[32m+    else {
[m         return Ok(None);
     };
     let Ok(w_buffer) = PyBuffer::<f64>::get(&w_arr) else {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:14240:
     if xp_buf.shape().len() != 1 || fp_buf.shape().len() != 1 {
         return Ok(None);
     }
[31m-    let (Some(x_cells), Some(xp_cells), Some(fp_cells)) = (
[m[31m-        x_buf.as_slice(py),
[m[31m-        xp_buf.as_slice(py),
[m[31m-        fp_buf.as_slice(py),
[m[31m-    ) else {
[m[32m+    let (Some(x_cells), Some(xp_cells), Some(fp_cells)) =
[m[32m+        (x_buf.as_slice(py), xp_buf.as_slice(py), fp_buf.as_slice(py))
[m[32m+    else {
[m         return Ok(None);
     };
     let nx = x_cells.len();
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:14274:
     }
     let shape: Vec<usize> = x_buf.shape().to_vec();
     let output_shape = PyTuple::new(py, shape.iter().copied())?;
[31m-    Ok(Some(flat.call_method1("reshape", (&output_shape,))?.unbind()))
[m[32m+    Ok(Some(
[m[32m+        flat.call_method1("reshape", (&output_shape,))?.unbind(),
[m[32m+    ))
[m }

 // Zero-copy parallel trapezoid for a 1-D f64 C-contiguous ndarray with uniform dx.
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:14407:
         }
     }
     let shape_tuple = PyTuple::new(py, shape[..ndim - 1].iter().copied())?;
[31m-    Ok(Some(flat.call_method1("reshape", (&shape_tuple,))?.unbind()))
[m[32m+    Ok(Some(
[m[32m+        flat.call_method1("reshape", (&shape_tuple,))?.unbind(),
[m[32m+    ))
[m }

 // Zero-copy trapezoid along the LAST axis for an f32 C-contiguous ndarray (1-D -> f32
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:14499:
         }
     }
     let shape_tuple = PyTuple::new(py, shape[..ndim - 1].iter().copied())?;
[31m-    Ok(Some(flat.call_method1("reshape", (&shape_tuple,))?.unbind()))
[m[32m+    Ok(Some(
[m[32m+        flat.call_method1("reshape", (&shape_tuple,))?.unbind(),
[m[32m+    ))
[m }

 fn trapezoid_impl(
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:14523:
         }
         kwargs.set_item("dx", dx)?;
         kwargs.set_item("axis", axis)?;
[31m-        return Ok(numpy.getattr(name)?.call((y.bind(py),), Some(&kwargs))?.unbind());
[m[32m+        return Ok(numpy
[m[32m+            .getattr(name)?
[m[32m+            .call((y.bind(py),), Some(&kwargs))?
[m[32m+            .unbind());
[m     }
     // Zero-copy fast path: 1-D f64 contiguous y with uniform dx (no x). numpy's
     // trapezoid is single-threaded and allocates temporaries; read the buffer directly
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:14728:
     let count = input.iter().filter(|cell| pred(cell.get())).count();
     let kwargs = PyDict::new(py);
     kwargs.set_item("dtype", "int64")?;
[31m-    let out = numpy.call_method("empty", (count,), Some(&kwargs))?.unbind();
[m[32m+    let out = numpy
[m[32m+        .call_method("empty", (count,), Some(&kwargs))?
[m[32m+        .unbind();
[m     if count > 0 {
         let Ok(out_buffer) = PyBuffer::<i64>::get(out.bind(py)) else {
             return Ok(None);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:15702:
             // hazard), so the shared block / row-wise folds autovectorize. The
             // fallback widened ints to an f64 Vec and ran the strided reduce
             // (~17-23x slower). Dispatch by width to the right buffer element type.
[31m-            let itemsize = a.getattr("dtype")?.getattr("itemsize")?.extract::<usize>()?;
[m[32m+            let itemsize = a
[m[32m+                .getattr("dtype")?
[m[32m+                .getattr("itemsize")?
[m[32m+                .extract::<usize>()?;
[m             match (kind.as_str(), itemsize) {
[31m-                ("i", 1) => zerocopy_any_all_buf::<i8, _>(py, &numpy, a, axis, is_all, &shape, |v| v != 0),
[m[31m-                ("i", 2) => zerocopy_any_all_buf::<i16, _>(py, &numpy, a, axis, is_all, &shape, |v| v != 0),
[m[31m-                ("i", 4) => zerocopy_any_all_buf::<i32, _>(py, &numpy, a, axis, is_all, &shape, |v| v != 0),
[m[31m-                ("i", 8) => zerocopy_any_all_buf::<i64, _>(py, &numpy, a, axis, is_all, &shape, |v| v != 0),
[m[31m-                ("u", 1) => zerocopy_any_all_buf::<u8, _>(py, &numpy, a, axis, is_all, &shape, |v| v != 0),
[m[31m-                ("u", 2) => zerocopy_any_all_buf::<u16, _>(py, &numpy, a, axis, is_all, &shape, |v| v != 0),
[m[31m-                ("u", 4) => zerocopy_any_all_buf::<u32, _>(py, &numpy, a, axis, is_all, &shape, |v| v != 0),
[m[31m-                ("u", 8) => zerocopy_any_all_buf::<u64, _>(py, &numpy, a, axis, is_all, &shape, |v| v != 0),
[m[32m+                ("i", 1) => {
[m[32m+                    zerocopy_any_all_buf::<i8, _>(py, &numpy, a, axis, is_all, &shape, |v| v != 0)
[m[32m+                }
[m[32m+                ("i", 2) => {
[m[32m+                    zerocopy_any_all_buf::<i16, _>(py, &numpy, a, axis, is_all, &shape, |v| v != 0)
[m[32m+                }
[m[32m+                ("i", 4) => {
[m[32m+                    zerocopy_any_all_buf::<i32, _>(py, &numpy, a, axis, is_all, &shape, |v| v != 0)
[m[32m+                }
[m[32m+                ("i", 8) => {
[m[32m+                    zerocopy_any_all_buf::<i64, _>(py, &numpy, a, axis, is_all, &shape, |v| v != 0)
[m[32m+                }
[m[32m+                ("u", 1) => {
[m[32m+                    zerocopy_any_all_buf::<u8, _>(py, &numpy, a, axis, is_all, &shape, |v| v != 0)
[m[32m+                }
[m[32m+                ("u", 2) => {
[m[32m+                    zerocopy_any_all_buf::<u16, _>(py, &numpy, a, axis, is_all, &shape, |v| v != 0)
[m[32m+                }
[m[32m+                ("u", 4) => {
[m[32m+                    zerocopy_any_all_buf::<u32, _>(py, &numpy, a, axis, is_all, &shape, |v| v != 0)
[m[32m+                }
[m[32m+                ("u", 8) => {
[m[32m+                    zerocopy_any_all_buf::<u64, _>(py, &numpy, a, axis, is_all, &shape, |v| v != 0)
[m[32m+                }
[m                 _ => Ok(None),
             }
         }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:15719:
             // float32 any/all (the f64 arm above is guarded): truthiness is v != 0.0
             // (NaN truthy, -0.0 falsy, matching numpy). The fallback widened to an
             // f64 Vec and ran the strided reduce (~28-40x slower).
[31m-            let itemsize = a.getattr("dtype")?.getattr("itemsize")?.extract::<usize>()?;
[m[32m+            let itemsize = a
[m[32m+                .getattr("dtype")?
[m[32m+                .getattr("itemsize")?
[m[32m+                .extract::<usize>()?;
[m             if itemsize == 4 {
                 zerocopy_any_all_buf::<f32, _>(py, &numpy, a, axis, is_all, &shape, |v| v != 0.0)
             } else {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:16417:
     // these dtypes through f64 (no PyBuffer Element) — ~680x slower for bool, ~2.7x for
     // f16. Defer (the f64/int/f32 zero-copy paths below return None for them anyway).
     if a.bind(py).is_exact_instance(&numpy.getattr("ndarray")?) {
[31m-        let kind = a.bind(py).getattr("dtype")?.getattr("kind")?.extract::<String>()?;
[m[31m-        let itemsize = a.bind(py).getattr("dtype")?.getattr("itemsize")?.extract::<usize>()?;
[m[32m+        let kind = a
[m[32m+            .bind(py)
[m[32m+            .getattr("dtype")?
[m[32m+            .getattr("kind")?
[m[32m+            .extract::<String>()?;
[m[32m+        let itemsize = a
[m[32m+            .bind(py)
[m[32m+            .getattr("dtype")?
[m[32m+            .getattr("itemsize")?
[m[32m+            .extract::<usize>()?;
[m         if kind == "b" || (kind == "f" && itemsize == 2) {
             return fallback();
         }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:16433:

     // Zero-copy float32 clip with scalar bounds: clamp in f32, gated on a no-op
     // promotion so result dtype matches numpy; skips the cold f64 round-trip.
[31m-    if let Some(out) =
[m[31m-        try_zerocopy_f32_clip(py, a.bind(py), a_min.bind(py), a_max.bind(py), min_val, max_val)?
[m[31m-    {
[m[32m+    if let Some(out) = try_zerocopy_f32_clip(
[m[32m+        py,
[m[32m+        a.bind(py),
[m[32m+        a_min.bind(py),
[m[32m+        a_max.bind(py),
[m[32m+        min_val,
[m[32m+        max_val,
[m[32m+    )? {
[m         return Ok(out);
     }

Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:16588:
             slot.set(src.get());
         }
     }
[31m-    Ok(Some(out_bytes.call_method1("view", (&arr_dtype,))?.unbind()))
[m[32m+    Ok(Some(
[m[32m+        out_bytes.call_method1("view", (&arr_dtype,))?.unbind(),
[m[32m+    ))
[m }

 #[pyfunction]
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:16726:
         return Ok(None);
     };
     let n = cells.len();
[31m-    let idx = if idx_raw < 0 { idx_raw + n as i64 } else { idx_raw };
[m[32m+    let idx = if idx_raw < 0 {
[m[32m+        idx_raw + n as i64
[m[32m+    } else {
[m[32m+        idx_raw
[m[32m+    };
[m     if idx < 0 || idx > n as i64 {
         return Ok(None); // idx==n appends; out of range -> numpy raises
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:16833:
         return Ok(None);
     };
     let n = cells.len();
[31m-    let idx = if idx_raw < 0 { idx_raw + n as i64 } else { idx_raw };
[m[32m+    let idx = if idx_raw < 0 {
[m[32m+        idx_raw + n as i64
[m[32m+    } else {
[m[32m+        idx_raw
[m[32m+    };
[m     if idx < 0 || idx >= n as i64 {
         return Ok(None); // numpy raises IndexError -> defer
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:17068:
     out_shape[ax] = out_axis;
     let restored = flat.call_method1("view", (out_dtype,))?;
     let output_shape = PyTuple::new(py, out_shape.iter().copied())?;
[31m-    Ok(Some(restored.call_method1("reshape", (&output_shape,))?.unbind()))
[m[32m+    Ok(Some(
[m[32m+        restored.call_method1("reshape", (&output_shape,))?.unbind(),
[m[32m+    ))
[m }

 // Typed-by-itemsize concatenate for the dtypes the f64 helper leaves to the cold
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:17348:
     let trim_f = mode.contains('f');
     let trim_b = mode.contains('b');
     let (lo, hi) = match bounds {
[31m-        Some((first, last)) => (if trim_f { first } else { 0 }, if trim_b { last } else { n }),
[m[32m+        Some((first, last)) => (
[m[32m+            if trim_f { first } else { 0 },
[m[32m+            if trim_b { last } else { n },
[m[32m+        ),
[m         None => (0usize, 0usize), // all-zero / empty → empty result
     };
     let slice = pyo3::types::PySlice::new(py, lo as isize, hi as isize, 1);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:17737:
         let m = shape[shape.len() - 2];
         let n = shape[shape.len() - 1];
         let owned_shape = shape.to_vec();
[31m-        if let Ok(values) =
[m[31m-            fnp_linalg::batch_pinv(array.values(), &owned_shape, rcond.as_rcond(), rtol.as_rtol())
[m[31m-        {
[m[32m+        if let Ok(values) = fnp_linalg::batch_pinv(
[m[32m+            array.values(),
[m[32m+            &owned_shape,
[m[32m+            rcond.as_rcond(),
[m[32m+            rtol.as_rtol(),
[m[32m+        ) {
[m             let mut out_shape: Vec<usize> = owned_shape[..owned_shape.len() - 2].to_vec();
             out_shape.push(n);
             out_shape.push(m);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:17746:
[31m-            let result =
[m[31m-                UFuncArray::new(out_shape, values, DType::F64).map_err(map_ufunc_error)?;
[m[32m+            let result = UFuncArray::new(out_shape, values, DType::F64).map_err(map_ufunc_error)?;
[m             return build_numpy_array_from_ufunc(py, &result);
         }
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:17850:
         let n = shape[shape.len() - 1];
         if let Some(rcond) = matrix_rank_default_rcond(array.dtype(), n) {
             let owned_shape = shape.to_vec();
[31m-            if let Ok(ranks) =
[m[31m-                fnp_linalg::batch_matrix_rank(array.values(), &owned_shape, rcond)
[m[31m-            {
[m[32m+            if let Ok(ranks) = fnp_linalg::batch_matrix_rank(array.values(), &owned_shape, rcond) {
[m                 let out_shape: Vec<usize> = owned_shape[..owned_shape.len() - 2].to_vec();
                 let vals: Vec<i64> = ranks.iter().map(|&r| r as i64).collect();
                 let result = UFuncArray::from_storage(out_shape, ArrayStorage::I64(vals))
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:17995:
             .and_then(|s| s.extract::<Vec<usize>>())
         && shape.len() == 2
         && shape[0] == shape[1]
[31m-        && a
[m[31m-            .bind(py)
[m[32m+        && a.bind(py)
[m             .getattr("dtype")
             .and_then(|d| d.getattr("kind"))
             .and_then(|k| k.extract::<String>())
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:18021:
     // parallel batch_slogdet, reducing the two matrix axes. On any Err (singular
     // is still valid: sign=0, logabsdet=-inf; only shape errors out) fall back to
     // numpy. Mirrors the 2-D guards.
[31m-    if shape.len() >= 3
[m[31m-        && shape[shape.len() - 1] == shape[shape.len() - 2]
[m[31m-        && real_f64_finite
[m[31m-    {
[m[32m+    if shape.len() >= 3 && shape[shape.len() - 1] == shape[shape.len() - 2] && real_f64_finite {
[m         let owned_shape = shape.to_vec();
         if let Ok((signs, logs)) = fnp_linalg::batch_slogdet(array.values(), &owned_shape) {
             let out_shape: Vec<usize> = owned_shape[..owned_shape.len() - 2].to_vec();
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:18031:
[31m-            let sign_arr = UFuncArray::new(out_shape.clone(), signs, DType::F64)
[m[31m-                .map_err(map_ufunc_error)?;
[m[31m-            let log_arr =
[m[31m-                UFuncArray::new(out_shape, logs, DType::F64).map_err(map_ufunc_error)?;
[m[32m+            let sign_arr =
[m[32m+                UFuncArray::new(out_shape.clone(), signs, DType::F64).map_err(map_ufunc_error)?;
[m[32m+            let log_arr = UFuncArray::new(out_shape, logs, DType::F64).map_err(map_ufunc_error)?;
[m             return build_numpy_slogdet_result_arrays(py, &sign_arr, &log_arr);
         }
         return fallback();
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:18465:
     // [n, k]. A defensive element-count check + Err fall back to numpy for any case
     // batch_solve does not accept (non-trivial broadcast, singular, etc.). Mirrors the
     // det/inv/eigh/matrix_rank batched wirings.
[31m-    if a_shape.len() >= 3 && a_shape[a_shape.len() - 1] == a_shape[a_shape.len() - 2] && real_f64_finite
[m[32m+    if a_shape.len() >= 3
[m[32m+        && a_shape[a_shape.len() - 1] == a_shape[a_shape.len() - 2]
[m[32m+        && real_f64_finite
[m     {
         let n = a_shape[a_shape.len() - 1];
         // NumPy 2.0+ treats `b` as a single vector ONLY when b.ndim == 1; any
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:18485:
                     out_shape.push(b_shape[b_shape.len() - 1]);
                 }
                 let expected: usize = out_shape.iter().product();
[31m-                if let Ok(values) = fnp_linalg::batch_solve(
[m[31m-                    a.values(),
[m[31m-                    a_shape,
[m[31m-                    b.values(),
[m[31m-                    b_shape,
[m[31m-                    vector_rhs,
[m[31m-                ) && values.len() == expected
[m[32m+                if let Ok(values) =
[m[32m+                    fnp_linalg::batch_solve(a.values(), a_shape, b.values(), b_shape, vector_rhs)
[m[32m+                    && values.len() == expected
[m                     && let Ok(result) = UFuncArray::new(out_shape, values, DType::F64)
                 {
                     return build_numpy_array_from_ufunc(py, &result);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:18501:
         return fallback();
     }

[31m-    if a_shape.len() != 2
[m[31m-        || a_shape[0] != a_shape[1]
[m[31m-        || !real_f64_finite
[m[31m-    {
[m[32m+    if a_shape.len() != 2 || a_shape[0] != a_shape[1] || !real_f64_finite {
[m         return fallback();
     }

Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:18629:
             .and_then(|s| s.extract::<Vec<usize>>())
         && shape.len() == 2
         && shape[0] == shape[1]
[31m-        && a
[m[31m-            .bind(py)
[m[32m+        && a.bind(py)
[m             .getattr("dtype")
             .and_then(|d| d.getattr("kind"))
             .and_then(|k| k.extract::<String>())
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:18684:
         if let Ok(values) = fnp_linalg::batch_eigvalsh(&sym, &owned_shape) {
             let mut out_shape: Vec<usize> = owned_shape[..owned_shape.len() - 2].to_vec();
             out_shape.push(n);
[31m-            let result =
[m[31m-                UFuncArray::new(out_shape, values, DType::F64).map_err(map_ufunc_error)?;
[m[32m+            let result = UFuncArray::new(out_shape, values, DType::F64).map_err(map_ufunc_error)?;
[m             return build_numpy_array_from_ufunc(py, &result);
         }
         return fallback();
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:18739:
             .map(|k| k == "f")
             .unwrap_or(false)
     {
[31m-        return Ok(numpy.getattr("linalg")?.getattr("det")?.call1((bound,))?.unbind());
[m[32m+        return Ok(numpy
[m[32m+            .getattr("linalg")?
[m[32m+            .getattr("det")?
[m[32m+            .call1((bound,))?
[m[32m+            .unbind());
[m     }
     if let Ok(array) = extract_numeric_array(py, bound, "det(a)") {
         let shape = array.shape();
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:18791:
     // (>=3-D) batch_inv below still wins (numpy loops serial per lane). Re-enable
     // native 2-D only if a future NumPy/BLAS reintroduces the getri cliff.
     if bound.is_exact_instance(&numpy.getattr("ndarray")?)
[31m-        && let Ok(shape) = bound.getattr("shape").and_then(|s| s.extract::<Vec<usize>>())
[m[32m+        && let Ok(shape) = bound
[m[32m+            .getattr("shape")
[m[32m+            .and_then(|s| s.extract::<Vec<usize>>())
[m         && shape.len() == 2
         && shape[0] == shape[1]
     {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:19017:
             let kind: String = x.getattr("dtype")?.getattr("kind")?.extract()?;
             // int/uint canonicalize lossy; non-contiguous bails the fast path into the
             // transpose-copy extract (~55x slower). Delegate both to numpy.
[31m-            let c_contiguous = x.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()?;
[m[32m+            let c_contiguous = x
[m[32m+                .getattr("flags")?
[m[32m+                .getattr("c_contiguous")?
[m[32m+                .extract::<bool>()?;
[m             if kind == "i" || kind == "u" || !c_contiguous {
                 return Ok(numpy.getattr("signbit")?.call1((x,))?.unbind());
             }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:19037:
 ) -> PyResult<Py<PyAny>> {
     if kwargs.is_none_or(|kwargs| kwargs.is_empty()) && args.len() == 1 {
         let x_arg = args.get_item(0)?;
[31m-        signbit_native(py, &x_arg)
[m[31m-            .or_else(|_| core_numpy_passthrough(py, "signbit", args, kwargs))
[m[32m+        signbit_native(py, &x_arg).or_else(|_| core_numpy_passthrough(py, "signbit", args, kwargs))
[m     } else {
         core_numpy_passthrough(py, "signbit", args, kwargs)
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:19421:
     // native path maps every integer to float64 (widening int8/int16), so defer all
     // non-float64 inputs to numpy for exact dtype + value parity.
     if !numpy_dtype_is_f64(py, x) {
[31m-        return Ok(py.import("numpy")?.getattr(numpy_name)?.call1((x,))?.unbind());
[m[32m+        return Ok(py
[m[32m+            .import("numpy")?
[m[32m+            .getattr(numpy_name)?
[m[32m+            .call1((x,))?
[m[32m+            .unbind());
[m     }
     let x = extract_precise_numeric_array(py, x, extract_label)?;
     build_numpy_scalar_or_array(py, &x.elementwise_unary(op))
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:19440:
 ) -> PyResult<Py<PyAny>> {
     if kwargs.is_none_or(|kwargs| kwargs.is_empty()) && args.len() == 1 {
         let x_arg = args.get_item(0)?;
[31m-        degrees_native(py, &x_arg)
[m[31m-            .or_else(|_| core_numpy_passthrough(py, "degrees", args, kwargs))
[m[32m+        degrees_native(py, &x_arg).or_else(|_| core_numpy_passthrough(py, "degrees", args, kwargs))
[m     } else {
         core_numpy_passthrough(py, "degrees", args, kwargs)
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:19460:
 ) -> PyResult<Py<PyAny>> {
     if kwargs.is_none_or(|kwargs| kwargs.is_empty()) && args.len() == 1 {
         let x_arg = args.get_item(0)?;
[31m-        radians_native(py, &x_arg)
[m[31m-            .or_else(|_| core_numpy_passthrough(py, "radians", args, kwargs))
[m[32m+        radians_native(py, &x_arg).or_else(|_| core_numpy_passthrough(py, "radians", args, kwargs))
[m     } else {
         core_numpy_passthrough(py, "radians", args, kwargs)
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:19534:
         Ok(Some(flat.unbind()))
     } else {
         let shape_tuple = PyTuple::new(py, shape.iter().copied())?;
[31m-        Ok(Some(flat.call_method1("reshape", (&shape_tuple,))?.unbind()))
[m[32m+        Ok(Some(
[m[32m+            flat.call_method1("reshape", (&shape_tuple,))?.unbind(),
[m[32m+        ))
[m     }
 }

Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:19690:
             .call1((x1.bind(py), x2.bind(py)))?
             .unbind());
     }
[31m-    if let Some(out) =
[m[31m-        try_zerocopy_f64_binary(py, x1.bind(py), x2.bind(py), BinaryOp::Logaddexp)?
[m[31m-    {
[m[32m+    if let Some(out) = try_zerocopy_f64_binary(py, x1.bind(py), x2.bind(py), BinaryOp::Logaddexp)? {
[m         return Ok(out);
     }
     if noncontiguous_ndarray(&numpy, x1.bind(py))? || noncontiguous_ndarray(&numpy, x2.bind(py))? {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:19730:
             .call1((x1.bind(py), x2.bind(py)))?
             .unbind());
     }
[31m-    if let Some(out) =
[m[31m-        try_zerocopy_f64_binary(py, x1.bind(py), x2.bind(py), BinaryOp::Logaddexp2)?
[m[32m+    if let Some(out) = try_zerocopy_f64_binary(py, x1.bind(py), x2.bind(py), BinaryOp::Logaddexp2)?
[m     {
         return Ok(out);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:20051:
     {
         let numpy = py.import("numpy")?;
         if x.bind(py).is_exact_instance(&numpy.getattr("ndarray")?)
[31m-            && x.bind(py).getattr("dtype")?.getattr("kind")?.extract::<String>()? == "b"
[m[32m+            && x.bind(py)
[m[32m+                .getattr("dtype")?
[m[32m+                .getattr("kind")?
[m[32m+                .extract::<String>()?
[m[32m+                == "b"
[m         {
             let kwargs = PyDict::new(py);
             kwargs.set_item("nan", nan)?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:20845:
     }
     let shape: Vec<usize> = v.getattr("shape")?.extract()?;
     let output_shape = PyTuple::new(py, shape.iter().copied())?;
[31m-    Ok(Some(flat.call_method1("reshape", (&output_shape,))?.unbind()))
[m[32m+    Ok(Some(
[m[32m+        flat.call_method1("reshape", (&output_shape,))?.unbind(),
[m[32m+    ))
[m }

 // Typed binary-search core for np.searchsorted over a sorted haystack. For each
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:21083:
         let kwargs = PyDict::new(py);
         kwargs.set_item("dtype", "int64")?;
         let counts = numpy.call_method("zeros", (nbins,), Some(&kwargs))?;
[31m-        return Ok(Some(
[m[31m-            PyTuple::new(py, [counts, edges])?.into_any().unbind(),
[m[31m-        ));
[m[32m+        return Ok(Some(PyTuple::new(py, [counts, edges])?.into_any().unbind()));
[m     }
     let Some(s) = buf.as_slice(py) else {
         let histogram_fn = numpy.getattr("histogram")?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:21118:
             .par_iter()
             .map(|&raw| {
                 let v = to_f64(raw);
[31m-                (
[m[31m-                    v,
[m[31m-                    v,
[m[31m-                    value_supported(raw),
[m[31m-                    !check_finite || v.is_finite(),
[m[31m-                )
[m[32m+                (v, v, value_supported(raw), !check_finite || v.is_finite())
[m             })
             .reduce(
                 || (f64::INFINITY, f64::NEG_INFINITY, true, true),
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:21192:
         for (slot, value) in cs.iter().zip(local_counts) {
             slot.set(value);
         }
[31m-        return Ok(Some(
[m[31m-            PyTuple::new(py, [counts, edges])?.into_any().unbind(),
[m[31m-        ));
[m[32m+        return Ok(Some(PyTuple::new(py, [counts, edges])?.into_any().unbind()));
[m     }

     let mut mn = to_f64(s[0].get());
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:21270:
             slot.set(slot.get() + 1);
         }
     }
[31m-    Ok(Some(
[m[31m-        PyTuple::new(py, [counts, edges])?.into_any().unbind(),
[m[31m-    ))
[m[32m+    Ok(Some(PyTuple::new(py, [counts, edges])?.into_any().unbind()))
[m }

 fn histogram_f32(
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:21302:
         let kwargs = PyDict::new(py);
         kwargs.set_item("dtype", "int64")?;
         let counts = numpy.call_method("zeros", (nbins,), Some(&kwargs))?;
[31m-        return Ok(Some(
[m[31m-            PyTuple::new(py, [counts, edges])?.into_any().unbind(),
[m[31m-        ));
[m[32m+        return Ok(Some(PyTuple::new(py, [counts, edges])?.into_any().unbind()));
[m     }
     let Some(s) = buf.as_slice(py) else {
         let histogram_fn = numpy.getattr("histogram")?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:21426:
             }
         }
     }
[31m-    Ok(Some(
[m[31m-        PyTuple::new(py, [counts, edges])?.into_any().unbind(),
[m[31m-    ))
[m[32m+    Ok(Some(PyTuple::new(py, [counts, edges])?.into_any().unbind()))
[m }

 // O(n) uniform-bin np.histogram for exact 1-D f64/f32/integer ndarrays with
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:21453:
     match (kind.as_str(), itemsize) {
         ("f", 4) => histogram_f32(py, &numpy, a, nbins),
         ("f", 8) => histogram_typed::<f64>(py, &numpy, a, nbins, |x| x, true, |_| true),
[31m-        ("i", 1) => {
[m[31m-            histogram_typed::<i8>(py, &numpy, a, nbins, |x| x as f64, false, |_| true)
[m[32m+        ("i", 1) => histogram_typed::<i8>(py, &numpy, a, nbins, |x| x as f64, false, |_| true),
[m[32m+        ("i", 2) => histogram_typed::<i16>(py, &numpy, a, nbins, |x| x as f64, false, |_| true),
[m[32m+        ("i", 4) => histogram_typed::<i32>(py, &numpy, a, nbins, |x| x as f64, false, |_| true),
[m[32m+        ("i", 8) => {
[m[32m+            histogram_typed::<i64>(py, &numpy, a, nbins, |x| x as f64, false, i64_is_f64_exact)
[m         }
[31m-        ("i", 2) => {
[m[31m-            histogram_typed::<i16>(py, &numpy, a, nbins, |x| x as f64, false, |_| true)
[m[32m+        ("u", 1) => histogram_typed::<u8>(py, &numpy, a, nbins, |x| x as f64, false, |_| true),
[m[32m+        ("u", 2) => histogram_typed::<u16>(py, &numpy, a, nbins, |x| x as f64, false, |_| true),
[m[32m+        ("u", 4) => histogram_typed::<u32>(py, &numpy, a, nbins, |x| x as f64, false, |_| true),
[m[32m+        ("u", 8) => {
[m[32m+            histogram_typed::<u64>(py, &numpy, a, nbins, |x| x as f64, false, u64_is_f64_exact)
[m         }
[31m-        ("i", 4) => {
[m[31m-            histogram_typed::<i32>(py, &numpy, a, nbins, |x| x as f64, false, |_| true)
[m[31m-        }
[m[31m-        ("i", 8) => histogram_typed::<i64>(
[m[31m-            py,
[m[31m-            &numpy,
[m[31m-            a,
[m[31m-            nbins,
[m[31m-            |x| x as f64,
[m[31m-            false,
[m[31m-            i64_is_f64_exact,
[m[31m-        ),
[m[31m-        ("u", 1) => {
[m[31m-            histogram_typed::<u8>(py, &numpy, a, nbins, |x| x as f64, false, |_| true)
[m[31m-        }
[m[31m-        ("u", 2) => {
[m[31m-            histogram_typed::<u16>(py, &numpy, a, nbins, |x| x as f64, false, |_| true)
[m[31m-        }
[m[31m-        ("u", 4) => {
[m[31m-            histogram_typed::<u32>(py, &numpy, a, nbins, |x| x as f64, false, |_| true)
[m[31m-        }
[m[31m-        ("u", 8) => histogram_typed::<u64>(
[m[31m-            py,
[m[31m-            &numpy,
[m[31m-            a,
[m[31m-            nbins,
[m[31m-            |x| x as f64,
[m[31m-            false,
[m[31m-            u64_is_f64_exact,
[m[31m-        ),
[m         _ => Ok(None),
     }
 }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:21571:
             // 1-D: a single row gives no row-level parallelism -> parallelize the interior.
             o[0] = (data[1] - data[0]) / dx;
             o[l - 1] = (data[l - 1] - data[l - 2]) / dx;
[31m-            o[1..l - 1].par_iter_mut().enumerate().for_each(|(j, slot)| {
[m[31m-                let i = j + 1;
[m[31m-                *slot = (data[i + 1] - data[i - 1]) / (2.0 * dx);
[m[31m-            });
[m[32m+            o[1..l - 1]
[m[32m+                .par_iter_mut()
[m[32m+                .enumerate()
[m[32m+                .for_each(|(j, slot)| {
[m[32m+                    let i = j + 1;
[m[32m+                    *slot = (data[i + 1] - data[i - 1]) / (2.0 * dx);
[m[32m+                });
[m         } else if parallel {
             // N-D last axis: each contiguous row is independent -> parallelize over rows.
             o.par_chunks_mut(l)
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:21618:
     // with no axis on N-D it returns a list. So take native only when the target axis is
     // the last one: axis=None requires ndim==1; axis=k requires k to normalize to ndim-1.
     let axis_is_last = uniform_dx.is_some()
[31m-        && match f.bind(py).getattr("ndim").and_then(|n| n.extract::<usize>()) {
[m[32m+        && match f
[m[32m+            .bind(py)
[m[32m+            .getattr("ndim")
[m[32m+            .and_then(|n| n.extract::<usize>())
[m[32m+        {
[m             Ok(ndim) if ndim >= 1 => match axis.as_ref() {
                 None => ndim == 1,
                 Some(a) => a
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:21735:
                 }
                 if all_ok {
                     let td_dtype = numpy.call_method1("dtype", (td_str,))?;
[31m-                    return Ok(current.bind(py).call_method1("view", (&td_dtype,))?.unbind());
[m[32m+                    return Ok(current
[m[32m+                        .bind(py)
[m[32m+                        .call_method1("view", (&td_dtype,))?
[m[32m+                        .unbind());
[m                 }
                 return fallback();
             }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:21792:
     // pass); the extract path below bridges bool->f64 and runs ~700x slower (85ms vs
     // 122us @4M). numpy is the parity reference, so defer bool to it. (Checked only on
     // the fallthrough — f64/int/f32 already returned.)
[31m-    if a
[m[31m-        .bind(py)
[m[31m-        .is_exact_instance(&numpy.getattr("ndarray")?)
[m[31m-        && a.bind(py).getattr("dtype")?.getattr("kind")?.extract::<String>()? == "b"
[m[32m+    if a.bind(py).is_exact_instance(&numpy.getattr("ndarray")?)
[m[32m+        && a.bind(py)
[m[32m+            .getattr("dtype")?
[m[32m+            .getattr("kind")?
[m[32m+            .extract::<String>()?
[m[32m+            == "b"
[m     {
         return fallback();
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:22209:
     // view-semantics divergence). Delegate to numpy.rot90 for the exact view,
     // dtype, and error surface.
     let numpy = py.import("numpy")?;
[31m-    Ok(numpy.getattr("rot90")?.call1((m.bind(py), k, axes))?.unbind())
[m[32m+    Ok(numpy
[m[32m+        .getattr("rot90")?
[m[32m+        .call1((m.bind(py), k, axes))?
[m[32m+        .unbind())
[m }

 #[pyfunction]
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:22559:
     // Normalize indices to a contiguous int64 array; only integer index dtypes
     // are accepted (float indices raise in numpy — defer).
     let ind_arr = numpy.call_method1("asarray", (ind,))?;
[31m-    let ind_kind = ind_arr.getattr("dtype")?.getattr("kind")?.extract::<String>()?;
[m[32m+    let ind_kind = ind_arr
[m[32m+        .getattr("dtype")?
[m[32m+        .getattr("kind")?
[m[32m+        .extract::<String>()?;
[m     if ind_kind != "i" && ind_kind != "u" {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:22939:
                 np_dt,
             )
         }
[31m-        _ => ("i".to_string(), 8, numpy.getattr("dtype")?.call1(("int64",))?),
[m[32m+        _ => (
[m[32m+            "i".to_string(),
[m[32m+            8,
[m[32m+            numpy.getattr("dtype")?.call1(("int64",))?,
[m[32m+        ),
[m     };
     if kind != "i" && kind != "u" {
         return Ok(None);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:23487:
             Err(_) => return Ok(None),
         },
     };
[31m-    let Ok(shape) = value.getattr("shape").and_then(|s| s.extract::<Vec<usize>>()) else {
[m[32m+    let Ok(shape) = value
[m[32m+        .getattr("shape")
[m[32m+        .and_then(|s| s.extract::<Vec<usize>>())
[m[32m+    else {
[m         return Ok(None);
     };
     let total: usize = shape.iter().product();
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:23532:
         }
     }
     let shape_tuple = PyTuple::new(py, shape)?;
[31m-    Ok(Some(flat.call_method1("reshape", (&shape_tuple,))?.unbind()))
[m[32m+    Ok(Some(
[m[32m+        flat.call_method1("reshape", (&shape_tuple,))?.unbind(),
[m[32m+    ))
[m }

 #[pyfunction]
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:23557:
     // (out[i] = mask[i] ? fill : data[i]). Pure element selection, so bit-identical to the
     // generic path. Non-f64 data, non-contiguous, structured/non-float fill, or any buffer
     // failure fall through to the generic extraction.
[31m-    if let Some(out) = try_zerocopy_ma_filled_f64(py, a.bind(py), fill_value.as_ref().map(|v| v.bind(py)))? {
[m[32m+    if let Some(out) =
[m[32m+        try_zerocopy_ma_filled_f64(py, a.bind(py), fill_value.as_ref().map(|v| v.bind(py)))?
[m[32m+    {
[m         return Ok(out);
     }

Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:24569:
         if (a_is_ma || a_b.is_exact_instance(&ndarray_type))
             && (b_is_ma || b_b.is_exact_instance(&ndarray_type))
         {
[31m-            let a_data = if a_is_ma { a_b.getattr("data")? } else { a_b.clone() };
[m[31m-            let b_data = if b_is_ma { b_b.getattr("data")? } else { b_b.clone() };
[m[32m+            let a_data = if a_is_ma {
[m[32m+                a_b.getattr("data")?
[m[32m+            } else {
[m[32m+                a_b.clone()
[m[32m+            };
[m[32m+            let b_data = if b_is_ma {
[m[32m+                b_b.getattr("data")?
[m[32m+            } else {
[m[32m+                b_b.clone()
[m[32m+            };
[m             if numpy_dtype_is_f64(py, &a_data) && numpy_dtype_is_f64(py, &b_data) {
                 let ashape = a_b.getattr("shape").and_then(|s| s.extract::<Vec<usize>>());
                 let bshape = b_b.getattr("shape").and_then(|s| s.extract::<Vec<usize>>());
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:24597:
                     } else {
                         None
                     };
[31m-                    let a_mask_buf = a_mask_view.as_ref().and_then(|v| PyBuffer::<u8>::get(v).ok());
[m[31m-                    let b_mask_buf = b_mask_view.as_ref().and_then(|v| PyBuffer::<u8>::get(v).ok());
[m[32m+                    let a_mask_buf = a_mask_view
[m[32m+                        .as_ref()
[m[32m+                        .and_then(|v| PyBuffer::<u8>::get(v).ok());
[m[32m+                    let b_mask_buf = b_mask_view
[m[32m+                        .as_ref()
[m[32m+                        .and_then(|v| PyBuffer::<u8>::get(v).ok());
[m                     let a_mask = a_mask_buf.as_ref().and_then(|buf| buf.as_slice(py));
                     let b_mask = b_mask_buf.as_ref().and_then(|buf| buf.as_slice(py));
                     let n = ad.len();
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:24634:
                             for i in 0..n {
                                 let masked = a_mask.is_some_and(|s| s[i].get() != 0)
                                     || b_mask.is_some_and(|s| s[i].get() != 0);
[31m-                                let res = if masked { fill_value } else { ad[i].get() == bd[i].get() };
[m[32m+                                let res = if masked {
[m[32m+                                    fill_value
[m[32m+                                } else {
[m[32m+                                    ad[i].get() == bd[i].get()
[m[32m+                                };
[m                                 if !res {
                                     equal = false;
                                     break;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:24892:
     }
     // Multi-axis (tuple) reductions extract the whole array then fall back to numpy
     // on the axis re-parse below (wasteful 8x bool-bridge / f64 copy). Delegate up front.
[31m-    if axis.as_ref().is_some_and(|ax| ax.bind(py).cast::<PyTuple>().is_ok()) {
[m[32m+    if axis
[m[32m+        .as_ref()
[m[32m+        .is_some_and(|ax| ax.bind(py).cast::<PyTuple>().is_ok())
[m[32m+    {
[m         return fallback();
     }
     let a = match extract_numeric_array(py, a.bind(py), "nanmean(a)") {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:25075:
 // Bit-exact flat mean (axis=None) = numpy's pairwise sum / n. numpy's unweighted
 // np.average and np.mean both reduce to umr_sum (pairwise) / n, so this matches
 // both. Empty input defers (numpy raises / warns). C-contiguous f64 only.
[31m-fn try_zerocopy_f64_mean_flat(
[m[31m-    py: Python<'_>,
[m[31m-    a: &Bound<'_, PyAny>,
[m[31m-) -> PyResult<Option<Py<PyAny>>> {
[m[32m+fn try_zerocopy_f64_mean_flat(py: Python<'_>, a: &Bound<'_, PyAny>) -> PyResult<Option<Py<PyAny>>> {
[m     let numpy = py.import("numpy")?;
     let Some(in_buffer) = f64_contiguous_cells(py, a, &numpy)? else {
         return Ok(None);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:25306:
     }
     // Multi-axis (tuple) reductions extract the whole array then fall back to numpy
     // on the axis re-parse below (wasteful 8x bool-bridge / f64 copy). Delegate up front.
[31m-    if axis.as_ref().is_some_and(|ax| ax.bind(py).cast::<PyTuple>().is_ok()) {
[m[32m+    if axis
[m[32m+        .as_ref()
[m[32m+        .is_some_and(|ax| ax.bind(py).cast::<PyTuple>().is_ok())
[m[32m+    {
[m         return fallback();
     }
     let a = match extract_numeric_array(py, a.bind(py), "nansum(a)") {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:25422:
     }
     // Multi-axis (tuple) reductions extract the whole array then fall back to numpy
     // on the axis re-parse below (wasteful 8x bool-bridge / f64 copy). Delegate up front.
[31m-    if axis.as_ref().is_some_and(|ax| ax.bind(py).cast::<PyTuple>().is_ok()) {
[m[32m+    if axis
[m[32m+        .as_ref()
[m[32m+        .is_some_and(|ax| ax.bind(py).cast::<PyTuple>().is_ok())
[m[32m+    {
[m         return fallback();
     }
     let a = match extract_numeric_array(py, a.bind(py), "nanprod(a)") {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:25554:
             }
             acc
         };
[31m-        let parallel =
[m[31m-            outer >= 2 && outer * axis_len * inner >= (1 << 16) && rayon::current_num_threads() >= 2;
[m[32m+        let parallel = outer >= 2
[m[32m+            && outer * axis_len * inner >= (1 << 16)
[m[32m+            && rayon::current_num_threads() >= 2;
[m         let mut out = vec![0.0f64; outer * inner];
         if parallel {
             let planes: Vec<Vec<f64>> = (0..outer).into_par_iter().map(group_prod).collect();
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:25573:
     let mut out_shape = shape.clone();
     out_shape.remove(ax);
     let flat = numpy_array_from_slice(py, &numpy, &out, "float64")?;
[31m-    let reshaped =
[m[31m-        flat.call_method1("reshape", (PyTuple::new(py, out_shape.iter().copied())?,))?;
[m[32m+    let reshaped = flat.call_method1("reshape", (PyTuple::new(py, out_shape.iter().copied())?,))?;
[m     if out_shape.is_empty() {
         return Ok(Some(reshaped.get_item(())?.unbind()));
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:25615:
     while off < full {
         let c = V::from_slice(&data[off..off + L]);
         vsaw |= c.simd_eq(c); // non-NaN lanes (c == c)
[31m-        acc = if take_max { acc.simd_max(c) } else { acc.simd_min(c) };
[m[32m+        acc = if take_max {
[m[32m+            acc.simd_max(c)
[m[32m+        } else {
[m[32m+            acc.simd_min(c)
[m[32m+        };
[m         off += L;
     }
     let mut m = init;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:25659:
             .reduce(
                 || (init, false),
                 |(am, asaw), (bm, bsaw)| {
[31m-                    (
[m[31m-                        if take_max { am.max(bm) } else { am.min(bm) },
[m[31m-                        asaw || bsaw,
[m[31m-                    )
[m[32m+                    (if take_max { am.max(bm) } else { am.min(bm) }, asaw || bsaw)
[m                 },
             )
     } else {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:25824:
         // groups, fan across groups; a single group (2-D axis=0) privatizes across
         // row-blocks and merges the planes elementwise.
         let lane = axis_len * inner;
[31m-        let row_max = |mut acc: Vec<f64>, mut sw: Vec<bool>, base: usize| -> (Vec<f64>, Vec<bool>) {
[m[31m-            for r in 0..axis_len {
[m[31m-                let row = &data[base + r * inner..base + r * inner + inner];
[m[31m-                if take_max {
[m[31m-                    for ((a_acc, a_sw), &v) in acc.iter_mut().zip(sw.iter_mut()).zip(row) {
[m[31m-                        *a_acc = a_acc.max(v);
[m[31m-                        *a_sw |= v == v;
[m[32m+        let row_max =
[m[32m+            |mut acc: Vec<f64>, mut sw: Vec<bool>, base: usize| -> (Vec<f64>, Vec<bool>) {
[m[32m+                for r in 0..axis_len {
[m[32m+                    let row = &data[base + r * inner..base + r * inner + inner];
[m[32m+                    if take_max {
[m[32m+                        for ((a_acc, a_sw), &v) in acc.iter_mut().zip(sw.iter_mut()).zip(row) {
[m[32m+                            *a_acc = a_acc.max(v);
[m[32m+                            *a_sw |= v == v;
[m[32m+                        }
[m[32m+                    } else {
[m[32m+                        for ((a_acc, a_sw), &v) in acc.iter_mut().zip(sw.iter_mut()).zip(row) {
[m[32m+                            *a_acc = a_acc.min(v);
[m[32m+                            *a_sw |= v == v;
[m[32m+                        }
[m                     }
[31m-                } else {
[m[31m-                    for ((a_acc, a_sw), &v) in acc.iter_mut().zip(sw.iter_mut()).zip(row) {
[m[31m-                        *a_acc = a_acc.min(v);
[m[31m-                        *a_sw |= v == v;
[m[31m-                    }
[m                 }
[31m-            }
[m[31m-            (acc, sw)
[m[32m+                (acc, sw)
[m[32m+            };
[m[32m+        let group_plane = |o: usize| -> (Vec<f64>, Vec<bool>) {
[m[32m+            row_max(vec![init; inner], vec![false; inner], o * lane)
[m         };
[31m-        let group_plane =
[m[31m-            |o: usize| -> (Vec<f64>, Vec<bool>) { row_max(vec![init; inner], vec![false; inner], o * lane) };
[m         let mut out = vec![init; outer * inner];
         let mut saw = vec![false; outer * inner];
         if parallel && outer >= 2 {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:26150:
         if keepdims.unwrap_or(false) {
             let ndim = a.bind(py).getattr("ndim")?.extract::<usize>()?;
             let arr = numpy.call_method1("asarray", (out.bind(py),))?;
[31m-            let reshaped =
[m[31m-                arr.call_method1("reshape", (PyTuple::new(py, vec![1usize; ndim])?,))?;
[m[32m+            let reshaped = arr.call_method1("reshape", (PyTuple::new(py, vec![1usize; ndim])?,))?;
[m             return Ok(reshaped.unbind());
         }
         return Ok(out);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:26160:
     // than the extract → native scan; all-NaN/±0 lanes defer to numpy).
     if let Some(axis_val) = axis.as_ref()
         && !axis_val.bind(py).is_none()
[31m-        && let Some(out) = try_zerocopy_f64_nanextreme_axis(py, a.bind(py), axis_val.bind(py), true)?
[m[32m+        && let Some(out) =
[m[32m+            try_zerocopy_f64_nanextreme_axis(py, a.bind(py), axis_val.bind(py), true)?
[m     {
         if keepdims.unwrap_or(false) {
             let ndim = a.bind(py).getattr("ndim")?.extract::<usize>()?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:26184:
     }
     // Multi-axis (tuple) reductions extract the whole array then fall back to numpy
     // on the axis re-parse below (wasteful 8x bool-bridge / f64 copy). Delegate up front.
[31m-    if axis.as_ref().is_some_and(|ax| ax.bind(py).cast::<PyTuple>().is_ok()) {
[m[32m+    if axis
[m[32m+        .as_ref()
[m[32m+        .is_some_and(|ax| ax.bind(py).cast::<PyTuple>().is_ok())
[m[32m+    {
[m         return fallback();
     }
     let a = match extract_numeric_array(py, a.bind(py), "nanmax(a)") {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:26255:
         if keepdims.unwrap_or(false) {
             let ndim = a.bind(py).getattr("ndim")?.extract::<usize>()?;
             let arr = numpy.call_method1("asarray", (out.bind(py),))?;
[31m-            let reshaped =
[m[31m-                arr.call_method1("reshape", (PyTuple::new(py, vec![1usize; ndim])?,))?;
[m[32m+            let reshaped = arr.call_method1("reshape", (PyTuple::new(py, vec![1usize; ndim])?,))?;
[m             return Ok(reshaped.unbind());
         }
         return Ok(out);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:26265:
     // than the extract → native scan; all-NaN/±0 lanes defer to numpy).
     if let Some(axis_val) = axis.as_ref()
         && !axis_val.bind(py).is_none()
[31m-        && let Some(out) = try_zerocopy_f64_nanextreme_axis(py, a.bind(py), axis_val.bind(py), false)?
[m[32m+        && let Some(out) =
[m[32m+            try_zerocopy_f64_nanextreme_axis(py, a.bind(py), axis_val.bind(py), false)?
[m     {
         if keepdims.unwrap_or(false) {
             let ndim = a.bind(py).getattr("ndim")?.extract::<usize>()?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:26289:
     }
     // Multi-axis (tuple) reductions extract the whole array then fall back to numpy
     // on the axis re-parse below (wasteful 8x bool-bridge / f64 copy). Delegate up front.
[31m-    if axis.as_ref().is_some_and(|ax| ax.bind(py).cast::<PyTuple>().is_ok()) {
[m[32m+    if axis
[m[32m+        .as_ref()
[m[32m+        .is_some_and(|ax| ax.bind(py).cast::<PyTuple>().is_ok())
[m[32m+    {
[m         return fallback();
     }
     let a = match extract_numeric_array(py, a.bind(py), "nanmin(a)") {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:26416:
     }
     // Multi-axis (tuple) reductions extract the whole array then fall back to numpy
     // on the axis re-parse below (wasteful 8x bool-bridge / f64 copy). Delegate up front.
[31m-    if axis.as_ref().is_some_and(|ax| ax.bind(py).cast::<PyTuple>().is_ok()) {
[m[32m+    if axis
[m[32m+        .as_ref()
[m[32m+        .is_some_and(|ax| ax.bind(py).cast::<PyTuple>().is_ok())
[m[32m+    {
[m         return fallback();
     }
     let a = match extract_numeric_array(py, a.bind(py), "nanstd(a)") {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:26557:
     }
     // Multi-axis (tuple) reductions extract the whole array then fall back to numpy
     // on the axis re-parse below (wasteful 8x bool-bridge / f64 copy). Delegate up front.
[31m-    if axis.as_ref().is_some_and(|ax| ax.bind(py).cast::<PyTuple>().is_ok()) {
[m[32m+    if axis
[m[32m+        .as_ref()
[m[32m+        .is_some_and(|ax| ax.bind(py).cast::<PyTuple>().is_ok())
[m[32m+    {
[m         return fallback();
     }
     let a = match extract_numeric_array(py, a.bind(py), "nanvar(a)") {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:26604:
     if !a.is_exact_instance(&numpy.getattr("ndarray")?) || !numpy_dtype_is_f64(py, a) {
         return Ok(None);
     }
[31m-    if !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()? {
[m[32m+    if !a
[m[32m+        .getattr("flags")?
[m[32m+        .getattr("c_contiguous")?
[m[32m+        .extract::<bool>()?
[m[32m+    {
[m         return Ok(None);
     }
     let Ok(buffer) = PyBuffer::<f64>::get(a) else {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:27433:
     if !matches!(
         parsed,
         Some(
[31m-            DType::I8
[m[31m-                | DType::U8
[m[31m-                | DType::I16
[m[31m-                | DType::U16
[m[31m-                | DType::I32
[m[31m-                | DType::U32
[m[31m-                | DType::I64
[m[32m+            DType::I8 | DType::U8 | DType::I16 | DType::U16 | DType::I32 | DType::U32 | DType::I64
[m         )
     ) {
         return Ok(None);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:27789:
     }
     // Fast hashed-set membership for matched integer dtypes — runs BEFORE the cold
     // extract→UFuncArray path (~25x slower) so common id-membership skips it.
[31m-    if let Some(out) = try_zerocopy_int_isin(py, element.bind(py), test_elements.bind(py), invert)? {
[m[32m+    if let Some(out) = try_zerocopy_int_isin(py, element.bind(py), test_elements.bind(py), invert)?
[m[32m+    {
[m         return Ok(out);
     }
     let ar1 = match extract_precise_numeric_array(py, element.bind(py), "isin(element)") {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:28998:
         Ok(Some(flat.unbind()))
     } else {
         let shape_tuple = PyTuple::new(py, shape.iter().copied())?;
[31m-        Ok(Some(flat.call_method1("reshape", (&shape_tuple,))?.unbind()))
[m[32m+        Ok(Some(
[m[32m+            flat.call_method1("reshape", (&shape_tuple,))?.unbind(),
[m[32m+        ))
[m     }
 }

Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:29461:
         // straight off the borrowed buffers with a rayon-parallel atan2, skipping the
         // extract_numeric_array copies (the serial extract+build marshalling, not the
         // atan2, dominated the old path — ~26ms of a 47ms 1M call).
[31m-        if let Some(out) =
[m[31m-            try_zerocopy_f64_binary(py, &args.get_item(0)?, &args.get_item(1)?, BinaryOp::Arctan2)?
[m[31m-        {
[m[32m+        if let Some(out) = try_zerocopy_f64_binary(
[m[32m+            py,
[m[32m+            &args.get_item(0)?,
[m[32m+            &args.get_item(1)?,
[m[32m+            BinaryOp::Arctan2,
[m[32m+        )? {
[m             return Ok(out);
         }
         let numpy = py.import("numpy")?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:30224:
         }
     }
     let output_shape = PyTuple::new(py, shape.iter().copied())?;
[31m-    Ok(Some(flat.call_method1("reshape", (&output_shape,))?.unbind()))
[m[32m+    Ok(Some(
[m[32m+        flat.call_method1("reshape", (&output_shape,))?.unbind(),
[m[32m+    ))
[m }

 // Dispatch the narrow-width integer shift across i8/i16/i32 and u8/u16/u32/u64.
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:30247:
     let itemsize = dtype.getattr("itemsize")?.extract::<usize>()?;
     match (kind.as_str(), itemsize, is_left) {
         ("i", 1, true) => shift_typed_nw::<i8, _>(py, &numpy, a, b, "int8", "i", 1, |a, b| {
[31m-            if (0..8).contains(&b) { a.wrapping_shl(b as u32) } else { 0 }
[m[32m+            if (0..8).contains(&b) {
[m[32m+                a.wrapping_shl(b as u32)
[m[32m+            } else {
[m[32m+                0
[m[32m+            }
[m         }),
         ("i", 1, false) => shift_typed_nw::<i8, _>(py, &numpy, a, b, "int8", "i", 1, |a, b| {
[31m-            if (0..8).contains(&b) { a.wrapping_shr(b as u32) } else if a < 0 { -1 } else { 0 }
[m[32m+            if (0..8).contains(&b) {
[m[32m+                a.wrapping_shr(b as u32)
[m[32m+            } else if a < 0 {
[m[32m+                -1
[m[32m+            } else {
[m[32m+                0
[m[32m+            }
[m         }),
         ("i", 2, true) => shift_typed_nw::<i16, _>(py, &numpy, a, b, "int16", "i", 2, |a, b| {
[31m-            if (0..16).contains(&b) { a.wrapping_shl(b as u32) } else { 0 }
[m[32m+            if (0..16).contains(&b) {
[m[32m+                a.wrapping_shl(b as u32)
[m[32m+            } else {
[m[32m+                0
[m[32m+            }
[m         }),
         ("i", 2, false) => shift_typed_nw::<i16, _>(py, &numpy, a, b, "int16", "i", 2, |a, b| {
[31m-            if (0..16).contains(&b) { a.wrapping_shr(b as u32) } else if a < 0 { -1 } else { 0 }
[m[32m+            if (0..16).contains(&b) {
[m[32m+                a.wrapping_shr(b as u32)
[m[32m+            } else if a < 0 {
[m[32m+                -1
[m[32m+            } else {
[m[32m+                0
[m[32m+            }
[m         }),
         ("i", 4, true) => shift_typed_nw::<i32, _>(py, &numpy, a, b, "int32", "i", 4, |a, b| {
[31m-            if (0..32).contains(&b) { a.wrapping_shl(b as u32) } else { 0 }
[m[32m+            if (0..32).contains(&b) {
[m[32m+                a.wrapping_shl(b as u32)
[m[32m+            } else {
[m[32m+                0
[m[32m+            }
[m         }),
         ("i", 4, false) => shift_typed_nw::<i32, _>(py, &numpy, a, b, "int32", "i", 4, |a, b| {
[31m-            if (0..32).contains(&b) { a.wrapping_shr(b as u32) } else if a < 0 { -1 } else { 0 }
[m[32m+            if (0..32).contains(&b) {
[m[32m+                a.wrapping_shr(b as u32)
[m[32m+            } else if a < 0 {
[m[32m+                -1
[m[32m+            } else {
[m[32m+                0
[m[32m+            }
[m         }),
         ("u", 1, true) => shift_typed_nw::<u8, _>(py, &numpy, a, b, "uint8", "u", 1, |a, b| {
             if b < 8 { a.wrapping_shl(b as u32) } else { 0 }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:30516:
 ) -> PyResult<Py<PyAny>> {
     // Bare call → native fast path; out=/where=/dtype= etc → numpy passthrough (full
     // ufunc kwarg surface, matching np.square which accepts out=).
[31m-    native_unary_elementwise_or_passthrough(py, args, kwargs, UnaryOp::Square, "square", "square(x)")
[m[32m+    native_unary_elementwise_or_passthrough(
[m[32m+        py,
[m[32m+        args,
[m[32m+        kwargs,
[m[32m+        UnaryOp::Square,
[m[32m+        "square",
[m[32m+        "square(x)",
[m[32m+    )
[m }

 #[pyfunction]
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:30978:
     a2: &Bound<'_, PyAny>,
 ) -> PyResult<Option<bool>> {
     let u8t = numpy.getattr("uint8")?;
[31m-    let (Ok(v1), Ok(v2)) = (a1.call_method1("view", (&u8t,)), a2.call_method1("view", (&u8t,)))
[m[31m-    else {
[m[32m+    let (Ok(v1), Ok(v2)) = (
[m[32m+        a1.call_method1("view", (&u8t,)),
[m[32m+        a2.call_method1("view", (&u8t,)),
[m[32m+    ) else {
[m         return Ok(None);
     };
     let (Ok(b1), Ok(b2)) = (PyBuffer::<u8>::get(&v1), PyBuffer::<u8>::get(&v2)) else {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:30991:
     let (Some(s1), Some(s2)) = (b1.as_slice(py), b2.as_slice(py)) else {
         return Ok(None);
     };
[31m-    Ok(Some(s1.iter().zip(s2.iter()).all(|(x, y)| x.get() == y.get())))
[m[32m+    Ok(Some(
[m[32m+        s1.iter().zip(s2.iter()).all(|(x, y)| x.get() == y.get()),
[m[32m+    ))
[m }

 // Zero-copy np.array_equal for two SAME-dtype numeric ndarrays (int/uint/bool of
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:32595:
     let ndarray_type = numpy.getattr("ndarray")?;
     let a_arr = a.is_exact_instance(&ndarray_type) && numpy_dtype_is_f64(py, a);
     let b_arr = b.is_exact_instance(&ndarray_type) && numpy_dtype_is_f64(py, b);
[31m-    let a_buf = if a_arr { PyBuffer::<f64>::get(a).ok() } else { None };
[m[31m-    let b_buf = if b_arr { PyBuffer::<f64>::get(b).ok() } else { None };
[m[31m-    let a_sc = if a_buf.is_none() { a.extract::<f64>().ok() } else { None };
[m[31m-    let b_sc = if b_buf.is_none() { b.extract::<f64>().ok() } else { None };
[m[32m+    let a_buf = if a_arr {
[m[32m+        PyBuffer::<f64>::get(a).ok()
[m[32m+    } else {
[m[32m+        None
[m[32m+    };
[m[32m+    let b_buf = if b_arr {
[m[32m+        PyBuffer::<f64>::get(b).ok()
[m[32m+    } else {
[m[32m+        None
[m[32m+    };
[m[32m+    let a_sc = if a_buf.is_none() {
[m[32m+        a.extract::<f64>().ok()
[m[32m+    } else {
[m[32m+        None
[m[32m+    };
[m[32m+    let b_sc = if b_buf.is_none() {
[m[32m+        b.extract::<f64>().ok()
[m[32m+    } else {
[m[32m+        None
[m[32m+    };
[m     let verdict = match (&a_buf, &b_buf) {
         (Some(ab), Some(bb)) => {
             if ab.shape() != bb.shape() {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:32661:
     if !a_arr && !b_arr {
         return Ok(None); // need at least one f32 array
     }
[31m-    let a_buf = if a_arr { PyBuffer::<f32>::get(a).ok() } else { None };
[m[31m-    let b_buf = if b_arr { PyBuffer::<f32>::get(b).ok() } else { None };
[m[31m-    let a_sc = if a_buf.is_none() { a.extract::<f64>().ok() } else { None };
[m[31m-    let b_sc = if b_buf.is_none() { b.extract::<f64>().ok() } else { None };
[m[32m+    let a_buf = if a_arr {
[m[32m+        PyBuffer::<f32>::get(a).ok()
[m[32m+    } else {
[m[32m+        None
[m[32m+    };
[m[32m+    let b_buf = if b_arr {
[m[32m+        PyBuffer::<f32>::get(b).ok()
[m[32m+    } else {
[m[32m+        None
[m[32m+    };
[m[32m+    let a_sc = if a_buf.is_none() {
[m[32m+        a.extract::<f64>().ok()
[m[32m+    } else {
[m[32m+        None
[m[32m+    };
[m[32m+    let b_sc = if b_buf.is_none() {
[m[32m+        b.extract::<f64>().ok()
[m[32m+    } else {
[m[32m+        None
[m[32m+    };
[m     let verdict = match (&a_buf, &b_buf) {
         (Some(ab), Some(bb)) => {
             if ab.shape() != bb.shape() {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:32673:
             let (Some(sa), Some(sb)) = (ab.as_slice(py), bb.as_slice(py)) else {
                 return Ok(None);
             };
[31m-            sa.iter().zip(sb.iter()).all(|(x, y)| {
[m[31m-                allclose_pair(x.get() as f64, y.get() as f64, rtol, atol, equal_nan)
[m[31m-            })
[m[32m+            sa.iter()
[m[32m+                .zip(sb.iter())
[m[32m+                .all(|(x, y)| allclose_pair(x.get() as f64, y.get() as f64, rtol, atol, equal_nan))
[m         }
         (Some(ab), None) => {
             let Some(bs) = b_sc else { return Ok(None) };
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:35474:
         return Ok(None);
     }
     // SAFETY: ReadOnlyCell<u8> is repr(transparent) over u8; read-only under the GIL.
[31m-    let in_data: &[u8] = unsafe { std::slice::from_raw_parts(in_cells.as_ptr().cast::<u8>(), nbytes) };
[m[32m+    let in_data: &[u8] =
[m[32m+        unsafe { std::slice::from_raw_parts(in_cells.as_ptr().cast::<u8>(), nbytes) };
[m     let total = before + n + after;
     let out_kwargs = PyDict::new(py);
     out_kwargs.set_item("dtype", &dtype)?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:35517:
     // numpy surface is exposed.
     let numpy = py.import("numpy")?;
     // Native 1-D constant-mode fast path (bypasses np.pad's ~9us Python dispatch).
[31m-    if let Some(out) =
[m[31m-        try_zerocopy_f64_pad_1d_constant(py, &numpy, array.bind(py), pad_width.bind(py), mode, kwargs)?
[m[31m-    {
[m[32m+    if let Some(out) = try_zerocopy_f64_pad_1d_constant(
[m[32m+        py,
[m[32m+        &numpy,
[m[32m+        array.bind(py),
[m[32m+        pad_width.bind(py),
[m[32m+        mode,
[m[32m+        kwargs,
[m[32m+    )? {
[m         return Ok(out);
     }
     // Same fast path for non-f64 numeric dtypes (f32/int*/complex/bool) via a byte copy.
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:35526:
[31m-    if let Some(out) =
[m[31m-        try_zerocopy_pad_bytes_1d_constant(py, &numpy, array.bind(py), pad_width.bind(py), mode, kwargs)?
[m[31m-    {
[m[32m+    if let Some(out) = try_zerocopy_pad_bytes_1d_constant(
[m[32m+        py,
[m[32m+        &numpy,
[m[32m+        array.bind(py),
[m[32m+        pad_width.bind(py),
[m[32m+        mode,
[m[32m+        kwargs,
[m[32m+    )? {
[m         return Ok(out);
     }
     let call_kwargs = PyDict::new(py);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:35612:
     let svd_mode: Option<u8> = ord.as_ref().and_then(|ord_val| {
         let bound = ord_val.bind(py);
         if let Ok(text) = bound.extract::<String>() {
[31m-            if text == "nuc" {
[m[31m-                Some(2)
[m[31m-            } else {
[m[31m-                None
[m[31m-            }
[m[32m+            if text == "nuc" { Some(2) } else { None }
[m         } else if let Ok(f) = bound.extract::<f64>() {
             if f == 2.0 {
                 Some(0)
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:35659:
                             out_shape.push(1);
                             out_shape.push(1);
                         }
[31m-                        let result = UFuncArray::new(out_shape, out, DType::F64)
[m[31m-                            .map_err(map_ufunc_error)?;
[m[32m+                        let result =
[m[32m+                            UFuncArray::new(out_shape, out, DType::F64).map_err(map_ufunc_error)?;
[m                         return build_numpy_array_from_ufunc(py, &result);
                     }
                 }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:36239:
         let out_shape: Vec<usize> = shape[..ax].to_vec();
         let avg: Vec<f64> = pairs.iter().map(|(n, d)| n / d).collect();
         let avg_flat = numpy_array_from_slice(py, &numpy, &avg, "float64")?;
[31m-        let avg_arr = avg_flat
[m[31m-            .call_method1("reshape", (PyTuple::new(py, out_shape.iter().copied())?,))?;
[m[32m+        let avg_arr =
[m[32m+            avg_flat.call_method1("reshape", (PyTuple::new(py, out_shape.iter().copied())?,))?;
[m         let avg_output = if out_shape.is_empty() {
             avg_arr.get_item(())?
         } else {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:36257:
                 sow_arr
             };
             return Ok(Some(
[31m-                PyTuple::new(py, [avg_output, sow_output])?.into_any().unbind(),
[m[32m+                PyTuple::new(py, [avg_output, sow_output])?
[m[32m+                    .into_any()
[m[32m+                    .unbind(),
[m             ));
         }
         return Ok(Some(avg_output.unbind()));
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:36802:
         if let Ok(values) = fnp_linalg::batch_svd(x.values(), &owned_shape) {
             let mut out_shape: Vec<usize> = owned_shape[..owned_shape.len() - 2].to_vec();
             out_shape.push(k);
[31m-            let result =
[m[31m-                UFuncArray::new(out_shape, values, DType::F64).map_err(map_ufunc_error)?;
[m[32m+            let result = UFuncArray::new(out_shape, values, DType::F64).map_err(map_ufunc_error)?;
[m             return build_numpy_array_from_ufunc(py, &result);
         }
         return fallback();
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:37271:
         return Ok(None);
     }
     if a.getattr("ndim")?.extract::<usize>()? != 1
[31m-        || !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()?
[m[32m+        || !a
[m[32m+            .getattr("flags")?
[m[32m+            .getattr("c_contiguous")?
[m[32m+            .extract::<bool>()?
[m     {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:37326:
             Some(kw) => kw.iter().all(|(k, v)| {
                 k.extract::<String>().ok().as_deref() == Some("axis")
                     && (v.is_none()
[31m-                        || v.extract::<isize>().map(|x| x == -1 || x == 0).unwrap_or(false))
[m[32m+                        || v.extract::<isize>()
[m[32m+                            .map(|x| x == -1 || x == 0)
[m[32m+                            .unwrap_or(false))
[m             }),
         };
         if axis_ok {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:37355:
         return Ok(None);
     }
     if a.getattr("ndim")?.extract::<usize>()? != 1
[31m-        || !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()?
[m[32m+        || !a
[m[32m+            .getattr("flags")?
[m[32m+            .getattr("c_contiguous")?
[m[32m+            .extract::<bool>()?
[m     {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:37384:
     // SAFETY: fresh numpy.empty intp buffer we own (no alias).
     let perm: &mut [i64] =
         unsafe { std::slice::from_raw_parts_mut(out_cells.as_ptr() as *mut i64, n) };
[31m-    perm.par_iter_mut().enumerate().for_each(|(i, s)| *s = i as i64);
[m[32m+    perm.par_iter_mut()
[m[32m+        .enumerate()
[m[32m+        .for_each(|(i, s)| *s = i as i64);
[m     // No NaN -> partial_cmp is a total order on the values.
     perm.par_sort_unstable_by(|&x, &y| {
         data[x as usize]
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:37417:
             Some(kw) => kw.iter().all(|(k, v)| {
                 k.extract::<String>().ok().as_deref() == Some("axis")
                     && (v.is_none()
[31m-                        || v.extract::<isize>().map(|x| x == -1 || x == 0).unwrap_or(false))
[m[32m+                        || v.extract::<isize>()
[m[32m+                            .map(|x| x == -1 || x == 0)
[m[32m+                            .unwrap_or(false))
[m             }),
         };
         if axis_ok {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:37439:
         return Ok(None);
     }
     if a.getattr("ndim")?.extract::<usize>()? != 1
[31m-        || !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()?
[m[32m+        || !a
[m[32m+            .getattr("flags")?
[m[32m+            .getattr("c_contiguous")?
[m[32m+            .extract::<bool>()?
[m     {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:38087:
             && let Ok(mask) = arr.bind(py).getattr("mask")
             && mask.is_instance(&numpy.getattr("ndarray")?)?
             && let (Ok(mshape), Ok(dshape)) = (
[31m-                mask.getattr("shape").and_then(|s| s.extract::<Vec<usize>>()),
[m[31m-                arr.bind(py).getattr("shape").and_then(|s| s.extract::<Vec<usize>>()),
[m[32m+                mask.getattr("shape")
[m[32m+                    .and_then(|s| s.extract::<Vec<usize>>()),
[m[32m+                arr.bind(py)
[m[32m+                    .getattr("shape")
[m[32m+                    .and_then(|s| s.extract::<Vec<usize>>()),
[m             )
             && mshape == dshape
[31m-            && mask.getattr("dtype")?.getattr("kind")?.extract::<String>()? == "b"
[m[32m+            && mask
[m[32m+                .getattr("dtype")?
[m[32m+                .getattr("kind")?
[m[32m+                .extract::<String>()?
[m[32m+                == "b"
[m         {
             return Ok(mask.unbind());
         }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:38217:
     if !numpy_dtype_is_f64(py, &data_obj) {
         return Ok(None);
     }
[31m-    let Ok(shape) = value.getattr("shape").and_then(|s| s.extract::<Vec<usize>>()) else {
[m[32m+    let Ok(shape) = value
[m[32m+        .getattr("shape")
[m[32m+        .and_then(|s| s.extract::<Vec<usize>>())
[m[32m+    else {
[m         return Ok(None);
     };
     let total: usize = shape.iter().product();
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:38484:
             .and_then(|s| s.extract::<Vec<usize>>())
         && shape.len() == 2
         && shape[0] == shape[1]
[31m-        && a
[m[31m-            .bind(py)
[m[32m+        && a.bind(py)
[m             .getattr("dtype")
             .and_then(|d| d.getattr("kind"))
             .and_then(|k| k.extract::<String>())
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:38544:
             val_shape.push(n);
             let eval_arr =
                 UFuncArray::new(val_shape, eigenvalues, DType::F64).map_err(map_ufunc_error)?;
[31m-            let evec_arr = UFuncArray::new(owned_shape, eigenvectors, DType::F64)
[m[31m-                .map_err(map_ufunc_error)?;
[m[32m+            let evec_arr =
[m[32m+                UFuncArray::new(owned_shape, eigenvectors, DType::F64).map_err(map_ufunc_error)?;
[m             return build_numpy_eigh_result(py, &eval_arr, &evec_arr);
         }
         return fallback();
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:39728:
                             // all-zero / fully singular matrix) to +Inf whenever the
                             // input had no NaN entries. Our gate already requires
                             // all-finite input, so every NaN here becomes Inf.
[31m-                            out.push(if ratio.is_nan() {
[m[31m-                                f64::INFINITY
[m[31m-                            } else {
[m[31m-                                ratio
[m[31m-                            });
[m[32m+                            out.push(if ratio.is_nan() { f64::INFINITY } else { ratio });
[m                         }
[31m-                        let out_shape: Vec<usize> =
[m[31m-                            owned_shape[..owned_shape.len() - 2].to_vec();
[m[31m-                        let result = UFuncArray::new(out_shape, out, DType::F64)
[m[31m-                            .map_err(map_ufunc_error)?;
[m[32m+                        let out_shape: Vec<usize> = owned_shape[..owned_shape.len() - 2].to_vec();
[m[32m+                        let result =
[m[32m+                            UFuncArray::new(out_shape, out, DType::F64).map_err(map_ufunc_error)?;
[m                         return build_numpy_array_from_ufunc(py, &result);
                     }
                 }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:39757:
 fn svd_matrix_norm_mode(py: Python<'_>, ord: &Py<PyAny>) -> Option<u8> {
     let bound = ord.bind(py);
     if let Ok(text) = bound.extract::<String>() {
[31m-        if text == "nuc" {
[m[31m-            Some(2)
[m[31m-        } else {
[m[31m-            None
[m[31m-        }
[m[32m+        if text == "nuc" { Some(2) } else { None }
[m     } else if let Ok(f) = bound.extract::<f64>() {
         if f == 2.0 {
             Some(0)
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:41345:
     };
     let restored = flat.call_method1("view", (numpy.getattr(orig_name.as_str())?,))?;
     let output_shape = PyTuple::new(py, s_idx.iter().copied())?;
[31m-    Ok(Some(restored.call_method1("reshape", (&output_shape,))?.unbind()))
[m[32m+    Ok(Some(
[m[32m+        restored.call_method1("reshape", (&output_shape,))?.unbind(),
[m[32m+    ))
[m }

 #[pyfunction]
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:41392:
     // indices); covers int/float/bool via a uintN bit-view. Skips the cold extract +
     // rebuild. Bit-identical; broadcast, axis=None, OOB indices, and other layouts
     // fall through.
[31m-    if let Some(out) =
[m[31m-        try_zerocopy_take_along_axis(py, arr.bind(py), indices.bind(py), axis)?
[m[31m-    {
[m[32m+    if let Some(out) = try_zerocopy_take_along_axis(py, arr.bind(py), indices.bind(py), axis)? {
[m         return Ok(out);
     }

Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:41430:
     // (same module, same getattr+call). Large-array ops are unaffected (amortized).
     static NUMPY_MODULE: PyOnceLock<Py<PyModule>> = PyOnceLock::new();
     let numpy = NUMPY_MODULE
[31m-        .get_or_try_init(py, || -> PyResult<Py<PyModule>> { Ok(py.import("numpy")?.unbind()) })?
[m[32m+        .get_or_try_init(py, || -> PyResult<Py<PyModule>> {
[m[32m+            Ok(py.import("numpy")?.unbind())
[m[32m+        })?
[m         .bind(py);
     Ok(numpy.getattr(name)?.call(args, kwargs)?.unbind())
 }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:42178:
         return Ok(Some(zerod.get_item(())?.unbind()));
     }
     let output_shape = PyTuple::new(py, out_shape.iter().copied())?;
[31m-    let output = flat_bool.call_method1("reshape", (&output_shape,))?.unbind();
[m[32m+    let output = flat_bool
[m[32m+        .call_method1("reshape", (&output_shape,))?
[m[32m+        .unbind();
[m     Ok(Some(output))
 }

Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:42801:
     // scalar fold. Delegate (axis=None bool and f64 stay native).
     if axis_val.is_some()
         && a.bind(py).is_exact_instance(&numpy.getattr("ndarray")?)
[31m-        && a.bind(py).getattr("dtype")?.getattr("kind")?.extract::<String>()? == "b"
[m[32m+        && a.bind(py)
[m[32m+            .getattr("dtype")?
[m[32m+            .getattr("kind")?
[m[32m+            .extract::<String>()?
[m[32m+            == "b"
[m     {
         return fallback();
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:42895:
     // scalar fold. Delegate (axis=None bool and f64 stay native).
     if axis_val.is_some()
         && a.bind(py).is_exact_instance(&numpy.getattr("ndarray")?)
[31m-        && a.bind(py).getattr("dtype")?.getattr("kind")?.extract::<String>()? == "b"
[m[32m+        && a.bind(py)
[m[32m+            .getattr("dtype")?
[m[32m+            .getattr("kind")?
[m[32m+            .extract::<String>()?
[m[32m+            == "b"
[m     {
         return fallback();
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:43572:
     if !a.is_exact_instance(&ndarray_type) {
         return Ok(None);
     }
[31m-    if !a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()? {
[m[32m+    if !a
[m[32m+        .getattr("flags")?
[m[32m+        .getattr("c_contiguous")?
[m[32m+        .extract::<bool>()?
[m[32m+    {
[m         return Ok(None);
     }
     let shape: Vec<usize> = a.getattr("shape")?.extract()?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:43620:
         // beats numpy) and 4M is ~8x. Gate at 1<<20. Lane scans are independent ->
         // serial/parallel bit-identical.
         const ARGEXTREME_LASTAXIS_PARALLEL_MIN: usize = 1 << 20;
[31m-        let parallel = outer * lane >= ARGEXTREME_LASTAXIS_PARALLEL_MIN
[m[31m-            && rayon::current_num_threads() >= 2;
[m[32m+        let parallel =
[m[32m+            outer * lane >= ARGEXTREME_LASTAXIS_PARALLEL_MIN && rayon::current_num_threads() >= 2;
[m         let per: Vec<Option<usize>> = if parallel {
             data.par_chunks_exact(lane)
                 .map(|l| simd_argextreme_f64(l, take_max))
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:44367:
     // the result, so defer those.
     if let Some(kw) = kwargs {
         for key in kw.keys() {
[31m-            if key.extract::<String>().map(|k| k != "optimize").unwrap_or(true) {
[m[32m+            if key
[m[32m+                .extract::<String>()
[m[32m+                .map(|k| k != "optimize")
[m[32m+                .unwrap_or(true)
[m[32m+            {
[m                 return Ok(None);
             }
         }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:44380:
     };
     let inb: Vec<char> = inp.trim().chars().collect();
     let oub: Vec<char> = outp.trim().chars().collect();
[31m-    let distinct = |s: &[char]| {
[m[31m-        s.iter().collect::<std::collections::HashSet<_>>().len() == s.len()
[m[31m-    };
[m[32m+    let distinct = |s: &[char]| s.iter().collect::<std::collections::HashSet<_>>().len() == s.len();
[m     // Must be a permutation of ALL labels: same length, all alphabetic, both sides
     // distinct, and same label set.
     if inb.is_empty()
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:44410:
         .iter()
         .map(|c| inb.iter().position(|x| x == c).unwrap())
         .collect();
[31m-    Ok(Some(
[m[31m-        operand.call_method1("transpose", (perm,))?.unbind(),
[m[31m-    ))
[m[32m+    Ok(Some(operand.call_method1("transpose", (perm,))?.unbind()))
[m }

 #[pyfunction]
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:44992:
     let outc: Vec<char> = out.chars().collect();
     if !labels.iter().all(|c| c.is_ascii_alphabetic())
         || outc.len() >= labels.len()
[31m-        || labels.iter().collect::<std::collections::HashSet<_>>().len() != labels.len()
[m[32m+        || labels
[m[32m+            .iter()
[m[32m+            .collect::<std::collections::HashSet<_>>()
[m[32m+            .len()
[m[32m+            != labels.len()
[m     {
         return Ok(None);
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:45035:
         return Ok(None);
     }
     // Contiguous-block 8-accumulator dot — bit-stable per block, allclose vs numpy.
[31m-    let block_dot = |ra: &[pyo3::buffer::ReadOnlyCell<f64>],
[m[31m-                     rb: &[pyo3::buffer::ReadOnlyCell<f64>]|
[m[31m-     -> f64 {
[m[31m-        let mut acc = [0.0f64; 8];
[m[31m-        let mut ca = ra.chunks_exact(8);
[m[31m-        let mut cb = rb.chunks_exact(8);
[m[31m-        for (ga, gb) in ca.by_ref().zip(cb.by_ref()) {
[m[31m-            acc[0] += ga[0].get() * gb[0].get();
[m[31m-            acc[1] += ga[1].get() * gb[1].get();
[m[31m-            acc[2] += ga[2].get() * gb[2].get();
[m[31m-            acc[3] += ga[3].get() * gb[3].get();
[m[31m-            acc[4] += ga[4].get() * gb[4].get();
[m[31m-            acc[5] += ga[5].get() * gb[5].get();
[m[31m-            acc[6] += ga[6].get() * gb[6].get();
[m[31m-            acc[7] += ga[7].get() * gb[7].get();
[m[31m-        }
[m[31m-        let mut s = acc.iter().sum::<f64>();
[m[31m-        for (x, y) in ca.remainder().iter().zip(cb.remainder()) {
[m[31m-            s += x.get() * y.get();
[m[31m-        }
[m[31m-        s
[m[31m-    };
[m[32m+    let block_dot =
[m[32m+        |ra: &[pyo3::buffer::ReadOnlyCell<f64>], rb: &[pyo3::buffer::ReadOnlyCell<f64>]| -> f64 {
[m[32m+            let mut acc = [0.0f64; 8];
[m[32m+            let mut ca = ra.chunks_exact(8);
[m[32m+            let mut cb = rb.chunks_exact(8);
[m[32m+            for (ga, gb) in ca.by_ref().zip(cb.by_ref()) {
[m[32m+                acc[0] += ga[0].get() * gb[0].get();
[m[32m+                acc[1] += ga[1].get() * gb[1].get();
[m[32m+                acc[2] += ga[2].get() * gb[2].get();
[m[32m+                acc[3] += ga[3].get() * gb[3].get();
[m[32m+                acc[4] += ga[4].get() * gb[4].get();
[m[32m+                acc[5] += ga[5].get() * gb[5].get();
[m[32m+                acc[6] += ga[6].get() * gb[6].get();
[m[32m+                acc[7] += ga[7].get() * gb[7].get();
[m[32m+            }
[m[32m+            let mut s = acc.iter().sum::<f64>();
[m[32m+            for (x, y) in ca.remainder().iter().zip(cb.remainder()) {
[m[32m+                s += x.get() * y.get();
[m[32m+            }
[m[32m+            s
[m[32m+        };
[m     let (out_values, out_shape): (Vec<f64>, Vec<usize>) = if is_prefix {
         // Keep leading outc.len() axes; sum the trailing (contiguous) block.
         let n_groups: usize = sa[..outc.len()].iter().product();
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:45063:
         let block = if n_groups == 0 { 0 } else { total / n_groups };
         let vals = (0..n_groups)
[31m-            .map(|g| block_dot(&va[g * block..(g + 1) * block], &vb[g * block..(g + 1) * block]))
[m[32m+            .map(|g| {
[m[32m+                block_dot(
[m[32m+                    &va[g * block..(g + 1) * block],
[m[32m+                    &vb[g * block..(g + 1) * block],
[m[32m+                )
[m[32m+            })
[m             .collect();
         (vals, sa[..outc.len()].to_vec())
     } else {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:45197:
     ) else {
         return Ok(None);
     };
[31m-    let (nrows, ncols) = if sa.len() == 2 { (sa[0], sa[1]) } else { return Ok(None) };
[m[32m+    let (nrows, ncols) = if sa.len() == 2 {
[m[32m+        (sa[0], sa[1])
[m[32m+    } else {
[m[32m+        return Ok(None);
[m[32m+    };
[m     // Vector length must match the contracted axis: column→ncols, row→nrows.
     let want_vlen = if contract_col { ncols } else { nrows };
     if sb.len() != 1 || sb[0] != want_vlen {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:45237:
             s
         };
         if keep_axis {
[31m-            ((0..nrows).map(|i| row_dot(&va[i * ncols..(i + 1) * ncols])).collect(), vec![nrows])
[m[32m+            (
[m[32m+                (0..nrows)
[m[32m+                    .map(|i| row_dot(&va[i * ncols..(i + 1) * ncols]))
[m[32m+                    .collect(),
[m[32m+                vec![nrows],
[m[32m+            )
[m         } else {
             let mut total = 0.0f64;
             for i in 0..nrows {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:45312:
         return Ok(Some(result));
     }
     if args.len() == 2
[31m-        && let Some(result) = try_zerocopy_f64_einsum_single_reduction_2d(
[m[31m-            py,
[m[31m-            &subscripts,
[m[31m-            &args.get_item(1)?,
[m[31m-        )?
[m[32m+        && let Some(result) =
[m[32m+            try_zerocopy_f64_einsum_single_reduction_2d(py, &subscripts, &args.get_item(1)?)?
[m     {
         return Ok(Some(result));
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:45358:
     }
     // Two-operand matrix·vector contraction ('ij,j->i' / 'ij,j->'): zero-copy fused scan.
     if args.len() == 3
[31m-        && let Some(result) = try_zerocopy_f64_einsum_matvec(
[m[31m-            py,
[m[31m-            &subscripts,
[m[31m-            &args.get_item(1)?,
[m[31m-            &args.get_item(2)?,
[m[31m-        )?
[m[32m+        && let Some(result) =
[m[32m+            try_zerocopy_f64_einsum_matvec(py, &subscripts, &args.get_item(1)?, &args.get_item(2)?)?
[m     {
         return Ok(Some(result));
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:45398:
         let op = &operands[0];
         let inb: Vec<char> = inp.chars().collect();
         let oub: Vec<char> = outp.chars().collect();
[31m-        let distinct = |s: &[char]| {
[m[31m-            s.iter()
[m[31m-                .collect::<std::collections::HashSet<_>>()
[m[31m-                .len()
[m[31m-                == s.len()
[m[31m-        };
[m[32m+        let distinct =
[m[32m+            |s: &[char]| s.iter().collect::<std::collections::HashSet<_>>().len() == s.len();
[m         if op.dtype() == DType::F64
             && !inp.contains('.')
             && !outp.contains('.')
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:45463:
     if !item.is_exact_instance(&numpy.getattr("ndarray")?) || !numpy_dtype_is_f64(py, item) {
         return Ok(None);
     }
[31m-    if !item.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()? {
[m[32m+    if !item
[m[32m+        .getattr("flags")?
[m[32m+        .getattr("c_contiguous")?
[m[32m+        .extract::<bool>()?
[m[32m+    {
[m         return Ok(None);
     }
     let Ok(buffer) = PyBuffer::<f64>::get(item) else {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:45651:
 // (O(n+range) vs the sort path's O(n log n)). Gated to a C-contiguous integer
 // ndarray; dispatched by (kind, itemsize). Wide ranges / non-contiguous / empty
 // inputs return None so the caller keeps the existing sort path.
[31m-fn try_zerocopy_int_unique(
[m[31m-    py: Python<'_>,
[m[31m-    item: &Bound<'_, PyAny>,
[m[31m-) -> PyResult<Option<Py<PyAny>>> {
[m[32m+fn try_zerocopy_int_unique(py: Python<'_>, item: &Bound<'_, PyAny>) -> PyResult<Option<Py<PyAny>>> {
[m     let numpy = py.import("numpy")?;
     let ndarray_type = numpy.getattr("ndarray")?;
     if !item.is_exact_instance(&ndarray_type) {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:45756:
     }
     let size = range as usize + 1;
     let mut seen = vec![false; size];
[31m-    let mut first_idx = if want_index { vec![0i64; size] } else { Vec::new() };
[m[32m+    let mut first_idx = if want_index {
[m[32m+        vec![0i64; size]
[m[32m+    } else {
[m[32m+        Vec::new()
[m[32m+    };
[m     let mut counts = if want_counts || want_inverse {
         vec![0i64; size]
     } else {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:45778:
     let mut uvals: Vec<i128> = Vec::new();
     let mut idxv: Vec<i64> = Vec::new();
     let mut cntv: Vec<i64> = Vec::new();
[31m-    let mut rank = if want_inverse { vec![0i64; size] } else { Vec::new() };
[m[32m+    let mut rank = if want_inverse {
[m[32m+        vec![0i64; size]
[m[32m+    } else {
[m[32m+        Vec::new()
[m[32m+    };
[m     let mut w: i64 = 0;
     for (i, &b) in seen.iter().enumerate() {
         if b {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:46212:
     let kwargs = PyDict::new(py);
     kwargs.set_item("dtype", "uint8")?;
     let out = numpy.call_method("empty", (PyTuple::new(py, &shape)?,), Some(&kwargs))?;
[31m-    let out_buf =
[m[31m-        PyBuffer::<u8>::get(&out).map_err(|e| PyValueError::new_err(e.to_string()))?;
[m[32m+    let out_buf = PyBuffer::<u8>::get(&out).map_err(|e| PyValueError::new_err(e.to_string()))?;
[m     let Some(out_slice) = out_buf.as_mut_slice(py) else {
         return Ok(None);
     };
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:46787:
         return Ok(None);
     }
     let c_contig = |a: &Bound<'_, PyAny>| -> PyResult<bool> {
[31m-        a.getattr("flags")?.getattr("c_contiguous")?.extract::<bool>()
[m[32m+        a.getattr("flags")?
[m[32m+            .getattr("c_contiguous")?
[m[32m+            .extract::<bool>()
[m     };
     if !c_contig(x1)? || !c_contig(x2)? {
         return Ok(None);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:46877:
         return Ok(Some(PyTuple::new(py, [&qs, &rs])?.into_any().unbind()));
     }
     Ok(Some(
[31m-        PyTuple::new(py, [&quotient, &remainder])?.into_any().unbind(),
[m[32m+        PyTuple::new(py, [&quotient, &remainder])?
[m[32m+            .into_any()
[m[32m+            .unbind(),
[m     ))
 }

Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:47018:
         ("i", 1) => {
             sign_typed::<i8, _>(py, &numpy, a, "int8", |v| i8::from(v > 0) - i8::from(v < 0))?
         }
[31m-        ("i", 2) => {
[m[31m-            sign_typed::<i16, _>(py, &numpy, a, "int16", |v| i16::from(v > 0) - i16::from(v < 0))?
[m[31m-        }
[m[31m-        ("i", 4) => {
[m[31m-            sign_typed::<i32, _>(py, &numpy, a, "int32", |v| i32::from(v > 0) - i32::from(v < 0))?
[m[31m-        }
[m[32m+        ("i", 2) => sign_typed::<i16, _>(py, &numpy, a, "int16", |v| {
[m[32m+            i16::from(v > 0) - i16::from(v < 0)
[m[32m+        })?,
[m[32m+        ("i", 4) => sign_typed::<i32, _>(py, &numpy, a, "int32", |v| {
[m[32m+            i32::from(v > 0) - i32::from(v < 0)
[m[32m+        })?,
[m         ("i", 8) => sign_typed::<i64, _>(py, &numpy, a, "int64", |v| {
             i64::from(v > 0) - i64::from(v < 0)
         })?,
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:47201:
         let mul = |x: i64, y: i64| x.wrapping_mul(y);
         return match axis {
             None => prod_typed::<u8, i64, _, _>(py, &numpy, &viewed, "int64", 1, |v| v as i64, mul),
[31m-            Some(ax) => {
[m[31m-                prod_axis_typed::<u8, i64, _, _>(py, &numpy, &viewed, "int64", 1, |v| v as i64, mul, ax)
[m[31m-            }
[m[32m+            Some(ax) => prod_axis_typed::<u8, i64, _, _>(
[m[32m+                py,
[m[32m+                &numpy,
[m[32m+                &viewed,
[m[32m+                "int64",
[m[32m+                1,
[m[32m+                |v| v as i64,
[m[32m+                mul,
[m[32m+                ax,
[m[32m+            ),
[m         };
     }
     // Dispatch each (input type T, accumulator type A) pair to either the full
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:47211:
     macro_rules! dispatch {
         ($t:ty, $a:ty, $dt:literal, $conv:expr) => {
             match axis {
[31m-                None => prod_typed::<$t, $a, _, _>(
[m[31m-                    py,
[m[31m-                    &numpy,
[m[31m-                    a,
[m[31m-                    $dt,
[m[31m-                    1,
[m[31m-                    $conv,
[m[31m-                    |x: $a, y: $a| x.wrapping_mul(y),
[m[31m-                ),
[m[32m+                None => prod_typed::<$t, $a, _, _>(py, &numpy, a, $dt, 1, $conv, |x: $a, y: $a| {
[m[32m+                    x.wrapping_mul(y)
[m[32m+                }),
[m                 Some(ax) => prod_axis_typed::<$t, $a, _, _>(
                     py,
                     &numpy,
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:47674:
     if itemsize <= 2 || size < (1 << 23) {
         let kwargs = PyDict::new(py);
         kwargs.set_item("axis", axis)?;
[31m-        return Ok(Some(numpy.getattr("ptp")?.call((a,), Some(&kwargs))?.unbind()));
[m[32m+        return Ok(Some(
[m[32m+            numpy.getattr("ptp")?.call((a,), Some(&kwargs))?.unbind(),
[m[32m+        ));
[m     }
     let result = match (kind.as_str(), itemsize) {
         ("i", 1) => ptp_axis_typed::<i8, _>(py, &numpy, a, axis, "int8", |x, y| x.wrapping_sub(y))?,
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:47681:
[31m-        ("i", 2) => ptp_axis_typed::<i16, _>(py, &numpy, a, axis, "int16", |x, y| x.wrapping_sub(y))?,
[m[31m-        ("i", 4) => ptp_axis_typed::<i32, _>(py, &numpy, a, axis, "int32", |x, y| x.wrapping_sub(y))?,
[m[31m-        ("i", 8) => ptp_axis_typed::<i64, _>(py, &numpy, a, axis, "int64", |x, y| x.wrapping_sub(y))?,
[m[31m-        ("u", 1) => ptp_axis_typed::<u8, _>(py, &numpy, a, axis, "uint8", |x, y| x.wrapping_sub(y))?,
[m[31m-        ("u", 2) => ptp_axis_typed::<u16, _>(py, &numpy, a, axis, "uint16", |x, y| x.wrapping_sub(y))?,
[m[31m-        ("u", 4) => ptp_axis_typed::<u32, _>(py, &numpy, a, axis, "uint32", |x, y| x.wrapping_sub(y))?,
[m[31m-        ("u", 8) => ptp_axis_typed::<u64, _>(py, &numpy, a, axis, "uint64", |x, y| x.wrapping_sub(y))?,
[m[32m+        ("i", 2) => {
[m[32m+            ptp_axis_typed::<i16, _>(py, &numpy, a, axis, "int16", |x, y| x.wrapping_sub(y))?
[m[32m+        }
[m[32m+        ("i", 4) => {
[m[32m+            ptp_axis_typed::<i32, _>(py, &numpy, a, axis, "int32", |x, y| x.wrapping_sub(y))?
[m[32m+        }
[m[32m+        ("i", 8) => {
[m[32m+            ptp_axis_typed::<i64, _>(py, &numpy, a, axis, "int64", |x, y| x.wrapping_sub(y))?
[m[32m+        }
[m[32m+        ("u", 1) => {
[m[32m+            ptp_axis_typed::<u8, _>(py, &numpy, a, axis, "uint8", |x, y| x.wrapping_sub(y))?
[m[32m+        }
[m[32m+        ("u", 2) => {
[m[32m+            ptp_axis_typed::<u16, _>(py, &numpy, a, axis, "uint16", |x, y| x.wrapping_sub(y))?
[m[32m+        }
[m[32m+        ("u", 4) => {
[m[32m+            ptp_axis_typed::<u32, _>(py, &numpy, a, axis, "uint32", |x, y| x.wrapping_sub(y))?
[m[32m+        }
[m[32m+        ("u", 8) => {
[m[32m+            ptp_axis_typed::<u64, _>(py, &numpy, a, axis, "uint64", |x, y| x.wrapping_sub(y))?
[m[32m+        }
[m         _ => return Ok(None),
     };
     let Some((flat, out_shape)) = result else {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:47770:
             unsafe { std::slice::from_raw_parts(input.as_ptr().cast::<f64>(), input.len()) };
         use rayon::prelude::*;
         const PTP_AXIS_PARALLEL_MIN: usize = 1 << 21;
[31m-        let parallel = outer * axis_len * inner >= PTP_AXIS_PARALLEL_MIN
[m[31m-            && rayon::current_num_threads() >= 2;
[m[32m+        let parallel =
[m[32m+            outer * axis_len * inner >= PTP_AXIS_PARALLEL_MIN && rayon::current_num_threads() >= 2;
[m         let lane = axis_len * inner;
         // ptp tracks NaN-presence SEPARATELY and emits NaN for any nan lane, so the
         // running max/min can use branchless NaN-skipping f64::max/min (vectorizes;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:47812:
             let group_plane = |obase: usize| -> (Vec<f64>, Vec<f64>, Vec<bool>) {
                 let mut mxv = data[obase..obase + inner].to_vec();
                 let mut mnv = mxv.clone();
[31m-                let mut nanv: Vec<bool> = data[obase..obase + inner].iter().map(|&v| v != v).collect();
[m[32m+                let mut nanv: Vec<bool> =
[m[32m+                    data[obase..obase + inner].iter().map(|&v| v != v).collect();
[m                 for a_idx in 1..axis_len {
                     let slab = &data[obase + a_idx * inner..obase + a_idx * inner + inner];
                     for (((m, n), nf), &v) in mxv
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:48556:
 // ndim>1 (axis=None raises a ValueError instead of flattening). Route the pure case to the
 // native parallel `cum{sum,prod}` (which beats numpy's single-threaded scan ~3.7x at 4M
 // f64), and defer `include_initial` / `dtype` / `out` / the axis=None-on-ND error to numpy.
[31m-type CumulativeNative =
[m[31m-    fn(Python<'_>, Py<PyAny>, Option<Py<PyAny>>, Option<Py<PyAny>>, Option<Py<PyAny>>) -> PyResult<Py<PyAny>>;
[m[32m+type CumulativeNative = fn(
[m[32m+    Python<'_>,
[m[32m+    Py<PyAny>,
[m[32m+    Option<Py<PyAny>>,
[m[32m+    Option<Py<PyAny>>,
[m[32m+    Option<Py<PyAny>>,
[m[32m+) -> PyResult<Py<PyAny>>;
[m
 fn cumulative_dispatch(
     py: Python<'_>,
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:48980:
                 .getattr("kind")?
                 .extract::<String>()?;
             if kind == "i" || kind == "u" || kind == "b" {
[31m-                let a_f64 =
[m[31m-                    numpy.call_method1("asarray", (a_bound, numpy.getattr("float64")?))?;
[m[32m+                let a_f64 = numpy.call_method1("asarray", (a_bound, numpy.getattr("float64")?))?;
[m                 if let Some(out) =
                     try_zerocopy_f64_isclose_array_scalar(py, &a_f64, b_bound, rtol, atol)?
                 {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:51875:
 #[cfg(test)]
 mod tests {
     use super::{
[31m-        PyFromPyFunc, PyVectorize, PythonNativeGemmOp, argwhere, bincount,
[m[32m+        NarrowSetOp, PyFromPyFunc, PyVectorize, PythonNativeGemmOp, argwhere, bincount,
[m         build_numpy_array_from_ufunc, ceil_native, choose, compress, copysign, count_nonzero,
[31m-        degrees_native, diag, diag_indices, diag_indices_from, diagflat, diagonal, digitize, extract,
[m[31m-        extract_numeric_array, extract_precise_numeric_array, fill_diagonal, flatnonzero, flip,
[m[31m-        fliplr, flipud, floor_native, fnp_python, frexp, hypot, indices, interp, isfinite_native,
[m[31m-        isinf_native, isnan_native, isneginf_native, isposinf_native, ix_, ldexp, logaddexp,
[m[31m-        logaddexp2, meshgrid, modf, nan_to_num, NarrowSetOp, narrow_bitmap_setop,
[m[32m+        degrees_native, diag, diag_indices, diag_indices_from, diagflat, diagonal, digitize,
[m[32m+        extract, extract_numeric_array, extract_precise_numeric_array, fill_diagonal, flatnonzero,
[m[32m+        flip, fliplr, flipud, floor_native, fnp_python, frexp, hypot, indices, interp,
[m[32m+        isfinite_native, isinf_native, isnan_native, isneginf_native, isposinf_native, ix_, ldexp,
[m[32m+        logaddexp, logaddexp2, meshgrid, modf, nan_to_num, narrow_bitmap_setop, nextafter, place,
[m[32m+        put, put_along_axis, putmask, python_native_gemm_f64_2d,
[m[32m+        python_native_gemm_f64_2d_eligible, python_native_gemm_f64_2d_metadata_gate,
[m[32m+        radians_native, ravel_multi_index, required_dict_item, rfftfreq, rint_native, searchsorted,
[m[32m+        select, sign, signbit_native, sinc, solve_triangular, spacing, take, take_along_axis,
[m[32m+        tensorinv, tensorsolve, trapezoid, trapz, tri, tril_indices, tril_indices_from,
[m[32m+        triu_indices, triu_indices_from, trunc_native, unravel_index, where_py,
[m         wide_int_table_bounds,
[31m-        nextafter, place, put, put_along_axis, putmask, python_native_gemm_f64_2d,
[m[31m-        python_native_gemm_f64_2d_eligible, python_native_gemm_f64_2d_metadata_gate, radians_native,
[m[31m-        ravel_multi_index, required_dict_item, rfftfreq, rint_native, searchsorted, select, sign,
[m[31m-        signbit_native, sinc, solve_triangular, spacing, take, take_along_axis, tensorinv,
[m[31m-        tensorsolve, trapezoid, trapz, tri, tril_indices, tril_indices_from, triu_indices,
[m[31m-        triu_indices_from, trunc_native, unravel_index, where_py,
[m     };
     use fnp_dtype::{ArrayStorage, DType};
     use fnp_ufunc::UFuncArray;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:56380:
             int16_kwargs.set_item("dtype", numpy.getattr("int16")?)?;
             let (ours, theirs) = random_generator_pair(&random, &numpy_random, 275)?;
             assert_random_sample_matches_numpy(
[31m-                &ours.call_method("integers", (-300_i64, 300_i64, 6_usize), Some(&int16_kwargs))?,
[m[32m+                &ours.call_method(
[m[32m+                    "integers",
[m[32m+                    (-300_i64, 300_i64, 6_usize),
[m[32m+                    Some(&int16_kwargs),
[m[32m+                )?,
[m                 &theirs.call_method(
                     "integers",
                     (-300_i64, 300_i64, 6_usize),
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:60334:
             let np_where = numpy.getattr("where")?;
             let fnp_where = module.getattr("where")?;
             let array_equal = numpy.getattr("array_equal")?;
[31m-            let rng = numpy.getattr("random")?.getattr("default_rng")?.call1((8_u64,))?;
[m[32m+            let rng = numpy
[m[32m+                .getattr("random")?
[m[32m+                .getattr("default_rng")?
[m[32m+                .call1((8_u64,))?;
[m             let v = rng.getattr("standard_normal")?.call1((4096_i64,))?;
             let cond = v.call_method1("__gt__", (0.0,))?;
             // (description, x, y) where x/y are arbitrary python objects.
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:60342:
             let i = |x: i64| -> Py<PyAny> { x.into_pyobject(py).unwrap().into_any().unbind() };
             let arr = || v.clone().unbind();
             let cases: Vec<(Py<PyAny>, Py<PyAny>)> = vec![
[31m-                (arr(), f(0.0)),       // arr, float scalar -> f64
[m[31m-                (f(5.0), arr()),       // float scalar, arr -> f64
[m[31m-                (arr(), i(0)),         // arr, int scalar -> f64 (array dominates)
[m[31m-                (f(1.0), f(0.0)),      // float, float -> f64
[m[31m-                (f(1.0), i(0)),        // float, int -> f64
[m[31m-                (i(1), i(0)),          // int, int -> int64 (MUST defer / match numpy)
[m[31m-                (arr(), f(f64::NAN)),  // nan scalar
[m[32m+                (arr(), f(0.0)),      // arr, float scalar -> f64
[m[32m+                (f(5.0), arr()),      // float scalar, arr -> f64
[m[32m+                (arr(), i(0)),        // arr, int scalar -> f64 (array dominates)
[m[32m+                (f(1.0), f(0.0)),     // float, float -> f64
[m[32m+                (f(1.0), i(0)),       // float, int -> f64
[m[32m+                (i(1), i(0)),         // int, int -> int64 (MUST defer / match numpy)
[m[32m+                (arr(), f(f64::NAN)), // nan scalar
[m             ];
             for (x, y) in cases {
                 let ours = fnp_where.call1((cond.clone(), x.bind(py), y.bind(py)))?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:60607:
                     h = h.wrapping_mul(0x100000001b3);
                 }
             }
[31m-            assert_eq!(h, 0x9c25806588f3a004, "argwhere zero-copy golden FNV drifted");
[m[32m+            assert_eq!(
[m[32m+                h, 0x9c25806588f3a004,
[m[32m+                "argwhere zero-copy golden FNV drifted"
[m[32m+            );
[m             Ok(())
         });
     }
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:62907:
             let a_stack = np_eye
                 .call1((3_i64,))?
                 .call_method1("__mul__", (2.0_f64,))?
[31m-                .call_method1("__add__", (np_arange.call1((9_i64,))?.call_method1(
[m[31m-                    "reshape",
[m[31m-                    ((3_i64, 3_i64),),
[m[31m-                )?.call_method1("__mul__", (0.1_f64,))?,))?;
[m[32m+                .call_method1(
[m[32m+                    "__add__",
[m[32m+                    (np_arange
[m[32m+                        .call1((9_i64,))?
[m[32m+                        .call_method1("reshape", ((3_i64, 3_i64),))?
[m[32m+                        .call_method1("__mul__", (0.1_f64,))?,),
[m[32m+                )?;
[m             let a_batched2 = numpy
                 .getattr("stack")?
                 .call1((vec![a_stack.clone(), a_stack.clone()],))?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:66854:
             // (op, dtypes-to-check). Angle ops add narrow ints; sign adds float16;
             // sinc/nanmedian cover narrow floats.
             let cases: [(&str, &[&str]); 7] = [
[31m-                ("degrees", &["float32", "float16", "int8", "int16", "uint8", "int32"]),
[m[31m-                ("radians", &["float32", "float16", "int8", "int16", "uint8", "int32"]),
[m[32m+                (
[m[32m+                    "degrees",
[m[32m+                    &["float32", "float16", "int8", "int16", "uint8", "int32"],
[m[32m+                ),
[m[32m+                (
[m[32m+                    "radians",
[m[32m+                    &["float32", "float16", "int8", "int16", "uint8", "int32"],
[m[32m+                ),
[m                 ("deg2rad", &["float32", "float16", "int8", "int16", "uint8"]),
                 ("rad2deg", &["float32", "float16", "int8", "int16", "uint8"]),
                 ("sinc", &["float32", "float16"]),
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:70622:
                 h = h.wrapping_mul(0x100000001b3);
             }
             assert_eq!(
[31m-                h, 0x2a9a4ef553c1d16c,
[m[32m+                h,
[m[32m+                0x2a9a4ef553c1d16c,
[m                 "extract f64 branchless golden FNV drifted (len={})",
                 actual_bytes.len()
             );
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:70849:
             let numpy = py.import("numpy")?;
             let np_select = numpy.getattr("select")?;
             let fnp_select = module.getattr("select")?;
[31m-            let rng = numpy.getattr("random")?.getattr("default_rng")?.call1((4_u64,))?;
[m[32m+            let rng = numpy
[m[32m+                .getattr("random")?
[m[32m+                .getattr("default_rng")?
[m[32m+                .call1((4_u64,))?;
[m             let v = rng.getattr("standard_normal")?.call1((5000_i64,))?;
             // conditions and choices and the array default are all derived from v.
[31m-            let lt = |t: f64| -> PyResult<Bound<'_, PyAny>> {
[m[31m-                Ok(v.call_method1("__lt__", (t,))?)
[m[31m-            };
[m[31m-            let scaled = |s: f64| -> PyResult<Bound<'_, PyAny>> {
[m[31m-                Ok(v.call_method1("__mul__", (s,))?)
[m[31m-            };
[m[32m+            let lt = |t: f64| -> PyResult<Bound<'_, PyAny>> { Ok(v.call_method1("__lt__", (t,))?) };
[m[32m+            let scaled =
[m[32m+                |s: f64| -> PyResult<Bound<'_, PyAny>> { Ok(v.call_method1("__mul__", (s,))?) };
[m             let array_equal = numpy.getattr("array_equal")?;
             for (conds, choices) in [
                 (vec![lt(-1.0)?, lt(1.0)?], vec![scaled(2.0)?, scaled(3.0)?]),
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:73300:
             let cases: &[(&str, Vec<i64>, Vec<i64>)] = &[
                 ("int8", vec![100, 100], vec![100, 100]), // 20000 wraps int8 -> 32
                 ("int16", vec![1, 2, 3], vec![4, 5, 6]),
[31m-                ("int32", vec![1 << 30, 1 << 30, 7], vec![1 << 30, 1 << 30, 3]), // 2^61+21 -> int32 21
[m[32m+                (
[m[32m+                    "int32",
[m[32m+                    vec![1 << 30, 1 << 30, 7],
[m[32m+                    vec![1 << 30, 1 << 30, 3],
[m[32m+                ), // 2^61+21 -> int32 21
[m                 ("uint8", vec![200, 200], vec![200, 200]),
                 ("uint32", vec![1 << 30, 1 << 30], vec![4, 4]),
             ];
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:80203:
             // float32 nanstd previously returned a float64 scalar.
             for narrow in ["float32", "float16"] {
                 let dtype_obj = numpy.getattr(narrow)?;
[31m-                let base = numpy
[m[31m-                    .getattr("array")?
[m[31m-                    .call1((vec![1.0_f64, 2.0, f64::NAN, 4.0, 8.0],))?;
[m[32m+                let base =
[m[32m+                    numpy
[m[32m+                        .getattr("array")?
[m[32m+                        .call1((vec![1.0_f64, 2.0, f64::NAN, 4.0, 8.0],))?;
[m                 let narrow_arr = base.call_method1("astype", (dtype_obj,))?;
                 let ours_n = nanstd_fn.call1((narrow_arr.clone(),))?;
                 let theirs_n = numpy_nanstd.call1((narrow_arr.clone(),))?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:80359:
             // float32 nanvar previously returned a float64 scalar.
             for narrow in ["float32", "float16"] {
                 let dtype_obj = numpy.getattr(narrow)?;
[31m-                let base = numpy
[m[31m-                    .getattr("array")?
[m[31m-                    .call1((vec![1.0_f64, 2.0, f64::NAN, 4.0, 8.0],))?;
[m[32m+                let base =
[m[32m+                    numpy
[m[32m+                        .getattr("array")?
[m[32m+                        .call1((vec![1.0_f64, 2.0, f64::NAN, 4.0, 8.0],))?;
[m                 let narrow_arr = base.call_method1("astype", (dtype_obj,))?;
                 let ours_n = nanvar_fn.call1((narrow_arr.clone(),))?;
                 let theirs_n = numpy_nanvar.call1((narrow_arr.clone(),))?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:82239:
             (-5_000, 4096, DType::I64),
         ];
         let lanes: &[(NarrowSetOp, fn(&BTreeSet<i64>, &BTreeSet<i64>) -> Vec<i64>)] = &[
[31m-            (NarrowSetOp::Intersect, |a, b| a.intersection(b).copied().collect()),
[m[32m+            (NarrowSetOp::Intersect, |a, b| {
[m[32m+                a.intersection(b).copied().collect()
[m[32m+            }),
[m             (NarrowSetOp::Union, |a, b| a.union(b).copied().collect()),
[31m-            (NarrowSetOp::Setdiff, |a, b| a.difference(b).copied().collect()),
[m[31m-            (NarrowSetOp::Setxor, |a, b| a.symmetric_difference(b).copied().collect()),
[m[32m+            (NarrowSetOp::Setdiff, |a, b| {
[m[32m+                a.difference(b).copied().collect()
[m[32m+            }),
[m[32m+            (NarrowSetOp::Setxor, |a, b| {
[m[32m+                a.symmetric_difference(b).copied().collect()
[m[32m+            }),
[m         ];
         for &(min, size, dt) in dims {
             let hi = min + size as i64;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:82381:
                     }),
                 )?;
                 for &(op_name, _op) in &ops {
[31m-                    let ours = module.getattr(op_name)?.call1((left_arr.clone(), right_arr.clone()))?;
[m[31m-                    let theirs = numpy.getattr(op_name)?.call1((left_arr.clone(), right_arr.clone()))?;
[m[32m+                    let ours = module
[m[32m+                        .getattr(op_name)?
[m[32m+                        .call1((left_arr.clone(), right_arr.clone()))?;
[m[32m+                    let theirs = numpy
[m[32m+                        .getattr(op_name)?
[m[32m+                        .call1((left_arr.clone(), right_arr.clone()))?;
[m                     assert_array_matches_numpy(&ours, &theirs)?;
                     proof_bytes.extend_from_slice(case.as_bytes());
                     proof_bytes.push(0);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:82389:
                     proof_bytes.extend_from_slice(op_name.as_bytes());
                     proof_bytes.push(0);
[31m-                    proof_bytes.extend_from_slice(ours.getattr("dtype")?.str()?.to_string().as_bytes());
[m[32m+                    proof_bytes
[m[32m+                        .extend_from_slice(ours.getattr("dtype")?.str()?.to_string().as_bytes());
[m                     proof_bytes.push(0);
[31m-                    proof_bytes.extend_from_slice(ours.getattr("shape")?.str()?.to_string().as_bytes());
[m[32m+                    proof_bytes
[m[32m+                        .extend_from_slice(ours.getattr("shape")?.str()?.to_string().as_bytes());
[m                     proof_bytes.push(0);
                     let raw: Vec<u8> = ours.call_method0("tobytes")?.extract()?;
                     proof_bytes.extend_from_slice(&raw);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:82400:

             let digest = py_sha256_hex(py, &proof_bytes)?;
             assert_eq!(
[31m-                digest,
[m[31m-                "acd30f91b7d4d0a28e13171ea131ce4f34cd24693be4b30372fd7329f4e05f0b",
[m[32m+                digest, "acd30f91b7d4d0a28e13171ea131ce4f34cd24693be4b30372fd7329f4e05f0b",
[m                 "wide-int range bitmap set-op golden sha256 changed",
             );
             Ok(())
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:82474:
                     proof_bytes.push(0);
                     proof_bytes.extend_from_slice(op_name.as_bytes());
                     proof_bytes.push(0);
[31m-                    proof_bytes.extend_from_slice(
[m[31m-                        ours.getattr("dtype")?.str()?.to_string().as_bytes(),
[m[31m-                    );
[m[32m+                    proof_bytes
[m[32m+                        .extend_from_slice(ours.getattr("dtype")?.str()?.to_string().as_bytes());
[m                     proof_bytes.push(0);
[31m-                    proof_bytes.extend_from_slice(
[m[31m-                        ours.getattr("shape")?.str()?.to_string().as_bytes(),
[m[31m-                    );
[m[32m+                    proof_bytes
[m[32m+                        .extend_from_slice(ours.getattr("shape")?.str()?.to_string().as_bytes());
[m                     proof_bytes.push(0);
                     let raw: Vec<u8> = ours.call_method0("tobytes")?.extract()?;
                     proof_bytes.extend_from_slice(&raw);
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:82490:

             let digest = py_sha256_hex(py, &proof_bytes)?;
             assert_eq!(
[31m-                digest,
[m[31m-                "ec0d35d19fdfc33accacb0c9a96cfd020ccd007989c970f193948b449a5a1e4b",
[m[32m+                digest, "ec0d35d19fdfc33accacb0c9a96cfd020ccd007989c970f193948b449a5a1e4b",
[m                 "float set-op numpy passthrough golden sha256 changed",
             );
             Ok(())
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:90125:

             // Rectangular batched (5, 4, 3): exercises the parallel batch_svd path on
             // tall lanes; output shape must be (5, 3) = batch dims + min(M,N).
[31m-            let rng = numpy.getattr("random")?.getattr("default_rng")?.call1((7_u64,))?;
[m[31m-            let rect = rng.getattr("standard_normal")?.call1(((5_i64, 4_i64, 3_i64),))?;
[m[32m+            let rng = numpy
[m[32m+                .getattr("random")?
[m[32m+                .getattr("default_rng")?
[m[32m+                .call1((7_u64,))?;
[m[32m+            let rect = rng
[m[32m+                .getattr("standard_normal")?
[m[32m+                .call1(((5_i64, 4_i64, 3_i64),))?;
[m             let ours_r = sv_fn.call1((rect.clone(),))?;
             let theirs_r = numpy_sv.call1((rect.clone(),))?;
             let ok_r: bool = allclose.call1((&ours_r, &theirs_r))?.extract()?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:90135:
             assert_eq!(r_shape, vec![5, 3], "svdvals(5,4,3) shape must be (5,3)");

             // 4-D batched (2, 3, 6, 6): nested batch dims through the same path.
[31m-            let big = rng.getattr("standard_normal")?.call1(((2_i64, 3_i64, 6_i64, 6_i64),))?;
[m[32m+            let big = rng
[m[32m+                .getattr("standard_normal")?
[m[32m+                .call1(((2_i64, 3_i64, 6_i64, 6_i64),))?;
[m             let ours_g = sv_fn.call1((big.clone(),))?;
             let theirs_g = numpy_sv.call1((big.clone(),))?;
             let ok_g: bool = allclose.call1((&ours_g, &theirs_g))?.extract()?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:90142:
             assert!(ok_g, "svdvals 4-D batched mismatch");
             let g_shape: Vec<usize> = ours_g.getattr("shape")?.extract()?;
[31m-            assert_eq!(g_shape, vec![2, 3, 6], "svdvals(2,3,6,6) shape must be (2,3,6)");
[m[32m+            assert_eq!(
[m[32m+                g_shape,
[m[32m+                vec![2, 3, 6],
[m[32m+                "svdvals(2,3,6,6) shape must be (2,3,6)"
[m[32m+            );
[m
             // Complex input stays on the numpy fallback path but must still
             // match exactly.
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:90205:
             // Integer lists must defer to numpy (int result dtype) and still work.
             let ai = PyList::new(py, [1_i64, 2, 3])?;
             let vi = PyList::new(py, [0_i64, 1, 2])?;
[31m-            let ours_i = module.getattr("convolve")?.call((ai.clone(), vi.clone(), "full"), None)?;
[m[32m+            let ours_i = module
[m[32m+                .getattr("convolve")?
[m[32m+                .call((ai.clone(), vi.clone(), "full"), None)?;
[m             let theirs_i = numpy.getattr("convolve")?.call1((ai.clone(), vi.clone()))?;
             let eq_i: bool = numpy
                 .getattr("array_equal")?
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:90229:

             // choose must accept array_like (Python list) choices, not just ndarrays
             // — the int fast path used to read items[0].dtype unguarded.
[31m-            let idx = numpy.getattr("array")?.call1((PyList::new(py, [0_i64, 1, 2, 0])?,))?;
[m[32m+            let idx = numpy
[m[32m+                .getattr("array")?
[m[32m+                .call1((PyList::new(py, [0_i64, 1, 2, 0])?,))?;
[m             let choices = PyList::new(
                 py,
                 [
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:90238:
                     PyList::new(py, [30_i64, 31, 32, 33])?,
                 ],
             )?;
[31m-            let ours_ch = module.getattr("choose")?.call1((idx.clone(), choices.clone()))?;
[m[31m-            let theirs_ch = numpy.getattr("choose")?.call1((idx.clone(), choices.clone()))?;
[m[32m+            let ours_ch = module
[m[32m+                .getattr("choose")?
[m[32m+                .call1((idx.clone(), choices.clone()))?;
[m[32m+            let theirs_ch = numpy
[m[32m+                .getattr("choose")?
[m[32m+                .call1((idx.clone(), choices.clone()))?;
[m             let okch: bool = numpy
                 .getattr("array_equal")?
                 .call1((&ours_ch, &theirs_ch))?
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:90260:
             fnp_python(&module)?;
             let numpy = py.import("numpy")?;
             let allclose = numpy.getattr("allclose")?;
[31m-            let rng = numpy.getattr("random")?.getattr("default_rng")?.call1((9_u64,))?;
[m[32m+            let rng = numpy
[m[32m+                .getattr("random")?
[m[32m+                .getattr("default_rng")?
[m[32m+                .call1((9_u64,))?;
[m             let x = rng.getattr("standard_normal")?.call1((300_i64,))?;
             let y = rng.getattr("standard_normal")?.call1((300_i64,))?;

Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:90267:
             // histogram2d: numpy returns float64 counts even unweighted.
[31m-            let h2 = module.getattr("histogram2d")?.call((x.clone(), y.clone(), 7_i64), None)?;
[m[32m+            let h2 = module
[m[32m+                .getattr("histogram2d")?
[m[32m+                .call((x.clone(), y.clone(), 7_i64), None)?;
[m             let our_h = h2.get_item(0)?;
             let np_h = numpy
                 .getattr("histogram2d")?
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:90280:
             let sample = numpy
                 .getattr("column_stack")?
                 .call1((PyList::new(py, [&x, &y])?,))?;
[31m-            let hdd = module.getattr("histogramdd")?.call((sample.clone(), 6_i64), None)?;
[m[32m+            let hdd = module
[m[32m+                .getattr("histogramdd")?
[m[32m+                .call((sample.clone(), 6_i64), None)?;
[m             let our_hdd = hdd.get_item(0)?;
             let np_hdd = numpy
                 .getattr("histogramdd")?
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:90307:
             let numpy = py.import("numpy")?;
             let numpy_cond = numpy.getattr("linalg")?.getattr("cond")?;
             let allclose = numpy.getattr("allclose")?;
[31m-            let rng = numpy.getattr("random")?.getattr("default_rng")?.call1((11_u64,))?;
[m[32m+            let rng = numpy
[m[32m+                .getattr("random")?
[m[32m+                .getattr("default_rng")?
[m[32m+                .call1((11_u64,))?;
[m
             // Batched square / rectangular / 4-D, default p (None==2-norm).
             for shp in [
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:90324:
             }

             // Explicit p=2 and p=-2 selectors must also hit the native path.
[31m-            let a = rng.getattr("standard_normal")?.call1((vec![32_i64, 8, 8],))?;
[m[32m+            let a = rng
[m[32m+                .getattr("standard_normal")?
[m[32m+                .call1((vec![32_i64, 8, 8],))?;
[m             for p in [2_i64, -2] {
                 let ours = cond_fn.call1((a.clone(), p))?;
                 let theirs = numpy_cond.call1((a.clone(), p))?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:90333:
             }

             // Non-2-norm p stays on numpy passthrough (p='fro', square only).
[31m-            let sq = rng.getattr("standard_normal")?.call1((vec![16_i64, 5, 5],))?;
[m[32m+            let sq = rng
[m[32m+                .getattr("standard_normal")?
[m[32m+                .call1((vec![16_i64, 5, 5],))?;
[m             let ours_f = cond_fn.call1((sq.clone(), "fro"))?;
             let theirs_f = numpy_cond.call1((sq.clone(), "fro"))?;
             let ok_f: bool = allclose.call1((&ours_f, &theirs_f))?.extract()?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:90345:
             let ours_z = cond_fn.call1((zeros.clone(),))?;
             let theirs_z = numpy_cond.call1((zeros.clone(),))?;
             let isinf = numpy.getattr("isinf")?;
[31m-            let all_inf_ours: bool = isinf
[m[31m-                .call1((&ours_z,))?
[m[31m-                .call_method0("all")?
[m[31m-                .extract()?;
[m[32m+            let all_inf_ours: bool = isinf.call1((&ours_z,))?.call_method0("all")?.extract()?;
[m             assert!(all_inf_ours, "cond(zeros) must be +Inf, got non-inf");
             let eq_z: bool = numpy
                 .getattr("array_equal")?
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:90372:
             let numpy = py.import("numpy")?;
             let numpy_pinv = numpy.getattr("linalg")?.getattr("pinv")?;
             let allclose = numpy.getattr("allclose")?;
[31m-            let rng = numpy.getattr("random")?.getattr("default_rng")?.call1((23_u64,))?;
[m[32m+            let rng = numpy
[m[32m+                .getattr("random")?
[m[32m+                .getattr("default_rng")?
[m[32m+                .call1((23_u64,))?;
[m
             // Square / tall / wide / 4-D batched. Output is (..., N, M).
             for (shp, out) in [
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:90392:
             }

             // hermitian=True batched stays on the numpy passthrough but must match.
[31m-            let sq = rng.getattr("standard_normal")?.call1((vec![8_i64, 5, 5],))?;
[m[32m+            let sq = rng
[m[32m+                .getattr("standard_normal")?
[m[32m+                .call1((vec![8_i64, 5, 5],))?;
[m             let kw = PyDict::new(py);
             kw.set_item("hermitian", true)?;
             let ours_h = pinv_fn.call((sq.clone(),), Some(&kw))?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:90416:
             let numpy = py.import("numpy")?;
             let numpy_mn = numpy.getattr("linalg")?.getattr("matrix_norm")?;
             let allclose = numpy.getattr("allclose")?;
[31m-            let rng = numpy.getattr("random")?.getattr("default_rng")?.call1((41_u64,))?;
[m[32m+            let rng = numpy
[m[32m+                .getattr("random")?
[m[32m+                .getattr("default_rng")?
[m[32m+                .call1((41_u64,))?;
[m
             // SVD-derived orders 2 / -2 / 'nuc' across square / tall / wide / 4-D.
             for shp in [
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:90440:
             }

             // keepdims=True must keep the reduced last two axes as (1, 1).
[31m-            let a = rng.getattr("standard_normal")?.call1((vec![4_i64, 3, 5, 5],))?;
[m[32m+            let a = rng
[m[32m+                .getattr("standard_normal")?
[m[32m+                .call1((vec![4_i64, 3, 5, 5],))?;
[m             let kw = PyDict::new(py);
             kw.set_item("ord", 2_i64)?;
             kw.set_item("keepdims", true)?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:90479:
             let numpy = py.import("numpy")?;
             let numpy_norm = numpy.getattr("linalg")?.getattr("norm")?;
             let allclose = numpy.getattr("allclose")?;
[31m-            let rng = numpy.getattr("random")?.getattr("default_rng")?.call1((57_u64,))?;
[m[32m+            let rng = numpy
[m[32m+                .getattr("random")?
[m[32m+                .getattr("default_rng")?
[m[32m+                .call1((57_u64,))?;
[m
             // ord 2 / -2 / 'nuc' over the trailing two axes, both axis orders.
             for shp in [vec![32_i64, 6, 6], vec![16, 7, 4], vec![2, 3, 5, 5]] {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:90501:
             }

             // keepdims preserves the reduced trailing axes as (1, 1).
[31m-            let a = rng.getattr("standard_normal")?.call1((vec![4_i64, 3, 5, 5],))?;
[m[32m+            let a = rng
[m[32m+                .getattr("standard_normal")?
[m[32m+                .call1((vec![4_i64, 3, 5, 5],))?;
[m             let kw = PyDict::new(py);
             kw.set_item("ord", 2_i64)?;
             kw.set_item("axis", (-2_i64, -1_i64))?;
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:93252:

             // (subscripts, operand (n, shape) list)
             let cases: &[EinsumCase<'_>] = &[
[31m-                ("ij,jk,kl->il", &[(20, &[4, 5]), (30, &[5, 6]), (18, &[6, 3])]),
[m[32m+                (
[m[32m+                    "ij,jk,kl->il",
[m[32m+                    &[(20, &[4, 5]), (30, &[5, 6]), (18, &[6, 3])],
[m[32m+                ),
[m                 ("ij,jk,ki->", &[(35, &[5, 7]), (42, &[7, 6]), (30, &[6, 5])]),
[31m-                ("ijk,jkl,kl->il", &[(24, &[2, 3, 4]), (24, &[3, 4, 2]), (8, &[4, 2])]),
[m[31m-                ("bij,bjk,bkl->bil", &[(24, &[2, 3, 4]), (16, &[2, 4, 2]), (12, &[2, 2, 3])]),
[m[31m-                ("ij,jk,kl,lm->im", &[(6, &[2, 3]), (6, &[3, 2]), (8, &[2, 4]), (8, &[4, 2])]),
[m[32m+                (
[m[32m+                    "ijk,jkl,kl->il",
[m[32m+                    &[(24, &[2, 3, 4]), (24, &[3, 4, 2]), (8, &[4, 2])],
[m[32m+                ),
[m[32m+                (
[m[32m+                    "bij,bjk,bkl->bil",
[m[32m+                    &[(24, &[2, 3, 4]), (16, &[2, 4, 2]), (12, &[2, 2, 3])],
[m[32m+                ),
[m[32m+                (
[m[32m+                    "ij,jk,kl,lm->im",
[m[32m+                    &[(6, &[2, 3]), (6, &[3, 2]), (8, &[2, 4]), (8, &[4, 2])],
[m[32m+                ),
[m             ];
             for (subs, ops) in cases {
[31m-                let built: Vec<Bound<'_, PyAny>> =
[m[31m-                    ops.iter().map(|(n, s)| mk(*n, s)).collect::<PyResult<_>>()?;
[m[32m+                let built: Vec<Bound<'_, PyAny>> = ops
[m[32m+                    .iter()
[m[32m+                    .map(|(n, s)| mk(*n, s))
[m[32m+                    .collect::<PyResult<_>>()?;
[m                 let subs_obj = pyo3::types::PyString::new(py, subs).into_any();
                 let mut args: Vec<Bound<'_, PyAny>> = vec![subs_obj];
                 for b in &built {
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:93268:
                 }
                 let ours = ours_fn.call1(PyTuple::new(py, &args)?)?;
                 let theirs = np_einsum.call1(PyTuple::new(py, &args)?)?;
[31m-                let close: bool = allclose
[m[31m-                    .call1((ours, theirs))?
[m[31m-                    .extract()?;
[m[32m+                let close: bool = allclose.call1((ours, theirs))?.extract()?;
[m                 assert!(close, "einsum '{subs}' diverges from numpy beyond allclose");
             }
             Ok(())
Diff in /data/projects/franken_numpy/crates/fnp-python/src/lib.rs:93310:

             // (subscripts, n, shape) — fast-path cases AND deferred cases (same harness).
             let cases: &[(&str, i64, &[i64])] = &[
[31m-                ("ij,ij->", 4225, &[65, 65]),  // full contraction (fast path)
[m[31m-                ("i,i->", 9000, &[9000]),      // 1-D inner product (fast path)
[m[31m-                ("abc,abc->", 5040, &[14, 12, 30]), // 3-D full contraction (fast path)
[m[31m-                ("ij,ij->i", 4002, &[58, 69]), // 2-D partial keep-row (fast path)
[m[31m-                ("ij,ij->j", 4002, &[58, 69]), // 2-D partial keep-col (fast path)
[m[31m-                ("ijk,ijk->i", 1680, &[8, 14, 15]), // N-D prefix reduce keep-1 (fast path)
[m[31m-                ("ijk,ijk->ij", 1680, &[8, 14, 15]), // N-D prefix reduce keep-2 (fast path)
[m[32m+                ("ij,ij->", 4225, &[65, 65]),            // full contraction (fast path)
[m[32m+                ("i,i->", 9000, &[9000]),                // 1-D inner product (fast path)
[m[32m+                ("abc,abc->", 5040, &[14, 12, 30]),      // 3-D full contraction (fast path)
[m[32m+                ("ij,ij->i", 4002, &[58, 69]),           // 2-D partial keep-row (fast path)
[m[32m+                ("ij,ij->j", 4002, &[58, 69]),           // 2-D partial keep-col (fast path)
[m[32m+                ("ijk,ijk->i", 1680, &[8, 14, 15]),      // N-D prefix reduce keep-1 (fast path)
[m[32m+                ("ijk,ijk->ij", 1680, &[8, 14, 15]),     // N-D prefix reduce keep-2 (fast path)
[m                 ("ijkl,ijkl->ij", 1680, &[4, 6, 7, 10]), // 4-D prefix reduce (fast path)
[31m-                ("i,j->ij", 60, &[60]),        // outer product (fast path)
[m[31m-                ("ij,ji->", 4225, &[65, 65]),  // transpose contraction (deferred)
[m[31m-                ("ij,ji->i", 4225, &[65, 65]), // transposed partial (deferred)
[m[31m-                ("i,j->ji", 60, &[60]),        // transposed outer (deferred)
[m[31m-                ("ii,ii->", 2500, &[50, 50]),  // repeated labels (deferred)
[m[32m+                ("i,j->ij", 60, &[60]),                  // outer product (fast path)
[m[32m+                ("ij,ji->", 4225, &[65, 65]),            // transpose contraction (deferred)
[m[32m+                ("ij,ji->i", 4225, &[65, 65]),           // transposed partial (deferred)
[m[32m+                ("i,j->ji", 60, &[60]),                  // transposed outer (deferred)
[m[32m+                ("ii,ii->", 2500, &[50, 50]),            // repeated labels (deferred)
[m             ];
             for (subs, n, shape) in cases {
                 let a = mk(*n, shape)?;
Diff in /data/projects/franken_numpy/crates/fnp-python/tests/conformance_histogram_bincount.rs:281:
     ];

     for (label, setup, call_expr) in cases {
[31m-        let numpy_result =
[m[31m-            numpy_oracle(&numpy_outcome_script("np.histogram_bin_edges", setup, call_expr))?;
[m[32m+        let numpy_result = numpy_oracle(&numpy_outcome_script(
[m[32m+            "np.histogram_bin_edges",
[m[32m+            setup,
[m[32m+            call_expr,
[m[32m+        ))?;
[m         let rust_result =
             numpy_oracle(&fnp_outcome_script("histogram_bin_edges", setup, call_expr))?;

Diff in /data/projects/franken_numpy/crates/fnp-python/tests/conformance_histogram_bincount.rs:314:
             "",
             "op([0.5, 1.5, 3.5], [4, 3, 2, 1], right=True)",
         ),
[31m-        (
[m[31m-            "nonmonotonic bins error type",
[m[31m-            "",
[m[31m-            "op([1, 2], [0, 2, 1])",
[m[31m-        ),
[m[32m+        ("nonmonotonic bins error type", "", "op([1, 2], [0, 2, 1])"),
[m     ];

     for (label, setup, call_expr) in cases {
Diff in /data/projects/franken_numpy/crates/fnp-python/tests/conformance_integer_overflow.rs:54:
     let g_bytes = g.call_method0("tobytes")?.extract::<Vec<u8>>()?;
     let x_bytes = x.call_method0("tobytes")?.extract::<Vec<u8>>()?;
     assert_eq!(
[31m-        g_bytes, x_bytes,
[m[32m+        g_bytes,
[m[32m+        x_bytes,
[m         "{label}: bytes mismatch (fnp={:?} numpy={:?})",
         g.call_method0("tolist")?.str()?.to_string(),
         x.call_method0("tolist")?.str()?.to_string()
Diff in /data/projects/franken_numpy/crates/fnp-python/tests/conformance_integer_overflow.rs:126:
         }

         // ── cross: preserves the promoted integer dtype with wraparound ──
[31m-        for dt in ["int8", "int16", "int32", "int64", "uint8", "uint16", "uint32", "uint64"] {
[m[32m+        for dt in [
[m[32m+            "int8", "int16", "int32", "int64", "uint8", "uint16", "uint32", "uint64",
[m[32m+        ] {
[m             // Values fit int8 (so they construct for every dtype) but their cross
             // component products (e.g. 120*110, 100*90) overflow int8/int16 → wrap.
             let a = np_int_array(py, &numpy, &[120, 100, 3], &[3], dt)?;
Diff in /data/projects/franken_numpy/crates/fnp-python/tests/conformance_interp_trapz.rs:103:
             "xp = np.array([0.0, 2.0, 4.0])\nfp = np.array([0.0, 20.0, 40.0])",
             "op((1.0, 3.0), xp, fp)",
         ),
[31m-        (
[m[31m-            "missing fp error type",
[m[31m-            "",
[m[31m-            "op([0.0], [0.0])",
[m[31m-        ),
[m[32m+        ("missing fp error type", "", "op([0.0], [0.0])"),
[m         (
             "xp fp length mismatch error type",
             "",
Diff in /data/projects/franken_numpy/crates/fnp-python/tests/conformance_mean.rs:136:
 #[test]
 fn mean_python_container_keyword_outcomes_match_numpy() -> Result<(), String> {
     let cases = [
[32m+        ("list input scalar", "result = MODULE.mean([1, 2, 3])"),
[m         (
[31m-            "list input scalar",
[m[31m-            "result = MODULE.mean([1, 2, 3])",
[m[31m-        ),
[m[31m-        (
[m             "tuple input axis keepdims",
             "result = MODULE.mean(((1, 2, 3), (4, 5, 6)), axis=1, keepdims=True)",
         ),
Diff in /data/projects/franken_numpy/crates/fnp-python/tests/conformance_mean.rs:160:
             "out = np.empty((2,), dtype=np.float64)
 result = MODULE.mean(np.array([[1.0, 2.0], [3.0, 4.0]]), axis=0, out=out)",
         ),
[31m-        (
[m[31m-            "axis error type",
[m[31m-            "result = MODULE.mean([1, 2, 3], axis=2)",
[m[31m-        ),
[m[32m+        ("axis error type", "result = MODULE.mean([1, 2, 3], axis=2)"),
[m     ];

     for (name, body) in cases {
Diff in /data/projects/franken_numpy/crates/fnp-python/tests/conformance_statistics.rs:287:
 #[test]
 fn average_python_container_keyword_outcomes_match_numpy() -> Result<(), String> {
     let cases = [
[31m-        (
[m[31m-            "list input scalar",
[m[31m-            "result = MODULE.average([1, 2, 3, 4])",
[m[31m-        ),
[m[32m+        ("list input scalar", "result = MODULE.average([1, 2, 3, 4])"),
[m         (
             "tuple input axis weights returned",
             "result = MODULE.average(
Diff in /data/projects/franken_numpy/crates/fnp-python/tests/conformance_std.rs:138:
 #[test]
 fn std_python_container_keyword_outcomes_match_numpy() -> Result<(), String> {
     let cases = [
[32m+        ("list input scalar", "result = MODULE.std([1, 2, 3])"),
[m         (
[31m-            "list input scalar",
[m[31m-            "result = MODULE.std([1, 2, 3])",
[m[31m-        ),
[m[31m-        (
[m             "tuple input axis keepdims",
             "result = MODULE.std(((1, 2, 3), (4, 5, 6)), axis=1, keepdims=True)",
         ),
Diff in /data/projects/franken_numpy/crates/fnp-python/tests/conformance_std.rs:166:
             "out = np.empty((2,), dtype=np.float64)
 result = MODULE.std(np.array([[1.0, 2.0], [3.0, 4.0]]), axis=0, out=out)",
         ),
[31m-        (
[m[31m-            "axis error type",
[m[31m-            "result = MODULE.std([1, 2, 3], axis=2)",
[m[31m-        ),
[m[32m+        ("axis error type", "result = MODULE.std([1, 2, 3], axis=2)"),
[m     ];

     for (name, body) in cases {
Diff in /data/projects/franken_numpy/crates/fnp-python/tests/conformance_trim_zeros.rs:18:
         .stdin
         .take()
         .ok_or_else(|| format!("python3 stdin should be available\nScript: {script}"))?;
[31m-    stdin
[m[31m-        .write_all(script.as_bytes())
[m[31m-        .map_err(|error| format!("failed to write Python oracle script: {error}\nScript: {script}"))?;
[m[32m+    stdin.write_all(script.as_bytes()).map_err(|error| {
[m[32m+        format!("failed to write Python oracle script: {error}\nScript: {script}")
[m[32m+    })?;
[m     drop(stdin);

[31m-    let output = child
[m[31m-        .wait_with_output()
[m[31m-        .map_err(|error| format!("failed to read Python oracle output: {error}\nScript: {script}"))?;
[m[32m+    let output = child.wait_with_output().map_err(|error| {
[m[32m+        format!("failed to read Python oracle output: {error}\nScript: {script}")
[m[32m+    })?;
[m     if !output.status.success() {
         let stderr = String::from_utf8_lossy(&output.stderr);
         return Err(format!("NumPy oracle failed: {stderr}\nScript: {script}"));
Diff in /data/projects/franken_numpy/crates/fnp-python/tests/conformance_var.rs:138:
 #[test]
 fn var_python_container_keyword_outcomes_match_numpy() -> Result<(), String> {
     let cases = [
[32m+        ("list input scalar", "result = MODULE.var([1, 2, 3])"),
[m         (
[31m-            "list input scalar",
[m[31m-            "result = MODULE.var([1, 2, 3])",
[m[31m-        ),
[m[31m-        (
[m             "tuple input axis keepdims",
             "result = MODULE.var(((1, 2, 3), (4, 5, 6)), axis=1, keepdims=True)",
         ),
Diff in /data/projects/franken_numpy/crates/fnp-python/tests/conformance_var.rs:166:
             "out = np.empty((2,), dtype=np.float64)
 result = MODULE.var(np.array([[1.0, 2.0], [3.0, 4.0]]), axis=0, out=out)",
         ),
[31m-        (
[m[31m-            "axis error type",
[m[31m-            "result = MODULE.var([1, 2, 3], axis=2)",
[m[31m-        ),
[m[32m+        ("axis error type", "result = MODULE.var([1, 2, 3], axis=2)"),
[m     ];

     for (name, body) in cases {
