[package]
name = "fnp-python"
version.workspace = true
edition.workspace = true
license-file.workspace = true
repository.workspace = true
homepage.workspace = true
readme.workspace = true
keywords.workspace = true
categories.workspace = true
description = "PyO3 module exposing the full numpy.__all__ surface for drop-in fnp_python use."

[lib]
name = "fnp_python"
crate-type = ["cdylib", "rlib"]

[features]
default = []
python-extension = ["pyo3/extension-module"]

[dependencies]
# pyo3: PyO3 binding macros + Python interpreter integration. Version pinned at
# workspace level (0.28.3, auto-initialize). The `extension-module` feature flag
# (gated by this crate's `python-extension` feature) switches to cdylib mode.
pyo3 = { workspace = true }
# fnp-* path deps: this crate is the PyO3 surface for the entire workspace.
# Every fnp_python.<name> Python binding routes to one of these crates:
# - fnp-dtype: numpy dtype objects + scalar types
# - fnp-iter: nditer / flatiter / ndindex Python wrappers
# - fnp-ndarray: shape/stride/broadcast utility bindings
# - fnp-ufunc: the bulk of numpy.<func> bindings (350+ wrappers)
# - fnp-linalg: numpy.linalg.* bindings (solve, svd, eig, ...)
# - fnp-random: numpy.random.* bindings (Generator, BitGenerators, distributions)
# - fnp-io: numpy.load / numpy.save / numpy.savez / loadtxt / savetxt bindings
fnp-dtype = { path = "../fnp-dtype" }
fnp-iter = { path = "../fnp-iter" }
fnp-ndarray = { path = "../fnp-ndarray" }
fnp-ufunc = { path = "../fnp-ufunc" }
fnp-linalg = { path = "../fnp-linalg" }
fnp-random = { path = "../fnp-random" }
fnp-io = { path = "../fnp-io" }
rayon.workspace = true

[dev-dependencies]
criterion.workspace = true
# sha2: the bench binary hashes its OWN `env::current_exe()` and prints that
# digest as line one of every run (see benches/common/mod.rs `self_identity`).
# A hash computed by a shell step next to the run proves nothing about which ELF
# actually executed: rch compiles into an opaque per-worker pool target dir that
# the caller cannot predict, and concurrent agents have edited crates mid-run.
sha2 = "0.11.0"

[[bench]]
name = "criterion_python_surface"
harness = false

[[bench]]
name = "criterion_python_extract"
harness = false

[[bench]]
name = "criterion_python_unique_medium"
harness = false

[[bench]]
name = "criterion_python_sort_complex"
harness = false

[[bench]]
name = "criterion_python_structural"
harness = false

[[bench]]
name = "criterion_python_f16_transcendental"
harness = false

[[bench]]
name = "criterion_python_io_text"
harness = false

[[bench]]
name = "criterion_python_string"
harness = false

[[bench]]
name = "criterion_python_unique_rows"
harness = false

[[bench]]
name = "criterion_python_argsort"
harness = false

[[bench]]
name = "criterion_python_linalg"
harness = false

[[bench]]
name = "criterion_python_complex"
harness = false

[[bench]]
name = "criterion_python_median_gate"
harness = false

[[bench]]
name = "criterion_python_setops"
harness = false

[[bench]]
name = "criterion_python_struct"
harness = false

[[bench]]
name = "criterion_python_sortmisc"
harness = false

[[bench]]
name = "criterion_python_misc1"
harness = false

[[bench]]
name = "criterion_python_misc2"
harness = false

[[bench]]
name = "criterion_python_unaryred"
harness = false

[[bench]]
name = "criterion_python_elementwise"
harness = false

[[bench]]
name = "criterion_python_reductions"
harness = false

[[bench]]
name = "criterion_python_gradnorm"
harness = false
